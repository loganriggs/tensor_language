# Status & return-ranking — end of 2026-08-10

Mode (Logan): one stream at a time from here, memorization post first; ranked list below is
the order to return to things afterwards.

## Done today

**Tier 1 aggregate (all three, §35-37 in qk_mdl/RESULTS_l0_mdl.md):** scalar-mass collapse mostly
replicates (geometry's sliver real only at tight budgets); realised-interface ledger — the
weight-space null-tie was gauge pollution, 32/36 channels beat the corrected null, 21% nameable;
static-fraction port profile 98%→negative by layer 17.

**Tier 2 #4 planted-modular DGP (§38-38c, full cycle: build → run → independent review → fix round):**
- The pipeline is NOT certified on known truth; registered rule fired — bilin18's "content is
  alien / near-zero codability" verdicts DOWNGRADED to tool-limited.
- F0: even noiseless modular truth need not be the CP optimum of its own third moment.
- The naming gate is STRUCTURALLY broken (oracle truth certifies 6/24; 24 units through a
  16-dim per-head write space must leak; the coder itself is fine).
- Recovery is seed-stably ~45% vs an analytic ceiling of 100% (estimation noise).
- SGD's free embedding is MORE recoverable AND ~2.2x more angular-noise-robust than the planted
  orthonormal table (both real, survived the artifact hunt).
- Equal-ablation's vs-PCA channel CAN detect planted selection (privilege >2SE at all doses) —
  bilin18's negative there now carries evidence.

**Tier 2 #5 archetype extraction (§39; independent review IN FLIGHT, last active agent):**
- Oracle-assembly gate at ±4e-6 nats; frozen-embedding arm reassembles at +0.017 nats
  (extraction-to-machine demonstrated); free-embedding arm fails (+0.505) via lost key spans.
- Overlap machines FUNCTIONAL at +0.28-0.30 with ZERO individuated units — assembly works on
  spans; "recovered the mechanism" and "recovered the parts" are separable claims.
- Shared directions are most of the machine but no consistent per-energy privilege (3x bar failed).
- Caution: assembly CE is not monotone in component quality (oracle parts through the recipe
  score worse than recovered parts).

**Memorization post (Parts 1-2 COMPLETE; consolidated figures artifact
https://claude.ai/code/artifact/cfdfcbbc-5604-46b4-8611-e52ec37ea501):**
- C1 centerpiece: Gram-predicted collateral r=0.943/rho=0.931 (registered pre-measurement).
- 1b confirmed: positive pair + negative absent-feature diagonal, every seed/decay.
- KKT unlearning exact (r=1.000000 collateral prediction; naive baseline fails IN KIND).
- F12: 2^20 enumeration — bound holds everywhere, 35-55x loose; only the bound is the guarantee.
- F9b (Logan's correction): D→±I structure real once dead units excluded (live-unit dominance
  0.78-0.95, ~one live unit per class); the SIGN is gauge in our parameterization.
- Blind rank-1 fact extraction fails completely; facts are a joint code.

**Tiny models:** width-transition sweep FINISHED (X1-X4 partially scored earlier: predicate-slots
decisive at w32, slots transition between w64-128; X5/vanilla-w192 ladders trained, UNSCORED);
fix-round chain (seeds 3-5 + co-tenancy control, S1-S3) stopped pre-training, resumable by
rerunning tf_v4096_seeds35_chain.sh.

## Ranked return list (after memorization Parts 3-4)

1. **Tier 2 #6: planted-tree topology recovery** — completes the calibration batch, gates #8,
   and is the instrument aimed next at bilin18. Logan's standing warning: solver calibration
   FIRST (only 1 of 5 CP fitters passed; trees will be worse). Predictions before build.
2. **Close the tiny-models loose ends** — score X5 + consolidated V2 write-up from the
   already-trained ladders (zero GPU cost), then resume the stopped fix-round chain (S1
   co-tenancy verdict decides whether concurrent training stays). Cheap, keeps FINDING 21 clean.
3. **Phase-2 tool improvements** (Logan-authorized, generalizability-filtered), now with sharp
   measured targets: (a) naming-gate spectrum construction past the per-head bottleneck,
   (b) estimation-noise-robust CP fitting, (c) then RE-RUN on bilin18 — this converts the
   downgrade into new bilin18 verdicts and is the highest-leverage interp work on the board.
4. **§39 review fix round** — whatever the in-flight reviewer sustains (permuted-class-map
   control is the likely must-run).
5. **Tier 3: windowed-unigram moment model (#9) and weights-only unigram recovery (#7).**
6. **Width-1152 confirmation** — blocked until a rented box returns.

Standing item needing Logan: accept/reject "priced substitution + named selection + exact
spectra as content description" as the success criterion for "fully interpreting."
