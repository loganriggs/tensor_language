IN FLIGHT (2026-08-08, chain qk_e34_chain.sh, results -> qk_e34.json)
  qk_e34_ablate_run.py -- what the named predicate terms are actually worth.
  Four trainings, one seed each, ~5 h; all controls pass before training.
    E34a  the identical named terms on the VANILLA w264 model (no slots, no
          per-slot norm, no lasso, Muon). Registered: the gain is SMALLER
          than the constrained arm's -0.086 (interval (-0.05, 0.00], point
          -0.02) => the named terms are an interpretability-TAX REDUCER, not
          a general modelling win. -0.05 or better flips that.
    E34b  profile only            (drops MATCH_prev and MATCH_same)
    E34c  profile + MATCH_prev    (drops MATCH_same)
    E34d  MATCH_prev + MATCH_same (drops the positional profile)
  With E22a and E19a on disk this reads off all four marginal costs.
  Registered: the profile carries most of the CE gain; MATCH_same costs
  < 0.01 nats; dropping MATCH_prev costs INDUCTION massively even if CE
  barely moves -- so induction advantage (qk_e28 repeated-prefix probe) is
  measured for EVERY arm, not just CE.

NEXT (do not pre-commit; decided by E34's arm-1 verdict)
  - If arm 1 shows the named terms help the UNCONSTRAINED model too, the
    scale box should add them to the w1152 vanilla baseline, not only to the
    recipe arms -- that is a general-architecture claim and needs the width
    check before it is believed (every structural claim does).
  - If arm 1 shows the tax-reducer pattern, the honest headline is that the
    predicate library buys back interpretability tax, and the remaining work
    is on the CONTENT side (codebooks/catalogs), not more named terms: the
    library is at its naming ceiling (BRAINSTORM_STATE, E32).
  - Standing: predicate-library growth is CLOSED as a line. Do not queue
    another named term without a new argument.
