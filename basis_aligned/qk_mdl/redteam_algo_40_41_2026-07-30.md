# Adversarial review of §40 (greater-of-two) + §41 (bracket) — 2026-07-30

Both defensible with rewording; corrections applied.

## §40 greater-of-two — 3 real flaws fixed
- **std-0.0 is TAUTOLOGICAL, not a control**: final query token "->" is identical across all 72 prompts,
  so all-attention ablation forces byte-identical final logits by construction. Retracted "decisive
  control / reads NONE of the pair"; kept the real evidence acc_static 0.944 = task prior-saturated (95%
  accuracy / 82% margin).
- **"carried entirely by attention" CONTRADICTED**: all-feed-forward ablation removes 0.384 ≈ all-attn
  0.387. Corrected to "attention AND feed-forward jointly in series".
- **layer-8 H7/H3 attribution overstated** (H7=25.4% not "<25%"; mlp.L7 outranks H3; within-noise single
  seed). Reframed to "mild concentration in layer-8 attention, no separable single-head attribution".
- **NEW untested hypothesis**: static profile peaks at digits 7,8 = the few-shot demo answers -> possible
  in-context copying, not magnitude-unembedding. Flagged; demo-answer-swap control to be queued.

## §41 bracket — the stronger finding; softened 3 overclaims
- "bidirectional swaps PROVE it" -> forward decisive (100%->0%), reverse only PARTIAL (0%->45%); payload
  necessary and largely-but-not-fully sufficient. Kept (harness validated by identity control).
- "genuine attention circuit" carried by '[' ; '(' -> ')' is prior-confounded like §40. Stated explicitly.
- generalization caveat added (single template, n=20/opener, one seed).
- CREDIT: L13H8 dominance (24.5%) + value-payload swap causally clean & internally validated.

Both corrected in RESULTS. Loop discipline working: over-claims caught before enshrinement.
