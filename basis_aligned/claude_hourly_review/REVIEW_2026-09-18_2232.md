# Hourly review — 2026-09-18 22:32 UTC (review 30; previous 21:32; cron-fired)

## A. Higher-level view
1. **Goal.** Simple / predicts OOD / extracted / selective / composes, each with a gate and a null (better_circuits §1); depth per path,
   lane runs by default (Logan, ~13:50); every review ends with QUEUED or STOP (hard rule since review 28).
2. **Changed since 21:32.** Worked 21:32–21:41: six receipts (v278–v283, 126 forwards, 18 of 25 registered readings held) and six commits.
   The temporal family's detectors are one block below its MLP-8 port: MLP 7 at the bank is concentrated (unit 1250 23%, top-10 73%),
   1250 is a since-context detector (32/32), the MLP-7 trio removes 2.2% under edit (10× random), it is a cue copy carried by attention
   (head 7.8 32%, embedding 0), and it does not transfer to natural text (0.09%: the bank is a panel construct) — declared. Then STOP by
   decision; 51 idle minutes under the STOP line.
3. **Scorecard — pronoun-number component.** Unchanged. Collection-level: five of seven families now have named MLP units (number,
   gender, determiner, correlative at the final, temporal at MLP 7); two remain ports at MLP 8 without a look one block lower (person,
   selection).
4. **Local-direction checks.** (a) On the goal: yes. (b) Falsifiable: 7 of 25 false, kept. (c) No fits. (d) Depth: the "look one block
   lower" lesson from the temporal family has not been applied to the two remaining ports — a 3-forward check each. (e) Serving Logan: the
   synthesis table carries the temporal correction; it would carry a false "port" for person and selection if MLP 7 is concentrated there too.
5. **Kill criteria.** If MLP 7 is spread (top-10 < 0.30) for both person and selection, the two families are declared ports at MLP 7 and 8
   and the lane STOPs for the night with that written.
6. **Decision: CONTINUE with two 3-forward censuses — MLP 7 on 10.5's direction (person, pronoun and final) and on 13.8's (selection,
   adjective and final).** A concentrated result gets one edit tomorrow; a spread one closes the table. Then STOP.

## B. Efficiency
1. **Time buckets (21:32–22:32).** Review + helper: 3 min; six receipts run/record: 6 min; idle by STOP: 51 min.
2. **GPU.** 126 forwards for six receipts; decision-changing: v278 (2), v281 (40), v283 (40). Census receipts (2 forwards) again did the deciding.
3. **Repeated work.** Deriving a runner by regex on whole strings failed six times today ("MISSING" asserts, stale names); each cost a
   minute or an idle loop. Replacements should target assignments by NAME, not by exact text.
4. **ONE improvement (executed now).** `ops/dod_derive.py <src> <dst> --set NAME=<python literal> ... --docstring <file>`: AST-aware
   derivation — rewrites the module-level assignment of each named constant (whatever its current text), swaps the docstring, and renames
   the receipt / candidate-id stems from the file names; asserts every requested name existed. Used for v284 / v285.
