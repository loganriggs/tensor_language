# Hourly review — 2026-09-19 11:24 UTC (review 43; previous 10:40)

## A. Higher-level view
1. **Goal.** Simple / predicts OOD / extracted / selective / composes, each with a gate and a null (better_circuits §1); depth per path; lane runs by default;
   every review ends `QUEUED:`.
2. **Changed since 10:40.** Receipts v542–v564 (22 GPU runners + the v544 CPU miner; 182 forwards; 110 predictions, 27 failed and kept; one stale-MODES run
   lost, v543), scorecard rows T255–T276, a board entry each, §4.11 of `for_logan/in_depth_circuit_number.md` extended (adjacent-row stages, third set),
   `LATEST.md` 10:50 section, and — written this review — §4.12 (the gender line) and a LATEST 11:25 section. Threads: (i) the adjacent-row MLP stages and MLP-8
   units (T255–T256) closed the noun-as-answer map; (ii) a THIRD natural set mined outcome-blind (v544) and both reader sets replayed on it at head and unit
   grain (T257–T260, 20/20 predictions) — the number circuit is done on three sets; (iii) the pronoun-GENDER line opened with the number method (T261–T276):
   values at the noun 0.90, five readers 0.60 (10.1 / 12.4 / 9.6-led), self-copies 8.1 + 6.1 0.31, MLP stack ~0.17, detectors 3152 / 3943 0.063 (random 0),
   token-only value branch 0.31 (half the read; number ≤ 0.06), readout linear in the readers (α × 0.600 at four α), writes into the answer 0.88 (12.4-led).
3. **Scorecard — number circuit:** all five held on three natural sets (T252–T260); written up. **Gender line (new target):** *Simple:* seven heads by edit
   (five readers + two self-copies), two MLP-8 detectors, one token-only branch — held. *Predicts OOD:* natural fineweb rows only so far — v565 (pile rows)
   queued. *Extracted:* the two detectors are 0.81 of MLP 8's part, but MLP 8 is 0.08 of the gap — the extracted objects are the token-only branch (weights:
   block-0 value map × readers' OV maps × signed pattern) and the self-copies; partly held. *Selective:* untested (does the gender swap move number / person?).
   *Composes:* readers read the noun AND the positions after it (writes 0.88 > values 0.60) — the relay is unnamed. Cells moved: simple, extracted (partly).
4. **Local-direction checks.** (a) New behaviour opened only after the number target met the definition of done (§7 satisfied; recorded 10:55). (b) Replay
   ≤ 2e-5 throughout; no time on replay. (c) No fits; the weights-only token term is a fold, not a fit. (d) Highest information: the fold-vs-edit gap chase
   (v552→v558) cost 60 forwards and ended in one crisp fact (half of the gender read is token-only); the direct-term instrument (v560–v562, 6 forwards) failed
   three times on magnitude and was refuted by v563 — kept, not softened. (e) No Codex collision.
5. **Kill criteria.** The gender account dies if the five readers' writes fail to close ≥ 0.50 on the pile rows (v565) or if the gender swap moves the number
   margin by ≥ 0.10 (selectivity, next). The direct-term instrument is dead as a magnitude tool until it is rebuilt against v563's edit (not scheduled).
6. **Decision: continue** the gender line — v565 (OOD), then selectivity (gender swap on the they − he margin), then the relay (which positions after the noun
   the readers read gender from — the token after the noun, as for number's copier?). Then write the gender circuit up as a second entry in the collection.

## B. Efficiency
1. **Time buckets (10:40–11:24, 44 min).** Scripts ~18 min (all regex-derived; two-minute turnarounds), waiting ~6 min (8–16-forward edits finish in 20–40 s),
   analysis and recording ~14 min, docs ~4 min, this review ~4 min. Science (ii+iv) 32 of 44.
2. **GPU.** 182 forwards for 22 receipts (8 per runner); ~10 lost (v543's stale MODES). Decision-changing: v542 (MLP 8 alone 0.30), v545–v548 (OOD held),
   v549–v551 (gender head set), v558 (token-only half), v563 (writes 0.88). Low value: v560–v562 (instrument bugs: sign, rms, chain).
3. **Repeated work.** The same run() loop (manual attention with value/write swaps) now lives in ~12 gender runners; the gender_pairs builder was parametrised
   once (rows_path). The direct-term instrument re-derived three constants (pattern sign, rms, λ chain) that the v406 fold already carried — reuse the fold.
4. **ONE improvement (executed).** Conventions memory: the grep list for regex-derived runners gained `MODES = {"` (v543) and the direct-logit-term rule
   (sign × chain / rms, and then still check it against an edit before trusting the magnitude).
QUEUED: /workspace/tensor_language/basis_aligned/bilinear_quotient/ops/run_gender_five_writes_into_answer_pile_v565.py
