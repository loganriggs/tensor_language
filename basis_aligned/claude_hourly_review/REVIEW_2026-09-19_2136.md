# Hourly review — 2026-09-19 21:36 UTC (review 45; previous 12:06. The cron did not fire between 12:22 and 21:36; this review is written now, at the box clock, and covers only the work actually done in the 12:06–12:22 span — no hours are backfilled for the quiet gap.)

## A. Higher-level view
1. **Goal.** A circuit is done only when simple / predicts OOD / extracted / selective / composes, each against a preregistered gate and a null (better_circuits §1).
   Lane runs by default; every review ends `QUEUED:`.
2. **Changed since 12:06 (receipts v591–v597, board rows T302–T308).** PERSON was carried to a verdict and CLOSED as a spread readout: the seven heads
   {8.1, 13.1, 10.5, 15.1, 9.6, 9.7, 5.3} write 0.549 of the myself − yourself channel jointly (bar for a named-set circuit was 0.60), OOD held on pile
   (four readers 0.375, all 0.917), composition 0.563 from two sources — recorded in §4.13 of `for_logan/in_depth_circuit_number.md`. The value-branch edit was
   run on all three lines by one instrument (number CUR/TOK 0.411/0.075, verb 0.347/0.027, gender 0.291/0.314): number and agreement are computed, gender is
   half read off the token. Two new lines were probed by the same in-place-swap method: ASPECT (since↔by) is NOT aligned (gap 0.48 logits, sign 0.53) and was
   parked; TEMPORAL (tomorrow↔earlier) IS aligned (sign 0.935) and opened — values at the cue 0.789, four readers 0.254, low blocks 0.664.
3. **Scorecard — the driven circuit is now temporal will/had (opening).** *Simple:* four named readers carry only 0.25; likely spread (untested). *Predicts OOD:*
   two corpus panels already in the rows; a second-set edit not yet run. *Extracted / selective / composes:* untested. Cells moved: none held yet — the line is
   one census old. The finished entries behind it: number (all five, three sets) and gender (all five at head grain, three sets).
4. **Local-direction checks.** (a) Temporal opened only after person reached a recorded verdict; not a premature new behaviour. (b) Replay ≤ 2e-5 on every edit.
   (c) No fits. (d) Highest-information: the aspect probe (10 forwards) returned a negative that saved a whole mis-specified line — recorded as a method lesson.
   (e) No Codex collision (temporal readouts are the Claude lane's own). **Miss:** v595 lost 12 forwards to a stale write-swap guard copied from the gender lineage.
5. **Kill criteria.** Temporal dies as a named-set circuit if no ≤ 8-head set writes ≥ 0.60 into the answer (as person did); it is then recorded as spread with the
   block census as its depth limit. It dies as a *line* only if a second natural set fails the sign-constancy check (0.935 on set one — unlikely).
6. **Decision: continue** temporal — v598 (readers singly + low-block groups) is queued; then the write side and a named-set test against 0.60, then OOD.

## B. Efficiency
1. **Time buckets (12:06–12:22, 16 min of actual work; the 12:22–21:36 gap was idle, not worked).** Scripts ~7 min (regex-derived), waiting ~3 min (8–20-forward
   edits finish in under a minute), analysis/recording ~5 min, this review ~1 min. Science (ii+iv) ~12 of 16.
2. **GPU.** ~120 forwards for v591–v597; decision-changing: v593 (person verdict), v594–v595 (branch comparison across three lines), v596 (aspect negative),
   v597 (temporal aligned). Low value: v595's re-run (12 forwards, stale guard).
3. **Repeated work.** Four near-identical pair builders now exist (number/gender/person/temporal); the `RULES`/branch-swap `run()` body is copied across ~15 runners.
   A single `swap_pairs(rows, partner_map)` and a shared value-branch/pattern-swap kernel would remove both — the biggest standing duplication in the lane.
4. **ONE improvement (executed).** Conventions memory now carries the in-place-swap rule: both cue forms must fill the same slot with the same syntax, and the
   native gap's sign constancy (≥ 0.75) must be checked before spending edits — the rule that turned the aspect misfire into a 10-forward negative, not a lost hour.
QUEUED: /workspace/tensor_language/basis_aligned/bilinear_quotient/ops/run_temporal_heads_and_low_groups_v598.py
