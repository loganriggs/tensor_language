# Hourly review — 2026-09-18 03:43 UTC (review 11; previous full review 03:12, delta 03:27; written in-turn)

## A. Higher-level view
1. **Goal.** Simple / predicts OOD / extracted / selective / composes, each with a gate and a null (better_circuits §1); depth per
   path, a better collection across paths.
2. **Changed since 03:27.** Correlative family closed: v125–v128 natural (24/24; either/or and neither/nor carried, not/but and
   both/and weak), v129/v130 folds (all four heads token readers, 10/10), v131/v132 token-only generators (4/5 ×2, CV bar failed).
   Cross-family: v133 8.1 is one token lookup across temporal / person / correlative / gender (signs right, beats random pairs;
   contrast-specificity failed at 0.27–0.37); v134–v136 shared heads carry families along orthogonal directions (0.00 cross-family
   removal at three sets). Gender token readers v137 (78% / 79%, both bars failed). Number: v138/v139 natural were/was rows live
   but SELECTIVITY FAILED on the tense reader on both corpora; v140 the leak is mostly 11.3 (61%; ≥ 70% failed; the remainder not
   live); v141 orthogonalizing against tense does not remove it (4/5). Correlative report; collection index at six families; registry
   rebuilt; `dod_line.py --only-natural`, screened pools, `dod_folds.py`. Commits through this one. Failed predictions kept: v131/v132 c;
   v133 c; v137 b, e; v138/v139 e, f; v140 b, d; v141 c.
3. **Scorecards.** Six families, thirteen lines closed at head grain. New scope limit on the number family: on natural text its
   removal moves tense, and neither dropping 11.3 nor orthogonalizing its direction gives a live, selective number component —
   recorded on `NUMBER_DOD_SCORECARD.md` as a failure, not softened.
4. **Local-direction checks.** (a) no new behaviour this half hour; the number thread is a scope question on a closed line;
   (b) no replay polishing; (c) no fits; (d) highest-information next step: WHERE the tense change is produced under number removal
   on natural rows (a census on the has−had reader, 4 forwards) — if it is MLP-borne, the number→tense dependency is a suffix
   feature, and the family's natural-text selectivity is a real limit, not a direction artifact; (e) no Codex collision.
5. **Kill criteria.** For the number natural-selectivity thread: if the tense change under number removal is direct (heads' own
   writes ≥ 0.7), the readout directions are the cause and a per-head orthogonalization must be revisited; if MLP-borne, stop the
   thread and declare the limit. Either result ends the thread.
6. **Decision: continue** the number thread for exactly one run (v142 census on the tense reader), then declare; afterwards the
   collection's remaining gaps are the temporal family's narrative/modal natural rows (cues spread over a sentence; a miner would
   be weak) — likely stop there and write the end-of-night synthesis for Logan.

## B. Efficiency
1. **Time (03:27–03:43).** ~4 min waiting, ~8 min writing, ~3 min analysis, ~1 min ceremony.
2. **GPU.** ≈ 500 forwards (v125–v141), all decision-changing; 11 receipts.
3. **Repeated work.** None new; dod_line.py produced v121–v128 from specs. The number miner config was copied from the pronoun-number
   one (a `labels` argument would have made it a one-liner).
4. **Improvement executed:** `dod_line.py --only-natural`. **Scheduled:** the end-of-night synthesis for Logan (one page: six families,
   what closed to tokens, what stays a port, the three falsified regularities), from `READOUT_COLLECTION.json`.
