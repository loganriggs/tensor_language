# Hourly review — 2026-09-17 23:07 UTC (review 3; previous 22:32; written in-turn, not cron-fired)

## A. Higher-level view
1. **Goal.** A circuit is simple, OOD-predictive, extracted, selective and composing, each against a
   preregistered gate and a null (better_circuits §1); depth on one path until done.
2. **Changed since 22:32.** 15 receipts (v13–v27), 33 commits pushed. Bank writer fold (v13), attention8
   bank fold (v14), 8.1 token-only at four positions (v15), MLP8 pair fold (v16, diffuse), strong-form OOD
   on new templates (v17), relay mediation edit (v18), frozen 0.54±0.15 prediction on a third lexicon +
   new construction (v19), natural FineWeb rows (v20/v21; labels later corrected: in-distribution), cue
   key folds (v22/v23, diffuse), lib refactor bit-exact (v24), random head-set null (v25), Pile OOD panel
   (v26, 5/5), cross-task reuse on will/had (v27). Failed registered predictions preserved in v13, v16,
   v20, v21, v22, v23, v27. For-Logan update written; registry cross-link added.
3. **Scorecard (aspectual readout component {8.1, 9.1, 9.4}).** Simple: held (blind sweep + random
   triple null). Predicts OOD: held (authored panels, frozen numeric prediction, FineWeb and Pile rows;
   strong token-only form for 8.1's value branch). Extracted: held at the head boundary; 8.1 closed;
   two declared open ports (MLP5–8 bank polynomial; contextual cue key) — both folds tripped the kill
   criterion. Selective: held. Composes: held; reuse shown (block-9 relay + 8.1 recipe serve will/had).
   No property remains "untested"; the remaining gaps are the two ports, and they do not close by
   folding at head/MLP grain.
4. **Local-direction checks.** (a) v27 touched the temporal line's rows but as a reuse receipt for this
   component, not a new discovery thread; (b) no replay-error polishing; (c) no fitted proxies (v12/v15
   constants are medians, declared); (d) the aspectual path has reached the rubric's limit at this grain —
   continuing to fold would be the "improving the same examples" failure; (e) no collision with Codex.
   One honest error this hour: I labelled FineWeb rows "fresh corpus"; corrected on the board/scorecard.
5. **Kill criteria.** Tripped twice as registered (v16, v23) → folding on this path stopped. The
   component is done-at-grain with declared ports; better_circuits §7 permits the next path.
6. **Decision: redirect to the next circuit** with the same battery. Choice: the temporal will/had line —
   v27 already shows its readout is led by 11.3 (1.48), 9.1 (1.05), 15.5, 9.4, sharing the block-9 relay
   with the aspectual component, and it is the portfolio's Tier-4 candidate. Start: v28 = weight-only
   removal of the sweep-led set on FRESH temporal rows with the norm-matched null and three readers.

## B. Efficiency
1. **Time (box clock).** 22:32–23:07 continuous; 15 receipts in 35 min (≈2.3 min per run including
   board/scorecard/commit). Ceremony per run ≈ 40 s after `dod_record.sh`. No idle this hour (the
   28-idle-minute problem of hour 2 is fixed by writing reviews in-turn).
2. **GPU.** 954 forwards (736 of them the will/had head sweep); every receipt changed a scorecard cell
   or closed a port question. Model reloads: 15 × ≈40 s ≈ 10 min of runner time — the dominant GPU-side
   cost; a resident-model runner would remove it but the runner is runner-owned.
3. **Repeated work.** Removed: the three attention recomputations (v24 refactor, bit-exact). Remaining:
   each runner re-derives readout directions and re-runs native + producer replay (4–8 forwards each).
4. **Improvement executed:** `attention_factors` refactor + equivalence receipt (v24); `dod_record.sh`
   (v18); ops README index + registry cross-link. Scheduled: none new.
