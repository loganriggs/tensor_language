# Hourly review — 2026-09-18 00:50 UTC (review 5; previous 00:00; written in-turn at a work boundary)

## A. Higher-level view
1. **Goal.** A circuit is simple, OOD-predictive, extracted, selective and composing, each with a gate and a null
   (better_circuits §1); depth on one path, then the next; the deliverable is a better collection.
2. **Changed since 00:00.** 21 receipts (v48–v67), 5,067 forwards, all committed/pushed. The MLP relay ports
   stayed declared (v48: 32% of the MLP part is 8.1's response, below the 40% kill bar). Reuse censuses on
   six more lines (v49b–v54) split the auxiliary decisions into two readout families: temporal {9.1, 9.4, 15.5}+11.3
   (aspectual, temporal, narrative, modal) and number {5.7, 7.8, 9.7}+11.3 (perfect, lexical, quantifier,
   coordination); a non-auxiliary contrast (on/of) uses neither. Number family battery (v55–v65): 70% on fresh rows,
   additive, Simple held; the raw readout direction is entangled with has−had at the number heads (weight-level
   cosines −0.4..−0.6, v56) and a weight-only orthogonalized direction restores selectivity (v57); 11.3 and 7.8 are
   one-directional, 5.7 is not (v59); scope boundary: adjacent-subject number is MLP-borne (v63–v65). Modal line
   (v66–v67): 7/7 and 6/6 on fresh rows — fifth line done at head grain. Report for Logan: readout families.
   Failed registered predictions preserved in v48, v50b–v53, v55, v58, v58b, v59, v61, v62, v63, v64, v65.
3. **Scorecards.** Aspectual, temporal, narrative, modal: all five properties held at head grain (declared ports
   on the first two; strict four-way additivity fails by 3–4% on the four-head sets). Number: held within a
   declared scope (non-adjacent subject), Extracted for the pair {11.3, 7.8}.
4. **Local-direction checks.** (a) New lines were opened only after the previous reached its declared limit, and
   the census makes each further line cheap (one script, ~1 min); (b) no replay polishing; (c) no fits — the
   orthogonalized direction and constants are weight objects / declared medians; (d) the next step is the
   collection itself: the census over every remaining screened behaviour (the "readout atlas"), which is the
   user's ask; (e) no collision with Codex (typed-face/regional). Errors this hour: three price mis-counts
   (v49/v50), one protocol mismatch (v58), one two-token place noun with a wait loop that ignored the refusal
   (10 min), one verdict swap on the board (corrected).
5. **Kill criteria.** For the atlas: a behaviour whose top-4 readout set is not live or not null-beating is
   recorded as "no compact head readout" — that is a result, not a failure of the method; no rescue by widening.
6. **Decision: continue — build the readout atlas** over the remaining ~40 screened behaviours with
   `dod_reuse_census`, then run `dod_battery` on any new family that appears.

## B. Efficiency
1. **Time (box clock).** 00:00–00:50 continuous; 21 receipts (≈2.4 min/receipt incl. recording). Waste: the
   v62 refusal loop (10 min), v49/v50 price re-runs (3 min), lexicon fiddling (3 min).
2. **GPU.** 5,067 forwards (5 × 724-forward sweeps dominate); every receipt changed a cell or closed a branch.
3. **Repeated work.** Four near-identical battery runners existed (v28, v43, v55, v66) → replaced by
   `dod_battery.py` (dry-run tested). The enqueue+wait pattern is now guarded (`&&` on QUEUED).
4. **Improvements executed:** `dod_reuse_census.py` (per-batch price), `dod_battery.py`, README index.
   **Scheduled:** a `lexicon.py` registry with the freshness check (the corpus lists + every panel used).
