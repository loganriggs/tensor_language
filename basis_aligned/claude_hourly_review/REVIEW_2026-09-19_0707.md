# Hourly review — 2026-09-19 07:07 UTC (review 40; previous 06:21)

## A. Higher-level view
1. **Goal.** Simple / predicts OOD / extracted / selective / composes, each with a gate and a null (better_circuits §1); depth per path; lane runs by
   default, no STOP; every review ends `QUEUED:`.
2. **Changed since 06:21.** Receipts v399–v418 (20 runners, 129 forwards), one board entry each, scorecard rows T113–T132, LATEST.md section (06:50) and
   the subspace + both-ends report sent to Logan (07:09). Logan's two requests are executed: the subspace fold (v390–v400, closed) and his first-pick
   family, both-ends contrast + degree expansion (v401–v418, with its edit). v419 enqueued 07:07.
3. **Scorecard — the number circuit's readout, now at the mechanism grain.** *Simple:* the readers 9.6 / 12.4 / 15.1 and the copier 4.5 COPY number
   through the value branch of the current state; their squared-attention patterns are number-blind (≤ 6% change; exact 8-term expansion, closure 1e-14;
   v401–v410). *Predicts OOD:* replays on 122 natural aligned pairs made by swapping the cue noun's number in place (v405, v410); the value swap closes
   0.37 / 0.50 (one / two sites) of the natural-text margin gap, keys 0.00 (v416–v417); the copier's value alone 0.19 text / 0.30 panel (v418).
   *Extracted:* writers of the copied number at the noun — MLP 8 half, MLPs 5–7 a quarter, embedding a tenth, attention nothing (v406); units 829 / 953 /
   1030 first on text, 3152 fifth on both frames (v407–v408). *Selective:* the class axes — the circuit writes "plural" and "not he/she", leaves case
   alone (v400); the gain (pattern weight) is number-blind but row-varying, set by noun-side context (12.4, 15.1) or a content match of both ends
   (9.6), not by token identity or rotary (v411–v415; 15.1's gain is recency). *Composes:* v419 (queued) tests whether the copier's seed reaches the
   answer through the readers' post-noun read (overlap) or adds to it. Cells moved: simple + predicts OOD + extracted at the readout, by intervention.
4. **Local-direction checks.** (a) Logan's two explicit requests, on the declared target. (b) Replay errors ≤ 1e-4 throughout; v416–v418 replay 1e-5.
   (c) No fits (SVDs of W_U slices, exact algebraic splits, two-way mean decompositions). (d) 20 receipts, 129 forwards, 11 falsified predictions kept
   (v401 c, v403 b, v406 e, v407 a/e, v411 b/c/e, v413 b/e, v414 b/c/e, v415 c, v417 b). (e) No Codex collision (UK/US left alone). **Misses:** v407's
   bias double count (corrected v408); v411 crashed twice before running (mixed-length batch, a comment that swallowed `idx`); v414's 1e-6 closure bar was
   below float32 resolution for a 244² sum.
5. **Kill criteria.** The value-copy account dies if a key-only swap ever moves the margin by ≥ 0.05 of the gap (it moved 0.00–0.01 in four edits) or if
   the value swap fails to replay on a third natural set. The gain thread dies if v419 shows the copier's effect is additive to the readers' two-site read
   (then the seed reaches the answer by another route and the readers are not the whole readout).
6. **Decision: continue** — the both-ends series has converted the readout from a fold claim to an edited mechanism on natural text; v419 closes its
   composition question. Then: the template-contraction library on the readers' OV maps (Logan's second direction), now that the readers are known to be
   value copiers (the natural place for a shift/contrast template).

## B. Efficiency
1. **Time buckets (06:21–07:07, 46 min).** Scripts ~20 min (v400–v419 mostly derived by regex from v401 / v405 / v416), waiting ~10 min, analysis and
   recording ~12 min, LATEST.md + report to Logan ~4 min.
2. **GPU.** 129 forwards for 20 receipts (~6 per receipt); the three edits cost 21 each (3 passes × 7 batches) and were the decision-changing numbers.
3. **Repeated work.** Every degree-expansion runner recaptured q / k / v at the readers (4 text + 3 panel batches); one cached factor table for the 244
   natural sequences and the 96 panel rows would have saved ~40 forwards across v411–v415. The `natural_pairs()` helper (v406) is already reused by four
   runners — the right pattern.
4. **ONE improvement (executed).** Conventions memory updated (`tensor-language-ops-conventions.md`): natural aligned pairs via the in-place tokenizer
   swap; the exact 8-term degree expansion recipe; and (new this review) closure bars on float32 sums over ≥ 10⁴ terms are set at 1e-3, not 1e-6.
QUEUED: /workspace/tensor_language/basis_aligned/bilinear_quotient/ops/run_value_swap_compose_v419.py
