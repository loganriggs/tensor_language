# Hourly review — 2026-09-19 05:31 UTC (review 38; previous 04:39; written ahead of the 05:39 cron)

## A. Higher-level view
1. **Goal.** Simple / predicts OOD / extracted / selective / composes, each with a gate and a null (better_circuits §1); depth per path; lane runs by
   default, no STOP; every review ends `QUEUED:`.
2. **Changed since 04:39.** Receipts v354–v378 (25 runners, 393 forwards), one board entry each, scorecard rows T68–T92, §4.8 / §4.9 / §4.10 pointer
   paragraphs in `for_logan/in_depth_circuit_number.md`, two handoff sections (agreement at MLP 3; the unit chain), `for_logan/LATEST.md` updated
   (the LATEST path is `for_logan/LATEST.md`; a first attempt at `basis_aligned/LATEST.md` failed and was committed separately). v379 enqueued 05:31.
3. **Scorecard — the pronoun-number chain at unit grain.** Simple: one named chain — MLP 1's entry → 3465 / 493 (MLP 3) → 1036 (MLP 5) → 2483 (MLP 6)
   → 1779 (MLP 7) → 829 (MLP 8), copied by head 4.5 once a determiner precedes the noun — held (v356–v368). Predicts OOD: every stage live on 128
   natural sentences with the panel signs (v369); the six-unit edit costs 14% on the panel and 8.8% on text (56× / 60× null; v370–v371); the
   top-ten-per-layer populations 34% / 19% and saturate by five per layer (v372–v375) — held. Extracted: head 4.5's flip is in the noun's own value
   (99%), written by MLP 3 0.52 / MLP 2 0.18 / embedding 0.16 / MLP 1 0.11 on panel and text (v359–v365); 829 fed by 1036 / 2483 / 1779 (v367–v368) —
   held. Selective: the mismatch populations are 30–58× quieter on grammatical verbs (v355); the restorer's rise is specific (10×; v378) — held.
   Composes: the §4.9 compensation acts on the population cut (1779 +17%, 829 −9%, margin −4.7%; v376–v378) — held. Cells moved: all five for the
   unit-grain chain; the MLP stack's causal share is bounded near a third of the margin (v373), the rest attention.
4. **Local-direction checks.** (a) On the declared target throughout. (b) No replay error. (c) No fits. (d) 393 forwards, 25 receipts, 4 falsifications
   kept (v359 copy-from-determiner; v363 trio share; v373 monotonicity; v376's three bars by 0.03). (e) No Codex collision. **Misses:** four gate
   refusals / failed dry-runs from derive-by-regex leftovers (stale token-index line, stale plan key, duplicate constants ×2, writer dict) — all caught
   before GPU except one (v367, 3 forwards).
5. **Kill criteria.** The unit-chain account dies if a stage's OOD sign flips (none did) or if the six-unit edit fails to beat layer-matched nulls (it
   beat them 56–60×). The population-saturation reading dies if k = 20 exceeds k = 10 on text (it did not: 16.6% vs 18.8%).
6. **Decision: continue.** v379 (queued) splits the restorer's rise by factor. Then: fold the unit-chain account into §4.6–4.9 as a single revised
   passage (the pointer paragraphs are accumulating), refresh the synthesis document, and return to the attention share (the two thirds of the margin
   outside the MLP populations) with the same single-token instrument on the reader heads.

## B. Efficiency
1. **Time buckets (04:39–05:31, 52 min).** Scripts ~22 min (25 runners by derive), waiting ~8 min, analysis/recording ~18 min, review 4 min. Idle 0.
2. **GPU.** 393 forwards, ~35 GPU-s; 25 receipts; every receipt gave a decision-changing number; 3 forwards wasted (v367 first run).
3. **Repeated work.** The per-layer census (H of MLPs 3/5/6/7/8 at the noun on the v76 rows) was recomputed in five runners (v372–v376); a cached
   census file would save 3 forwards and ~40 lines each. Derive-by-regex leaves stale module constants: the gate catches duplicates, but a small
   `dod_derive.py --drop-consts` option would remove the class of failure.
4. **ONE improvement (executed).** The lane memory now records the derive pitfall with the exact gate message and the fix pattern (inherited PHRASE /
   K / BATCH lines), so the next derive checks for them first.
QUEUED: /workspace/tensor_language/basis_aligned/bilinear_quotient/ops/run_restorer_factor_split_v379.py
