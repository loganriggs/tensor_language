# Hourly review — 2026-09-18 14:33 UTC (review 22; previous 13:32; cron-fired)

## A. Higher-level view
1. **Goal.** Simple / predicts OOD / extracted / selective / composes, each with a gate and a null (better_circuits §1); depth per path.
   **Standing change (Logan, ~13:50):** depth over breadth, and the lane runs by default — every review leaves an item queued.
2. **Changed since 13:32.** v163 (numeral rank-2 keep 0.62: sufficiency declared open). Logan's requests: `for_logan/in_depth_circuit.md`
   (the reflexive-person component from token to logit, with diagram, equations, a worked example, code and a pass over it) written and
   corrected; protocol and memory updated for run-by-default. Then depth through the pronoun family's declared port: v164 MLP 8 at unit grain
   (two units, 3152 + 3943, carry 76% of MLP 8's gender write on 9.6's direction at the noun; 4/4), v165 edit of the two units (3% of the
   margin at the noun, 8% at all positions; LIVE failed; sized exactly by the chain 0.27 × 0.30 × 0.76), v166 the same unit at the verb (78%;
   3/4), v167 weights: 3152 is a male-noun detector, 3943 a female-noun detector (3/3), v168 the number line's units are different (829, 953,
   1030; 4/4), v170 they are plural / singular detectors (2/3). v169 completed but its receipt was withheld by the price guard (mis-registered
   two batches for a three-batch panel); re-registered as v169b and running. Commits through `4360dd059`. Failed predictions kept: v165 b,
   v166 d, v170 b.
3. **Scorecards.** Seven families, sixteen lines. New depth on the pronoun lines: the MLP 8 port is opened to named units with stated
   functions (one-sided token detectors), and their causal size in the behaviour is measured (small, as the chain implies).
4. **Local-direction checks.** (a) no new behaviour; (b) no replay polishing; (c) no fits — unit functions read from weights and raw
   embeddings; (d) next depth step: v169b sizes the number units; then the natural question is whether the detectors' *inputs* (Left / Right
   factors) read the embedding directly or an earlier MLP's write — the writer fold at the noun for unit 3152's Left and Right directions
   (exact, 2 forwards); (e) no Codex collision (Codex's MLP8 work is the city path; different units, different lines — worth a board note).
5. **Kill criteria.** If v169b's unit edit is not live, the number units are recorded as small like the gender ones (expected ~0.14, borderline).
   If the detectors' factors read the embedding directly, MLP 8's gender / number units close to the token; if they read MLPs 4–7, one more
   layer of the port opens.
6. **Decision: continue.** v169b (running) → record; then v171: writer fold of the inputs to units 3152 / 829 at the noun.

## B. Efficiency
1. **Time (13:32–14:33).** ~14 min writing the in-depth document and its pass, ~20 min runners/records (v164–v170), ~5 min analysis,
   ~3 min ceremony, ~19 min waiting on runs. No idle.
2. **GPU.** ≈ 190 forwards (v163–v169) + two CPU folds; all decision-changing. One price mis-registration cost a rerun (63 forwards).
3. **Repeated work.** v166 / v168 / v170 derived by substitution from v164 / v167; the unit-census and unit-edit bodies should become
   `dod_units.py` (census(layer, reader, positions) / edit(units, positions)) before a third line uses them.
4. **Improvement executed:** run-by-default written into the protocol (13:50). **Scheduled:** `ops/dod_units.py` after v169b.
