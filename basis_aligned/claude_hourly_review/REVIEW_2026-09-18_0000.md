# Hourly review — 2026-09-18 00:00 UTC (review 4; previous 23:07; written in-turn)

## A. Higher-level view
1. **Goal.** A circuit is simple, OOD-predictive, extracted, selective and composing, each against a preregistered
   gate and a null (better_circuits §1); depth on one path until done, then the next.
2. **Changed since 23:07.** 20 receipts (v28–v47), 1,185 forwards, all committed/pushed. Temporal will/had line
   (v28–v41) taken through the battery: set {11.3, 9.1, 15.5, 9.4}, 81% of the margin, selective, keep-only
   sufficient, frozen 0.80±0.15 held on a fourth lexicon + new construction, natural FineWeb/Pile rows pass; 11.3
   reads a subject-NP state written by blocks 8–10 whose attention part is head 8.1 reading the adverb token-only
   (port closed with 8 constants); MLP part diffuse (kill). Narrative was/is line (v42–v47): the blind sweep on
   fresh rows returns the SAME four heads; 72–75% of the margin; frozen 0.74±0.15 held; keep-only, random null,
   three new tense frames and pairwise composition pass; readout heads read the second sentence's state.
   Cross-line summary `SHARED_READOUT_COMPONENTS.md`; temporal for-Logan report; registry cross-links.
   Two label corrections posted (v33 places overlapped the corpus object list; lexicon-3 agents overlapped the
   reporter-alternate list). Failed registered predictions preserved in v28, v29, v30, v32, v34, v38, v41, v43, v44, v47.
3. **Scorecards.** Aspectual: all five held at head grain, two declared ports. Temporal: all held except the
   strict four-way additivity bar (pairwise held); two declared ports. Narrative: Simple, OOD (authored),
   Extracted (head boundary), Selective held; Composes pairwise held; natural rows not built (spread cue).
4. **Local-direction checks.** (a) Two new lines were opened, each only after the previous reached its declared
   limit (§7); (b) no replay-error polishing; (c) no fitted proxies — constants are declared medians; (d) the
   next step targets the one cell no line has closed: the MLP-generated part of the relay states; (e) no collision
   with Codex (typed-face / regional claims only).
5. **Kill criteria.** For the next step: if the exact MLP response to the 8.1 edit at the NP does not account for
   ≥ 40% of the MLP part of the relay contrast, the MLP part is not "generated by 8.1" and the ports stay declared.
6. **Decision: continue on the open ports with a different instrument** — better_circuits §3.4's exact bilinear
   response formula, used as a response census of the 8.1 edit through MLP8–10 (temporal NP) rather than a
   decomposition of native state. If it fails, write the cross-line consolidation and stop opening lines.

## B. Efficiency
1. **Time (box clock).** 23:07–23:30: temporal line, 14 receipts in 23 min (≈1.6 min/receipt incl. recording).
   23:30–23:54: 24 minutes on lexicon construction for the narrative confirmation (two failed strictness checks,
   one wrong-cwd import crash) — the hour's waste; 23:54–23:59: 5 receipts. No idle.
2. **GPU.** 1,185 forwards; every receipt changed a cell or closed a port question. Model reloads ≈ 20 × 40 s.
3. **Repeated work.** Freshness checks were re-derived per script; the lexicon lesson (single-token occupational
   nouns are nearly exhausted) cost 24 min. Runner scripts for sweep/set/null/templates/composition are now
   near-identical across lines — a generic `dod_battery(line_spec)` would replace ~8 files per line.
4. **Improvement executed this hour:** `SHARED_READOUT_COMPONENTS.md` (one place for the cross-line picture) and
   the lexicon rule (`shared` = every corpus list ∪ every panel; declare reuse instead of hunting).
   **Scheduled for the next quiet moment:** a `dod_battery.py` generic runner (sweep, set, null, keep-only,
   templates, composition from one line spec) and a `lexicon.py` registry of used words with the freshness check.
