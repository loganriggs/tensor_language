# Hourly review — 2026-09-17 22:32 UTC (review 2; previous 21:32)

## A. Higher-level view
1. **Goal.** A component is a circuit only when simple, OOD-predictive, extracted, selective and
   composing, each against a preregistered gate and a null (better_circuits §1); depth on one path.
2. **Changed since 21:32.** Nine receipts on the aspectual path (v4–v12, 21:35–22:03), all
   committed/pushed: weight-only readout removal (v4), template transfer (v5), response census
   (v6), blind 162-head sweep (v7: third live head 8.1), three-head promotion (v8), keep-only
   sufficiency (v9), source fold (v10), cue-source edit (v11b; v11 withheld on my mis-counted
   price), constant-pattern token-only generator for 8.1 (v12). Failed registered predictions
   preserved: v6 largest responder (mlp11, not mlp12/14/17), v7 "nothing outside 9–15", v10
   cue-leads-everywhere and top-2≥0.70, v12 pattern CV≤0.35. Codex: regional path, no collision.
3. **Scorecard (aspectual, component {8.1, 9.1, 9.4} on `O_h^T(u_has−u_had)`).**
   Simple: 3 of 162 heads + the has/had contrast; 8.1 = two stored constants (held at head grain).
   OOD: sign/positivity/band predicted on new lexicon + 3 templates; strong form held for 8.1 only.
   Extracted: held at the head boundary; 8.1's port closed; 9.1/9.4 still read a native bank.
   Selective: held (random-direction null, 3 readers, 4 templates); 11.3 fails (shared).
   Composes: held (additive across heads; 5-way random-split null; mlp4→attn5 serial term open).
   Moved this hour: Selective/Composes untested→held; Extracted partial→held-with-open-ports.
4. **Local-direction checks.** (a) same behaviour, same path; (b) no replay-error polishing;
   (c) no fit — v12's constants are medians from the other construction, declared as 2 stored
   scalars; (d) next step targets the last open ports (block-9 bank), the highest-information
   unheld cell; (e) no collision with Codex's typed-face/regional claims.
5. **Kill criteria.** If the block-9 bank fold shows the reader-projected bank contrast is
   spread over >4 writers with no token-only share, the 9.1/9.4 ports cannot close by folding
   at this grain; I would then stop folding and write the path's final scorecard, reporting
   three open ports, rather than fit a proxy (better_circuits §3.8).
6. **Decision: continue.** Next: v13 fold — decompose the bank states 9.1/9.4 read (resid9 at
   `last`, period, `the`) by writer module through the λ-recurrence, projected on the weight-only
   reader direction `V_h^T v̂_h`, oriented since−by; then an edit on the dominant writer.

## B. Efficiency
1. **Time (box clock).** 21:32–22:04: nine runs designed, executed, written up (32 min science
   + write-up; each run ≈ 3–4 min including the board entry). 22:04–22:32: IDLE — I ended the
   turn so the :23 cron could fire and it fired at 22:32 (jitter). Idle was the largest bucket
   this hour (28 of 60 min). Ceremony inside the science window ≈ 8 min (board/scorecard/commit).
2. **GPU.** 1,232 forwards in 9 receipts; every receipt changed a scorecard cell; wall compute
   ≈ 1 min plus 9 model loads (runner-owned, ≈ 40 s each ≈ 6 min — the real GPU-side waste).
3. **Repeated work.** Native forward + producer replay recomputed per run (4 forwards each,
   negligible). Each run rebuilt `readout_directions` and re-captured slices (cheap). The board
   entry + scorecard edit + commit sequence is hand-repeated nine times.
4. **Improvement executed:** do the hourly review IN-TURN at the boundary and keep working; the
   cron is only a fallback if I am idle. (This review is the first written that way? No — it was
   cron-fired after 28 idle minutes; the rule applies from now.) Scheduled: fold the three
   post-run steps into `ops/dod_record.sh` at the next quiet moment.
