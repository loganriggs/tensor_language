# Hourly review — 2026-09-18 16:32 UTC (review 24; previous 15:32; cron-fired)

## A. Higher-level view
1. **Goal.** Simple / predicts OOD / extracted / selective / composes, each with a gate and a null (better_circuits §1); depth per path,
   lane runs by default (Logan, ~13:50): every review leaves an item queued.
2. **Changed since 15:32.** Worked 15:32–15:43 only (6 commits; then idle — no item queued, by review 23's "depth complete for numbers").
   - Review 23's decision executed: `for_logan/in_depth_circuit_number.md` (205 lines; diagram, product/carrier math, named units MLP 3–8,
     edit calibration, natural-text ladder, worked example, code, limits, pass-over). LATEST.md points to it.
   - v201–v204 (12 forwards, 7/16 predictions held): the chain bottoms out in MLP 1 (embedding 5–6%), MLP 1 itself diffuse (top-10 38%) with
     MLP-2 unit 2907 opposing (95% of the block's magnitude) → MLP 1 declared a port at unit grain; at the verb the same detectors re-fire
     (829 33%, 953 25%) and the carriers are the verb-position MLPs (74%), attention 27% led by blocks 4/5 (9%, 8%).
   - Corrected my own record once (v201 head 3.5 = 11.5% was a real failure, not a check artefact). `dod_units.carrier_split` in use.
3. **Scorecard — pronoun-number component.** Unchanged from review 23 except: Extracted now reaches MLP 1 (port declared) and the verb copy
   is identified as re-detection by the same units (v203) with an unnamed attention carrier (v204). Rows 1–40 on the scorecard.
   | property | status |
   |---|---|
   | Simple | 4 heads + 2 MLP-8 detectors + named units MLP 3–6; MLP 1 port |
   | Predicts OOD | heads 12/12 natural; units 3.3% / 0.8% / 0.5% at the cue, each ≥ 40× null |
   | Extracted | folds exact; edits 2–4× below carrier shares (three-edit calibration) |
   | Selective | readers gated at every unit edit; random-set nulls ≤ 0.001 |
   | Composes | counter-case holds at unit grain (fails at head grain) |
4. **Local-direction checks.** (a) On the goal: yes. (b) Falsifiable: 9 of 16 registered readings false this hour, all kept. (c) No fits.
   (d) Depth only, as ruled. (e) Serving Logan: two in-depth documents now exist; the second is the deeper one. **Miss:** the lane sat idle
   49 minutes with nothing queued, against the run-by-default rule — review 23 closed a thread without opening the next.
5. **Kill criteria.** Stop the noun→verb thread if no head of blocks 4–5 carries ≥ 0.15 of the verb-position contrast at the source level
   (then the copy is diffuse and declared). Stop the number line entirely after that; next path is 12.4's inputs (gender) or the
   compensation fold (why edits run below shares).
6. **Decision: CONTINUE — one bounded item, then the lane must hold a queued item at every step.** v205: head-level source fold at the
   verb for the nine heads of blocks 4 and 5, reader = 829's product gradient at the verb (v185's construction), per head: share of the
   block's carrier and the fraction from the noun position. Names the copy or declares it diffuse. 9 forwards.

## B. Efficiency
1. **Time buckets (15:32–16:32).** Review + library 4 min; document 5 min; four receipts (derive + run + record) 9 min; idle 49 min.
2. **GPU.** 12 forwards, 4 receipts; decision-changing: v202 (MLP 1 port) and v204 (attention 27% at the verb).
3. **Repeated work.** Every receipt's record step is a hand-written heredoc that edits the scorecard, the shared doc and the board with
   the same three strings — 25 times today. Runner derivation-by-regex works but copies bodies; acceptable while bodies differ.
4. **ONE improvement (executed now).** `ops/dod_scorecard_row.py <scorecard> <row-markdown> <shared-line>`: inserts the row above
   "## Five-property status" and appends the shared-doc line, so a record step is one call plus `dod_record.sh`. Used from v205 on.
