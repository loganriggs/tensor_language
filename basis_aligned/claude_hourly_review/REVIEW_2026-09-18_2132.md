# Hourly review — 2026-09-18 21:32 UTC (review 29; previous 20:32; cron-fired)

## A. Higher-level view
1. **Goal.** Simple / predicts OOD / extracted / selective / composes, each with a gate and a null (better_circuits §1); depth per path,
   lane runs by default (Logan, ~13:50): every review ends with QUEUED or STOP (hard rule since review 28).
2. **Changed since 20:32.** Worked 20:32–20:44: the verb-annotation thread — miner records `second_offset`; v272/v273 mined 128
   verb-annotated rows; v274: the MLP-8 detectors at the natural verb remove 2.7% (9.9% all positions), selective, above random — the
   verb site generalises at MLP-8 grain (the counter-case fails there); v275 wrong unit set (recorded); v276/v277: the verb-site MLP-5/6
   units weak out of the panel (0.2–0.35%) — declared. Then STOP by decision; 48 idle minutes under an explicit STOP line, as the rule allows.
3. **Scorecard — pronoun-number component.** Predicts OOD now includes the verb site at MLP-8 grain (v274). Everything else as at
   review 27. Declared limits: MLP 1 a port; 4.5 axis weak OOD; verb-site MLP-5/6 units weak OOD; the counter-case at the verb.
4. **Local-direction checks.** (a) On the goal: yes; the number line is as deep as the tools reach, at two sites, in and out of the panel.
   (b) Falsifiable: 8 of 20 registered readings false since 20:32, kept, incl. one design error. (c) No fits. (d) Depth: the temporal
   family is the one family where the unit-grain lens stopped at "MLP 8 is a port" without looking one block down, although its bank
   state is written 10–17% by MLP 7 and 10–12% by MLP 6 (its rows 26–27). (e) Serving Logan: three documents current; the STOP was
   honest but "run by default" prefers a bounded item to an idle GPU when one exists at 2 forwards.
5. **Kill criteria.** The temporal MLP-6/7 census dies if both blocks are spread at the bank (top-10 < 0.30) — then the temporal family is
   declared a port at every MLP grain and the lane STOPs for the night with that written.
6. **Decision: CONTINUE with one bounded item — the temporal family one block down.** v278 / v279: MLP 7 and MLP 6 at unit grain on
   9.1's has − had reader direction at the bank (last / period / the) on the v1 rows (v253's census with LAYER 7 / 6). 2 forwards each.
   If either names units, one edit follows; if not, STOP.

## B. Efficiency
1. **Time buckets (20:32–21:32).** Review + protocol + miner: 4 min; mining (CPU): 4 min; five receipts run/record: 5 min; idle by STOP: 48 min.
2. **GPU.** 400 forwards for five receipts; decision-changing: v274 (80). v275's 80 were spent on the wrong units — a design error the
   checker cannot catch (the runner was valid); 240 forwards on weak-result confirmations (v275–v277).
3. **Repeated work.** The record step is still ~8 lines of Python per receipt (row + shared line + document clause) with shell-quoting
   hazards (the backtick truncation at 18:39).
4. **ONE improvement (executed now).** `ops/dod_scorecard_row.py` gains a `--json <file>` mode: one JSON file with `scorecard`, `row`,
   `shared`, and optional `doc`/`old`/`new` performs the scorecard insert, the shared-log append and the document replacement in one call,
   asserting each landed — no shell quoting, no format characters. Used from v278 on.
(21:41 UTC, after the thread ran) Outcome: v278 MLP 7 at the bank is concentrated (unit 1250 23%, top-10 73%); v279 MLP 6 intermediate; v280 1250 is a since-context detector (32/32, edit −0.56%, 8× random); v281 the MLP-7 trio removes 2.2% at the bank (10× random 3-unit sets); v282 the detector is a cue copy carried by attention (head 7.8 32%; embedding 0); v283 the trio shows nothing on natural rows (0.09%) — the bank is a panel construct. The temporal family joins the detector families one block below its MLP-8 port, in the panel only.
STOP: the temporal MLP-7 thread is complete (named, edit-decided, OOD limit declared); no bounded receipt is pending that would decide a stated claim. The lane idles until review 30 (22:32 UTC) or a person redirects.
