# Hourly review — 2026-09-19 03:27 UTC (review 35; previous 02:26; written ahead of the 03:32 cron)

## A. Higher-level view
1. **Goal.** Simple / predicts OOD / extracted / selective / composes, each with a gate and a null (better_circuits §1); depth per path;
   lane runs by default, no STOP (Logan, ~02:00); every review ends `QUEUED:`.
2. **Changed since 02:26.** Receipts v294–v321 (28 runners; all `circuits/followups/mlp1_*|mlp2_*|mlp12_*`), one board entry each, scorecard
   `MLP1_TOKEN_TABLE_SCORECARD.md` rows T8–T34, `SHARED_READOUT_COMPONENTS.md` lines, `for_logan/in_depth_circuit_number.md` §4.10 written
   (facts 1–5, the mechanism equation, the unit story, the loss prices, the reader, MLP 2, classes; receipt table v287–v320), two conventions
   memories (term-wise censuses; block patch-back). v321 enqueued 03:27.
3. **Scorecard — MLP 1 as a component of the number chain.** Simple: the description is one equation, W = γ²T + cross + context², with γ² ≈ 0.9,
   cross ≈ −0.44…−0.87 on T, and α = attention self-share (v291/v297/v301/v302) — held. Predicts OOD: every filler-context law replayed on 2,944
   natural positions (v297, v302, v309) — held. Extracted: the gain head {3289, 624} named by the NET census (v306), causal by replace-edit
   (v307, +0.152 vs census +0.169, 380× null), dose–response down the census (v308), OOD (v309) — held; loss price 0.06 nats for the head,
   0.70 for the whole table (v310–v313). Selective: the head's number-margin effect is −0.7% (v307) — MLP 1's gain is not what the number
   chain reads; it reads the entry's direction (v296: 0.74 of the carriage) — held as a negative. Composes: reader named — block 2, MLP 2's
   input (v316/v317), densely; MLP 2 obeys a weaker version of the law (v318/v319); class order shared (v320) — held. Cells moved: all five
   for MLP 1 as a described component (it is not itself a behaviour circuit; it is the bottom stage of the number chain).
4. **Local-direction checks.** (a) Not a new behaviour: MLP 1 is the number chain's declared lowest port and Logan's request; MLP 2 entered only
   as MLP 1's named reader. (b) No replay error. (c) No fits: all quantities are exact folds, projections or edits; the one SVD (v294) is a summary
   and was a null result. (d) Highest information per GPU-second: 514 forwards this hour, ~40 GPU-s, 28 receipts, 9 falsifications kept (v294,
   v295, v299, v300, v305-as-read, v312's price, v315's instrument, v318's direction, v320's numbers). (e) No collision with Codex (blocks 0–2).
   **Miss:** v305 was mis-read as a falsification of the gain-unit claim until v306 showed the counterfactual was wrong (zero vs replace); both
   readings stand in §4.10 and on the board. v298 run 1 and v310 run 1 wasted ~60 forwards on instrument bugs (hook layout; non-contiguous view).
5. **Kill criteria.** The MLP-1 description dies if a replace-edit of the census head fails to move α as the census says on text (not run: v307 was
   filler; the loss test v310 was text and held) — one bounded item, not yet queued. The MLP-2 thread dies if its lookup law adds nothing the
   number chain uses; v320 already suggests numbers lose their lookup by MLP 2, which closes that thread for the number circuit.
6. **Decision: continue, then return to the number chain.** v321 (kind-split gains on text) closes the class question OOD. Next: fold the
   MLP-1 description into the pronoun-number circuit's account (does unit 3465's read of MLP 1's entry direction survive the head restore on the
   v76 rows at the α = 1 limit — the all-self-only attention edit — and does the number margin then change?), then the handoff page.

## B. Efficiency
1. **Time buckets (02:26–03:27, 61 min).** Scripts/preregs ~26 min (28 runners by AST-derive from siblings); waiting ~8 min (runs < 3 s each; queue
   latency 15–60 s); analysis/recording ~22 min; review 5 min. Science 48 min. Idle 0.
2. **GPU.** 514 forwards, ~40 GPU-s; 28 receipts; every receipt produced at least one decision-changing number; ~60 forwards wasted (two void runs).
3. **Repeated work.** Every natural-text runner recaptures the unique-token table (6 forwards) and the natural rows (2): a cached table file would
   save 8 of ~14 forwards per text receipt (trivial in GPU time; the cost is queue latency). Runners derive from siblings by string edits; two derive
   failures (undefined `losses` after a block replace; stale bars name) were caught by the gate/checker before GPU.
4. **ONE improvement (executed).** Two conventions written to memory with their receipts: term-wise censuses nominate and the counterfactual must
   match the claim (replace vs zero; v295/v305/v306), and block patch-back does not localise readers in bilin18 (v315) — path-restricted injection
   does (v316). Both are also in the scorecard readings so a fresh session finds them.
QUEUED: /workspace/tensor_language/basis_aligned/bilinear_quotient/ops/run_mlp12_kind_gain_natural_v321.py
