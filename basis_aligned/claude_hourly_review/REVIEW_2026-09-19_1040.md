# Hourly review — 2026-09-19 10:40 UTC (review 42; previous 07:36 — the cron did not fire for three hours; written by hand from receipts, not backfilled per hour)

## A. Higher-level view
1. **Goal.** Simple / predicts OOD / extracted / selective / composes, each with a gate and a null (better_circuits §1); depth per path; lane runs by
   default, no STOP; every review ends `QUEUED:`.
2. **Changed since 07:36.** Receipts v438–v541: 104 runners, 1822 forwards, 520 predictions, 103 failed and kept (runlogs 07:39–10:37); scorecard rows
   T152–T254; board entries per runner; §4.11 of `for_logan/in_depth_circuit_number.md` extended by seven paragraphs (by-token output, two readouts one hub,
   verb readout at both ends, both readouts from the answer side, template library, noun-as-answer map — the last written this review). Threads closed:
   the output axis (third-plural vs singular of both genders; gender tilts cancel, v438–v440), the verb readout's five heads {11.3, 7.8, 13.1, 9.7, 5.3} named
   by edit at both ends (v445–v488), the MLP-8 hub shared by both readouts (v441–v444), the detector 701/2059 and its feeds (v465–v480), the pronoun-slot
   re-expression as a population (v503–v506), the panel's MLP-17 'he' population and small relays (v531–v537), the template library (v420–v527), and the
   attention map into the noun's own position on adjacent rows (v539–v541, T252–T254).
3. **Scorecard — the number circuit (both readouts).** *Simple:* five pronoun readers + five verb readers + one copier + one hub + one detector, all named by
   weights-only templates (v521–v526) and confirmed by edit. *Predicts OOD:* every edit on 122 natural pairs (43 adjacent / 79 distant) with the panel as
   second frame; template false positives inert (v527). *Extracted:* hub units 829/953/1030 (both readouts), detector 701/2059 (verb only); pronoun side has
   no detector (population, v506 refuted 22/3428). *Selective:* cross-selectivity 0.04 / 0.10 (v449–v453); gender margin untouched (v438). *Composes:* copier →
   MLPs 4–8 → readers; 5.3 relays the copier's seed (v478); early and late routes add. Cells moved this window: composes (5.3's second hop), selective (cross),
   extracted (detector; pronoun population negative). Open: MLP side of the noun-as-answer position by stage (v542 queued); the 0.31 small exits (dropped, ≤ 0.04 each).
4. **Local-direction checks.** (a) Same target throughout; no new behaviour. (b) Replay ≤ 2e-5 on every edit; no time on replay. (c) No fits; templates are
   weights, not fits. (d) Diminishing: T252–T254 spent 30 forwards to place 0.05 of margin — at the 0.04-per-item bar; v542 is the last split at this position.
   (e) No Codex collision (regional path untouched). **Misses:** v539 ran mis-filtered on distant rows and price-aborted (12 forwards); v540 kept v539's OUT
   filename; nine instrument re-runs (v494/v495/v513/v514/v520–v524/v528/v529) cost ~90 forwards — regex-derived runners inherit row filters, replay blocks and
   bar names that must be re-targeted; the conventions memory now lists the grep leftovers.
5. **Kill criteria.** The adjacent-row thread dies after v542 whatever it shows (every attention item is placed; the MLP split is the last named-component
   question). The verb-readout account dies if a third natural set fails to give ≥ 0.40 for the five heads into a distant verb (0.557 here) — not yet tested.
6. **Decision: continue** through v542, then redirect to the third-set check of both reader sets (new natural rows, different nouns/verbs) — the one
   untested OOD cell, and a cheap one (two runners). After that the number circuit is written up as done and the lane opens the next depth item.

## B. Efficiency
1. **Time buckets (07:36–10:40, 184 min).** Scripts ~70 min (all regex-derived), waiting ~45 min (edits 8–40 forwards, ~1 min each), analysis/recording
   ~50 min, docs/synthesis ~12 min, this review 7 min. Science (ii+iv) 120 of 184; ceremony small; the largest waste was re-running mis-targeted derivatives.
2. **GPU.** 1822 forwards for 104 receipts; ~100 forwards (5%) on aborted/mis-targeted runs. Decision-changing: v436-style censuses (both readouts, both
   ends), v478 (5.3's relay), v506 (population), v521–v527 (templates), v539–v541 (noun position).
3. **Repeated work.** Every edit runner recomputes the 2–7 native forwards; the 122 natural pairs are re-tokenised per runner; the v445-lineage filter
   `d >= 2` and the replay block are copied 20+ times. A shared `adjacent_rows()/distant_rows()` helper and a cached native-margin table would remove both.
4. **ONE improvement (executed).** Conventions memory updated with the grep list for regex-derived runners (row filter, replay rows, bar names, OUT name,
   int/str head keys, d=0 guard) — the v539 abort was the fourth instance; and §4.11's noun-as-answer paragraph written with the receipts, not deferred.
QUEUED: /workspace/tensor_language/basis_aligned/bilinear_quotient/ops/run_direct_path_mlp_singles_v542.py
