# Hourly review — 2026-09-17 21:32 UTC (first review; work began ~20:41 UTC)

## A. Higher-level view
1. **Goal.** A component is a circuit only when it is simple, predicts OOD, is extracted, is
   selective and composes — each against a preregistered gate and a null (better_circuits §1).
   Depth on one path until done; no new behaviours.
2. **Changed since start.** Instance restored (torch, checkpoint, runners). Three receipts on the
   aspectual has/had path: `aspectual_anchor_dod_removal_v1` (20:59), `_v2` (21:02),
   `_composition_v3` (21:05). Commits 6eed3cf, 71585c2, de211d9 pushed. Scorecard written.
   Codex (regional path) independently found at 21:29 that "physical-write additivity is
   generic" — same regime finding as my v3 random-split null.
3. **Scorecard (aspectual).** Simple: untested. OOD: partial (prior fit-free lexicon/prefix
   transfer; no token-only generator). Extracted: partial (v12 program, 4 ports). Selective:
   held for the cue-defined delta at five slices vs norm-matched null + 3 readers; NOT held
   for the module partition (v3: 80/80 random pieces pass the same gate). Composes: five-way
   gap 0.033 vs random-split median 0.063; 9/10 Möbius pairs below gate; mlp4→attn5 fails.
   Moved this hour: Selective untested→partially held; Composes untested→measured with null.
4. **Local-direction checks.** (a) no new behaviour; (b) not polishing replay error; (c) no fit
   anywhere — but the midpoint removal is cue-defined and paired, which is why v3 could show
   its selectivity is generic; (d) next step targets the unheld cell (a removal direction not
   defined by the cue contrast, which also closes the partner-row port); (e) no collision:
   Codex's claims are all typed-face/regional.
5. **Kill criteria.** If a weight-derived (cue-independent) removal at attention9 H1/H4 does not
   beat its norm-matched null, the "selective component" claim for this path reverts to
   "selective delta" and the property stays unheld; I would then move to Simple/OOD counting
   rather than more removal variants.
6. **Decision: continue.** Next action: v4, weight-derived removal direction `O_h^T (u_has − u_had)`
   per head (no partner row), null = random 128-d direction of equal removed norm; also report
   cosine to the mean paired delta (fold).

## B. Efficiency
1. **Time (box clock).** 20:41–20:56 orientation + restore (15 min; restore overlapped reading);
   20:56–21:05 design + three scripts + three runs (9 min); 21:05–21:10 analysis, board, commits
   (5 min); 21:10–21:32 summary to Logan, memory notes, this review (ceremony ~10 min).
   Science ≈ 14 min, orientation 15, ceremony ~10. Orientation was the largest bucket; it was
   a one-off for a fresh instance but the ~200 MB of board/ledger is the cause.
2. **GPU.** 552 forwards, ~19 s compute (+3 model loads ≈ 1.5 min). Decision-changing numbers:
   the attn5 null (v1), the v2 pass, the v3 random-piece result — all three runs changed a cell.
3. **Repeated work.** Each run reloads the 546M model (runner-owned; not mine to cache). The
   native forward and captured writes were recomputed in v2 and v3 (2+2 forwards each; trivial).
   I hand-typed three board stamps wrong.
4. **Improvement executed:** `ops/board_append.sh` stamps board entries from the box clock.
