# Hourly review — 2026-09-18 03:27 UTC (cron-fired 15 min after review 9 at 03:12; delta only)

## A. Higher-level view (delta since 03:12)
1. **Goal.** Unchanged: simple / predicts OOD / extracted / selective / composes, each with a gate and a null (better_circuits §1);
   depth per path and a better collection across paths.
2. **Changed.** Person family closed on both lines: v115 object-control fold (token readers replicate), v117 token-only generator
   5/5 (constant patterns keep 94%), v118 10.5's source diffuse (declared port; "MLP ≥ 0.50" false). Residue cluster {11.3, 7.8, 17.4}
   shown to be the number family by weight geometry (v116, 1/4: mood rides the number/tense axes, finiteness separate); atlas number
   core widened, 85/100 live lines assigned. Sixth family opened and batteried: correlatives {8.1, 16.8, 14.8, 7.8} — v119 either/not
   7/7, v120 both/neither 6/7 (over-additive), v121/v123 nulls, v122/v124 censuses direct with the strongest late-MLP counter-response
   seen (+1.0 / +1.4). Natural correlative panels mined; v125–v128 running (one receipt in). Tooling: `dod_line.py` (emits the standard
   runners; used for v121–v128), `dod_folds.py`, `dod_lexicon.AGENT_POOL / OBJECT_POOL`, `collection_registry.py` →
   `READOUT_COLLECTION.json`. Commits through `26d8adffe`. Failed predictions kept: v116 b, c, d; v118 b; v120 e.
3. **Scorecards.** Six families, thirteen lines at head grain; natural in-/out-of-corpus rows on ≥ 1 line of every family once
   v125–v128 land. Ports: MLP relay parts (temporal, in/of, have/has), MLP 8 (pronoun), 10.5's diffuse source (person).
4. **Local-direction checks.** (a) the correlative line was opened only after the person lines closed, from the atlas residue;
   (b) no replay polishing; (c) no fits; (d) next highest-information step: the correlative natural rows now running, then the
   correlative report and the index at six families; (e) no Codex collision (its heads absent from every set).
   **Process bug found and recorded:** v119/v120 ran with will−would readers because importing v104 for a word list set
   `L.READERS` at module level; docstrings corrected, board entry, memory rule (readers explicit; pools from dod_lexicon).
5. **Kill criteria.** Unchanged; none fired (a second correlative line failing the battery would have demoted the family).
6. **Decision: continue.** Wait for v125–v128, record, write the correlative report; then the correlative source fold (is 8.1 a
   token-only either/both reader too?).

## B. Efficiency (03:12–03:27)
1. **Time.** ~4 min waiting, ~8 min writing, ~3 min analysis; this file 2 min.
2. **GPU.** ≈ 500 forwards (v115–v124), all decision-changing.
3. **Repeated work.** None new; the generator removed the shell templates. The reader side effect cost one docstring correction.
4. **Improvement executed:** `dod_line.py --only-natural`; screened word pools. **Scheduled (unchanged):** none new this quarter hour.
