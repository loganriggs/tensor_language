# Hourly review — 2026-09-19 12:06 UTC (review 44; previous 11:24)

## A. Higher-level view
1. **Goal.** Simple / predicts OOD / extracted / selective / composes, each with a gate and a null (better_circuits §1); depth per path; lane runs by default;
   every review ends `QUEUED:`.
2. **Changed since 11:24.** Receipts v565–v590 (24 GPU runners, 280 forwards, 120 predictions, 27 failed and kept) plus the v578 CPU miner (third gender set);
   scorecard rows T277–T300; a board entry each; §4.12 (gender) closed and §4.13 (person) opened in `for_logan/in_depth_circuit_number.md`; `LATEST.md` 11:52
   section. Threads: (i) gender closed at head grain on THREE natural sets — writes 0.88 / 0.89, composition 0.90 / 0.90 / 0.89 (fineweb, pile, fresh fineweb),
   selective (0.06 on the number axis); the '18 Sep self-copies' 8.1 / 6.1 are copiers into the following context (self-writes 0.02 / 0.01), and the channel
   closes from two named sources (readers' noun values 0.60 + copiers' post-noun copies 0.26 → 0.90); (ii) the position decomposition of the readers' writes was
   nested and stopped (T279–T282); (iii) the PERSON line opened by the same method: values at the cue 0.99, four named readers 0.36 (writes 0.41), early copier
   route 0.15 (block 4 a population), other writers into the answer 0.53 by block (block 9 0.11: 9.6 0.058, 9.7 0.035; block 5 0.06; blocks 6–12 0.02–0.05).
3. **Scorecard.** *Gender:* simple (5 readers + 2 copiers + token branch) held; predicts OOD held (three sets); extracted partly (token branch and copiers by
   edit; detectors small); selective held; composes held (two sources = channel). Done at head grain. *Person (new):* simple — the four named readers are a
   third; the write side is spread over ~8 blocks (block 9 by head; block 5 next); predicts OOD — v590 (pile) running; extracted / selective untested;
   composes — v591 queued (copies + cue values). Cells moved this hour: gender OOD (three sets), gender composes, person simple (partly).
4. **Local-direction checks.** (a) Person opened only after gender met the definition of done at head grain (recorded 11:53). (b) Replay ≤ 2e-5 throughout.
   (c) No fits. (d) Diminishing returns caught twice: the position decomposition (five runners, nested) and block-4 person copiers (a population at 0.02 per
   head) — both stopped and recorded. (e) No Codex collision. **Miss:** v560–v562's instrument had three bugs; v563 refuted it by edit — 6 forwards, lesson kept.
5. **Kill criteria.** Person dies as a *named-set* circuit if no set of ≤ 8 heads writes ≥ 0.60 into the answer (today: four readers 0.41 + 9.6 / 9.7 0.09 +
   block 5 ≈ 0.55); then it is recorded as 'spread' with the block census as its depth limit. Gender's remaining item (unit grain on the third set) is optional.
6. **Decision: continue** person — OOD (v590), composition (v591), then the block-5 writer by head and a candidate seven-head set {8.1, 13.1, 10.5, 15.1, 9.6,
   9.7, 5.x} scored jointly against the 0.60 bar. If it fails, close person as spread and open the next line (temporal or selection) with the same method.

## B. Efficiency
1. **Time buckets (11:24–12:06, 42 min).** Scripts ~16 min (regex-derived; one anchor miss on v568), waiting ~6 min, analysis and recording ~14 min, docs ~4 min,
   this review ~3 min. Science (ii+iv) 30 of 42.
2. **GPU.** 280 forwards for 24 receipts (~12 per runner; the two 20–26-forward singles censuses were the price of naming heads). Decision-changing: v565 / v577 /
   v582 (OOD), v573–v576 (copiers, composition), v579–v580 (person census), v586–v589 (person write side). Low value: v568–v570 (nesting, 22 forwards).
3. **Repeated work.** The person runners re-tokenise 43 pairs per run; the gender/person builders are two near-identical functions (`gender_pairs`,
   `person_pairs`) — a shared `swap_pairs(rows, partner_map)` would serve all three lines. The by-position RULES mechanism is copied into six runners.
4. **ONE improvement (executed).** The miner and the pair builder both gained a `rows_path` / skip override (v544, v578), so a fresh set costs one CPU run and no
   new code; the third gender set (v578) and its runner (v582) took four minutes end to end.
QUEUED: /workspace/tensor_language/basis_aligned/bilinear_quotient/ops/run_person_composition_v591.py
