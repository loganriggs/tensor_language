# Hourly review — 2026-09-19 02:26 UTC (review 34; previous 01:32; written ahead of the 02:32 cron with results fresh)

## A. Higher-level view
1. **Goal.** Simple / predicts OOD / extracted / selective / composes, each with a gate and a null (better_circuits §1); depth per path;
   lane runs by default with **no STOP** (Logan, ~02:00): every review ends `QUEUED:`.
2. **Changed since 01:32.** Logan redirected the lane to MLP 1 (decompose it along Embedding→Attn0→(MLP0)→MLP1 from single tokens, classes
   with exponential sizes, describe what it does; check the dossier first). Receipts v287–v293 (`circuits/followups/mlp1_*_v28[7-9]|v29[0-3]_result.json`),
   7 board entries, scorecard `MLP1_TOKEN_TABLE_SCORECARD.md` rows T1–T7, `SHARED_READOUT_COMPONENTS.md` lines, protocol amendment (no STOP),
   `dod_units.attention_self_share`, README line. v294 enqueued 02:26.
3. **Scorecard — MLP 1 (a component description, not yet a circuit).** What is established from exact folds (closure 0.0 everywhere):
   write(token in context) = α·table(token) + R. α = the token's own-key share of the bilinear attention pattern (blocks 0+1): 0.555/0.440/0.365/0.335
   for 1/2/4/8 filler tokens vs share 0.525/0.399/0.279/0.210, class-uniform (CV ≤ 0.20), independent of WHICH context token (42 cells 0.41–0.65),
   floors at ≈0.29 from 16 tokens. R is orthogonal to the token's and the context's table entries (v292 falsified the two-term reading); R = ~40%
   one shared context-set direction + ~60% high-rank token-specific parts with class structure (own-class cos 0.78 vs 0.49). Untested: what the
   shared direction is (v294), what the class-structured cross part encodes, and whether downstream readers (3465/829) read α·table, the shared
   direction, or the cross part. Nothing here is a behaviour circuit; it is the port description Logan asked for.
4. **Local-direction checks.** (a) Not a new behaviour: MLP 1 is the declared lowest port of the number chain, and Logan asked for it. (b) No replay
   error; every number is an exact fold. (c) No fits: α, β are projection coefficients; the SVD in v294 summarises captured writes. (d) Highest
   information per GPU-second: yes — 37 forwards produced five decision-changing numbers (the ½ gain, its context-independence, self-share tracking,
   two-term falsification, the shared direction). (e) No collision with Codex (blocks 0–1, no regional path). **Miss:** none this hour.
5. **Kill criteria.** The self-share law dies if α stops tracking the share on natural text (not yet tested; v291 used filler phrases). The shared-
   direction thread dies if v294 finds it context-dependent (cos < 0.7 across phrases) — then it is not "one register direction" and the description
   must carry a per-context term. Neither has occurred.
6. **Decision: continue.** Next action after v294: relate the description to the number contrast — does unit 3465 / MLP 3 read α·table (the
   token-identity term) or the cross part? That closes the loop to the pronoun-number circuit.

## B. Efficiency
1. **Time buckets (01:32–02:26, 54 min; 01:32–02:00 was idle under the old STOP until Logan's message).** Orientation/reading 4 min (dossier check,
   attention source); scripts/preregs 22 min (v288–v294, 7 runners); waiting 3 min (each run < 2 s GPU; queue latency ~15 s); analysis/recording
   18 min; review 7 min. Science 40 min. Idle 0 since 02:00.
2. **GPU.** 37 forwards over v288–v293 (4+4+12+5+9+3), ~10 GPU-seconds total; 5 decision-changing numbers. Three wasted runs: v291 twice (wrong
   attention hook name; signed pattern averaged to zero) — ~2 GPU-s, ~4 min. v293 saved once by `dod_check_runner` (stale bars name).
3. **Repeated work.** Each v29x runner re-captures the 224-token table (1 forward) and re-derives Lt/Rt; a cached table would remove 1 forward per
   run (trivial at this size). The capture function is imported from v289, not copied. The v291 hook was written twice because the attention
   module's signature and signedness were unknown — now documented in the helper.
4. **ONE improvement (executed).** `ops/dod_units.py` gains `attention_self_share(backend, tokens, pos, layers)` — the exact own-key share of the
   signed, unnormalised bilinear pattern with the signedness warning in its docstring — and `ops/README.md` indexes it.
QUEUED: /workspace/tensor_language/basis_aligned/bilinear_quotient/ops/run_mlp1_shared_direction_v294.py
