# Hourly review — 2026-09-18 04:32 UTC (review 12; previous 03:43; cron-fired)

## A. Higher-level view
1. **Goal.** Simple / predicts OOD / extracted / selective / composes, each with a gate and a null (better_circuits §1); depth per
   path, a better collection across paths.
2. **Changed since 03:43.** v142 tense census (the number set's natural-text tense leak is MLP-borne, direct −4%: declared limit);
   v143 / v144 cross-family composition in one sentence (gender + correlative; person + correlative sharing head 8.1): inert at each
   other's positions, joint exactly additive, 8/8; v145 8.1 copies the gendered noun's token value to the verb for 9.6 (4/4); v146
   8.1's vocabulary table along six contrasts (signs held; top-20 / cue-class readings failed: both, by, king, tomorrow sit far down).
   End-of-night synthesis for Logan written and pointed to from LATEST; collection registry reads every schema; ops README index;
   v143 / v144 panels registered replay-safely. Commits through `d836fec21`. **Idle 03:55–04:32**: queue empty, no work done, no
   backfill.
3. **Scorecards.** Six families, thirteen lines closed at head grain; natural in-/out-of-corpus rows on every family; cross-family
   structure (shared heads / separate directions; sentence-level composition; 8.1 as one copier) recorded in
   `SHARED_READOUT_COMPONENTS.md` and the synthesis. Declared limits: number/tense on natural text; MLP ports on four lines.
4. **Local-direction checks.** (a) no new behaviour since 03:27; (b) no replay polishing; (c) no fits (v146's cue-class list was
   fixed in the file before ranking); (d) highest-information next step is small: a third correlative line at fresh grain (either /
   neither → or / nor, atlas 0.55) makes that family three-deep at the cost of 123 forwards; larger questions (temporal natural rows
   for narrative / modal) need a miner for sentence-spread cues and are not cheap; (e) no Codex collision (city MLP8 path).
5. **Kill criteria.** The correlative third line failing the battery would be recorded as a two-line family; nothing kills the
   collection claims recorded so far.
6. **Decision: continue, bounded.** Run the third correlative line (v147) with the existing templates, record it, then stop the
   collection thread for the night unless a review finds a gap; leave the queue to Codex.

## B. Efficiency
1. **Time (03:43–04:32).** ~12 min writing/records, ~4 min analysis, ~2 min ceremony, 37 min idle waiting for this cron slot.
   Fix: when the queue is empty and the decision is "bounded", say so and end the turn rather than wait — the idle time is honest
   but not useful.
2. **GPU.** ≈ 110 forwards (v142–v145) + two CPU-lane folds; every run decision-changing.
3. **Repeated work.** None; v144 was derived from v143 by substitution, v146 reused v133's block-0 construction.
4. **Improvement executed:** collection registry tolerant of the older receipt schemas with a `--md` table (synthesis table is
   regenerated, not typed). **Scheduled:** none new — the tooling list in `ops/README.md` is current.
