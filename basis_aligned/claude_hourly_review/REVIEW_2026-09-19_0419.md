# Hourly review — 2026-09-19 04:19 UTC (review 36; previous 03:27; written ahead of the 04:32 cron)

## A. Higher-level view
1. **Goal.** Simple / predicts OOD / extracted / selective / composes, each with a gate and a null (better_circuits §1); depth per path; lane runs
   by default, no STOP (Logan); every review ends `QUEUED:`.
2. **Changed since 03:27.** Receipts v322–v345 (24 runners), one board entry each, scorecard `MLP1_TOKEN_TABLE_SCORECARD.md` rows T36–T58,
   `SHARED_READOUT_COMPONENTS.md` lines, `for_logan/in_depth_circuit_number.md` §4.10 extended (MLP 1's harm routes, the single-token chain
   fold, agreement readers, factor splits, writers), handoff page gains two sections (MLP 1 resolved; the chain's units from single tokens).
3. **Scorecard — the number chain's bottom.** Simple: unit 3465 (and 493 / 1036 / 829) is a bilinear product whose two inputs both carry the noun's
   number, fed 58–88% by MLP 1's context-free lookup entry (v341/v343/v344) with MLP 2 adding context-conditioned number (v342) — held. Predicts
   OOD: separations replay on 400 vocabulary pairs (v333/v335) and on 128 natural sentences (v344: 1.0–2.2 std) — held at the stated strengths.
   Extracted: the chain's units named from single tokens through blocks 0–8 (v329–v332); 953 / 1030 removed from the lexical set (v333) — held.
   Selective: 3465 fires for plural-NOUN sense only (irregular plurals yes; -s verbs only with a noun reading; numerals, pronouns, mass and
   collective nouns no; v329–v331) — held; the frame-design factor labels (L = noun, R = determiner) did NOT transfer to sentences (v339) —
   falsified and kept. Composes: un-conditioning MLP 1 costs the margin 32% via MLPs 2 / 3 (v323–v325) but the harm is generic (v328); the
   agreement units detect at the noun and not at the answer (v334/v345 pending) — held. Cells moved this hour: extracted and selective for the
   chain's lower units; the MLP-1 thread closed for the number circuit (v328).
4. **Local-direction checks.** (a) On the declared target; MLP 2 and the MLP-3 'leak channel' entered only as MLP 1's consumers. (b) No replay
   error. (c) No fits (two-way variance shares in v338 are exact sums of squares). (d) Information per GPU-second: 24 receipts, ~250 forwards,
   9 falsifications kept (v322 carriage, v325 additivity, v327 sign, v333 bars, v336 length, v337 determiner bars, v338 set bars, v339 transfer,
   v344 two bars by 0.02). (e) No Codex collision. **Misses:** the v333/v337/v338 bars were set from lexicon or frame results and were too strict
   for vocabulary pairs and sentences — the fix (quote the 256-pair / sentence numbers, set bars from them) is in the scorecard readings; one
   runner (v340) failed once on writer tracking past block 3 and was fixed before its receipt.
5. **Kill criteria.** The 'agreement reader' description dies if the units' contrast on sentences fails to split into MLP-1-lookup-fed terms (it
   did split: v341–v344). The single-token fold as an instrument dies if a unit's single-token separation has the wrong sign in context (none did).
6. **Decision: continue.** v345 (queued) reads the units at cue / verb / final on natural sentences. Then: fold the chain's account into the
   in-depth document's §4.6 (the named units are agreement products fed by MLP 1's lookup) and the `LATEST.md` summary; then the next depth item
   is the verb site (829 at have/has) with the same single-token instrument.

## B. Efficiency
1. **Time buckets (03:27–04:19, 52 min).** Scripts/preregs ~22 min (24 runners by AST-derive); waiting ~6 min; analysis/recording ~20 min;
   review 4 min. Science 42 min. Idle 0.
2. **GPU.** ~250 forwards (the tally above), ~20 GPU-s; 24 receipts; two wasted runs (v330 multi-token entry, v340 KeyError) ≈ 4 forwards.
3. **Repeated work.** Every single-token runner re-derives the class lists from `dod_token_classes_v287.json` and recomputes the table pass; the
   `(1, T)`-shaped single-token captures for the 256 vocabulary pairs were recomputed five times (v333–v337) — a cached hidden-by-unit table would
   have saved 8 forwards and, more usefully, script lines. The derive-by-regex pattern produced one stale-name failure caught by the gate.
4. **ONE improvement (executed).** The scorecard readings for v333 / v337 / v338 now state the bar-setting lesson (bars from a clean lexicon or a
   2 × 2 frame do not transfer to vocabulary pairs or sentences; set bars from the broader set) and the conventions memory carries the block-patch
   and counterfactual lessons from the previous hour — a fresh session inherits both without re-deriving them.
QUEUED: /workspace/tensor_language/basis_aligned/bilinear_quotient/ops/run_chain_units_positions_natural_v345.py
