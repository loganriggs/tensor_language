# Hourly review — 2026-09-18 02:07 UTC (review 7; previous 01:32; written in-turn after the pronoun line closed at head grain)

## A. Higher-level view
1. **Goal.** A circuit is simple, OOD-predictive, extracted, selective and composing, each with a gate and a null
   (better_circuits §1); depth per path, and a better collection across paths.
2. **Changed since 01:32.** v70 receipt (shared temporal axis: 52–58% on temporal/narrative/modal, ≥ 50% of own removal on
   all four lines; aspectual misses LIVE — recorded as 3/4). Atlas v68 crashed at line 204 (zero rows), guard added,
   remainder run: `readout_atlas_v68_result.json` (225/226 scored, 118 capable, 100 live), `READOUT_ATLAS_TABLE.md`.
   Pronoun gender line opened on the atlas's most recurrent core {9.6, 12.4, 15.1} (19 pronoun lines, no auxiliary line):
   v71 fresh rows 6/7 (frozen band failed upward: 1.11 on one frame), v72 random-set null 4/4, v73 natural FineWeb 6/6
   (77%), v74 response census 5/5 (94% direct), v75 Pile 6/6 (83%). `PRONOUN_GENDER_DOD_SCORECARD.md`, for_logan report,
   registry + shared-components cross-links. Commits through `28ddd10b2`. One failed prediction kept (v71 pred_g).
3. **Scorecards.** Pronoun gender: all five properties held at head grain within 70 minutes of opening (fresh, natural
   in-corpus, natural out-of-corpus, random-set null, keep-only, response census, additivity at the bar). Others unchanged.
   The pronoun readout is *direct* (no MLP relay port), unlike both auxiliary families.
4. **Local-direction checks.** (a) new behaviour — yes, but opened only after the atlas named it as the largest unclaimed
   family, and the previous paths are at their declared head-grain limit; (b) no replay polishing; (c) no fits; (d) next
   highest-information step: the family claim itself — does the same core carry a second pronoun decision (number it/they)
   on fresh rows with the same battery? That is what makes it a collection rather than one more line; (e) no collision:
   Codex's regional heads 8.2/9.8/17.2 are absent from every pronoun set; Codex's lane-1 jobs ran between my short jobs.
5. **Kill criteria.** If pronoun number's atlas set {9.6, 12.4, 15.1, 10.5} fails the fresh-row battery, the "pronoun
   family" is recorded as gender-only co-occurrence. The gender line's own kill (frozen band) fired upward once and is
   recorded; a failure on a fresh re-frozen panel would reopen Predicts OOD.
6. **Decision: continue.** Next action: `dod_battery` on pronoun number it/they (fresh nouns, three unused frames, frozen
   from the atlas), then a pattern fold at the noun position for the gender set.

## B. Efficiency
1. **Time (box clock, 01:32–02:07).** ~8 min waiting (atlas remainder 7 min, five short runs < 1 min each), ~20 min
   writing runners/records, ~5 min analysis, ~2 min ceremony. Science dominated; the battery module paid off: five
   receipts from four runner files, v75 a 30-line wrapper over v73.
2. **GPU.** Atlas remainder ≈ 16.6k forwards (23 decisions); v71–v75 200 forwards, every one decision-changing.
3. **Repeated work.** Two enqueue refusals (gate reads literal dict keys; battery-delegating runners had none) — fixed and
   noted in `dod_battery.py` and memory. The miner was parameterized by source rather than copied.
4. **Improvement executed:** `pronoun_gender_dod_natural_rows.py` takes a source argument (fineweb | pile) and v75 imports
   v73's body. **Scheduled:** lift v73's body into `dod_natural_line.py` (LineSpec + rows file → natural-row receipt) so the
   remaining lines get FineWeb/Pile panels from a 20-line runner each.
