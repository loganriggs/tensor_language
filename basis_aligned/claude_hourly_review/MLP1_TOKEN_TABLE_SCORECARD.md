# MLP 1 as a token table — scorecard (Claude circuit lane, opened 2026-09-19 02:10 UTC at Logan's direction)

Question: what does MLP 1 do, described in a basis-free way from the embedding up, and why does it look diffuse at unit grain?
Prior record: 79% context-free lookup table (RESULTS §250); corpus output = one register direction + long tail (§§16–17); plural − singular write
at a noun rank-1 in context (tier-5 v167); no linear-plus-norm quotient for the temporal readers (exact commutator proof). Rows: single tokens at
position 0 (the context-free fold), seven classes from `ops/dod_token_classes_v287.json`, sizes 4 / 16 / 64 / 256 (Logan: exponential scaling).

| # | claim | kind | rows | registered / outcome | score — reading |
|---|---|---|---|---|---|
| T1 | Single-token MLP-1 table over 1110 tokens (closure of x1 as four writers exact): r90 per class ≈ 0.7 × n at every size (4 → 3, 16 → 11–13, 64 → 33–46, 256 → 131); union r90 18 / 64 / 164 / 271 at 28 / 112 / 418 / 1110 tokens (ratio 256-per-class vs 64-per-class 1.65); between-class share of variance 0.55 / 0.34 / 0.29 at 4 / 16 / 64 (at 256 only one class qualifies — artefact); plural − singular differences over 126 lexicon pairs: top-1 energy 0.42 (0.56 / 0.45 / 0.40 at 4 / 16 / 64 pairs), r90 = 89; mean difference cos 0.72 with the embedding's plural direction, median norm ratio 172; verb past − present: top-1 0.39, cos 0.70, ratio 218; write norm 9182 vs residual 1323; radial share 0.21 | fold | single tokens (v287) | closure, "rank scales sub-linearly (≤ 2×)", "amplifies the embedding's number direction (≥ 1, cos ≥ 0.3)" held; "classes separable ≥ 0.30 at 64" (0.29), "number difference rank-1 ≥ 0.50" (0.42), "radial ≤ 0.10" (0.21) false | 3/6 — context-free, MLP 1 is mostly a TOKEN-SPECIFIC expansion (near full rank per class; 7× the residual norm) with a modest shared subspace where class features live (a plural direction aligned with the embedding's and amplified 170×, top-1 42%) and a fifth of its energy along the residual. The unit-grain "diffuseness" is real at the token level, not a basis artefact; v167's rank-1 number direction is the in-context, 16-pair view of a 42% top direction |
| T2 | MLP 1's write at the noun of the v76 rows (position 1, after "The") vs its single-token table entry (position 0, alone; closure of x1 exact): median cosine 0.87 (min 0.77), median relative error 0.89; in-context norm 5796 vs table 9294 (0.62×); plural − singular difference: mean-direction cosine 0.87, pairwise median 0.83, the table's difference 1.6× the in-context norm; the x1 gap between contexts is 64% attention (attn 0 + attn 1) and 36% MLP 0 by writer norm | fold | v76 rows + 32 single tokens (v288) | closure held; "table matches the write (err ≤ 0.25, cos ≥ 0.95)", "number difference matches (cos ≥ 0.90, norm share ≥ 0.75)", "gap ≥ 0.90 attention" false | 1/4 — the context-free table gives MLP 1's DIRECTION (cos 0.87 for the write and for the number contrast) but not its MAGNITUDE: one context token cuts the write by 38% and the number contrast by 1.6×. MLP 1 on these rows = token-keyed direction × context-set gain; the gain enters through attention 0 / 1 and MLP 0 (which reads attention 0). v289 decomposes the change exactly into token × context and context × context terms |
| T3 | Exact bilinear decomposition of the change W_ctx − W_table at the noun (input n = t + c, c the normalised-input change from "The"): closure 0.0; cross terms (token × context) carry 0.76 of the change's norm, context × context 0.24; the change projects on the table entry: best scalar α median 0.54 (in-context write = 0.54 × table along the table direction), residual energy after the scalar 0.23 (median); α across the 32 nouns: range 0.44–0.65, coefficient of variation 0.09 | fold | v76 rows + 32 single tokens (v289) | 4/4 held | MLP 1's write at the noun is its token-table entry times a context GAIN (≈0.54 here) set by the cross terms, uniform across tokens (CV 0.09), with a 23% non-scalar remainder. Reading: table = direction, context × token cross terms = gain. v290 asks whether the gain depends on the context token and how it scales with class size |
| T4 | Context gain across 6 context tokens (The , and of very 1) × 7 classes × sizes 4/16/64 (two-token inputs, fold exact): α median 0.41–0.65 in every one of the 42 cells; CV across targets ≤ 0.20 everywhere and stable with size (CV(64) ≤ 1.5 CV(16) for all classes); cross-term share 0.77; residual after the scalar median 0.28 but up to 0.63 (The + function words; punctuation and function words 0.3–0.6); the spread of α ACROSS contexts (std 0.046) is 0.92× the within-cell spread | fold | 2688 two-token rows + 448 tokens (v290) | 3/5: closure, uniformity within class, stability with size held; "scalar reading holds every class" (≤ 0.30) and "gain is set by the context" (≥ 2×) failed | The gain is NOT set by which context token precedes: it is ≈ ½ for every context and every class. That points to position, not content: with two tokens the squared-bilinear attention splits between two positions, so the target's own value share in x1 drops. The scalar reading is good for content words (residual 0.12–0.30) and poor for function words / punctuation. v291: context LENGTH 1/2/4/8 filler tokens; if α ≈ 1/(k+1) the gain is the attention self-share |
| T5 | Context LENGTH 1/2/4/8 filler tokens before 7 classes × 32 targets (fold exact): gain α median 0.555 / 0.440 / 0.365 / 0.335; the target's own-key share of the bilinear attention pattern (|p_kk| / Σ|p_kj|, mean over 9 heads, blocks 0+1) 0.525 / 0.399 / 0.279 / 0.210 (single token: 1.0); α(k)/α(1) vs s_k/s_1 gap 0.03 / 0.13 / 0.20; α's CV across the 7 classes 0.12–0.19; cosine(write, table) 0.86 / 0.83 / 0.78 / 0.77 | fold | 896 rows + 224 tokens (v291) | 4/5: closure, monotone fall, tracks self-share (within 0.25), class-uniform held; "direction kept ≥ 0.80 at every length" failed at 4 and 8 (0.78, 0.77) | The gain IS the attention self-share: MLP 1's write at a token = (its own-key attention share) × (its table entry) + a remainder. With one preceding token the target keeps ~half the attention row, so the write halves; the share falls as 1/(context) and α follows, flattening above it at 8 tokens (0.33 vs 0.21) as the remainder grows and the direction drifts (cos 0.77). Reading: MLP 1 amplifies the token-identity part of x1 (the part attention 0/1 kept from the token's own value); the non-table remainder must be the context's contribution — v292 tests whether it is the context's own table write |
| T6 | Two-term test, context length 1/2/4/8/16/32/64 (cyclic ", and of the very"): is the non-table remainder the context's own MLP-1 write (its write at the last filler position, run alone)? Remainder cosine with the context write −0.14 / −0.02 / −0.04 / 0.02 / 0.02 / 0.05 / −0.02; projection coefficient on it β ≈ 0 (−0.06…0.04); span{table, context write} explains 0.68 → 0.38 of the write's energy (8: 0.60, 64: 0.38); α 0.58 / 0.44 / 0.37 / 0.33 / 0.28 / 0.30 / 0.29 (floor ≈ 0.29 from 16 tokens on); cos(write, table) 0.86 → 0.61 | fold | 1568 rows + 224 tokens + 7 filler runs (v292) | 2/5: closure and "gain floors ≥ 0.20" held; "remainder is the context write", "two-term explains ≥ 0.85", "β rises" failed | FALSIFIED: the remainder is neither the token's table write nor the context's table write; it is orthogonal to both. By v289 it is the token × context bilinear CROSS term Down[(L t)(R c) + (L c)(R t)] — a direction MLP 1 makes from the pair, not from either alone. The token's own term floors at α ≈ 0.29 over 16–64 tokens (the self-share floor of attention 0/1), while the cross term grows to 60% of the energy. Next (v293): what the cross remainder is — rank across tokens, class coherence, shared direction |
| T7 | The cross remainder R = W − α·table at context length 1 and 8 (224 targets): energy share of the write 0.26 / 0.40; r90 54 / 72 vs table 76 (high-rank, token-specific); the grand-mean remainder carries 0.40 / 0.37 of its energy (one shared direction); leave-one-out class-mean cosine 0.80 / 0.76 vs other-class 0.51 / 0.47 (gap 0.28); class-mean cosines: nouns–vocab nouns 0.90–0.92, nouns–verbs 0.73–0.77, nouns–adjectives 0.77–0.80, numbers / punctuation / function words 0.42–0.65 from the rest; cross-term share of R 0.65 / 0.66 | fold | 448 rows + 224 tokens (v293) | 3/5: closure, high rank, class structure held; "no single shared direction ≤ 0.30" (0.40, 0.37) and "cross ≥ 0.70" (0.65, 0.66) failed | The remainder is two things: ~40% ONE shared direction (context-set, token-independent, orthogonal to both table entries) and ~60% token-specific directions that carry the class structure (content words cluster, numbers / punctuation / function words apart). Cross and context-only terms both contribute (65/35). Reading so far: MLP 1 write = self-share × table(token) + shared in-context direction + class-structured cross term. v294: is the shared direction one fixed direction across context phrases and lengths, and is it MLP 1's dominant corpus direction (dossier: one register direction)? |
| T8 | The shared remainder direction u(phrase, length) over 3 phrases (A ", and of the very"; B " I said that we"; C " 1 2 3 4 5") × lengths 1/8/64: grand-mean share stable 0.31–0.43, but u is NOT one direction: min |cos| across lengths 0.26 (A) / 0.62 (B) / 0.57 (C); across phrases at length 8 0.40; |cos| with the corpus PC1 of MLP 1's writes (2,944 natural positions; PC1 = 16.5% of energy, = the mean write, cos 0.99) 0.02–0.16; median |cos| with table entries 0.10–0.23 | fold | 2016 rows + 224 tokens + 2,944 natural writes (v294) | 1/5: only closure held; fixed across lengths / phrases, corpus-PC1 identity, orthogonality to the table all failed | FALSIFIED: the shared part of the remainder is not the dossier's register direction and not any fixed direction of MLP 1; it is CONTEXT-SPECIFIC and token-independent. Given v293's split (cross 0.65 / context-only 0.35 ≈ grand-mean share 0.37–0.40), the candidate identity is the context-only bilinear term Down[(L c)(R c)] with c the attention-mixed context input; v295 tests it |
| T9 | Is the shared remainder the context-only bilinear term Down[(Lc)(Rc)]? Phrase A, lengths 1/8/64: the context input change c is only partly token-independent (median cos with its mean 0.81 / 0.75 / 0.78); cos(grand-mean R, mean context-only term) 0.40 / 0.28 / 0.09; the context-only term's energy is 0.49 / 1.32 / 1.22 of R's (it is LARGER than the remainder at 8+ tokens, so cross and context-only terms largely cancel); the table-removed cross term has a shared part 0.27–0.33; the three named terms (α·table + mean context-only + cross) leave 0.16 / 0.48 / 0.53 of the write's energy unexplained | fold | 672 rows + 224 tokens (v295) | 1/5: only closure held | FALSIFIED: the shared direction is not the context-only term; the term-wise expansion is not the description — cross and context-only terms are large and cancelling, so neither is a meaningful part on its own. What stands from v288–v295: write = (attention self-share) × table(token) + a context-specific remainder (25–40% of energy at 1–8 tokens) that is neither table entry nor a single bilinear term. Next: which part the number chain reads (v296: MLP-3 unit 3465's exact gradient on the noun write, split α·table vs remainder) |
| T10 | Which part of MLP 1's noun write the number chain reads: on the v76 rows, W = α·table + R (α median 0.54), each part's exact product-level carriage of the plural − singular contrast into MLP-3 units 3465 and 493 (λ chain folded; closure 0.0): table part 0.74 / 0.75 of MLP 1's total carriage, remainder 0.38 / 0.49, mixed quadratic term −0.12 / −0.24; the table part's per-pair contrast has the total's sign for 100% of pairs (both units) | fold | v76 rows + 32 tokens (v296) | 3/5: closure, "table part ≥ 0.70", "sign every pair" held; "remainder ≤ 0.30" failed (0.38; 493: 0.49) | The number chain reads the context-free TABLE part of MLP 1: three quarters of what units 3465 / 493 receive from MLP 1 is α × (the noun's single-token lookup entry), scaled by the noun's attention self-share. The remainder carries more than predicted (0.4–0.5, partly cancelled by the mixed term), so the context-specific part is not number-neutral. Standing description of MLP 1 on this path: a token-identity amplifier — it writes the token's lookup entry (~170× the embedding's number direction, v287) times the share of attention 0/1 the token keeps on itself, plus a context remainder; what the pronoun-number circuit reads from it is the amplified lookup |
| T11 | OOD: the self-share law on natural text — 2,944 positions of the 128 natural rows (FineWeb + Pile): α (projection of MLP 1's write on the token's table entry) vs s (own-key share of the bilinear pattern, blocks 0+1, all positions in one hooked pass): Pearson r 0.65 (alphabetic word tokens 0.72, other tokens 0.52); median |α − s| 0.09; α median 0.26; cosine(write, table) median 0.64 (words 0.64, rest 0.59); instrument closure 0.0 | fold, natural rows | 2,944 natural positions + ≤ 2,944 tokens (v297) | 5/5 held | The law survives natural text: MLP 1's write at a token in running text is its context-free lookup entry scaled by how much attention 0/1 the token keeps on itself (r 0.65, gap 0.09), better for content words. Direction retention 0.64 means a third of the write's energy on text is the context remainder. Next (v298): which heads of blocks 0/1 set the self-share |
| T12 | Which heads set the self-share (phrase A, 8 tokens, 224 targets; natural text for shares): per-head context reading 1 − s_h is spread (0.58–0.95; top-4 of 18 carry 0.26); replacing ONE head's pattern row by its own-key entry moves α by ≤ 0.15 (heads 1.1 and 1.8 +0.15 each; 1.5 +0.04; 0.3 −0.08; 0.5 −0.03; the rest |·| ≤ 0.02; head 1.4 already reads 78% itself and its edit changes nothing); replacing ALL 18 heads' rows gives α = 0.999 (the write becomes the table entry exactly); the per-head changes sum to ≈ 0 against the joint 0.66 (closure failed by 0.44); attribution vs context reading r = −0.16; top-4 context-reading heads shared between filler and text: 2 of 4 (0.1, 0.4). Run 1 void (hook layout bug; preserved as `*_VOID_run1_result.json`) | edit (pattern rows) | 224 filler rows + 2,944 natural positions (v298) | 1/5: only "same heads on text" held | No self head: the gain is a JOINT property of the 18 heads — with every head reading only itself MLP 1 reproduces the table entry exactly (α 1.00), but no single head's context reading accounts for the loss, and their effects do not add (head 0.3's context reading raises α). The share law (v291/v297) holds for the averaged pattern, not head by head. Next (v299): the α floor at long context — is it the λ1·x0 embedding re-injection, which carries token identity past attention? |
| T13 | Is the α floor the embedding re-injection λ1·x0? Phrase A, lengths 1/8/64, native vs the re-injection zeroed in blocks 0–1 (table recomputed under the same edit): native α 0.555 / 0.335 / 0.291 with re-injection norm share of x1 flat at 0.29 / 0.25 / 0.27; edited α 0.918 / 0.470 / 0.270 (drop at 64 only 0.02), edited |α − s| 0.43 / 0.28 / 0.16, edited cos(write, table) 0.59 / 0.45 / 0.25 | fold + edit | 672 rows × 2 conditions (v299) | 2/5: closure held; "floor is the re-injection" (drop ≥ 0.10) failed; "share law exact without re-injection" failed; "re-injection share grows" failed (flat); the floor-match arithmetic held (0.36 vs 0.29) but its mechanism is falsified by pred_b | FALSIFIED: the floor is not the λ1·x0 bypass — removing it leaves α at 64 tokens unchanged (0.27) and instead destroys the direction (cos 0.25) and the short-context law (α 0.92 at one token). The re-injection stabilises MLP 1's input direction; it does not set the floor. The floor's writer is still open (MLP 0's own-token part is the remaining candidate; v300 decomposes x1's token-identity content by writer) |
| T14 | Token-identity content of MLP 1's INPUT x1 by writer (projection β on the target's single-token x1 direction, in units of the table input's norm; exact writer split, closure 4e-8): β 1.17 / 1.32 / 1.20 at 1 / 8 / 64 tokens (the input keeps MORE than its table input along that direction); by writer: attn1 0.67 / 0.85 / 0.77, mlp0 0.41 / 0.38 / 0.34, x0 terms 0.09, attn0 0.000 (block-0 attention writes nothing along it at any length); Pearson r(β, α) = −0.36; ‖x1‖ / ‖table x1‖ 1.22 / 1.39 / 1.30; MLP 0's own-table gain α0 0.85 / 0.80 / 0.69 | fold | 672 rows + 224 tokens (v300) | 2/5: closure and "MLP 0 keeps ≥ 0.5 of its own lookup" held; "input identity tracks α", "attention identity tracks self-share", "floor writer is MLP 0" (it is attention 1) failed | FALSIFIED: MLP 1's gain is not a loss of token identity in its input — x1 keeps ≥ 1.0 of the table input's direction at every length (normalised: 0.97 / 0.95 / 0.93 of the unit direction). So the output's identity loss must come from the bilinear CROSS terms between the token input and the context input projecting NEGATIVELY on the table entry (v289: cross = 76% of the change; the change = −0.46 T). MLP 1 suppresses its own lookup in proportion to the context it sees. v301 tests the sign and size of that cross projection directly |
| T15 | The gain as a cross-term cancellation (exact three-term expansion of MLP 1's write with the normalised input split into its table-input component γ·t̂ and the rest c): γ² = 0.93 / 0.90 / 0.86 at 1 / 8 / 64 tokens (the table's own quadratic term stays near full); the cross term's projection on the table entry −0.44 / −0.75 / −0.75, NEGATIVE for 100% of rows; the context-only term's projection +0.07 / +0.19 / +0.20; α = γ² + both (0.555 / 0.335 / 0.291); cross projection vs −‖c‖ Pearson 0.68; identity closure 1.1% (float32 over 4608 units; bar 1e-3 missed); attention 0's write norm 227–366 (real, but orthogonal to the token direction per v300) | fold | 672 rows + 224 tokens (v301) | 3/5: "table term near full", "cross negative ≥ 90%", "tracks context size" held; closure bar (1e-3) missed at 1.1%; "cross alone is the loss" failed because the context-only term restores +0.19 at 8+ tokens | MECHANISM FOUND. MLP 1's in-context write = γ²·(token lookup) − (token × context cross term, which points AGAINST the lookup by 0.44–0.75 of it and grows with the context input) + (context² term, +0.07–0.20 along the lookup, plus the off-lookup remainder). The gain α is not identity loss in the input (v300) but active self-cancellation: the bilinear form's cross terms between the token and whatever attention 0/1 mixed in subtract from the lookup. This is why α tracks the attention self-share (v291/v297): the self-share sets ‖c‖ |
| T16 | The cancellation on natural text (2,944 positions, exact three-term expansion per position; decomposition closes to 1.8%, instrument 0.0): γ² median 0.85 (words 0.85, other tokens 0.82); cross projection on the lookup −0.87 (words −0.85, other −0.93), negative at 99.3% of positions, deepening with position −0.51 (pos 1) → −0.75 (pos 3) → −0.9 (pos 8+); context² projection +0.29; α 0.26 (words 0.27, other 0.20); Pearson r(cross projection, −‖c‖) 0.05 | fold, natural rows | 2,944 natural positions (v302) | 4/5: table term, negative cross, decomposition closure, instrument held; "tracks the context input's size" (r ≥ 0.40) failed (0.05) | On text the cancellation is near-complete and SATURATED: from position 8 on the cross term removes ~0.9 of the lookup, so what MLP 1 still writes along the token's entry (α ≈ 0.26) comes mostly from the context² term (+0.29), not from the surviving lookup (γ² + cross ≈ 0). The size of the context input no longer predicts the depth of the cut on text (it did on filler, r 0.68): with real context the cross term is saturated, not proportional. Next (v303): is the cancellation carried by a few MLP-1 units or spread over all 4,608? |
| T17 | Unit census of the cancellation (exact per-unit split of the lookup and cross projections on the unit lookup direction; closure 3e-7), phrase A, 1/8/64 tokens, 224 targets: per-unit pooled lookup vs pooled cross Pearson −0.96 / −0.99 / −0.99 (the units that write the lookup are the units that cancel it); 89–93% of units cancel; the cancellation is heavy-tailed but not sparse — top-10 units 34 / 36 / 39%, top-200 51 / 52 / 55%, top-2000 88–90%; units 3289 and 624 alone ≈ 30% of the cancellation (each ≈ 9% of the lookup and ≈ 15% of the cut); per-unit ratio cross/lookup median −0.44 / −0.74 / −0.77 with interquartile range 0.45 / 0.57 / 0.78; top-200 sets at 1 and 64 tokens share 109 | fold | 672 rows + 224 tokens (v303) | 3/5: closure, "lookup units are the cancelling units", "same units across lengths" held; "spread ≤ 0.50 of top-200" failed (0.51–0.55) and "per-unit ratio uniform (IQR ≤ 0.50)" failed at 8 / 64 | The cancellation is a layer-wide law with a heavy head: every lookup unit cancels its own contribution by a token-and-context-dependent ratio (median −0.4 to −0.8), and two units (3289, 624) carry a third of it. Not sparse in the unit basis, but the head is worth naming: v304 asks what 3289 / 624 read (a token-constant factor times a context factor would be a literal gain unit) |
| T18 | What the head units read (3289, 624; also 1715, 3804): over 224 single tokens both factors of 3289 and 624 are token-constant (coefficient of variation L 0.23 / 0.17, R 0.20 / 0.23; means ≈ ∓20 with opposite signs, so the product is a large negative constant); across 672 in-context rows both factors track the attention own-key share (3289: L −0.62, R +0.70; 624: L +0.79, R −0.70); write directions against the mean lookup direction cos −0.60 / −0.49 (bar 0.50 missed by 624); on 2,944 natural positions the activation correlates with α at −0.49 / −0.55 (1715 −0.39, 3804 +0.51); instrument closure 1e-6 | fold, natural rows | 672 rows + 224 tokens + 2,944 positions (v304) | 4/5: closure, token-constant factor, context factor tracks share, gain-on-text held; "writes the common lookup direction ≥ 0.50" failed for 624 (0.49) | NAMED COMPONENT: units 3289 and 624 of MLP 1 are context-GAIN units — they do not read which token is present; each factor reads "a token is here" (constant) shifted by how much context attention mixed in, and their write points against the common component of the lookup table. They carry ≈ 30% of the self-cancellation (v303); the other 70% is the layer-wide per-unit cancellation. Logan's gain hypothesis is right for this head and wrong as the whole story. v305: zero 3289 + 624 (random-pair nulls) — α should rise ≈ 0.1 and the number margin move |
| T19 | EDIT of the head units: zero 3289 + 624 at every position (12 seeded random 2-unit nulls). Phrase A, 8 tokens: α baseline 0.335 (replays v291 to 3e-4) → edit 0.321 (−0.013; null max |Δ| 0.0003); cosine(write, table) 0.773 → 0.754. v76 pronoun rows: oriented they − he margin 2.048 → +0.070 (+3.4%; null max 0.0025, i.e. 28× the null) | edit + null | 224 filler rows + v76 rows (v305) | 2/5: replay and "direction kept" held; "α rises ≥ 0.10" FAILED (it falls 0.013); "rise beats nulls" failed; "margin moves beyond null" held (+3.4%) | FALSIFIED as a causal claim: removing the two "gain units" does not restore the lookup — their net along-entry write (lookup + cross + context² together) is slightly positive, so the cross-term census (v303) named them by one term of three. Same lesson as v295 at unit grain: cross and context² terms cancel inside a unit; only the NET per-unit write is causal. The pair does move the number margin (+3.4%, far beyond the null), which is a real but separate effect to be understood. v306: census of the NET per-unit along-entry change between context and isolation, then edit its head |
| T20 | NET per-unit census of the context change δ_j = (D_j·T̂)(h_j(context) − h_j(alone)), which sums exactly to (α − 1)‖T‖ (closure 1e-6): 92–96% of units lose; per-unit lookup (alone) vs net loss Pearson −0.97 / −0.99 / −1.00; top-200 carry 0.47–0.48, top-2000 0.85–0.87 (spread); units 3289 and 624 are rank 1 and 2 at every length: alone they write +867 / +664 along the entry per row, in context they lose −807 / −621 (8 tokens), i.e. they are cancelled to ≈ +60 each; the pair = 22% of the net change; 1715 third (−243 of +288); top-200 sets at 1 and 64 tokens share 119 | fold | 672 rows + 224 tokens (v306) | 4/5: closure, spread, lookup-units-lose, stable head held; "3289 / 624 outside the top-200" FAILED — they are the top two | CORRECTS T19's reading: 3289 and 624 ARE the largest net cancellers. v305's zeroing edit was the wrong counterfactual — zeroing removes the unit's whole in-context write, which is already ≈ 0 along the entry (+60 of +867), so α could only fall slightly (−0.013 observed; ≈ −0.014 predicted from these numbers). The edit that tests "this unit cancels the lookup" is to REPLACE its in-context activation with its single-token one (v307): predicted rise ≈ 0.15 for the pair at 8 tokens |
| T21 | REPLACE-edit: units 3289 + 624 given their single-token activations at the target (phrase A, 8 tokens; 12 random pairs as null): α 0.335 → 0.487 (+0.152; census-predicted +0.169; null max 0.0004 — 380× the null); cosine(write, table) 0.773 → 0.832; on the v76 rows at the noun the oriented they − he margin moves −0.014 on 2.048 (−0.7%; 7.6× the null of 0.0018) | edit + null | 224 filler rows + v76 rows (v307) | 5/5 held | EDITS DECIDE: the pair IS the head of MLP 1's context-gain — putting back what context took from just these two of 4,608 units restores 0.15 of the lookup's 0.67 loss (22%), as the net census said, with the direction improving. Their downstream effect on the number margin is tiny and negative (−0.7%): the number chain reads the token entry's direction, not its gain (consistent with rms-normalised readers). Component named: MLP-1 gain head {3289, 624}: token-constant factors × attention self-share, writing against the common lookup component |
| T22 | Dose–response: restore the top-k net-census units' single-token activations (phrase A, 8 tokens; 4 random k-sets per k as null): rise in α for k = 2 / 10 / 50 / 200: 0.148 / 0.205 / 0.242 / 0.317 (baseline 0.335; loss 0.665) vs null max 0.0001 / 0.0014 / 0.0051 / 0.023; cosine(write, table) 0.773 → 0.832 / 0.838 / 0.862 / 0.893 | edit + null | 224 filler rows (v308) | 5/5 held — but pred_b ("rise = census") is an identity, not a test: Down is linear, so replacing a set's activations changes the write by exactly the census quantity; the informative predictions are the nulls (random sets of the same size do 0.1–7% of the effect) and the monotone direction gain | The net-census ordering is causal down the list: 4% of MLP 1's units (200) hold 48% of what context takes from the token lookup, the top 10 hold 31%, and restoring them repairs the direction as well as the gain. Random sets of the same size do nothing. Open: the same census on natural text (v309, queued) |
| T23 | OOD: the net per-unit census on 2,944 natural positions (closure 0.0): 98.9% of units lose; per-unit lookup vs net loss Pearson −0.998; units 3289 and 624 rank 1 and 2 (per position: 3289 writes +1076 alone → loses −1046 in context; 624 +582 → −565; 1715 +370 → −326), the pair 22.4% of the net change (filler: 22%); top-10 31%, top-200 46%, top-2000 82% | fold, natural rows | 2,944 positions (v309) | 5/5 held | The MLP-1 gain head {3289, 624} generalises to text unchanged: same two units, same share, the same layer-wide proportional loss. On text the head units are cancelled to ≈ 3% of their single-token value — MLP 1's strongest constant-factor units are switched off by any real context. Open: what this does for the model's predictions (v310: restore the head on text and read the next-token loss against random pairs) |
| T24 | What the gain head does for the model: next-token loss on the 128 natural rows (2,944 predicted positions; manual forward replays the native loss 4.2209 exactly): restoring units 3289 + 624's context-free activations at every position raises the loss by +0.059 nats (12 random pairs: ≤ 0.0002 — 300× the null), at 58% of positions, growing with position (positions 1–6 +0.024, 12–23 +0.081); zeroing the pair changes the loss by −0.0004 | edit + null, natural rows | 2,944 positions (v310) | 5/5 held | The cancellation is doing work: the model depends on MLP 1 switching its two constant-factor units off in context — putting back what they would write with no context costs 0.06 nats and more the longer the context — while their in-context write is already inert (zeroing is free). Functionally, the head is a first-token / no-context signal that MLP 1 must not emit once context exists |
| T25 | Loss dose–response on text: restoring the top-2 / 10 / 50 / 200 net-census units' context-free activations costs +0.059 / +0.106 / +0.125 / +0.156 nats (random sets of the same size: ≤ 0.0003 / 0.0005 / 0.0016 / 0.030); the sets hold 22 / 31 / 37 / 46% of the cancellation, so the cost per unit share is 0.27 / 0.34 / 0.34 / 0.34 nats — a stable price, extrapolating to ≈ 0.34 nats for the whole cancellation (8% of the 4.22 loss) | edit + null, natural rows | 2,944 positions (v311) | 4/5: replay, monotone growth, beats nulls at every k, stable price held; "top-200 costs ≥ 0.20" failed (0.156) | The cancellation is priced linearly: every unit share of the lookup that context takes away is worth the same ≈ 0.34 nats to the model. MLP 1's context behaviour is one coherent function — suppress the context-free lookup in proportion to context — not a collection of unit quirks. v312: the whole-layer version (replace MLP 1's write by its table entry everywhere; and zero MLP 1) to close the price |
| T26 | Whole-layer price on text (2,944 positions; manual forward replays the native loss 4.2209 exactly): replacing MLP 1's write everywhere by the token's context-free table entry costs +0.70 nats (positions 1–6: +0.31; 12–23: +0.95; position 1 alone: +0.24, rising to +1.26 at position 22); removing MLP 1's write entirely costs +0.41 nats; the table is worse than nothing at all 22 positions | edit, natural rows | 2,944 positions (v312) | 2/5: replay and growth-with-position held; "full restore within 2× of 0.34" failed (0.70); "zeroing costs more than the table" failed (0.41 < 0.70); "table beats nothing at every position" failed (0 of 22) | The uncancelled lookup is actively harmful: MLP 1 with its context-free entries is worse for the model than no MLP 1 at all, and the gap widens with context. The price is superlinear in the census — the 54% tail beyond the top-200 costs ≈ 0.54 nats (≈ 1.0 nats per unit share vs 0.34 for the head), so the tail's in-context activations are not merely 'switched off'; they carry the context-dependent write the model needs (the class-structured remainder of T7). Reading: MLP 1 is a token lookup whose raw output the model cannot use in context; blocks 0–1 attention lets it convert the lookup into a context-conditioned write, and that conversion is worth 0.7 nats. v313: tail-only restore and additivity |
| T27 | Head vs tail of the net census on text (2,944 positions): restoring the top-200 units costs +0.156 nats, the other 4,408 (the tail, 54% of the cancellation) +0.473, all 4,608 +0.701 (head + tail = 0.629: superadditive by 0.07); per unit share of the cancellation head 0.34 vs tail 0.88 nats (2.6×); tail cost grows with position 0.22 (1–6) → 0.63 (12–23) | edit, natural rows | 2,944 positions (v313) | 5/5 held | Confirms v312's reading: the tail's in-context activations are the valuable part — worth 2.6× more per share of cancelled lookup than the head's. The head (incl. 3289 / 624) is mostly a switch-off; the tail is the context-conditioned write. The two interact downstream (superadditive). Description of MLP 1 now complete at this grain; remaining open item: what the tail / class-structured write encodes for later blocks |
| T28 | Does attention 0/1 alone do the conversion? On the 2,944 natural positions, every head of blocks 0 and 1 reading only its own key: MLP 1's write becomes the table entry (α median 0.9998, p10–p90 0.999–1.001) and the loss rises +0.81 nats (positions 1–6 +0.36, 12–23 +1.12); block-0 heads only +0.19, block-1 heads only +0.31; the table-everywhere edit (v312) cost +0.70 | edit, natural rows | 2,944 positions (v314) | 5/5 held | Attention 0/1's context reading is what converts MLP 1's lookup into the usable write: removing it reproduces the table (α = 1.000) and costs 0.81 nats, of which 0.70 is exactly the table-everywhere cost — attention's other context writes into the residual account for only ≈ 0.11. Block 1 matters more than block 0 (it carries most of x1's identity content, T14). The path Embedding → Attn 0/1 (context read) → MLP 1 (bilinear cross terms) is the whole story of MLP 1's conversion at this grain |
| T29 | Who reads the tail write — per-block patch-back (tail restored; one later block's attention + MLP writes set back to native): recovery share of the 0.473-nat tail cost by block 2…17: 0.89 / 0.89 / 0.88 / 0.83 / 0.67 / 0.59 / 0.49 / 0.59 / 0.40 / 0.44 / 0.30 / 0.23 / 0.16 / 0.17 / 0.07 / −0.08; the shares sum to 7.5 | edit, natural rows | 2,944 positions (v315) | 2/5: replay and the tail cost's replay (0.473) held; "early > late" and "no single block ≥ 0.50" failed — but the instrument is the problem | INSTRUMENT VOID AS A LOCALISER: any single early block patched back recovers ~88% of the cost, and the shares sum to 7.5, so the harm is not read at one place; it propagates as a cascade in which every block's native write pulls the residual back onto its native trajectory (the λ-recurrence re-injects state each block). Block-wise patch-back cannot say who reads MLP 1's tail write. The right instrument is path-restricted: inject the corrupted MLP-1 write into ONE block's input with all other paths native (v316) |
| T30 | Who reads the tail write — path-restricted injection (the corrupted MLP-1 write, λ-chain scaled, delivered to ONE consumer's input with the residual and every other block native): share of the 0.473-nat tail cost taken by block 2: 0.45; block 3: 0.06; block 4: 0.01; block 5: 0.002; blocks 6–17: 0.00 each; direct to the unembedding: 0.0005; the first-order shares sum to 0.53 | edit, natural rows | 2,944 positions (v316) | 5/5 held | Block 2 is the principal reader of MLP 1's context-conditioned write: 45% of the harm of un-conditioning MLP 1 enters through block 2's own reading of the residual, 6% through block 3, and nothing through the direct path to the logits or any block past 5. The remaining 47% is second-order (block 2's corrupted output feeding later blocks), which is what v315's cascade measured. So MLP 1 → block 2 is the local circuit; v317 splits block 2 into attention 2 vs MLP 2 |
| T31 | Block 2 split (the un-conditioned MLP-1 write delivered to attention 2's input only / MLP 2's input only / both; all else native): shares of the 0.473-nat tail cost 0.08 / 0.35 / 0.45 (both replays v316; attn + mlp = 0.43, additive within 0.02); MLP 2's per-unit response change to the delivery is spread — top-10 units 0.9%, top-200 6.9%, top-500 14.7% of the total | edit, natural rows | 2,944 positions (v317) | 5/5 held | MLP 2 is the reader: 35 of block 2's 45 points enter through MLP 2's input, 8 through attention 2, additively. MLP 2's reading is layer-wide (no unit holds even 1%), matching MLP 1's layer-wide writing — the MLP 1 → MLP 2 link at this grain is a dense bilinear map, not a unit-to-unit wire. Open: whether MLP 2 obeys the same lookup × self-share law (v318, queued) |
| T32 | MLP 2 under the same instruments (2,944 natural positions; closure 0.0; three-term decomposition closes to 0.4%): projection of MLP 2's write on its own single-token table entry α₂ median 0.20 (0.34 at position 1 → 0.19 from position 5), cosine 0.35, write norm 0.58× the table's; γ² (its lookup quadratic term after normalisation) 0.40; token × context cross term −0.43 on the entry (negative at 99.4% of positions); context² +0.23; Pearson r(α₂, α₁) 0.72 | fold, natural rows | 2,944 positions (v318) | 4/5: closure, α₂ ≤ 0.60, cross negative, α₂ tracks α₁ held; "direction kept ≥ 0.40" failed (0.35) | MLP 2 obeys the same cancellation law but starts from a weaker lookup: its input already carries mostly context-conditioned content (γ² 0.40 vs MLP 1's 0.85), so its context-free entry explains only a third of its write's direction. The gains of the two layers move together position by position (r 0.72). Reading: the lookup × self-share × cross-cancellation structure is a property of the bilinear MLP stack at the bottom of the model, strongest at MLP 1 and fading by MLP 2 |
| T33 | MLP 2 vs context length (phrase A, 1/8/64 tokens, 224 targets; decomposition closes to 0.4%): α₂ 0.41 / 0.26 / 0.28 (not monotone: 64 > 8); cosine 0.65 / 0.44 / 0.40; γ² 0.69 / 0.50 / 0.39; cross −0.43 / −0.52 / −0.27 (negative for 100% of rows); context² +0.14 / +0.25 / +0.16; context input norm 19 / 24 / 26 | fold | 672 rows + 224 tokens (v319) | 4/5: closure, cross negative, lookup weaker than MLP 1's (γ² ≤ 0.70), floor ≥ 0.10 held; "α₂ falls monotonically" failed (0.26 at 8, 0.28 at 64) | MLP 2 has the same three-term anatomy but its lookup term itself shrinks with context (γ² 0.69 → 0.39; MLP 1's stayed 0.86–0.93): MLP 2's input direction moves away from the token's, because MLP 1 has already conditioned it. MLP 2's cross term is non-monotone in length. The clean lookup × self-share law is an MLP-1 property; MLP 2 is a reader of conditioned content with a residual lookup |
| T34 | Lookup gain by token class (phrase A, 8 tokens, 32 targets per class; closure 1.1%): MLP 1 α by class — nouns 0.43, vocabulary nouns 0.40, adjectives 0.36, numbers 0.33, verbs 0.32, punctuation 0.28, function words 0.26 (spread 0.17); γ² 0.84–0.92 for all classes, so the differences are in the cross term (nouns −0.62 … function words −0.80) and in the context input's size (nouns 9.5, function words 13.2); MLP 2 α — nouns 0.33, vocabulary nouns 0.29, punctuation 0.28, adjectives 0.25, verbs 0.25, function words 0.18, numbers 0.14; class order shared between the layers, Spearman 0.64 | fold | 224 rows + 224 tokens (v320) | 4/5: closure, classes differ, function words and punctuation lowest at MLP 1, shared order held; "numbers keep the most" failed (numbers are mid at MLP 1 and LOWEST at MLP 2, 0.14) | Content words keep more of their lookup than function words and punctuation at both layers; the mechanism is the same everywhere (γ² ≈ 0.9), the difference is how much context input each class's token admits and how strongly its cross term cancels. Numbers lose their lookup fastest by MLP 2 — consistent with the number chain (MLP 3 unit 3465, v296) reading MLP 1's table part rather than MLP 2's write |
| T35 | Lookup gain by token kind on natural text (2,944 positions; alphabetic 2,428 / punctuation 365 / other 107 / numeric 44): MLP 1 α — alphabetic 0.27, numeric 0.28, punctuation 0.21, other 0.18 (γ² 0.80–0.88 for all; cross −0.85 / −0.85 / −0.93 / −0.95); MLP 2 α — alphabetic 0.21, other 0.18, numeric 0.17, punctuation 0.14 (γ² 0.34–0.41; cross −0.35…−0.44, negative at ≥ 94% within every kind) | fold, natural rows | 2,944 positions (v321) | 4/5: closure, alphabetic > punctuation at MLP 1 (+0.06), order kept at both layers, MLP 2 cross negative in every kind held; "numeric lowest at MLP 2" failed (punctuation is lowest; numeric 0.17, n = 44) | The class picture survives text: content tokens keep more of their lookup than punctuation at both layers, with the same anatomy (γ² ≈ 0.85 at MLP 1, ≈ 0.4 at MLP 2; the class difference sits in the cross term). The filler-context claim that numbers lose their lookup fastest by MLP 2 did not replay on text (44 numeric positions; punctuation lower) — kept as a filler-only observation |
| T36 | The number chain at the α = 1 limit (v76 rows; attention 0/1 all-self-only): MLP 1's noun write becomes the table entry (α 0.9993, cosine 1.00; native 0.540 / 0.87 replays v289); MLP 1's exact number carriage into MLP-3 unit 3465 is 0.92× native and into 493 1.01× (table share 1.00 by construction; per-pair signs 100% kept); the oriented they − he margin falls 2.05 → 1.28 (−37%) | edit | v76 rows + 32 tokens (v322) | 3/5: replay, write = table, sign kept held; "carriage rises ≥ 1.3×" failed (0.92×); "margin moves ≤ 10%" failed (−37%) | Doubling MLP 1's lookup gain does not add number contrast into 3465 / 493: the contrast MLP 1 carries is already the table part's direction and the product with the (also changed) block-3 state keeps it flat. The 37% margin drop is NOT attributable to MLP 1 — the edit removes every context read of blocks 0/1 attention for all downstream consumers; v323 applies the α = 1 limit to MLP 1's input only |
| T37 | Only MLP 1 un-conditioned (MLP 1 reads a parallel self-only-attention stream; the residual keeps native attention; everything else native), v76 rows: MLP 1's noun write = table (α 0.9993, cosine 1.00); its number carriage into 3465 / 493 flat (0.93× / 1.01×; per-pair signs 100%); the oriented they − he margin 2.05 → 1.39 (−32%; v322's whole-attention edit gave −37%) | edit | v76 rows + 32 tokens (v323) | 3/5: replay, write = table, carriage flat held; "margin change ≤ 15%" failed (−32%); "≤ half of v322's drop" failed (0.66 of 0.77) | Un-conditioning MLP 1 alone costs the pronoun-number margin a third — and not through unit 3465 / 493's read of MLP 1 (flat). MLP 1's context-conditioned write matters for the behaviour through other consumers (block 2 / MLP 2 by v316–v317 on text), i.e., the circuit uses more of MLP 1 than the entry direction the named MLP-3 units read. The raw lookup is harmful here as on text (v312). v324: path-restricted injection on the pronoun rows with the margin as readout |
| T38 | Routes of MLP 1's un-conditioning to the pronoun-number margin (v76 rows; delta = un-conditioned − native MLP-1 write, delivered to one consumer's input at a time, residual native; full delta replays v323's −0.655): first-order shares by consumer — block 2 0.23, block 4 0.15, block 3 0.12, block 5 0.05, block 7 0.03, blocks 6 / 8–17 ≤ 0.01 each, direct to the unembedding 0.001; the shares sum to 0.59 | edit | v76 rows (v324) | 5/5 held | On the behaviour rows, as on text, MLP 1's context-conditioned write is consumed by the early blocks — 2, 4, 3 in that order (59% first-order; the rest second-order propagation) — and not at all by the logits directly or by anything past block 7. The pronoun-number circuit's use of MLP 1 therefore runs MLP 1 → blocks 2–4 → (the named chain from MLP 3 on), with the MLP-3 units 3465 / 493 reading only the entry's direction. v325: attention vs MLP within blocks 2, 3, 4 |
| T39 | Blocks 2–4 split into attention and MLP consumers of MLP 1's un-conditioning (v76 rows, margin readout; both-input shares replay v324): shares of the −0.655 change — block 2: attention 0.02, MLP 0.44, both 0.23; block 3: attention 0.06, MLP 0.65, both 0.12; block 4: attention 0.06, MLP 0.12, both 0.15 | edit | v76 rows (v325) | 3/5: replay, blocks replay v324, MLP > attention in every block held; "parts additive within 0.05" FAILED (block 2: 0.46 vs 0.23; block 3: 0.71 vs 0.12); "MLP 2 largest" failed (MLP 3 is, 0.65) | The MLPs of blocks 2 and 3 are the consumers, and MLP 3 — the block of units 3465 / 493 — takes 65% of the harm when it alone sees the un-conditioned write; but when attention 3 sees it too, the block's net share collapses to 12%: attention 3's reading of the same delta cancels most of what MLP 3 does with it (strong sub-additivity; the same in block 2: 0.44 → 0.23). MLP 1's conditioned write is used by MLP 3 and protected/compensated by attention 3 |
| T40 | MLP 3's consumption at unit grain (v76 rows; MLP-3-only delivery replays v325's 0.647): per-unit response-write norm to the delta is spread (top-10 2.6%, top-50 6%, top-200 15%, top-500 26%); units 3465 / 493 rank 113 / 72; yet delivering the delta to only the top-10 responding units reproduces 71% of MLP 3's margin effect (top-50 76%, top-200 76%); top units 664, 872, 1612, 615, 919, 2570, 1090, 190, 1250, 1335 | edit | v76 rows (v326) | 3/5: replay ×2 and "top-200 reproduces ≥ 0.60" held; "top-50 carry ≥ 0.50 of the response norm" failed (0.06); "3465 / 493 in the top 50" failed | Concentration is in the EFFECT, not the response: ten MLP-3 units carry 71% of what MLP 1's un-conditioning does to the number margin through MLP 3, while the response energy is spread over hundreds. These ten are not the named number units 3465 / 493 (which read the entry's direction): they are a second MLP-3 port reading MLP 1's context-conditioned remainder. Candidates to name; v327 edits them on native rows against random 10-sets |
| T41 | Edit of the second MLP-3 port on NATIVE v76 rows: zeroing the ten units (664, 872, 1612, 615, 919, 2570, 1090, 190, 1250, 1335) at every position moves the oriented they − he margin +0.063 on 2.048 (+3.1%; 12 random 10-sets: max |Δ| 0.018, so 3.5× the null); zeroing {3465, 493}: −0.046 (−2.2%) | edit + null | v76 rows (v327) | 3/5: replay, beats nulls, larger than the pair held; "≥ 5% of the margin" failed (3.1%); "negative" failed (positive: natively the ten weakly oppose the correct number) | The ten are real (3.5× null) but small on native rows and of the opposite sign: natively they slightly oppose the correct pronoun, and they matter mainly as the channel through which an UN-conditioned MLP 1 would damage the margin (v326: 71% of that harm). So they are not a component of the native number circuit; they are where MLP 1's raw lookup would leak if attention 0/1 did not condition it. Named as such, not as number units |
| T42 | Is the un-conditioning harm number-selective? (v76 rows; MLP 1 alone un-conditioned, replays v323's −0.655): the gender control he − she moves +0.17 on the same rows (0.26× the number margin's change, though the rows do not manipulate gender); final-token KL(native ‖ edited) mean 0.47 nats, median 0.33; top-1 prediction agreement 15%; correct-pronoun log-probability −2.80 → −2.92 | edit | v76 rows (v328) | 5/5 held (the registered reading was 'not selective') | NOT selective: un-conditioning MLP 1 scrambles the final-token distribution (top-1 changes at 85% of rows; KL 0.47 nats) and moves an unmanipulated contrast a quarter as much as the manipulated one. MLP 1's conditioning is a general prerequisite the whole model relies on (v312: raw lookup worse than nothing), not a number component. The pronoun-number circuit's dependence on it is generic, and the MLP-3 'leak channel' (v326/v327) is generic too. Closes the MLP-1 thread for the number circuit: what the chain reads from MLP 1 is the entry's direction (v296), and that is all |
| T43 | Single tokens folded through blocks 0–3 into MLP-3 unit 3465 (each token alone; one forward; closure 0.0): median product h — singular nouns −37, plural nouns −432 (gap 4.2 pooled std; 100% of 64 plurals on the plural side; gap stable across set sizes 16 / 64: −395 / −395, 4: −542), function words −1 (6% plural side), numerals 2–9 / two–nine / 10 / 12 / 20 / 100 median 0.3 (0% on the plural side; each within ±23), " 1" −6.5, " one" +16.6 | fold, single tokens | 260 tokens (v329) | 4/5: closure, plural/singular separation, "one" singular side, set-size stability held; "numerals ≥ 2 read as plural" FAILED (they read as neutral / singular-like) | From the token alone, unit 3465 is a morphological-plural detector of the noun's own lookup — the -s form — not a quantity or numeral detector: numerals do not activate it at all. This is the entry direction the chain reads from MLP 1 (v296), now seen end to end from single tokens with the weights folded: Embedding → attention 0–3 (own key) → MLPs 0–2 → 3465, a fixed function of the token that separates plural from singular nouns at 4 standard deviations |
| T44 | More single-token classes into unit 3465 (same fold; noun medians replay v329): position on the singular → plural axis (0 = singular-noun median, 1 = plural-noun median): irregular plurals (men, women, children, people, feet, teeth, mice) 0.85; -s verbs (runs, walks, makes, takes, goes, says, looks, needs) 0.41; are / were 0.19 vs is / was −0.07; plural pronouns and demonstratives (they, we, them, us, these, those) −0.06; singular pronouns −0.09; -s adverbs / adjectives (always, perhaps, famous, various, serious) −0.08 | fold, single tokens | 60 tokens (v330) | 4/5: closure, replay, irregular plurals plural side (≥ 1 std), are/were below is/was held; "plural pronouns / demonstratives on the plural side" FAILED (−0.06) | Unit 3465 detects NOUN PLURALITY in the token's own lookup, not the -s ending: irregular plurals score 85% of regular ones, -s adverbs score nothing, and plural pronouns score nothing (the pronoun's number is not this unit's business — it is written by the noun and read at the pronoun). -s verbs score 0.41, probably through their noun-plural readings (runs, walks, looks, needs are also nouns); v331 tests unambiguous -s verbs |
| T45 | Ambiguous vs unambiguous -s verbs, mass and collective nouns into unit 3465 (same fold; noun medians replay): position on the singular → plural axis — -s verbs that are also plural nouns (runs, walks, looks, needs, works, plays, calls, turns) 1.13 (every one of the eight between −149 and −650, i.e. plural-noun range); unambiguous -s verbs (seems, becomes, does, has, wants, knows, goes, says) 0.09; mass nouns (water, music, money, information, advice, furniture) −0.11; collective nouns (team, group, family, committee, crowd, army) +0.03 | fold, single tokens | 156 tokens (v331) | 5/5 held | Unit 3465 reads the token's LEXICAL noun-plural sense: an -s token activates it exactly when it has a plural-noun reading (runs, walks…), not when it is only a verb (seems, has…); plurality of sense without plural form (collectives) does not count and mass nouns are singular. Together with v329/v330: a lexical detector of plural-noun form-with-sense, fed by MLP 1's entry, blind to numerals, pronouns and grammatical number elsewhere |
| T46 | Single tokens folded up the whole named chain (blocks 0–8, each token alone; closure 0.0): plural − singular noun gap in pooled std and plural-side fraction — MLP 3: 3465 −4.2 (100%), 493 +3.8 (100%); MLP 5: 1036 +5.4 (100%); MLP 8: 829 +4.4 (100%), 953 −2.1 (100%), 1030 +1.5 (78%); MLP-8 max gap / 3465's = 1.03; axis positions (0 singular, 1 plural) of irregular plurals 0.85 / 0.49 / 0.62 / 0.73 / 0.96 / 0.32, numerals −0.09 / −0.11 / +0.19 / −0.05 / +1.02 / −0.50, plural pronouns −0.06 / −0.03 / +0.49 / +0.46 / +0.95 / −0.50 | fold, single tokens | 161 tokens (v332) | 4/5: closure, 3465 replay, every unit separates ≥ 1 std with the known sign, no sharpening up the chain (1.03×) held; "numerals and pronouns ignored everywhere" FAILED (1036 and 829 give plural pronouns ≈ 0.5; 953 gives numerals 1.0 and pronouns 0.95) | Every named unit of the chain already separates plural from singular nouns from the token's own lookup, at the same strength from MLP 3 to MLP 8 — the chain does not sharpen lexical number, it adds context (the noun → pronoun copy). What changes up the chain is breadth: at MLP 3 the units read plural-NOUN sense only; by MLP 5 / 8 (1036, 829) they also read plural pronouns, and 953 responds to anything plural-ish including numerals. The lexical detector at the bottom, the generalised plural feature at the top |
| T47 | Class-size scaling on 400 VOCABULARY s-pairs (not the hand lexicon), 4 / 16 / 64 / 256 pairs, each token alone through blocks 0–8: gap in pooled std (plural-side fraction) at 256 — 3465 −2.6 (0.96), 493 +2.7 (0.98), 1036 +2.0 (0.89), 829 +1.8 (0.82), 953 −0.06 (0.59), 1030 −0.04 (0.58); 3465's gap grows with size −2.2 / −1.9 / −2.3 / −2.6 (not within 30% from 64 to 256); the lexicon gaps (v332) were 1.5–2× larger | fold, single tokens | 512 tokens (v333) | 1/5: only closure held; "stable from 16", "vocabulary ≥ 0.5× lexicon with the same sign" (953 / 1030 lose it), "≥ 0.85 plural side" (829 0.82), "top-3 order" all failed | Partial replication: the four lower units (3465, 493, 1036, 829) keep separating plural from singular on arbitrary vocabulary s-pairs (2–3 std, 82–98% on the plural side) — the lexicon gave the clean version — while 953 and 1030 do not: their v332 separation was a lexicon-specific effect and they are not lexical number detectors. The vocabulary set is noisier (s-pairs that are not noun pairs), so the bars set from the lexicon were too strict; the measurement had not stabilised by 64 and the 256 point is the one to quote |
| T48 | The chain's named units on the native v76 rows, at the noun and at the final (answer) position (plural − singular rows; gap in pooled std, fraction of aligned pairs with the pooled sign): noun — 3465 −7.3 (100%), 1036 +4.0 (100%), 493 +3.2 (100%), 829 +3.0 (100%), 953 −2.9 (100%), 1030 +2.8 (100%); final — 953 −0.83 (96%), 3465 −0.66 (98%), 1036 −0.19 (40%), 829 −0.16 (54%), 1030 −0.16 (67%), 493 +0.12 (73%); margin replays 2.048 | fold, native rows | v76 rows (v334) | 2/5: instrument and "every unit separates at the noun" held; "1036 / 829 separate at the final position" FAILED (≤ 0.2 std); "3465 / 493 silent at the final" failed (3465 −0.66 with 98% pair sign — a small, consistent echo); "top-3 order = single-token order" failed (3465, 1036, 493 here) | The named units are NOUN-position detectors: in context they separate plural from singular rows 1.5–2× more strongly than from the token alone (3465: −7.3 vs −4.2 std), and none carries the number to the answer position (all < 1 std there; 3465 and 953 leave a faint, sign-consistent echo). The number reaches the final token by attention reading the noun state (heads 9.6 / 11.3 etc., earlier chapters), not by these units firing at the pronoun. Their single-token pronoun breadth (v332) is therefore inert on these rows |
| T49 | The 256 vocabulary s-pairs read at X in the two-token frame "The X" (closure 0.0): gap in pooled std (plural-side fraction) and sharpening vs the token alone (v333) — 3465 −3.8 (1.00; ×1.47), 493 +3.0 (1.00; ×1.13), 1036 +2.4 (0.98; ×1.18), 829 +3.0 (0.98; ×1.71), 953 −0.00 (0.50; ×0.05), 1030 +0.03 (0.54; ×0.83) | fold | 512 two-token rows (v335) | 4/5: closure, 3465 sharpens ≥ 1.3×, 953 / 1030 stay silent, plural-side fraction rises for all four held; "every surviving unit sharpens ≥ 1.3×" failed (493 ×1.13, 1036 ×1.18) | One context token makes the vocabulary separation near-perfect for the four lexical units (100% / 100% / 98% / 98% of 256 plurals on the plural side) — the same sharpening seen on the lexicon rows (v334) — with 3465 and 829 gaining most (×1.5–1.7) and 493 / 1036 modestly; 953 / 1030 gain nothing, confirming they are not number detectors. Context sharpens the detectors it feeds (MLP 1's conditioned write, §4.10) without creating detection where the lexical signal is absent |
| T50 | Context-length scaling 0 / 1 / 2 / 4 / 8 filler tokens (", and of the very" cyclic) before X, 256 vocabulary pairs: 3465 −2.6 / −2.3 / −1.8 / −3.7 / −3.7 std (plural side 0.96 / 0.92 / 0.86 / 1.00 / 1.00); 493 +2.7 / +2.8 / +2.7 / +2.7 / +3.6; 1036 +2.0 / +1.3 / −0.04 / +1.3 / +0.9 (0.89 / 0.80 / 0.52 / 0.91 / 0.79); 829 +1.8 / +1.3 / +1.4 / +2.1 / +2.1 (0.82 / 0.82 / 0.78 / 0.99 / 0.92); 953 / 1030 < 0.35 everywhere | fold | 2,560 rows (v336) | 2/5: closure and "953 / 1030 silent at every length" held; "peak at k = 1–2" (peak at 8), "all four ≥ 1.5 std at 8" (1036 0.87), "≥ 0.90 plural side at 8" (1036 0.79) failed | Not a length effect: the separation depends on WHICH token immediately precedes the noun. After " and" (k = 2) unit 1036 vanishes (−0.04, 52%) and 3465 weakens (−1.8); after " the" (k = 4) both recover and 3465 / 829 reach their best (100% / 99%); after " of" (k = 8) 3465 / 493 are strongest of all. The lexical detectors are sharpened or suppressed by the preceding word — a determiner sharpens, a coordinator suppresses — which is the context-conditioned part of MLP 1's write doing its work at the noun. v337 tests single preceding tokens directly |
| T51 | Twelve single preceding words before the 256 vocabulary pairs (plural − singular gap in pooled std): 3465 — these −4.2, The −3.8, those −3.9, two −3.5, of −2.9, very −2.8, the −2.5, comma −2.3, a −1.6, or −1.6, and −0.5, " 1" +0.4 (sign flips); 493 — a +3.9, very +3.3, or +3.2, The +3.0, and/comma +2.8, " 1" +2.5, of +2.5, the +2.4, these +1.3, those +0.06, two −0.4 (collapses after plural words); 1036 — the +2.7, The +2.4, of +2.3, or +2.0, very +2.0, comma +1.3, and +0.4, these +0.4, two +0.5, a −0.03, " 1" −0.4, those −0.3; 829 — The +3.0, these +2.3, the +2.2, those +2.1, or +2.0, very +1.8, of +1.5, two +1.4, and/comma +1.3, " 1" +0.7, a −1.35 (sign flips); 953 / 1030 < 0.35 for all twelve | fold | 6,144 rows (v337) | 2/5: closure and "953 / 1030 silent for every word" held; "determiners sharpen 3465" (a −1.6 < alone 2.6), "and / or suppress 1036 to ≤ half" (or +2.0), "493 within 2–4 for all" (those 0.06, two −0.4) failed | The lexical detectors are AGREEMENT readers at the noun: 3465 is sharpest when the preceding word licenses a plural (these, those, two, The) and flips sign after " 1"; 493 is its complement — strongest after singular-licensing words (a) and collapsing after plural ones (those, two); 829 flips after " a"; 1036 wants a determiner or preposition and ignores coordinators, numerals and plural demonstratives. None is a pure noun-number detector once a word precedes: each reads the noun's number jointly with the previous word's expectation, exactly what a bilinear unit computes from MLP 1's context-conditioned write. 953 / 1030 remain silent everywhere |
| T52 | Bilinear factor split of the agreement readers on the 2 × 2 design {a, the, these, those} × {singular, plural} × 256 vocabulary pairs (exact two-way variance shares of each factor; closure 0.0): 3465 — L: noun 0.81 / frame 0.02; R: frame 0.51 / noun 0.19 (L medians singular +6…+9 vs plural −16…−32 in every frame; R +13 / +13 after a / the for plurals and −10 / −6 for singulars, but +18…+25 for BOTH numbers after these / those); 829 — L: frame 0.53 / noun 0.14; R: noun 0.49 / frame 0.24; 493 — R: noun 0.74 / frame 0.06; L: noun 0.38 / frame 0.29; 1036 — L: frame 0.47 / noun 0.10; R: interaction 0.32 / noun 0.20; 953 / 1030 — residual 0.65–0.96 (no structure); product-of-medians sign matches the unit's table in all four cells for 3465, 1036, 829, not for 493 | fold | 2,048 rows (v338) | 1/5: only closure held; the four-unit bars (noun ≥ 0.50 with frame < 0.20; frame ≥ 0.30 with noun < 0.30; sign match ×4; same side for 3465 / 493) failed as sets — 3465 alone passes all of them | 3465 is a textbook bilinear agreement unit: one factor is the noun's number (81% of its variance), the other the preceding word's licensing (51%; positive for both numbers after these / those, sign-split after a / the), and their product is plural-detection gated by plural-licensing context. 829 has the same structure with the sides swapped (frame on L 0.53, noun on R 0.49). 493 reads the noun on R (0.74) with a mixed L; 1036 is frame × interaction. 953 / 1030 have no factor structure at all. The number chain's bottom is thus a small set of bilinear agreement detectors, each a (noun number) × (context expectation) product built from MLP 1's context-conditioned write |
| T53 | Class-wise factor split of the agreement units' plural − singular contrast at the noun on the v76 rows (exact: h_p − h_s = ΔL·R̄ + L̄·ΔR; closure 5e-6; margin replays 2.048): share on the L-contrast term / R-contrast term — 3465 0.30 / 0.70; 493 0.28 / 0.72; 1036 0.62 / 0.38; 829 0.57 / 0.43; the pair-wise relative spread of each factor is 2–6× its mean for every unit (R of 3465: 6.4×) | fold, native rows | v76 rows (v339) | 2/5: instrument and factor closure held; "3465's contrast on L ≥ 0.80" (0.30), "829's on R ≥ 0.70" (0.43), "licensing factor constant across pairs" (6.4×) failed | FALSIFIED as a transfer: the factor roles found in the 2 × 2 frame design (v338: 3465's L = noun, R = determiner) do not carry to full sentences — on the rows BOTH factors move with the noun's number, and for 3465 / 493 most of the contrast sits on the factor that read the determiner in the frames. The roles were a property of what varied in that design, not fixed wiring: with a fixed determiner and rich context, the "licensing" factor reads the noun too. Kept: the units are bilinear products whose two inputs both carry number in context; the frame-design labels are retired |
| T54 | Per-writer split of unit 3465's two factor contrasts at the noun on the v76 rows (exact; each writer's term with the pair's own rms; closure 0.0; margin replays): L factor (pooled contrast −1762) — mlp1 0.40, mlp2 0.37, attn1 0.00, attn2 0.10, attn0 0.00, mlp0 0.02, embedding 0.04, attn3 0.07; R factor (+1021) — mlp1 0.41, mlp2 0.17, attn1 0.01, attn2 0.21, attn0 0.00, mlp0 0.03, embedding 0.05, attn3 0.12 | fold, native rows | v76 rows (v340) | 3/5: instrument, writer closure, "MLP 1 + MLP 2 ≥ 0.50 of each factor" held; "embedding ≥ 0.20" failed (0.04 / 0.05); "attention 3 ≤ 0.10" failed for R (0.12) | Both inputs of the agreement unit are MLP-written: MLPs 1 and 2 carry three quarters of the L contrast and three fifths of the R contrast; the embedding's direct terms carry almost nothing (4–5%) — the token's identity reaches 3465 only after MLP 1 has looked it up and MLP 2 has re-read it. Attention 3 adds a tenth to R. v341 splits MLP 1's share per factor into its context-free table part and its conditioned remainder |
| T55 | MLP 1's share of unit 3465's two factor contrasts at the noun (v76 rows), split exactly into its context-free table part α·T (α median 0.54) and its conditioned remainder (closure 7e-8; margin replays): L factor — table 0.81, remainder 0.19; R factor — table 0.85, remainder 0.15 | fold, native rows | v76 rows + 32 tokens (v341) | 4/5: instrument, split closure, "table ≥ 0.60 on L", "signs agree" held; "remainder larger on R than on L" failed (0.15 < 0.19) | Both inputs of the agreement unit take their number from MLP 1's context-free lookup entry (81–85% of MLP 1's contribution to each factor), not from its conditioned remainder — matching the product-level 0.74 (v296) and closing the loop: the -s plural sits in MLP 1's table row for the noun (v287: amplified ~170× from the embedding), MLP 1 writes it at gain α ≈ 0.54, MLPs 1 and 2 deliver it to both sides of 3465's product, and the frame-design 'licensing' factor was number too |
| T56 | MLP 2's share of unit 3465's two factor contrasts (v76 rows), split exactly into its own context-free table part α₂·T₂ (T₂ = MLP 2's write for the noun alone through blocks 0–2; α₂ median 0.44) and its conditioned remainder (closure 9e-8): L factor — table 0.34, remainder 0.66; R factor — table 0.63, remainder 0.37 | fold, native rows | v76 rows + 32 tokens (v342) | 5/5 held | The two MLPs feed the agreement unit differently: MLP 1 through its lookup entry (81–85%, v341), MLP 2 mostly through its context-conditioned write (66% on L; 37% on R) — which is MLP 2's dense re-read of MLP 1's conditioned write (v317). So 3465's L input takes lexical number from MLP 1 and context-conditioned number from MLP 2; its R input is lookup-fed by both. The chain's bottom is now accounted for writer by writer and part by part |
| T57 | MLP 1's part of the other agreement units' factors (v76 rows; exact table-vs-remainder split with the λ chain to each block; closure 1e-7; margin replays): table share of MLP 1's contrast contribution — 493: L 0.76 / R 0.76; 1036 (MLP 5): L 0.88 / R 0.71; 829 (MLP 8): L 0.67 / R 0.70; remainder the rest (0.12–0.33); all signs agree | fold, native rows | v76 rows + 32 tokens (v343) | 5/5 held | Completeness: every lexical agreement unit of the chain (3465, 493, 1036, 829) takes 67–88% of what MLP 1 gives each of its two factors from MLP 1's context-free lookup entry, five blocks up as much as two. The -s plural stored in MLP 1's table row is the number signal the whole chain's bilinear detectors multiply, on both sides |
| T58 | OOD: MLP 1's lookup entry into the four agreement units' factors at the cue noun of the 128 natural verb rows (FineWeb + Pile; plural vs singular cue, unpaired; closure 7e-8): table share of MLP 1's contrast contribution — 3465 L 0.67 / R 0.82; 493 0.69 / 0.66; 1036 0.74 / 0.64; 829 0.58 / 0.66; separation of plural from singular cue rows at the noun in pooled std — 3465 −2.2, 829 +1.7, 493 +1.3, 1036 +0.98 (all with the panel sign). The reported α median (17.0) is a ratio artefact on rows where the λ-scaled entry is small and is not used | fold, natural rows | 128 natural rows + 71 cue tokens (v344) | 3/5: rows, split closure, "3465 ≥ 0.60 on both factors" held; "all four ≥ 0.60" failed on 829's L (0.58); "all four separate ≥ 1 std" failed on 1036 (0.98) | The panel result replays on text: on real sentences MLP 1's context-free entry is still 58–82% of what MLP 1 gives each factor of every agreement unit, and the units separate plural from singular nouns in running text at 1–2 std (3465 strongest). Two bars missed by 0.02 each; the numbers stand |
| T59 | The six chain units at the cue noun, the verb and the final token of the 128 natural sentences (plural-cue minus singular-cue rows, pooled std): cue — 3465 −2.2, 1030 +1.7, 829 +1.7, 493 +1.3, 953 −1.2, 1036 +0.98 (replays v344); verb — 953 −0.97, 1030 +0.35, 829 +0.31, 1036 +0.29, 493 +0.05, 3465 +0.02; final — all within ±0.12 | fold, natural rows | 128 natural rows (v345) | 4/5: rows, cue replay, nothing at the final, verb weaker than cue held; "829 or 1036 ≥ 0.5 std at the verb" failed (0.31 / 0.29) | On text as on the panel, the agreement units detect at the noun and are silent at the answer; between them, at the verb, only 953 keeps a number trace (−0.97 std) — the unit that treated numerals and pronouns as plural from the token alone (v332) and lost the lexical separation on vocabulary pairs (v333). 953 looks like the carrier of a generalised number state to the agreement site, not a lexical detector; 3465 / 493 are strictly noun-position. Next: the verb site with the single-token instrument (v346) |
| T60 | Verb tokens alone up the chain (plural-agreeing are / were / have / do / go / say / make / take vs singular-agreeing is / was / has / does / goes / says / makes / takes; blocks 0–8; closure 0.0): separation in pooled std, signed by each unit's noun-plural direction — 1036 +7.8, 1030 +2.8, 953 +1.7, 829 +0.94, 493 −0.68, 3465 −1.35 (i.e. 3465 puts the singular-agreeing -s verbs on its plural side); have − has and are − is have the same sign for all six units | fold, single tokens | 144 tokens (v346) | 3/5: closure, 953 ≥ 1 std with the noun sign, have/has ≈ are/is held; "829 ≥ 1 std" (0.94) and "3465 / 493 ignore verbs" (3465 1.35, wrong sign) failed | The verb's own number lives in different units from the noun's: MLP-5 unit 1036 reads it from the token alone at 7.8 std (and 1030 at 2.8, 953 at 1.7) — these are the chain's verb-number readers — while the lexical noun units are weak (829 0.9, 493 −0.7) or read the -s ENDING (3465 treats is / was / has as plural-like, the mirror of its plural-noun -s reading). 1036 is therefore not only a plural-noun detector but the unit that reads number off verbs, consistent with its determiner / preposition dependence (v337). v347: at a verb after a noun, does 1036 read the verb's own form or the noun's number (agreement check on grammatical and ungrammatical pairs) |
| T61 | [noun, is / are] frames on the 256 vocabulary pairs, read at the VERB (exact two-way medians; closure 0.0): 3465 — is|singular −0.5, is|plural +15, are|plural +7, are|singular −302 (the ungrammatical cell alone; interaction 2.9 std, form −3.0, noun +3.2 both driven by it); 1036 — are 26 / 12 vs is −4 / 7 (form 1.7 std, interaction 1.2: are|plural highest); 829 — are 42 / 65 vs is 8 / 7 (form 2.9; larger for the ungrammatical are|singular); 493 — is|plural 46 vs the rest ≤ 1 (the other ungrammatical cell); 953 / 1030 ≤ 0.5 std | fold | 1,024 two-token rows (v347) | 2/5: closure and "an interaction ≥ 1 std exists" held; "1036 form ≥ 2" (1.7), "1036 noun ≥ 0.5" (0.16), "3465 form > noun" (3.0 vs 3.2) failed | At the verb the chain's units are AGREEMENT-VIOLATION detectors, not noun-number carriers: 3465 fires ~300 for "singular noun + are" and nothing for the three other cells; 493 fires 46 for the mirror violation "plural noun + is"; 829 and 1036 read the verb's form (are > is), 829 more when the noun disagrees. Together with v337 (3465 sharpest after plural-licensing words, sign-flip after " 1"): these bilinear units compute (what the preceding word expects) × (what the current token is), and light up when the two disagree — 3465 for singular-then-plural, 493 for plural-then-singular. v348 tests the violation reading across more verbs |
| T62 | Violation reading across four more verb pairs (was/were, has/have, does/do, goes/go) × noun number, 256 pairs, read at the verb (closure 0.0): 3465's "singular noun + plural verb" cell −256 / −210 / −128 / −133 vs its singular-verb cells −21 / +1 / −22 / +14 (gap −5.1 / −4.4 / −2.1 / −2.2 pooled std, all four); its "plural noun + singular verb" cell is also raised for goes / does (+135 / +63) but not for was / has (+4 / +43); 493's mirror gap +1.9 / +1.0 / −0.5 / −0.5 (verb-dependent); 829 and 1036 read verb form (plural verb higher by 2.6–4.3 std) regardless of the noun | fold | 4,096 two-token rows (v348) | 2/5: closure and "3465 fires for singular-then-plural verb ≥ 1 std ×4" held; "3465 silent on the other cells" (does / goes 1.7–1.8 spread), "493 mirror ≥ 0.5 ×4", "pattern on ≥ 3 of 4 for both" failed on 493 | 3465 is a robust detector of the singular-noun-then-plural-verb violation across were / have / do / go (2–5 std), and for do / go it also flags the mirror violation; 493's plural-then-singular reading (v347) is verb-dependent and not a general detector; 829 / 1036 are verb-form readers. Named at this grain: unit 3465 of MLP 3 = a bilinear (preceding-word expectation) × (current-token number) product that fires when a singular subject is followed by a plural verb form, feeding on MLP 1's lookup entries for both tokens |
| T63 | "The X is / are" three-token frames on the 256 pairs, read at the verb (closure 0.0): 3465 — is|singular −4, are|plural −49, are|singular −165, is|plural −88 (violation gap −3.0 std, as in the two-token frames; with the determiner it now flags BOTH violations); 493 — is|plural +43 vs the rest −12…+5 (mirror gap +1.6 std); 1036 verb-form effect +2.4 std, 829 +3.0 (are above is, whatever the noun) | fold | 1,024 three-token rows (v349) | 5/5 held | The violation reading survives a realistic subject: after "The", 3465 fires for a singular subject followed by are (−165) and, more weakly, for a plural subject followed by is (−88), while the grammatical cells stay near zero; 493 flags plural-then-is; 829 / 1036 keep reading the verb's form. The determiner does not dilute the detector, it broadens it to both violations |
| T64 | EDIT of the violation detectors on the "The X is / are" frames (native 3465 medians replay v349): zeroing 3465 at the verb changes the next-token distribution by KL 0.0008 nats on the singular + are cell and 0.0000 on the other three; zeroing 493 or both ≤ 0.0009; eight random MLP-3 units 0.0000; plural-vs-singular continuation log-odds on singular + are move +0.05 (1.41 → 1.46) | edit + null | 1,024 three-token rows (v350) | 5/5 held on paper (violation cells > grammatical cells; > 3× a zero null; log-odds rise; 493's largest cell is plural + is) — but every effect is at the third decimal | The detector is real but causally near-inert at the verb: removing 3465's ~−165 activation moves the next prediction by less than a thousandth of a nat. Either its write is read later (the pronoun-number chain reads MLP 3's state at the noun via attention, not at the verb) or it is redundant with a population of units flagging the same violation. Not a component of the next-token prediction here; v351 asks how many MLP-3 units flag the violation and edits the population |
| T65 | MLP-3 population flagging the violation at the verb of "The X are" (X singular) vs "The X is": write-weighted median contrast across 4,608 units — top units 3040 (−5665), 114 (−3329), 565 (+2343), 1651 (+1605), 2257 (+1299), 3667 (−1203) …; 3465 ranks 11th (−730); top-10 / 50 / 200 / 500 carry 6 / 13 / 26 / 44% of the summed contrast. Population edit on the violation cell: zeroing the top 10 / 50 / 200 units moves the next-token distribution by KL 0.094 / 0.105 / 0.100 nats (random sets 0.00002 / 0.00014 / 0.0012) and lowers the plural-vs-singular continuation log-odds by −0.73 / −0.76 / −0.76 from +1.41 | census + edit + null | 512 + 256 rows (v351) | 3/5: replay, "population spread", "top-50 > 3× 3465 alone" held; "3465 is the top violation unit" (11th) and "the population edit stays small ≤ 0.05" (0.10) failed | The violation flag IS used, by a population: ten MLP-3 units (3040, 114, 565 lead; 3465 is a minor member) carry the singular-subject-then-"are" signal, and removing them cuts the model's plural-continuation preference after "are" by 0.73 log-odds — the flag is what lets the model follow the recent plural verb over the singular noun. 3465, named from the noun rows, is the lexical member of a larger violation population; the population, not the unit, is the causal component. Candidates 3040 / 114 / 565 to examine (v352) |
