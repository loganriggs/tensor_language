# Narrative tense L11H3 rank-one scalar program V1

The prior FIT-selected uncentered rank-one basis transfers row-wise projected six-source value deltas to lexical HOLDOUT. That result leaves open whether the basis is a signed tense axis with reusable amplitudes or merely preserves a separate fitted coefficient for every row.

Using only the 16 target FIT rows, recompute the frozen uncentered rank-one basis and each exact post-value coefficient `alpha = delta dot basis`. Freeze three scalar programs before HOLDOUT: the mean alpha for each ordered tense direction pooled across A1/A2; the mean alpha for each construction × direction cell; and one global mean absolute alpha whose sign is set by the FIT mean for each direction. The rank-one basis sign itself is arbitrary; a signed-axis preflight requires the two direction means to have opposite signs and every FIT row coefficient to agree with its direction mean.

On the 16 untouched target HOLDOUT rows, evaluate expanded native, exact native reinstall, complete H3, exact all-six post value, exact row-wise rank-one projection, direction-mean scalar, construction × direction scalar, global signed scalar, and the negative global scalar. Each positive program passes when every A1/A2 direction cell recovers at least .80 of exact all-six mean donor-margin effect and donor CE gain with donorward fraction at least .75. The wrong-sign program must have anti-donorward margin effects on at least .75 of each cell.

Registered outcomes: `pred_a_exact_signed_axis_instrument`, `pred_b_direction_scalar_program`, `pred_c_global_signed_scalar_program`, `pred_d_construction_conditioned_scalar_only`, `pred_e_wrong_sign_negative_control`, `pred_f_row_projection_only`, and `pred_g_scalar_program_null`. No HOLDOUT statistic may alter the basis or scalars. This is a manipulation test on target tense directions; P/C selectivity of the endogenous projection was established in the preceding rank-one experiment and is not redefined here. Maximum price: five forwards, 208 sequences, zero backwards, zero updates.
