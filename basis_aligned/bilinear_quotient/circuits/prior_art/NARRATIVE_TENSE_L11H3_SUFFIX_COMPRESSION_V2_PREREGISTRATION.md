# Narrative tense L11H3 suffix compression V2 instrument correction

V1 is preserved as invalid because its native-reinstall arm used the generic replacement-head execution path and produced maximum logit error `5.1975250244140625e-05`, above the frozen `5e-05` bound. Source sums and both head-additivity checks passed, but no V1 suffix fraction is scientific evidence.

V2 keeps the V1 authority, license, source partitions, arms, predictions, thresholds, price, and scoring unchanged. The only correction supplies the evaluator's `native_reinstall_mask` for the native-reinstall arm, matching the exact execution path already validated in the parent carrier experiment. The output path and candidate ID advance to V2; V1 remains immutable. If exactness still fails, V2 is invalid with no further interpretation.
