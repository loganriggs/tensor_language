# Narrative tense L11H3 suffix compression V1

The licensed combined-authority carrier result localizes 94.5–98.8% of the unchanged-source effective-value effect to the six source positions after the last tense-changing token. In both A1 and A2 these six positions end with the identical self-relative suffix `role of the` at offsets −3, −2, and −1. The three earlier positions differ by construction.

This exact factor experiment partitions the six-position post-change carrier into early offsets −6:−4 and shared suffix offsets −3:−1, then partitions the suffix into `role` at −3 and `of the` at −2:−1. Frozen arms are native reinstall, complete H3, all-six post value, early-three value, suffix-three value, role-only value, and of-the value. Effective values are donor values under recipient attention probabilities. The runner also verifies source-factor reconstruction, same-batch native reinstall, and exact head-level additivity of the two disjoint partitions.

Registered predictions:

- `pred_a_instrument_live`: exactness passes and complete H3 transfers every A1/A2 cell donorward.
- `pred_b_shared_suffix_compresses_post_carrier`: suffix-three supplies at least .85 of all-six post margin and CE in every target cell, with donorward fraction at least .75; early-three supplies at most .25 in magnitude.
- `pred_c_role_single_source`: role-only supplies at least .70 of suffix-three margin and CE in every target cell.
- `pred_d_of_the_source_pair`: of-the supplies at least .70 of suffix-three margin and CE in every target cell.
- `pred_e_suffix_downstream_interaction`: suffix-three passes but neither component passes, and the downstream role×of-the interaction supplies at least .20 of suffix effect in magnitude in every target cell.
- `pred_f_suffix_distributed_additive`: suffix-three passes, neither component passes, and the registered interaction condition fails.
- `pred_g_suffix_compression_null`: the exact instrument is live but suffix-three does not meet the compression bar.

Each candidate suffix arm must remain selective on both licensed fixed-present P and C controls: mean absolute answer-margin and full-vocabulary CE changes may not exceed .25 of the smallest target all-six effect. The test is scoped to the exact licensed authority. Maximum price is six forwards, 576 endpoint-arm evaluations, zero backwards, and zero updates.
