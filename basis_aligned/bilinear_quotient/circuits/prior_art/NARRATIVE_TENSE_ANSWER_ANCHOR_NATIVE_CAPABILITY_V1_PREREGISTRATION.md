# Narrative tense answer-clause anchor native capability V1

This native-only capability gate follows the valid V1 finding that direct A1 passed while a reported A2 without an explicit answer-clause anchor answered ` was` for every present endpoint. It is an independently frozen authority with 48 new one-token content words and prompt endpoints disjoint from all earlier narrative authorities. Groups 0–7 are FIT; groups 8–15 are an unopened lexical HOLDOUT.

A1 states `Yesterday/Today ... served/serves one role` and repeats `Yesterday/Today` immediately before the answer clause. A2 uses the distinct construction `Yesterday/Today, records described/describe ...` and independently repeats the same time word immediately before the answer clause. P and C keep tense and answer fixed while changing only an answer-clause modifier (`formal/public` and `official/recorded`).

Every phase × family × direction × side cell contains four endpoints. Every FIT cell must be at least .875 accurate, requiring 4/4. HOLDOUT opens only after all FIT cells pass and must meet the same bar without row removal, fallback, or threshold changes. A hash-bound native license is emitted only if all 32 cells pass.

Predictions: `pred_a_fit_all_cells_pass`, `pred_b_holdout_all_cells_pass`, and `pred_c_license_issued`. The maximum price is two forwards and 128 endpoint evaluations, with zero backwards, updates, attention access, or causal interventions. Passing establishes capability for this frozen construction only.
