# Narrative tense combined native capability V1

Two independently frozen FIT screens localized complementary capability: the direct `At that/this time` A1 passed 16/16 endpoints, while the reported construction with a repeated `Yesterday/Today` answer-clause anchor passed 16/16 A2 endpoints. This gate freezes that combination on 48 additional one-token content words, all disjoint from every prior narrative authority. It is confirmation of the outcome-derived combination, not an independent claim of construction universality.

Groups 0–7 are FIT and groups 8–15 are an unopened lexical HOLDOUT. A1 and A2 each vary past versus present in balanced reciprocal directions. P and C are fixed-present invariance controls in the matching constructions and change only an answer-clause modifier. Fixing the controls to present is predeclared because both earlier answer-anchor control failures occurred only on past endpoints; this authority therefore licenses selectivity against capable present-state rewrites, not against arbitrary past controls.

Every phase × family × direction × side cell has four endpoints. All FIT cells must be 4/4 before HOLDOUT opens; all HOLDOUT cells must then be 4/4. There is no row removal, fallback, threshold adjustment, attention access, or causal intervention. Passing both phases emits a hash-bound license for the exact authority and the L11H3 combined-authority causal candidate.

Predictions: `pred_a_fit_all_cells_pass`, `pred_b_holdout_all_cells_pass`, and `pred_c_license_issued`. Maximum price: two forwards, 128 endpoint evaluations, zero backwards, zero updates, and zero interventions.
