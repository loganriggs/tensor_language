{
  "schema_version": 1,
  "contract_id": "bilin18.shared-value-output-codec-fixture.v1",
  "status": "cpu_fixture_only_not_checkpoint_priced",
  "scope": "one head across all layers; common cross-head S9 permutation remains unresolved",
  "canonicalization": "shared_value_gauge.canonicalize_shared_value_bus",
  "exact_gauge": "one GL(head_dimension) action shared across depth",
  "encoding": {
    "header": "canonical sorted compact JSON",
    "tensor_order": "V0..V(L-1), O0..O(L-1)",
    "quantization": "round(canonical_tensor * 2^exponent) to signed little-endian int32",
    "literal_bits_are_descriptive_only": true
  },
  "required_tests": [
    "byte equality under a shared GL gauge away from quantization boundaries",
    "decoded action approaches canonical action within a quantization bound",
    "independent per-layer gauges are not treated as equivalent",
    "rank-deficient shared parent and malformed containers fail closed",
    "literal bytes are not promoted without held-out, composite, extraction, removal, and OOD gates"
  ]
}
