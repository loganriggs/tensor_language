# Destination role of the inherited-city head8.2 write

Use the exact union of the two prior frozen 48-row panels. For each row, compute
the recipient-routing, donor-inherited-first changed-city source contribution of
head8.2. Split its destination write at the unique first period after the city:
descriptive continuation `(city, period]` versus explicit instruction
`(period, quote]`; their union is the frozen framing mask. Feed the full,
description-only or instruction-only induced block9 current state only through
head9.8 O and the native suffix. Native plus three arms on96 rows costs384 body
forwards under180 seconds.

- `pred_a`: native/full-inherited target/work scores replay the two parent
  artifacts within `1e-5`; recomputed native head8.2 source channels replay
  captured head2 within `1e-5`; destination writes partition within `1e-5`,
  outside masks are exact, values finite, and exactly384 forwards execute.
- `pred_b`: native cue is positive for at least10/12 pairs and full-inherited
  target-effect RMS is at least `1e-5` in every family.
- `pred_c` (description role): description-only cue error versus full is at most
  35%, while instruction-only error is at least50%, in all four families.
- `pred_d` (instruction role): instruction-only error is at most35%, while
  description-only error is at least50%, in all four families.
- `pred_e`: separately measured description and instruction cue effects compose
  full inherited within10% in every family.
- `pred_f`: every unrelated-control RMS ratio is at most.5 in every family.

This tests a token-defined destination role without fitting. It does not claim
a stable role if either opposing prediction fails, and does not test corpus OOD,
whole-head sufficiency, static compression adoption, or quantization.

