# Structured producer value component: native test

Registered 2026-09-12 before the native cache or behavioral results.

The previous goal turn made progress: updated report/primary receipts and CPU accounting establish that head13.0 contributes >99.94% of the recurring source block's summed head-labelled coefficient energy. That is not a behavioral fraction. The earlier whole-head dossier lists this head as broadly expressive; this is a proposed within-head value component, not a new whole-head semantic discovery.

## Object and opposing predictions

Fold the four current-source readings of the frozen head17.2 regional branch backwards through attention8,9,13, with actual residual scales. For head13.0 let M be its four-output value map on concatenated current/first source state. Keep the leading left singular output projector P=uu^T while retaining BOTH complete QK operations, input-dependent normalization and native producer inputs. P is fixed entirely from weights. Compare native contributions to the four readings: G (three-producer sum), H (head13.0), R=PH and H-R.

Hypothesis: R carries nearly all of H's regional cue transfer and a meaningful fraction of G. Null: the high coefficient-energy fraction fails native behavioral fidelity, or captures general cue sensitivity without selective effects. No optimizer, no text-data fitting, no candidate revision after scoring.

## Native cache instrument

run_structured_producer_cache_v1.py: 12 original-model body forwards, 96 existing competing-city contexts of length21–24; three producer components, 4 readings per position. No suffix arms, <=3MB output, 180second execution cap. A: each four-output producer replay <=1e-5 relative. B: pre-MLP17/current17 cache and residual/RMS replay <=1e-5. C: projector idempotence and rank1+complement reconstruction <=1e-12. These are instrumentation bars only. Hash-bound source, rows and prior cache; unchanged row validator.

## Frozen CPU behavioral test

For recipient i and target-cue donor j=i XOR 1, replace source readings at every position by

$$
f_i'(t)=f_i(t)+\frac{X_j(t)-X_i(t)}{\rho_{17,i}(t)},\qquad X\in\{G,H,R,H-R\}.
$$

Here f contains the four frozen current readings plus the token-derived first readings, and rho is the recipient residual RMS. Only the selected upstream additive contribution changes; query, current key-normalization inputs, other residual producers and token lookup remain recipient-native. Execute the explicit-reading regional branch, add its changed write to the recipient pre-MLP17 state, and run the validated CPU last-MLP/selected-token suffix. This is a conditional producer-to-reader edge intervention, NOT a full native-module swap propagated recursively.

A: no-edit branch replay <=1e-5 relative; saved native selected-logit baseline <=1e-4 relative; all cache instrument bars pass.
B: R versus H regional swap-effect relative L2 error <=10% in EACH of four cells (two city-role assignments x two clause orders); H regional effect norm must exceed 1e-6 in each cell, otherwise no fidelity claim.
C: in EACH cell, G mean directed regional transfer >0; R mean directed transfer >=10% of G; >=20/24 directed swaps move regional margin toward donor cue; unrelated mean absolute transfer <=0.5 times regional mean absolute transfer. Direction multiplier is -1 for British recipient and +1 for American recipient, since donation flips regional cue. No division by an unsigned or nonpositive reference rescues a miss.

Report R+complement versus H nonlinear effect discrepancy descriptively; no additive-effect assumption. Preserve all four cells and original bars. No full four-property promotion from a pass. These contexts reuse known cities and spelling concepts; new word/corpus OOD still needed.

## Literal price and restrictions

Retain all four head13.0 QK maps (589,824 scalars), native gates and state generators. Rank1 describes a four-output value component, not a low-cost independent producer or matched-budget victory over the source dictionary. The downstream mixed regional package and last-MLP suffix remain charged. Primary circuit targets: computational specification, within-head splitting, selective manipulation, and producer–consumer composition at a stated conditional interface. Broader path discovery remains open.
