# Correlative value-source transfer on new combinations v1

Freeze the saved26-head/14-block interface and original folded weight maps.
No fit, dose, head, layer, value-mix or rank selection. Previous induction
value-source nulls remain closed; this tests a different behavioral interface.

The exact block scalar decomposes as s=s_first+s_local. The first term reads
the native first-layer value producer; the local term reads contextual values.
Each still uses the COMPLETE native live QK routing. Compare first-only and
local-only donor scalar swaps against the complete donor swap, always computing
the current recipient component after earlier edits. Full background stays.
A positive first result would identify a conditional value-source simplification,
not token-only routing or independent upstream extraction.

Fresh combinations: deterministic16groups seed9111280, new A1 reporter/noun
offset5 instead of11, A2 offset13 instead of19, C reporter/object offset3 instead
of0. P changes reporter and preserves cue/answer. Frozen rows are
CORRELATIVE_RECOMBINED_ROWS_V1.json. All112unique selected tokenized prompts
are disjoint from384unique prompts in complete original v505 source banks.
Vocabulary and templates are reused. This is unseen factor combinations relative
to that interface's source data, not training OOD or universal benchmark novelty.
The old generator's seed merely permutes its32fixed lexical cases; a seed-only
rerun would not supply new combinations. No outcome-based row filtering.

Nine body forwards per each of A1/A2/P/C: full donor capture, full base capture,
independent native base, full donor swap, first donor capture, local donor capture,
first-only swap, local-only swap, full-map swap with summed first/local donor
scalars. Total36forwards576sequences. Zero native updates/backwards.

Frozen predicates:

A. Instrument: exact map sum/zero structure; native-base and split-joint versus
full-joint logits maxabs<=1e-3 AND relativeL2<=1e-5; all full-map scalar checks
absdifference<=1e-4+1e-5*abs(reference); donor first+local scalar versus full within
the same combined tolerance; finite tensors and exactly36forwards576sequences.

E. Full interface generalization: native base and donor answer margins >0 on
ALL64rows; mean raw full donor recovery>=.8 separately on A1/A2, with all their
donor-recovery denominators>1e-6; P and C full donor absolute margin movement
normalized by fresh A1 median native separation<=.23 each. Failure is retained.

B. First-value sufficiency: E holds; first-only mean raw recovery>=.8 AND
centered full-vocabulary effect error versus complete swap<=.15 separately on
A1/A2; P and C first-only normalized margin movement<=.23 each.

C. Local-value sufficiency: the same B conditions for local-only swaps.

D. Additive effects: E holds and centered full-vocabulary nonlinear interaction
||z_full-z_first-z_local+z_base||/||center(z_full-z_base)||<=.10 separately on
A1/A2, with centering also applied to the numerator. A D miss establishes that
adding singleton effects fails at this tolerance; it does not invalidate a
program that explicitly recomputes the joint nonlinear suffix.

Effect errors aggregate all scored prediction-position vocabulary coordinates
and rows in the named panel. Native correctness and full-interface transfer gate
the interpretation of partial branches; no silently dropped failed examples.
Both source hypotheses are registered now. If both fail, close this source-only
transfer simplification for the fixed interface without choosing a gain or mixture.
Exact split/joint equivalence remains a control, not evidence of semantic reuse.

Reuse correlative_native_folded_executor_v1.forward. Component maps zero exactly
one of the two value-reader vectors; output writers remain unchanged. Its partial
scalar-versus-full-head discrepancy is expected, so only full-map checks and the
component-sum identity constitute the scalar instrument. Native original maps,
normalizers, rotary and signed mixing coefficients are retained.

All545902902 native parameters stay charged. The76,032coefficient full port core
already exists; a hypothetical single-source core would retain26*1152 reader
plus14*1152 writer coefficients=46,080, alongside all native dependencies.
This is conditional description accounting, not achieved model savings.
Managed GPU only, hash-bound files and immutable result; actual counts/timing
reported. Data preflight checks tokens, positions, roles, lengths and provenance.
