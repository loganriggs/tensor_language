# Fresh contextual transfer of physical producers and their composed interaction

2026-09-12, before model evaluation. Prior physical pair removal carries67–70%of cue contrast on reused48rows; direct+mixed MLP8bridge predicts serial effects within.38–1.14%. Value-only producer9generation fails one family, so retain complete jointQK. Newline selectivity remains failed and is not retested away by this panel.

Fixed48rows: email/letter templates, Glasgow/Phoenix and Cambridge/Detroit, neighbours/neighbors, organise/organize, realise/realize, labelled/labeled, defence/defense, metre/meter. Cities/endpoints checked against existing REGIONAL*/STRUCTURED_PRODUCER*rowfiles. British/American pairs differ by one token at identical positions. These are controlled held-out contexts, not new corpusOOD or proof of pretraining disjointness. Native capability is unobserved; no prompt filtering after scores.

All original source readers, jointQKmaps, physical writers, and J_ddirectional map frozen. Arms: native; physical8; physical9; dynamicjoint; physical8+pristine-frozen9field removal; physical8+directmixed-generated9field removal; physical8+exact-directional-generated9field removal. All later states recompute. Native forward supplies pristine z8,a8,r9 and biasfreeMLP8output as external background to the exact response generator; no fit or hidden regeneration of inputs is claimed.

A: EACHtemplate native mean cue contrast>=.2 and>=10/12positive; exact-directional generated joint effect difference versus frozen9 reference reproduces native dynamicjoint-minus-frozen9 effect<=1e-4relative. Also report scalar generator replay<=1e-5 against actual layer9 scalar after8-only removal. Numerical checks and native capability reported separately; capability miss is an inconclusive transfer panel, not absent circuit structure.

B EACHtemplate: physical8/9/joint removal each reduces cue contrast in>=10/12pairs; joint mean reduction>=50%native mean contrast (registered based on prior67–70% result, not the older5%minimum); unrelated token-margin meanabs change<=.5regional meanabs for each of these3arms. No requirement that each component equal its wholehead, because that previous claim already failed and is not being reasserted. Preserve that older failure explicitly.

C EACHtemplate: directmixed-generated joint-minus-frozen9 effect versus native dynamicjoint-minus-frozen9 effect <=10%relative. This is the signed serial-effect denominator, not the much larger full pair effect. Exactfullresponse A may pass while reduced C fails; report both without replacing C by exact algebra.

Price336single-prefix forwards48*7, max32tokens,180seconds; full original model and explicitbackground inputs remain charged. No optimizer or model-scoreselection. Managedlane1 only. A/B/C pass strengthens held-out contextual removal and conditional composition, not full input extraction, global selectivity or a four-property circuit promotion.
