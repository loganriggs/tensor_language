# Contextual source location of the odd branch

Use the48 article-corrected rows without filtering. The paired difference fixes
one city token at position1 or2. Partition head9.8 O sources into: positions
before the city, the city, positions after the city but before the final token,
and the final self-source. Query, native states, normalizers, values, rotary and
the full suffix remain live. No boundary is learned from outcomes.

Seven arms are native; remove pre, city, post, or self; remove post+self; and
remove all O. The run has336 body forwards and a180-second cap.

- `pred_a`: native/all-O anchors replay the frozen cube within `1e-5`, and the
  four live source pieces reconstruct O within `1e-10`.
- `pred_b`: removing post+self approximates all-O cue effects within20% relative
  L2 in each family.
- `pred_c` (context relay): post-only error is at most35% and self-only error is
  at least50% in each family.
- `pred_d` (self consolidation, opposing): self-only error is at most35% and
  post-only error is at least50% in each family.
- `pred_e`: post+self control-effect RMS is at most50% of its target-cue RMS in
  each family.

If B passes but neither C nor D passes, both later-context and self sources are
needed at this intervention boundary. If B fails, pre/city sources or nonlinear
composition remain material. This is a causal source-position screen of the
already material O branch. It does not remove upstream descendants, identify a
semantic algorithm, establish OOD transfer, compress weights or use quantization.

