# Context versus finite transport diagnostic

Opened frozen two-site OODv2 contexts and sixteen signed coordinates. Same finite mixed attention11 removal as prior experiment, no fitted parameters. Compare baseline reader, reader at actual joint post-attention state, midpoint reader, and three-node Gauss-Legendre integration along the exact straight removal path. Directional JVP returns all four output contrasts and avoids materializing full readers at every node.

For suffix F and removed write delta, the exact effect is F(z-delta)-F(z) = integral_0^1 DF(z-alpha delta)[-delta] d alpha. Three-node quadrature is an approximation, not an exact nonlinear identity. Nodes .5 plus/minus sqrt(3/5)/2, weights 5/18,8/18,5/18. Midpoint is shared, not recomputed.

Registered predictions: instrument counts and prior algebra/output replay pass; joint reader predicts number-removal effect within 10% for all64cell-settings (absolute norm floor1e-8); quadrature predicts allfour effects within1e-4 relative or1e-8 absolute. Report errors and target norms, including small-denominator cells. Opposing outcomes: joint success with baseline failure supports context dependence; joint failure with integrated success supports finite curvature along removal; integrated failure leaves quadrature or instrument unresolved and requires convergence checks. None changes the previous0/13causal-sufficiency failure or establishes a reusable extracted circuit.

Counts12prefix,232suffix evaluations,68first-attention writes,16baseline-reader reverse calls,128logical directional JVPs (four per32joint settings). Native generators and integration costs remain charged. Existing native baseline/local-core replay and planted core tests are controls. No fresh OOD claim.
