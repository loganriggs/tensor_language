# Compose two independent source-edit components

Use the frozen substituted-five-source subject and distractor edits from OODv2
at their distinct pre11 positions. Retain baseline x0, cache and other positions.
Scale the subject patch by s and distractor patch by t. Texts and individual
edits are opened; joint effects have not been evaluated. This is conditional
composition of local residual patches, not a full joint embedding edit.

TWO_SITE_COMPOSITION_V1R1_BINDING.json freezes rows, amplitude vectors,16signed
settings, implementation hash and budgets. Initial12setting binding is retained.
The pre-outcome amendment adds half-axis controls and conditional-increment
gates because attractor effects are only0.35–10.9%ofsubject effects. A joint
prediction test alone could pass while ignoring the smaller component.

For the eight mixed settings and eight base cells, test all64cell-setting pairs:

- Joint prediction error uses B=|s|*Ns+|t|*Na, where Ns/Na are the frozen norms
  of individual full-size native number effects.
- Conditional attractor increment Y(s,t)-Y(s,0) uses budget |t|*Na.
- Conditional subject increment Y(s,t)-Y(0,t) uses budget |s|*Ns.
- Each number error must be<=10%ofitsbudget and each modal error<=5%.

Y is baseline minus edited contrast. Report all failures, absolute errors and
actual target norms. These are prediction gates, not a new claim of selective
preservation under a target that can cancel. The eight axis settings are controls;
positive full-size axes must replay prior outcomes1e-4. Use the ignored-attractor
increment as a live weak-component control; do not hide it in total target energy.

Baselines: full two-input quadratic output response including its cross term;
the same response with cross term omitted; and exact final readout of a quadratic
state with basis[1,s,t,s²,st,t²]. The latter folds eight unembedding rows and
the shared quartic norm through a six-feature triangular factor,69coefficients
including constants versus24for the full output quadratic and20separable.
Charge native source and state-derivative generators. The univariate two-square
compiler must not be applied to this two-input polynomial.

Capture state coefficients from directional jets along(1,0),(0,1),(1,1), including
the mixed coefficient. Validate full compiled first/second derivatives against
independent native autograd1e-8, exact quadratic-state readout1e-10 and double/native
replay1e-4. Planned12prefix,84double and68native suffix calls;12first+12second
logical state JVPs;16native gradients/32native Hessian rows;16+32cheap compiled
derivative checks and64local readout checks. Final runner must verify its counts.

The planted two-axis capture/replay control passes and removing its mixed state
coefficient changes outputs substantially. Native composition is still pending.
