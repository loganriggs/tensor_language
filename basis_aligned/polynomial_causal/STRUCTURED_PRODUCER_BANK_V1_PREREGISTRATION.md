# Frozen 27-head value-component bank

2026-09-12 before new all-head cache or per-head outcomes. Head13.0-only B/C failures remain. Earlier layer-level producer tracing motivates layers8/9/13; selection of this producer group is behavior-informed. Components inside the bank are defined only by fixed weights and the previously controlled consumer metric H, not by their outcomes on these contexts.

For each of the27 heads, form its four-output value map M with actual residual re-entry scale and current/first mixing. Select exactly one component Mvv^T by SVD of H^(1/2)M. Retain full joint QK/gates. No rank/hyperparameter search. H and its isotropic query/current, uniform-token and32position assumptions remain frozen. Literal retained QK scalars589824/head plus native state generators, value component/readers and downstream package. Native head boundaries propose candidates; they do not define semantic units.

Cache all27 native four-reader contributions on the same96 contexts,12native batches, <=3MB,180seconds. Original cache A/B/C instrumentation bars remain; head-sum versus selected producer-group replay also <=1e-5. Replay saved native states and pre suffix exactly when possible. Managed lane1 only.

CPU score EACH of27 candidate components against its native wholehead and the complete selected producer group, using exactly the intervention semantics and four-cell bars of STRUCTURED_PRODUCER_NATIVE_V1_PREREGISTRATION.md. A baseline<=1e-4, perposition noedit<=1e-5 and cache/metric controls held. B component-to-head regional effect error<=10% and head effectnorm>1e-6 eachcell. C component mean directed regional transfer>=10%positive groupreference, >=20/24 directed signspositive, unrelatedmeanabs<=.5regionalmeanabs eachcell. Report every candidate and every miss; no aggregate maximum presented as general recovery. Component/remainder interaction descriptive. Head13.0 row must replay the previous weighted component test within1e-4relative effect.

Opposing predictions: another frozen weight component has meaningful, selective and repeatable conditional transfer; or value-only splitting with native QK retained fails broadly in this producer group. Any bank pass is a multiple-candidate exploratory screen and needs new lexical/context confirmation. No OOD or fully identified circuit claim from this reused panel. No component refit on screen data.
