# Centered blocks plus an exact shared common channel

The full-U native congruence search converged and failed its near-null/gap screen.
Its leading derivative response contains47.82%known common-output energy. Test a
different decomposition: retain the exact vocabulary-common function separately,
and seek congruence blocks in Q_v-Qmean. This allows the shared common function
to couple otherwise separated blocks. It does not discard it before physical tanh.

Use exact Uc^TUc=U^TU-Vmean(U)mean(U)^T, all50304outputs and1152²input-operator
coordinates. Existing mixed search and independentFP64verification,seed1009,
ncv40,maxiter150,tol1e-5, four smallest nontrivial modes; identity removed.

A:4modes converged and independently verified with residual/operatorbound<=1e-7,
identity/orthogonality<=1e-8, native centered total matches92104252412.19983within
1e-8relative; fixedmatrix FP32/FP64action error<=1e-5; no significant negative
eigenvalues (<-1e-8operatorbound). Common quadratic energy pluscentered total
matches whole-U total within1e-8relative. Native shape tripwire remains1152/4608.
B: firsttwoeigenvalues<=.01*2/(1152+1). C: lambda2/lambda3<=.10withlambda3>1e-10bound.
B/CrequiresA. Preserve partial convergence as optimization unresolved.

For each newZ measure exact common-channel constraint energy. Its normalized
centered residual pluscommon residual gives the full-U Rayleighcost; compare
with the full-U modes but do not require equality for different metrics.

Price:0forwards0tokens; no output/input truncation; same~170MBGram,~425MBKrylov,
~42.5MBchecksummed ephemeral /dev/shm witnesscache as prior run.900sprocesscap.
Shared common scalar quadratic and its vocabulary writer remain fully charged,
alongside any block bases, cores, residuals and native background.

Null: no near-null/gapped modes even withcommonchannel separate. This is a
narrow screen on a globally separated centered family, not all approximate
block structures, overlapping programs, or task-conditioned computations.
Red-team with original-coordinate conditioning and weak-input diagnostics.
