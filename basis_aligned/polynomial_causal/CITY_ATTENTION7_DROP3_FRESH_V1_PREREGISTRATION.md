# Fresh confirmation of physically packed attention7 drop-head3 city removal

Freeze the physically packed eight-head attention7 program from CITY_ATTENTION7_DROP3_PACK_V1, the V1 five-reader formula and approximate RMS8 before selecting new rows. Head3 was selected on the opened omission panel; this fresh test cannot reselect heads.
Use the existing outcome-blind Pile selector: first twenty eligible documents
not present in any frozen *_ROWS.json document list, the same preposition/city
filter, two city arms and six spelling probes. Preserve selection exclusions and
report distinct documents, untouched versus substituted arms, and all failures.
No native capability filtering or replacement of failed rows. These are fresh
regional evaluation documents, not necessarily pretraining-disjoint documents;
fixed endpoint probes do not claim to be actual next-token predictions.

Extend only initial/first-value token tables from checkpoint weights for newly
encountered token IDs. Keep all retained attention7, five-reader and head8 weights and the
normalization formula unchanged. Charge the table growth. No fit or threshold tuning.

Capture residual6 on CPU in one padded native batch, and directly capture the
native full city-token write at attention8 for the reference. Verify independently reconstructed native-reference queries against captured native queries at relative1e-4 and finite supported writes before installing anything. Candidate queries deliberately change when head3 is omitted: report their error, without using native-query equality as a candidate gate. Approximate local write error is reported, not used to select
rows. No old approximate block9-response or donor-route prediction is a reference.

CPU screen: native, full native city removal, single-input generated removal,
and16same-site per-position norm-matched random removals. Same paired-seed/opposite
sign design, fresh seed18098000+1000*k+context_id. Nineteen padded batches,
760sequence-equivalent full forwards,342block calls; capture adds40full forwards
and18block calls, total800equivalents/360block calls. CPU two threads,600second cap.
Four unrelated readers remain cat/dog, red/blue, Monday/Tuesday, apple/orange.

Gates frozen before row creation:
a. Finite outputs, exactly40sequences/20documents/240probe rows, no document or
   exact-input overlap; native-reference query replay<=1e-4 every sequence; support zero;
   null norm error<=1e-5; CPU only; required call counts.
b. Generated target-effect relative L2 error<=.05, target RMS>=1e-5 and at least
   90/120native capable contrasts (UK-minus-US margin>=.1).
c. Every control RMS movement<=.5generated target RMS.
d. Positive attenuation>=.90 on capable contrasts and mean>=.02.
e. Generated target RMS>=2random median and beats all16nulls.

Also report frozen opened-panel mean-effect and zero-effect prediction baselines,
separately on untouched/substituted rows; no added retrospective pass threshold.
Composition is not tested here. A pass supports this declared residual6 extraction
boundary and fresh selective removal, not token-only extraction, independent
composition or matched-effect simplicity. A miss remains a miss; do not drop a
reversed document. Isolated export certification uses separate opened fixtures.

The stricter .05 effect-error bar is retained from the opened head-omission selection. Table weights are derived outcome-blind; physical tensor storage must equal the retained eight-head factors plus literal fresh token tables. Zero behavioral outcomes have been opened for this new panel.
