# Fresh joint value-path composition and transmission

Freeze direct attention8 response A and MLP8-induced response B into head9.8 current value. Opened composition passes; norm-preserving random-split specificity passes globally by a small margin, but FAILS descriptively in reversed documents. Do not claim uniform specificity or independently identified semantic variables.

Build next20eligible unused FineWeb sample-10BT documents with unchanged selector, pairedcityarms and fixed6probes;exclude every prior frozen document/input. No fitting, rowreplacement or capability filtering. Native exact head8.2 cityswap parent. Capture native/edited z8,mixed9 and MLP8 outputs. A=(1−mu)lambda Wv*d/rho9edited. B is fixed four-term folded MLP8 response. Independent native joint reference=(1−mu)lambda Wv*(d+MLP8edited−MLP8native)/rho9edited. Keep block9 normalizer term outside both. Edited RMS is externally supplied.

5arms:native,swap,swap−A,swap−B,swap−(A+B). Reuse native/swap capture forwards, then3additional;200sequence-equivalent forwards/90blockcalls,2CPUthreads180secondcap. Allkeys/otherheads/fullsuffix native. Fresh measure relative to fullswapbackground: eA,eB,eJ. This tests suppression of the parent's transmission through the two value paths, not that the corrected swap must itself maximize citycue attenuation.

pred_a:20docs/40sequences/240probes/nooverlap;folded joint value correction<=1e-4relative persequence against independent nativejointreference;offmask0;subtraction<=1e-5relative;otherheadsunchanged;finite/counts/CPU.
pred_b:||eJ−eA−eB||<=.35min(||eA||,||eB||) globally.
pred_c:bothsingles>=.10joint targetRMS globally;interaction<=.35smaller separately in fresh native-reversed and otherdocumentgroups. If either group absent, report untested and fail this joint gate;do not seek replacements.
pred_d:jointcorrection targetRMS>=.10parent-swap targetRMS and>=1e-5;each unrelatedreader RMSchange<=.5joint targetRMS.
pred_e:joint removal suppresses parent's target effect with signed projected fraction −dot(eJ,eSwap)/||eSwap||²>=.20 globally. Report unchanged nativecitycue capability/direction for everyarm separately;this projection is not a substitute for the prior90%regional direction gate.

Report perdocument jointinteraction, signs, source/nativecontexts and subgroup controls. No random-split fresh test is included; opened reversed-group specificity failure remains a limit even if fresh absolutecomposition passes. No token-only or matched-effect simplicity claim. Failure stops this independent-path promotion, retaining coupled mediator and exact algebra.
