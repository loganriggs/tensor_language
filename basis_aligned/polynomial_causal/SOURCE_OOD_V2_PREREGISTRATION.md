# Prospective transfer of the fixed five-source substitution

Rows and algorithm hashes are frozen in SOURCE_OOD_V2_BINDING.json before
model outcomes. The48texts use six noun pairs and two templates, opposite
and congruent distractors, both subject and distractor edits:96sites in16
template/number/panel/role cells. Teacher replaces baker solely because
GPT2 splits " bakers" into two tokens; this occurred before model evaluation.
Novelty is relative to the declared source-response series, not all project
data or model training. Preserve every cell including native capability failures.

Compare original five ports, all six closed ports, and fixed ports[0,2,3,4,5]
(embedding, middle writes, MLP8, MLP10, residual complement). Each selector
receives only its own gradient/Hessian, own unit-B reference and exact-null
LP solution. The frozen multistart quadratic optimizer uses an8%per-input
modal budget and amplitude box[-1,1]. No full-six solution initializes a
five-port selector. Source support, starts, thresholds and rows stay fixed.

Use exact float64 edit closure with the rounding complement and one final
float32 native-state cast, as in the validated r1 interface. Retain baseline
x0/cache and other positions. Derivatives come from fresh native contexts:
this tests a conditional selection/prediction rule, not a circuit executable
without the original prefix or a transferable fixed coefficient dictionary.

Primary: fixed substituted five-port selector achieves at least80%aligned
unit-B number strength and at most10%modal collateral in every cell.
Prediction: quadratic number error<=10%, modal error<=5%ofnumber-effectnorm,
for all three selected candidates. Capability: native baseline correct
signed number contrast in at least90%of each cell; report failures separately,
never drop them. Instrument: exact state closure1e-10, edit closure1e-5abs/
1e-6relative, double/native replay1e-4, centered directional gradient1%and
Hessian15%, with declared forward/reverse counts in the eventual runner.

Report matched-width five-port candidate versus original five-port baseline,
and full-six upper-capacity control separately. Price80versus108conditional
coefficients plus native generators, derivatives and optimizer calls. A pass
does not imply reusable semantic features, broad unrelated-behavior preservation
or complete extraction. Those remain independent requirements of the goal.
