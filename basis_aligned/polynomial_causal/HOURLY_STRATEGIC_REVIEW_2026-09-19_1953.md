# Hourly strategic review

ACTIVE_TRACK: WEIGHT_FOLDING

Actual UTC: **2026-09-19T19:53:21Z**. Next deadline: **2026-09-19T20:53:21Z**. Previous review: [18:47 CIRCUIT](HOURLY_STRATEGIC_REVIEW_2026-09-19_1847.md), 1h06m09s old. This is one live review, not an offline backfill. Historical `/workspace` paths were resolved against this checkout. No GPU model, agent, job, queue, service, timer, commit, push, external contact, experiment/result, existing registry/dossier, or `TYPED_FACE_EXTRACTION_V1` implementation was changed. The sole bounded CPU/process repair was the truthful 19:53 phase mark.

The previous review declared `CIRCUIT`; this review therefore alternates to `WEIGHT_FOLDING`. No frozen complete-city forward-response census or causal screen/null landed during that circuit hour. Preserve that exact response-before-suffix handoff for the next circuit hour; unfinished work does not override alternation.

## Seven circuit targets and full goal

1. **Computational specification:** identify what is read, the operation/composition, what is written, and every downstream user.
2. **Cross-boundary grouping and within-module splitting:** group pieces across native modules when a reader treats them as one variable, and split a native module when its parts serve different computations.
3. **Held-out and OOD prediction:** predict activations and behavioral effects on unseen inputs, task variants, and shifted corpora.
4. **Extraction or sufficiency:** execute an isolated circuit at a declared boundary with every retained port and background named.
5. **Selective manipulation:** removal, swap, or edit changes the target against matched controls while preserving unrelated behavior and accounting for redundancy/interactions.
6. **Composition and reuse:** shared subcomputations serve multiple tasks/modules and their joint behavior follows a tested composition law.
7. **Stable identification:** units survive document/corpus splits, plausible gauges, and fitting restarts, or are defined by downstream operational equivalence.

Goal: a smaller transparent tensor program that **predicts held-out/OOD behavior, is extracted at a declared boundary, supports selective manipulation/removal, and composes/reuses**. Simplicity is the fifth, separately priced property: independently stored scalars/bytes, products, readers/writers, ports/states, edges, peak state, compute, and measured runtime. Lower error, storage, rank, variance, or CE cannot replace a missing behavioral property.

| Property | Evidence now | Verdict |
|---|---|---|
| Held-out/OOD prediction | Regional intermediate extraction has 20 fresh FineWeb context cells and scoped Pile tests, but packed FineWeb direction remains `88/102`; the seven-term census is opened replay. | **PARTIAL** |
| Extraction at a declared boundary | Residual6-to-attention8 and norm-closed MLP8 packages replay with declared ports; one complete residual6-to-head9.8 executor/replay is absent. | **PASS only at intermediate boundaries** |
| Selective manipulation/removal | Scoped fresh edits beat same-site nulls; the complete path lacks the frozen equal-norm, matched-split, three-reader full-suffix battery. | **PARTIAL** |
| Composition/reuse | Regional key/value interaction is `2.3448x` the smaller piece; omitting four higher-order head9.8 terms costs `.2853` local relative L2. Closed Möbius accounting is not specificity. | **FAIL overall** |
| Measured simplicity | Local package prices and exact rank floors exist; no deduplicated residual6-to-head9.8 or suffix bill exists. | **LOCAL ONLY** |

Claims remain separated as **fold/edit/response/fit** and **fresh/opened/replay**. Transfer units are actual distinct `(template, city pair, endpoint)` context cells, not row counts.

## Progress, failures, confounds, and alternatives

Since 18:47 there is **no new scientific receipt**: no model execution, CPU analysis result, response census, screen/null, preregistration, registry/dossier update, or commit. Only maintenance canaries ran. Consequently the prior circuit action made no progress and the five-property score is unchanged. The last scientific receipt remains the 18:33 opened fold replay: all four order-at-least-two `QK1*QK2*V` terms together are `1.8837x` the smallest target single and their omission costs `.2853` relative local-head L2 over 20 actual context cells; this forbids a three-single shortcut but proves no suffix effect or causal specificity.

Verdict-changing confounds remain baseline subtraction and frame mixing; native RMS/background dependence; nonlinear suffix/loss composition; shared token difficulty; opened-panel/post-selection leakage; dead knobs and tiny smallest-piece denominators; precision/noise floors; shared residual inputs masquerading as independent ports; and treating a closed Möbius identity as small interaction or random-split specificity. The fold must retain all seven head products, actual distinct-cell accounting, both ordered attention/MLP cross terms, and explicit open backgrounds.

Different routes remain: (1) **chosen**, finish the deepest exact regional replay and deduplicated price, killed as a useful simpler program if it does not exactly replay the native head9.8 delta or costs no less after all ports/state are charged; (2) fold head17.2 backward from its response-localized role, deferred because it abandons the deepest partially closed route; (3) open a token-family unembedding fold, deferred because it lacks the regional path's extraction and causal base; (4) fit or compress a suffix, rejected until the forward response census names a stable reader. Depth, exact port closure, and response-before-suffix therefore remain the highest-information policy.

## Chosen folding action

Build one exact, CPU-checkable program ending at the **complete head9.8 output-write delta** for the regional intervention:

`token IDs + residual6 -> attention7/MLP7 -> head8.2 city-write delta -> norm-closed MLP8 -> z9 -> complete head9.8 QK1*QK2*V delta`.

- **Native factors and shapes:** residual/write states `[T,1152]`; each attention Q1/K1/Q2/K2/V map `[128,1152]`, head output `O[1152,128]`; each MLP Left/Right `[4608,1152]`, Down `[1152,4608]`, bias `[1152]`; causal score cells `[T,T]` on `s<=t`; output delta `[T,1152]`. The existing observed interaction array is `[7,240,5]` over 20 distinct context cells and five readouts, but the executor must produce the vector write, not only those projections.
- **Included source terms:** every changed residual6/initial7/attention7/MLP7 source term permitted by the selected head8.2 and MLP8 readers; attention-output self, MLP self, attention-to-MLP, and MLP-to-attention ordered cross terms; live MLP quadratic and both ordered background/edit cross terms; and all seven head9.8 terms `dK1`, `dK2`, `dV`, `dK1*dK2`, `dK1*dV`, `dK2*dV`, `dK1*dK2*dV`.
- **Omitted terms:** no changed reader-permitted term. Terms provably identical between baseline and intervention may be cancelled in the delta, but their unchanged native background remains an explicit charged port rather than being claimed absent. Blocks10-17/final readout are outside this endpoint and remain the later causal test, not a hidden fold.
- **Nonlinearities/background:** native RMS with `eps=1.1920928955078125e-7`, causal mask, rounded rotary phases, two bilinear QK scores and their product with V, signed value mixing, learned residual/re-entry coefficients, MLP bilinear product and Down bias. Token IDs, residual6, unchanged query/source states, normalizers, masks/phases, and static weights must be declared and priced.
- **Simplicity measure:** compare against the same native boundary using deduplicated static scalars/bytes, runtime products/FLOPs, reader/writer count, edges, live ports/states, peak state, and measured CPU runtime. Shared arrays count once; lower replay error or storage alone is not promotion.
- **Exact replay check:** on the frozen manifest, compare every head9.8 output-write element against a direct native recomputation in float64; require registered relative-L2 and maximum-absolute gates, seven-term closure, off-support zero, and exact agreement of the five frozen readouts. Record actual distinct cells and hashes. A miss is an implementation failure, not approximation evidence.
- **Later falsifier:** first run the frozen forward-response census through blocks10-17, final RMS, unembedding, and softcap. Then test baseline/routing-only/value-only/product-suppressed/true-joint arms with equal-norm same-site directions, preregistered matched random splits, at least three unrelated readers, full logits/CE, smallest-live-piece interaction normalization, and fresh structurally varied cells. Unstable reader signs, failed recursive response replay, no matched-null advantage, or collateral comparable to target kills the claimed reusable path.

**Connection between tracks:** the fold identifies the exact seven native terms and their ports; the circuit response census determines which downstream users actually exploit them. Only the intersection licenses a reader-specific omission, grouping, or split.

## Registry and dossier audit

`COMPUTATION_PATH_REGISTRY.md` still stops `PATH-REGIONAL-CITY-001` at the head8.2 city-removal write. `CIRCUIT_REGISTRY.md` and `MODULE_DOSSIERS.md` carry much of the regional head8.2/head9.8/MLP7/MLP8 evidence, but not one canonical residual6-to-head9.8 endpoint linked to the seven-term census and both rank certificates. `CIRCUIT_GRAPH_REGISTRY_V1.md` inventories 49 packages but omits `city_mlp8_norm_closed_v1`; its `four_trait_verified` label also does not price the fifth simplicity property. The MLP index points to the MLP7 addendum and generic MLP8 dossier, not one canonical complete path.

Missing cross-link: extend `PATH-REGIONAL-CITY-001` to join the regional circuit record, MLP7/MLP8 and head8.2/head9.8 dossiers, `city_mlp8_norm_closed_v1`, the seven-term census, both rank certificates, and the pending response/factorial, with fold/edit/response/fit, fresh/opened/replay, distinct-cell manifest, ports, and deduplicated price. The path registry and board are concurrent dirty state, so no registry/dossier repair was attempted here.

## PAST_HOUR_TIMING

Window: **2026-09-19T18:47:12Z-19:53:21Z**. End-to-end serial candidate latency (`prior-art -> hypothesis/counterfactual -> native capability -> causal screen -> dossier/null`) is **unknown/not started**; no candidate entered the pipeline.

- **Design/scientific thinking:** unknown; no science phase boundaries.
- **Implementation/data building/indexing/refactoring:** none evidenced; no relevant file or run start appeared.
- **Validation:** unknown/none evidenced.
- **Model execution:** no research model execution. Maintenance canaries ran 18:53:04-18:53:32 and 19:23:36-19:23:54, **46 serial seconds total**; they are health checks, not evidence.
- **Interpretation:** unknown/none evidenced.
- **Documentation/review:** the prior review was written at 18:48:36; this review began with the authoritative 19:53:21 phase mark. Unmarked writing duration is unknown.
- **Idle/blocked:** unknown. Commit gaps and absent receipts are not converted into idle time; parallel durations are not summed.

The actual-date `RESEARCH_PHASES_2026-09-19.jsonl` contains sparse hourly review boundaries through 19:53 and no science/implementation/validation/publication boundary in this window. Coverage is inadequate for labor allocation; marks are boundary events, not uninterrupted activity. The last commit remains `9e0c03df3` at 12:21:10 UTC. Both queue files are empty; runner processes remain present and canaries completed through 19:23. The configured per-user `supervisorctl` path is absent, so process/log evidence—not that failed control command—is used for liveness.

## PROCESS_IMPROVEMENT

Ranked by likely saving/cost: (1) canonical regional path cross-link with uniform evidence/evaluation, cells, ports, and deduplicated price—likely 5-10 minutes saved per handoff at low cost; (2) one declarative complete-path replay manifest reusing existing exact helpers—larger scientific saving at medium validation cost; (3) consistent science/implementation/validation phase calls in existing runners—small code cost but only useful if actually used.

No shared helper, runner, or registry mutation is justified in this bounded review: the relevant registry and board are concurrently dirty, and a replay refactor before the exact end-to-end program exists risks extra ceremony. The sole bounded repair was the 19:53 phase mark, improving future timing coverage without fabricating past work.

## Explicit verdicts and handoffs

- `TRACK_ALTERNATION: PASS` — previous `CIRCUIT`; current `WEIGHT_FOLDING`.
- `TRACK_PROGRESS: FAIL` — the circuit hour produced no forward-response census, screen, null, or other scientific receipt.
- `CEREMONY_BUDGET: FAIL` — known scientific execution was zero while review activity recurred; unknown labor cannot establish that safeguards were smaller than science.
- `NOVELTY_LESSON_GATE: PASS` — the review searched the circuit/path/graph registries and relevant module dossiers, preserves full `QK1*QK2*V`, actual context cells, exact port closure, and response-before-suffix; it opens no duplicate candidate. The missing canonical cross-link remains organization debt.

**Highest-value action:** exact-replay and deduplicately price the complete residual6-to-head9.8 regional delta program specified above; do not substitute another rank/reconstruction sweep.

**CIRCUIT handoff:** run the frozen complete-city seven-term forward-response census through the native suffix on structurally varied distinct cells, with recursive response closure, full logits/CE, and at least three unrelated readers; only afterward run the matched-null five-arm factorial.
