# Hourly strategic review — 11 September, 14:22 UTC

Circuit targets: explicit read/operation/write specification; grouping across modules and splitting within them; held-out/OOD prediction; executable extraction or sufficiency; selective manipulation; composition/reuse; stable identification. The full goal is a simpler executable model explanation satisfying these properties. The user's weights-first direction remains authoritative; reconstruction is a discovery instrument, not circuit identification.

Since13:22, shared-input conditional solvers and output-sharing LL1 tools were implemented, controlled, and compared. Exact conditional updates alone do not ensure recovery: local traps and cancellation occurred on planted problems. Whole-group regularization bounds cancellation but does not remove poor minima. Eight-start projected versus joint LL1 each recovered only1/8. A restricted independent-input-block algebraic pencil recovers planted structure exactly; shared input intersections violate its assumptions. These are optimization/recovery lessons, not negative evidence against native structure.

The native four-arm pilot completed14:19:20. At nearly equal1.25Mparameter budgets, LL1 captures11.6604/11.6396% versus shared-input8.5285/8.5355%. Numerical, gain, and cost bars hold; shared-input10%capture bar fails. All120-second endpoints are unconverged. Complete-function cosines .90065/.86428 do not identify individual groups. No fresh behavioral circuit was identified; zero new four-property records. Original misses remain in their receipts.

The user asks whether hierarchy and DAG discovery must follow factorization. It need not: explicit graph structure can be optimized alongside weights. A dense-core symbolic control actually executed: two fully dense5variable quadratics (15monomials each) factor exactly into two products with a shared linear parent. This establishes the possibility in a small exact case, not scalable approximate/native discovery. The existing sparse dictionary already offers limited reader reuse, so general DAG work must add a distinct tested capability, not rename that implementation.

Decision: prioritize reliable shared-node discovery with adjustable bases and explicit graph price over another unchanged timed fit. Dense/sparse Tucker and LL1 are proposal families; neither core sparsity nor a group hierarchy certifies an arithmetic DAG. Before any larger native search, use existing implicit coefficient kernels and define the particular additional graph move and its recovery measurement. Avoid constructing a generic compiler or repeating numerical controls already available. A native LL1 projected continuation is still an option if it addresses a specific optimizer bottleneck; it is not automatically queued from its better pilot score.

Confounds: fixed-basis sparsity, hidden adapter cost, output-rank versus arithmetic rank, signs and gauge freedom, overparameterized graphs, numerical near-equalities mistaken for identities, local stationarity versus recovery, and polynomial equality mistaken for intervention equivalence. Natural-input ranking already disagreed with coefficient ranking; freeze before FineWeb validation. Pile stays separately OOD with historical-use caveats. No new semantic component claim warrants a duplicate dossier entry.

Foreground phase accounting13:22–14:22: implementation17.88min, science10.08, validation7.97, review8.71, publication15.35. These are labelled elapsed intervals including recovery, not CPU/GPU timings; sharedGPU runtime is not added. Science+implementation27.97min versus validation+review+publication32.03min. User-requested explanations, organization, and cleanup explain part of the overhead, but do not change the accounting.

- CIRCUIT_FOCUS: PASS within explicit user weights-first authorization. The work targets reusable computations and identification limits; no behavioral-throughput claim.
- CEREMONY_BUDGET: FAIL on aggregate elapsed labels. Repair: this short review and primary receipts suffice; no new automatic essay, redundant control framework, or duplicated native fit. Subsequent implementation must reuse kernels and target one additional graph capability.
- NOVELTY_LESSON_GATE: PASS. Existing shared dictionaries, LL1 controls, initialization failures and user/browser additions were checked. No new pronoun/gender discovery claimed.

Concrete continuation: board claim and executed DENSE_CORE_DAG_V1_CONTROL.json, with exact polynomial replay, recovered shared parent, and dense-versus-product distinction all held. Requested explanation now contains this result and a clearly labelled proposed graph search. General graph fitting remains unimplemented. Next hourly15:22, mathematical16:51.

Recorded 2026-09-11T14:25:13.178453+00:00.
