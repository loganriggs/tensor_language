# End-effect validation of the composed bridge

2026-09-12 before evaluation. Native scalar-generation test passed: exact<=8e-7, direct+mixed0.5–1.3%error, directonly33–36%error. These are intermediate changes, not causal effect guarantees. Keep original failures and all factors frozen.

48same regional rows; five body arms native, dynamic physical8+9 removal, physical8 plus exact-bridge predicted9 write removal, physical8 plus direct+mixed predicted9 removal, physical8 plus direct-only predicted9 removal. Reuse scalar fields computed analytically from pristine native z8,a8,r9 in SCALAR_PRODUCER_MLP_BRIDGE_NATIVE_V1_ARTIFACT.pt, arms1,2,5respectively. All later native layers recompute; no refit or new source-state capture.

Reference frozen9joint margins F come from the completed serial interaction test, where the removed9write stayed at its original value. Native dynamicjoint margins J define D=J-F, the effect of the second removed component changing after8removal. Approximate bridge arm T defines D_T=T-F. Compare these differences, not just T against nativebaseline or a much larger fullpair effect.

A EACHtemplate baseline/dynamicjoint regional/control margin replay<=1e-4relative versus previous native artifacts; exactbridge D_T versus D <=1e-4relative. This verifies the compiled conditional generator at the signed behavioral boundary.
B EACHtemplate direct+mixed D_T versus D <=10%relative. C EACHtemplate direct-only D_T versus D >=25%relative. Report wholepair cue effects and control-margin changes descriptively without replacing these fixed serial-effect bars.

Null: small intermediate scalar error does not imply signed effect fidelity, or directonly can suffice at the final boundary despite intermediate error. Any miss prompts outcome-sensitive/cancellation audit rather than fitting the test data. A pass is conditional compositional prediction on reused rows, not independently extracted inputs, broadOOD or repaired newline selectivity.

Price240single-row bodyforwards<=22tokens,120seconds,no optimization; managedlane1. Original head8.2 component still generated from its native input. All backgrounds, remaining QK/state generators and suffix stay charged.
