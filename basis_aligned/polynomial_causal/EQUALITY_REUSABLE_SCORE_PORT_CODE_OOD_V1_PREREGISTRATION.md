# Equality reusable score port code-OOD V1

## Question

The reversible L8H4 equality node now has exact semantic score, raw-payload,
support, and output-weight ports.  Earlier work froze L5H5 as the only portable
score donor for L8H4 and measured `.91918` causal recovery on the separate
code-OOD role using the older projected-payload arithmetic.

Without refitting its natural-text scale (`.5371214944`), insert L5H5's live
equality score into the exact-order L8H4 node while retaining L8H4's raw payload
and output projection.  Compare against the older projected-payload donor and
against the frozen wrong-score donor L7H3, using its own frozen natural scale.
Run all 192 code-OOD documents.  No OOD outcome chooses a donor or scale.

## Predictions

- **A — instrument:** all hashes, row roles, exact-node parent, intended edits,
  call counts, and finite nonzero target/control terms pass; native removal
  remains live in both halves.
- **B — exact-node score reuse:** the L5H5 exact-order donor recovers `[.85,
  1.05]` of the aggregate copy effect and more than `.70` in both halves and
  every near/far and predecessor-count cell.
- **C — arithmetic agreement:** exact-order and older projected-payload L5H5
  donor recoveries differ by at most `.03` aggregate and `.06` in each cell and
  half.
- **D — donor specificity:** exact L5H5 recovery exceeds exact L7H3 recovery by
  at least `.50`, and L7H3 recovery is at most `.35`.
- **E — preservation:** exact L5H5 changes all-noncopy mean NLL by at most `.01`
  nat from native.
- **F — reusable graph port:** the receipt records zero new learned parameters,
  one source score port, and the existing recipient raw-payload/output ports;
  no native L8H4 score is used in the donor term.

Passing upgrades the L5H5 score to a reusable upstream port of the extracted
L8H4 edge on code OOD.  It does not extract the residual-to-score Q/K producer
inside L5H5 or solve natural-to-code behavioral amplitude calibration.

## Price

One checkpoint load; 192 code-OOD documents; five forwards per four-document
batch (native, absent, projected target donor, exact target donor, exact wrong
donor): exactly 240 forwards.  No fitting, gradients, new text, or parameter
updates.
