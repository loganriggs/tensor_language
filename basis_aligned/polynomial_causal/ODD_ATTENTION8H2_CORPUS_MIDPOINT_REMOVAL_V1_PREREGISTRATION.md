# ODD_ATTENTION8H2_CORPUS_MIDPOINT_REMOVAL_V1 preregistration

Reuse all144 outcome-blind FineWeb and Pile/reference rows from the bound corpus
transfer result. For each recipient, the compiled edge receives the recipient
city embedding, paired donor city embedding, their arithmetic midpoint, or the
recipient itself. Only the inherited-city head8.2 write at frozen post-city
destinations is changed; its induced block9 state change is read only through
head9.8 O before the native suffix. Arms are native, full donor, midpoint, and
self:576 body forwards and no fitting or row selection.

- **A — exact descendant-preserving instrument:** native and full-donor target
  plus control scores replay the bound corpus-transfer artifact within `1e-5`;
  self scores and self compiled delta are within `1e-5`/`1e-8`; all values are
  finite and all nonself edge deltas are live.
- **B — capability:** retain the parent capability definition independently in
  FineWeb and Pile/reference: at least half the native paired city cues are
  positive and the full-donor target effect is live.
- **C — midpoint law:** paired midpoint effects differ from half the full-donor
  effects by at most10% in each corpus.
- **D — pair-centered necessity:** among parent-capable pairs, at least75% are
  attenuated and mean attenuation is at least2% of native paired-cue magnitude
  in each corpus.
- **E — selectivity:** each midpoint cat/dog, red/blue, Monday/Tuesday and
  apple/orange RMS is at most.5 of midpoint target-effect RMS per corpus.

This is a pair-centered removal of the edge's regional value contrast with
recipient routing held fixed. It does not remove head8.2, head9.8, other city
paths, or either module's other services. Existing broader producer-pair newline
collateral remains part of the record. Cached corpora are not proven disjoint
from pretraining or near duplicates.
