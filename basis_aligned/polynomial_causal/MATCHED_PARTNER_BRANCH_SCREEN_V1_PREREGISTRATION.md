# Frozen shared-parent branch screen

The weight-only SVD is frozen. Token readout suggested branch3 past-tense and branch8 progressive readouts; these are post-discovery hypotheses tested here, not independent replication. Use all512cached context pairs, all32cells, no exclusions, no task fitting. Cache was previously inspected.

Compute branch j write as $w_j(x)=s(x)t_j(x)d_j/\rho_{17}(x)^2$, retaining actual MLP16 product features and compiled readers. Branch0 uses $t_0=s$. Report self and first8 partners, plus sum of3+8. Each branch uses the same parent but a different partner. Removing writes keeps the rest of the native state fixed; it is a conditional output-port intervention, not upstream renormalized input deletion.

- A: finite effects, compiled joint-write identity<=1e-8, complete512pair/32cell coverage.
- B: branch3 past andbranch8 progressive each have donor-answer swap-effect RMS at least2x every other task within at least6of8contexts; target RMS>=1e-3. No favorable-cell exclusion. This is effect localization, not necessarily helpful support.
- C: each targeted branch has nontrivial coefficient change: scalar donor-base RMS / endpoint scalar RMS>=.01 in every context. Report all task coefficient RMS to distinguish input change from output-readout specificity.
- D: joint3+8 swap effects agree with sum of single effects to relative RMS<=.1 in every cell, with joint reference RMS>=1e-4. Report signed nonlinear interaction even on failure. Exact write additivity does not imply behavioral additivity.

Null: readable output directions do not imply task-selective input operations or additive causal effects. Native capability reported separately, same cached panel limits, no OOD or circuit-count claim. The existing671branch program remains14,481,792conditional floats. Executing one branch alone requires native MLP16 Left/Right,2compiled4608readers and1152writer:10,627,200floats; joint3+8 uses3readers and2writers:10,632,960. Full U, model background and normalization interfaces remain external and charged for any adoption. No sparsity-price promotion.
