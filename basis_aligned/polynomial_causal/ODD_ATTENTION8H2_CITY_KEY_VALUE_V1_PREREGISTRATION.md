# Head8.2 changed-city routing versus value

At block8 head2, retain every recipient query and every non-city source. At the
single changed-city source, independently substitute the paired donor's two key
states or its mixed current/inherited value state. The exact factorial is
recipient, full donor, donor routing with recipient value, recipient routing
with donor value, and their additive sum without the routing-times-value mixed
term. Apply only the resulting head8.2 city-source write difference at frozen
framing destinations, then feed only its induced block9 current-value difference
through the isolated head9.8 O branch and native suffix. Native plus four arms
on48 rows costs240 body forwards under180 seconds.

- `pred_a`: native and full-city target/work-jobs scores replay the preceding
  source-edge artifact within `1e-5`; additive plus mixed replays the full donor
  city channel within `1e-5`; the mixed channel norm is at least `1e-8`, outside
  framing is exact, values are finite, and exactly240 forwards execute.
- `pred_b`: native cue is positive for at least10/12 pairs and the full-city
  target-effect RMS is at least `1e-5` in each template.
- `pred_c`: omitting the explicit mixed term changes the full-city paired cue
  effect by at most10% in each template.
- `pred_d` (routing transport): routing-only error versus full city is at most
  35%, while value-only error is at least50%, in each template.
- `pred_e` (value transport): value-only error is at most35%, while routing-only
  error is at least50%, in each template.

Four unrelated control ratios are reported. This resolves the computation at a
specific changed-city -> head8.2 framing write -> head9.8-O current-value edge.
It does not test whole-head sufficiency, upstream city representation, corpus
OOD, static compression adoption, or quantization.

