# Frozen minimax support: fresh native syntax validation

September14 10:32UTC. The minimax mask improves the matrix spectral certificate
17.75% at equal stored-value count; historical cached ABC pass, but the
no-regression comparison D fails. This is a tradeoff, not adoption.

Freeze original sparse and minimax artifacts. Construct96new full prefixes
without model scores:4templates,2existing city pairs (London/Boston and
Manchester/Chicago),6existing UK/US endpoints,2counterfactual sides.
Include24old anchor prefixes. Scan existing rowfiles for duplicate full token
sequences, and use the shared article/one-token-cue check. No scoring filter.
This is fresh syntax, not new endpoints/cities, FineWeb or corpus OOD.

Managed GPUlane1 only:360nativebodyforwards,480finalreadouts,360extraMLPevals,
180second computation cap. Capture the same three native trajectories N/C/R,
actual upstream child/remainder fields, head17.2 three-contraction predictor,
and compact/MLP-only final states and matching linear parts. No candidate
weights are fitted or installed into the native body during capture.

Cache instrument A:24anchor raw margins and saved states/linear states replay
old cache within1e-4relative and1e-4maxabs margins. Validate frozen source/row
hashes before enqueue; syntax/dryrun and reviewed runner hash required.
After freezing cache, score both sparse programs with the existing conditional
mixed-numerator scorer and exact zero-operator reference. Report all4fresh
groups separately: ownmixederror<=10%,compacteffecterror<=5%, no material
own/compactsign reversals (threshold1e-5). Compare every group to original;
report>=5%aggregate squared own-error improvement and<=1%group regressions
as a separate Dgate, preserving historical Dfailure regardless of new result.

All native backgrounds, normalization, retained producers and unmodified
readers remain external dependencies. Unchanged control readers are unchanged
by construction, not evidence of discovered selectivity. No adoption from
fresh-syntax success alone; corpus/OOD and complete deployment remain open.
