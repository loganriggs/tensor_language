# Matched quotation control

The original new-lexeme quote control failed20%bar with20.9044%relative effect, but added a sentence. This separate control retains exactly18tokens on both endpoints, changing only the quoted they will VERB to they are VERBing while keeping the outside suffix identical. Same16lexemes and frozen branch8; branch3 descriptive. No capability filtering or revised original verdict.

A:5body forwards/40sequences including8sequence physical replay; native relative replay<=1e-5, all16pairs exactly equal length, finite and complete. B: branch8 swap RMS<=20%of the previously frozen immediate-progressive RMS0.012281792630968562. C: native capability>=75%both endpoints. Report the sign and signed removal CE, even if capability fails. Null: irrelevant quoted morphosyntax affects this branch beyond the stated selectivity tolerance. No data fit; program and background price unchanged.
