# Task14 subject–verb agreement capability-FIT execution authorization amendment

**Frozen prospectively:** 2026-09-04 08:40 UTC. **Status:** authorization successor awaiting final independent
review; not enqueued. This amendment changes no authority row, prediction, threshold, metric, model computation,
call request, evidence value, output namespace, or pass/fail continuation.

## Exact reviewed chain

This amendment is based only on the exact approved task14 authority/compiler and blocked producer build:

| Object | Exact Git identity or SHA-256 |
|---|---|
| repaired authority build | `e9686bc9bbb40f872d8e8320b30fab4f019e524d` |
| authority approval | `ea7efad782c088ba91a2ce338a9f740563c4e7c1` |
| capability compiler build | `fc586c1158ddeee7df8f4b502deec54189609c4c` |
| compiler approval | `10afc5d6005d169879b07e92cb5fcb4e3a65f312` |
| compiler review document | `a1707dd88949a9b5beb439b275e665cda1a7a62a6d5eedf076d20d192c852e59` |
| task14 generator | `33d7b62b3a0ffb4c798e75f085b7e96988e09b07be16667c5f9f8871c6339f94` |
| complete authority logical digest | `1cf6cf12668c7428719134bbee03ab84f57cc150f2653cc12ffc4a71566c8db1` |
| FIT authority file | `e88fd860c28c9b369abe4a8ec28372f93bb94b6e841265206c43e6929a25ac2f` |
| FIT authority-record digest | `3cf3315a77b3176418739e7a9357c0dbd9b95724d6b276038f53691b873377d1` |
| capability preregistration | `06a9747b4707999e11637a45cf83588bfd9cb8671d6b3a25790518af62900f8b` |
| capability compiler source | `98b2d263c5120c1a7b700dc4bb451f65cc9f9b338740d2cfbc7ae25a3ba5aab1` |
| compiled capability contract | `84f8e1cf85323dba94d13c7c716afef448b8621bff6b534c2025715420e86a82` |
| call manifest | `4b4da44c5090914f87d52e018bc9a8d18b74a202bdb82667283a9f1564682e0e` |
| metric manifest | `5da9f66829156e352afe087c75f92a7a6a37f06fe1ec5177efeffd9442609dcc` |
| blocked producer build | `26d45e89797515240eec368bc313728925d5f48a` |
| implementation preregistration | `d84d345c8d2b4183979cd09a57d60c87fccc5a36f03bddf0fa9316f07779a6f3` |
| model-facing producer | `9ba9448fcebcd764aa2b91e91333b3bbb2549a899b1f8304f2ce3f83bf741e3e` |
| approved blocked adapter | `7c0ef18db572dede3a65a355860efbc8d15787e7486c10f48e2643c0aa6f4f38` |
| producer-review commit | `753afa27e05b594acc39b0c1d84d72272c26e640` |
| producer-review document | `fddb2bac0595f733b765669cb41de1d21ad81a17205df4156505f332c0ea1ccc` |

The independent producer review approves exact build `26d45e89797515240eec368bc313728925d5f48a` only as input to this
separate prospective authorization. The successor must capture and hash-check this amendment and the exact review
document in addition to retaining the reviewed producer and complete upstream closure. The successor has a new
digest and is not covered by the earlier review.

## Exact one-run authority

Subject to final different-agent approval of the exact authorization-enabled adapter and use of the independently
reviewed hash-bound managed queue, this amendment authorizes exactly one managed invocation of task14 subject–verb
agreement FIT native capability. It is limited to:

- the frozen FIT authority only; no generated or read SELECT, TEST, or OOD authority;
- exactly eight native forward calls in literal order base A1, A2, P, C, donor A1, A2, P, C;
- exactly 256 unique row-side evaluations, 32 rows per call, at frozen final-token coordinates;
- exactly 2,048 raw numeric evidence bytes: one finite C-contiguous `float32[32]` answer-logit array and one
  finite C-contiguous `float32[32]` opposite-copula foil-logit array for every call;
- zero full-logit retention, hidden states, activations, gradients, backward calls, parameter updates, readers,
  writers, heads, MLPs, subspaces, localization labels, or later-phase artifacts; and
- only the frozen `circuit_battery_task14_capability_fit_v1` result, evidence, and receipt namespaces, installed
  create-only as evidence then result then receipt last using `renameat2(RENAME_NOREPLACE)`.

No direct producer invocation, alternate adapter, changed artifact, changed call order, extra array, second complete
execution, or unreviewed queue record is authorized. Runtime, source, module-identity, checkpoint, canary, namespace,
call, array, or price failure produces no scientific terminal and grants no automatic retry. Any retry requires a
failure audit and new explicit authorization.

## Runtime identity and scientific terminal

Real mode must capture all runtime roles and load them only from captured bytes. Before `run_science`, the successor
must prove that the producer's compiler/package/framework are the captured objects; the facade's `TT` is the captured
`jacclust.tt_model`; the captured fastloader resolves both its captured MLP-in-situ dependency and the captured
`jacclust.tt_model`; and producer imports of facade and fastloader resolve those captured objects. The existing exact
runtime/CUDA gates, pre/post full checkpoint hash replay, pre/post canary checks, native soft-capped forward, evidence
limits, and no-clobber publication remain mandatory.

The frozen opposing outcomes are unchanged. If every capability bar passes, the only possible continuation is a new,
separately frozen FIT-only localization preregistration. If any scientific capability bar fails, the valid terminal
is `hard_abort`, with every scientific projection null and no localization namespace. Instrument invalidity remains
distinct and creates no scientific claim. Neither terminal opens SELECT, TEST, or OOD.

## Final-review dependency

This amendment licenses construction of one authorization-enabled adapter successor; it is not an enqueue receipt.
The successor may set `EXECUTION_AUTHORIZED=True` only while it captures this exact amendment, the exact producer
review, and every reviewed source/authority role. It requires final independent approval at its exact SHA-256. The
managed queue record must bind that final reviewed digest, and the trusted runner must safely capture, verify, compile,
and execute those exact bytes without reopening a mutable path.

Until then, no queue edit, enqueue, model import, checkpoint read, CUDA use, model forward, result/evidence/receipt
publication or access, localization namespace, or task14 outcome access is authorized.
