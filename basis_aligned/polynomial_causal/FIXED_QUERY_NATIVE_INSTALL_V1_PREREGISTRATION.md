# Native installation of fixed-query source-column executor

Opened four OODv2 contexts, both roles and nine outputs; same per-input23-port oracle amplitudes as joint query-freeze receipt. Native attention11 is replaced, not merely post-hooked: baseline native attention output and baseline Q/K/V fields are prepared once; each edit recomputes only the nominated source K1/K2/V projections, normalized with native head RMS and native rounded RoPE. Source input RMS remains native. Explicit executor distributes O before token broadcast; no native attention call inside replacement. Baseline old writers currently uncached in this adapter, so charge2d² writer multiplies per edit (plus baseline generation). FP64 summation casts completed attention write to FP32; weights/source projections stay native FP32. Static weights and background ports remain charged, no end-to-end compression claim.

Counts12prefix,4baseline attention preparations,4reference baselines,8compiled edit suffixes,4compiled zero suffixes =16suffix. Prior full model and joint-query-frozen effects are read only, no oracle effect fitting. Instrument requires compiled zero-vs-native baseline logit maxerror<=1e-4; compiled-vs-native joint-frozen effect error<=.001 per output relative to full number-effect norm in all16cells. Report absolute and relative errors. Native fidelity to full effects uses10%number/5%controls; selectivity retains.8target/.1collateral. Baseline projection shape changes are a possible roundoff confound; failure is retained and investigated, not relabeled scientific rejection. Fresh OOD and exported model-free port package remain pending.
