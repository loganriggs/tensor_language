# Fresh confirmation and shared-output proposal for head8.2/head9.8

2026-09-12, before new panel model evaluation. Two exploratory bank passers were selected from27 tests on reused contexts. Freeze their source readers/projectors and the consumer metric. No confirmation-data refitting or candidate replacement.

Their four-output writers have cosine.999252, and .999491 under H, while combined source-reader cosine magnitude is.5162 (current.6308; first.0114). Layer8 and9 current states are different variables. This supports testing a common downstream output interface with distinct input/routing branches; it does not establish shared QK or interchangeable input readings.

Shared-output candidate: H-normalize and sign-align the two output writers, normalize their sum to u^T H u=1, and replace each w_i by u(u^T H w_i). Keep its frozen source reader and native joint QK. This fixes the merge from weights, with no new search. Compare both individual merged effects and joint merged effect to the unmerged components.

Fresh panel: Bristol/Boston and Oxford/Austin (new to the96-context bank screen), two new templates, six new spelling pairs centre/center, honour/honor, licence/license, travelled/traveled, cancelled/canceled, grey/gray. All pairings fixed by lexical design and one-token endpoint/cue checks, not model capability. 48rows. Register native capability per template: mean British-minus-American cue contrast>=.2 and >=10/12 positive. A native capability miss makes that template's circuit conclusion inconclusive, not a claimed absent mechanism.

Registered confirmation on EACH template: original component/fullhead swap-effect relative error<=10% for each passer; each component directed transfer>=10%of positive full three-producer group mean; >=20/24 directed signspositive and unrelatedmeanabs<=.5regionalmeanabs. Joint two-component swap must retain>=80%of positive group transfer and >=20/24 direction. Merged versus unmerged individual and joint effects each<=10%relative. Preserve all original thresholds and misses. Instrument original-model/fold/noedit<=1e-5, selected CPU/GPU or independently cached baseline<=1e-4.

Additionally measure reciprocal component-removal effects, interaction of the two components and newline-vs-comma margin on the spelling panel. This last endpoint is descriptive only: it is not a validated newline capability panel. Before claiming selective preservation of head8.2's known newline-setter function, add a separate predeclared newline-capability panel and establish native capability. This intervention targets the regional reader interface only; it cannot certify preservation of the whole native head's other consumers.

New examples establish controlled lexical/template transfer, not full corpus OOD or pretraining disjointness. Full native state generation and both QK computations remain charged. A passed common-output interface is conditional cross-boundary grouping, not a complete four-property circuit.
