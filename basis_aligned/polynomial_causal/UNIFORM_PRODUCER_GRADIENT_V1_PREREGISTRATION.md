# Fixed-support gradient under the actual producer's synthetic input law

Frozen September14 10:15UTC before execution; board entry is the full contract.
Two256-row gradient panels, one512-row held-out panel; exact first-panel line
solve on the first gradient direction. Preserve original sparse mask, frames,
value count and FP32 runtime. Native attention-output injection order is now
the declared convention, after the preceding <=0.0374% numerical audit.

This is a weight-only synthetic fit: uniformly sampled checkpoint-token rows,
native upstream hierarchy fields and suffix, conditional mixed-operator output.
It does not use language-corpus statistics, CE, labels or Fisher information.
All exact body/hierarchy/background dependencies remain external and charged.

A: directional derivative and FP32 executor replay <=1e-6, unchanged mask and
adapters, serialized bytes within1% of original. B: >=5% total-energy improvement
and >3 paired standard errors on512fresh contexts. C: independent gradient
cosine >=.5 and selected-contrast energy <=1.01baseline. Record all failures.
The single line solve is not global convergence; a pass licenses follow-up,
not adoption. No adaptive extension. CPU120seconds/two threads.
