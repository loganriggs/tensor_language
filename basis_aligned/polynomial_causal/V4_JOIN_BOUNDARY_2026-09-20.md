# Selective-source join boundary and first-block split

No additive-state join after a completed block11–17 preserves weak-component prediction in every openedv4cell. The exact pre11join passes8/8, postblock11passes2/8, and later boundaries pass1–2/8. Worst number errors at boundaries11–17 are.440,.377,.452,.459,.504,.512,.384 of the weaker single effect. This rules out the proposed short late suffix under this join approximation and dataset; it is not a lower bound on every possible smaller circuit. Nonmonotonicity prevents reading differences as local causal contributions.

Native singleton/joint/pre11 replays0; final-boundary shared-readout check passes.12prefix16full32joined suffix calls,1.458s. Method reuses the existing CrossFirst additive-boundary idea at a new selective source/control interface; no algorithm novelty claimed.

Splitting block11 at its postattention boundary yields6/8passes. The two next_to_subject plural cells miss at.1267/.1506 number error, while every control error is below.05. Thus the joint normalized MLP11 and later suffix recover much of the interaction, but dropping attention11's direct mixed state still fails.12prefix16full16joined suffixes plus4MLP11calls,1.410s; anchor replay0. This is conditional necessity under the stated reset intervention, not unique identification of one module.

## Executed next mathematical consequence

For two distinct edited tokens, query changes affect their rows and key/value changes affect their columns. The direct mixed attention write is confined to the later edited query reading the earlier edited source. Other destinations see independent changed source columns with unchanged queries; causality removes the reverse edge. This assumes tokenwise input/head normalization, fixed first-layer cache and the actual unnormalized product of two QK scores; a softmax row denominator would invalidate this simple support statement.

Reuse attention_mixed_edge_core.py rather than reimplementing its rational feature map. It represents the edge as a3x3bilinear core per head between normalized query features and normalized key/value features, retaining both QK products, input/head RMS, rounded RoPE and cached V. The mixed edge uses differences of both feature vectors. Background/source/reader coefficient generation remains charged. This was already algebraically validated earlier and failed as a standalone causal explanation; nothing here retracts that failure.

New CPU consequence check_two_site_edge_join_v1.py compares the existing compiled edge against an independent FULL-sequence four-corner attention calculation, with both token orders across the batch. Maximum mixed-write error2.78e-16, off-later-query residue2.22e-16. Adding the edge to the sum of singleton postattention states, then applying a native-form normalized bilinear residual MLP, reproduces the joint MLP state to1.78e-15. Omitting it changes the planted MLP output by.1142, a live control. The toy core has93values/input; for nine heads and1152output directions the same fully expanded output-port core costs31,296values/input, versus435for nine projected readers. Native coefficient and downstream state generation are not free.

Next native discriminator is to install this already defined edge correction into the postattention join, retaining all remaining native computations and existing weak-effect bars. That would test conditional compositional closure, not independently generated circuit states, parameter reduction or prospective OOD transfer. Upstream sources/selectors, remaining suffix and context generation still prevent full-goal completion.

Primary receipts v4_join_boundary_v1_result.json, v4_first_block_join_v1_result.json, TWO_SITE_EDGE_JOIN_CPU_V1.json and managed logs. Preregistrations V4_JOIN_BOUNDARY_V1_PREREGISTRATION.md and V4_FIRST_BLOCK_JOIN_V1_PREREGISTRATION.md. Shared evaluator native_source_observables32 optional start_layer and opt-in block capture leave defaults unchanged, with native replays above.
