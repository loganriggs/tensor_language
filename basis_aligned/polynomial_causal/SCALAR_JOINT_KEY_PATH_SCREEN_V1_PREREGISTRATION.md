# Frozen joint-key path pair screen

Question: can a pair of complete-QK input-product paths within8.2-first and9.8-current preserve regional spelling transfer while avoiding the existing newline collateral? These value sectors were selected by the earlier exploratory factorial; this is a finer exploratory screen, not independent confirmation.

Weights-only bank: SCALAR_JOINT_KEY_BANK_V1_ARTIFACT.pt. Each head's full256D joint-key rowspace is partitioned into four64D eigenvector bands of the uniform32relative-position averaged joint-numerator influence. Ten unordered band pairs include both slot assignments for unequal bands. All directions and mixed terms retained. Native full QK normalizers and complete query product retained. Basis small boundary gaps prevent semantic identification from eigenvectors alone.

Frozen candidates: all10 individual8-first paths; all10 individual9-current paths; all100cross-head pair combinations. No other value-sector selection after behavior is seen. A physical path intervention subtracts its scalar times the original physical producer writer; the later model recomputes, including dynamic head9field. This is a numerator routing-path edit, not a raw input-state projection or separate-head normalization.

Rows: previous48email/letter regional prompts and32FineWeb newline prefixes, all retained, including outlier30. Current screen is reused-data discovery; successful candidates require a later fresh confirmation with frozen identities and bars.

Regional123arms: native +120candidates +full8first/9current pair +originalfullproducerpair. Natural124arms adds actual meanhead8.2positivecontrol. Total9872bodyforwards,600second runtime cap, managedlane1. Syntax/no-model/source-hash checks required; no optimization. Runtime cost includes full model background.

A: all10keypaths x2value sectors sum to each native scalar<=1e-5relative; native/fullselectedsector/originalfullpair/meanheadcontrols replay prior factorial<=1e-4relative. Use relative differences with explicit nonzero reference checks and absolute FP32 floor, not an all-zero closeness gate.

B: report EACHcandidate regional coverage and controls. A regional passer requires>=50%nativecue contrast reduction,>=10/12positivepairedreductions, unrelated meanabsolute margin effect<=.5times regional meanabsolute effect in EACHtemplate. No lowering of earlier50%bar.

C: at least one SAME B-passing candidate has meanabsolute newlineCE change<=.02 and maxabsolute<=.1 in EACH16rowhalf, with existing native capability and actual meanheadpositivecontrols held. All120candidate outcomes reported regardless. A screenpass is not circuit promotion or multiplicity-corrected significance.

Null: finer weight-only jointkeyproduct paths still do not separate regional and newline services. Strongest methodological alternatives: sourceband basis need not align with taskspaces; full-normalizer reuse defines routingedge rather than inputstate intervention; current controls reused. Preserve failed four-sector result, allindividual/newline effects and unused path remainder. Do not expand candidate combinations after scoring within this registration.
