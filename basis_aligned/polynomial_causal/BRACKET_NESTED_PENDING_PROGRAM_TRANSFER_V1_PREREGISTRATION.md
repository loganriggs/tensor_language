# Bracket nested-pending program transfer v1

The nested two-pending-delimiter authority passed native capability on all 144 endpoints. This prospective test changes neither its rows nor the previously exported L13H8 program. It tests two frozen objects on the third construction:

1. the six immutable 1152-dimensional ordered closer-pair displacement vectors exported from SELECT rows; and
2. six scalar donorward effects, one per ordered closer pair, computed as the equal-weight mean of the two historical construction-specific tables in the committed suffix-free feasibility receipt.

For every endpoint, replay the native model through the exact L13H8 factorization. On target rows, compare exact donor semantic-opener-term interchange with adding the dispatched fixed vector to the recipient term. On answer-preserving outer-change controls, dispatch zero. The scalar predictor receives only the ordered answer pair. No row, vector, scalar, fit, direction, site, or threshold changes are allowed.

Price is four full forwards over 144 endpoints: direct native, exact factor replay, exact donor-term interchange, and fixed-program installation. This is 576 sequence evaluations, zero fits, gradients, updates, or quantization.

- **A — instrument:** bound capability passes; direct/factor replay maximum final-query logit error <= `1e-5`; 72 rows, 144 endpoints, 72 target endpoints, 72 controls, and exact price.
- **B — unchanged vector program:** against exact donor-term effects, overall cosine >= `0.80`, relative L2 error <= `0.60`, sign agreement >= `0.90`, and norm ratio in `[0.50, 1.50]`.
- **C — ordered-pair recurrence:** each of six 12-endpoint ordered-pair cells has cosine >= `0.70`, relative L2 error <= `0.60`, and sign agreement >= `0.90`.
- **D — controls:** zero dispatch changes every control final-query logit by <= `1e-5`, and every control retains a positive native closer margin.
- **E — six-scalar prospective law:** scalar-versus-exact overall cosine >= `0.80`, relative L2 error <= `0.60`, sign agreement >= `0.90`, norm ratio in `[0.50, 1.50]`, and exact donor effects are positive on >= `0.90` of every ordered-pair cell.

All five predicates must pass for `program_and_scalar_transfer`. A valid failure is a null and closes this construction without refitting. A pass is prospective structural transfer of the conditional exact-term program and a six-scalar behavioral effect law; native state generation and the downstream suffix remain external, and selective pair-centered necessity remains unresolved.
