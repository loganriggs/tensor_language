# Physical producer removal with recursive native execution

2026-09-12, before evaluation. Conditional shared-output pair passes fresh regional confirmation and natural newline edge-preservation V2. It does not yet support removal at producers. Physical unmerged writers are frozen by SCALAR_PRODUCER_NATIVE_LIFT_V1_RESULT.json: d_i=O_i Vconcat_i v_i, s_i C d_i equals original unmerged writer within4.3e-16. Use original source readers and d_i, not an arbitrary lift of the merged downstream writer. At each chosen producer subtract d_i*a_i(t) at all positions, with a_i recomputed from that arm's actual input. Later layers and their norms/routing recompute. In particular layer9 input changes after layer8 removal.

Rows: same frozen48fresh regional rows (now reused validation, not newly unseen); same32natural FineWeb newline rows from V2 (known training corpus). No refit or model-score selection. Regional arms native,physical8,physical9,physicaljoint,zero-wholehead8,zero-wholehead9,zero-wholeheadjoint. Natural arms native,physical8,physical9,physicaljoint,mean-wholehead8. Use frozen V2 pre-OV headmean; no additional calibration. Wholehead removal/replacement propagates recursively and preserves globally shared first values.

A instrument: baseline regional margins versus original fresh native cache <=1e-4relative; natural baseline and mean-head CE/effects versus V2 <=1e-4relative. Physical lift receipt must pass <=1e-10 and source bindings match. Zero/mean subtraction versus direct pre-OV head replacement <=1e-5relative. Frozen scalar executor has prior native contribution replay<=1e-5. No-op body execution is baseline.

B EACHregional template: native paired cue contrast mean>=.2 and>=10/12positive. Physical8/9/joint removal each reduces cue contrast on>=10/12pairs; joint mean reduction>=5%native mean contrast. Physical individual removal effect vectors match corresponding actual wholehead zero effect vectors<=10%relative; joint effects match wholeheadjoint<=10%. Eachphysical arm unrelated token-margin meanabs change<=.5regional token-margin meanabs. Report all per-arm effects and individual-sum/joint nonadditivity. These bars test whether the consumer-level components are faithful and selective as native interventions. A miss does not retract previous conditional results.

C EACHnatural16-row half: capability and mean-head positive control reproduce V2 (meanNL-comma>=.2,>=12positive,meanNLCE<=5; meanheadCEincrease>=.02,>=8positive). Eachphysical arm meanabsNLCEchange<=.02,maxabs<=.1. Same preservation thresholds as consumer-edge V2. Signed improvements do not repair absolute preservation failures.

Null: conditional component grouping does not define a globally selective producer unit; later consumers, changed QK/norms and layer8->9 dependencies change effects. Opposing prediction: source rank1 components retain their whole-head regional effects while sparing newline service even under recursive execution. No completed extraction or corpus OOD claim from a pass.

Price496single-prefix native forwards (48*7+32*5), maxprefix179,300seconds, no optimization. Full model and all native background still charged. Managed lane1. Preserve every miss and audit strongest methodological alternative before attributing a failure to absent structure.
