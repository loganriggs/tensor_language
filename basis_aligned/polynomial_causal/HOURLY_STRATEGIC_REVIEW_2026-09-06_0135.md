# Hourly strategic circuit review — 2026-09-06 01:35 UTC

## Circuit-first reminder

The objective is an executable predictive, composable, manipulable, and simpler transparent tensor program—not a growing collection
of correlations or fitted tables. Every next experiment must either remove a named runtime dependency, bound an attempted removal, or
package already causal evidence into an executable interface. Do not spend GPU work on generic rank sweeps, outcome-conditioned feature
growth, redundant localization, or stress tests without a decision consequence.

## Material progress since 00:35

- Closed Task14 local base-head compression after the final fixed two-reader span failed preregistered causal bars (overall cosine
  `0.72966`; two direction-template groups below `0.65`). One- and two-reader rank growth is finished.
- Compiled a common 16-scalar baseline-conditioned counterfactual-margin actuator. After one native baseline margin, it needs one lookup,
  one addition, and no native suffix execution. Task14/bracket effect recurrence is `0.96927`/`0.99626` cosine.
- Proved exact typed composition: identity/no-edit, idempotent and right-biased same-slot overwrite from an immutable baseline, and
  commuting independent slots across 3,285 exhaustive cases. Donorward effects are never incorrectly summed.
- Tested minimal native-margin generators with only six Task14 semantic coefficients and five bracket closer coefficients. Retrospective
  row-disjoint feasibility passed, so coefficients and two untouched corpora were frozen before outcomes.
- On untouched corpora, both baseline generators transferred (Task14 cosine `0.98702`, bracket `0.96896`) and Task14 absolute
  counterfactual margins passed (`0.98672`, perfect signs). The fully standalone 27-scalar combined program was nevertheless a valid
  null because bracket counterfactual margins failed (`0.80754` cosine, `1.02870` relative L2).
- Exact post-result attribution preserved that null and localized it: newest-construction bracket effects themselves transfer at cosine
  `0.99816`, relative L2 `0.06068`, and perfect signs. Baseline error is `95.07%` of total counterfactual-error norm; median cancellation
  amplification is `5.80x`, p90 `13.39x`, and maximum `46.15x`. The remaining problem is precision near cancellation, not edit transport.
- Released the strongest honest hybrid: six Task14 baseline coefficients, ten Task14 effects, and six bracket effects = 22 scalars
  (`88` bytes). Task14 is standalone; bracket retains one native baseline scalar. The exact composition contract is preserved.
- Materialized a hash-bound importable/CLI executable and evidence manifest. It passes all 64 typed Task14 and 45 bracket conformance
  cases, strict invalid-input rejection, and contains no model, Torch, transformer, training, or network import.

## Efficiency accounting

The decisive chain used fixed, bounded experiments: the two-reader closure test (5 forwards), semantic-baseline feasibility (4), and
untouched standalone validation (7), totaling 16 physical model forwards for the new scientific decisions. Compilation, error
attribution, composition, pricing, and API conformance were CPU-only. Each null closed its registered route; there was no threshold
repair, rank escalation, or re-fit after seeing outcomes.

## Current strongest claim and exact boundary

The v7 release is a predictive/composable/manipulable transparent answer-margin tensor program for the controlled Task14 and bracket
domains. It is much simpler than the v3 operational package (`22` versus `14,992` scalars) and directly executable. It is not a whole
model. Task14 needs only explicit direction, E/A/U/W membership, and edit specification. Bracket still needs the native unedited
donorward margin plus the ordered closer edit. Full logits, free-form text, internal-state generation, and whole-model replacement remain
closed.

## Strategic rejection

Do not rescue the bracket standalone null with a six-pair table, construction/template intercept, lexical feature, generic low-rank
state, or tuned cancellation threshold. The prior semantic-open zero-removal result was nonselective, and the direct-fold result explains
the effect of an already causal residual factor—not the complete native baseline. Therefore neither result licenses claiming that L13H8
alone generates the baseline.

## Next one-hour decision

Run one fit-free causal ceiling on the newest ledger/appendix construction: compare native replay, exact semantic-opener-term zeroing,
and complete L13H8-head zeroing at all 72 endpoints. Freeze bars before outcomes for (1) exact replay/live intervention, (2) fraction of
native donorward-margin norm explained, (3) residual-margin norm, and (4) recurrence across all six ordered pairs. This asks whether the
already localized term can plausibly supply the missing baseline precision. A pass licenses one predeclared direct-readout compression;
a failure closes local L13H8 baseline generation and retains the single native bracket-margin input. No rank or feature follow-up.

Next hourly review due around **2026-09-06 02:35 UTC**. The three-hour mathematical review remains due around **02:35 UTC** as well.
