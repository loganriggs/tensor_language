# Third-noun mixed residual state v1

Frozen parent: THIRD_NOUN_ANIMACY_V1_ROWS.json, all256 prefixes, all8 worlds, no fitting/filtering. Outputs already open; new interventions are prospective. This is operation localization, not renewed testing of the four failed sign rules. Prior art: token-support audit, four_corner_residual_lineage, v23 direct residual-readout factorial. Shared backend and finite-cube features are reused.

For each fixed (verb c, subject number s, attractor number a), let x(o,h) be a captured semantic-position vector. P_oh x=(x-flip_o(x)-flip_h(x)+flip_oh(x))/4. This orthogonal projector removes all terms containing both o and h, including their interactions with the fixed context. It is not just the mean oh coefficient across other factors. Remove P_oh from final raw residual x18, then decode x18-P_oh x18 through native final RMS/unembedding/softcap. No other token, suffix, or module is changed. This synthetic edit uses all four native corners and is not an independently produced circuit.

A instrument: bound inputs/source hashes, allfinite, restoredhooks, exact16nativeforwards/256seq; parent native margin replay and independent final decoder replay maxabs<=1e-3 AND relativeFrobenius<=1e-5. FP64 raw transported-write telescope relerror<=1e-5; interaction-removed FP32 rawstate residual mixednorm/original<=1e-4. Shared projector symmetric/idempotent, annihilates additive terms and preserves planted mixed terms. 600-second executor cap. A failure invalidates scientific interpretation.

B internal-interaction dependence: EACH of8worlds has original mixed-margin RMS>=.05 and remaining mixed-margin RMS after removal <=.25 times original. Allworld gate; no mean pooling, sign cancellation or bar rescue. Report signed effect projection and full-vocabulary mixed-effect metrics as diagnostics, not additional promotion bars. If B fails, do not assume an internally stored multiplicative selector; inspect relative internal/decoder contributions without fitting a rescue. If B passes, proceed to native producer/consumer localization before naming a circuit.

C raw first-attention boundary control: semanticquery is unchanged and both independent edits are at earlier source positions; first-layer unnormalized additive attention read therefore has zero cross-source oh interaction (no softmax). Its mixednorm<=1e-6 max(1,wholeattentionnorm), and embedding mixedexactzero. Following RMS/MLP need not remain zero. C failure invalidates this structural interpretation; report separately from A.

Descriptive source lineage: x18=k_e e+sum_l k_l(attn_l+mlp_l), k_l=product of later lambda0 scalars. Capture all55 native semantic state/write sites; save perworld mixednorms and the36x36 Gram matrix of transported P_oh writes. Matrix entries are native tensor inner products, not independent causal effects. They enable later source-cancellation audit without another forward. No module/head/source/rank selected by this run. Native floating-point ordering differs from the FP64 telescope; compare relative norm with registered A tolerance.

Price:16nativebatchesof16; 2x16 final decoder calls over512 finalstates (zero transformer forwards in decoder calls); nofits/backwards. Each biggest full-vocabulary FP64 array256x50304 is98.25MiB. All545902902nativeparameters retained, structural savings0. No independent extraction, selective unrelated controls, new held-out semantic predictions, or compositional reuse is claimed.
