# Hourly strategic circuit review — 2026-09-06 18:14 UTC

## Goal check

The active goal remains a predictive, composable, manipulable, simpler transparent tensor program. It is not complete: the component program and a construction-complete causal mode quotient now exist, but the quotient is still wider than necessary and has not yet been compiled into a standalone predictive calculation.

## Evidence added since 17:16

- Converged source-position programs: 8 `has/had` and 11 `is/was` components before pruning.
- Exact leave-one-out and simultaneous composition showed a 7-component irredundant `has/had` core but distributed small effects in `is/was`.
- Fit-only backward pruning prospectively sealed 7-component `has/had` and 10-component `is/was` programs, preserving 75.0%/76.3% and 69.4%/82.4% heldout/A2 recovery.
- Aligned-objective constrained DAS beat DIM on sealed A2 full-vocabulary error (0.23343 versus 0.23851). The earlier optimizer was not intrinsically ineffective; its scalar complement target was misaligned with the claimed vector quotient.
- Exact weight tensors were generated for 18 task/MLP contractions and 162 task/head maps. All 180 byte hashes reproduced. Relative tensor replay error was 1.17e-6 and gauge-score error 1.18e-7.
- Shared `MLP3/4/8` tensors have leading cross-task computation-mode cosines 0.791, 0.793 and 0.803. Shared machinery therefore reuses weight computation modes even though the literal task value subspaces have no high-cosine intersection.
- Static weight-read modes and A1-only suffix-Jacobian modes each failed complement closure. Their union nearly closed three panels but left an `is/was` A2 route.
- Complete A1 base/donor endpoint spans improved `is/was` A2 mode recovery to 93.6%, proving the missing route was partly state coverage rather than a discarded large singular mode.
- Construction-complete endpoint spans (original A1+A2), unioned with exact static weight modes, passed on a fourth disjoint lexicon: mode recovery retained 97.8–99.0% of the full programs and every complement was <=20% of its full-program effect.

## Strategic interpretation

Circuit discovery has crossed a boundary: expanding heads/modules is no longer the highest-value action for these tasks. The authoritative objects are now:

1. a prospectively pruned component program;
2. exact task-subspace weight contractions;
3. construction-complete contextual endpoint modes with fresh complement closure.

The negative interactions between shared and task branches show that component effects are saturating/overlapping, not additive scalar terms. Composition must occur as ordered tensor transformations or subspace unions, not by summing recovery fractions.

The remaining width is concentrated in empirical gradient tails. Full construction-complete MLP unions use at most 390/1152 dimensions for `has/had` and 119/1152 for `is/was`. Their 99%-energy endpoint ranks are much smaller (MLPs: 92–212 for `has/had`, 19–34 for `is/was`) and can be tested prospectively without consulting another causal outcome.

## Efficiency decisions for the next hour

- Stop general component-pool expansion for `has/had` and `is/was` unless a fresh full-program transfer fails.
- Do not repeat static weight rankings; exact static connectivity is now a certified compiler input, not a causal sufficiency test.
- Use one fixed 99%-energy construction-complete truncation, then one new disjoint lexicon. Do not sweep causal rank thresholds on the confirmation panel.
- If 99% passes, compile the retained MLP bases into explicit low-rank weight factors and test prediction/manipulation. If it fails, compare the preregistered failure to the already-known full-span screen before adding modes.
- Preserve full `L8H1` for `has/had` if necessary: its construction-complete endpoint span reached raw rank 128, so current evidence does not support head-output compression.

## Next checkpoint

Next hourly strategic review is due around 19:14 UTC. The three-hour mathematical review remains due around 20:26 UTC.
