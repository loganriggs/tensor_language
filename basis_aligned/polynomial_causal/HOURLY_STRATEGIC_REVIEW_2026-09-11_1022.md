# Hourly strategic review — 11 September 10:22 UTC

The circuit targets remain explicit reads/operations/writes; grouping across
modules and splitting within them; held-out/OOD prediction; extraction or
sufficiency; selective removal/interchange; composition/reuse; and stable
identification. The full goal is a simpler transparent executable program
with the four behavioral properties. Weight reconstruction alone is not that
goal. This checkpoint covers 09:22–10:22 under the user's weights-first direction.

## Decision

Continue the now-live coupled optimizer test on the two saved overcomplete
dictionaries. It changes the optimizer, not the dictionary family, penalty,
weight split or repaired encoder. It must use full joint convergence checks
and test the final folded function. Do not launch an identical alternating
continuation, another orthogonal rotation, or a million-token discovery fit.

If this locally converges without stable functional gain, demote this reader
proxy and favor a direct folded objective or genuinely different interacting
structure. If it times out, inspect its actual bottleneck before choosing more
steps; timeout does not establish absent structure. Existing free-product,
block, fixed-support ALS and output-metric receipts are prior art, not new ideas.

## Evidence that changed the decision

The zero-padding counterexample exposed an inference defect: identical features
could produce different reconstructed functions after arbitrary zero-score
support selection. OLS support completion fixed the controlled example and
raised full coefficient capture of both complete dictionaries from about29.9%
to33.2%, unchanged size. Their complete functions still differ, cosine.423.

Both overcomplete L1 fits completed about1800seconds/start without joint
convergence. The repaired four-arm comparison then raised learned full capture
to53.604/53.607%, versus sampled-initial dictionaries39.170/39.384%. All numerical
checks and registered encoding/tensor gains held. Historical weight-reader
gain remains below2percentage points; whole-function cosine.698 misses.9.
These are larger programs, not a matched-capacity win over complete dictionaries.
No stable circuit units or behavioral circuit screens have been established.

Coupled fixed-orthant L-BFGS-B plus global code refresh reached full joint
stationarity below7e-9 in both planted controls. This is numerical evidence
for trying it natively, not a native convergence or global-recovery theorem.
The managed run started10:20:52. Its first round lowers the objective but
increases joint stationarity after code refresh; improvement and convergence
must remain separate. No live-source change is justified by one round.

The frozen FineWeb radial check passed execution controls and failed every
registered improvement prediction. Orthogonal programs have lower MLP error
but worse KL; the oblique program worsens both. The cached-state red-team audit
replays native energies and the exact radial/traceless identity: normalized
energies .211+1.915−1.123=1.003, with mean-vector metric cosine−.966. Natural
states contain strong cancellation absent from the ideal-sphere metric.
This is a post-result mechanism diagnosis, not independent predictive evidence.
It requires no new body pass or data-guided feature discovery.

Primary detail and receipts are linked once in the
[10:21 explanation](explanations/2026-09-11/explanation_2026-09-11_1021.md).
The earlier snapshot instrument boolean incorrectly accepted missing replay;
its explicit correction remains in the09:22 review and primary index.

## Alternatives, confounds and circuit relevance

First priority is the coupled optimizer result: it distinguishes an unresolved
optimization problem from a locally settled weak reader objective. Second is
direct full-U discovery allowing cancellation and new products, using existing
operators rather than rediscovering conditional Down or fixed-support solves.
Adaptive overlapping structures remain preferable to another fixed orthogonal
rotation if the proxy settles weakly. Multi-stage factorization is still
unconverged; it has not been ruled out.

The structural test can improve explicit shared computation and stable
identification, but reconstruction is only a screen. Any candidate subsequently
advanced as a circuit needs extraction, selective intervention, composition
and held-out/OOD prediction. Keep native background and index storage priced.
Native pairing, normalized readers, fixed penalty, feature count, initialization
anchoring, repeated historical holdout inspection and function instability are
live confounds. Natural-input cancellation and final RMS/tanh explain why
coefficient or spherical energy cannot substitute for behavior. FineWeb is the
training corpus; Pile is separate shift testing, with historical adaptation
explicitly disqualifying untouched-OOD claims.

## Throughput and continuation

Authoritative phase intervals: science13.14, implementation19.17,
publication22.75 and review4.94minutes. Science plus implementation32.31minutes
exceeds other categories27.69, but publication is still too close to half the
hour. These coarse foreground labels include asynchronous GPU overlap; do not
retroactively relabel missing boundaries. Keep the next block scientific and
reuse short primary receipts instead of another narrative per control.

- **CIRCUIT_FOCUS: PASS within user-directed weights-first scope.** Shared
  features and stable functions were tested; zero new behavioral circuit
  screens/identified circuits. No claim of meeting the older circuit-throughput target.
- **CEREMONY_BUDGET: PASS, narrowly.** Science plus implementation remains
  larger than overhead. Further narrative waits until a major result, user
  request or hourly boundary; reuse existing math/encoders/executors.
- **NOVELTY_LESSON_GATE: PASS.** Prior mechanisms were searched and reused.
  Encoder repair, coupled optimization and cached cancellation diagnose distinct
  limitations. None is relabeled as a newly identified circuit.

Concrete continuation: the native coupled run is managed-live, source SHA
`d8dda4d7c1443de4cf08d0e87020ad6a061cdb8562761c1cb6b5b0dcc41d34a5`,
with saved-round states and original joint stopping criteria. The cached
cancellation analysis was actually executed. Next hourly11:22; math10:51.
