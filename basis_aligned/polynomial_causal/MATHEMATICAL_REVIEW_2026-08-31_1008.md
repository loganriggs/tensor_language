# THREE-HOURLY MATHEMATICAL REVIEW — 2026-08-31 10:08Z

Convention (§2135): CE numbers are damage above the real model; LOWER IS BETTER.

## Fresh facts this review answers to
Frontier +1.8765 fresh / +1.9474 census (0/62 certificates). Three clean laws landed since the 0707 review:
(1) EXACTNESS-VS-STEERING (§2210/§2221 vs §2224/§2227, all on controlled instruments): refit only FITTED
objects (dictionaries) and only with FULL-OUTPUT trajectory targets; never refit real weights (exact
+1.947 < in-frame refit +2.148 < trajectory refit +2.305), never trajectory-target a span edit (frame mixing,
+3.05). (2) The Möbius lattice is measured; cross-type J ≈ 0. (3) The class map INVERTED (§2228): name +3.13
and subword +2.32 now dominate; the §2145 retrieval law is dead in its old form.

## Top three mathematical moves

### 1. Gauss–Newton/sensitivity-WEIGHTED estimation → EXECUTED (rung 135, queued)
Object: every dictionary fit in the program (aXL CV/LW first). Operational fact worth recording: a CONSTANT
output metric provably cannot change multi-output least squares (the objective separates in the metric's
eigenbasis and each direction's minimizer is scale-invariant) — so metric-aware fitting must enter as
PER-POSITION weights w_t = ‖∂CE/∂h_t‖ (one backprop on the real model). Weighted class means then differ
within class; weighted LW ridges differ through the input Gram. Assumption that may fail: sensitivity on the
REAL model mismatches sensitivity in the config frame. Consequence beyond reconstruction: a principled bridge
between the two ledgers (weights concentrate exactly where certificates measure). Falsifier: rung 135's
bars (census ≤ 1.92, increment ≤ 0.30). Closed-list distinction: NOT §2125 Fisher unit-selection, NOT §2118
metric-K.

### 2. The exactness-vs-steering law as a compilation TYPE SYSTEM (record, no run needed)
Objects: replacement sites typed {real-weight, fitted-full-output, fitted-span-edit}. The four controlled
rungs induce the placement rule: trajectory targets ONLY on fitted-full-output; in-frame ONLY as scaffolding;
real weights NEVER refit at 54k samples. This is a checkable static discipline for every future rung — filed
in the ledger (§2227) and the review; violations are now predictable failures, which is what a type system is.

### 3. Automata/trie structure for ASSEMBLY classes (design; falsifier queued as rung 136)
Object: the name/subword damage (+3.13/+2.32) — multi-token composition, near-deterministic continuations.
A weighted-automaton (Hankel/minimal-realization) view says these need STATE (prefix identity), which no
10-class memoryless dictionary can carry; the cheapest discriminating fact is WHERE the damage originates —
rung 136 (exact-front arm) splits front-assembly from motif/tail causes before any module is designed.

## Pruned
Fisher-orthogonality direct test (superseded: the J_ij lattice already measures the cross terms it would);
invariant-theory quotients (still gated on rung 90); bisimulation metrics (certificates already operational);
IB (no new handle); simultaneous factorization (CP-with-own-units made it moot at the front).

## Executed
Rung 135 (sensitivity-weighted dictionary estimation) and rung 136 (exact-front class attribution) built,
preregistered, dryrun-clean, queued. §2227/§2228 written first (score-as-written, failures preserved).
