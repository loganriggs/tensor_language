# Hourly circuit-only review — 2026-09-06 00:35 UTC

## Circuit interpretation targets and full goal

The target remains a transparent tensor program that states what information is read, what computation is performed, where the result
is written, and which downstream consumer turns it into behavior. It must predict untouched activations and effects, support selective
removal/swapping/editing, compose without unexplained cross-terms, and be literally simpler under storage, arithmetic, dependency, and
native-compute accounting. Native heads and MLPs remain intervention coordinates, not presumed semantic atoms.

This hour produced two fixed executable programs, tested bounded cross-program composition, priced the first program literally, compiled
both into a single deterministic dispatcher, and found a six-scalar suffix-free bracket effect law that transfers across two
constructions. The full goal is not complete: the combined dispatcher still needs an external manipulation specification and native base
activations; Task14 still needs a native suffix for causal execution, and the bracket suffix-free law has not yet passed a fresh-corpus
prospective test. No whole-model or end-to-end simplicity claim is licensed. The durable goal remains active.

## What changed since 23:35

The Task14 downstream six-gain mediator law failed its sealed fourth-corpus promotion. Although its overall MLP15 aggregate passed, the
separately required singular-to-plural under/beyond recurrence collapsed to cosine `0.06627` and sign agreement `0.42969`. The joint
mediator verdict remains null; the ten-vector upstream program and fixed reader survived independently at cosine `0.94456` with perfect
signs. Four additional collateral behaviors—polarity, narrative tense, preposition selection, and voice frame—were preserved by all ten
writes, with worst median normalized effect `0.00405` and zero flips. The initial `5e-5` FP32 tripwire was repaired append-only at the
existing `1e-4` activation tolerance without rerunning outcomes.

Literal accounting corrected the earlier control/program conflation. Task14's selected state is ten 1,152-D vectors plus two 1,152-D
readers: `13,824` FP32 scalars (`55,296` bytes), not 13,824 upstream-vector scalars. Installation is 1,152 additions; installation plus
one dense reader is 3,455 scalar operations. Storage falls `64.706%` relative to a 32-vector table with the same readers, but the harness
eliminates zero native blocks and zero native parameters. The correct classification is `interface_simple_not_end_to_end`.

A second fixed program was compiled for bracket pending-opener state: six SELECT-frozen L13H8 ordered-pair vectors. On 144 untouched OOD
target endpoints it reproduced exact opener-term swaps at cosine `0.98986`, relative L2 `0.16973`, norm ratio `1.08280`, and perfect signs;
216 answer-preserving endpoints dispatched exact zero. The complete export, capability, and causal price was five forwards and 1,584
example evaluations with no gradients or fits.

Exact four-corner composition was asymmetric at native scale. Bracket stress was `2.2515×` the isolated Task14 effect, while Task14
survived at cosine `0.99983`, relative error/interaction `0.01863`, and perfect signs. Task14 stress on bracket prompts was only
`0.0004896×`, so the reverse claim was correctly withheld. An outcome-free median-norm match made reverse stress material at `0.31572×`;
bracket then retained cosine `0.98621`, relative error/interaction `0.17838`, and perfect signs. This licenses robustness at the artificial
matched norm, not original-gain two-sided or natural joint-semantic composition.

The two programs were packaged into one exact dispatcher holding 16 vectors (`18,432` FP32 scalars, `73,728` bytes). Across all 512
Task14 validation cells, 144 bracket targets, and 216 bracket controls it selects the exact frozen vector—or exact zero—from only the
compact manipulation specification. Recipient/donor role prompts, row/template/family lookup, and a model call are no longer dispatch
dependencies. External edit specification, native base activation, and native suffix execution remain.

Finally, a leave-target-family-out diagnostic tested six scalar bracket effects instead of the native suffix. Bidirectional transfer
between direct-type and completed-then-reopened families reached pooled cosine `0.98181`, relative L2 `0.19025`, norm ratio `0.99373`,
and perfect signs; each fold exceeded cosine `0.989`. This is retrospective feasibility only. A third construction with an identical
completed distractor before a newly opened delimiter has now been frozen: 36 untouched rows, 72 endpoints, six balanced ordered pairs,
and zero opened model outcomes. The prospective runner and thresholds are written and under preflight.

## Throughput and systems audit

From 23:35 through the review build there were 37 commits, with median inter-commit gap `1.12` minutes, mean `1.57`, and maximum `7.02`.
GPU work stayed serialized through the managed queue. The main efficiency improvements were immutable-evidence reuse for zero-GPU audits,
batched endpoint interventions, literal elimination of prompt-based vector lookup, and reducing the next suffix hypothesis to six scalars.
The second program cost only five forwards end-to-end; cross-program stress used ten forwards; the dispatcher and scalar feasibility audits
used none. The new prospective suffix test is budgeted at two forwards over 144 example evaluations.

`CIRCUIT_FOCUS: PASS.` Every scientific action changed executable program extraction, causal transfer, collateral, composition,
dependency removal, or a prospectively testable suffix-free prediction. No generic rank, reconstruction, or representation sweep opened.

`CEREMONY_BUDGET: PASS WITH THREE PRE-OUTCOME REPAIRS AND ONE NUMERICAL AUDIT.` The bracket capability target-cell count was corrected
before outcomes; a staging path error was disclosed and the identical source was committed before its queued runner began; and a compiled
dispatcher v1 contradiction (self-pairs both zero and rejected) was abandoned before execution and corrected in v2. The broad-collateral
FP32 audit reused immutable outcomes. No scientific result was overwritten or silently reinterpreted. The recurring lesson is to run a
literal balance/API consistency check before claiming and enqueueing, and to use `set -e` for every multi-step shell transaction.

`NOVELTY_LESSON_GATE: PASS.` Prior-art receipts and append-only claims preceded every outcome. The mediator null, native-scale reverse
inconclusive, and malformed dispatcher v1 remain preserved. Artificial norm matching and retrospective scalar transfer are labeled with
their exact limited scope rather than promoted into stronger claims.

## Confounds and evidence boundary

- Task14's ten vectors transferred prospectively, but its fixed six-gain downstream mediator law did not; do not merge those verdicts.
- Bracket's six vectors transferred prospectively on OOD prompts and preserve exact-zero controls, but their scalar suffix replacement is
  so far retrospective across two already opened constructions.
- Norm-matched reverse stress is chosen solely from artifact vector norms, not outcomes, but its gain is artificial and not part of either
  natural program.
- The combined dispatcher removes role-prompt lookup only. It does not infer an edit request from raw text, construct a base activation,
  or predict full logits.
- The six-scalar law predicts donorward program-effect magnitudes, not native behavior or the entire vocabulary distribution.
- The fresh third-construction rows were content-addressed, token-roundtripped, pair-balanced, and semantically located before any model
  execution. Their native capability is still unknown and is a hard stop.

## Alternative routes and decision

1. **Fresh-corpus suffix-free scalar validation — selected.** Freeze the six values from the original direct family only and test native
   capability, vector recurrence, scalar cosine/error/signs, and every ordered pair on the untouched completed-distractor construction.
2. **Task14 suffix-free effect reader — second.** Its existing fixed readers already predict installed effects, but a literal package must
   separate which context values remain required and prospectively test prediction without native suffix execution.
3. **Base-state construction — third.** Once effect readouts are prospectively stable, isolate whether the native contextual base at each
   write site can be replaced by a smaller local state program. This is the largest remaining dependency and should not be attacked with
   an unconstrained reconstruction sweep.
4. **Natural joint-semantic composition — deferred.** It remains valuable, but the native-scale cross-program result already establishes a
   strong one-sided interaction bound; suffix/base dependency removal now advances literal simplicity more directly.

The decisive next action is to commit and enqueue the already-preflighted two-forward fresh-corpus bracket scalar validation. On a native
capability failure, stop without interpreting the scalar law; on capable scalar failure, preserve the retrospective screen as diagnostic
only; on a pass, promote the six scalars as a prospective bracket effect reader and update the combined program price.
