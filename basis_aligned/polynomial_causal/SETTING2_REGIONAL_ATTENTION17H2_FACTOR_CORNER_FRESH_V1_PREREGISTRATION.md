# Fresh confirmation of the head17.2 QK2/value corner

Frozen after the opened `QK1 × QK2 × value` factorial selected exactly the
`QK2`, `value`, and `QK2×value` Möbius terms. Algebraically this is one corner
program: retain native head17.2 QK1 while replacing QK2 and the mixed
current/inherited value with their states under the fixed upstream head9.8 QK1
edit. No support, coefficient, threshold, row, or reader may change after model
execution.

The authority contains 48 entirely new controlled rows: two templates,
Norwich/Portland and Cardiff/Denver, and six one-token UK/US endpoint pairs.
Exact prompt overlap with all prior row manifests is zero. This is fresh
controlled transfer, not proof of pretraining or corpus OOD.

## Frozen predictions

- `pred_a_exact_instrument`: exactly 12 full executions; carry, head9.8 manual
  attention, attention17 head sum, head17.2 projection, native/full corner
  endpoints, and `selected = corner(0,edited,edited)-corner(native)` all pass
  relative error `2e-6`; outputs are finite.
- `pred_b_native_capability`: at least 9/12 native paired cue effects are
  positive in each family and native paired target RMS is at least `.01`.
- `pred_c_fresh_response_replay`: selected versus full head-response paired
  reader effects have aggregate cosine at least `.90`, aggregate relative L2 at
  most `.25`, and each-family relative L2 at most `.35`.
- `pred_d_fresh_causal_installation`: selected versus full installation into
  the native layer-17 background has cosine at least `.90`, relative L2 at most
  `.35`, and sign agreement at least `.80` in each family.
- `pred_e_fresh_causal_removal`: selected versus full removal from the
  upstream-edited layer-17 background passes the same per-family bars.
- `pred_f_controls_and_random_null`: for install and removal, selected control
  RMS for each of five unrelated readers is no more than `1.25×` the full
  program plus `1e-6`; selected aggregate target relative L2 beats the best of
  eight deterministic equal-norm random writes in the native head17.2 output
  subspace by at least `.10` in both directions.

Price: 12 full-model executions/96 sequences; three factor corners; four
additional full/selected suffix evaluations; sixteen random-null suffix
evaluations; 960 additional suffix sequences; zero fits, backwards, gradients,
updates, or quantization. A pass establishes fresh predictive and bidirectional
causal transfer of this factor-corner operation. Extraction remains conditional:
native QK1 and upstream-edited QK2/value are three explicit live ports.
