# Hourly circuit strategy review — 2026-09-06 15:17 UTC

## Circuit interpretation targets

The live target remains a computation-level circuit: what is read, computed, and written; operational grouping across native boundaries and splitting within modules; held-out/OOD prediction; executable sufficiency; selective removal/swap/edit; predictable composition and reuse; and stable identification across corpora and fits. The program must ultimately be predictive, composable, manipulable, and literally simpler in stored state, compute, edges, and program description. Rank, reconstruction, correlation, and CE alone do not complete it.

## What changed since 14:17

The exact temporal writer-to-reader path was made executable: writer-free installation of the captured block9 H1/H4 and block11 H3 responses reconstructs roughly 96–101% of the writer effect. Reader responses are largely rank-one within a corpus, but their axes are cue/template dependent. The summed block8H1 writer coefficient predicts reader coefficient direction well, yet repeated prospective compilers fail causal dose under reverse cue transfer, frame conditioning, and norm normalization. Adding base subject-attention mass raises coefficient correlations to 0.982–0.990 but worsens installed recovery, closing that reader-gain account. The live alternative is therefore the exact local attention operation (pattern, value, interaction), not another writer scalar transform.

Two corrections matter. A fresh capability manifest prospectively removed three incapable row pairs before the reverse compiler. The claim ledger also became unreadable because eight releases used descriptive outcome strings; append-only history is retained through a narrow legacy reader allow-list, while new writes remain canonical.

The user's DAS challenge reopened a more fundamental identification question. The old cached cross-layer DAS result is invalid because full-rank projected patches do not close. The repaired single-H3 result does optimize its registered scalar loss better than DIM and transfers better on A2, but its published evaluation uses signed mean answer-margin fractions. A frozen-axis target audit is now claimed to test the exact held-out scalar loss, cancellation-resistant row metrics, and centered full-vocabulary causal-effect fidelity without refitting.

## Confounds and alternatives

- Baseline subtraction: exact H3 and q=I closure are required separately for every evaluated split.
- Frame mixing: the reverse compiler is now capability-filtered and the DAS audit keeps A1-heldout and A2 separate.
- Nonlinear composition: subspace and complement effects are evaluated independently; their signed sum is not assumed to equal the full behavioral effect.
- Leakage/post-selection: the DAS axis is frozen from 16 even A1 rows; heldout A1 and A2 are unopened by the audit fit because there is no audit fit.
- Signed cancellation: the original near-zero heldout complement mean may hide large opposite row effects; mean absolute, RMS, and p90 are now mandatory.
- Target leakage: optimizing only the answer/foil margin may identify a readout-specific direction. Centered full-vocabulary causal-effect fidelity is the discriminating independent output.
- Alternative object: exact attention pattern/value/interaction decomposition remains the best next mechanism-level test if the DAS audit completes; it changes computational specification and within-head splitting rather than rank.

## Throughput audit

From repository timestamps 14:17–15:17, 25 commits landed across the shared lanes. This lane completed executable response composition, response-rank tests, three coefficient/compiler falsifiers, OOD/cross-cue mode tests, a prospective capability repair, and the reader-gain null. GPU runs were seconds; repeated bespoke authoring, interpretation, and claim handling remained the limiting cost. The circuit target advanced rapidly, but the number of nearly adjacent compiler variants and a broken claim ledger exceeded the intended ceremony budget.

`CIRCUIT_FOCUS: PASS` — every experiment targeted path extraction, held-out prediction, composition, or reader-state identification.

`CEREMONY_BUDGET: FAIL` — repeated one-off compiler wrappers and malformed manual releases dominated more than the underlying short screens. Forced repair: the current DAS work is a frozen no-fit audit, and the next attention-operation experiment must factor a reusable exact response-decomposition helper with a focused algebra test before another bespoke runner.

`NOVELTY_LESSON_GATE: PASS` — prior DAS, response-rank, cross-cue, and writer/compiler receipts were searched; cross-layer closure invalidity, capability gating, and the distinction between correlation and causal dose were applied.

## Ranked next moves

1. Complete the frozen constrained-DAS target audit. Kill the optimized-axis claim if it loses DIM on held-out scalar loss; restrict it to a task-margin quotient if it loses full-vocabulary fidelity or relies on cancellation.
2. Implement exact pattern/value/interaction response decomposition for the fixed block9 H1/H4 and block11 H3 readers. Kill the account if the algebra does not close or no operation type transfers across both cue corpora.
3. Only after an operation type is identified, test joint installation and selective cue edits. Kill promotion if the operation cannot reproduce the writer effect on prospective capable rows or produces comparable P collateral.

The current route survives alternatives because it moves from a known exact path to the operation producing its dose. More coefficient transforms are closed; broad head sweeps and compression work have lower information value.
