# Frozen common-quadratic source block: native FineWeb validation

Use64 fixed cached FineWeb prefixes of9tokens, selected before this run. No reader,
writer, topology or data-metric fitting. The source dictionary and algebraic
private query projection are frozen from the exact-secant and common-quadratic
artifacts. Recompile their private query maps at source0/7 for query8, as specified
by the original weight-only objective. Other source positions remain native.

A: existing mixture executor control<=1e-10; reconstruct complete query8 attention
from all9 source contributions, preserving native rounded rotary and per-head
RMS normalization, and match actual native attention output within1e-5 relative.
B: full projected writes and two-feature block writes from collapsed versus stable
secant agree within.1 relative norm at EACH source0/7. A must pass.
C: remove the block summed over these two source positions, retaining the actual
native remainder. Full50304-logit effect error collapsed versus secant<=.1 relative
and secant effectnorm>=1e-6. A must pass. Report two child removals and their
nonadditivity relative to joint removal; no assumed linear logit composition.

These are function/extraction and removal-preservation screens, not evidence of
semantic selectivity, OOD generalization, multi-task reuse or whole-model adequacy.
Shared parent has two consumers mainly in head17.2. The native remainder, all
normalizers, private query maps and full downstream weights remain dependencies.
Report changed token ids descriptively, without selecting a behavior to score.

8 partial-body forwards,64 sequences,576tokens; at most6batched final-suffix arms;
one managed GPU,180second hard limit. Save native current/base source ports and
children for replay, not full vocab logits or checkpoint weights. Source-reader
sharing prices46independent1152vectors, but this validation prototype duplicates
readers; no implemented storage-saving or whole-model adoption claim.
