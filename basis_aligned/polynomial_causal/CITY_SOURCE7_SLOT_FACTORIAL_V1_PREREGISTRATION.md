# Separate MLP7 key-routing and value slots at the same city-write boundary

Prior registered facts: the MLP7-present group fails fresh direction80%vs90%; both
self and mixed terms reverse in the same four documents; reversals persist at.1and.5
strength with approximate amplitude linearity. Stop attributing the failure solely
to large-edit curvature or only to cross terms. This opened diagnostic instead
separates the operational role of MLP7 in the keys from its role in current value.

Keep native full queries and normalization denominators fixed. Let Pkv be the
negative city write with MLP7 included in BOTH key slots iff k=1 and in current
value iff v=1; all other sources and inherited value remain. Then:
present=P11-P00; keymain=P10-P00; valuemain=P01-P00;
cross=P11-P10-P01+P00. Preserve the full QK1*QK2*V product. Assign native-reference
rounding remainder to cross so present=keymain+valuemain+cross exactly. Neither key
factor is silently held outside the routing intervention. This is not module ablation.

CPU preflight reuses one native block7 call on saved residual6 (40sequences), verifies
native full and present replay<=1e-4relative, support0/finite and sum closure<=1e-12.
No learned coefficients or selection of terms by outcome. Use the same now-opened
CITY_SOURCE7_FRESH_V1 panel and retain prior reversing document IDs4,8,11,19.

Installed seven arms: native,present,keymain,valuemain,cross,keymain+valuemain,
keymain+valuemain+cross.280full sequence-equivalents/126blockcalls,2CPUthreads,
180second cap; full native MLP8/suffix recomputed. No new random-null promotion.

Predictions:
a. Native/present replay previous native/group scores and fullsum replay present,
   abs<=1e-4 ANDrel<=1e-5; correctcalls,finite,support0,CPUonly.
b. Keymain alone has negative attenuation at all six endpoints in each of the four
   reversing documents, and effect norm>.5present in each. This predicts that the
   routing role itself carries the reversals, rather than only the key/value cross.
c. Valuemain attenuates>=90%of120capable contrasts with mean>=.02 and all four
   control RMS movements<=.5its target RMS. A miss rejects this clean value-role claim.

Report all effects and failures. Contrast present effect with keymain+valuemain arm
to measure the causal consequence of retaining cross at the head; separately compare
the summed-main arm effect with the sum of main effects to expose suffix interaction.
None of these exact partitions establishes independent composition or source ownership.
A promising role would still need matched nulls and fresh rows, without gate relaxation.
