# Hourly strategic review

ACTIVE_TRACK: WEIGHT_FOLDING

Actual UTC: **2026-09-19T09:27:13Z**. Next deadline: **2026-09-19T10:27:13Z**. Previous Codex review: [08:23 CIRCUIT](HOURLY_STRATEGIC_REVIEW_2026-09-19_0823.md), 1h03m26s old. This is one current review, not a backfill. Evidence cutoff is 09:26:15 (v505). Historical `/workspace` paths were resolved against this live checkout. No GPU model, job, queue, timer, service, experiment/result, registry, dossier, commit, push, external contact, or `TYPED_FACE_EXTRACTION_V1` implementation was changed.

The prior hour was declared `CIRCUIT`. It did not execute the registered regional five-arm factorial, but v464-v505 materially advanced the subject-number circuit: the two answer channels were bounded from both ends by edits, the verb channel gained the second-hop copier 5.3, the late detector was separated from the pronoun path, and direct versus distant agreement paths were distinguished. Alternation is mandatory despite this unfinished work, so this review switches to `WEIGHT_FOLDING`. The circuit handoff is preserved below.

## Seven circuit targets and full goal

1. **Computational specification:** what is read, what operation/composition is performed, what is written, and which downstream computations use it.
2. **Cross-boundary grouping and within-module splitting:** group pieces across modules when a reader treats them as one variable; split native modules when their pieces serve different computations.
3. **Held-out/OOD prediction:** predict activations and behavioral effects on unseen inputs, task variations, and shifted corpora.
4. **Extraction or sufficiency:** run an isolated circuit at a declared boundary with every retained port and background named.
5. **Selective manipulation:** removal, swap, or edit changes the intended behavior against matched controls while preserving unrelated behavior and accounting for redundancy/interactions.
6. **Composition and reuse:** shared subcomputations serve multiple tasks/modules and their joint behavior follows a tested composition law.
7. **Stable identification:** claimed units survive document/corpus splits, gauges, and fitting restarts, or are defined by downstream operational equivalence.

Goal: a smaller transparent tensor program that **predicts held-out/OOD behavior, is extracted at a declared boundary, supports selective manipulation/removal, and composes/reuses**. Simplicity is the fifth separately measured property: independently stored scalars/bytes, product nodes, readers/writers, ports/states, edges, and execution. Lower error, rank, storage, variance, or CE cannot replace any missing behavioral property.

## What changed since 08:23

The strongest circuit result is now a cross-boundary split of one number state into two operationally different channels. **Edits** v475-v488 close attention entering the distant verb (`.943`) and pronoun (`.973`) answer positions. Five pronoun heads jointly close `.803` of the pronoun margin; five verb heads, now correctly including 5.3 rather than 5.7, close `.557` of distant agreement. Cross-use is asymmetric and small (`.041` verb-head effect on pronouns; `.096` pronoun-head effect on verbs), and the sets compose locally. Head 5.3 is a second-hop copier of 4.5's post-noun seed; its joint route is `.149`. This is a genuine cross-module grouping by downstream use, not by native block.

The endings differ. **Folds/edits** v464-v474 show no MLP17 pronoun detector, while distant verbs use head11.3 plus distributed MLP9-16 rebuilding into unit701; its product term is `.80` of the change. Unit2921 is a small conditional contributor, not a sufficient relay. **Edits** v497-v505 show detector701/2059 writes the verb-reader vector (`cos=.908`) but is a singular suppressor on adjacent rows (`cos=.63` with its distant vector); adjacent agreement is a nested embedding/attention/MLP accumulation, while the pronoun slot has a smaller `.214` MLP rebuild led by MLP12/9/13/11 and no late stage. MLP12 unit22 dominates its opened-row fold, but top-50 closure (`.761 < .80`), sign constancy (`.615`), and overlap with the noun MLP8 set fail. These are opened/replay candidates, not stable identities.

Failures and corrections matter. v465 required two undefined-name gate repairs. v466 rejected a simple slot/twin-detector account. v469 showed that edits can reach701 indirectly where direct-write folds do not. v471/v473 rejected a sparse MLP16 verb-site relay. v490 failed the gender-leakage gate (`.216 > .20`), while v493 showed opposing reader tilts cancel. v494 and v495 used the wrong replay reference; only v496 is clean. v499 and the first v500 exceeded the registered price (`15 > 11` forwards); v499 was not rerun, and v500 was re-registered and later duplicated. v501 rejected additivity of early/late MLP groups. v504's receipt carries a copied `verb_slot` field identical to its `text` field, so the board interpretation is usable only at its stated opened pronoun-slot scope and schema cleanup remains due.

Evidence remains typed: algebra is **fold**, intervention is **edit**, downstream allocation after an edit is **response**, and regression/probe is **fit**. v464-v505 use the already opened 122 aligned pairs and do not declare distinct context cells; rows are not independent cells.

| Property | Current evidence | Verdict |
|---|---|---|
| Held-out/OOD prediction | Regional intermediate extraction replays on 20 distinct fresh FineWeb cells and selective Pile panels, but packed FineWeb sign transfer is `88/102`. The new number route has no frozen new-template/corpus law or distinct-cell manifest. | **PARTIAL overall; number extension FAIL/untested** |
| Extraction at a declared boundary | Regional residual6-to-attention8 and norm-closed MLP8 interfaces have declared ports. Complete head9.8/suffix is open. Number has native folds/edits but no standalone token/noun-state-to-two-readout executor or port list. | **PASS at intermediate regional boundaries; incomplete end-to-end** |
| Selective manipulation/removal | Regional scoped fresh edits beat same-site nulls. Number edits separate two head sets at both ends, but use opened rows and lack a fresh three-reader battery plus equal-norm same-site directions at the complete boundary. | **PARTIAL** |
| Composition/reuse | Number head sets and early/late routes are locally additive/reused, but no fresh smallest-live-piece Mobius gate against matched random splits exists. Regional key/value interaction/smaller is `2.3448`; best additive factorial RMS is at least `.5862` of the smaller live single. | **FAIL overall** |
| Measured simplicity | Regional components have local prices and a 128-product numerical lower-bound witness. There is no deduplicated full-path price. Number head/unit counts omit upstream generators, ports, suffix, and executor cost. | **LOCAL COUNTS ONLY** |

## Highest-value WEIGHT_FOLDING action

Complete one exact forward fold with endpoint **head9.8's output delta at the residual10 boundary**, then expose its selected regional readout through the unchanged suffix only as a later causal test:

`(native residual6, token IDs) -> all nine attention7 writes -> complete MLP7 and five head8.2 readers -> head8.2 city-write delta -> norm-closed MLP8 -> head9.8 QK1*QK2*V output delta`.

- **Native factors and shapes:** residual states `T x 1152`; each head's Q/K/V coordinates `T x 128`; each causal QK score `T x T`; each head write after `O` is `T x 1152`; MLP left/right factors are `T x 4608`, their elementwise product is `T x 4608`, and `Down` returns `T x 1152`. At the normal fixture length, `T=32`.
- **Included sources/terms:** all residual6, initial/re-entry, attention7, and MLP7 sources used by the five head8.2 readers; every reader-permitted ordered self/cross term. For MLP8 with input `z+d`, retain `z*z`, `z*d`, `d*z`, and `d*d` separately, plus bias, learned re-entry, and changed RMS. For head9.8 retain all seven nonempty finite-difference products of `(dp1,dp2,dv)`. Inside both QK factors retain attention-output x attention-output, MLP x MLP, attention x MLP, and MLP x attention terms separately wherever the selected reader can use them.
- **Omitted terms/background:** omit only the unchanged baseline head9.8 write from the delta program. Native residual6, unrelated query/source content, all RMS denominators, rotary phases, causal mask, learned mixing/re-entry, unchanged attention fields, MLP biases, and the blocks9-17 suffix remain explicit background/open ports until folded. No QK2, value, cross term, or live source partition is dropped; current composition failures license no smaller subset.
- **Nonlinearities:** exact epsilon-aware RMS, rounded rotary, two bilinear QK scores with no softmax, their product with V, the bilinear MLP product and bias, and learned residual re-entry. Final RMS and `30*tanh(U*RMS(h)/30)` are outside the local endpoint replay and must be live in the later behavioral test.
- **Simplicity measure:** one deduplicated inventory of independent FP32 scalars/bytes, 4,608-wide MLP product nodes, live attention triplet products per causal cell, reader/writer and graph-edge counts, open-state scalars, and measured CPU execution. Shared native factors are charged once. An exact but larger expansion is transparency/extraction progress, not a simplicity pass.
- **Exact replay check:** compare every boundary and final head9.8 delta with frozen native captures on 40 opened fixtures and 20 genuinely distinct fresh FineWeb document/context cells; require float64 algebraic closure at `<=1e-10`, FP32 maximum per-sequence relative L2 at `<=5e-5`, exact off-support zero, and named-source sums equal their parent. Report failures rather than relaxing the gate.
- **Later causal/predictive falsifier:** after freezing the fold, run the structurally varied full-suffix factorial: baseline, routing/key only, value only, additive installation with the explicit product suppressed, and true joint native product. Require signed regional transfer on distinct cells, at least three unrelated readers, full logits/CE, 16 equal-norm same-site directions, and matched random splits. Kill the retained grouping if either single is dead, target sign fails, collateral breaches, or joint-minus-additive is not specific and small relative to the smallest live piece. Exact Mobius closure alone does not pass.

This action advances targets 1, 2, and 4 directly and creates the frozen object for targets 3, 5, and 6. It is higher value than folding another opened number unit: the regional path is the authority-designated depth target, has the deepest port closure, and current algebra proves independent key/value compression cannot represent its live arm function.

**Concrete track connection:** edits on the number route show why folds must be organized by downstream consumer: the same noun state reaches nearly disjoint pronoun and verb head sets, and unit701 has different adjacent/distant response laws. Conversely, the regional seven-term fold tells the next circuit edit exactly where a native product joins routing and value. Consumer-specific edits decide whether those algebraic terms should be grouped, split, or discarded.

**CIRCUIT handoff:** execute the frozen regional full-suffix five-arm factorial on structurally varied distinct cells with the three-reader battery, equal-norm directions, matched splits, and interaction normalized by the smallest live single. Preserve the fresh number crossover as the next number-specific circuit test; do not open another reader/unit census first.

## Confounds, alternatives, and decision

The fold must preserve baseline subtraction and replace-versus-zero semantics; source ordering; frame/position/city/lexical mixing; shared token/document difficulty; FineWeb/Pile leakage; every RMS, rotary, re-entry, attention-product and suffix nonlinearity; dead deltas/tripwires; BF16/FP32 floors; and post-selection. Actual distinct context cells, not rows, determine independence.

Alternatives ranked by information per cost:

1. **Complete regional residual6-to-head9.8 fold** (chosen). Kill or repair the instrument if any intermediate fails exact replay; deny simplicity if its deduplicated price does not beat the scoped native comparator.
2. **Fold the number route by consumer and position:** MLP8 hub to the five pronoun heads versus 4.5->5.3/four late verb heads->distributed MLP9-16->701. Kill if a frozen term grouping fails on distinct new cells or requires the full opaque residual at every edge. Valuable next folding hour, but the current 122 pairs are opened and the route is unconsolidated.
3. **Backward-fold the verb unembedding contrast through MLP17 unit701 and its two inputs.** Kill if the adjacent/distant response law cannot be expressed with a declared context slot or if matched readers show the same factorization. This risks post-selecting around v466/v498.
4. **Predictive-state quotient or signed composition basis.** Defer until a fresh factorial supplies multiple context-response environments; otherwise it is another fitted basis without identification.
5. **More unit/rank/variance censuses.** Demoted: v464-v505 show the missing evidence is fresh identification, extraction, composition specificity, and priced execution, not another opened ranking.

## Registry and dossier audit

`CIRCUIT_REGISTRY.md` still records the older four-head pronoun-number set and does not contain the corrected five-head pronoun and verb channels, 5.3 second hop, MLP8 hub, or position-dependent701/2059 ending. `COMPUTATION_PATH_REGISTRY.md` has the updated regional intermediate path and old `PATH-SUBJECT-001`, but no current number path. `CIRCUIT_GRAPH_REGISTRY_V1.md` remains a generated 49-package inventory: it omits `city_mlp8_norm_closed_v1` and the number route, and its two `four_trait_verified/next gap: none` rows do not price the fifth simplicity property. The MLP8/16/17 dossiers predate v464-v505; MLP12's current dossier also does not link v504/v505. Primary ownership of `TYPED_FACE_EXTRACTION_V1` was respected.

The missing cross-link remains one canonical **`PATH-SUBJECT-NUMBER-002`** connecting the current circuit record, v338-v505 primary receipts and corrections, the five pronoun heads, corrected five verb heads, copier4.5->5.3, units829/953/1030 and701/2059, MLP12 unit22 as an opened candidate, MLP8/12/16/17 dossiers, aliases/sites, opened 122-pair status, and the missing fresh/extraction/composition/simplicity gates. It must explicitly mark v494/v495 invalid and v499 aborted rather than copying their claims. Concurrent files were changing through the cutoff, so this review does not overwrite them.

## PAST_HOUR_TIMING

Window: **2026-09-19T08:23:47Z-09:27:13Z**. Forty-one successful receipts v464-v505 finished from 08:24:52 to 09:26:15 and declare **94.498s** total internal serial runtime; median **2.057s**, range **1.681-4.399s**, 847 declared forwards. Their finish cadence is about **92.1s per successful candidate**. This cadence includes unmeasured design, authoring, interpretation, queue gaps, and corrections and is not active-labor time. Parallel durations were not summed.

- **Design:** unknown. Registered predictions exist, but no science phase boundaries were recorded.
- **Implementation/data building:** unknown. There are 42 sibling candidate scripts for v464-v505; commit gaps are not labor time.
- **Validation:** successful internal runtime is included above. v465 had two pre-run name failures; v494-v496 took 2m31s wall from the first wrong-reference run to the clean run; v499/v500's price correction/duplicate sequence spans 5m46s between successful receipts. Active debugging time is unknown.
- **Model execution:** 94.498s for 41 successful receipts, plus one approximately 3s failed v499 process and an extra approximately 3s v500 execution visible in the authoritative runner log. The receipt plans say no model load/backward/update; the managed cached-forward processes ran for roughly 2-5s each.
- **Interpretation:** unknown. Board claims and two narrative updates follow results, but no phase marks measure their authoring.
- **Documentation/review:** the independent three-hour review is timestamped 09:19 and its CPU control took `.0023s`; active review-writing time is unknown and must not be added to the science lane as wall time.
- **Idle/blocked:** unknown. The largest result-to-result gap is 5m46s around v499/v500, but it contains failures, queue/canary activity, re-registration, and an unexplained delay; it cannot be labelled idle.

The newest actual work-date log is `RESEARCH_PHASES_2026-09-19.jsonl`. Before this review it contained only sparse review boundaries at 04:04, 05:07, 06:18, 07:21, and 08:23; it covered none of v464-v505's design, implementation, validation, execution, interpretation, or publication. The existing helper appended one truthful **09:27:13 review** boundary. No interval was treated as uninterrupted labor and nothing was backfilled.

## PROCESS_IMPROVEMENT

Ranked by likely time saved versus cost:

1. **One declarative number-circuit runner with a shared row builder, intervention schema, scorer, reference-label check, and price computed from actual batches:** likely saves roughly 45-75s of the observed ~92s cadence and would have prevented v494-v496 and v499/v500; medium cost plus equivalence validation.
2. **Receipt schema fields for `started_utc`, `finished_utc`, evidence/freshness, distinct context-cell IDs, and semantic slot labels:** low cost; fixes the timing blind spot and would catch v504's copied field.
3. **Canonical `PATH-SUBJECT-NUMBER-002` plus module links after the live sequence freezes:** likely saves 5-10 minutes per handoff and prevents stale four-head summaries; low documentation cost.

No additional CPU repair was justified in this bounded hourly review. The same shared-runner repair was audited at 09:19, while the live sequence advanced through v505 during this review; changing its active inputs or consolidating the registry now risks collision and needs equivalence tests larger than a safe micro-repair. The only bounded nonconflicting action was the truthful phase boundary. The next number candidate should be gated on the declarative helper or explicit reference/price assertions; today's scientific action remains the regional exact fold.

## Explicit verdicts

- `TRACK_ALTERNATION: PASS` — previous `CIRCUIT`; current `WEIGHT_FOLDING`.
- `TRACK_PROGRESS: PASS WITH SCOPE DEVIATION` — v464-v505 materially identify and manipulate two number channels, but the declared regional five-arm circuit action was displaced and the evidence remains opened/unextracted.
- `CEREMONY_BUDGET: PASS WITH MEASUREMENT CAVEAT` — 41 decision-bearing receipts dominate the visible output and successful compute is cheap; active design, validation, interpretation, and review minutes are unmeasured, so no claim about their relative shares is licensed.
- `NOVELTY_LESSON_GATE: FAIL` — prior folds/edits and falsifications were applied, but 42 sibling scripts, repeated reference/price faults, a copied receipt field, and the still-orphaned number path show that shared-helper and registry lessons remain unimplemented. Repair is gated to the first safe boundary after the live sequence freezes.

**Highest-value action now:** complete the exact, fully coupled residual6-to-head9.8 output-delta fold with all ordered source terms, both ordered MLP cross terms, all seven `QK1*QK2*V` delta products, exact opened/fresh replay, and one deduplicated program price.
