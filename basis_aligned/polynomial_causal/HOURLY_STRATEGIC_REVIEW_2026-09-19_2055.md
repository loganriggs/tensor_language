# Hourly strategic review

ACTIVE_TRACK: CIRCUIT

Actual UTC: **2026-09-19T20:55:32Z**. Next deadline: **2026-09-19T21:55:32Z**. Previous review: [19:53 WEIGHT_FOLDING](HOURLY_STRATEGIC_REVIEW_2026-09-19_1953.md), 1h02m12s old. This is one live review, not an offline backfill. Historical `/workspace` paths were resolved against this live checkout. No GPU model, agent, job, queue, service, timer, commit, push, external contact, experiment/result, existing registry/dossier, or `TYPED_FACE_EXTRACTION_V1` implementation was changed. The sole bounded CPU/process consequence was the truthful 20:55 phase mark.

The previous review declared `WEIGHT_FOLDING`; this review therefore alternates to `CIRCUIT`. The declared complete residual6-to-head9.8 replay and deduplicated price did not land. Preserve that exact fold for the next folding hour; unfinished work does not override alternation.

## Seven circuit targets and full goal

1. **Computational specification:** identify what is read, the operation/composition, what is written, and every downstream user.
2. **Cross-boundary grouping and within-module splitting:** group pieces across native modules when a reader treats them as one variable, and split native modules when their pieces serve different computations.
3. **Held-out and OOD prediction:** predict activation and behavioral effects on unseen inputs, task variants, and shifted corpora.
4. **Extraction or sufficiency:** execute an isolated circuit at a declared boundary with every retained port and background named.
5. **Selective manipulation:** removal, swap, or edit changes the target against matched controls while preserving unrelated behaviors and accounting for redundancy/interactions.
6. **Composition and reuse:** shared subcomputations serve multiple tasks/modules and their joint behavior follows a tested composition law.
7. **Stable identification:** claimed units survive document/corpus splits, plausible gauges, and fitting restarts, or are defined by downstream operational equivalence.

Goal: a smaller transparent tensor program that **predicts held-out/OOD behavior, is extracted at a declared boundary, supports selective manipulation/removal, and composes/reuses**. Simplicity is the fifth, separately priced property: independently stored scalars/bytes, products, readers/writers, ports/states, edges, peak state, compute, and measured runtime. Lower error, storage, rank, variance, or CE cannot substitute for any missing behavioral property.

| Property | Evidence now | Verdict |
|---|---|---|
| Held-out/OOD prediction | Regional intermediate extraction has 20 fresh FineWeb context cells and scoped Pile transfer, but the packed FineWeb direction gate remains `88/102`; the seven-term census is opened replay. | **PARTIAL** |
| Extraction at a declared boundary | Residual6-to-attention8 and norm-closed MLP8 packages replay with declared ports; no complete residual6-to-head9.8 executor/replay exists. | **PASS only at intermediate boundaries** |
| Selective manipulation/removal | Scoped fresh edits beat same-site nulls; the complete path lacks the frozen equal-norm, matched-split, three-reader full-suffix battery. | **PARTIAL** |
| Composition/reuse | Regional key/value interaction is `2.3448x` the smaller piece; omitting four higher-order head9.8 terms costs `.2853` local relative L2. A closed Mobius identity is not specificity or smallness. | **FAIL overall** |
| Measured simplicity | Local packages and exact rank floors are priced; no deduplicated residual6-to-head9.8 or suffix bill exists. | **LOCAL ONLY** |

Claims remain separated as **fold/edit/response/fit** and **fresh/opened/replay**. Transfer units are actual distinct `(template, city pair, endpoint)` context cells, not row counts.

## Progress, failures, confounds, and alternatives

Since 19:53 there is **no scientific receipt**: no fold replay, model execution, CPU analysis result, response census, circuit screen/null, preregistration, registry/dossier update, or commit. Three maintenance canaries completed; they are health checks, not evidence. Therefore the prior folding action made no progress and all five property scores remain unchanged. The latest scientific result is still the 18:33 **fold/opened-replay** census: all four order-at-least-two `QK1*QK2*V` terms together are `1.8837x` the smallest target single and their omission costs `.2853` relative local-head L2 over 20 distinct context cells. It forbids a three-single shortcut but proves neither suffix effect nor causal specificity. The exact rank certificates also forbid an exact linear width below 128 at seven immediate block10 readers; they do not establish reachability, use, or causal adoption.

Verdict-changing confounds remain baseline subtraction and frame mixing; live RMS/background dependence; nonlinear suffix/loss composition; shared token difficulty; opened-panel and post-selection leakage; dead knobs and tiny smallest-piece denominators; precision/noise floors; shared residual inputs masquerading as independent ports; positional colon/quote structure; and mistaking exact Mobius closure for a small or random-split-specific interaction. The circuit test must preserve all seven head products, both ordered attention/MLP cross terms upstream, actual distinct-cell accounting, full logits/CE, matched random splits, and unrelated readers.

Ranked routes are: (1) **chosen**, run the already specified complete-city forward-response census before naming a suffix; kill it as a localization instrument if the decomposed recursive response fails closure or downstream reader signs/ranks are unstable across structurally varied cells; (2) only if (1) names stable readers, run the five-arm baseline/routing-only/value-only/product-suppressed/true-joint factorial, killing reusable composition if interaction relative to the smallest live piece or collateral is no better than preregistered matched splits/equal-norm directions; (3) template-boundary controls, promoted if response location moves with colon/quote structure; (4) a new behavior or fitted suffix, rejected now because it abandons the deepest regional path and violates response-before-suffix. Depth on the regional path remains higher information than another partial route.

## Highest-value circuit action

Run one frozen **complete-city seven-term forward-response census** from the installed regional head9.8 write through blocks10-17, final RMS, unembedding, and softcap on preregistered structurally varied, distinct context cells. The intervention endpoint is the complete head9.8 output-write delta, retaining `dK1`, `dK2`, `dV`, `dK1*dK2`, `dK1*dV`, `dK2*dV`, and `dK1*dK2*dV`. Decompose the recursive suffix into each attention/MLP write plus direct residual propagation; require full-vector response closure, then score target logits, full-distribution CE damage, at least three unrelated readers, per-family signs, and actual cell counts. This advances computational specification and stable cross-boundary grouping; it can support held-out prediction and selective manipulation only after the frozen response is tested against equal-norm same-site controls. It is killed by failed closure, unstable downstream users, target effects no better than matched nulls, or collateral comparable to target. Do not infer a suffix from the older QK1-only response census (`attn9 .5422`, `attn17 .2495`, anti-aligned `mlp17 -.1378`): that receipt used a narrower opened edit.

**Connection between tracks:** the folding census says all seven native head9.8 terms must enter the intervention; the circuit response census says which suffix readers can actually use their joint write. Only their intersection can justify a reader-specific grouping, omission, or module split.

## Registry and dossier audit

`COMPUTATION_PATH_REGISTRY.md` still stops `PATH-REGIONAL-CITY-001` at the head8.2 city-removal write, though its live handoff asks for the coupled full-suffix circuit test. `CIRCUIT_REGISTRY.md` and `MODULE_DOSSIERS.md` contain the regional head8.2/head9.8 and MLP7/MLP8 evidence, including the older QK1 response census, but do not expose one canonical residual6-to-head9.8 endpoint. `CIRCUIT_GRAPH_REGISTRY_V1.md` still inventories 49 packages but omits `city_mlp8_norm_closed_v1`; its two `four_trait_verified` entries also do not price the fifth simplicity property. The MLP index links the MLP7 addendum and generic MLP8 dossier, not a canonical complete regional path.

Missing cross-link: extend `PATH-REGIONAL-CITY-001` to join the regional circuit record, MLP7/MLP8 and head8.2/head9.8 dossiers, `city_mlp8_norm_closed_v1`, the seven-term census, both rank certificates, and the pending full-response/factorial receipts, carrying fold/edit/response/fit, fresh/opened/replay, distinct-cell manifests, ports, and deduplicated price. The path registry and board are concurrent dirty state, so this review does not mutate them beyond the required append-only board note.

## PAST_HOUR_TIMING

Window: **2026-09-19T19:53:21Z-20:55:32Z**. End-to-end serial candidate latency (`prior-art -> hypothesis/counterfactual -> native capability -> causal screen -> scored dossier/null`) is **unknown/not started**; no candidate entered the pipeline.

- **Design/scientific thinking:** unknown; no science phase boundaries.
- **Implementation/data building/indexing/refactoring:** none evidenced; no relevant file or run start.
- **Validation:** unknown/none evidenced.
- **Model execution:** no research model execution. Maintenance canaries ran 19:53:58-19:54:20, 20:24:24-20:24:42, and 20:54:46-20:55:03: **57 serial seconds total** by runner start/end marks. Internal canary runtimes overlap within each job and are not summed.
- **Interpretation:** unknown/none evidenced.
- **Documentation/review:** the prior review file and board append landed at 19:54:44; this review began with the authoritative 20:55:32 phase mark. Unmarked writing duration is unknown.
- **Idle/blocked:** unknown. Commit gaps and absent receipts are not converted into idle time; parallel durations are not summed.

The actual-date `RESEARCH_PHASES_2026-09-19.jsonl` contains sparse hourly review boundaries through 20:55 and no science, implementation, validation, or publication boundary in this window. These marks show boundary coverage only, not uninterrupted labor. The last commit remains `9e0c03df3` at 12:21:10 UTC; both queues are empty. Runner logs show the canaries completed, but the configured legacy per-user `supervisorctl` binary is absent, so no service-state inference is made from that failed control command.

## PROCESS_IMPROVEMENT

Ranked by time likely saved versus cost: (1) canonical regional path cross-link with uniform evidence/evaluation labels, distinct cells, ports, and deduplicated price—likely 5-10 minutes per handoff at low cost; (2) one declarative full-response manifest reusing the native suffix and shared row/null scorers—larger execution savings at medium validation cost; (3) consistent science/implementation/validation phase marks in existing runners—small implementation cost but useful only if called.

No shared-helper, runner, data-building, indexing, or registry refactor is justified inside this bounded review: the registry and board are concurrently dirty, and a response helper before the frozen census specification is bound would risk more ceremony. The sole safe repair was the truthful 20:55 phase mark. The failed `--help` probe exposed that `research_phase_clock_v1.py` has no CLI help handling, but adding it would not save more time than the scientific action and is therefore not changed.

## Explicit verdicts and handoffs

- `TRACK_ALTERNATION: PASS` — previous `WEIGHT_FOLDING`; current `CIRCUIT`.
- `TRACK_PROGRESS: FAIL` — the folding hour produced no complete path replay, fold receipt, falsifier, or other scientific result.
- `CEREMONY_BUDGET: FAIL` — known scientific execution was zero while review/health ceremony recurred; unknown labor cannot establish that safeguards were smaller than science.
- `NOVELTY_LESSON_GATE: PASS` — the review searched circuit/path/graph registries and relevant module dossiers, preserves full `QK1*QK2*V`, exact port closure, distinct cells, matched nulls, and response-before-suffix, and opens no duplicate candidate. The canonical cross-link remains explicit organization debt.

**Highest-value action:** execute the frozen complete-city seven-term forward-response census through the native suffix on structurally varied distinct cells; name downstream readers only after recursive response closure, then condition the matched-null factorial on that result.

**WEIGHT_FOLDING handoff:** exact-replay and deduplicately price the complete `residual6 -> attention7/MLP7 -> head8.2 -> norm-closed MLP8 -> head9.8` delta, retaining every reader-permitted self/ordered-cross term and all seven `QK1*QK2*V` products.
