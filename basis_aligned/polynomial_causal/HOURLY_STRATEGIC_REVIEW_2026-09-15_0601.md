# Hourly strategic review — 2026-09-15 06:01 UTC

ACTIVE_TRACK: CIRCUIT

Actual UTC: **2026-09-15T06:01:15.346012622Z**. Next deadline:
**2026-09-15T07:01:15.346012622Z**. This is one elapsed checkpoint after the
05:01:38Z review, not an offline backfill. The prior `WEIGHT_FOLDING` receipt is
the regional head9.8 QK1 late-group route: its grouped attribution transferred
and its removal was material, but the proposed MLP16×MLP17 suffix predicted the
opposite sign; the response census instead localized the effect to direct
attention9 propagation plus attention17. Preserve for the next folding hour:
split the `.24952` attention17 response by head and then by QK factor only for a
stable head, reusing the exact `parent - R×R` rank-two executor.

The seven circuit targets remain: **(1) computational specification** of reads,
operations, writes, and downstream consumers; **(2) cross-boundary grouping and
within-module splitting** by operational role; **(3) held-out and OOD
prediction**; **(4) extraction or sufficiency** with an explicit interface and
background; **(5) selective removal, swapping, and editing**, including
redundancy and collateral; **(6) predictable composition and reuse**; and
**(7) stable identification** across corpora, documents, gauges, and fitting
restarts or by operational equivalence. The full goal is a smaller executable
tensor program that is jointly predictive on fresh and OOD text, composable when
replacements are installed together, manipulable under registered removals,
swaps, and edits, and simpler in literal storage, compute, edges, live state,
interfaces, adapters, and independently specified operations. Exact algebra,
rank, reconstruction, compression, variance, or CE alone is not completion.

## What changed since 05:01

The 17-source/289-ordered-term carry census closed exactly but its top ten terms
left `.66738` relative replay error, rejecting a fine-grained sparse story. The
task-selected group `D={attention5, MLP5, MLP6, MLP7}` was more useful:
`DD`, `DR`, and `RD` left only `.11186` selected-row error, with cross-boundary
change norm `.44088`. On 48 fresh-relative rows, its folded replay remained
`.13851`; recursive removal was `.52843/.59816` of the full head9.8 effect and
`6.99/5.76×` the matched `R×R` removal. This is causal materiality, not selective
adoption: one unrelated-reader ratio missed at `.53452`, and QK2 controls were
large at `.695/.796`.

The decisive failure is downstream. The frozen MLP16×MLP17 folded prediction had
cosine `-.92536` and `0/24` signs versus the recursive edit. The exact 18-write
census assigned `.54223` change norm to direct attention9 propagation, `.24952`
to induced attention17, and `.15921` to MLP17 with anti-aligned fraction
`-.13785`; MLP9 was only `.04892`. The complete pre-RMS numerator nevertheless
predicted the final effect at cosine `.99903` and `24/24` signs, so final RMS and
softcap are not the explanation. The 05:52 CPU theorem/control then proved the
retained score subtotal is exactly `B(D+R,D+R)-B(R,R)`: 256 rather than 384
scalar multiplications per query-key cell, closure `5.15e-16`, but no repair of
the causal suffix.

Four substantive folding receipts were published from 05:03:52 through
05:34:27. Valid managed model executions were about three seconds each; serial
scientific publication intervals were approximately 12, 12, and 7 minutes after
the first receipt, with the carry experiment already frozen before the 05:01
boundary. The activity log has publication markers but no defensible complete
phase split, so no finer science/validation/review times are invented. Fresh
routing required two pre-science implementation repairs (rotary broadcast and
report axis), and the response census required one FP32 audit-tolerance
correction. A shared grouped-interaction helper and shape tests landed at 05:36.
The later mathematical review and CPU control ran 05:52–05:56 and did not alter
the scientific outcomes. Git shows these research commits plus live runner,
canary, activity-log, board, and untracked historical artifacts; they were
preserved. `theseus-bench` is clean and unchanged at `e233031`.

## New circuit decision

The highest-value circuit action is a **fresh 2×2 routing/value composition
factorial** for the regional circuit, using (a) the frozen rank-two late-touching
head9.8 QK1 edit and (b) the independently identified head8.2→head9.8-O
current-value midpoint edit. Measure native, routing-only, value-only, and joint
arms on rows unused by either selection. Freeze the exact attention-product
inclusion/exclusion prediction before observing joint outcomes; score signed
UK-minus-US numerator and behavioral effects, family-wise transfer, and matched
unrelated readers. The compositional hypothesis predicts the joint effect from
the two fixed branches plus their explicit routing×value interaction. A
redundancy/correlated-attribution alternative predicts subadditivity or control
spillover. Kill the proposed reusable split if the frozen joint prediction fails
direction/magnitude on either family, if either single edit is dead, or if the
joint arm increases collateral beyond the registered single-arm controls.

This action can advance targets 1, 2, 5, and 6: it tests whether a cross-layer
value transport and a within-head routing subtotal are distinct computations
that compose through the same native attention product. It is higher information
than another selected-row source subdivision because both branches already have
single-arm causal evidence and the central uncertainty is their interaction. It
also makes the track connection concrete: circuit evidence selected the
head8.2 value writer, folding selected the `D/R` QK1 reader terms, and the
factorial asks whether those discoveries jointly predict one native computation.

The main alternative is the subject-number route: derive a donor-free
predictive-state coordinate oriented by the frozen L11H3 response operator and
require fresh causal substitution. It is genuinely different and remains
second-ranked because response-level transfer is strong (`.28804` error) but
the composed program failed (`.51585`), and activation/displacement rank two
barely moved it (`.51072`). Kill it if an operator-oriented coordinate cannot
beat `.40` complete-program relative L2 on sealed constructions or loses the
known response signs. A third alternative is a causal-response quotient of the
nine attention17 heads; it is reserved for the next folding hour because the
current evidence is an unsplit module response, not yet a circuit unit.

## Confounds, organization, and verdicts

Baseline subtraction remains arm-sensitive; report both absolute and paired
effects. Regional framing and clause state can mix, while the row validator only
removes the known article error. QK1×QK2×value multiplication and the nonlinear
suffix make single-arm effects non-additive unless the explicit interaction is
included. Shared token difficulty, FineWeb/pretraining leakage, and corpus-near
duplicates remain possible. Every edit knob needs a live single-arm tripwire;
otherwise a seemingly compositional null is uninterpretable. FP32/BF16 rotary
and residual closure floors near `1e-7–1e-6` must be separated from FP64 algebra.
The `D` group and regional readers are post-selected, so the factorial must use
fresh rows and fixed thresholds. Control-family sensitivity is already real
(`.53452` unrelated-reader miss and large QK2 controls), not a footnote.

The circuit registry, computation-path registry, L11H3/head17.2/attention9/
head9.8/attention5/MLP5–7/MLP17 dossiers, and current primary receipts were
checked. Completed artifacts are cross-linked and no orphaned receipt was found.
The missing cross-link is a **joint** circuit↔path composition record connecting
the head8.2 value edge to the QK1 `D/R` path; it does not yet exist because the
factorial has not been preregistered or run. Add it to both registries only with
a valid result or honest null, rather than treating this review as evidence.

**TRACK_ALTERNATION: PASS.** `CIRCUIT` is opposite the prior hourly
`WEIGHT_FOLDING` field.

**TRACK_PROGRESS: PASS.** The prior folding hour produced exact source/group
expansions, a fresh causal routing null that falsified the proposed suffix, an
exact downstream-response localization, and a priced rank-two executor.

**CEREMONY_BUDGET: PASS, WITH REPAIR PRESSURE.** Four scientific receipts landed
within about 33 minutes and compute was not the bottleneck. Three pre-result
shape/precision repairs are material overhead, but the shared helper/test repair
now exists; reuse it rather than cloning another runner. Unmarked time is not
assigned to invented phases.

**NOVELTY_LESSON_GATE: PASS.** Registries and relevant dossiers were searched;
the plan applies the established head8.2 value circuit, the QK1 causal edit, the
failed MLP16×MLP17 suffix, large QK2 controls, post-selection warning, and prior
activation-rank nulls. It does not reopen a generic rank/reconstruction sweep.

**Highest-value `CIRCUIT` action:** preregister and run the fresh four-arm
head9.8-QK1-routing × head8.2-value composition factorial with exact joint-effect
prediction and unrelated-reader controls. **`WEIGHT_FOLDING` handoff:** split the
induced attention17 response by nine heads and then QK factor for a transferring
head, using the shared shape helpers and exact rank-two QK1 executor.

This bounded review did not load a model, execute a new experiment, mutate
results/registries/dossiers/queues/services/timers, commit or push, create a goal
or agent, or contact anyone.

## Post-review execution receipt — 06:13 UTC

The registered highest-value action completed on a new 48-row panel with zero
prior token-context overlap. All five predictions pass. Both single branches are
live in both families; the exact routing×value head term is `.5913/.6194` of the
smaller single, and recursive target nonadditivity is `.5693/.6103`. Joint signed
UK-minus-US effects are `−.7976/−.9231` logits, joint/additive cosines are
`.99991/.99966`, and every unrelated-reader ratio is below `.288`. Exact carry,
manual-attention, midpoint-scaling, and joint-algebra audits are all at or below
`1.11e-7`. This closes the review's missing circuit↔path composition record and
hands the next weight-folding hour a causally confirmed QK1×current-value product.

The immediate five-arm attribution completed at 06:21 UTC on the same now-opened
panel. Joint minus the additive-without-cross head arm predicts the factorial
interaction at `.99930/.99891` cosine and `.16533/.09669` relative error. All
five gates pass. This assigns the interaction primarily to the explicit
head9.8 product, with smaller downstream magnitude curvature, and does not count
as another fresh-family transfer.

The exact source-token census completed at 06:28 UTC. Four offsets selected by
head-vector norm replay at `.2190/.1095` error and cross-transfer at
`.1437/.2894`; instruction-only replay reaches `.1906/.1498`, while
description-only misses at `.8171/.8544`. All three registered gates pass. The
leading common positions are quote, colon, and offset −7, but token identities
at the latter position differ, so retain a positional/boundary candidate rather
than inventing a shared word-level role. V1's padded-mask reporting failure and
the token-length-only V2 correction are preserved.

Fresh recursive instruction-source testing completed at 06:33 UTC. Geometry and
target behavior transfer: head errors `.1891/.2351`, recursive cross-effect
errors `.2467/.3226`, and cosines above `.9978`. The complete gate set is a valid
null because family 1's work/jobs control reaches `1.1507×` target, over `.75`;
family 0 remains below `.194`. Keep instruction-source compression as mechanism
evidence but do not adopt it as a selective circuit package.

The 06:38 UTC collateral attribution passes all four gates. Instruction- and
full-cross-specific controls are below `.0966/.0922`; the failed family's
additive work/jobs RMS is `1.0036×` its full-joint RMS. Thus the original
combined-package failure remains, but it is assigned to the additive single
branches. The instruction-source cross advances as a fresh selective component.

The 06:42 UTC additive split passes all four gates. In failed family 1,
routing/additive/value work/jobs RMS is `.5260/.5304/.0327`; routing alone is
within `.0735` of additive. Both singles are target-live (`.2908/.3647` RMS),
and all recursive sums compose within `.1114`. Preserve value and cross; split
the three selected QK1 routing blocks when the circuit track next resumes.
