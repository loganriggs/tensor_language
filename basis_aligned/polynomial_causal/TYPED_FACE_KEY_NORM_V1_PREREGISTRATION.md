# Head8.2 key-normalizer dependence V1

Prior-art search: earlier routing closure retains native head RMS factors; no
key-normalizer ablation for the fixed routing/inherited face was found in its
path preregistrations or head8/head9 dossier. This is not a new support search.

Keep recipient queries/current values and donor inherited city value fixed.
For the donor-city key only, compare native carried denominators with recipient
K1 denominator frozen, K2 frozen, and both frozen. Preserve the complete
QK1*QK2*V product, rotation, causal mask, head output and destination mask.
The normalized block8-state input itself remains open and unchanged; this tests
head-key RMS only, not the upstream residual RMS.

First CPU gate on eight saved native inputs (opened): native carried replay
<=1e-5 relative write error. Diagnostic prediction: both-frozen write differs
from carried face by <=.10 relative L2 in every example. Null: a changing key
normalizer is essential at this boundary; do not simplify it out. Keep the
full four-arm denominator factorial even if a singleton looks small. No
behavioral conclusion can follow from this state-only diagnostic.

Prospective recursive test, to bind before enqueue: batch all six endpoints
and four unrelated controls at once,8inputs*5arms=40forwards,300seconds.
Original/native, carried face, K1frozen, K2frozen, bothfrozen. Predict bothfrozen
vs carried target-effect error<=.10 in each cue and endpoint cell; preserve
original prediction if it fails. A joint interaction is measured explicitly.
Use the exact existing opened contexts for instrument/response diagnosis, then
require a fresh panel before any omission is adopted. No fit or threshold rescue.
