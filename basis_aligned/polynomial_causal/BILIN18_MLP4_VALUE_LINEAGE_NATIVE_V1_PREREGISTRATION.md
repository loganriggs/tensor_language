# Native MLP4-to-value direct-lineage folding screen

10 September2026; before execution. Original bilinear handoff controls.
The query-carrier test failed. Prior restricted MLP4-to-localV9 mediation
supports a value-content route, but does not show whether direct residual
transport suffices or later nonlinear modules are essential.

Use unchanged72 opened has/had and is/was evalpairs, no filtering or fits.
Replace MLP4 outputs at ALL valid token positions with the paired outputs,
both directions. This scope is broader than earlier selected-source-bank
experiments; do not require their numerical results to replay.

Capture original residual u9 by read-only hooks following native FP32
residual/embedding recurrence in its actual order. Let delta_m be paired
MLP4 output minus receiving output and gamma=product(lambda[j,0],j=5..9).
Pure direct lineage changes localV9 to Wv RMS(u9+gamma delta_m). It holds
all later source writes fixed; actual MLP4 replacement recomputes them.

Fold Wv Down4 into the exact midpoint bilinear difference. Candidate field
is nativeV9 + gamma*(Wv Down4)*delta_feature/s1
                 + Wv*u9*(1/s1-1/s0).
s0/s1 are receiving/direct-edited RMS scales. The receiver's exact native
FP32 value is the additive base; native rounding is verified by an independent
direct normalized-input projection. Outputbias cancels only in the MLP
difference and remains included in u9. All opaque maps/background charged.

Arms, both endpoints: native; full_mlp4 (all valid source outputs paired,
capture its new V9); value_full (insert that hybrid localV9 into native
receiving context at H1/H4 only); direct_fold; value_identity; raw_direct
(independent RMS(u9+gamma delta_m) projection). All value arms keep query,
key, shared-first-value input, other heads and residual background native.
48 full forwards/864 sequence evaluations. Full suffix recomputed in each.

A instrument: authorities/rows/checkpoint bound; paired lengths/positions
match; six capture controls and five fold controls; read-only residual
recurrence bitwise normalized-input replay; identity full-logit<=1e-3 AND
relative Frobenius<=1e-5; directfold vsrawdirect local field AND full-logit
same bars; FP64 folded normalized content versus independently contracted
FP64 MLP difference <=1e-8 AND<=1e-9; allunselectedvalue entries unchanged,
hooks restored, counts exact, finite, parent native contrasts replay.

B full value carrier: every panel/direction signed projection of value_full
effect onto native paired change >=.10 in BOTH centered full-logit and
answer-margin frames. Otherwise retain the restricted route's capability
limit; do not infer the old selected-source experiment was invalid.

C direct folding: direct_fold effect relative error versus value_full <=.10
in BOTH centered full-logit and margin frames on EVERY panel/direction.
Zero reference norm<=1e-8 uses absoluteerror<=1e-8. Report signed projection
and relative effect norms; no cancellation-prone mean alone. This is a basic
screen, not final adoption fidelity. D: value_full and direct_fold effects
have centered-logit norm>1e-8 in all cells. A miss invalidates; B/C/D misses
remain explicit. No scope, rank, source, dose, norm or threshold rescue.

Zero learned/new model parameters;545902902 nativeweights remain. Selected
WvDown4 has256*4608 entries, separately charged; actual modelweight saving0.
Nativeprefix/intervening modules still execute as background. Passing C
would nominate a directly foldable partial path, not independent extraction
or reuse of a newly named semantic operation.600-second watchdog; managed
enqueue only. Save immutable JSON, no checkpoint writes.
