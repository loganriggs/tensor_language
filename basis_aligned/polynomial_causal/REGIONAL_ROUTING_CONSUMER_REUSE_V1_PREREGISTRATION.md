# Frozen routing approximation across two value consumers

Use unchanged geographic48 rows and forward13 support. Native, mixed-numerator
with true denominator, and complete-replay routing each feed first-value,
contextual cue-value, and their combined donor swaps. Baseline plus9 arms.
Current-value swap is cue-local, unlike the earlier all-position current stream.
A: native first replay, complete write replay, producer/attention replay and
linear first+current=both write composition <=1e-5.
B: approximate write and signed-prefix regional effect relative errors <=.1 for
EACH branch in EACH geographic family. C: nonlinear combined interaction error
absoluteL2<=1e-5 OR relative<=.1 in EACH family; report both and native signal norm.
Null: first-consumer success does not transport to another consumer or their
combination. Failure is not absence of routing structure; inspect cancellation
and task effect magnitude before attributing it to a specific mechanism.
No new fit, support selection, input rows or native generator closure. This is
reuse across two consumers serving ONE behavior, not multitask circuit discovery.
Price:10bodybatches48rows6–14tokens10suffixarms180sec managed cap;<6MB artifact.
All native context/value/query/key-generator weights remain charged. Reuses the
frozen correction runner's algebra and checks, with explicit new arms/scoring.
