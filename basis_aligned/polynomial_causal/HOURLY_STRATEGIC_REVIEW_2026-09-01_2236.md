# Hourly strategic review — 2026-09-01 22:36 UTC

## Full goal

Compile bilin18 into a smaller transparent tensor program that remains predictive on fresh/OOD text, composes when
several replacements are installed, supports selective edits/removals without unrelated collateral, and is simpler
under explicit storage/compute/graph/interface prices. CE alone and a small coordinate description are insufficient.

## What changed since the 21:36 Codex review

1. The sub-500M dtype repair failed its position-level gate and the quantization direction was withdrawn: changing
   numerical precision is storage engineering, not an interpretability mechanism.
2. Rung439b tested the previously prose-only Archetypal Q/K proposal. Real-token convex geometry beat a destroyed-
   token control by about21%, but damaged score/write/reader/CE consequences and missed absolute restart/half-split
   stability. Raw factor-row hull anchoring is closed at this object.
3. Rungs440–441 turned the learned-simplicity prose into an audited data instrument.130 clean receipts reduce to45
   conservative one-row-per-program arms. OOD and extraction slices exist, but removal has2 arms in1 family and
   composition0; no named consequence reaches the frozen three-family fitting license.
4. Rung442 proves the raw nine-head score spectrum is diffuse: rank6 retains only.812/.790 marginal energy. The
   successful6/6 continuous block therefore lives in product/downstream response geometry, not raw score rank.
5. Dossier preflight recovered an older matching108-candidate compiler bank at MLP0/MLP1. Rung443's MLP0-frozen
   structural score predicts MLP1 recovery at Spearman.654 and73.8% pairwise accuracy, beyond bytes/rank/shuffle
   controls. Direct MLP0 recovery transfers better at.820. This is positive within known grammars, not an unseen-
   family or interpretability consequence result.
6. Bits-level MDL pricing now gives corpus-specific break-even lengths (~30–35B tokens for two adopted tiers). This
   validates a compression consequence only; it does not imply semantics, removal locality, or composition.

## Confound audit

- **Post-selection/adaptivity:** every rung443 site1 value is historical and partially summarized in old prose. It is
  a screen, never a prospective confirmation.
- **Wrong statistical unit:**108 candidates repeat ranks and regularization inside five grammar families. Effective
  family count is5, not108. Site transfer is not unseen-grammar transfer.
- **Difficulty and shared design:** MLP0 and MLP1 banks share candidate specifications and training machinery. Their
  correlation can reflect a common capacity curve. Price/rank and shuffled-data controls help but do not remove this.
- **Family classifier leakage:** affine/state-complete/causal flags can identify the five known families. The leave-
  family-out site0 penalty choice is useful, but the final site1 test contains the same families.
- **Frame/metric mixing:** raw Q/K factors, raw score matrices, two-score products, routed writes, and downstream
  causal responses are different metrics. Rungs439/442 forbid using a positive in one as a claim in another.
- **Baseline subtraction and nonlinear loss:** future composition labels must record signed component effects and
  the nonlinear joint residual; adding mean CE damages is not a sufficient prediction.
- **Precision/noise floors:** numerical path identities remain separate from scientific bars after the 439 gauge
  repair and the two 443 output aborts.
- **Goodhart risk:** learned simplicity must beat bytes, rank, sparse edges, and randomized/shuffled controls on a
  sealed consequence. Optimizing its score before that test is forbidden.

## Genuinely different routes

1. **Response-metric Archetypal/causal dictionary.** Move atoms from raw folded Q/K rows into the gauge-invariant
   downstream response metric identified by 424/425. Opposing prediction: if the task geometry is real, response-
   anchored atoms become split/restart stable while preserving product/write/readers; if not, 439's instability and
   consequence collapse repeat. This is a changed object, not a looser raw hull.
2. **Prospective learned-simplicity family.** Freeze rung443's score and uncertainty support before constructing one
   new grammar family. Require it to rank candidates against bytes/rank and then open a sealed site/consequence.
   Kill if the new grammar lies outside support or the score cannot order candidates before outcomes.
3. **Prospective removal/composition bank.** Reuse already physical MLP0, MLP-PCA, and attention programs, but define
   typed program-local interventions and signed joint effects. Kill if the intervention semantics cannot be made
   comparable across families without arbitrary normalization.
4. **Predictive-state/Hankel realization in response space.** Treat the downstream consumers as observations and
   seek a minimal causal state from finite response strings, rather than an SVD of raw scores. Kill if the response
   Hankel spectrum has no gap or state transitions fail held-context prediction.
5. **Task-weighted tensor lower bound.** Form the exact quadratic/product contraction with the downstream response
   Gram as norm and derive rank/width lower bounds. Kill if the norm depends on exposed test labels or cannot bound
   executable consequence error.

## Ranked next moves

1. Define and preregister the response-metric Archetypal dictionary. It directly follows the paired 439/442 nulls,
   is the highest-information structural test, and supplies a genuinely new grammar family for a later prospective
   simplicity check. Death evidence: no real-vs-permuted response-hull advantage, restart/half stability below bars,
   or causal consequence collapse.
2. In parallel at CPU/design level, specify common signed removal and composition labels for three physical families,
   with feature and label hashes separated before any GPU evaluation. Death evidence: no common intervention unit.
3. Only after one of those produces a sealed new-family bank, apply the frozen rung443 score. Do not refit it on the
   new family. Death evidence: it loses to both complete bytes and rank or violates its support envelope.

The response-metric route survives the alternatives because two independent certificate-quality negatives now say
the missing structure is specifically the task norm, while rung424/425 already demonstrate a compact stable object
inside that norm. The next experiment must test that statement causally, not return to raw Euclidean atoms.
