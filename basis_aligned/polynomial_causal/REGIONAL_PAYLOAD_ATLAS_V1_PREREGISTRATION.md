# All-producer atlas of the regional payload

Reuse32frozen V2cue prompts. Exact scalar residual recurrence expresses r17 as
one aggregated embedding re-entry term plus attention and MLP updates0through16.
Project each onto the frozen2current payload readers. Keep nativeRMS17divisor
and first-attention-stream contribution as separate ports. The recurrence has
35numerator terms and37ports total. Each isolated numerator swap uses recipient
RMS and fixed recipientparent, QKgate andprivatequerywriter. Jointall-port swap
must reproduce the earlierchild-onlydonorwrite. This is conditional mediation,
not ablation of each whole module throughout the network.

A: native projected residual unroll, reconstructed child and jointdonorwrite
errors<=1e-5relative. B:A and eachfamily whole-child transfer>=10%native cue gap,
>=5/8positive pairs. C:A/B and embedding-only transfer>=50%whole-child transfer,
unrelatedcontrast meanabs<=.5regional meanabs eachfamily. Large weight-derived
embedding coefficient145.12motivates C but is not causal evidence. Preserve Cfail
if other updates dominate. All35producer ranks and signs are descriptive; selecting
a producer from this atlas requires a separate held-out test before identification.

7bodyforwards32sequences4-12tokens,39suffix arms (baseline,37singleports,all),
180secondmanagedGPUlimit. Project hooks to2numbers pertoken immediately; do not
save full layer-state stacks or vocablogits. Save only writevertices/scalarmargins.
No fitting. ExistingMLP16-only Cfailure remains; avoid17serial one-layer assays.
