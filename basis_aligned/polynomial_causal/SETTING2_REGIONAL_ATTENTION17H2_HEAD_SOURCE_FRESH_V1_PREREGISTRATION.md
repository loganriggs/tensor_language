# Frozen head17.2 head-source program on cue-role crossover rows

Freeze the opened-panel eight-edge program exactly as
`attn9h8 + attn10h2 + attn10h5 + attn11h1 + attn11h6 + attn11h8 + mlp9 + attn15`.
Test it without reselection on all 48 rows of
`REGIONAL_CITY_ROLE_CROSSOVER_V1_ROWS.json`.  These rows change templates,
endpoints, cities, and introduce a second geographic role; they were not used
to choose the head support.  This is fresh for head-support selection, not a
claim that the panel has never appeared elsewhere in the regional lineage.

Retain the discovery runner's exact head split, factor corner, twelve suffix
readouts, family gates, thresholds, and BF16 accounting.  Evaluate only:

- the frozen eight-edge support;
- the exact 21-head expansion of the prior five-module program;
- the exact target-only `attn9h8 + attn15 + mlp16` baseline; and
- sixteen seed-`202609160548` preselected width-eight support nulls.

No fresh-panel term choice, coefficient fit, threshold change, gradient, or
parameter update is allowed.  Predictions are exact instrumentation, fresh
response replay, fresh bidirectional causal fidelity, fresh preservation,
strict minimax improvement over the coarse 21-term program, and `.10`
specificity against the median null.  All must pass for promotion.

Maximum price: 48 rows; 12 native/edited full-model executions over 96
sequences; 19 fixed support evaluations and 1,824 candidate suffix sequences,
plus 96 complete-corner suffix sequences; twelve token logits per suffix
sequence; zero backwards, fits, parameter updates, or quantization.
