# Hourly strategic review

ACTIVE_TRACK: WEIGHT_FOLDING

Actual UTC: **2026-09-19T17:45:30Z**. Next deadline: **2026-09-19T18:45:30Z**. Previous review: [16:43 CIRCUIT](HOURLY_STRATEGIC_REVIEW_2026-09-19_1643.md), 1h02m03s old. This is one live review, not an offline backfill. Historical `/workspace` paths were resolved against this checkout. No GPU model, agent, job, queue, service, timer, commit, push, external contact, experiment/result, existing registry/dossier, or `TYPED_FACE_EXTRACTION_V1` implementation was changed. The sole bounded CPU/process repair was the truthful 17:45 phase mark.

The previous review declared `CIRCUIT`; this review therefore alternates to `WEIGHT_FOLDING`. The circuit handoff—one frozen complete-city forward-response census through the suffix, followed only then by the matched-null five-arm factorial—did not run and is preserved unchanged for the next circuit hour.

## Seven circuit targets and full goal

1. **Computational specification:** identify what is read, the operation/composition, what is written, and every downstream user.
2. **Cross-boundary grouping and within-module splitting:** group pieces across native modules when one reader treats them as one variable, and split a native module when its pieces serve different computations.
3. **Held-out and OOD prediction:** predict activations and behavioral effects on unseen inputs, task variants, and shifted corpora.
4. **Extraction or sufficiency:** execute an isolated circuit at a declared boundary with every retained port and background named.
5. **Selective manipulation:** removal, swap, or edit changes the target against matched controls while preserving unrelated behavior and accounting for redundancy/interactions.
6. **Composition and reuse:** shared subcomputations serve multiple tasks/modules and their joint behavior follows a tested composition law.
7. **Stable identification:** units survive document/corpus splits, plausible gauges, and fitting restarts, or are defined by downstream operational equivalence.

Goal: a smaller transparent tensor program that **predicts held-out/OOD behavior, is extracted at a declared boundary, supports selective manipulation/removal, and composes/reuses**. Simplicity is the fifth, separately priced property: independently stored scalars/bytes, products, readers/writers, ports/states, edges, peak state, compute, and measured runtime. Lower error, rank, storage, variance, or CE cannot substitute for a missing behavioral property.

| Property | Current evidence | Verdict |
|---|---|---|
| Held-out/OOD prediction | Intermediate regional extraction covers 20 distinct FineWeb document/context cells and Pile tests, but packed FineWeb direction remains `88/102`; no frozen end-to-end formula passes structurally varied cells. | **PARTIAL** |
| Extraction at a declared boundary | Residual6-to-attention8 and norm-closed MLP8 packages replay with declared ports; complete head9.8 plus suffix is open. | **PASS only at intermediate boundaries** |
| Selective manipulation/removal | Fresh regional edits beat same-site nulls, but the complete path lacks one frozen equal-norm, three-reader, matched-split, full-logit battery. | **PARTIAL** |
| Composition/reuse | Regional key/value `interaction/smaller=2.3448`; additive lower bound `.5862`; matched random-split specificity fails. A closed Mobius face is accounting, not evidence of a small or specific interaction. | **FAIL overall** |
| Measured simplicity | Local package prices and exact full-vector rank floors exist; no deduplicated residual6-to-suffix price or matched-effect advantage exists. | **LOCAL ONLY** |

Claims remain separated as **fold/edit/response/fit** and **fresh/opened/replay**. Actual distinct `(template, city pair, endpoint)` context cells—not row counts—are the unit for transfer claims.

## Progress, failures, confounds, and alternatives

Since 16:43 there is no circuit census, causal screen/null, fold, scientific result, commit, registry/dossier update, or candidate start/end receipt. The only managed executions were successful health canaries at 16:51:26–16:51:44 and 17:21:48–17:22:12. Both queues are empty and the long-lived Supervisor-managed `bqrunner`/`bqrunner2` processes remain present. Thus the declared circuit response census made no progress; its response-before-suffix lesson remains open rather than being converted into a guessed suffix.

The latest substantive regional evidence remains the 15:29 **fold/weight-only** certificate: exact rank 128 for head9.8 through each of seven immediate block10 reader maps. It rules out a generic exact linear width `<128` at those tested boundaries, not a task/readout quotient, attainable-manifold approximation, regional reachability, causal fidelity, OOD prediction, composition, or favorable price.

Confounds still capable of reversing a verdict are baseline subtraction and frame mixing; nonlinear loss/suffix composition; shared token difficulty; opened-panel/post-selection leakage; dead single arms; RMS/precision floors; treating shared residual inputs as independent ports; and treating a closed Mobius identity as small relative to the smallest live piece or specific against matched random splits. Full `QK1*QK2*V` interactions and forward responses must be retained.

Ranked alternatives are: (1) the chosen exact complete regional replay and deduplicated price, killed by failure of native term closure or by no structural saving after deduplication; (2) a declared task/readout quotient after the response census, killed by frozen distinct-cell prediction or selective-control failure; (3) a later-boundary fold toward tokens, killed by port growth or worse literal price. Generic exact block10 rank reduction is demoted by the rank-128 certificate. The regional route remains highest information because it is the deepest extracted path with distinct-cell evidence and explicit composition failures; opening another behavior would violate the depth-first authority.

## Chosen WEIGHT_FOLDING action

**Endpoint:** the complete head9.8 output-vector delta at the final spelling-decision position, reached from token IDs plus native residual6 through attention7/MLP7, head8.2, and norm-closed MLP8. The native blocks10–17/final readout are a later causal/predictive test, not silently part of the exact folded endpoint.

**Exact native factors and shapes:** residual states `[T,1152]`; each attention head's `Q1,K1,Q2,K2,V` maps `[128,1152]` and `O` slice `[1152,128]`; nine attention7 heads; MLP7 and MLP8 `L,R [4608,1152]`, `Down [1152,4608]`, bias `[1152]`; head8.2 and head9.8 factor maps with the same head shapes; causal score fields `[T,T]`; per-head values `[T,128]`; writes `[T,1152]`. Preserve native learned current/inherited value mixing, residual re-entry, causal mask, rounded rotary maps, biases, and `eps=1.1920928955078125e-7` RMS.

**Included source terms:** expand each relevant residual into named residual6, attention-output, MLP-output, installed-city-delta, and unchanged-background sources. Retain every reader-permitted self term and both ordered attention/MLP cross terms in MLP7 and MLP8, including `attention x attention`, `MLP x MLP`, `attention x MLP`, and `MLP x attention`. For head9.8 retain all seven nonempty finite-difference products:

`dQK1*QK2*V`, `QK1*dQK2*V`, `QK1*QK2*dV`, `dQK1*dQK2*V`, `dQK1*QK2*dV`, `QK1*dQK2*dV`, and `dQK1*dQK2*dV`.

**Omitted terms:** only the unchanged baseline contribution is omitted from the delta. No attention/MLP source, ordered cross term, normalizer, or head product is omitted before a downstream reader census licenses it. Quantization is excluded.

**Nonlinearities/background:** MLP numerators are bilinear divided by live RMS squared; attention is the product of two normalized, rotated bilinear scores and a value; normalization makes the complete maps rational with square roots. Background ports are token IDs, residual6, unchanged query/source content, live denominators, mask/rotary phases, mixing/re-entry coefficients, biases, and all unchanged native fields. Gauges include hidden-unit permutation/rescaling, `L/R` exchange, compatible Q/K changes, reciprocal QK-factor scaling, and value/output basis change; the executable must replay invariantly rather than naming a gauge coordinate as semantic.

**Simplicity measure:** one deduplicated program bill for independent FP32 scalars/bytes, tensor interfaces, product nodes, readers/writers, graph edges, peak live state/cache, multiply-adds, and measured CPU runtime, compared with the exact covered native subgraph. Shared projections are charged once; overlapping package manifests cannot be summed. A smaller error or storage count alone is not a behavioral trait.

**Exact replay check:** compare the folded delta with native captured complete head9.8 writes on every sequence and source cell; require maximum per-sequence relative `L2 <= 5e-5`, maximum absolute error `<= 1e-4`, exact zero off the intervened destination, and numerical closure of the seven products to the directly recomputed delta. Report worst cases and distinct cells, not only aggregates. Failure is an honest folded-path null.

**Later causal/predictive falsifier:** on outcome-blind structurally varied cells, first run the frozen forward-response census through blocks10–17, final RMS, unembedding, and softcap. Then run baseline/routing-single/value-single/product-suppressed-additive/true-joint arms with 16 equal-norm same-site directions, matched random splits, at least three unrelated readers, full logits/CE, and interaction normalized by the smallest live single. Kill the retained subset on unstable signs, failure to replay the recursive response, prediction error beyond its frozen gate, collateral beyond controls, or nonspecific interaction.

This action can change targets 1–4 and 7 through an explicit cross-boundary computation, exact extraction, and stable replay; the later frozen tests address 3, 5, and 6. **Concrete track connection:** the circuit response census selects which suffix readers actually use the exact seven-product delta; only those reader-permitted terms may then be folded or omitted, while the folded identity supplies the product-suppressed arm that separates head interaction from suffix curvature.

## Registry and dossier audit

The circuit registry, computation-path registry, `CIRCUIT_GRAPH_REGISTRY_V1.md`, MLP index, and regional/head8.2/head9.8/MLP7/MLP8 dossier slices were checked. `PATH-REGIONAL-CITY-001` still stops at head8.2 despite later norm-closed MLP8/head9.8 evidence. It is not cross-linked by ID from either `CIRCUIT_REGISTRY.md` or `MODULE_DOSSIERS.md`; those files cross-link the older `PATH-SET2-001`. The graph registry inventories 49 packages but omits `city_mlp8_norm_closed_v1`, and its `four_trait_verified` label does not price the fifth simplicity property.

Highest-priority missing cross-link: join `PATH-REGIONAL-CITY-001`, the regional circuit record, relevant MLP7/MLP8 and head8.2/head9.8 dossier entries, `city_mlp8_norm_closed_v1`, the complete seven-product endpoint, the block10 rank certificate, and the pending response/factorial, with suffix prediction, selectivity, composition, and total price explicitly open. The computation-path registry and other shared files are concurrent dirty state, so this review did not edit them.

## PAST_HOUR_TIMING

Window: **2026-09-19T16:43:27Z–17:45:30Z**. End-to-end serial candidate latency (`prior-art -> counterfactual specification -> native capability -> causal screen -> dossier/null`) is **unknown/not observed** because no candidate traversed the pipeline.

- **Design/scientific thinking:** unknown; no science phase boundary or receipt.
- **Implementation:** none observed; active duration unknown. No data build, indexing, shared-helper, runner, or refactor receipt appeared.
- **Validation:** no scientific validation.
- **Model execution:** no research model execution. Health canaries occupied 18s and 24s, **42 serial seconds total** by runner start/end lines; these are health costs, not evidence. The latest nested canary JSON reports 11.08s + 12.84s inside the latter 24s and must not be added again.
- **Interpretation:** unknown; no new scientific result.
- **Documentation/review:** the prior review file reached disk at 16:44:39 after its 16:43:27 boundary, but its process began before this window; attributable duration is unknown. This review is still in progress, so its duration is unknown.
- **Idle/blocked:** unknown. Commit gaps and absent receipts are not treated as active or idle labor, and parallel durations are not summed as wall time.

The authoritative `RESEARCH_PHASES_2026-09-19.jsonl` contains sparse hourly-review marks through 17:45 and no science, implementation, validation, or publication boundaries in this window. Coverage is inadequate for labor accounting; boundary marks are events, not uninterrupted work. No phase time was backfilled. The latest repository commit remains `9e0c03df3` at 12:21:10; there was no commit in the window.

## PROCESS_IMPROVEMENT

Ranked by likely saving/cost: (1) add one canonical regional cross-link and uniform evidence/evaluation, distinct-cell, timing, and literal-price fields to the existing receipt path—low cost, likely 5–10 minutes saved per handoff; (2) consolidate repeated sibling intervention runners behind the existing declarative executor—high cumulative saving, medium collision/equivalence-test cost; (3) reduce canary frequency only after an explicit service-policy change—small saving, outside this review's authority.

No shared helper, runner, registry, schema, or result mutation is justified in this bounded review: the files needed for the best repair are already dirty with concurrent work, no new candidate exposed a safe isolated helper boundary, and schema changes require equivalence tests. The one nonconflicting CPU repair was the truthful 17:45 phase mark; it improves boundary coverage but not scientific throughput.

## Explicit verdicts and handoffs

- `TRACK_ALTERNATION: PASS` — previous `CIRCUIT`; current `WEIGHT_FOLDING`.
- `TRACK_PROGRESS: FAIL` — the prior circuit hour produced neither the declared response census nor a circuit screen/null/falsifier.
- `CEREMONY_BUDGET: FAIL` — no scientific execution existed for review/preflight to remain smaller than; 42 seconds of canaries are maintenance, not science.
- `NOVELTY_LESSON_GATE: FAIL` — no new science duplicated an old result, but the registered response-before-suffix lesson and canonical regional cross-link remain unapplied.

**Highest-value action:** exact-replay and deduplicately price `residual6 -> attention7/MLP7 -> head8.2 -> norm-closed MLP8 -> complete head9.8`, retaining every ordered source term and all seven `QK1*QK2*V` delta products.

**CIRCUIT handoff:** run the frozen complete-city forward-response census on structurally varied distinct cells; only then run the matched-null five-arm suffix factorial.
