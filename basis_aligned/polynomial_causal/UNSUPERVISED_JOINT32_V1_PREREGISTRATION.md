# Unsupervised joint32 shared-product discovery

Full50304-token quadratic tensor, actual1152-dimensional input, no token family or activation labels in fitting. Native4608-product representation is the exact baseline. Discover a32-product component with explicit native remainder, not a complete small model. Authorities: unembedding_folding_in_math.md and unembedding_factors_how.md; full goal still requires prediction/extraction/removal/composition evidence.

Optimize shared products H_j=sym(a_j b_j^T), all-vocabulary coefficients A=UW. At every step solve W=D G_cross G_candidate^-1, then use envelope gradients for a/b. Metric is U^TU, exact implicit coefficient loss; no fullVxdxd tensor. Normalize a/b row norms; real signs retained. Two starts: native32 products with largest standalone all-vocabulary coefficient energy, and seeded Gaussian factors (seed9114101). Strong matched baseline refits output writers for the same selected native input products. Native selection uses weights only.

Fixed240Adam steps per start,lr.03; save minimum training-loss iterate in that fixed trajectory. No subsequent learning-rate, rank, seed or threshold sweep. Candidate Gram must remain finite; report condition, no silent ridge repair. CPU planted3-product shared-DAG recovery and envelope-gradient fixtures must pass before native execution.

- pred_a instrument: CPU planted coefficient relative error<=.01 and gradient absolute error<=1e-7; exact implicit/dense algebra relative errors<=1e-10 on fixture; native independent coefficient contractions<=1e-8, finite objective/factors/writers and Gram condition<=1e10; exact fixed steps and0model forwards. Inspect native losses directly; increasing/failed runs not omitted.
- pred_b learned-factor gain: BOTH optimized starts improve captured full-tensor squared coefficient energy by at least.05 of native total against the matched native32-product baseline. This only tests discovery beyond selected existing products, not circuit identification.
- pred_c stable components: at least16mutual nearest component matches between starts have positive full-term tensor cosine>=.95, with negligible terms (<1e-6largest norm) excluded. Match actual term tensors (UW)_j tensor H_j, preserving signs; report function-subspace overlap and complete-program disagreement separately.

Add signed factor-to-token loading diagnostics, effective token participation and90%/99%squared-loading support counts, after factors are frozen. These are descriptive approximate usage, not exact sparsity. No sparse-output penalty is present in this first fit. Shared linear-reader overlap is distinct from token support overlap. Interpret token identities only after fitting. Full residual writers and all-vocabulary outputs retained; no arbitrary thresholding changes the executable program.

Price0native forwards,480optimizer steps total. Bank32products, at most64independent linear readers,1152x32residual writer; native remainder/allU/normalization retained. Stream U in512-row chunks; each new tensor<=256MiB. Native GPU execution only managedlane1,900secondhard timeout; planned coefficient work is bounded and no corpus training. Save readers/writers, initialization indices, loss histories, hashes and literal bytes. Native removal/held-out semantic nomination follows only with a scored useful candidate.
