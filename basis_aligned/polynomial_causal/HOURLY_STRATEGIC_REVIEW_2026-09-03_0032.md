# Hourly strategic review — 2026-09-03 00:32 UTC

## Circuit interpretation targets

A useful decomposition must eventually provide all of the following:

1. **Computational specification:** what information is read, what operation or composition is performed, what is
   written, and which later computations use it.
2. **Grouping across modules and splitting within modules:** merge pieces of different heads or MLPs when a later
   computation treats them as the same variable, and split a native head or MLP when its pieces do different jobs.
3. **Held-out and out-of-distribution prediction:** forecast activations and causal effects on unseen text, task
   variants, and shifted data.
4. **Extraction or sufficiency:** an executable isolated circuit, or a precise interface plus background, reproduces
   the target computation or signed causal effect.
5. **Selective manipulation:** removing, swapping, or editing the circuit changes the intended behavior while leaving
   unrelated behavior intact, including redundancy and interaction effects.
6. **Composition and reuse:** shared subcomputations work across tasks or modules and combine predictably with
   task-specific branches.
7. **Stable identification:** the unit survives document splits, plausible gauges, and fitting restarts, or is defined
   by downstream operational equivalence.

The program goal remains a smaller transparent tensor program that is jointly predictive, composable, manipulable,
and simpler under literal storage, compute, edges, states, and program description. Rank reduction, reconstruction,
variance retention, or lower cross-entropy alone does not accomplish these circuit goals.

## What changed since 23:31

- Rung511's corrected exact Left/Right/joint MLP10 branch test was published as a valid null:0/42 global cross-action
  relations.
- Rung512 moved to real consumers. Eighteen of42 branch-write relations existed at MLP10, but0/42 survived at
  attention11, MLP11, or the fixed question-mark form.
- Rung513 split those consumers exactly into31 attention factor interactions and3 MLP interactions. All612 tests
  were material, but0 passed the directional or residual rule. The instrument was exact and confirmation stayed
  closed.
- A parallel CPU audit of rung513's signed mismatch statistics found a descriptive positive: attention mismatch
  fingerprints have mean cross-relation cosine`.993` versus permutation q95`.095`, with the same top-three factors
  Q, Q2, and value in18/18 cases. MLP11 fingerprints have cosine1.000. This means the attribution shape is stable;
  it does not yet prove that the corresponding response vectors form a causal subspace.
- Rung514 is preregistered to retain full joint response statistics and compare fixed exact factor allocations with
  split-stable sparse signed sums. It includes planted recovery and familywide permutation controls before any model
  result counts.

## Is the current path still the highest-information route?

Yes, with one pre-outcome refinement. Rung513 shows that singleton exact interactions are large but cancellation-
coupled. The joint Gram is the minimal additional statistic needed to test sums without inventing a rotationally free
latent basis. The independent stable-mismatch result makes fixed factor allocations more promising, but it remains an
attribution result on the same documents. Add exactly one fixed `Shapley(Q)+Shapley(Q2)+Shapley(value)` object before
R514 execution, then require the same independent splits, permutation margin, untouched documents, and physical
interchange as every other candidate. Do not launch a separate post-hoc physical edit from the descriptive probe.

## Confound audit

- **Baseline subtraction:** the managed smoke now checks native calibration recovery directly; it was exactly1.0.
- **Frame mixing:** every R513 term compared branch-present and branch-removed factors at the same consumer and score
  action. R514 retains that frame.
- **Nonlinear loss composition:** discovery compares consumer response tensors, not summed CE attributions. Actual
  CE and circuit effects enter only through registered physical interventions.
- **Shared token difficulty:** all actions use the same positive-copy positions; the16 controls permute coordinates
  across actions while preserving each action's internal term algebra.
- **Leakage and post-selection:** R513 confirmation remained unopened. R514 has two independent selection pairs,
  frozen support/signs, a familywise null, and untouched752:1000 confirmation. The added top-three object is explicitly
  outcome-conditioned on published R513 statistics and frozen before R514 model execution.
- **Dead edits:** all1,736 R513 source edits were nonzero; the smoke exercised all consumer-patch types. R514 must
  retain per-patch capture verification.
- **Precision floor:** deployed endpoints replay exactly, attention numerical remainder is`6.83e-10` of response RMS,
  and MLP closure is`1.47e-19` relative squared error.
- **Identifiability:** sparse support is not accepted merely because it fits. The exact support and signs must recur in
  independent searches, beat familywide controls, recover eight planted programs, predict new data, and interchange
  physically. More than32 passers is a failure, not permission to select a favorite.
- **Stable-share overinterpretation:** cosine between attribution fingerprints is not cosine between model writes and
  is not selective causal evidence. The positive probe narrows a hypothesis; it does not satisfy a circuit target.

## Genuinely different routes

1. **Fixed factor accounting:** exact Shapley allocations, including the prospectively added Q+Q2+value sum. This has
   no learned coordinates and directly tests the stable mismatch shape.
2. **Sparse signed exact programs:** exhaustive two/three-term sums in the fixed Möbius basis. This tests literal
   compositional simplicity while controlling multiplicity and identifiability.
3. **Task-conditioned nonlinear reader:** if both linear object classes fail, learn a small polynomial map from exact
   term responses to the62 circuit outcomes, with circuits and documents nested as held-out sets. This changes the
   equivalence definition rather than widening support or reducing rank.
4. **Predictive-state causal quotient:** group term interventions only when all registered downstream interventions
   give the same conditional outcome distribution, allowing different raw outputs to count as the same variable if
   no downstream computation distinguishes them.
5. **Direct tensor-network canonicalization:** at the next three-hour checkpoint, map the exact five-linear consumer
   response and downstream polynomial readers to identifiable tensor or weighted-automaton objects. A theorem-backed
   canonical form would dominate empirical coordinate searches if its assumptions fit the tied, normalized network.
6. **Lower-bound route:** use the full joint Gram to prove that no two/three-term program can meet the response gates.
   A certified negative region is useful even when no candidate exists and prevents another cosmetic sweep.

## Ranked next moves and kill conditions

1. **Finish R514 joint-Gram implementation and planted tests.** It changes targets2,3,5,6,7 if a unit survives. Kill
   the instrument before model execution if any planted support is not uniquely recovered or control arithmetic fails.
2. **Run the fixed-plus-sparse bank under the frozen controls.** Kill both current linear object classes if there is no
   split-stable candidate above the familywise margin; do not increase support above3.
3. **If B fails, build the task-conditioned nonlinear-reader experiment.** Kill it if held-out circuits cannot be
   predicted above permuted circuit labels or if its proposed unit cannot be finitely removed.
4. **If B/C hold but physical interchange fails, locate the first downstream reader that rejects the swap.** Kill the
   grouping claim at that boundary rather than weakening the physical rule.
5. **At01:20 or the first safe boundary after it, perform the separate theorem-to-object mathematical review.** Kill a
   literature route unless it gives an explicit mapping, assumptions, complexity, identifiability statement, and an
   executable consequence for this exact contraction.

The current route survives because it tests a circuit-level grouping inside a consumer, uses a fixed exact basis,
and has prospective causal and held-out gates. It is not a rank-reduction experiment: rank never appears in candidate
construction or success criteria.
