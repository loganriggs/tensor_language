# Causal-response factorization v1 — Amendment 16: validation role boundary

Status: prospective after the 27-program v2 freeze received independent GO and before
the FIT bundle is reopened or any of its 114 validation responses is exposed.

The next transaction may expose exactly the 114 preregistered internal-validation
documents and no training response, EVAL value, model, corpus, or candidate-selection
choice. It must first replay the exact v2 freeze artifact and its independent audit,
then one-use load and semantically validate the receipt-bound FIT bundle. The private
full payload is reduced immediately to cloned validation-only tensors; no public alias
to the payload or 229-document training response survives.

Every one of the 27 frozen programs is scored. No rank pair, seed, or calibration
panel may be dropped after seeing validation. The training document codes stored in
each candidate may be used only for their mean in the unconditional arm. Calibrated
arms infer a new validation-document code from complete physical phase/source blocks.

Amendment 1 controls calibration. At budgets 2, 4, 8, and 16 physical arms, report both:

1. the outcome-blind SHA-256 arm order; and
2. candidate-specific training-only block D-optimal selection.

For each program/panel/budget report support, non-anchor MSE and correlation, every
owner-pair score, worst owner-pair NRMSE, smallest valid-design singular value,
calibration cells, solve cost, and prediction cost. Unconditional prediction reports
the same error slices. Training RMS is the frozen normalization currency. A document
requires at least twice the candidate code dimension valid selected cells; support
below 90% is ineligible and never silently pooled.

No winner is selected inside the scorer. The complete validation table is published
receipt-last. A later deterministic analyzer may form the preregistered Pareto frontier
over literal persistent price, per-document state, calibrated validation MSE, and
worst-owner NRMSE, while retaining pooled and block-relative views separately.

This validation is response tomography, not EVAL, OOD transport, semantic extraction,
selective removal, a terminal circuit, or whole-model ledger credit.
