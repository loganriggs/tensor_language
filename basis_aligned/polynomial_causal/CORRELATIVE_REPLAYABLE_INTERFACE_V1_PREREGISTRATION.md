# Correlative interface reconstruction, new fit v1

The v505 report does not store selected unit IDs or fitted directions. Reproduce
its frozen recipe once in a new namespace; this is a new fit identity. Agreement
of metrics cannot establish identity with an unavailable historic direction.
Original handoff/pilot authority. No claim that activation interchange constitutes
independent extraction, semantic discovery, selective removal, or fresh OOD.

Execute the unchanged run_unit_correlative_p_interchange_v505.py main function
through an isolated wrapper. Change only its output path and intercept its existing
prepare/greedy/fit calls to preserve artifacts. Keep all original data, filtering,
unit pool, ordering, optimizer, seed, steps, rank, loss and predicates. Persist
ordered units, q keyed by (layer,kind), FIT means, actual prepared row sets and
history. Local wrapper instrumentation must not alter returned values.

Frozen checks:

- A: all five original v505 predicates hold.
- B: exactly 26 chosen units, and absolute differences versus v505 in canonical
  A1 joint_extraction, A2_extraction, P_same_answer_effect and C_same_answer_effect
  are each <=.01. A miss remains a repeatability failure, with artifacts saved.
- C: reloaded q and means are tensor-identical, all q have one column, and each
  q^Tq differs from identity by maxabs<=1e-5. Charge SUM of block ranks explicitly.
- D: held-out canonical A1 patch using in-memory versus disk-loaded q has
  maximum donor-oriented answer-margin difference<=1e-5. No refitting on replay.
- E: producer/native/head-patch/MLP-patch bridge using existing
  verify_against_producer on four fixed A1 FIT rows passes its standing1e-4 bar.

Conservatively inherit v505 caps200000 forwards/6400000 example evaluations plus
20 forwards/320 examples for instrumentation. Count actual body calls/sequences.
No original checkpoint updates; model parameters stay frozen during120 fitting
steps. Fitted q has up to30*128 coefficients at construction, per-block QR.
Temporary activation captures and upstream model remain charged. Saved artifact
is an executable conditional interface, not a standalone token-to-logit circuit.

The managed queue serializes GPU work. Hash-bound source/helper/data recipe and
historical result; dryrun imports no model and builds deterministic expected rows.
Preserve both new recipe result and artifact if a gate fails. Never overwrite
v505, rerun under the same result name, change bars or select another fit.

Next mathematical use, conditional on a valid saved artifact: each head slice q_h
reads sum_s p_ts*(q_h^T v_s); fold q_h into actual local/first value maps, sum the
head scalars within its block, and fold the concatenated writer through c_proj.
The actual routers, current inputs, original value mixing and live suffix remain.
CPU weight identities must precede any native claim for that folded program.
