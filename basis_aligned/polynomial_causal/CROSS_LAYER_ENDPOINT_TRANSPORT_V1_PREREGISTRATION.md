# Complete forward endpoint path under middle renaming

Opened-data diagnostic, following failed local gate transport and degree tests.
Use all forward B2_later arrangements from middle-match populations14909/14910:
32 worlds, three forward orders each, base/both matched contexts, four query hops.
That is384 paired comparisons/768 executions, no sample or final-head selection.
The shared query-hop fork recomputes answers. No fitting, GPU, or new training.

Compute the existing direct endpoint field phi(e)=O2H1 V2H1 Emb(e)/8,
written at the two selected binding destinations with native P2H1*g2. Read this
write through all four final heads with each context's native P3 and g3:

    path = .5 sum_(destination,head) P3(q,destination,head)
                   folded_head V3_head(g3(destination)*write(destination)).

The endpoint itself is unchanged by matched middle renaming. Reader-dependent
sign compensation could make this full path stable despite the failed local
gate transfer. Retain complete29-logit vectors and all heads. This conditional
linear path is not the physical removal effect: changing the write also changes
final keys and RMS. Compute physical removal separately with the exact executor.

A: endpoint write matches independent native message isolation, and reading the
whole source state matches native final attention contribution, abs<=1e-9 and
relative<=1e-10 using the shared scorer. Live nonzero path required.
B: centered full-path donor/base difference divided by base-path RMS<=.01 in
every population/hop group (floor1e-6). Report writer change and whole-vector
cosines as diagnostics; neither aggregate norms nor the best head can pass B.
C: path predicts actual endpoint-write removal within1% relative centered RMS
in every population/hop group. This is a separate physical-sufficiency screen;
passing B without C is only stable conditional attribution.

If B fails, do not repair by fitting signs, amplitudes, heads, positions or hops.
If C fails, do not call frozen attribution a causal extraction. These opened
groups cannot establish held-out identification. All387968 export coefficients
and native gates/readers remain charged; no structural reduction from a pass.

CPU threads2, alarm180s, batch8 FP64, no tensor256MiB or larger. Existing modules:
forward_endpoint_program_reference, exact_source_edit_reference,
query_hop_fork_reference, field_intervention_metrics. New shared primitive only
evaluates a frozen final-query payload path. Preserve rows, hashes and null.
