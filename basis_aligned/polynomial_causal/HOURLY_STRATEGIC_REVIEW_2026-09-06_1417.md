# Hourly circuit strategy review — 2026-09-06 14:17 UTC

## Circuit interpretation targets and full goal

The live decomposition must specify what information is read, what operation is performed, what is written, and which downstream computations use it; split native modules and group cross-boundary pieces by operational equivalence; predict held-out and OOD effects; provide an executable sufficiency interface; support selective removals, swaps, and edits; compose and reuse across tasks; and remain stable across populations, gauges, and fitting restarts.

The program goal remains a smaller transparent tensor program that is jointly predictive on fresh/OOD text, composable under installed replacements, manipulable under registered counterfactuals, and simpler under literal storage, compute, edge, state, and program prices. The new temporal circuit is unusually complete at causal-response grain, but one circuit is not program completion.

## What changed since 13:17

The residual offset sweep moved the final fresh-writer consumer from the registered block15 guess to block11. Two attempted block11 component cubes were preserved invalid: v1 captured the wrong hybrid entry, while v2 repaired tensor closure but failed its own frozen recurrence equation because a boundary12 subject-only endpoint included local block11 updates. The route then changed object to exact native head responses rather than repairing a narrative post outcome.

An external prediction from older number/has-had authorities passed: block11 H3 alone carries 101.9%/100.7% of the full nine-head response to the fresh writer, while the other eight heads are slightly antidirectional. Exact source decomposition then found that the two unchanged subject positions alone equal the entire final-query H3 response; every non-subject group is exactly zero.

Claude's v4 constrained-DAS result was red-teamed at the user's request. Its same-layer full-rank control closed at `7.6e-6`, but every cross-layer `q=I` intervention missed the exact unit set by `0.094–1.895` logits because later cached deltas were added to already-changed live states. The joint cross-layer DAS and DIM interpretations were retracted; whole-module/head and exact greedy-set results survive. The optimizer had in fact beaten DIM on its own training loss, so optimizer failure was not the explanation.

The corrected method restricts DAS to one causally established component, requires identity closure, searches only the empirical intervention-delta span, includes DIM as a retained baseline, optimizes normalized exact-effect plus complement inertness, and reports restart stability. On block11H3 it passed: fit objective improved 24.55x over DIM; heldout/A2 axis fractions were `1.0055/0.8402`, complements `0.0059/0.1565`, P/C `0.0344/0.00033`, and restart minimum cosine `0.8358`. Two independent reruns then produced the identical projector, exported a hashed 128-vector, and showed donor-side necessity of `0.435/0.352` with 100% directional agreement and negligible P/C collateral.

Finally, an exact simultaneous mediation closes the fresh writer response. Clamping block9 H1/H4 subject terms alone leaves `55.31%/51.15%`; clamping block11 H3 alone leaves `48.53%/49.93%`; clamping both leaves only `3.46%/0.85%`. Joint non-subject clamps retain `99.96%/97.69%`. Thus `cue -> block8H1 -> subject positions -> {block9 H1/H4, block11 H3} -> final query` explains 96.5–99.2% of the writer-induced behavior at exact native response grain.

## Throughput and systems audit

Fourteen managed results landed after 13:17, including broad greedy/DAS work, the offset/cube/head/source sequence, two explicit invalids, the DAS red team, corrected H3 DAS, joint mediation, and executable necessity. Codex's eight newest bespoke runners total 1,562 lines; five reusable evaluators now total 1,031 lines. Most native causal jobs took 2–3 seconds. The broad v4 fit took 167 seconds, corrected H3 DAS 24 seconds, and deterministic necessity 56 seconds. GPU wait was negligible; scientific interpretation and repeated runner shells still dominate elapsed time.

`CIRCUIT_FOCUS: PASS` — the hour localized a final consumer, split it to one head and exact source terms, invalidated a wrong subspace algebra, produced a selective necessary H3 axis, and nearly closed a two-reader causal path.

`CEREMONY_BUDGET: FAIL` — reusable kernels improved, but 1,562 new runner lines for short jobs remains excessive. The next bounded block must add one generic multi-layer head-response installer and use a thin declarative runner; no new bespoke capture framework is allowed.

`NOVELTY_LESSON_GATE: PASS` — recent authorities and failure records were searched, both invalid cubes remained invalid, final-residual/readout DAS was excluded, full-rank closure was required before interpreting complement optimization, and the method changed rather than increasing rank after a miss.

## Confounds, alternatives, and decision

The strongest remaining confound is removal/insertion asymmetry across the complete two-reader circuit. Joint clamping shows the reader union is necessary for the writer response, but does not prove that installing the two captured reader responses into an otherwise native base run is sufficient. The native suffix is nonlinear, and block11's response is conditioned on the live block9 path. A direct response-program installation is therefore necessary before calling the interface executable.

Claude's in-progress block-live v7 is a genuinely different repair for multi-site direction fitting. Its mandatory per-block full-rank closure is useful. However, an `S+C=1` logit-effect sum is not a theorem in a nonlinear suffix; failure of that sum diagnoses downstream interaction, not automatically an invalid direction. Cross-layer direction claims remain secondary until their exact counterfactual and held-out semantics survive this distinction. They do not outrank the already closure-valid single-component result.

Ranked next moves:

1. Add a reusable ordered multi-layer head-response installer, then install captured block9 H1/H4 and block11 H3 responses separately and jointly without the writer. Kill condition: joint installed response recovers <75% of writer effect or strong non-subject/control collateral appears. This directly tests extraction/sufficiency and composition.
2. If installation passes, package the typed writer/two-reader response interface plus the hashed H3 axis and test it on another genuinely fresh cue/frame. Kill condition: either construction falls below 70% of its discovery response or selectivity fails.
3. Translate the response interface into checkpoint contractions and literal edge/state/compute price only after step 2. Kill condition: the purported program still requires unbounded native hidden state or full module execution at every declared edge.
4. Keep block-live multi-site DAS as a secondary method audit, not the main circuit route. Kill condition: identity closure fails, restarts are nonidentifiable, or held-out causal/complement bars fail.

The current route remains highest information because it can turn the nearly complete mediated path into an independently executable causal program in one small experiment. The next mathematical review remains due around 14:30 UTC.
