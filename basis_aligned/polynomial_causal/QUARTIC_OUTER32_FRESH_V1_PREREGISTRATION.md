# Frozen outer32 interface: fresh lexical/construction validation

12 September2026. The developmental screen passes; this test is registered before any model evaluation on the new rows. Its target is the pure bias-free MLP16→MLP17 quadratic/quadratic numerator in the previously frozen two-output subspace, with native input normalization, MLP17 denominator and background retained. It is a component approximation, not a standalone model or a semantic circuit claim.

Freeze `QUARTIC_OUTER32_V1_PROGRAM.pt`, SHA256 `090998427b7f3265c8c558fdf71bcd5190c3f9b84aef46f8df019ff4e91d9999`. The program reads the exact native bias-free MLP16 producer, uses32 spectral terms per output, and writes through its two frozen residual directions. No fitting, rank changes, answer-conditioned factor choices or outcome filtering are allowed.

[Rows](QUARTIC_OUTER32_FRESH_V1_ROWS.json):64pairs/128endpoints,16pairs each for agreement verbs, count nouns, past and progressive. Authority digest `76ae028307fae869f085c107b34e022b54938c3831c58de063066249e95342fe`. Answer tokens are disjoint from four earlier registered panels, and all sentence constructions differ. This is a lexical/construction generalization test, not evidence of pretraining-disjointness or corpus OOD. The deterministic builder takes the first16 eligible lexical groups using tokenization and prior-token exclusion only. V1 had only14eligible nouns; V2 appends a fixed noun list before any outcomes, yielding27eligible nouns. Preserve both sources and their explicit tokenization exclusions.

Execution reuses established native prefix/capture and tail calculations. Capture normalized MLP16 input and actual MLP17 pre-MLP residual/native output. Evaluate the exact selected composed component and frozen approximation on the same states. On the first batch, compare manual native tail and a physical last-MLP replacement hook to establish that the intervention site is correct. Bucket lengths8–11;16batch8 prefix forwards plus two first-batch hook controls,144sequences total. Source/checkpoint/row bindings and managed lane1 execution are mandatory. No direct GPU run. Literal price is76,096 fitted values plus15,925,248 shared native producer values, scalar residual scale, native normalization and background. These dependencies are not removed by a passing test.

Predictions, scored independently:

- A: finite values, source/row identity, manual versus physical tail relative error<=1e-5, and native exact-component algebra error<=1e-8. No reference to old-row effects for the fresh reference.
- B: every family has swap relative RMS error<=0.10, sign agreement>=0.90 and at least4live pairs at the existing1e-4effect cutoff. Use the existing symmetric relative-RMS convention.
- C: every family has removal-effect CE mean absolute disagreement<=0.02nats. CE added above the real model is damage; lower is better.
- D: every family has physical-write relative error<=0.05.
- E: native answer-versus-foil capability>=0.75 for each family and each base/donor side. Failure restricts behavioral interpretation even if approximation fidelity holds. Do not discard the offending rows.

The null is that the developmental approximation fails under the lexical/construction change. Preserve all failures. Successful component fidelity would justify subsequent shared-dependency/composition and broader FineWeb/corpus-OOD checks; it does not prove task selectivity, independence, stable factor identification, or the four-property goal. A failure is followed by an accounting check or a different weight-derived model assumption, not fitting to these rows.

Current implementation status: row builder executed and authority frozen; managed native capture/scoring job is the next implementation task and has not been enqueued.
