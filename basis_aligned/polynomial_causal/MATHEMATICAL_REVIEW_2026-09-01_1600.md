# THREE-HOURLY MATHEMATICAL REVIEW — 2026-09-01 16:00 UTC
Context: the mechanism arc published (grammar I>T>C>S; head-3-dominant-but-distributed carriage), and the 403/404 bridge showed the p448 compressor's error is TOKEN-GRAMMAR-LED (T+I jointly; I-only dominance died at 4× scale). The live mathematical question: what objective should encoders be fit in? Sign convention §2135: damage = CE added above native, LOWER IS BETTER.

## Top three moves (ranked)

### 1. EXECUTED AS SPEC — Two-sided weighted low-rank approximation (WLRA) for the encoder, with the branch factorial as its evaluation instrument
**Object:** the shared-input encoder Q of any MLP context-RRR program (p448/p768 family).
**The math:** current encoders solve min E‖[L;R](x−QᵀQx)‖² under input covariance — one-sided Eckart–Young on W·C^½. The bridge says the loss that matters is concentrated in the T+I branches, i.e., an OUTPUT-space anisotropy the input metric cannot see. The correct generalization is two-sided WLRA: minimize ‖S^½ · W · (I−QᵀQ) · C^½‖² with S an output-space (branch-sensitivity) weight — closed form: Q = top right-singular basis of S^½·W·C^½ (weighted Eckart–Young; exact for orthogonally-diagonalizable S, C).
**Why this is NOT the falsified reweighting class:** 350/353/399 reweighted SAMPLES (input-density scalars: leverage, gradient-norm, response-PCA rotations) — all one-sided input-metric changes. This is an OPERATOR weight on the output mode, the same structural slot where the only measured family differences live (355's 3.3× family gains; 399's rank-1-vs-flat consumer spectra). Different mathematics, untested.
**Assumptions that may fail:** (i) a usable S must be estimable from the T+I branch responses without circularity; (ii) CE-vs-weighted-MSE alignment (the family-gain lesson says the gain differs by family — one calibration point fixes it).
**Measurable consequence beyond reconstruction:** at EQUAL price (p448, 9,954,432 values), the branch factorial (403's instrument, now exact) directly measures whether T+I Shapley damage drops. That is a mechanism-targeted compressor test, not an MSE claim.
**Preregistration template handed to Codex (bars its to freeze):** arms = {covariance-RRR p448 (measured baseline: SELECT total +.00564, T+I ≈ +.0075 combined), WLRA p448 with S from branch-response Gram, S-shuffled control (permute S's eigenbasis — the falsifier that would catch another instrument illusion), equal-price PCA control}. pred: WLRA cuts combined T+I Shapley by ≥25% at equal total price without raising C/S/AUX damage by more than the cut; shuffle within noise of baseline. Null: WLRA within .0005 of baseline or shuffle matches WLRA (weight not causal). Evaluate on 403's FIT/SELECT + one fresh wave.
**Cheapest falsifier:** exactly that one physical p448 rebuild (~130s) — the branch factorial makes it decisive either way.

### 2. Multi-intervention abstraction certificate (bisimulation battery)
Still the right upgrade at the NEXT adoption: the gate family now has a16 KO + branch factorials; a frozen intervention basis (a16, m16, one head-write, one branch injection) scored jointly turns "intervention-faithful" into an abstraction radius. Costs ~4×140s per artifact; schedule with adoption, not before.

### 3. MDL for the mechanism dossier (prequential pricing of explanations)
The grammar/mechanism results now have predictive content (e.g., 404's per-wave T+I predicts compressor damage ordering). A prequential score — bits saved predicting held-out branch damages using the grammar vs without — would price the EXPLANATION itself, closing the loop on "explained fraction" with a principled unit. CPU; a candidate for a quiet hour, mine.

## Pruned
- One-sided sample reweightings (three falsifications — closed as a class); degree-1 token kernels (396); Q compression (399); routing/sharing/blocks (all closed); further ANOVA granularity without a compressor consequence (the factorial is now an instrument, not a product line).
