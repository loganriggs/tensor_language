# Fold MLP8 into the first receiver's current-value reader

The head9.8 seven-term fold falsifies routing-only prediction: error.8122 on reversed documents and1.1343 elsewhere. Its delta-value term has aligned fractions.7588 and1.1075. This nominates the attention8 edit→MLP8→head9.8 current-value path, not a new independently identified circuit.

Fold C=Wv9.8 Down8, retaining native Left8 and Right8. For unnormalized MLP8 input z and attention8 edit d, expand the normalized bilinear difference into ordered Ld*Rz, Lz*Rd, Ld*Rd and native Lz*Rz times inverse-RMS-squared change. Add direct Wv*d, propagate lambda9 and current-value mixing, then add the separate block9 input-normalizer change. MLP8 bias cancels in its difference, but remains in the supplied native mixed9 background. No fitted coefficient or dropped cross term.

Implement six signed value-response pieces: direct, left-cross, right-cross, quadratic, MLP8 normalization, block9 normalization. Both mixed9 states remain native normalization inputs; this does not close their ports. Frozen exact interchange, all40opened FineWeb sequences. TwoCPUthreads;80sequence-equivalent forwards/36block calls;180second cap. Compare independently captured head9.8 complete value difference and previous deltaV response contribution.

pred_a: native/edited anchors<=1e-4abs and1e-5relative; summed six value pieces match captured deltaV<=1e-4relative; pulled-back deltaV contribution<=1e-4abs and1e-4relative; finite/support/count/CPU. pred_b: omit MLP8 response (retain direct and block9 normalizer) and require deltaV target-effect error<=.35 in BOTH frozen reversed and other document groups. pred_c: quadratic piece norm<=.10deltaV target-effect norm in BOTH groups. All failures retained; no behavioral sufficiency/selectivity or standalone-circuit claim.

Report exact factor shapes/counts, source dependency ports, signed fractions plus norms, source positions and controls. A passing algebraic fold supports a smaller reader computation only; a candidate selected from these opened outcomes needs a separate fresh installed edit.
