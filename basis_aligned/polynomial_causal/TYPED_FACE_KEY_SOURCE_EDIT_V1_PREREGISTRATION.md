# Both head8 key factors: native residual-source factorial V1

Follow the exact MLP7 fold with a causal test, not norm-based promotion. At the
city, raw block8 residual r=c+m+e has sources c=l80*g7, m=l80*MLP7,
e=l81*initial. For all eight donor/recipient combinations, form the raw hybrid,
recompute its residual RMS, project BOTH K1 and K2, carry both head-key RMS,
and use the full QK1*QK2*V product. Recipient queries and current value stay fixed;
inherited value is held donor across all eight source corners. This isolates
key-source differences relative to the donor-inherited, recipient-key baseline.
All other head9 odd-path and suffix backgrounds are the existing conditional ones.

Native + eight source corners, eight opened sequences,72forwards/300seconds.
No weights fitted or inputs chosen by outcomes. Original two-port composition
failure and generic three-write additivity remain unchanged.

pred_a: reconstructed native city current<=1e-5 relative; full-source corner
matches compiled full face and zero-source corner matches inherited-only physical
write within1e-5 maxabs AND1e-6 relF on ten readouts. Pair/full target RMS>=1e-5.
pred_b (carry sufficiency screen): carry-only key increment (corner1-corner0)
predicts full key increment (corner7-corner0) within .35 relative L2 in each cue
and endpoint cell. The native fold suggested this, but does not establish it.
pred_c: carry+MLP7 increment (corner3-corner0) predicts full increment within .20
in every same cell. pred_d: all8corner Mobius expansion closes<=1e-12.
Report all singles, pairs, third-order effects and four unrelated readers.
A failure rejects the proposed source sufficiency; it does not license source
reselection or claim causal dominance from the earlier aligned norm fractions.

This is an opened-context screen. No fresh generalization, selective source unit,
random-group specificity, or source omission is certified here. All three source
arrays still come from native state generation. Only the ensuing valid causal
result can select which earlier computation deserves the next exact fold.
