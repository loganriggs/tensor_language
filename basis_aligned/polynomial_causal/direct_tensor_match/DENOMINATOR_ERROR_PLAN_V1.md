# Which states drive normalization-weighted removal error?

Use the completed REMOVAL_STAGE_STATES_V1 cache and its producer hash. For each of the five lean final-effect failure cells, concatenate the selected states, preserve document identity, and compare raw squared scalar residual/reference sums with sums weighted by inverse denominator squared. This exactly reproduces stages zero and one. No model fitting or candidate selection.

Pre-register a descriptive hypothesis: the bottom10% of denominator values account for more than50% of denominator-weighted squared residual in every failing cell. Report corresponding reference-energy shares and worst10% weighted-residual shares, so concentrated errors cannot be mistaken for merely concentrated signal. Repeated alpha cells share the same scalar calculation; they are not independent confirmations. A false hypothesis means the denominator failure is not universally localized to that small-denominator tail.

This is finite opened-state diagnosis, not a causal explanation of input semantics or a new approximation. No alteration of queued L-BFGS or native helpers.
