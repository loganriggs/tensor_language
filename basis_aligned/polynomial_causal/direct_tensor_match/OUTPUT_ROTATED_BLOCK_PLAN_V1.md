# Output-rotated block-term quadratic baseline

22 September 2026, 03:15 UTC. Native channel subsets and independent fixed-output decompositions remain inaccurate. Change the output feature directions: diagonalize the16-by16 coefficient Gram of the true selected last-MLP quadratic tensor. This orthogonal output rotation is exact and preserves total coefficient error. Independently factor each rotated quadratic slice, then optimally allocate signed bilinear products using the decreasing eigenvalue-energy gains. This supplies block terms with learned shared output effects; it is still restricted to an orthogonal output basis chosen by SVD, not a globally optimal joint tensor factorization.

Budgets512/1024/2048/4608 total products. Compare the existing optimal natural allocation in the original fixed-output coordinates. Primary prediction: at512 and4608products, rotated block terms reduce natural centered error by at least20%relative. Report original-coordinate per-output errors by rotating residual matrices back; no basis change may hide weak-coordinate errors. Include uncentered Gaussian errors, mean fraction, reader/output coefficient costs, native exact4608baseline. Preserve priors if predictions fail. No text-state fitting or causal claim.

Controls: planted orthogonal shared-output mixture of distinct low-rank quadratic forms must recover to<1e-10 when its source forms are orthogonal; complete output rotation must preserve tensor/Frobenius and random polynomial evaluation<1e-12. Native full rotation/replay<1e-10. Reuse already checked signed eigenpair factors and exhaustive product-allocation controls, with no queued helper mutation.

03:16 descriptive follow-up: natural rotation passed both primary improvements but leaves worst original output31.54%at4608products. Repeat after normalizing original output matrices to unit Frobenius norm, rotate in that metric, and undo normalization in the output map. This preserves the equal-output objective, not the natural objective. Compare its own equal-weight independent-output baseline; report both metrics and original-coordinate errors. Original natural-weight predictions remain unchanged.
