21 September 2026, 18:06 UTC. Full MLP16 source -> residual carry -> explicit RMSNorm17 -> full MLP17 -> final RMSNorm/unembedding/softcap. Source lambda17[0] D16[(L16 z)(R16 z)], z uniform radius sqrt(1152). Source bias lambda17[0] Down_bias16 explicitly added. Carry is independent uniform sphere, RMS .5,1,2 times sqrt(E||source||²/1152), derived exactly from weights. It absorbs all other paths as a declared artificial random background, not a native generative model.

Use 2048 samples, fixed seed 180606. Donor source is cyclic shift by one; same carry for both endpoints. Compare four frozen full-layer programs: original covariance pruning, learned covariance/response, mixed, isotropic. Each has same product/coefficient budget; affine correction remains in export, so evaluating on artificial probes is not end-to-end data-free discovery. Report normalized full-vocabulary natural replacement discrepancy and source-swap response discrepancy, centered per vocabulary row after final softcap. Weight-only artificial distribution, no fitted new candidate or semantic labeling.

pred_a_moments: empirical source second moment trace within 10% of exact prediction; mean error <= .1 times exact total source RMS norm; all arms finite. pred_b_ranking: mixed has smaller source-swap response error than unguarded learned candidate at all three ratios. pred_c_global: isotropic candidate has smaller source-swap response error than original pruning at all three ratios. Null: this weight-generated law does not explain the native candidate ordering. Failures retained; one artificial sample seed does not validate a distributional conclusion. Moment checks validate generator, not agreement with native text states. No new hyperparameters selected from outcomes.

Exact source moment controls use independent spherical quadrature on five toy structures (<1e-12). Price: 2048 source evaluations, three carry ratios, two endpoints, five last-MLP models (teacher plus four students); vocabulary output evaluated in batches128, no transformer forwards. FP64 exact moments, FP32 functional probes, TF32 disabled. Save JSON only. If rankings differ from actual source-swap tests, synthetic loss cannot be assumed adequate for circuits; choose composed calibration or richer carry/source dependency before optimizing it.
