# Covariance: objective versus conditioning; native statistics

Two independent steps follow the synthetic covariance tradeoff.

**Paired-coordinate toy control:** x=Lz where LL^T equals the fixed earlier calibration covariance. Transform teacher and random student weights exactly, so raw and whitened runs have the same physical initial function and the same covariance objective. Compare Adam/Muon, matched/wide architectures, seeds0/1/2,1200steps,lr.05, no penalties:120fits. Evaluate every fitted function in original coordinates under both isotropic and covariance metrics. Independent monomial transformations, factor transformations and metric congruence pass CPU controls. A change in convergence is a parameterization/optimizer effect, not evidence that the underlying objective changed. The shared coefficient implementation contains all repeated-input monomials.

**Native covariance capture:** fixed32documents from fineweb_n96_skip1200.pt for calibration and32from fineweb_n192_skip7000.pt for evaluation, first64tokens, batches4. Exactly16native forwards. Capture actual normalizedMLP17 inputs h and transform to the same effective coordinates q used by the folded joint tensor: E=R^T Q^T and row h=qR. Float64 triangular solve; save mean, centeredcovariance, uncenteredsecondmoment and rows separately with tokenhashes. These panels are document-disjoint by cachedsource offsets, not asserted broad OOD or historicallyunused.

pred_a_capture:16forwards and2048tokenrows/panel. pred_b_identity:h=qR and secondmoment=covariance+meanoutermean replay<1e-10relative. pred_c_shift:relative covariance difference>1%. No fit or supportselection on evaluation covariance. Future metric uses calibrated covariance with declaredridge; preserve zero-meanGaussian versus actual nonzero-mean/higher-moment distinction. Native normalization remains explicit outside the homogeneous teacher tensor; collected q corresponds to normalized input coordinates, not a claim that the complete model is polynomial.
