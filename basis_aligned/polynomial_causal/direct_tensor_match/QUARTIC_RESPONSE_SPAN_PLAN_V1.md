**Fixed-feature finite-response span, before observing results.**

Native interchange failed with comparable errors in parent656 and paired384. Determine whether the wider dictionary can represent the missing polynomial changes with unrestricted readout. Reuse first16documents/domain, context256, positions16:255, next-document cyclic donors and strengths.5/1 from PAIRED_ROOT_INTERCHANGE_PLAN_V1. Frozen narrow26 and parent656 feature directions and artifact hashes unchanged.

Compute deltaPhi=phi(x')-phi(x), deltaY=F(x')-F(x) for purequartic native numerator, projected by R_U from U=QR_U. Divide native outputs and reference writers by fixed teacher_scale from NATIVE_VARIATION_AUDIT_V1 for numerical scaling. This preserves relative Euclidean error in unembedding space, NOT the actual nonlinear logit response metric and NOT RMS-denominator weighting. The diagnostic answers a restricted capacity question before further native optimization.

For each dictionary/domain/strength, SVD least squares with RMS-scaled columns and rtol1e-10 gives an in-sample oracle. Also fit one readout to both strengths within each domain and report individual errors. Keep original writer as baseline. No export or claimed validation: every oracle sees the evaluated targets. Width4 permits10rootproducts; width32 permits528. The original32 dictionary's readout is unrestricted here, not rank16 or paired-constrained.

Pred_a_integrity: relative least-squares orthogonality residual<1e-8; physical-vs-QR numerator response replay<1e-5; independent known-residual and basis-change CPU controls pass. Pred_b_capacity: each width32 single-cell oracle error<.10. Pred_c_writer_gap: each width32 oracle error<=.5 original writer error. Null: error persists under the oracle, so writer-only refitting cannot satisfy this polynomial metric for fixed features. Report singular ranks and minimum singular ratios. A passing oracle does not guarantee transfer, accurate nonlinear endpoints, or feature identity.
