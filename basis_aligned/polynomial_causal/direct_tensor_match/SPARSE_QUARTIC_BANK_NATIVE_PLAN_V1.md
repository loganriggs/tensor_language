Broader sparse shared quartic bank — native V1

Target unchanged: native pure quartic MLP16→17 projected onto16fixed data-informed vocabulary-metric readers. Exact symmetric coefficient self/cross loss with readout profiled using ridge1e-6; no input probes in fitting. Scale19054614563.464127. Fixed topology144quadratic features x4bilinear products,512selected upper-triangular pairs (all144diagonals+368offdiagonals supportseed1100). Topology is a hypothesis, not optimized/discovered sparsity. Every selected pair is computed once and shared by16readouts. 1088variableproducts,1353728floatcoefficients,1024integerindices; full1152inputspan possible. Compare1536productCP512 and384product oldhierarchy without equalbudget claim.

Two random initializations1101/1102. U,V parametrized as576x1152matrices for Muon, reshape144x4x1152 and normalize each quadratic coefficient Frobenius norm. Muonmatch_rms_adamw,100steps,rate.1sqrt(4/1152),cosine1%floor. Scale training loss by absolute initial explainedscore, no teacherconstant. Five dense controls pass<6e-15; known wider sparse-bank recovery with8features24pairs atd4 passes Muon10/10 below1% at400steps. This is a heuristic transfer, not a native convergence guarantee. Timing and peak memory reported;100steps andCP400 are not equal computation budgets.

Predictions: a) finite objective/gradients,readout normalresidual<1e-8 and physical exported FP32/FP64 scalar replay<1e-4; b) both starts improve exact regularized explained score>=2x and independent sampled coefficient error; c) some start beats the best CP400 values on BOTH sampled coefficient error .9777571 andGaussianerror .9887785. Text errorreported separately, with old empirical8.13% reference. Zero100%is not sufficient comparison. If c fails, distinguish finite optimizer effort, topology assumption and capacity; do not declare HT/generalDAG impossible.

Independent evaluation indices4096seed951,Gaussian1024seed939,openedtext2048 identical previous comparisons. Save raw factors, pair support, physical root coefficients and fixedwriter. No graph compiler approximation. Native manipulation validation follows only if predictive fidelity warrants. CPUdryrun exercises actual144x4/512Gram gradients and metrics before enqueue; existing helpers used without modifying live files.
