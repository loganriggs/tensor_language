# Component balance and separation — 2026-09-20 16:58 UTC

Redteam the successful residual control over a3x3 grid: ratio of output-writer amplitudes {1,.5,.1}, correlation between corresponding input vectors {0,.5,.95}. Four input slots use separately sampled orthogonal pairs in1152 dimensions; teacher writers are orthogonal. Thus quartic feature overlap is measured, not assumed equal to the linear overlap. Compare joint1500steps with residual600+600+joint300, Adam.05, finalrank2, one fixed teacher orientation and one initialization per cell. Normalize initial joint raw vectors to unit norm to remove the identified scale difference in final refinement. Hypothesis: residual growth helps separated unequal components; near-collinearity makes intermediate dictionary recovery ill-conditioned and may erase its advantage. This is a controlled interaction grid, not a multi-seed population estimate. Report signed squared errors and atom correlations to distinguish numerical floor and non-identifiability.
