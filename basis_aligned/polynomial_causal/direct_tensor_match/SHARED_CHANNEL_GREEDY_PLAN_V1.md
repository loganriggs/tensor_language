# Shared native quadratic channel dictionary: residual-aware selection

22 September 2026, 03:13 UTC. Target: true last MLP quadratic projected to the fixed16 output readers, all1152input dimensions. Earlier NATIVE_CHANNEL_BASELINE used the full output frame and independent-energy/random selection. This test uses the selected16-output target and forward selection by exact joint residual reduction, then globally refits the output readout. Native channel factors remain fixed; this is a constructive baseline, not random-start structure discovery.

Each dictionary element is phi_j(x)=(l_j.x)(r_j.x). Its symmetric coefficient Gram is K_ij=0.5[(l_i.l_j)(r_i.r_j)+(l_i.r_j)(r_i.l_j)]. Normalize each dictionary element in this metric. Select the channel maximizing squared residual cross-correlation across outputs divided by the residual dictionary norm. Orthogonalize in coefficient space; accumulated gains equal exact orthogonal-projection improvements. Compare natural output weighting and per-output Frobenius normalization at64/128/256/512channels. Compare exact readout refits on individual-energy-selected channels at the same widths. Count shared products once, 2*d*k+16*k reader/readout floats, no frame overhead beyond fixed readers shared by all alternatives.

Primary prediction: at512channels, residual-aware selection reduces its own weighted relative coefficient error at least20% versus individual-energy selection. Also report absolute natural/equal-output centered Gaussian error and per-output error without changing weighting after seeing outcomes. No text/OOD/causal claim. Five small planted dictionary controls must match independent dense least squares and replay with residual<1e-10 relative, including cancellation and duplicated atoms. Native selected readout must match accumulated gains<1e-8 relative energy. Shared immutable GPU helpers are not edited.
