# Allocate the same six products differently — 2026-09-20 18:26 UTC

Prior turn: progress (native shared-bank/input-space receipts, audited successors and pushed report). Native jobs remain queued behind a confirmed live GPU process. Useful independent CPU question: does allocating a fixed number of bilinear products into fewer richer quadratics or more simpler quadratics change recoverability?

Reuse the exact five teachers from SHARED_BANK_TOYS_V1, dimension6, three outputs. Compare bank×products-per-feature 2×3, 3×2, 6×1. All have72 input-reader scalars and at most the same full6-dimensional input span. Root outputs cost9,18,63 extra scalars respectively; report separately, never claim equal total price. Reuse existing forty3×2 fits; execute eighty new fits for2×3 and6×1, Adam/Muon, rates.005/.05, seeds0/1,600steps and same cosine schedule/best-training checkpoint. No model/GPU work.

Six-by-one has an exact constructive capacity witness: split each of the three teacher quadratics into its two products, then expand its root interactions. Validate writer-refit recovery at that split initialization by exact full coefficient enumeration; do not infer functional capacity from successful optimization alone. Two-by-three need not contain the teacher; failures there remain capacity-or-optimization ambiguous.

Pred_a_witness: all five split witnesses coefficient error<1e-6. Pred_b_recovery: six-by-one random fits attain<1% coefficient error on all five families. Pred_c_tradeoff: at least one family's best two-by-three error exceeds best six-by-one by0.05. Preserve failed predictions. Report every seed, parameter count and selected step; compare function recovery, not semantic identity. Dense root cores are not sparse circuits. This controls input coverage while varying dictionary/interactions, addressing which structural constraint to change next.

## Follow-up registered after allocation result — 18:28 UTC

All five six-by-one oracle splits replay within2.5e-8, but random recovery fails on coordinate teacher (best31.11%); predicted all-family recovery failed. That teacher is built from positive coordinate squares, while a general bilinear product has an indefinite quadratic form except at collinear factors. Test an explicit square-leaf hypothesis q_a=(u_a^T x)^2 for coordinate and rotated_signed families (signed combinations remain in the root writers). Both have exact six-square witnesses. Eight fits each: Adam/Muon×rates.005/.05×seeds0/1,600steps. Charge36reader+63writer scalars, versus72+63general-products. Same exact coefficient objective; no universal positivity assumption.

Pred_d_square_capacity: witness error<1e-6 on both families. Pred_e_coordinate_rescue: best coordinate random fit<.01. Pred_f_signed_preserved: best rotated_signed random fit<.01. Negative results are optimization/parameterization evidence, not capacity evidence. Preserve initial/best checkpoints and independently enumerate coefficient errors of selected fits.
