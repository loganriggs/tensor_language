# Frozen hybrid feature stability — 22 September 2026, 08:23 UTC

Claim: determine whether the two completed hybrid starts recover the same local computations, rather than merely similar error. No fitting, selection or graph adoption. All twelve outputs 4–15 included. Both dictionaries have eight quartic atoms per output, full 1152-dimensional inputs, same fixed CP1001 parent.

Compare each pair of frozen dictionaries under (1) exact calibration-covariance Gaussian moments and (2) the opened 16384-state text metric. Whiten the two within-dictionary Gram matrices, then take singular values of the cross-Gram matrix. These are canonical correlations, invariant to invertible changes of basis. Compare scalar correction functions separately by normalized inner product using their actual coefficients.

Prediction A (integrity): all within-dictionary Grams positive definite without ridge; canonical correlations lie in [0,1] up to 1e-7; identical dictionary control yields correlations 1 within 1e-9. A singular dictionary is an instrument/representation issue to report, not repair silently with ridge.

Prediction B (subspace stability): smallest canonical correlation at least 0.9 for every one of the twelve outputs in both metrics.

Prediction C (correction stability): correction cosine at least 0.9 for every output in both metrics.

Null: comparable reconstruction losses may arise from different local feature spaces or correction functions. Subspace failure with correction pass would mean a stable aggregate effect but not a stable reusable dictionary. Failure does not exclude overcomplete dictionaries with shared useful subspaces, or a common circuit in a different representation. Success does not identify semantics or establish OOD/causal fidelity. No threshold tuning, output omission or candidate export.

CPU only, cached inputs, no new GPU capture. Price and reconstruction errors of the candidates remain as already measured. This check changes the evidence for stable identification before arithmetic graph assembly; it is not another readout oracle.
