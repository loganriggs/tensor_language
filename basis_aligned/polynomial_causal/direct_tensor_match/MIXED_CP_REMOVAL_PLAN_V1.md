Frozen mixed-CP root1 native finite-removal check

Both1001/1002artifacts frozen after best-training-objective selection. No coefficient refit, graph edits or evaluation selection. Compare each scalar root1 to the original native pure-quartic function projected onto the SAME fixed vocabulary-metric reader, remove alpha times that scalar along the SAME writer from the original final residual divided by actual MLP17 denominator. Alpha .25 and1; retain all other branches. Execute actual finalRMSNorm and30tanh(logit/30). This compares executors of an operational component; it does not assert a unique semantic native component.

Reuse original ROOT_CASE_ABLATION_V1 panel:16FineWeb/16codeprefixes, positions16–254, current-token newline versusother. These are opened panels, not untouchedOOD. Case-preference readout remains8805paired uppercase/lowercase initial token variants. Native reference effects/energies must replay the prior result; the prior native capitalization-selectivity failure remains in force. No redundant new semantic success criterion.

Pred_a_instrument: nativebilinear-replay and normalizedreader/writererror<1e-5; zeroeditexact0; native referenceenergy relative replay<1e-4. Pred_b_absolute: all16seed/domain/strength/conditioncells fullvocabulary logit-responseerror<=10%. Pred_c_improvement: everycellerror<=.8itsolderempirical384counterpart. Preserve both misses and improvements. No seedselection. Every candidateprice1536products2385920coefficients, versus384/322048baseline. Fullscope includes quarter/full and FineWeb/code, not only the bestnewlinecell.

CPUpreflight checks actual512term CPvalues, two-seed aggregation branch, existing casepairset. Managed runner only. If finite removal fails despite sensitivity improvement, compare the changed context/domain and scalar versus downstream effect before assigning a semantic label. No end-to-end model replacement, extraction, stableunitidentity, causalreuse or circuitadoption claimed from this screen.
