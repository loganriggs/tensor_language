# Cross-bank stability of canonical output-shared features

Eight root restarts preserve centered functions but fail primitive identity.
Post-hoc output-variance canonicalization yields minimum feature cosine.98355
and output-direction cosine.99959. This is a deterministic basis convention
for a stable low-rank function, not evidence of monosemanticity or unique
primitive computations.

Next independent structural perturbation: replace the selected shared6 bank
with the other high-rate Muon seed from BANK_WIDTH_FRONTIER_V1, keeping its
learned native input readers and four bank outputs. No selection on semantic
or empirical text scores. Fit root4 with Muon.03,seeds0–3,500steps, same exact
root covariance and penalty.001. Keep the currently exported candidate frozen.

Compute each alternate bank's Gaussian root moments from its actual archived
FP32 readers. Compare output-shared modes against selected rootseed0 using
exact cross-covariance between the two banks' quartic root features under the
same input mu,M. Do not compare raw coefficient coordinates from different
input frames. Register minimum matched canonical feature cosine>=.95 and
minimum output-direction cosine>=.98 across all four alternate fits, plus
complete-program Gaussian prediction cosine>=.995. Also report output energy
gaps and objective gaps; failures may reflect approximation or optimization.
No output target fitting or semantic labels. If held, this supports basis
stability across both hierarchy levels, not selective native intervention.
