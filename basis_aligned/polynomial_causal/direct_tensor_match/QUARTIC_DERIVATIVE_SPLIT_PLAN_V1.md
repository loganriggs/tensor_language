**Distinguish unread input directions from wrong computations within the reader span.**

Use the same pure quartic native target and16 evenly spaced anchors on each opened panel. Four fixed32x4 dictionary readouts: empirical-long, isotropic coefficient, second-moment coefficient, second-moment Gaussian functional. Their U,V leaf directions are identical. Transform physical writers into the native QR output frame. Compare isotropic, centered_floor01, second_floor01 input perturbation laws; centered is primary.

Let L be the perturbation square root and Q span the transformed leaf readers. Decompose E=(Jteacher-Jstudent)L into EQQ^T and E(I-QQ^T). These are orthogonal; the latter equals the teacher's unread response because JstudentL lies in Q. Report total relative derivative error and each squared-error share. The within-span term is not automatically removable by an allowed nonlinear graph, but cannot be blamed on unread directions.

Pred_a_integrity: nativefirstJacobianFP32/64<1e-3, squared-errorpartitionclosure<1e-8, studentoutside error<1e-8relative toteacher. IndependentbankJacobian/autograd andrank-deficient/broadshapes controls<1e-12. Pred_b_inside: empirical centered within-span squared-error share>50%bothpanels. Pred_c_gaussian: Gaussianreadout centered derivativeerror<empiricalbothpanels. Null: readerrestriction dominates, orGaussianreadoutfailsdespiteexactGaussian fitting.

No fitting, no candidate selection/export, no new inputs. Same656products903168coefficients. Prior rank bounds do not prove nonlinear realizability; this audit locates the observed derivative gap. Local derivatives of the polynomial are not complete normalized-model interventions or semantic evidence.
