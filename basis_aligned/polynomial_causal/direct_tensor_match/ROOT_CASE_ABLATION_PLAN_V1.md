**Native rank-one quartic removal: newline/case contrast screen**

Frozen empirical384 feature1. Exact reference component w1 t(x), t(x)=r^T purequartic(x), r=U^TUw1/||Uw1||². Candidate w1 h1(x). Remove strengths.25 and1 from original final residual, divided by actual MLP17 input RMS denominator; other terms unchanged. All effects subtract the ORIGINAL native unedited logits (no candidate replacement baseline here). Final RMS and softcap explicit. This is removal of an output-projected purequartic component, not an independently identified native module.

Behavior: matched token byte strings differing only in uppercase versus lowercase first ASCII letter, optionally after one leading space; aggregate score logsumexp(uppercase IDs)-logsumexp(lowercase IDs). Fix full tokenizer-derived pair list before model execution. Rows: first16FineWeb and16stdlib prefixes from existing FULL_CHANNEL_FRESH_TOKENS_V1, context256 positions16:254; currenttoken contains newline versus allotherpositions. No newfitting or untouchedOOD claim. Null: rootassociationdoesnotproduce selective caseeffect, or approximationmissesnative removal.

Registered predictions: (a) purequartic re-evaluation relativeerror<1e-5, r^Tw1=1within1e-5, zeroeditexact; (b) candidate versus native projectedremoval full-logitresponse error<=.10 in everydomain/strength/condition cell, with>=20newlinepositions and>=5newlineprefixes eachdomain; (c) nativeprojectedremoval lowerscasecontrast bymean>=.01 afternewline for bothstrengths/FineWeb, and meanabsolute newline contrastchange >=2 times nonnewline. Keep codecaseeffects descriptive because codecase semantics differ. Allgatesmustholdforpromotion; a pass would still require independent task/OOD/selectivecontrols.

Graphprice384products322048coeff+768indices; targetprojectionstillrequiresnativequarticteacher andisreferenceonly. This pilot does not claim160productextraction or wholemodelspeedup. CPUpreflightcheckscasepaircorrectnessandrankoneprojection/denominator algebra beforemanagedGPU.
