# Exact product reassociation into a shared arithmetic DAG

22 September2026,03:46UTC. Reuse arithmetic_dag.DAG; add a bounded compiler for quartic CP products over an already canonicalized linear-reader bank. Existing compiler interns a fixed tree; this compiler enumerates all binary product trees per quartic multiset, allowing shared quadratic AND cubic nodes and repeated-root/cancellation merging. Try four deterministic root orders and choose smallest reachable product count, then edges. Greedy across roots, not globally optimal arithmetic-circuit search. No approximate equality, changed feature directions, or new factor fitting.

Canonical monomials are sorted multisets of reader IDs. Exact rational output coefficients merge duplicate roots and remove exact cancellation. This does not claim arbitrary linear-reader algebraic identities are detected. Dense reader cost remains explicitly separate at the input interface. Count each reachable product once; routing/edge overhead reported. Tests: independent products, fourth powers, common quadratic, common cubic, duplicate cancellation. Toy DAG polynomial expansion must equal the original rational polynomial exactly and attain the known construction's product count. Shared cubic test distinguishes this from a fixed quadratic/quartic tree.

Apply to both frozen native CP512 parents and both128-reader-reuse exports. Compare direct values on256 opened states in float64 (<1e-10relative). Native originals have distinct readers, so no product saving is expected; primary descriptive question is whether linear-reader sharing exposes any higher-order repeated products. No new native approximation or intervention claim, and no export absent a measured saving. Existing queued helpers remain immutable.

03:49 implementation refinement before any export: prefer fewer cubic nodes when a local plan ties on count/future reuse, and lower product depth when whole programs tie on count. Initial lexicographic ties gratuitously turned isolated quartics into cubic chains. Preserve counted-product objective, but avoid claiming a runtime improvement from fewer operations. Common-quadratic control places shared readers in different fixed-tree halves to exercise reassociation, without changing its polynomial class. Native seed1002fixed-tree interning already saves one product (1535); distinguish the additional10from total11versusnaive1536.
