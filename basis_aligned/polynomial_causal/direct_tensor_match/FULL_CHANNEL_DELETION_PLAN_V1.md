**Broad-output baseline with conditional deletion scores**

Keep native quadratic products and compare magnitude-energy retention to one-channel conditional deletion importance. For positive-definite product Gram K, removing channel j while optimally refitting all other output weights costs ||C_j||²/(K^{-1})_jj. Independently check this identity against seven explicit feature-matrix regressions. Bulk ordering by singleton costs is only a heuristic; report failure if it loses to magnitude selection.

Full lastMLP unembedding-folded target, folded Euclidean and historical covariance input geometries. Retain2304/3072/3686of4608channels; solve output weights exactly for each subset. Twelve fits,0native forwards. Necessary baseline for simple-program claims, not random-start discovery. Output dimension remains1152. Reduced factor storage3*1152*k plus common input/output frame cost; product countk. No semantic unit claim from selected native channels.

Predictions: relative normal-equation residual<1e-8, refit never worse than unchanged selected writers; conditional ordering beats energy ordering at every budget/geometry; best3686channel fit<10%coefficient error for bothgeometries. Preserve allmisses. No program adoption without normalizedmodel/intervention tests.
