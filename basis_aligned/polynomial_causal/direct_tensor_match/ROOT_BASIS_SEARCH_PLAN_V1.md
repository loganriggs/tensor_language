# Sparse interactions under quadratic feature changes — 2026-09-20 18:33 UTC

Previous turn: progress (native matched-price pruning,120allocation controls,16square-leaf controls, exact output-rank audit). Native input-capacity and covariance jobs remain queued behind a live whole-attention job. CPU test now: can an invertible change among four frozen quadratic features make eight root products approximate the exported ten-root student much better?

Target is each exported student, NOT the native teacher. Evaluate identity, quadratic-Gram whitening,64seeded orthogonal rotations, and64rotations after whitening. For each of130 bases enumerate45eight-of-ten supports and exact coefficient writer fits. All8students. Student-self Gram makes this deterministic and data-free; no activation or native-weight query. Charge16mixing scalars,16support integers,46,080other scalars. Still8root products; base quadratics remaincomputed once thenmixed. Unlike independent refactorization, sharedreaders remainunchanged.

Pred_a_replay: induced symmetric-square basis transform agrees with direct polynomial products and full-rank coefficient replay<1e-10. Pred_b_improvement: for each high-rate Adam student, best basis cuts identity-basis squared approximation error by at least2×. Pred_c_compact: both high-rate Adam students reach<5% relative coefficient error vs their exported fullstudent. These are exploratory train-on-full-known-tensor results, not identification or native error estimates. Report allmisses,chosenbasiscondition,actualprices. A random orthogonal basis search is not a canonical or interpretable feature discovery guarantee.
