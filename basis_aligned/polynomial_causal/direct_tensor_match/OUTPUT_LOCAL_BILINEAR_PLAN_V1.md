# Output-local bilinear baseline for the native last MLP

22 September 2026, 03:09 UTC. Prior result: one pooled orthogonal basis with sparse core is inaccurate. This test instead allows each of the 16 fixed output readers its own input directions. It targets the true last-MLP quadratic map on all 1,152 inputs, not the previously audited approximate quartic hierarchy's quadratic intermediates.

A symmetric quadratic matrix represented by k real products of linear forms has at most k positive and k negative eigenvalues. Under Frobenius error the best such matrix retains the k largest positive and k largest-magnitude negative eigenvalues. Pair each retained positive square with a negative square via (a+b)(a-b); unmatched terms remain signed squares. This gives an exact optimum within independent output-local product dictionaries, not a lower bound for a shared multi-output circuit at the same total product budget.

Report k=8,16,32,64,128,288,512,1152 per output; natural and equal-output coefficient error, uncentered Gaussian error including traces, all per-output centered errors, actual products and reader coefficient counts. Count shared factors for a square once, and each output-local pair once. Do not mistake preserving Gaussian mean for preserving varying signal. Native shared-channel baseline has 4608 products, 10690560 floats and zero error.

Primary opposing predictions: at k=288 (at most 4608 products total), natural centered error <20% and every output's centered error <30%. Failure means this independent-output restriction is an uneconomical substitute at this budget; it does not establish a general Tucker/DAG limitation. No native text or intervention adoption from this algebraic screen. Five planted controls (positive, negative, indefinite, rank-deficient, signed shared outputs) must pass explicit product replay <1e-12 and eigen-tail identity <1e-12. Native complete folded replay <1e-10 and selected k64 factor replay <1e-10.

03:09 follow-up after fixed-budget results: uniform k288 gives24.16% natural centered error, maximum25.21%; primary natural threshold fails, per-output threshold passes. Before interpreting independent dictionaries, also optimize allocation across outputs at fixed total512/1024/2048/4608products. Sort each output's decreasing marginal retained energy, either natural or divided by its Frobenius energy. Prefix monotonicity makes this the exact optimum for the respective coefficient metric in this output-local class. Descriptive extension, not a replacement of the original predictions.
