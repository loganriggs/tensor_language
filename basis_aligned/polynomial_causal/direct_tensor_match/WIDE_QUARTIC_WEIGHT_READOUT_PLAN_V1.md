Fixed width32, four products per quadratic feature,528root products. Compare two dictionaries: inherited1000step empirically learned directions; and a newly seeded normalized random dictionary (seed3180), with an empirical readout as its explicit baseline. For each, solve output weights in isotropic coefficient Frobenius geometry and the previously saved second_floor01four-slot coefficient geometry. The latter uses the old calibration second moment normalized by mean eigenvalue and eigenvalue floor.01. It is not exact expected functional error under that distribution.

Teacher-feature cross contractions are exact algebra, evaluated in bounded batches8rootpairs. Float32native cross compared withFloat64for three roots; Gram and528x528conditional solves Float64. Normalize by Gram diagonal; ridge1e-6on normalized coefficient features. Report the coefficient objective excluding the fixed teacher norm, not an invented relative error from an unknown constant. The fitted target is the true native purequartic path; directions remain frozen.

pred_a_integrity: Gram eigenvalues no worse than−1e-9relative, FP32/64cross<1e-3, normal residual<1e-8, physicalexport replay<1e-4. pred_b_weighted: second_floor01has lower second-panel functional error than isotropic for both dictionaries. pred_c_transfer: weighted learned dictionary beats its empirical long baseline's15.0498%second-panel error by>=20%while calibration error<=5%. All arms retained; no selection from second-panel outcomes. Each program656products903168coefficients, physicalresidualwriter withQR/scale folded. Only random+isotropicreadout is entirely weight-only; learned dictionary or secondmoment geometry includes data. No model-wide fidelity, semanticcircuits or OOD adoption claim.
