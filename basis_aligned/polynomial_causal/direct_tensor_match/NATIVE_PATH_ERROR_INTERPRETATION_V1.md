**The residual carry is an important cross-boundary error term**

The exact six-term decomposition passes source and scalar replay; the managed run completed in2.67seconds. All registered predictions pass. On code/spaced-word sites the graph-minus-baseline mean error from carry times second-read error is12.6494 for natural states and12.6665 for donor hybrids, larger in magnitude than any other single mean contribution.

This does not make the carry the sole cause. The first-read-error times the true source contribution adds10.6563 and10.2256; centering terms cancel substantial amounts. These are signed algebraic contributions, not independent causal effects. A large mean contribution also need not dominate squared error.

The carry remains the native residual supplied to MLP17 after subtracting the selected MLP16 write. It includes earlier computation and attention17. Before introducing a graph correction tied to it, the successor uses native prehooks to separate preceding residual, embedding skip, attention17 and MLP16 bias. A fifth bucket retains floating-point closure error, including donor arithmetic. Holding recipient carry fixed preserves the existing donor-intervention meaning.

The next prediction is that the preceding residual, rather than attention17, supplies the largest graph-minus-baseline carry mean. The competing outcome would direct folding toward attention17. Neither outcome alone identifies a semantic circuit. Frozen programs, data and donors are unchanged; this remains opened-data diagnosis.

[Results](NATIVE_PATH_ERROR_V1.json) · [Mean/energy accounting](NATIVE_PATH_ERROR_AUDIT_V1.json) · [Named-source plan](NAMED_CARRY_ERROR_PLAN_V1.json).
