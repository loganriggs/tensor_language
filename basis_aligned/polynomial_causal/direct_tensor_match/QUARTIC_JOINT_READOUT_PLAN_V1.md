**Matched-cost joint value and local derivative fitting.**

Freeze the learned32x4 dictionary, all528pair products, and use calibration panel0values on2048rows. Compute full native and feature Jacobians at the16previously specified calibration anchors, weighted by centered_floor01. Target and student are the same pure quartic functions used in preceding audits. Fit output weights exactly for relative squared value loss +lambda*relative squared derivative loss +the prior1e-6RMS-feature ridge. Each term has its own teacher-energy denominator. Only calibration rows enter either fit statistic.

Lambda={0,.01,.1,1,10}, primarylambda1 fixedbeforeoutcomes. Lambda0must replay existing empirical readout. No second-panel selection or newly chosen primary. Report both opened-panel value errors and both16-anchor derivative errors. Primary predictions: integritynormalresidual<1e-8/export<1e-4/lambda0valueerrorswithin1e-5absolute; primaryvalueerror<=.8*.1504982867onpanel1 andcal<=.05; primaryderivativeerror<=.8*.3560211862onpanel1. Record primary misses even if a secondary arm does better.

Five structural solver controls match independently stacked least-squares systems and value-only ridge, including cancellation. Programprice remains656products903168coefficients. This is output-only fitting, so second-panel value floor7.58%andfixed-reader derivative floor9.64%still apply; achieving the lower bounds is not guaranteed. No arbitrarygraphsearch, semanticidentification orfullnormalized-modelclaim. A positive joint fit would justify a subsequent frozen fresh/intervention comparison, not immediateadoption.
