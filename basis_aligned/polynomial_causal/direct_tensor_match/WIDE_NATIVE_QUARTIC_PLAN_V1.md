21 September2026. Compare widths4and32, four bilinear products per quadratic feature, all unordered root products. Exact reduced-output native target is the saved true purequartic MLP16->MLP17->unembedding path, not the archived low-rank approximation. Fit original2048calibration rows; report second cached2048-row panel separately, never choose checkpoints by it. This is data-informed empirical functional fitting, not coefficient Frobenius matching or new OOD validation.

Optimizer selected from the five-family300-step Adam/Muon controls by count below1%then aggregate fresh-probe error: Adam. Short random-start recovery was weak; longer3000-step controls and perturbed-teacher controls are recorded before native execution. Two native starts per width: inherited first four quadratic features plus random additions, and fully random. One100-step pilot atlr .03*sqrt(4/1152) (about.001768), cosine schedule. This preserves the toy step relative to coordinate scale when unit-norm factors move from dimension4to1152; exact conditional output ridge solves on RMS-normalized root features. Ridge=n*1e-6, included in the objective and envelope-gradient checks. Best training objective only. No convergence claim from100steps.

Literal representation price: width4uses26variable products and48,384coefficients; width32uses656products and903,168coefficients. Original unembedding is a shared external operation. Export normalized input directions and a physical residual writer (QR inverse and teacher_scale folded into writer), so no extra QR/frame matrix is hidden in the learned program. Normalization, residual cross terms and biases remain outside this purequartic target. No circuit adoption from this fit alone.

pred_a_integrity: cached true-target QR replay<1e-4 and FP32export replay<1e-4, finite losses. pred_b_capacity: bestwidth32second-panel error<=.8bestwidth4second-panel error (descriptive preregistered comparison; no native-outcome program selection). pred_c_target: at least one width32arm has both calibration and second-panel errors<5%. Keep all arms and failures. The1%capacity bound motivated a broad enough dictionary, but does not promise1%achievement or interpretable features. Native behavior/intervention evaluation remains required if a candidate earns further work.

Completed control outcomes: short Adam1/10 versus Muon0/10; longer Adam6/10, four of five families have a passing start (shared-output best1.071%stillfails). All five perturbed-teacher controls<.06%. Numerical solver/gradient controls pass. These qualify the numerical instrument, not global convergence or native feature recovery.
