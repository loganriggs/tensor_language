Longer native CP512 comparison, registered after V1 terminal result. V1 integrity/learning pass, comparative fidelity fails: Gaussian99.92%, coefficient99.98–100%, text99.91–99.96%. Random start captures almost nothing despite ~17000x explained-score gain; omitted teacher constant prevents calling this an error reduction. Runtime19.74s, peak1.50GB.

V2 changes only schedule duration25→400steps, same Muonrate, rank512, seeds1001/1002, random initialization, profiledridge, target and evaluation panels. Cosine is stretched over the new duration. Compare with V1 and old shared hierarchy; it does not isolate pure iteration count at an identical prefix schedule. Same original pred_a/b/c thresholds retained. No tuning on opened evaluation metrics. One native400step fit is still not a convergence proof. Physical saved coefficient export now checked directly, replacing V1 normalized-coefficient check; V1 separate physical audit passes. CPU rank512 dryrun required before managed enqueue.
