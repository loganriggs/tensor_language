Constant-cost learned support in the shared quadratic hierarchy

Use the two archived144quadratic-feature banks1101/1102; each quadratic has4bilinear products. Freeze those directions and the16original output readers. Start with the512rootproducts from the seeded graph. Add a fixed candidate pool of256previouslyunused off-diagonal pairs, uniform without replacement withseed11700. This pool is target-independent. Protect all144diagonal products, so all144quadratics remain used and the final graph always has1088products,1353728floatcoefficients,1024indices. No savings are claimed until computations are actually removed.

At each of up to16edits, choose the best one-for-one replacement within the768-pair pool under exact symmetrized coefficient matching plusridge1e-6. Refit all16readouts after each accepted edit. Directions and candidatepool remain fixed; this is learned interaction support, not arbitrary-DAG global optimization or adaptive feature width. The function stays homogeneous degree4. Endpoint/native target remains the pure MLP16→MLP17 quartic projected onto16data-informed readouts; normalization and otherbranches remain outside.

For current regularized Gram K and candidate crossvector g, let v=K^-1g, d=g_tt+ridge-g^Tv, and r=X_t-Cg. Adding t gains ||r||²/d in capturedscore. After adding, existingreadouts are C_i-(r/d)v_i, and the augmented inverse diagonal is (K^-1)_ii+v_i²/d. Removing existing i loses the squared readout norm divided by that inverse diagonal. Thus every candidate/add-and-remove gain is exact for the profiled objective. Accept only positivegain and verify by direct refit. This is a concrete constant-size graph edit, with shared products charged once.

Pred_a_integrity: initial archivevalue replay<1e-4, normal equation residual<1e-8, each Schur/directgain discrepancy<1e-8 on declared normalized scale, physicalFP32 export<1e-4. Pred_b_learning: bothstarts gain>=10%capturedregularizedscore and sampledcoefficienterror<=initial. This is not10%relative tensorerror. Pred_c_component: bothstarts root1same-tokenresponse and sensitivityweightederror<=10%. Poor initialfidelity does not relax those componenttargets. Report text/Gaussian errors separately even when objective improves.

Independent controls: five planted polynomial dictionaries (including redundant/squared/cancelling features), exhaustive directsolve ranking of all swaps agrees<=1.2e-14; accepted pathscore ismonotone and protectedslots remain. CPUpreflight exercises actual144x4/768-pair Gram,512selected readout, one edit and complete metricshapes. Nativecross computedin64-pair batches to boundmemory; no autograd. GPU managedqueueonly.

Evaluation uses the same4096coefficientqueriesseed951,1024Gaussianpointsseed939,2048openedtextstates and30directed/20independent root1pairs. These are diagnostics, not untouchedOOD. Preserve the positive mixed-CP finite-removal baseline at highercost. If this coefficient-only graph edit improves its objective without improving conditional/native behavior, that is evidence that topologysearch needs a different fitting metric as well as a broader graph grammar. Do not claim sparsity alone identifies meanings.
