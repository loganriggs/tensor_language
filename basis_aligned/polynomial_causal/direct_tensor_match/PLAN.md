# Direct random-student tensor matching — user-directed restart

The user explicitly redirects from head/branch omission screens to optimization from weights. Implement teacher-versus-random-student loss, validate on five planted structures, compare Adam/Muon, then sweep native folded quadratic and quartic targets. This takes precedence over the hourly track's earlier head-screen handoff. No more causal omission follow-ups until this requested work is complete.

Primary source read: https://arxiv.org/pdf/2605.15183 (sections2.2–2.3, appendixA.1–A.3); code repository https://github.com/tdooms/tensor-similarity. Use squared difference from weight inner products rather than cosine alone, which ignores scale. Exact Gaussian moments and symmetrized coefficient Frobenius are distinct objectives. AppendixA.1 restricts the efficient deep tree recursion's permutation group; small quartic tests therefore validate against canonical repeated-input polynomials, not merely local symmetrization. This mapping is our implementation choice, not a claim the paper solves constrained discovery.

Five targets: coordinate-sparse quadratic; rank3bilinearCP; sharedinput3/output2 sparseTucker; independentbranch rank2quartic tree; shared3feature quarticDAG withq0reused at three rootedges. All d6,o3, degree2or4. Students randomly initialized, no teacher factors used for initialization. Structural widths matched and doubled; widevariant L1penalty1e-4. Adam and PyTorchMuon (zero weightdecay, match_rms_adamw adjustment), learning rates.01/.05, seeds0/1/2,1200steps with cosine decay.120fits. Store allcurves/failures, recovery fraction, error/norm/cosine, walltime, costs. Select optimizer settings fromtoys only. No sampledinputs train; independent shiftedinput checks are validation.

Loss: (c_student-c_teacher)^T M(c_student-c_teacher)/(c_teacher^T M c_teacher). M exactlycomputed from Gaussian moments; Frobenius diagonal weights account for monomialmultiplicities. Unit tests compare fullsymmetrization, exactGaussianquadrature, gradcheck and repeated-input cancellation. Known teacher is an exactrepresentability control; optimization failure is not a ranklowerbound. Thresholdedweights onlydiagnostic; deployable sparsity requires explicit zeroing/refitting and price metadata.

Native next: existing folded two-MLP quartic artifacts (fouroutputs,fiveinputs), independently optimize tree/sharedDAG/rank/sparsity and multiple restarts with frozen toy-selected optimizer; compare canonical280 and historicalHT. Also run direct implicit inner-product matching on full lastMLP jointtensor C=UD,A=LE,B=RE after exactambientQR, keeping input/output ports andweightmetric explicit. Normalization/bias/readout remain outside polynomialmatching and must be preserved for later behavioralvalidation. No wholemodel/circuitclaim from tensorloss alone.
