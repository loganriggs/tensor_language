**Matched inherited feature learning with/without finite responses.**

Frozen dictionaries: WIDE_NATIVE_QUARTIC_4_INHERITED_LONG_V1 and QUARTIC_JOINT_READOUT_LAMBDA_1_V1 (width32). Use their input factors, but solve both new readouts under the same profiled ridge convention. Width4secondary; width32primary. Response weights0and1,300Adamsteps, rate .03sqrt(4/1152), cosine decay to1%, fixed train-objective checkpoint selection. No new random seed selection. Response-aware objective is relative squared value error plus relative squared finite-response error, with RMS-scaled-feature ridge1e-6. Value-only control has zero response weight under identical design convention.

Use existing two2048-row NATIVE_QUARTIC_COVARIANCE_V1 panels. Donor indices are cyclic roll997 INSIDE each panel; fitting and evaluation pairs never cross panels. Delta=.5(donor-x). Teacher is true purequartic MLP16→MLP17 numerator in unembedding QR coordinates, divided by stored teacher_scale. Recompute values for this exact frame and report replay to cached targets. Train on panel0 only, evaluate panel1 only; both panels have been opened historically, so this is not untouched final OOD. Inherited dictionary learned panel0 previously. Counts remain26/48384 and656/903168; exports physical residual writers, no new compression claimed.

Pred_a_integrity: toy warm controls allpass forAdam; cachedtarget replay<1e-4, FP32export replay<1e-4, finite objectives. Pred_b_response: primaryweight1 panel1response error<=.8matchedweight0. Pred_c_values: primaryweight1 panel1value error<=1.1matchedweight0. Report fitting/evaluation values and responses, notjustscores. Null: response-aware features improve fitting but fail transfer or trade away values. No claim about native-logit intervention until installing exports in the prior fixed screen. No stage2pair sharing changes during this comparison.

Toy pilot: fivefamilies, two matched randomstarts/optimizer, plusnearplantedstarts;300steps. WarmAdam/Muon5/5; randomAdam1/10,Muon0/10 below1%bothvalue/response. SelectedAdam onlywithinthispilot; optimization remains hard. The new finite-difference/gradient controls and existing profiled envelope-gradient controls must pass before execution.
