# Exact paired-Gaussian response objective for frozen quartic dictionaries

22 September 2026, 03:22 UTC. Return to the native pure quartic MLP16→17 target and frozen CP512 dictionaries (seeds1001/1002). This changes the fitting metric, not the topology. Prior finite-path audit showed derivative metrics can misrank actual finite text changes; the proposed Gaussian response metric must therefore pass those actual response checks, not substitute for them.

Let X=mu+SZ, Y=mu+S(rho Z+sqrt(1-rho²)Z'), with independent standard Gaussians. For residual R, optimize E||R(X)-R(Y)||²/[2(1-rho)]. Decomposing in Gaussian Hermite degrees gives weights w_k=(1-rho^k)/(1-rho), k1..4, with constant weight0. At rho0 this is variance matching; rho1 denotes the exact limit w_k=k, a whitened derivative objective. Value-matching uses weights1 at all degrees0..4. No sampled fitting probes or actual matched text pairs enter the objective; mu,S come from the existing calibration cache. This is weights-first with data-informed geometry, not entirely data-free.

Implement degree-resolved CP Gram by counting cross-pair contractions in Wick partitions, and native cross by existing integration-by-parts terms grouped by derivative degree. Keep existing helpers immutable. Five small teacher/dictionary families must agree with independent paired 5-point Gauss-Hermite quadrature at rho0,.5,.9 and with direct derivative quadrature at rho1; require Gram/cross/gradient discrepancies<1e-9 and constant invisibility. Native readout normal residual<1e-8. Fit all16outputs independently; output values and means are not protected by a difference-only loss.

Native screen: value baseline and rho0,.5,.9,1, fixed ridge1e-6 after the stated loss normalization; two frozen factor banks. Primary rho.5 must reduce small-output finite-response RMS error by>=15% versus the new same-bank value-only refit on the original opened panel, for both seeds, while pooled value error increases<=10%. Score larger opened256-document panel and2494matched pairs descriptively; no untouched/OOD claim. Report mean drift, alloutputvalues/responses, criterion misses, unchanged1536products/2385920floats. No export unless meaningful response/retention passes. No native causal adoption from these caches.

Primary mathematical source: O'Donnell, Analysis of Boolean Functions, Gaussian-space/noise-operator section, https://www.cs.cmu.edu/~odonnell/papers/Analysis-of-Boolean-Functions-by-Ryan-ODonnell.pdf . Gaussian noise multiplies degree-k Hermite components by rho^k. Our degree<=4 implicit contractions and native experiment are an application, not a new theorem. This bounded lookup does not reset the three-hour review clock (next04:59).
