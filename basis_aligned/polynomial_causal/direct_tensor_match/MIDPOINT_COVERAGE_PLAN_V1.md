# Midpoint output-coverage sweep — 2026-09-21 00:58 UTC

The previous four-output programs reconstruct selected native interventions well, but omit 46.54% relative calibration output-variation norm. Expand the original folded-weight target to 4, 16, 64, and 256 output directions. Select these directions using only the original calibration covariance after vocabulary centering. Keep means and normalization explicit.

Each scalar output has a bilinear matrix. Fit rank 1, 4, and 16 SVD products using either isotropic coefficient error or the separable calibration second-moment metric. This creates 24 points per domain. Capture the same original calibration and reused FineWeb/code diagnostic panels. No held-out fitting or semantic identification claim.

Registered checks in the executable: exact teacher replay and orthogonal output-energy decomposition below 1e-9; weighted 256-output/rank4 full variation error below 0.3 on both held diagnostic domains; this candidate improves on its isotropic counterpart in both domains. Report failures unchanged.

Costs count both input projection coefficients and output writers. The rank4/256 candidate uses 1,024 scalar products, 2,359,296 input coefficients and 294,912 writer coefficients, excluding mean and upstream source computations. The full source-dependent native numerator uses two products per original 4,608 channels. Neither comparison establishes end-to-end speedup.

Export rank4 programs with 64 and 256 outputs for subsequent native joint-replacement tests. The present sweep measures pre-final-normalization functional variation, not intervention effects. The captured teacher output and exact retained output projection separate approximation error from omitted-output error.
