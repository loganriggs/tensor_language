# Executable midpoint scalar factors — 2026-09-20 23:50 UTC

Fit four scalar coordinates defined by the calibration vocabulary-centered output covariance. These describe varying output directions, not semantic concepts. For scalar reader q, its exact bilinear form in normalized midpoint n and previous residual source m is K=L^T diag(q^T C) R + R^T diag(q^T C) L. The previous projection m=Dprev p and original RMS denominator remain required upstream operations. This coordinate choice differs from the coefficient metric on independent (n,p); report the distinction.

Compare isotropic rank truncation of K with separable second-moment-weighted rank truncation, ranks1/4/16/64 per scalar. Weighted optimum factorizes Sn K Sm where Sn²=E[nn^T]+ridgeI and similarly m, then undoes both roots. Ridge=1e-6 times mean moment eigenvalue. Second moments are calibration-only, noncentral. The separable metric is exact for independent draws from the two marginal input distributions; native paired inputs are dependent, so test directly rather than assuming equivalence. Both approximate the same exact native scalar, minus its fixed calibration mean; no fitted intercept repair.

Predictions: a) exact K target replay<1e-10 and full-rank SVD matrix replay<1e-8; toy<1e-12. b) weighted rank16 scalar error relative to fixed-mean baseline<0.5 for all four scalars on FineWeb and code. c) weighted rank16 aggregate squared error at least20% lower than isotropic rank16 on both held panels. Null: output subspace simplicity does not make scalar forms cheaply approximable or separable moment weighting fails on joint input dependence.

Original calibration32context64; reused held FineWeb32/code16context256;80forwards. Price four independent scalar forms,4*r products,2*1152*4*r input coefficients plus four fixed scalar means and output interface. No cross-scalar sharing yet; this is the block-term baseline a shared DAG should beat. Exact output basis and native upstream computation still priced separately. Save factor programs and summary, not full activations. Native intervention validation remains pending.
