**Frozen root features: condition transfer and token-identity control**

CPU descriptive screen, registered before computing scores. Uses the frozen empirical384 program on its 6144 calibration states and the existing 2048-state second panel. No new optimization, GPU capture, or untouched OOD claim. Inventory suggested newline and incomplete-UTF8 contexts; these are post-selection hypotheses, not preregistered discoveries.

Define current-token newline by presence of byte 0x0a. Define pending UTF8 by the strict incremental decoder state after the full token prefix; invalid-prefix cases are flagged separately. For each condition, choose one signed scalar root feature by maximum calibration AUC; retain all16 scores. Measure evaluation AUC with that fixed index/sign. Also predict the selected feature using only current-token calibration means (unseen IDs use global mean); report R2 and seen-token coverage. AUC tests correlation with explicit byte conditions, not selective manipulation.

Predictions: (a) decomposed root/readout replay <1e-12 on both panels; (b) both selected features have evaluation AUC>=.90 with at least20 positives and20 negatives, otherwise insufficient-support or fail; (c) at least one selected feature has token-mean evaluation R2<.5, suggesting substantial variation beyond a token lookup. R2 can be negative. This third bar is a diagnostic and cannot establish contextual causality. The null is token-boundary correlation without stable semantic or causal units. Price unchanged384 products/322048 coefficients/768 indices. Use cached states only, CPUfloat64.
