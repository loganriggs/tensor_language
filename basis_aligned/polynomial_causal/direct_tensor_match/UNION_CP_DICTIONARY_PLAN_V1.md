# Do existing quartic dictionaries contain complementary missing computations?

22 September2026,02:19UTC. Two frozen CP512 banks (seeds1001/1002) already compute different quartic products. Combine them into1024products-atoms, solve only outputcoordinates4–15 from exact native Gaussian cross moments, and compare with BOTH individual banks refitted under exactly the same objective. Preserve dominantcoordinates0–3 from the seed1001parent. No factor learning or textlabel fitting.

This is a structural capacity screen, not a compression claim: union costs3072variableproducts versus1536parent and adds the second bank's four1152-dimensional projections. If complementarity exists, it motivates selecting/refactoring these candidate computations. Simple averaging of parent predictions previously failed; a union with native-refitted independent readouts has a larger hypothesis class and is a different test.

Objective E||F-small-C phi||² under existing calibrationGaussian, absolute ridge1e-6 as existing profiles. Keep ridgefixed and report normal-equation residuals; not an unregularized lower bound. Compare single-bank Gaussian refits to distinguish objective change from added features. Primary screen: union small-feature value AND existing30-pair all-small-output response RMS <=.85 of better single-bank score on original opened2048states. Secondaryopened256documents/2494pairs descriptive. Report originalmixedparents too. All costs and failures retained; no candidate selection/freshvalidation/semantic claim.

Five toy structures compare independentGaussianquadrature versus analyticGram/cross and union solve: complementary monomials, shared factors, repeated powers, signed cancellation and duplicate banks. Duplicate basis can change ridgegeometry, so it is explicitly a control rather than evidence of new computations. Native moment contractions use established helpers on CPU; no queuedhelpers modified. Passing a value-only or parent-only metric cannot promote a circuit.
