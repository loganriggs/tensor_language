# Full last-MLP source-block census — 2026-09-20 23:27 UTC

Return from fixed quartic readout sweeps to the full last-MLP joint bilinear map. At native MLP17 input h, define m=lambda17[0]*(MLP16output-Down_bias16), a=attention17output, r=h-m-a. Thus previousMLPbias is in r and each actual source enters once. Keep shared native s(h), finalMLPbias and finalRMSNorm/softcap explicit. The six normalized polynomial blocks are rr,rm,ra,mm,ma,aa. mm is the previously studied pure quartic branch; m/a are intermediate coordinates, not independent primitive inputs of the full normalized model.

48nativeforwards on reused SELECTIVE_CONFIRMATION32FineWeb/16code, context256. Exact source expansion with shared L/R/D. Compute6x6cross-energy Gram in exact reduced unembedding coordinates (QR of native U). Component energies are not additive. Test native removal of each polynomial block, all terms touching m (mm+rm+ma), all touching a (aa+ra+ma), and all polynomial terms. These keep h, its normalization denominator, attention andresidual background fixed; they are NOT full upstream source ablations. Preserve Down_bias17.

pred_a_replay: sum of sixblocks vs nativeMLP polynomial<1e-4, reconstructed finalstate<1e-4, mm vs oldpurebranch/s(h)^2<1e-4; toyexact<1e-12. pred_b_cross: at leastone m crossblock(rm orma) centerednative-logit effectnorm>.5times mm on BOTHdomains. pred_c_cancel: norm(mm+rm+ma)<.75sqrt(norm(mm)^2+norm(rm)^2+norm(ma)^2) in reducedunembedding metric on BOTHdomains. Both couldfail; report every signedGram entry andall CE/effectmetrics. No decomposedstudentfit orsemantic/circuitadoption claim. The purpose is to select a structurally meaningful native target and detect source-interaction cancellation that isolated selfterms omit.
