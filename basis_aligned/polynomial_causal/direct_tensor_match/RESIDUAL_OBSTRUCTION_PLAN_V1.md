# Residual obstruction diagnostic — 2026-09-20 17:02 UTC

Previous turn made progress:46 planted fits plus a native job queued and durable report. The overlap grid reverses the apparent benefit of residual initialization. Investigate three cells at amplitude ratio0.5 and linear overlap0/.5/.95. Reproduce two600step stages. Compute the exact residual output Gram and its rank-one output relaxation: any single CP atom has one output direction, so sqrt((trace-largest eigenvalue)/trace) is a rigorous lower bound for fitting this residual with one atom. This bound is a relaxation, not a proof of attainable CP error.

Compare final joint refinement for300 versus1500 steps,1500 steps after0.1 isotropic factor perturbation (renormalized), and300 steps from teacher factors plus0.1 noise as an oracle basin control. Adam0.05, same teacher and stage seeds as prior grid. The longer controls have larger budgets; do not present them as equal-budget victories. Hypothesis: first greedy atom mixes overlapping components; the second-stage residual is not itself rank one, and final joint refinement may be trapped. Preserve both loss and atom correlations. Teacher initialization diagnoses a basin only, not an available native algorithm.
