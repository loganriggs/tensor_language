# Signed writer basin control — 2026-09-20 17:28 UTC

120fit sweep found rank4-balanced width2 and full-balanced width4 allfitbadly despite constructive exact capacity. Their best analytic writers are negative and quadratic alignment nearzero. Coefficient inner products between nonnegative squares can be negative; e.g. (x²-y²)² and(2xy)² have negative Frobenius coefficient inner product. Variable projection squares that cross term and can prefer a negative-writer basin. Gaussian function inner products between these nonnegative functions are nonnegative.

Validate exact Gaussian squared-quadratic cross moment against independent quadrature/gradients. Compare three controls on those two families, rates0.01/0.05, seeds0/1,600steps: fixed +1 writer, learned softplus-positive writer initially1, Gaussian analytic writer. Same U,V starts as prior sweep. Keep best own training objective and evaluate both Frobenius/Gaussian errors and recovered quadratic alignment. Fixed output amplitude is appropriate only because this teacher is normalized and its square coefficient is known1. Do not transfer a positive-output constraint to arbitrary signed native vector readouts.24fits; compare against prior signed analytic baselines, not an invented capacity bound.
