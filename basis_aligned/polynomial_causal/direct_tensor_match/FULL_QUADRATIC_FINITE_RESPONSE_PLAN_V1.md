21 September2026,18:12UTC. Historical2048states only, full MLP16 raw source and full MLP17 polynomial. Let h'=h+roll(source,257)-source, x0=RMS(h), x1=RMS(h'), mu=historical mean x0. Use midpoint=(x0+x1)/2-mu and step=x1-x0 in the exact response feature matrix. Centering is required by the exported mean/tangent affine repair. Final normalization/softcap remain explicit native evaluation operations, not included in this fitting loss.

Objective: squared normalized covariance coefficient error + .1 squared normalized isotropic coefficient error + squared normalized finite-response error. Same inherited directions as earlier mixed fit; exact conditional output weights. Arm finite_fixed performs one output solve with readers frozen. Arm finite_learned uses100Adamsteps,lr.05cosine schedule, best training loss checkpoint only. Both export3686products14067072coefficients incl affine correction. Hold fixed existing covariance parameterization. No restarts/hyperparameter search in this discriminating pilot.

pred_a_finite: finite losses and FP32export discrepancy<1e-4 for both. pred_b_guard: both covariance error<=.12282 and isotropic error<=.4023864. pred_c_learning: learned best loss<=.95fixed loss. Five exact value/gradient controls already pass<1e-12. The current states may share documents and donor pairs are arbitrary, not same-token/answer-preserving causal controls. This fitting constraint is data-informed and does not imply semantic circuits. Evaluate both frozen arms under existing opened48document full-path, isolated-mode and natural-behavior checks; no new OOD claim for that reused panel.
