# Localize the coordinate1 native-removal failure

22 September2026,02:43UTC. Full/lean conditional programs failed native removal. The separate queued residual learner writes onlycoordinates4–15 and cannot repair this coordinate1failure. Before designing a coordinate1repair, distinguish inaccurate scalar prediction from downstream geometric reweighting.

Freeze both CPparents and both full/lean rank256programs. Reuse the opened16FineWeb/16stdlibprefixes, positions16–254, fractions.25/1, newline/otherstrata. At each state measure relative squared error/referenceenergy at five successive interfaces: raw native scalar; scalar after originalMLP17normalization denominator; residual after finalRMS; unembedded logits beforesoftcap; finalsoftcappedlogits. These are different metrics, not additive error components. Preserve originalbackgroundanddenominator. Cachednative x, finalresidual, denominator and scalar allow further CPUanalysis without another modelcapture.

Registered predictions:
- Instrument: stage5errors reproduce existing frozenparent/full/lean removalreceipts within1e-4 absolute; zeroedit exactlyzero; native branch replay<1e-5; all finite.
- Scalar-origin hypothesis: at least4of5 lean absolute-failure cells already have rawscalarrelativeerror>10%. If false, scalarprediction alone does not accountfor thresholdfailures underits ownnorm.
- Mild-geometry hypothesis: in every cell final-effecterror/rawscalarerror lies within[.5,2]. If false, identify which interface changes weighting materially. This ratio is descriptive, not a causal attribution proof.

Return allcells/perdocument and state-level scalarerrors, norms andsensitivity; do not fitcoefficients or selectnewcandidate. Domains and panels are alreadyopened. Cache is not newtraining or validationdata; future use must nameits provenance. Native run managedonly, no queuedhelperschanged.
