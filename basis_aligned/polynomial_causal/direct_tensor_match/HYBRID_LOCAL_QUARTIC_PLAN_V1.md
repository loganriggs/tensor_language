# Hybrid weight and native-sensitivity moments for new quartic computations

22 September 2026, 06:07 UTC. Registered before fitting. The completed Gaussian-only local learner made modest gains; existing purely empirical sensitivity feature learning overfit. This experiment combines those constraints, rather than repeating either alone or tuning frozen readouts.

Target remains native pure MLP16→MLP17 quartic minus frozen CP1001, outputs4–15, full1152 input, eight new quartic atoms per output. No capacity change, no new semantic labels. Coefficients are profiled jointly with learned input directions. The full third-order and broader circuit goals remain unachieved.

For each output, combine the exact existing Gaussian Gram/cross terms with empirical counterparts on6144 calibration states, weighted by the existing per-state/per-output native squared logit sensitivity in SENSITIVE_ROOT_CALIBRATION_V2. Normalize each sensitivity column to mean1; freeze it at the original model background. Use equal mixture eta=.5, existing output weights and ridge1e-6. Finite sensitivity is a tangent metric, not exact finite-removal fidelity. Empirical moments use native weight-generated targets and real states; this is a data-informed hybrid, not data-free weight matching or a raw covariance-only objective. The Gaussian half remains a global constraint, not a guarantee outside either measure.

Native planned arms: Adam.1, seeds25001/25002,250updates, same raw random initialization as Gaussian baseline. Select by hybrid objective only. Price remains96 atoms/288 added products/442464 added coefficients. Report calibration scores, original2048states, larger16384openedstates/2494responses, and Gaussian profiled scores; preserve all-start failures. Primary prediction: both starts improve larger-panel equal-small-output value AND response RMS by at least15% relative to their matched Gaussian-Adam exports. Secondary: on exact Gaussian objective the hybrid correction retains at least90% of matched Gaussian-Adam's unregularized explained residual energy (compute both through common contractions, teacher constant cancels). Null: sensitivity moments overfit or trade away weight fidelity without repairing components.

Controls before native execution: eta0 reproduces existing Gaussian objective and gradients; independent weighted-design formula reproduces eta1; differentiated solve vs envelope gradient at eta.5 agree; five distinct planted structures (generic, paired squares, shared quadratic factors, fourth powers, cross-output shared quadratic), two starts where fitting is used. Verify finite data weights and cache alignment; no evaluation labels enter fitting. Native implementation follows after controls. This plan does not authorize semantic/circuit adoption from improved reconstruction.
