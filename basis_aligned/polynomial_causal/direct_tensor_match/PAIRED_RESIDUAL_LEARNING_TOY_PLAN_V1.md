# Can a response objective learn missing quartic products?

22 September2026,03:36UTC. Fixed-bank Gaussian response readouts failed. Test feature learning on the same five planted output-local residual structures used to prepare the queued native value-based learner: generic, paired squares, squared quadratics, shared quadratic factors, fourth powers. Each target has exactly two missing quartic products per output, two outputs, four input dimensions; keep the known parent fixed.

Forty fits: five families x Adam/Muon x two starts x value/paired-response objective(rho=.5).250updates, constant ratesAdam.1/Muon.01 frozen from the earlier value-objective500step toy sweep. These rates were chosen under value loss, so this is not an unbiased optimizer ranking or fully tuned comparison for response loss. Same initial physical functions for each objective/optimizer within seed. Profile each output's two readout coefficients with ridge1e-6; normalize input factor rows. Choose checkpoints only by their fitting objective.

Report exact population Gaussian value error, correlated-Gaussian response error, and full symmetric coefficient error for every fit, plus known-structure witness checks. Primary feasibility prediction: for each optimizer, at least8/10 response-trained fits reach<5%response error. Comparative prediction: response training improves mean response error by>=10% versus same-optimizer value training. Preserve both failures and wins. No native promotion/parameter choice from this toy alone.

A separate correlated-moment recurrence must pass rectangular Gram and parameter-gradient comparisons against independent paired5-pointGauss-Hermite quadrature for five families and rho0/.5/1 (<1e-9). Reuse immutable earlier kernels only by import; do not modify queued native learner. This is an executable follow-up to the native metric failure, not another readout-only sweep.

03:40 red-team amendment after original40fits completed: paired_squares and squared_quadratics differ only by input-slot permutation at the polynomial-family level. Preserve the original40fit predictions and label them five configurations, four distinct structural classes. Add a descriptive8fit supplement with one quadratic factor shared across both output branches; seed target from genericfamily0, tie its firsttwo readers across allfour residualatoms. This supplies a fifth structural class. Verify exact shared-factor replay on13artificial inputs. Do not replace original predictions or select rates using this supplement.
