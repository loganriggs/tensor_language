# Exact native hierarchical writer refit — 2026-09-20 17:40 UTC

Use the eight retained root features from NATIVE_HIERARCHICAL_ROOT_V1. Form their16 dense quadratic matrices from the shared bank; normalize each matrix by Frobenius norm. Stream all4608 native teacher root pairs in blocks of16 and compute exact symmetric quartic feature Gram entries via quadratic_product_gram. Multiply by teacher writers to obtain the full weight-derived cross term, then solve the8x8 student Gram system in float64. Teacher norm remains the existing fixed estimate for normalized error labels; the optimizer's self/cross contractions are exact algebra, without coefficient sampling.

Store raw-to-normalized feature scales and compare the old coefficient-query fitted writer against the exact optimum on the same feature space. Validate a16teacher-by8student kernel block in float64 against float32 and record relative discrepancy; use float32 dense contractions with float64 accumulation/solve for the full scan. Report scan runtime and objective improvement before comparing heldout8192coefficient queries and256Gaussian vectors, seed1651. No new feature selection or input-factor optimization. Fullbank price unchanged. Predictions: kernelprecision<1e-4 and normalresidual<1e-10; exact objective no worse thanold writer; heldoutcoefficient error improves by≥1e-4. Failure ofthelast doesnotcontradict the exact objective guarantee. No fullteacher-norm certificate, covariance or circuitclaim.
