# Does input norm variation explain the edit-error transfer gap?

22 September 2026, 04:10 UTC. Frozen quadratic-sharing candidates, no fitting or new text evaluation. Native cached inputs are RMS-normalized; first measure their norm distribution rather than assume an exact sphere.

Draw four independent batches of 8,192 probes from the existing calibration mean/covariance Gaussian. Evaluate parent and edited quartic outputs, then evaluate the radially normalized probes using exact degree-four homogeneity. Use radius equal to the calibration RMS norm, without labels. Compare relative edit RMS against the existing exact Gaussian calculation and opened text panel, for both seeds and 64/128 edits. Report each batch as a Monte Carlo variability check.

A normalized/raw error ratio near one would weaken the hypothesis that radial variation explains the observed 2.7–3.8 fold transfer gap. A substantial increase toward text error would support a partial explanation, not make normalized Gaussian probes equivalent to text or validate interventions. Normalization changes the Gaussian mean and covariance too; it is a measure diagnostic, not an isolated causal perturbation of norm. Directly replay normalized-probe predictions against homogeneity on a small subset. No queued helper edits.
