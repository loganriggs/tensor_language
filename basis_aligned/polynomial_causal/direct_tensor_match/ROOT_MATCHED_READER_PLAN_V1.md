**Same-token context changes: does a root feature predict the native quartic reader?**

Frozen empirical384 graph and preselected feature1 (newline association). Use original2048-state calibration and2048-state second panels. Select current token IDs whose bytes contain newline; pair each recipient with a different-prefix donor of the identical token ID, selecting minimum positional distance then lowest flattened index. No feature/sign refit. This is a conditional pure-quartic readout prediction screen, not root-removal causality or full-model logits.

For native vocabulary unembedding U and program writer w1, define r=U^T U w1 / ||U w1||^2. Define native scalar t(x)=r^T D17[(L17 m(x))*(R17 m(x))], m(x)=lambda17[0]D16[(L16x)*(R16x)]. Predict t(donor)-t(recipient) with h1(donor)-h1(recipient). Full-program readout differences are a diagnostic comparator; check whether the16outputwriters are orthogonal in vocabulary geometry. Same-current-token lookup predicts exactly zero change; report its error and target energy.

Predictions: (a) projected graph replay against h1 <1e-5 and independent unfurled native scalar versus folded-reader evaluation <1e-10; (b) relative scalar response error<=.10 and cosine>=.90 on second panel with >=20pairs and >=10recipient prefixes; (c) absolute signed response correlation>=.90 on both panels and predicted/true norm ratio between .8 and1.2. Report everyfailure; nearzero reference produces inconclusive ratherthanpass. Ordinarytargetvalue errors additionallyreported. CPUfloat64, no fitting, no GPU, no untouchedOOD claim. Coefficients and graph costs unchanged; scalar diagnostic folds D17 into one reader and does not establish completecircuit extraction.
