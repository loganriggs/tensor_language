# Prospective v4 fixed-query source-column transfer

Before any v4 model evaluation: freeze SOURCE_OOD_V4 row hashes, native_fixed_query_source.py, fixed_query_source_column.py and the existing opposite-v2-only RESIDUAL_ROLE_BANK_FROZEN_V1.pt. Use frozen role-bank contractions with native23-port recipient source directions and the unchanged LP. No fresh per-input derivatives or refitting. Four contexts, both roles, all nine output contrasts.48texts/96sites;16family-role cells.

Compare full native, explicit source-column replacement, and unitB reference. Report baseline native capability per distinct family/panel, prediction fidelity to full effect (10% number/5% each control, relative full number-effect L2), retention>=.8 and collateral<=.1. All cells remain; inability of frozen selector or native capability to meet its original bars is a failure, not reason to drop data. Distinguish approximation fidelity from selector selectivity. Baseline context/native prefix generation and full suffix remain charged; successful local reuse is not independent full circuit extraction. Include old-context installed replay before interpreting new effects. Implementation and call budget to be fixed before enqueue; no v4 model outcome is presently known.

Execution fixed before enqueue: two congruentv2 installed-oracle replay contexts plus four freshv4 frozen-bank contexts.18prefix48native suffix6baseline attention preparations; six zero-edit compiled replays. Old installed/full effects must replay<=1e-8; zero-edit compiled baseline<=1e-4. Source-refinement collapse<=1e-10 and gauge corrections<=1%. No new derivatives. Native capability>=90% each cell (six rows means all six); separately report full-selector and compiled-selector selectivity plus bank tangent prediction errors. Binding checked at runtime before model load. Implementation run_fixed_query_fresh_v4.py.
