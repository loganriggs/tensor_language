# Head8.2 changed-city current versus inherited value

Retain recipient head8.2 queries and routing. At the single changed-city source,
split the donor value substitution exactly as
`(1-lambda)c_v(current) + lambda*first`: full value, donor-current only, and
donor-inherited-first only. Apply each city-source head8.2 write difference only
at the frozen framing destinations, then feed only its induced block9 current
value through the isolated head9.8 O branch and native suffix. Native plus three
arms on48 rows costs192 body forwards under180 seconds.

- `pred_a`: native and full-value target/work-jobs scores replay the preceding
  recipient-query-fixed artifact within `1e-5`; current and inherited value
  deltas sum to the full delta within `1e-5`; both source-delta norms are at
  least `1e-8`, outside framing is exact, values are finite, and exactly192
  forwards execute.
- `pred_b`: native cue is positive for at least10/12 pairs and the full-value
  target-effect RMS is at least `1e-5` in each template.
- `pred_c` (current provenance): current-only cue error versus full value is at
  most35%, while inherited-only error is at least50%, in each template.
- `pred_d` (inherited provenance): inherited-only error is at most35%, while
  current-only error is at least50%, in each template.
- `pred_e`: separately measured current and inherited cue effects compose the
  full-value cue within10% in each template.

Four unrelated control ratios are reported. This tests value provenance at the
specific changed-city -> head8.2 framing write -> head9.8-O edge. It does not
test upstream city-state generation, whole-head sufficiency, corpus OOD,
compression adoption, or quantization.

