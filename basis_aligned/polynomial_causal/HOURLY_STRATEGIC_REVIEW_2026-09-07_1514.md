# Hourly strategic review — 2026-09-07 15:14 UTC

## Circuit interpretation targets

The program must identify what information is read, what operation is performed, what is written, and which downstream readers consume it; merge cross-module pieces and split native heads/MLPs when downstream computation demands it; predict held-out and OOD effects; execute a sufficient interface or isolated circuit; support selective removals, swaps, and edits; compose shared and task-specific branches; and remain stable across corpora, gauges, and fitting restarts.  The end state remains a predictive, composable, manipulable, and literally simpler transparent tensor program.  Rank, variance, reconstruction, or cross-validation score alone is not a circuit claim.

## What changed since 14:14

Ten claim-release events landed at a 4.10-minute median gap.  Eight produced valid scientific or capability receipts; one DAS run was quarantined as invalid and its proposed zero-forward audit was abandoned once it became clear that differentiable reader calls and sealed closure had not been persisted.  Claude's v206-v209 screens expanded the one-protocol behavior inventory and used full within-family separability controls; v208 reports 14/16 separable members and moves its provisional distinct-circuit count from 21 to 24.  The longer v210 18-member family screen is currently executing and is itself circuit work rather than a queue stall.

The frozen five-MLP mask30 split transferred to different temporal/is-was constructions: behavior `.8640/.9234`, response projection `1.0807/1.0985`, and RSE `.01361/.01448`, statistically indistinguishable from full support.  Its absolute control gate failed with two flips, but matched attribution showed that mask30 and the full parent flip the identical rows; mask30 has slightly lower median KL (`.01220` versus `.01249`) and changes centered control logits by only 1.87% of the full-parent effect.  The within-MLP MLP0 deletion therefore transfers without adding collateral, while full-parent selectivity remains a dossier caveat.

The fully instrumented DAS tournament counted 24 native plus 1,208 differentiable reader evaluations and passed every atomic validity check.  KL produces stable fold axes (`|cos|=.8827`) and improves sealed-v12 mean/worst loss from `.9803/1.0095` to `.3979/.4803` within all target constraints.  It does not win both construction folds: v8-to-v10 improves `.7261→.6968`, while v10-to-v8 worsens `.3486→.4007`.  Component scores show that the reversal comes from larger margin/L15 errors on v8, not a target-limit violation.  This is positive regularization evidence with terminal `regularization_fold_heterogeneity`, not proof that optimization is worse than DIM.

The new canonical source-weight screen translates each exact rank-16 source interface through `A=Down^T Q`, fits shared-mean and task-contrast blocks from `delta_hidden A`, checks even/odd stability, and executes seven full-model patch arms.  Its first execution reached every causal arm but wrote no result because the final relative-control predicate indexed an is/was key absent from the temporal-only control authority.  The hash-bound retry now asserts the exact represented key set and keeps rows, patches, thresholds, and 48-forward ceiling unchanged.  A prospective v14 DAS family was also screened before optimizer design and honestly failed capability (A1 29/32 jointly correct, A2 19/32); it is excluded.  Text-disjoint v15 is frozen and queued as its replacement.

## Throughput and systems audit

The measured release gaps were `2.01, 4.98, 1.76, 5.10, 1.87, 9.90, 16.27, 4.10, 3.54` minutes; median `4.10`, below the ten-minute target.  The 16.27-minute interval is explained by v208's 15-member full-family GPU screen, not an empty queue.  Scientific execution dominated wall time: v208 took about 16.6 minutes, v209 about 4.2 minutes, the corrected DAS tournament 42 seconds over 1,232 reader/model evaluations, and the source-weight attempt about two minutes over all causal arms.  Authoring produced one task-weight screen, two capability authorities, a quantitative DAS red-team update, and a refreshed canonical dossier.  The avoidable terminal control-key error forces one repeated source-weight execution, but review/validation did not exceed the scientific screens.

`CIRCUIT_FOCUS: PASS` — the hour transferred an internal MLP split, attributed its collateral to the parent, expanded behavior/family circuit screens, translated a validated source interface into physical weight groups, and updated the canonical dossier.

`CEREMONY_BUDGET: PASS WITH ONE INSTRUMENT DEFECT` — shared gates and runners remained small relative to causal work, but the source-weight scorer assumed a nonexistent control task and cost one rerun.  Future relative-control predicates must quantify the keys returned by the frozen authority and assert that key set explicitly.

`NOVELTY_LESSON_GATE: PASS` — prior task-mode, weight-compiler, hidden-support, DAS, and control-family receipts were searched.  Weight participation remains only a candidate generator; causal patches and complements decide membership.  Capability failures are preserved rather than threshold-relaxed, and v12 is not reused as if sealed.

## Confound audit

- The MLP0 mask is stable across one construction shift, but the complete five-MLP parent still changes two controls.  Relative equivalence does not establish absolute selectivity.
- The source mean/contrast bases are fit on older task rows and checked by even/odd splits.  Row-half stability cannot substitute for construction-family stability; the full-model transferred patch is decisive.
- Canonical axes are individually nameable only when singular gaps and crossfit stability support them.  Degenerate blocks may be identifiable only as subspaces.
- `Down^T Q` compilation is exact and gauge-safe, but per-hidden participation ignores cancellation.  No hidden unit is admitted to the circuit by energy alone.
- The prior `.00138` interaction result licenses greedy search for the top50/full site choice.  It does not automatically prove that shared/contrast mode groups are additive; their site-level singleton effects must be measured first.
- DAS v12 is now open.  The large v12 improvement can motivate an objective but cannot validate the next one.  A capability-qualified v15 or later family must remain untouched until hyperparameters and stopping rules are frozen.
- Selecting a new family by native capability is legal pre-intervention filtering, but repeated family generation can narrow the distribution.  Every rejected capability family, including v14, remains in the ledger.

## Competing routes and ranking

1. **Finish the exact source mean/contrast causal screen.**  This changes within-module splitting, cross-task reuse, selective manipulation, and weight-level computational specification.  Kill the coordinate story if the union is not functionally equivalent to rank16, its complement remains sufficient, or even/odd blocks are unstable.
2. **If the union passes, execute shared-versus-contrast five-site factorials before greedy composition.**  This locates task-common and task-specific writes across native MLP boundaries and measures their interaction algebra.  Kill greedy pruning if higher-order interaction is material or singleton ordering fails held-out replay.
3. **Direct causal-response quotient.**  If activation-derived source modes fail, define source states by indistinguishable downstream interventional responses rather than another SVD or rank sweep.  This route dominates when operational equivalence is stable but canonical weight directions are not.
4. **Construction-adaptive DAS with inner validation and explicit effect-variance penalty.**  This changes stable subspace identification and selectivity, but remains secondary to the live circuit patches.  Kill it if a frozen moved checkpoint cannot beat DIM/no-reg on both outer families and a new capability-qualified family.
5. **More hidden-unit percentile or fixed-KL sweeps.**  Demoted: the former is compression without new semantics, and the latter repeats the measured construction heterogeneity.

The current route survives because it directly tests the user's proposed tensor translation at an already validated causal boundary.  The next decision is not whether the weight coordinates reconstruct activations; it is whether shared and contrast physical patches have stable, task-specific causal effects in the complete model.  V210 remains live, the corrected source-weight job is queued immediately behind it, and v15 capability follows without opening a DAS outcome.
