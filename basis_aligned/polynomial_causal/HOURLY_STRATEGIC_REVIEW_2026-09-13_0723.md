# Hourly research review — 13 September 2026, 07:23 UTC

Interpretation targets: explicit computation; cross-boundary grouping and within-module splitting; held-out/OOD prediction; extraction/sufficiency; selective manipulation; composition/reuse; stable identification. The full goal remains a simpler executable program satisfying prediction, extraction, removal and reuse. The user's interaction-compression direction governs this campaign.

Since06:18, setting2's sparse interaction passed conditional regional validation, but zero-operator and own-effect controls narrowed that claim. Packed storage improves while CSR resident memory and CPU execution lose to dense contraction. Every reader node remains active; generic coordinate sparsity has not produced a sparse computation graph. Learned short rotations improve a surrogate strongly but miss priced savings; a direct capped-energy objective gives only1.86% additional saving. These are useful bounded nulls, not absence-of-structure results.

Setting1 has the stronger mechanistic simplification: exact response coefficient dependence reduces four vectors to three and ten symmetric products to six. Native160-prefix outcomes are unchanged. Projection/context reuse helps repeated evaluations but can lose on short workloads; a matched simple-cache baseline was executed. Generating the evaluation background passes regional local-effect bars but misses one FineWeb criterion. No new semantic circuits were identified this hour. Historical texts and amplitude variations are not fresh language OOD validation.

Most importantly, the newest coverage audit measures the original interaction rather than only the local compressed term. Regional error remains11.9–16.1%; a diagnostic oracle identifies the inherited pre-block10 mixed contribution as the main omission. The retained suffix supplies much of the predicted interaction. Therefore a headline of under0.3% local error would overstate whole-circuit extraction.

Decision: carry the inherited two-vector response through the shared attention10/MLP10 generator at a, b and a+b. This changes the omitted computation, not optimizer settings. Its actual-weight CPU control has now run and passes at roughly2.3e-14 relative error. Next native comparison must predict the original joint-removal effect using generated branches consistently. Dense weights, pristine context and suffix remain charged.

Alternatives ranked: (1) native validation of the complete generated joint branch, rejected if original-interaction error stays large relative to the local precision baseline; (2) simplify its actually reused reader/product nodes, rejected if adapters/preparation erase savings; (3) revisit setting2 with block/node penalties rather than more scalar thresholding, rejected if useful accuracy demands every block. Another broad full-U fit is lower information until its objective/representation changes.

Confounds checked: local versus original effect, native versus generated background, signed projections versus variance shares, cancellation near zero, FP32 precision, matched caching, adapter costs and inherited interactions. The coverage oracle is explicitly diagnostic and cannot enter a candidate predictor. No data fitting or global-optimum claim was added.

CIRCUIT_FOCUS: PASS under the compression override; exact response sharing and the coverage test directly change conditional extraction/composition evidence.

CEREMONY_BUDGET: FAIL TO VERIFY exclusive wall-time shares; mixed CPU/GPU telemetry still does not justify percentages. Repair retained: reuse current helpers/receipts and use the0.77second direct algebra discriminator, with one hourly narrative rather than additional reporting machinery. GPU jobs measured37.87seconds and7.84seconds; these are execution times, not full serial research latency.

NOVELTY_LESSON_GATE: PASS WITH CORRECTION. An initially proposed background pilot duplicated prior eight-prefix work; that was caught before rerunning it, and the actual next job promoted the unchanged claim to the full160-prefix panel. Existing module and response records supplied the next algebraic simplification. Arms, controls and shared-code rewrites are not counted as separate circuits.

Primary notes: RESPONSE_PRODUCT_BASIS_V2_MATH.md, SPARSE_INTERACTION_EXECUTOR_V1_MATH.md, COMPOSED_MLP10_BACKGROUND_V1_MATH.md, COMPOSED_ORIGINAL_INTERACTION_COVERAGE_V1_MATH.md and COMPOSED_JOINT_RESPONSE_V1_MATH.md. Next mathematical review is due08:29UTC.
