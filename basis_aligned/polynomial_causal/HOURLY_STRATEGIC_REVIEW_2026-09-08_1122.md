# Hourly strategic circuit review — 2026-09-08 11:22 UTC

## Circuit targets and full objective

A useful decomposition must: specify what is read, computed, written, and consumed downstream;
group causal variables across native boundaries and split native modules when appropriate; predict
held-out and OOD effects; expose an executable circuit or explicit native-background interface;
support selective removal, swapping, and editing; compose and reuse shared subcomputations; and be
stably identified across documents, plausible gauges, and fitting restarts or by operational
equivalence. The full objective remains a smaller transparent tensor program that is jointly
predictive, composable, manipulable, and cheaper under literal storage, compute, edge, state, and
program prices. Rank, reconstruction, variance, or CE alone cannot satisfy these targets.

## What changed since 10:22

No new circuit outcome is available: the managed lane remained occupied by the pre-existing v256
family-separability run, which is verified live at its twentieth of twenty evaluations. The two
frozen circuit jobs remain first and second behind it, unchanged and unduplicated.

The waiting interval nevertheless closed the implementation branch prospectively:

1. Exact CPU analysis proved the controlled suffix recurrence
   `delta x18 = g10 delta x10`, where `g10=product(lambda_l[0], l=10,...,17)`, and the paired
   removal identity `writer x18 - g10 delta x10 = R0M1`. This is exact for clamped module writes;
   task-margin additivity is not assumed through final RMSNorm and tanh.
2. Prior-art review recovered the closest valid same-task entry-12 residual-route experiment and
   excluded the normalized-reader atlas whose differentiable-forward price gate failed. Static
   MLP tensor norms likewise remain incidence priors; any module-positive branch must retain the
   exact activation-conditioned `deltaL*R0 + L0*deltaR + deltaL*deltaR` split.
3. A shared residual-identity library and final-block raw-residual hook now implement exact add,
   remove, capture, registered-position replacement, deployed-precision state diagnostics, hook
   coverage, and cleanup with model-free tests.
4. Before the carrier outcome, a 14-forward original+OOD direct-add/direct-remove experiment was
   frozen. Its runner is fully implemented but deliberately unbound and cannot load the model or
   enter the queue unless the immutable factorial passes A/B/C. It tests direct versus dynamic
   state/logit equality, OOD prediction, directed removal, and temporal collateral without DAS,
   fitted coordinates, or rank selection.

This work improves computational specification, executable extraction, held-out/OOD prediction,
and selective manipulation. It does not yet identify a new circuit because its parent outcome is
still unavailable.

## Confound and instrument audit

- **Baseline and frame mixing:** all future effects remain native-relative within one population;
  original and OOD rows cannot be mixed or reselected.
- **Nonlinear composition:** exact hidden-state affine identities are checked separately from
  logits and margins. Any factorial margin interaction remains attributable to the final nonlinear
  readout after exact state controls, not silently assigned to the clamped recurrence.
- **Leakage/post-selection:** the direct-route populations, writer heads, source region, boundary,
  arms, bars, and price were frozen before factorial outcome. A separate result binding may record
  only the immutable parent SHA/predictions/terminal and reviewed runner SHA.
- **Dead hooks and precision:** x10, all sixteen module sites, and x18 require one call; a native
  x18 self-patch is a live no-op. Logits use a strict `1e-5` equality bar while state equality uses
  a scale-relative float32 diagnostic to avoid confusing bfloat16 reassociation with a scientific
  failure.
- **Native-boundary bias:** cumulative complete-module prefixes can miss within-module factors.
  The independent state×response factorial decides whether head/MLP splitting is warranted or the
  residual boundary is the more faithful causal unit.
- **Shared difficulty and collateral:** the compact writer is scored against its own immutable
  paired effect; temporal collateral is normalized to the temporal command-gold effect.
- **Authority risk:** the unbound runner refuses execution until factorial A/B/C pass and the
  exact parent object is bound. No threshold is outcome-conditioned.

## Alternatives and ranked next moves

1. **Consume cumulative prefixes and the residual×response factorial.** These remain highest
   information because P16 and R0M1 are intervention-equivalent across independent runners while
   the factorial separates identity carriage, module response, redundancy, and interaction. Kill
   this route if joint replay fails; choose the measured dominant branch rather than lowering bars.
2. **Identity-positive branch: bind and execute the already frozen direct add/remove runner.** It
   changes computational specification, extraction, OOD prediction, and manipulation. Kill the
   claim if either direct equality fails, OOD recovery falls below `.5`, or directed removal is
   nonselective.
3. **Response-positive branch: split only passing cumulative modules.** Attention modules proceed
   to exact nine-head patches; implicated MLPs proceed to the three-term activation-conditioned
   factorization. Require the split arms to replay the complete-module prefix and transfer without
   reselection. Kill a factor if it does not compose back to its parent.
4. **Interaction-positive branch: use the exact state cross-difference as a falsifier.** A zero
   hidden-state cross-difference with nonzero margin interaction localizes curvature to the final
   readout; then test a prospective causal-response basis under held-out interchange. Kill it if
   interaction is not stable across the original split before OOD.
5. **DAS remains demoted.** A regularized projector could fit a task direction, but it does not
   resolve the already available physical carrier question and risks task memorization. Reopen it
   only for a residual unexplained by exact physical routes, with frozen multi-environment
   feasibility and complement/noise controls.

The current route survives: it directly distinguishes causal boundaries and licenses the exact
weight/head/MLP successor needed by the result. No compression-only action has comparable circuit
information value.

## Throughput and systems audit, 10:22--11:22

Authoritative commits and logs show zero completed circuit receipts during the hour. This misses
the one-screen-per-ten-serial-minutes operating target, but the cause is measured: the only circuit
GPU lane stayed behind a healthy unrelated v256 run for the full interval, progressing from roughly
7/20 to 20/20 evaluations. No idle GPU gap, duplicate enqueue, direct launch, or restart occurred.
CPU work produced one prior-art decision, one exact algebraic route, two reusable intervention
libraries, one frozen conditional experiment, and its tested runner. Scientific derivation and
implementation dominated; focused tests/gates took seconds to minutes, Astra handoff work was a
bounded user-requested interruption, and review ceremony remained smaller than the implementation.
The immediate throughput repair is to consume the already queued small circuit jobs as soon as
v256 exits, rather than opening another bespoke atlas.

`CIRCUIT_FOCUS: PASS` — all research changes improved circuit boundary identification,
intervention machinery, OOD prediction, or selective manipulation.

`CEREMONY_BUDGET: PASS` — validation reused shared gates and remained smaller than scientific
analysis plus implementation; no new audit framework was built.

`NOVELTY_LESSON_GATE: PASS` — same-task residual precedents and invalid instruments were searched;
the state-sufficiency tautology, activation-conditioned MLP interaction, nonlinear readout, and
DAS-memorization lessons materially constrained the design.

## Concrete continuation

Both Supervisor runners are healthy. The exact cumulative-module runner `13734de1...` and
factorial `9e3c9773...` remain queued behind verified-live v256. The conditional direct runner
`bd3d230c...` is committed, tested, and unbound; bind and enqueue it only on an immutable factorial
A/B/C pass. Interpret every landing immediately and begin its evidence-selected successor.
