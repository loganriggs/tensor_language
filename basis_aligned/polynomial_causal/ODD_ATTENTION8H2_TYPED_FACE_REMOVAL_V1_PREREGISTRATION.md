# Fresh typed-face midpoint removal and matched-direction null V1

Token-only inventory before model outcomes finds four unused FineWeb source
documents (five eligible occurrences) within the frozen eight-token vocabulary,
after excluding all sixteen previous source documents. Use all four, one
SHA256-ordered occurrence each: 48 endpoint/cue rows, eight distinct token
sequences, four independent contexts. This is a limited fresh-context screen,
not new-city or structural-template OOD and not a high-power population test.

Freeze whole masks(1,4,5) face. Arms: native; original full corner5; compiled
full corner5; compiled half-face midpoint; sixteen seeded random midpoint
writes at the same head8.2 destination sites. Each null is a random 128-channel
head write projected through the same output map and scaled to the targeted
midpoint's residual norm at every destination; directions are paired with
opposite signs across city-cue swaps. Head9 odd-value descendant propagation
and full remaining native suffix are common to every arm. Seed base17092100.
960 forwards, 300 seconds, no fits or weight updates. Native program frozen
from the failed all-readout replay run; do not retroactively promote that run.

pred_a: compiled/original full write relative error <=1e-5, first-table relative
error <=1e-5, max per-destination null norm mismatch <=1e-5, outside-mask zero.
pred_b: native UK-minus-US city-pair margin difference >=0.1 in at least18/24
endpoint/context cells. On capable cells, midpoint reduces the contrast in
>=75% cells, with mean fractional attenuation >=0.02. No adaptive denominator.
pred_c: target midpoint RMS effect >=2 times median null target RMS and exceeds
at least15/16 individual null RMS values. Random directions have the same output
subspace and write norm. Null: arbitrary equal-norm changes explain the effect.
pred_d: RMS changes on each of cat/dog, red/blue, Monday/Tuesday, apple/orange
<=0.5 times target RMS. No claim about behaviors outside this battery.
pred_e: compiled full target effect matches original full effect <=0.01 relative
L2; midpoint target effect matches half compiled full effect <=0.10 relativeL2.
This newly registered causal screen tolerance is NOT a change to V1's failed
1e-4 all-readout replay gate. Report all readout errors in full.

No independent-composition or random-split gate here; those remain unpassed.
Midpoint removal is donor-pair centered and descendant preserving, not removal
of the whole head or a donor-free executable model. Report literal program,
state/port price and actual phase times, preserving failed bars.
