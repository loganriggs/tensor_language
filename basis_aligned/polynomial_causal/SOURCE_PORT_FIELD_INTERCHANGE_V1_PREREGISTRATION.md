# Content ports versus source-selection ports

The literal raw/join state-field abstraction fails. Its normalization mediation
audit reduces, but does not remove, interactions or repair raw retargeting.
Preserve those nulls. Test a distinct read interface: a physical source-state
edit changes final K1,K2,V together, so it can disturb source selection as well
as value content. Names K/V alone do not establish semantic roles; this factorial
tests their separate causal consequences without choosing the best subset.

Fresh dual-field generator seeds26909/26910. Use first8worlds/population as a fixed
screen budget, all24 entities×hops0..3:16worlds1536variants. IID24-cycle and OODthree
8-cycle, uniformly random record orders. Same prospectively specified raw
consumers (hop1/2/backwardhop3) and join consumer (forwardhop3). Distinct sigmaRaw
and sigmaJoin=sigmaRaw². No filtering by native performance or position.

For each raw,join,joint mapped state y=x+delta, recompute its live final RMS and
K1,K2,V. Test all8 source-port subsets: each port is independently native or from
y. Native query projections and residual skip remain from x at every position.
Apply the same mask to all heads; no head selection or normalizer clamp.
The fixed semantic candidate is V-only, not a selected winning factorial cell.

Also test V-only removeRaw, removeJoin, removeBoth from their literal source-state
removals, with live RMS recomputed. Including one native arm, this is28 named
arms (3×8 mapped port cells,3 V-only removals, native). Duplicate empty-port cells
are identity controls. Report every cell, not just the proposed V-only arm.

Independent exported program constructs all candidate fields and ports from
tokens/weights. Native K1/K2/V projection hooks implement the oracle, preserving
native absolute RoPE and query projections. All-port finalquery must equal the
physical source-state edit because that query state is unchanged. Binding-position
Q/residual are held fixed in port interventions, so do not equate their all-port
outputs to physical-state edits. All candidate port outputs must match their own
native port oracle at every position.

A mechanical: focused identity/live/hook/scope controls, nonempty groups, finite;
all-output port oracle max<=1e-9,relativeRMS<=1e-10(floor1e-6); all-port finalquery
versus physical source-state edit<=1e-9; restored hooks and native baseline.
B raw V interface: native accuracy>=.8, V-only raw removal goldP loss>=.25,
V-only raw mapping desiredaccuracy>=.8 and desiredP gain>=.5, separately for each
population/raw-consumer hop. On joined-consumer hop3, abs mean original-goldP
change from raw V-only map<=.10.
C join V interface: same native/removal/mapping bars for forwardhop3 separately
perpopulation; on each raw-consumer group, joined V-only mapping abs mean
original-goldP change<=.10.
D joint V interface: V-only joint state mapping desiredaccuracy>=.8 and desiredP
gain>=.5 for each population/consumer/hop group. Hop0 mean original-goldP change
<=.10 for each V-only individual/joint mapping. Quantify joint interactions but
do not impose zero: joint physical source normalization remains live.

Mechanical validity and semantic outcomes remain separate. A semantic null
rejects V-only as specified; do not select another port subset, rescale a field,
or reinterpret generic damage as retargeting. Other cells identify mediators and
may motivate a separately registered mechanism, not a posthoc adopted circuit.
All387968 constants/native background plus derived caches remain charged. No
structural parameter saving. B8FP64,1800s,<256MiB/tensor, managed GPU only.
