ACTIVE_TRACK: WEIGHT_FOLDING

# Hourly strategic review — 2026-09-20 23:03 UTC

The user's explicit two-day weights-first exploration overrides ordinary hourly alternation. The goal remains interpretable, predictive, extractable, manipulable, reusable and stably identified circuits, with literal simplicity. It is not achieved by a passing reconstruction threshold.

## Evidence and decision

Since22:01, moment-matched Gaussians failed to predict the sign of a real-data feature reconstruction change. Empirical calibration moments helped, but native code behavior remained weaker. Four executable scalar features now share10products. Exact rational analysis established a four-root lower bound only for the fixed primitive dictionary/linear-output class; approximate standalone simplification loses joint sharing.

Same-token swaps added context-dependent intervention evidence. A selective feature1 readout refit improved all16fresh codefiles at unchanged graph cost, but fresh same-token error.448/cos.895 missed the strict bars. Other scalar functions were preserved. Equal-reader, equal-norm probes then showed that27%of native spherical-gradient energy is invisible to existing readers. This is a local artificial-state counterexample, not a natural-data error floor.

New gradient-proposed quadratic products improved cached scalar prediction at higher cost, but native code swap error.451 did not improve the smaller selective program's.448. The matched random control was.459; the registered10%MSE advantage failed. The14product candidate is not promoted. Its existence does not invalidate the sphere counterexample: a useful missing direction need not be well represented by this quadratic correction or metric.

## Full interpretation requirements

- Computational specification: explicit pure quartic MLP16/17 branch and reusable scalar interface exist. Semantic constituent conditions are unidentified.
- Grouping/splitting: one cross-layer branch is factored; output projections alone do not establish a semantic grouping.
- Prediction: removals improve on disjoint documents; code same-token transfer still fails.
- Extraction: executable scalars and residual writers replay. They require an explicit native background and denominator.
- Selective manipulation: operational feature removal/swap tested; intended-vs-unrelated task effects untested.
- Composition/reuse: shared products and joint edits tested within this background; broader task/module reuse unproven.
- Stable identification: fixed canonical outputs are operational definitions; primitive dictionary identity across restarts was previously unstable.

## Confounds and next moves

1. Separate common-logit shifts from probability-relevant centered-logit effects. A prior output-frame audit found a large common-vocabulary component for feature1; final normalization/softcap mean this must be measured AFTER native operations. Compute both components exactly, retain token CE-effect metrics. This is a metric audit, not a retroactive new passing threshold.
2. If centered effects remain substantial, compare paired-context fitting objectives before expanding readers further. Existing native scalar improvements need not weight contextual differences correctly. Any such objective remains explicitly data-informed and requires controls.
3. Further derivative-directed additions require a stronger representation or clear superiority over matched capacity. More width alone is low priority after current results.

Native capability endpoints remain fixed; no arbitrary semantic labels or whole-model speedup claims. Covariance/empirical moment fitting is distinguished from purely weight-based objectives. More calibration fitting has not established the original circuit goal.

## Throughput and repair

Runner timestamps22:01–23:01 show10direct jobs, all exit0, totaling92seconds; longest16seconds. They cover mixed skip, moment test, metric blend, confirmation, extraction, swaps, decoder comparison, selective repeat/confirmation and sphere probes. CPU analyses, authoring and report time were not separately instrumented; do not report invented percentages. Fast GPU jobs do not justify repeated orchestration code.

TRACK_ALTERNATION: OVERRIDDEN by explicit user weights-first instruction.
TRACK_PROGRESS: PASS — concrete folded-branch extraction, native interventions and fixed-reader counterexample.
CEREMONY_BUDGET: UNVERIFIED — time categories unavailable; repeated small wrappers and reports remain overhead. Bounded repair executed this turn: parameterized the existing two-candidate confirmation orchestrator (candidate specifications, output prefix, scope), reused it for the current native comparison, and verified native controls. No duplicate native evaluator was added.
NOVELTY_LESSON_GATE: PASS for local sequence — Gaussian conditional-reader analysis checked before norm-constrained probes; earlier metric/native mismatch motivated matched random native control. Global registries were not comprehensively re-audited.

The next bounded implementation is an exact common/centered effect scorer, reusable in both removal and swap evaluators. Its algebraic replay and zero-common/zero-centered controls should be small; do not create another bespoke evaluator. This addresses interpretation and manipulation evidence rather than chasing a slightly lower scalar error.
