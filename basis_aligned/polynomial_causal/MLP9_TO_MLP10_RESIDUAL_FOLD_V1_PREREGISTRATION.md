# MLP9 to MLP10 residual-input fold

Registered before native execution, 13 September 2026. Reuses 96 regional and 64 FineWeb prefixes; no fitting or new OOD claim.

The previous residual-only prediction failed in two regional groups. Retain its mixed attention partner, then replace measured residual input changes with the exact fixed-writer MLP9 response, multiplied by the actual block10 residual re-entry coefficient. Native attention partners, full joint RMS denominator, additive block10 background, and suffix11–17 remain supplied.

17 arms retain the previous14 and add: joint residual/residual plus mixed residual/attention product; the same joint product with folded residual generators; and that folded product plus attention/attention. These are physical state interventions, not addition of separately measured final effects.

- pred_a: previous14 outcome replay and state partition relative errors <=1e-4, input product partition <=1e-10.
- pred_b: joint residual-plus-mixed effect versus full direct cross-product <=1% relative L2 in each of four regional groups.
- pred_c: folded residual-input aggregate error <=1% on each regional/FineWeb panel, and folded versus observed joint effect <=1% in each regional group.

Nulls: the independent-effect arithmetic approximation fails as a joint intervention; or finite-precision/native input response errors amplify through the subsequent product. Preserve failures. Report FineWeb absolute errors and per-prefix signs descriptively, with no uniform OOD claim.

Price: 2720 full native body forwards,160 extra MLP10 evaluations,160 prefixes <=248 tokens,180-second run bound. No new trained weights. Existing response matrix and entire model/background generators remain charged. CPU actual-weight response/product control is recorded separately; binding freezes executable inputs. Prior module dossiers checked in the preceding input-source analysis; this folds the existing interaction, not a newly identified circuit.
