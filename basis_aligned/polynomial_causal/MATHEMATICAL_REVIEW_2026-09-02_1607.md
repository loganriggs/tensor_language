# Mathematical review — 2026-09-02 16:07 UTC (three-hourly)

Context: since 1310 the cascade delivered rungs 487–494 — two full-pass
claims (488 interchange graph, 490 branch-resolved law, 491 A1-modulator)
and three nulls whose findings now bound the descriptive→causal gap from
FOUR directions: frame-relativity (§2592), output-algebra ≠ input-edit
(492), Gram-geometry ≠ merge-causality (493), and small-perturbation
additivity (494). The mathematics this cycle should serve the two live
objects: the λ-response structure 494 exposed, and the attention1
QK₁×QK₂×OV downstream-use decomposition now being registered.

## Ranked moves

### 1. Architecture-derived polynomial intervention-response theory —
### anchored on receipt aggregates; falsifier design FROZEN
**Object.** The per-occurrence response curve y_i(λ) of a site's scaled
query-product intervention (494's arms).
**Theorem-grounding.** The patch p → p − λδ injects an EXACTLY λ-linear
write perturbation (Down(λδ) = λ·Down(δ) — linearity of Down); every
downstream bilinear MLP is exactly quadratic in its input perturbation;
CE is analytic. So y(λ) is analytic in λ with architecture-determined
low-order structure — the quadratic model y = aλ + bλ² is the network's
OWN second-order theory, replacing 494's phenomenological monotone
readout with algebra.
**Receipt-level anchor (computed from 494 aggregates).** Actual effect
RMS grows sub-linearly: λ=.5→1.5 ratios 1.86/1.74 (code N/H), vs 3.0
for pure linearity — the response is CONCAVE (b opposing a), which is
precisely why additive (λ-linear) over-predicts at 1.5 and why the
monotone readout won only there.
**Frozen falsifier design (rung-ready, ~494-price).** Per occurrence,
solve a,b exactly from the two measured points (0.5, y½), (1, y1);
predict y(1.5) = 1.5a + 2.25b. Bars, measured from 494's receipt:
quadratic must beat additive at BOTH scales (it degenerates gracefully
to near-linear at small λ, fixing 494's B-failure) and beat isotonic's
.0096 median at λ=1.5 (code-N; per-cell analogues), against the same
16 position-permuted controls. Null: the response is not low-order —
composition raises effective degree and the curve is not
two-point-determined; then the regime map stands as 494 left it.
**Assumption that may fail.** 18 layers of quadratic composition give
degree 2^k in principle; low-order dominance at these magnitudes is an
empirical bet the falsifier prices.
**Consequence beyond reconstruction.** A two-parameter-per-occurrence
CAUSAL response chart (a,b) — supports prediction at any λ, selective
partial removal with predictable effect, and a certificate form (|b/a|
bounds the linear-regime width per occurrence).

### 2. The chokepoint interface contract (from 493's inversion)
**Object.** MLP1's write as a bottleneck variable: merging MLP1 writes
removes .84–.95 of EVERY branch-pair distinction.
**Operational definition.** Beckers–Halpern / Geiger-style causal
abstraction: MLP1-write is an (approximate) sufficient statistic for
MLP0-branch identity downstream. Certification = cross-context WRITE
INTERCHANGE (swap MLP1 writes between branch-matched contexts; behavior
must follow the write, unrelated behavior preserved).
**Consequence.** A compiled program may route all MLP0-branch semantics
through ONE interface tensor — extraction and selective manipulation at
a single named site.
**Falsifier.** The interchange test at bar; generic-damage controls per
493's aligned-fraction statistic. **Status: proposal** for the main line
after the attention1 arc (it composes with, not against, that route).

### 3. Isserlis/Wick null for the attention1 downstream-use grouping
**Object.** The incoming 495 decomposition of attention1's exact
QK₁×QK₂×OV computation by downstream use.
**Statement.** Products of near-Gaussian scores have overlap determined
by second moments alone (Isserlis) — the tensor-sim era's exact
machinery. Any "these score-pairs group by downstream use" claim should
beat the Isserlis-PREDICTED overlap computed from score covariances,
else the grouping is generic Gaussian geometry.
**Status: control-enrichment proposal** for Codex's 495 registration —
the analog of 478's alignment-destroyed and 493's shift controls, but
theory-computed rather than resampled.

## Pruned this cycle
Re-running single-index variants (closed at grain with regime mapped);
another merge-geometry pass (493 closed it); standing prunes unchanged.

## Citations
Beckers & Halpern, "Abstracting causal models" (AAAI 2019); Geiger et
al., causal abstraction for interpretability (2021–23); Isserlis (1918)
/ Wick's theorem; Higham, *Accuracy and Stability* (perturbation
composition); polynomial system identification: Volterra series
truncation (Schetzen, *The Volterra and Wiener Theories of Nonlinear
Systems*, 1980) — y(λ) here is a one-parameter Volterra slice with
architecture-known kernel orders.
