# Backward join origin writer: previous-key subpath v1

The fresh exact producer factorial supports E/current-value for forward H1 and
prefix/origin-key for backward H2. Within the predeclared factorial, Y1 alone
(second attention output, zero-based L1) recovers.951–.996 of the backward task
effect; Y0 alone is nearzero. These task effects do not meet full fidelity.

Test one explicit source hypothesis: at the earlier binding's value position,
L1 copies its immediately preceding key token, then L2H2 routes that origin into
the later binding's final-key consumers. Restrict L1 pattern to that single
key→value cell, retaining all four heads. Pass its O-projected output through
prefix residual coefficient1/2, the shared live L2 RMS gain, native L2 V, the
identified four L2 source→destination cells/H2, and native L2 O/residual coefficient.
This is a composed path in the exact native DAG. No heads/sources/subspaces are
selected after outcomes. The source function, two attention maps, gains and all
remaining native coefficients are explicit and charged.

Fresh19909/19910 two-chain generator,16IID+16OOD worlds, two arrangements and8
query forks as before. Exactly one backward and one independent forward route.
Use recipient contexts only. Compute native backward write dB and this subpath pB.
The complement dB-pB is defined exactly; it includes all omitted V producers.

Arms native,cutB,restoreP,restoreComplement,restoreAll; cutF,cutBoth,
restoreP_afterCutBoth,restoreAll_afterCutBoth. RestoreAll undercutBoth adds dB,
so its exact target is cutF. P restoration recomputes all downstream readers.
This distinguishes native upstream-layer removal from the registered compiled
path intervention: L2 routing and RMS are the original live values, not changed
by removing the other L1 paths. No cached donor state is used.

A: structural/causal path controls, all-source L1 path equals native Y1 V term,
nonzero controls, own dB replay<=1e-9 withforward live and removed, finite;
native ownhop3 accuracy>=.8 andbackward cut goldP loss>=.25 each pop/arrangement.
B: pB restores>=.8 of native-minus-cutB ownhop3 gold probability; complement<=.2.
Report controlquery andlowerhops; no correctness filtering.
C: restoreP vsnative andrestoreP_afterCutBoth vscutF full29-way distributions
meanKL<=1e-3,p99<=1e-2,all positions andquery separately,each pop/arrangement.
D: corresponding single/conditional-joint centered full-logit effects relative
RMS<=.01; absolute<=1e-8 below nativeRMS1e-6, all/query,each pop/arrangement.

Failure preserves exact DAG/path identity but rejects local origin-copy sufficiency
or its quantitative transfer, as scored. No posthoc source/head/threshold sweep.
A/B success licenses semantic key-edit verification, not compact extraction.
C/D remain necessary for any quantitative replacement claim. B4FP64,1800s,
<256MiB/tensor, managedGPU; no fitting, all400640 native parameters remain.
