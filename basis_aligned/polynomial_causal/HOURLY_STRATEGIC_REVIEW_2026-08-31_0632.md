# HOURLY STRATEGIC REVIEW — 2026-08-31 06:32Z

Convention (§2135): all damage numbers are CE added above the real model; LOWER IS BETTER.

## Where the program stands
Goal: reverse-engineer bilin18 into a predictive, manipulable, simpler tensor program.
Strict ledger: 5.348% / 10.923% coverage-credit variants, 4.727 nat, 0 of 68 modules fully closed (registry-
grade; the CP chapter below may eventually revise the front-module rows).
Since the 0537 review the program moved a whole chapter: the table+featmap grammar was falsified at the front
(§2201 capacity, §2202 objective), and the CP GRAMMAR CHAPTER opened and half-resolved in five rungs:
- §2203/§2204/§2205: the model's own top-K bilinear units dominate the incumbent replacement at EVERY front
  MLP by K=2304, at 7–29× fewer stored values; clean depth law (prunability rises with depth: at K=576,
  m0 +1.108 > m1 +0.280 > m2 +0.082 > m3 +0.075); exactness checks pass at full K (62/62).
- §2206: but the combined CP front costs +0.9427 with 0/62 valid = 3.3× its singles sum (+0.287). The enemy
  moved from SITES to DRIFT COMPOSITION.

## Largest gaps
tail dictionaries/coverage credit; m16 remainder; attn5's write (price cliff); the front COMPOUNDING (new —
the binding constraint on both the aggregate and certificate objectives).

## Candidates (pruned by information gain / falsifiability / GPU cost / redundancy)
1. **Stream-drift ledger (rung 115)** — is the 3.3× amplification in state space or only in the CE surface?
   Discriminates two repair strategies (K-escalation vs sequential refit). Cheap diagnostic. → EXECUTED.
2. Rungs 113/114 in flight (compounding anatomy; K-escalation with a discriminative 0.4 bar).
3. Sequential-conditioning repair: refit cf_i's Down weights in the drifted prefix frame (the build_arm trick
   applied to CP) — queue if 113 shows a concentrated entry point and 115 shows state-space amplification.
4. If 114's pred_a holds: the GLOBAL config rung — CP front + real front attention + §2144 middles/tail, full
   certificates; the first end-to-end config where nonzero validity is plausible.
5. Rung 90 document-grain gauge instrument (DESIGN) — still the covariant-compilation gate; still deferred
   behind the live compounding question.

## Decision
115 queued (depth 2 behind 114). Registry + memory synced with the CP chapter. Next review re-ranks with
113/114/115 all read out — the three together determine the repair strategy for the front, which is now the
critical path to both objectives.
