# Native-state bridge from component8.2 through MLP8 into producer9.8

2026-09-12, before native cache collection or scoring. Exact finite-amplitude four-term bridge passed actual-weight algebra probes; serial frozen-second removal diagnostic passes. Neither demonstrates which bridge terms are sufficient on native inputs. Existing MLP8/9/12 dossier checked: this is a specific composed circuit path, not a generic rank or activation reconstruction ladder.

Use the existing48regional confirmation rows, explicitly reused. No fit, amplitude tuning, new rank, or model-score row selection. Native and physical-component8-only removal executions capture pristine preMLP8 residual z, pristine/changed raw preRMS9 residuals r9, scalar a8 and pristine/changed scalar a9. Wholebody regional/control margin replay must match previous recursive native/physical8 arms.

The exact prediction from original z,a8,r9 uses delta=lambda9[-d*a +(rho'^-2-rho^-2)M(z) -a/rho'^2 D((Lz)*(Rd)+(Ld)*(Rz)) +a²/rho'^2 D((Ld)*(Rd))]. Four terms are direct, norm-background, mixed, quadratic. Form r9+delta, then apply actual RMS9 and full native QK1*QK2/value scalar executor. Source prefix inputs remain externally supplied; all norm and opaque weight costs remain charged.

A cache: original native/physical8 final regional/control margin <=1e-4relative; pristine scalar fields versus original compiled cache<=1e-5relative. CPU bridge: EACHtemplate exact predicted residual-change error<=1e-4relative and scalar-change error<=1e-4relative against true native removal; baseline scalar replay<=1e-5. Report absolute change norms, no division by baseline instead of change.

B EACHtemplate direct+mixed terms reproduce the full native a9 change within10%relative L2 over all valid positions/rows. This tests a smaller interaction path while retaining changed norm factors in the mixed term and recomputing the final r9 RMS. Also report omit-norm and omit-quadratic errors separately, with no posthoc substitution for B.

C EACHtemplate direct-only bridge has>=25%relative a9-change error. Opposing interpretation: the intervening MLP response is necessary to explain the serial interaction, rather than a direct residual write alone. Failing C means this particular direct path can suffice; it does not negate the previous algebra or imply absence of other interactions.

Price96single-prefix native forwards<=22tokens/120seconds,~15MBraw-statecache; CPU only last MLP8 weights and producer9maps,48rowsx5scalar evaluations, no optimization. Managedlane1 for native collection. A/B/C here concern a conditional producer-generator interface, not end-task sufficiency, a newOOD test, or repair of newline selectivity. A subsequent native effect test is required before adopting any shortened bridge.
