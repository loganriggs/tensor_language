# Signed source failure and matched-control specificity

The smaller predictor remains valid on its tested positive-source panels, but it does not pass the broader signed-amplitude gate. Native source specificity against random directions passes a separate screen. These are different claims.

## Signed and mixed intervention test

v692 keeps48v691rows and the frozen sparse-number/selected-modal formula. It adds(.5A+.5B),-A,-B,A-B,2A to A,B,A+B at the post11 boundary. This is new intervention amplitude coverage on opened text, not fresh text.

The original arms, half_sum, negA and twiceA pass target10%/modal5%. Number errors reach13.13% for negB and11.94% for difference, failing the registered bar. All modal prediction errors remain below3.37%; native modal collateral stays below10%. NegB's maximum source-budget-normalized number error is10.96%, so small own-effect denominators do not fully explain the failure. Budget normalization is supplementary, not a replacement gate.

v693 restores all36quadratic pairs per MLP. NegB still reaches12.20%,difference11.14%. Its measured runtime tensor count is3014including explicit pair indices; sparse v692 uses the1934-value runtime documented by the export. Historical inherited2582-value price prose in v692 is stale and is not the sparse runtime count. New v693/694scope/price fields are corrected; historical receipts are preserved.

v694 runs full native computations within groups of1,2,6blocks and projects only at group ends, using the existing initial/final interfaces. Worst number errors acrossall8arms/cells:

| Native group size | Worst error |
|---|---:|
| 1 | 13.35% |
| 2 | 11.47% |
| 6 | 5.49% |

Group2 improves the worst error but still fails10%. Group6 exactly reproduces the independently computed native-suffix/initial-interface oracle. These controls execute full native weights and are not compressed models. They motivate a backward-observable or joint-composition approach rather than another sparsity-only repair; they do not prove all width8 or all two-block decompositions impossible.

`audit_signed_source_results.py` independently verifies scoring and bit-identical native targets/modal effects acrossv692–694, and checks recorded native-call counts. Its receipt is `SIGNED_SOURCE_CPU_AUDIT_2026-09-20.json`.

## Matched random intervention screen

v695 changes to the CIRCUIT track. It applies8fixed random directions at the same post11 boundary, matched to each source's norm at every token, including zero-support positions. This is distinct from earlier pre11 joint-source random controls.

Per-token norm mismatch is at most1.71e-7. Native number-effect norm divided by median random-effect norm is19.61x for A and20.18x for B; the smallest individual cell ratios are12.21x and12.86x. The registered5x pooled specificity gate passes.

Control-prediction errors normalized by the corresponding **source** effect are at most6.65%/6.34% for number and3.08%/3.57% for modals (A/B), passing the10%/5% bars. A separate denominator audit finds median relative errors of54.8%/59.6% when normalized by the smaller **random** effects themselves. Thus this supports large source effects relative to controls, not general accurate reconstruction of random interventions. The denominator audit is `POST11_RANDOM_DENOMINATOR_AUDIT_2026-09-20.json`.

All native context/source/reader generators remain charged. There is still no token-input model, coherent residual-state replacement or universal selectivity claim.

## Next semantic control

v696 rows pair each subject with the opposite-number human noun inside its relative clause. CPU checks verify48control positions, distinct from subject/readout, and unchanged matrix-subject answer labels. Source hash remains c38db7a51d9f958e5020d5c921af04b0fe14a3f4a8be6a2ff88116e9bce8c62a; control hash8f5dd61488ce13d8eea3de10e832b9e12ec5c6a3bbb0d4a6c7232d119e690be8. Native attractor-removal execution is pending. This will test whether the observed specificity depends on subject role rather than merely a number-bearing noun direction.
