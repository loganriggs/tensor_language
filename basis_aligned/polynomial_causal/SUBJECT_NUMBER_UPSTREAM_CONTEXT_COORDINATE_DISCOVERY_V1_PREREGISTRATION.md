# Subject-number upstream context-coordinate discovery V1

The native base-head singular-coordinate readout preserved sign but failed amplitude, especially at E/A/U/W cardinality four. On the same already-open original authority, define $x_0$ as the recipient MLP8 input, $x_b$ as the input under background subset $b$, and $x_4$ as the full E/A/U/W background. Define

$$
g_b=4\frac{\langle x_b-x_0,x_4-x_0\rangle}{\|x_4-x_0\|_2^2},
$$

and let $z$ be the recipient/base L11H3 write projected onto its native top output singular direction. Compare three fixed ordinary-least-squares designs: affine `[1,z]`, bilinear `[1,z,g,z*g]`, and quadratic-context `[1,z,g,g^2,z*g,z*g^2]`. Evaluate by leave-one-construction-out prediction and fit each form once on all rows, for nine fits total.

The interaction claim passes if bilinear reduces relative L2 by at least `.10` against affine and reaches relative L2 at most `.40`. Select the smallest passing form, allowing quadratic-context only if bilinear fails and it reaches `.40`. This is outcome-blind discovery on program labels: no exact number-edit displacement, answer logits, behavioral effects, fresh activations, gradients, updates, or quantization. Price: one model forward, 96 role sequences, nine scalar OLS fits.
