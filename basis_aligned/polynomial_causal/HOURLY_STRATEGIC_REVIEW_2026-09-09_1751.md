# Strategic checkpoint — 2026-09-09 17:51 UTC

Circuit targets are explicit inputs/operations/writes/consumers; grouping across
modules and splitting within them; held-out/OOD prediction; extraction with a
declared executable background; selective manipulation; composition and reuse;
and stable identification. The complete goal remains a structurally simpler
predictive, composable, manipulable program, including every opaque weight and
adapter. Follow the bilinear reconstruction handoff and its appended criterion.

## What changed

Five managed research receipts landed since1651: source-support interaction,
exact exported source editor, origin-field swap, endpoint-field swap, and random
layout endpoint-family test. The first two are infrastructure/algebra validations,
not discovered circuits. Three semantic screens produced two failed broad claims
and one successful fixed-layout endpoint interface. CPU work classified failures,
translated a shared endpoint-message dictionary and located final readers.

Disjoint source-support composition is exact under one final bilinear-attention
layer. The token-derived source editor reproduces native edits and exports
independently, but is slower than native cached suffix execution and makes only
the already known generic readout-fold saving. All387968 constants stay charged.

Backward origin interchange fails50–69% target accuracy. Forward endpoint
interchange succeeds100% on the fixed IID/two12-cycle layouts. Its exact shared
operation is contextual scalar gate times a24×128 endpoint dictionary. The cache
costs24576bytes; no native parameter disappears through this translation.

The all-join random-layout extrapolation fails hop2 reuse despite valid numerical
execution. Hop3 retargeting/removal is strong, but hop2 original-answer loss is
only.009–.012. Preserve the combined failure. The runner's instrument_invalid
label came from mixing a scientific removal criterion into its mechanical gate;
a separate classification receipt exposes that fact without rewriting it.

The final-reader CPU diagnostic prevents a second mistaken inference: hop2 logit
effects are large despite weak task damage. Heads0/3 dominate hop3 changes but
neither is sufficient alone. These are opened-case diagnostic nominations, not
adopted head-defined circuits. No arbitrary-layout or smaller-program success is
claimed from the fixed-layout pass.

## Decision and alternatives

The strongest next scientific test separates original binding-value E/8 from
computed forward endpoint messages in the same postL2 record. Independent field
maps test whether different query computations consume those different values.
The edits overlap, so the complete RMS interaction must be retained. Raw E/8 may
be weak or redundant; a null will reject it without enlarging its producer set or
tuning its scale. The preregistration and primitive exist;7 CPU controls pass.

Alternatives: a final-head union/port sweep could refine localization but does not
explain the hop2 versus hop3 computation. A routing refit repeats the closed
whole-head kernel failure. More dictionary/rank compression would not identify
consumers and is not licensed. A full-model parameter reduction remains necessary
for the overall goal, but currently lacks a new exact closure or justified
approximation. Do not substitute the successful field interface for that goal.

Confounds checked: native errors remain in every panel; layouts/maps are fixed
before outputs; endpoint dictionaries are derived from weights and charged;
token-derived caches are not teacher activations; donor/gain changes are not
silently described as whole-token counterfactuals; common logit offsets and
nonlinear probabilities are separated from raw response algebra. Group-mean
control bars are not worst-case guarantees. Hop2 failure is neither absence of
any reader response nor a license to drop hop2 after seeing the outcome.

## Throughput and required engineering correction

Board/runlogs/Git provide approximate claim→managed completion→published dossier
bounds: source support16:48→16:55→17:01; exact editor16:58→17:11→17:19;
origin17:14→17:26→17:31; endpoint17:29→17:32→17:40;
random family17:37→17:43→current17:51 draft. These overlap and are not additive
worker-minute measurements. The first four published cycles have median~15min;
the semantic screen/report cycles also exceed the10min operating target.
GPU computation across the five receipts totals about34.5s. The19.2s CPU reader
audit and1.6s message audit are small relative to the authoring/publication gaps.

Repeated runner implementation, restoration of state and growing documentation
are visible costs; exact thinking-versus-review minutes were not instrumented and
must not be invented. The export proof was justified infrastructure, but copying
another130-line runner/scorer for each field test is not sustainable. The next
bounded block must share panel scoring and numerical correspondence before
integrating the already registered dual-field test. Existing completed runners
and outcomes remain immutable. No new scientific candidate is opened first.

CIRCUIT_FOCUS: PASS — semantic fields, consumers, causal edits and executable reuse.
CEREMONY_BUDGET: FAIL operationally — repeated authoring/publication exceeds the
basic-screen latency target; exact category fractions are unknown. Repair now by
using a shared scorer and compact result publication in the next integration.
NOVELTY_LESSON_GATE: PASS — preserve closed kernels/origin interchange, test a new
within-position producer distinction, and separate validity from semantic bars.

The corrective shared field_intervention_metrics.py implementation has begun at
this checkpoint. Its focused controls must pin signed target gain, collateral,
empty panels, offset invariance and mechanical-only validity. Then use it in the
dual-field runner rather than duplicating another scorer. Both managed runners
remain healthy; shared dirty work belongs to multiple agents and is preserved.
Next hourly after18:51; mathematical review remains due19:48. Goal active.
