# Attention16 and residual sources of the frozen quadratic branch

Earlier attention17 output pullbacks and parent1 producer quadratics already establish the general folding identities. Here the object is the current stable producer-folded parent and its partners3/8. Let r be block16 residual after its learned re-entry, a its attention output, and x16=RMS(r+a). For each stored symmetric reader A_j define

$$R_j=r^TA_jr,\quad X_j=2r^TA_ja,\quad T_j=a^TA_ja.$$

Under an attention gain t, numerator q_j(t)=R_j+tX_j+t^2T_j and input normalization

$$n(t)=\frac{\|r\|^2+2t r^Ta+t^2\|a\|^2}{d}+\epsilon.$$

The branch is

$$w_j(t)=\frac{q_0(t)q_j(t)}{n(t)^2\rho_{17}^2}d_j=\frac{\sum_{k=0}^4 c_{jk}t^k}{n(t)^2\rho_{17}^2}d_j.$$

The five coefficients are respectively R0Rj; X0Rj+R0Xj; T0Rj+X0Xj+R0Tj; T0Xj+X0Tj; T0Tj. This groups terms by attention degree, not by causal independence. Opposing signed terms can cancel; their magnitudes are not additive fractions of importance. Norms, re-entry and other pathways must not be silently frozen or omitted.

The initial executor keeps the actual rho17 port fixed while changing the producer's attention16 input edge. It is a conditional path intervention, not a full attention16 module ablation: other descendants of attention16 remain external. Subsequent native instrumentation must capture r16/a16 and verify their normalized sum against the real MLP16 input. CPU control first compares this rational program against direct packed-quadratic evaluation at gains−.5,0,.5,1,1.5 on arbitrary sourceports; numerical identity is not natural reachability or semantic evidence.
