# Attribute recursive routing/value interaction to the head9.8 cross term

Frozen 15 September 2026 after the successful four-arm composition result. Reuse
its 48-row panel without selecting rows or changing either component. The panel
is now opened; this is mechanism attribution, not another fresh transfer claim.

Add one arm, `additive_without_cross`, which installs the routing-only and
value-only head-vector changes together but omits the exact product term. With
native routing $P$, removed routing $R$, native value $v$, and changed value
$\Delta v$, compare

$$
\Delta z_{\mathrm{add}}=-Rv+P\Delta v
$$

against the true joint

$$
\Delta z_{\mathrm{joint}}=-Rv+P\Delta v-R\Delta v.
$$

Thus joint minus additive isolates the explicit head term $-R\Delta v$. The
ordinary factorial difference-of-differences can additionally contain curvature
from running each arm through the nonlinear suffix.

- `pred_a_exact_instrument`: the four existing exact audits remain at most
  $2\times10^{-6}$ and exactly 30 physical batches execute with finite outputs.
- `pred_b_both_single_branches_live`: preserve the prior liveness gate in both
  families.
- `pred_c_explicit_cross_explains_recursive_interaction`: for paired target
  logits in each family, joint-minus-additive has cosine at least `.90` with the
  factorial difference-of-differences and relative error at most `.35`.
- `pred_d_joint_family_transfer`: preserve joint/additive-single cosine at least
  `.50` in both families.
- `pred_e_unrelated_reader_selectivity`: preserve every joint unrelated-reader
  ratio at most `.75`.

Report the same pre-softcap numerator and final-logit metrics. No fit, gradient,
parameter update, row selection, or new compression claim.
