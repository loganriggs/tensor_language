# Hourly strategic review — 2026-09-03 03:30 UTC (Claude)

## Program state (sign convention §2135: frontier L2 = CE added above the real model, LOWER IS BETTER)

Strict-ledger explained fraction unchanged: 5.348% / 10.923% / 4.727 nat /
0 of 68; frontier §312 norm-2304 at +2.6735 CE-added. Largest gaps: tail
dictionaries/coverage credit, m16 remainder, attn5 write price cliff.

## Where things stand

The MLP0 source chapter is deepening on Codex's lane: 517 (diffuse+redundant
5-relation grammar §2652) → my §2652 rank-1 companion (the grammar is ONE
low-dim context summary, explaining the redundancy) → 518 (45 head×relation
pieces ungroupable, predicted by the rank-1 companion) → 519 (one-circuit
exact interaction atlas on r.2.0.2; currently INSTRUMENT-INVALID at the
float32 fixed-gain closure, self-labeled, Codex repairing). My equality-score
+ MLP0-rank parallel niche has delivered its positives.

## The genuine strategic observation

TWO independent rank-1 results now exist: §2649 (the four equality-score
implementations' copy-TASK effect is near-rank-1) and §2652 companion (MLP0's
five source-relation effect is rank-1). Both say "context is read through a
LOW-DIMENSIONAL summary." If that generalizes across the model's context-
reading MLPs, it is a structural LAW with real compiled-program consequences
(the context a compiled MLP must reproduce is low-rank per site). This is the
highest-value NEW question my lane can pose.

## Candidates

1. **Rank-generalization law** — does the low-dim-context-summary structure
   hold at another context-reading MLP (e.g. MLP1)? REGISTERED as a frozen
   falsifiable design this wake; it needs new GPU data at a fresh site built
   on 517's hash-pinned source-relation machinery — a careful build, NOT
   rushed into the 517/518/519 instrument-invalid churn pattern. Ready for my
   next quality build or Codex's machinery.
2. **519 repaired verdict** (Codex) — scored on landing with the rho
   denominator watch-item.
3. **Coverage-credit decision** — full 3-tier table + task-space + MDL basis
   filed; Codex-gated.
4. **Frontier repricing/OOD** (m16/attn5) — the strategic pivot, needs
   machinery-load and is the direction-lead's call.
5. **Gauge-invariant / info-bottleneck** (0107 moves) — parked.

## Executed

No safe QUALITY GPU rung is cleanly buildable-at-quality this wake without
either machinery I must load (frontier) or a fresh-site build I decline to
rush (candidate 1). Forcing a hasty rung would violate the quality bar the
program is built on — three consecutive rungs (517/518/519) each hit an
instrument-invalid first run despite Codex's discipline, so a rushed new-site
build would very likely churn too. Executed instead: (a) this assessment; (b)
candidate 1 REGISTERED as a frozen falsifiable design
(MLP1_CONTEXT_RANK_GENERALIZATION_RUNG_PREREGISTRATION.md), ready to build at
quality next wake or hand to Codex's source-relation machinery. Explanations
current (0259). The honest state: my lane is between deliverables; the next
real move is either the rank-generalization build (mine, next quality wake) or
a Codex decision (coverage) / direction call (frontier).
