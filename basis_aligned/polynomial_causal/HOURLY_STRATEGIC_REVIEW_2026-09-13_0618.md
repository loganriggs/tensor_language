# Hourly research review — 13 September 2026, 06:18 UTC

The interpretation targets remain: explicit computational specification; grouping across boundaries and splitting within modules; held-out/OOD prediction; extraction or sufficiency; selective manipulation; composition and reuse; stable identification. The full goal is a simpler executable program satisfying prediction, extraction, removal and reuse. The user's explicit interaction-compression direction supersedes the older compression-primary prohibition.

Since 05:12, the two-capacity shared/private fit completed twenty screening starts and six promoted refinements in 2,238 seconds of GPU time. Every promoted fit reached its sweep limit without the registered convergence criterion. Both improve exact coefficient error over matched-byte global subspaces but miss the 10% squared-gain bar. FineWeb validation improves versus those baselines but fails fidelity; regional circuit preservation also fails. In particular, the larger fit is worse than its matched global baseline on all five regional groups despite its better aggregate fit. Zero new identified circuits resulted.

The exact joint-QK coefficient metric was derived and checked during the 05:29 math cycle; in the tested fixed-producer class it barely changes the preferred value-side approximation. Duplicate-token and private-span audits narrow the interpretation without dismissing the full fit's gain. Current regional precision controls are much smaller than replacement errors. Historical cached text is not fresh OOD evidence. Weight fitting remains separate from validation, and whole-U extrapolation remains separate from replacing only the quadratic route.

Decision: prioritize the actual setting2 mixed interaction tensor. Native/full-HOSVD sparsity baselines exposed adapter overhead; a bitmap encoding corrected an overly pessimistic coordinate-list storage result. All eight partial-frame choices were actually executed next. Cheap output/head rotations improve the storage frontier; strict-accuracy gains remain modest and behavior is untested. This is a different object and representation comparison, not a repeat of global output rank fitting.

CIRCUIT_FOCUS: PASS under the user's compression override. Regional preservation directly discriminated which compression metric helps this circuit; compression itself is not identification.
CEREMONY_BUDGET: FAIL TO VERIFY quantitatively. Earlier phase telemetry was incomplete and concurrent GPU/CPU intervals cannot be added as exclusive wall-time buckets. Do not invent percentages. Repair is to reuse current executors and receipts, with a short CPU partial-frame comparison already executed rather than another bespoke validation framework.
NOVELTY_LESSON_GATE: PASS. Existing product, LL1, shared-DAG and orthogonal-frame work was checked; scope, adapters, local stationarity and control-family dependence remain explicit.

The ten-minute circuit-screen target is not met by the roughly 37-minute optimization run; twenty starts are not twenty circuit screens. That larger compute budget addressed the user's convergence concern, but all fits still remain unconverged. Do not automatically extend them. Next moves ranked: (1) test partial-frame sparsity on the existing regional interaction ports, killed by fidelity failure at useful savings; (2) learned sparse frames with cheap adapters, killed by no improvement over the fixed-frame price/error frontier; (3) shared intermediates across retained terms, killed by no net savings after private corrections and generators. None establishes absence of structure universally.

Primary receipts: SHARED_LOCAL_REGIONAL_V1_MATH.md, FULLU_SHARED_LOCAL_FIT_V1_MATH.md, INTERACTION_SPARSE_CORE_BASELINE_V1_MATH.md. Next mathematical review is due about 08:29 UTC.
