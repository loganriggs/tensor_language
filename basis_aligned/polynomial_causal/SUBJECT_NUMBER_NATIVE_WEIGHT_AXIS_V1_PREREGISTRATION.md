# Subject-number native weight axis V1

The held subject-number program still stores a fitted 1,152-dimensional write axis. A weights-only CPU diagnostic found that this axis has cosine `.94552` with the top left singular vector of the native L11H3 output-projection slice and that this single native direction explains `.89402` of its squared norm.

Before reading new causal outcomes, freeze that top singular vector as the replacement axis. Its sign is a gauge choice fixed to have positive dot product with the prior axis; no mixture, scale, gain, or downstream outcome may be fitted. Keep the held four-scalar direction-by-cardinality coefficient law exactly unchanged.

On the licensed 32-row fresh authority, evaluate every E/A/U/W background subset with base, native exact, held law-axis, and native-weight-axis arms. The native axis passes if its 512 causal effects reproduce the held law-axis with cosine at least `.90`, relative L2 at most `.50`, and sign agreement at least `.90`; reproduce native exact with cosine at least `.65`, relative L2 at most `.90`, and sign agreement at least `.70`; clear cosine `.60`, relative L2 `1.0`, and sign `.65` on intermediate cardinalities; and clear cosine `.50`, relative L2 `1.1`, and sign `.60` in each fresh template. Instrument closure must be at most `5e-5`.

A pass establishes that a distinguished direction of the model's own L11H3 output weights supplies most of the reusable causal axis. It does not derive the remaining axis residual or the direction/cardinality inputs from native activations. No causal fit, axis combination, scale correction, row or threshold change, rank sweep, gradient, update, or quantization. Exact price: nine forwards, 2,144 sequence evaluations, and 2,048 causal installations.
