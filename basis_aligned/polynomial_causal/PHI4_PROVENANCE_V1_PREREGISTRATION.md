# Four-mode source and mode provenance

24originalcity+72fresh prefixes, frozen modes/routing.192 full forwards, native then phi4 numerator donation,120second managed cap. Reuse same hooks and donation semantics; log native signedmode values[96,26,4], rho9[96,26], gamma[96,26,26], actual donor scalarfields and final target/control margins. No full activationdump or fitting.

A: native/donor margins replay prior fresh run (including24 oldanchor rows) <=1e-4relative separately old/fresh.
B: reconstruct actual donor scalarfield by summing routing times donor-minus-recipient modevalues times gain/rho9; allrow relativeerror<=1e-10.
C: cue-token-only contribution predicts final-position donor scalarwrite with<=20%relativeerror separately each24row group(oldcity,fronted,nearquote,distant). Null: the computational difference is not localized to the city token itself. This is an exact additive computational source decomposition, not single-source native causality or a new semantic classification. Record all modes/source contributions evenif Cfails tolocalize whatis omitted. Previous native fresh-context failure remains untouched. Mode basis sign is irrelevant to squaredmodevalue; individual modes not assumed independently identified circuits.
