# Hourly strategic circuit review — 2026-09-08 17:22 UTC

## Circuit interpretation targets and full goal

A useful circuit must state what is read, computed, written, and consumed; merge
cross-module pieces and split mixed native modules by computation; predict held-out
and OOD effects; expose an executable sufficient interface; support selective
removal, swaps, and edits under redundancy; compose and reuse shared variables; and
remain stable across corpora, gauges, and fitting restarts.  The full objective is
a smaller literal tensor program that is jointly predictive, composable,
manipulable, and simpler in storage, compute, edges, state, and description.

## What changed since 16:22

The frozen v17 U8 transferred weakly in coordinates but preserved the physical
A11 H3 endpoint on v18.  H3 retained `.887/.920` of complete A11 reader loss with
cosines `.998/.998`; fixed residual coordinates and old M11 factor prefixes had
construction-dependent magnitude.  Two instrument defects were preserved before
valid scoring: the residual basis dimension was incorrectly treated as MLP hidden
width, and canonical controls required suffix rather than same-index alignment.

The factor objective was then tested rather than assumed.  Exact fixed-coefficient
output-greedy selection failed: even 256 factors left `.750/.768` training residual,
held-out top-128 recovered only `.615`, and the large prefix moved canonical controls
by `.300`.  This is an honest null for activation/output reconstruction as a circuit
criterion.

Pulling the downstream answer-margin gradient back to each exact native M11 factor
changed the result.  The full factor tangent predicted complete M11 causal loss on
the two selection constructions with `.99987/.99998` cosine and `.993/1.002`
recovery.  A frozen top-16 subset recovered `.843` on held-out v18 A2 while leaving
`.952` of its output residual: behavior is sparse in task-read weight factors, not
in raw module-output energy.  P/C movement remained selective.

Pristine v19 confirmed a stable directional top-16 core (`.664/.658` recovery,
`.997/.981` cosine, gap `.006`) but missed the registered `.70` magnitude floor;
larger prefixes became construction-dependent.  The observed recovery interval
admitted one fixed gain, and a disclosed retrospective `1.25` screen put all three
observed targets in `[.8,1.2]` with controls at most `.139`.  That pass is only
calibration.  A new sealed corpus is required for confirmation.

## Confound and route audit

- Baseline subtraction remains writer-minus-native within row; A1/A2 and P/C are
  separate.  Output-energy failure cannot be hidden by average reconstruction.
- The downstream graph is nonlinear.  Near-perfect tangent closure and dose
  proportionality are empirical local facts, not assumed globally; v20 will carry
  a finite-dose linearity falsifier.
- V18/v19 now participated in factor selection or gain calibration.  They cannot
  confirm the compiled program.  V20 must be capability-gated and sealed first.
- The task-tangent order uses two constructions and may encode their shared lexical
  or positional difficulty.  History-disjoint cues/professions, P/C controls, and
  two new target forms are required.
- The basis-shape and suffix-alignment failures were instrument bugs before valid
  results, not scientific misses.  Relative squared output closure replaced an
  inappropriate BF16 max-absolute gate without changing scientific arms.
- A global `1.25` gain was post-selected from three target panels.  It is frozen
  prospectively; no row-, construction-, or corpus-specific adjustment is legal.

The highest-information route remains prospective fixed-program confirmation.  A
dataset PCA/SAE or hierarchical decomposition is demoted because raw energy already
failed and because a fitted activation basis would add another post-selection
surface.  A weight-side, task-functional decomposition becomes valuable after the
fixed factors generalize or when multiple identified tasks supply a response tensor.

## Throughput and systems audit

From 16:22 to 17:25, authoritative commits record seven substantive circuit-level
receipts: v18 frozen transfer, corrected factor replay, pristine v19 capability,
exact output-greedy null, task-tangent factor program, pristine v19 confirmation,
and fixed-gain calibration.  Successive scientific receipts landed mostly every
3--8 minutes after the first deeper transfer.  GPU jobs themselves completed in
seconds; scientific formulation and intervention implementation dominated.  The
main avoidable cost was two early transfer-instrument repairs.  Existing shared
builders and factor intervention helpers should be reused for v20 instead of adding
another framework.

`CIRCUIT_FOCUS: PASS` — work advanced physical endpoint stability, within-MLP
splitting, causal prediction, selectivity, and an executable partial program.

`CEREMONY_BUDGET: PASS` — validation was smaller than the scientific screens; the
extra precision replay repaired a concrete invalid measurement boundary.

`NOVELTY_LESSON_GATE: PASS` — new corpora were checked against full construction
history, and the known reconstruction/variance failure was tested once and then
converted into a task-functional objective.

## Ranked next moves and kill evidence

1. Seal v20, capability-gate it, and confirm the frozen top-16 plus gain `1.25` with
   a finite-dose linearity check.  This changes held-out prediction, selective
   manipulation, within-module splitting, and stable identification.  Kill the
   global compiler on registered recovery, cosine/direction, control, or curvature
   failure.
2. If v20 passes, build a prospectively split multi-task factor-response tensor and
   test whether the same exact factors serve another identified circuit.  This
   changes composition/reuse and cross-boundary grouping.  Kill shared-factor claims
   if cross-task removal/restoration or joint composition fails despite overlap.
3. If v20 has stable direction but magnitude failure, identify a tiny upstream
   construction-state gain or second disjoint factor branch.  This changes the
   computational specification and extraction target.  Kill it if a frozen state
   law does not predict a second sealed corpus.

Concrete continuation: the v20 authority must be written and hash-sealed before its
capability or causal outcomes, followed by the no-refit confirmation through the
managed GPU runner.

