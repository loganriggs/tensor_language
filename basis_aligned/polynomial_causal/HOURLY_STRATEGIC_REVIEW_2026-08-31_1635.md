# HOURLY STRATEGIC REVIEW — 2026-08-31 16:35Z

Convention (§2135): all damage numbers are CE added above the real model; LOWER IS BETTER.

## Where the program stands
Goal: reverse-engineer bilin18 into a predictive, manipulable, simpler tensor program. Strict ledger:
5.348%/10.923%, 4.727 nat, 0/68 closed. Pareto set: {47.8M → +1.8765}, {63.7M → +1.6599}.
CIRCUITS PROGRAM status after its first day: k* column complete (median 5); interchange = the surgical tool
(425× via member gating); carriers = principal variance, member-agnostic (§2266) — circuit identity is
POSITIONAL; family bases cover 58/62 at 0.37M values; removal capped ~2× by substrate sharing (§2264);
gates are not cheaply linear (§2267: stream AUC 0.702) and NOT class lookups (CPU check this review:
class-alone median AUC 0.581 — the census leaves are much finer than classify2).

## Three-property scorecard (updated, honest)
REMOVAL: measured and capped (~2× tool-invariant; 425× only with oracle gating; gate deployability under
test in rung 172). OOD: blocked at leaf recomputation (recorded in repertoire). EXTRACTION: sufficiency
running (rung 171); certificates still 0/62 for every global config; the reconnect rung (173) tests whether
per-circuit repair at component grain is even possible inside the frontier.

## Candidates (pruned)
1. **Minimal-set trajectory splice (rung 173)** — the reconnect: does fixing a circuit's own ~5 components
   repair it inside the frontier, and specifically so? Decides whether per-circuit repair is a real program
   or the positional story's hard form wins. → EXECUTED (queued).
2. Stream+class gate probe (rung 172) — closes or opens the deployable-gating thread. → EXECUTED (queued).
3. Rung 171 (sufficiency) in flight.
4. If 173 passes: per-circuit repair chapter (fix minimal-set fidelity, certificate-scored). If it fails
   indiscriminately: the two-ledger accounting becomes permanent and the program optimizes aggregate +
   documented circuit costs.
5. Explanation doc due next hour.

## Decision
172+173 queued behind 171. This completes the circuits program's phase-1 question list; the next review
integrates sufficiency + gate + reconnect and picks the phase-2 chapter.
