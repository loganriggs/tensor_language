# Causal additive-state boundary curve

13 September2026. Fixed existing160prefixes,13arms,2080fullforwards,180second limit. All code and dependencies bound before native execution.

For nativeN and childC/parentP/remainderR removal trajectories, replace the joint-removal state at one post-block boundary l=9,...,17 with hC+labeled hR-hN (all three states at that same boundary). This deletes accumulated mixed state while retaining both individual response states. Then execute the native suffix. Each boundary is a separate intervention; no cumulative reset across layers within an arm.

A: firstfour outcomes replay prior hierarchy; boundary9 matches measured-native MLP9 mixed correction; boundary17 matches previous final additive-state readout. Aggregate relative errors <=1e-4 separately regional/FineWeb.

B: at least one boundary9–12 leaves <=50% original final interaction norm in allfour regional constructions.

C: regional remaining-interaction norms are nonincreasing with boundary depth, allowing <=1% original-interaction norm slack per transition. Opposing null: nonlinear transport and cancellation prevent a monotone curve. Report every boundary regardless of verdict; do not select sites by held-out outcome and call that confirmation.

FineWeb curves reported descriptively with CE and margin available; tiny interactions require absolute error reporting. No newdata/OOD evidence. This is a diagnostic oracle: three native state arrays plus the native suffix, not an autonomous predictive circuit. Causal differences between cumulative reset arms do not isolate local generation because the background trajectories differ. Next local generation/propagation tests depend on this result.
