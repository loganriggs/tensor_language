# Exact native sources of the coupled MLP8 context

Weight-folding target: g=c+e+a, c=lambda80*h7, e=lambda81*x0,
a=attention8 output.40opened fresh-panel sequences,40native body forwards,
300seconds. No intervention or source selection in this capture.

pred_a: native readouts replay prior native8 fresh native arm<=1e-5abs;
FP64 source sum versus previously stored native g <=1e-6relative each;
finite,40forwards. pred_b CPU: expand the normalization correction into
nine ordered source products and both delta/source cross orders into six;
retain quadratic delta*delta and residual skip. Sum matches direct complete
formula on the exact source sum<=1e-10relative. pred_c: expanded response
matches40native joint-delta fixtures<=1e-4relative each. No gate rescue.

Report norm ratios AND signed aligned fractions on source and total-write
responses; RMS denominators always use all sources. These are exact folds on
opened activations, not source-removal or independent downstream edits.
Three context sources replace one supplied array, adding two inputs unless
producers are executable from existing inputs. No port closure claim. Search
prior source failures and preserve initial-state terms; no rank/norm-based
truncation. An omission needs a separately registered fresh causal test.
