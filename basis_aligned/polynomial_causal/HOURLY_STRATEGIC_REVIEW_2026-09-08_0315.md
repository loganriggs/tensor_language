# Hourly strategic review — 2026-09-08 03:15 UTC

## Circuit interpretation targets

The target is still a decomposition that states what information is read, what operation is
performed, what is written, and which later computation uses it; groups and splits units across
native head/MLP boundaries when the causal computation demands it; predicts held-out and OOD
behavior; is executable or has an explicit background interface; supports selective removal,
swap, and edit; composes and reuses subcomputations; and remains stable across splits and gauges.
The program-level goal remains a smaller transparent tensor program that is jointly predictive,
composable, manipulable, and literally simpler in storage, compute, edges, and state.  Neither a
low-rank basis nor a static weight alignment is completion.

## What changed since 02:15

The hour produced seven durable scientific receipts or corrections.

1. The sign-invariant Gram router recovered both targets but leaked P, closing that response
   signature under its strict gate.
2. A two-entry unordered token-pair router was a perfect selective v15 executable screen:
   `.795-.881` target projection, exact gold composition, and zero P/C effect.
3. Its sealed v16 audit was an exact OOD null.  None of 32 native-capable targets used either v15
   signature, so frozen default-off implied zero transfer without GPU execution.
4. A normalization-aware atlas mapped the entry12 span through literal weights.  Its first result
   was preserved invalid because the price omitted four reference forwards; a zero-model audit
   corrected only that accounting.  RMS Jacobians closely matched exact finite responses, while
   raw/exact top-ten rankings were already `.9-1.0` overlapping.
5. The normalized atlas stably nominated L12H0:v, L12H4:q/k, L13H2:q, and L15H5 factors; all four
   known oracle source heads ranked above the 97th upstream-writer percentile.
6. A full layer-12/13 module/head/factor mediation atlas rejected those static nominations:
   complete modules mediated below `.012`, nominated Q/K factors were essentially zero, and exact
   singleton composition ruled out hidden head interactions.
7. The coherent suffix-write factorial identified the dominant path.  Freezing all twelve
   layer-12--17 writes retains `.803-.871` signed projection with direction `1.0`; suffix writes
   contribute `.129-.201`.  The direct final-state delta equals the entry12 delta multiplied by
   `prod_{l=12}^{17} lambda_l[0] = 1.51363146` to `1.42e-6` relative L2, then the exact final RMSNorm,
   tied unembedding, and soft cap decode it.

Two corrections matter.  First, static `W J_rms(x) U` response is a reader nomination, not causal
mediation; L12/L13 repeat the earlier L15H5 lesson decisively.  Second, manual reference executions
must be counted explicitly in future prices.  Two schema-key mistakes also caused outcome-free
failed launches; runner tests should assert immutable result structure and canonical scorer keys,
not merely source-string presence.

## Is this still the highest-information route?

Yes, with a narrower object.  The direct residual law is now more informative than sweeping later
modules: it supplies an explicit cross-layer computation and explains why high static input-weight
scores can coexist with negligible mediation.  The next confirmation should manipulate the final
state coordinate and tied-unembedding response directly.  In parallel conceptually, the v16 null
makes semantic routing—not another DAS penalty—the limiting prediction gate.

## Confound audit

- **Baseline subtraction:** all mediation vectors use the same native-off margin and exact four-cell
  decomposition; closure is zero.
- **Frame mixing:** each union is fit on the opposite parity; the direct law is expressed in the
  shared residual gauge, and its scalar recurrent transport is checked rowwise.
- **Nonlinear composition:** suffix-write reset and rescue differ by small Mobius terms; these are
  preserved rather than added as if linear.  Final RMSNorm and soft cap are replayed exactly.
- **Shared-token difficulty/leakage:** the token gate's v15 success does not transfer to v16; the
  zero-coverage null is explicit.  Gold routing is used only to identify the downstream circuit's
  achievable conditional program.
- **Dead knobs/tripwires:** every factor hook has exact call/shape coverage; factor and state replays
  are zero-error.  The v1 weight price tripwire was live and correctly invalidated the artifact.
- **Precision/noise:** direct-state relative errors are about `1e-6`; exact logit replay is zero.
- **Post-selection:** L12H0/L12H4/L13H2 were frozen from the weight atlas before causal outcomes;
  their nulls are preserved.  No substitute head is promoted from the atlas.

## Distinct next approaches, ranked

1. **Final-state removal and construction swap.**  Project the native routed final-state delta onto
   the transported rank-two span, remove it, and swap A1/A2 coordinate responses before the exact
   final head.  This advances selective manipulation and computational specification.  Kill it if
   removal does not erase at least 75% of the direct-route response, if swaps fail the preregistered
   opposite-construction prediction, or if P/C changes.
2. **Frozen embedding-semantic router for v16.**  Use only tied token-embedding tensors and v15 cue
   phrases to construct a direction-invariant temporal-polarity/change representation; assess v16
   signatures before causal execution.  This advances OOD prediction and stable identification.
   Kill it if A1/A2/P are not separable without v16 labels or if construction routing is ambiguous.
3. **Quantify the minor suffix adjustment as a signed module algebra.**  The suffix bank contributes
   13--20%; reuse the already-compositional singleton module atlas to predict its correction.  This
   advances composition but is lower priority until the dominant final coordinate is manipulable.
   Kill it if held-fold correction coefficients or signs are unstable.
4. **More static reader ranking.**  Rejected: it changes no circuit target after two causal bypass
   nulls and would be another weight-geometry sweep.

The live direction therefore changes from downstream reader nomination to direct final-state
manipulation, followed by semantic OOD routing.  This is not rank drift: success is measured by
selective causal removal/swap and fresh-bank prediction, not by smaller rank or reconstruction.

## Throughput and systems checkpoint

Authoritative commits/results from 02:15--03:15 give approximately one receipt every 8--10 serial
minutes despite the deeper 156-forward factor atlas.  GPU computation remained seconds; scientific
design and reusable factor intervention code dominated.  Validation was focused (3--8 tests per
new contract).  Avoidable time came from one forward-price omission and two result-schema key
mistakes; the next bounded repair is to add assertions for canonical scorer keys and authority
geometry structure rather than expand general ceremony.  Both managed runners remained healthy;
all GPU work used the hash-bound queue and no direct competing process ran.

`CIRCUIT_FOCUS`: **PASS** — every receipt advanced routing, weight translation, causal reader
rejection, or explicit residual/final-head computation.

`CEREMONY_BUDGET`: **PASS WITH REPAIR** — experiment execution stayed cheap and checks caught a real
price error, but schema mistakes caused two avoidable outcome-free launches; targeted schema tests
are required in the next implementation block.

`NOVELTY_LESSON_GATE`: **PASS** — the route reused the L15H5 bypass lesson, rejected threshold/DAS
widening, and converted the new static-reader failure into a causal direct-path hypothesis.

Immediate continuation: publish the direct-route receipt, preregister final-state removal/swap, and
implement its pure final-head intervention contract before opening outcomes.
