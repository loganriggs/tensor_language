# Fresh confirmation at native attention8

Frozen40sequences/20context-city-pair cells/240endpoint rows, ten new authored
constructions and Edinburgh/Denver plus Bristol/Atlanta. No prior prefix overlap;
cities held out from the native8 screen, not globally unseen. Six old endpoints.
Program weights, token table, destination policy, half strength and null seed
formula remain fixed. All arms apply at actual attention8 output, recomputing
MLP8 and every downstream path. No conditional head9-only propagation.

Native, full-city parent, retained face, half-face midpoint,16matched random
half-write directions:800bodyforwards,300seconds. Parent additionally donates
current value; face donates both keys and inherited value, retaining current value.
Norm-matched nulls use existing head8 output-subspace procedure and paired signs.

pred_a: finite, outside support zero, independent face identity and null norm
errors<=1e-5,800forwards. pred_b: >=18/24native capable pairs per family (gap>=.1)
and parentRMS>=1e-5. pred_c: face predicts parent <=.35relativeL2 per family
AND per endpoint. pred_d: midpoint attenuation positive>=.75 and mean>=.02
per family. pred_e: midpoint target >=2median16nulls and beats>=15/16perfamily.
pred_f: each of four unrelated reader RMS<=.5target per family.

This tests native application transfer with the coupled MLP8 response intact.
Opposing prediction: selection or conditional routing hid city/construction
sensitivity, and either parent prediction or matched-null selectivity fails.
No gate/row edits after scores. No composition, token-only or standalone suffix
claim. Constant/fitted predictive comparisons for this broader intervention are
not included and remain an OOD evidence gap; earlier conditional baseline wins
do not transfer automatically. Historical failures remain visible.
