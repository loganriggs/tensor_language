# HOURLY STRATEGIC REVIEW — 2026-08-31 08:35Z

Convention (§2135): all damage numbers are CE added above the real model; LOWER IS BETTER.

## Where the program stands
Goal: reverse-engineer bilin18 into a predictive, manipulable, simpler tensor program.
Strict ledger: 5.348% / 10.923% coverage-credit variants, 4.727 nat, 0 of 68 modules fully closed (registry-
grade; the aggregate frontier moved but module closures are unchanged).
Since the 0733 review, the biggest hour of the program:
- **NEW FRONTIER, REGISTERED AND HELD (§2217):** the CP-front global config at L2_F fresh +2.0553 (previous
  +2.6662), increment in band, ridge-exact reproduction, 184M fewer stored values.
- Cross-type interactions ~0 (§2214: J(a0,cf3) = exactly 0; measured lattice predicts the 6-front to 0.013).
- Validity dies at pairs, uniformly — every pair keeps exactly 2/62 (§2215).
- **Direction chapter founded (§2218):** four ORTHOGONAL drifts produce ONE circuit-damage profile
  (median pairwise Spearman 0.961) — the certified circuits share a common vulnerable subspace.

## Largest gaps (re-ranked at the new frontier)
1. Tail-attn dictionaries: +0.5045 increment — now the largest single family cost in the frontier config.
2. The front×motif interaction (~0.9 census eaten; motif alphas never refit under the CP front).
3. The direction problem (certificates pinned ≤ 2/62 for every multi-site config; shared subspace unnamed).
4. m16 remainder / attn5 price cliff (standing).

## Candidates (pruned by information gain / falsifiability / GPU cost / redundancy)
1. **Trajectory-target tail dictionaries (rung 127)** — the §2210 move applied to the +0.5045 increment
   inside the exact frontier build; zero price. → EXECUTED (queued).
2. Rungs 125/126 in flight (motif×front interaction isolation; motif-α refit — if 126 holds, the frontier
   moves again at zero storage).
3. Combine-best rung 128 (CP front + refit alphas + trajectory tails) — pending 126/127 outcomes; the
   natural frontier-consolidation run.
4. Name the shared vulnerable subspace — the direction chapter's instrument (candidate: regress the common
   damage profile on circuit covariates ref/class/depth, then find the stream direction whose removal from
   the drift predicts per-circuit damage). Math-review slot (~09:37) owns the design.
5. Per-class tail decomposition at the new frontier — subsumed by 127's outcome; deferred.

## Decision
127 queued (depth 2 behind 125/126). If 126 AND 127 both hold, rung 128 consolidates them into a registered
frontier run — plausibly L2_F < 1.8 at unchanged storage. The direction instrument remains the deep
unsolved thread; it gets the math review.
