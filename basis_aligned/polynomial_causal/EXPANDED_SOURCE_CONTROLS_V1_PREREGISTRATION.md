# Prospective output-control expansion

Freeze five lexical contrasts in EXPANDED_SOURCE_CONTROLS_V1_BINDING.json before their model outcomes. Use frozen swap5source directions and opened OODv2 inputs; outputs are new controls, inputs are not freshOOD. No optimization or refitting against these controls. Replay the old four outputs to1e-10float64 to validate the extracted shared context builder before interpreting new outputs.

Primary single-axis screen: s=1,t=0 ands=0,t=1. For each of8panel/familycells andeachrole, every newcontrol L2effect must be<=10% of that role's frozen number-effect L2norm. Retain absolute effects and small-denominator failures. Compare old modal controls at the same10%bar. Additional signed/half-radius and joint/conditional increments are diagnostics on the same16coordinates; report separately. Never hide a failure under the large subjectbudget. The five pairs are heuristic number-invariant controls, not guarantees of identical conditional lexical distributions or a universal preservation benchmark.

Implementation underway: shared frozen_two_site_context.py replaces copied source/context setup for new runners. Syntax validated and tokenizer binding executed; native helper replay remains required. Existing frozen runners remain intact. No new-control outcomes have been read.

Execution detail before outcomes: score primary gates using native float32 effects, with float64 diagnostics and <=1%rolebudget numerical discrepancy. Counts12prefix68float64suffix12float32suffix. Existingoutputs/allcoordinates replay1e-10 verifies sharedhelper. Three gates: instrumentation,newcontrols,oldcontrols.
