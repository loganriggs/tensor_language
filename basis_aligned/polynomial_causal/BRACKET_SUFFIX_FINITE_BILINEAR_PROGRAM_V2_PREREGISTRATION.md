# Bracket suffix finite bilinear program V2

V1 completed its frozen 12-forward experiment and serialized `invalid` solely
because its liveness conjunction included the untouched `exact` reference arm.
That arm removes no MLP response by definition, so its recorded active fraction
is exactly zero; every causal candidate arm has active fraction one, replay is
exact, and the independent FP64 finite identity closes below `2e-25`.

V2 changes only the instrument conjunction to require liveness for causal
candidate arms and exclude the non-intervened exact reference. Bind the V1
result before execution. Preserve the exact donor source intervention, frozen
recipient states, online MLP13--17 decomposition, candidate order, selection
rule, behavioral bars, control bars, rows, conditional confirmation, and price.
The opened V1 measurements may not change any causal arm or verdict rule. No
coefficient, fit, rank, gain, gradient, update, quantization, subgroup, or new
construction is allowed.
