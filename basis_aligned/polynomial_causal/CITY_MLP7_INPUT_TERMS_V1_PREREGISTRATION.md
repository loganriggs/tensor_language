# Open the folded MLP7 readers into earlier sources

CPU-only, twenty opened Pile document pairs. Write u7=r+i+a, where r is the
lambda7,0-weighted residual6 source, i=lambda7,1*initial, and a=attention7.
Recover mixed7 and attention7 from saved source7 captures by dividing lambda8,0;
derive initial from the weight table, and r=mixed7−i. Rounding residue remains
part of this recovered source; do not claim an independently captured residual6.

For each ordered pair(s,t) among r,i,a, compute
C[(Ls)*(Rt)]/(mean(u7²)+epsilon), plus the separate folded bias c.
Keep all nine products, not just the three squares or one cross order. C is the
384×4608 matrix spanning full head8.2K1/K2/currentV readers, not the historical
four selected MLP8 eigenreaders. No fit, rotation or weight-magnitude selection.

Predictions: a, sum of nine terms+bias equals the FP64 full reader formula to
<=1e-10relative. b, full expanded reader values passed through the integrated
city-write helper reproduce the native complete city write<=1e-4relative on all
40sequences. c, omitting the six ordered cross terms causes>=.10 relative error
in paired UK−US reader contrast separately for all three128-dimensional readers.
d, that self-only reader approximation causes>=.10 aggregate native city-write
error. c/d are falsifiable necessity screens, not post-hoc adoption gates.

Report signed aligned fractions and norm ratios, with twenty effective paired
units. Full and self-only writes use the SAME native query factors, mixed8 RMS
and key RMS scalars. Thus omitted-cross effects here are conditional local-write
errors, not final behavioral effects or recomputed-normalizer interventions.
This closes no native state dependency; it specifies the next upstream input
terms that any smaller executable replacement must retain or test causally.
