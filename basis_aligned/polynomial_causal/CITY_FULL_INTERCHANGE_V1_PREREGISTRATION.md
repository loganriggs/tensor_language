# Selective interchange of the complete city-write interface

The full city-removal generator has passed fresh conditional prediction and selective
removal. Attempts to promote MLP7-present, self/mixed, or key/value subgroups as clean
directional components failed; freeze that lesson and stop further partition searches
without new evidence. Test a different manipulation of the supported coupled interface.

On each paired UK/US city-substituted sequence, replace head8.2's city-side K1,K2 and
mixed current/inherited V by those from the paired donor at the same city position.
Keep both recipient query fields and the native recipient background/suffix. Output
is donor-city write minus recipient-city write at the existing destination mask.
This is neither whole-head donation nor token replacement; donor and recipient native
prefixes remain external. Both QK factors and the complete value branch move together.

Prediction uses the frozen full-nine-head, five-reader residual6 generator on BOTH
recipient and donor residual6 sequences. Generate fields internally with the existing
approximate mixed8 RMS, retaining all numerator terms. No fit or attention7 omission.
Interface price: two logical residual6 sequences,73,728native floats for a pair atT32;
same shared weights plus weight-derived token tables. Do not call this a one-context
or token-only executor merely because a pair can be packed into one array.

First opened CPU preflight on CITY_FULL_PILE_V2: self-removal from the factored field
helper equals frozen generator writes<=1e-10relative; native-field self-removal equals
captured native full-city writes<=1e-4; all generated swaps finite/support0. Local swap
approximation error is reported, not used to select documents or tune weights.

Then freeze next20eligible distinct Pile documents from the existing outcome-blind
selector, excluding all frozen document hashes/inputs.40sequences/240fixed spelling
probes, samecity/endpoint vocabulary, not pretraining-disjoint or true next-token labels.
Extend only checkpoint-derived token tables and verify shared entries/non-table weights.

Native capture records actual attention8 Q1/K1/Q2/K2/V and residual6, then installs
native, native-reference swap, generated swap,16same-site per-position norm-matched
random edits. Freshseed18097000+1000*k+context_id, pairedseed/oppositecue signs.
CPUtwo threads,600secondcap;19installedbatches=760sequence-equivalent forwards,
342blockcalls plus40capture/18calls,800total/360calls. FullnativeMLP8/suffix recomputed.

Gates:
a.20disjointdocuments/40sequences/240probes, finite/support0, query/self-removal
   replay<=1e-4everysequence, nullnormerror<=1e-5, requiredcalls,CUDAuninitialized.
b. Generated-swap target-effect relativeerror<=.35versusnative-reference swap,
   targetRMS>=1e-5 and>=90/120native capablecontrasts (UK-USmargin>=.1).
c. Each of four unrelated-reader RMS movements<=.5targetRMS.
d. Positive UK-US contrast attenuation>=.90 andmean>=.02oncapablecontrasts.
e. TargetRMS>=2randommedian andbeatsall16nulls.

Report donor/recipient direction and untouched/substituted prediction errors, preserving
reversed documents. Success supports interchange at a conditional coupled city interface;
it does not establish independent composition, full-model sufficiency, or matched-effect
simplicity. A failure remains a failure and does not weaken earlier removal evidence.
