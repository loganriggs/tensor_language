# HOURLY STRATEGIC REVIEW — 2026-09-01 01:30Z

Convention (§2135): all damage numbers are CE added above the real model; LOWER IS BETTER.

## The fork in progress
§2381: the full-rank, values-real config still costs +0.0520 through the mixed-lineage replacement path —
the registered null won. If rung 282 (independent corner lineage, running) agrees within 0.01, the
§2348–§2377 chapter gets corrected: the "fine-band mechanism," its binary/global structure, the 0.984
collinearity, and the 11-certificate cap are dominated by CONSTANT PATH ERROR, and every registered census
number contains ~0.05 of instrument. Rung 283 (harness null) bounds harness leakage. Nothing publishes
until both land (flip-control rule). Roles: Codex holds direction (its rung-280 CPU basis audit is
registered); I hold red-team/ops and the ledger.

## What changes under each outcome
- PATH CONVICTED (282 agrees): registered Pareto set needs path-corrected companion numbers; the "floor"
  becomes an instrument floor; the top move becomes ENGINEERING the replacement path to numerical
  faithfulness (bf16-matched ops), after which the true rank/certificate curves get re-measured. Much of
  the model may be far more compressible than the ledger currently says.
- LINEAGE BUG (282 disagrees): §2381 becomes a mixed-lineage defect; corner-lineage results stand; hunt
  the bug in the all_last8 build chain.

## Top five
1. **Path decomposition (rung 284, EXECUTED)**: full-rank config with factor matmuls replaced by DIRECT
   weight-slice matmuls inside qkz (same zero-structure, same rotary/rms/einsum recompute). census ≤ 0.01
   → the factor arithmetic was the culprit; null ≥ 0.04 → the recompute structure (fp32 rotary/rms/einsum
   vs the native bf16 block) carries it. Either way it points at the exact fix.
2. Rung 282/283 (in flight) — the adjudicators.
3. Path-corrected accounting (CPU, needs 282's corner-lineage path vector): d_config − d_path per lineage,
   then re-scored certificates. Next wake.
4. Codex's rung-280 basis audit rerun against cev_pathfull*.pt (theirs).
5. Re-measured rank curve on a faithful path (blocked on 1's verdict).

Pruned: any new compression/allocation rungs (all census numbers suspect until the path question closes);
repair-direction fitting (§2377's direction is probably the path's).
