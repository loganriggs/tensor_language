# Shared MLP15 producer coordinates for two frozen branches

This tests whether a common upstream interface preserves the computational effects of the already frozen parent and partners3/8. It is not a rank-only success criterion. Prior late-MLP dossiers and MLP16 producer/routing folds are known; this is the MLP15 mixed-source boundary from the latest source test.

Let native quadratic producer atoms have coefficient Gram G and output map D. For re-entry scale lambda, H=lambda^2 D G D^T is the coefficient metric of the nonconstant producer output. Bias is retained exactly. For the three frozen symmetric input forms A_j, normalize each map M_j=A_j H^(1/2) by its Frobenius norm, preventing arbitrary quadratic scale from choosing the shared space. Choose orthonormal P to minimize sum_j ||M_j(I-PP^T)||_F^2. Leading eigenvectors of sum_j M_j^T M_j give the global optimum for this fixed linear interface objective. This is not globally optimal quartic decomposition or a text-distribution metric.

Main rank128, predeclared capacity diagnostic512, Gaussian-QR random128control seed73130. All are selected from weights before reading endpoint effects. Producer approximation is bias + H^(1/2) P P^T H^(-1/2)(producer-bias). The background remains exact, and native RMS16 and RMS17 are explicit external ports. In particular, this tests numerator extraction with fixed native normalization; it does not eliminate the original producer computation required to supply those ports.

A: planted exact common-space control<=1e-10, projector orthogonality<=1e-10, spectral optimum tail identity<=1e-10, fullrank source and branch3/8 replay<=1e-5. B: main shared128 preserves bothbranches' full native swap effects, symmetricrelativeRMS<=.1/sign>=.9/live>=4 ineach of3activefreshfamilies, and meanabs removal CE disagreement<=.02. C: shared128 total physical branch-write error beats random128 by at least20%. Bothbranches and quotedcontrol always reported; rank512 is diagnostic, never repairs B. Null: common weight-derived producer coordinates fail to preserve the branch computations, or are no more informative than a generic equally sized interface.

Use frozen128freshendpoints, no bodyforward and no datafit. Both main and diagnostic retain the same bias and normalization/background ports. At rankk a two-map interface stores2*1152*k coefficients; compiling read into producer Down stores4608*k readcoefficients plus1152*k outputcoefficients, plus full native MLP15 Left/Right (2*4608*1152), bias and downstream packedprogram1,994,688floats. This is a conditional grouping/extraction test, not an end-to-end compression claim. Three-form sharing saves duplicate interfaces only if behavioral fidelity holds. Managed GPU limit300seconds, exact eigen decomposition; no unconverged optimizer or additional restarts.
