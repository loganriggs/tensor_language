# Native descriptive-continuation versus instruction source split

On the frozen 48 fresh rows, split the already supported framing sources at the
first period after the changed city. `description=(city,period]` and
`instruction=(period,quote]`; their union is exactly the prior framing mask.
The five arms are native, remove description O, remove instruction O, remove
framing O, and remove all O. Read target, work/jobs, cat/dog, red/blue,
Monday/Tuesday and apple/orange margins. This is 240 body forwards with a
180-second cap.

- `pred_a`: description plus instruction reconstructs framing sources within
  `1e-10`; native, framing and all-O target/work-jobs arms replay the frozen
  fresh artifact within `1e-5`; all values are finite and exactly 240 forwards
  execute.
- `pred_b`: native target cue is positive for at least 10/12 pairs and framing
  target-effect RMS is at least `1e-5` in each template.
- `pred_c` (instruction-source hypothesis): instruction-only paired cue effect
  differs from all-framing by at most 35%, while description-only differs by at
  least 50%, in each template.
- `pred_d` (opposing description-source hypothesis): description-only differs
  from all-framing by at most 35%, while instruction-only differs by at least
  50%, in each template.

All four new-control ratios and separate-effect composition errors are reported
for both roles without changing C or D. If neither opposing prediction passes,
the framing effect is distributed or template dependent. These are source
removals inside head9.8 with native upstream descendants and suffix; no unique
semantic unit, independent input generator, corpus OOD, static compression, or
quantization claim follows.

