# MLP8 source by attention9 response factorial

All32original/fronted worlds from the unchanged role-aligned rowbank. SourceS0 is
native; S1 subtracts Qoh of actual full MLP8 output at every token. Capture full A9
write A0/A1 under each source. FirstV must stay bitwise equal: MLP8 is downstream
of first-attention value production. Preserve that tuple member when clamping A9.

Execute all four F_s(A_t), s/t in0,1. Prefix and residual background follow s; A9
write is installed from t; all later native operations recompute. Both diagonal
identity clamps replay their directly captured source runs; native/source-edited
runs replay MATURE_VALUE_MLP8_CONSUMERS_V1 baseline/full arm. No position/head subset.

Define total=F00-F11, attention=F00-F01, bypass=F00-F10,
interaction=total-attention-bypass. Attention is the effect of changing A9 response
with native source; bypass changes source with native A9 response held. Neither is
assumed background independent. All outputs and the source-by-attention interaction
are retained; the existing CPU coupled control rejects naive effect subtraction.

A instrument: finite/cleanup/counts, firstV unchanged; captured MLP8 incoming output
bitwise for every source patch; captured source-specific A9 incoming output bitwise
for every clamp; source/attention identity controls. Captured baseline/full source
and both diagonals replay with BOTH maxabs<=1e-3 and relative<=1e-5. Fourcapture
forwards plus8cube forwards perworld=384forwards6144sequence instances,0fits,600s cap.
No extra native factor compilation needed for this whole-output mediator.

B attention dominance: attention differs from total<=.10 relative in both Qoh correct
margin and centered Qoh three-readers, AND total effect norm>=.10 native mixed norm
in both, EVERY world. C bypass dominance: same for bypass. D small interaction:
interaction/total<=.10 in BOTH mixed and full-table versions of the two readout objects,
EVERY world. Zero scientific denominators fail, not an instrument failure.

A Bpass supports the whole attention9 response as a dominant mediator of this source
intervention, not the previous partial value component. Cpass supports routes bypassing
that response; it does not name them. Neither means retain both/interaction. No winner
from smaller error, no threshold/row/head/gain rescue. No semantic/OOD claim from opened
worlds. All545902902nativeweights, counterfactual inputs and suffix retained; saving0.
