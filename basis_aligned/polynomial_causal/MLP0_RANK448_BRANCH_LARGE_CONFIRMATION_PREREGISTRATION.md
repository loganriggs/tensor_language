# Rung 404 preregistration — large-document confirmation of rank-448 branch damage

Date: 2026-09-01 15:50 UTC
Convention: CE added above native is damage; lower is better.

## Purpose

Rung403 validly found that the fixed rank-448 MLP0 program's error is led by the centered token×context branch `I`,
not the prospectively predicted token-main branch `T`. On 96 held-out SELECT documents, five-factor Shapley damage
was `I=.00581 > T=.00190 > C=.00006 > S=-.00201`, auxiliary `=-.00012`; the named ordering matched FIT at Spearman
1.0. This routes toward a global shared-input projection trained to preserve the distributed interaction metric.

The user requested more documents when compute is cheap relative to research latency. Before changing the compact
objective, confirm the branch conclusion on four times as many independent source documents. This is a confirmation
of the fixed result, not a new selection population or a rank sweep.

## Frozen population and computation

Use the previously frozen, outcome-blind FineWeb authority
`.rowcache_mlp0_c512_mlp1_interchange_v1/eval_384_source_documents.pt` and its row receipt. It contains 585 chunks
from 384 source documents beginning at pinned dataset document index 43,000 and is registered disjoint from prior
source documents, exact rows, and 32-token prefixes. Select exactly the `chunk_id=0` row from each source document,
then positions `0:256`. This yields exactly 384 rows, partitioned prospectively by source-document ordinal into four
contiguous 96-document waves. Score positions `64:256`: 73,728 confirmation targets, four times rung403 SELECT.

Keep fixed:

- rung328's float32 fit-A p448 reconstruction, rank, rows, and 9,954,432-value price;
- rung401's BF16 evaluation model and FIT-derived product-reference moments;
- rung403's exact delta-T/C/I/S/A definitions and complete 32-arm physical factorial;
- no FINAL rows, no refit, no rank/bar/subspace choice from confirmation outcomes.

## Frozen predictions

### A — population, fixed program, and instrument are exact

- Frozen row tensor hash/shape, receipt, one-row-per-384-source-document rule, four 96-document waves, and
  disjointness gates hold.
- p448 retained energy exactly matches `0.9011108875` within `2e-6`; shapes/rank/price hold.
- On every wave, native and compact identities are at most `1e-8`, endpoint state errors and state replay errors are
  zero, and all 32 arms have the exact live call census.

### B — interaction damage dominates at larger scale

- `I` is the largest named Shapley damage on the pooled 384 documents and on each of the four fixed waves.
- Pooled `I` exceeds pooled `T` by at least `.0015 nat`.

### C — the routed branch transports from rung403

- Pooled `I` is positive and supplies at least 60% of the positive named Shapley total.
- The pooled named-branch ordering has Spearman at least `.80` with rung403 SELECT.
- `I` is positive on all four waves.

### D — result is not numerical closure or one unstable wave

- Total p448 damage is in `[0,.020] nat` pooled and positive in every wave.
- Absolute pooled and per-wave auxiliary Shapley damage is at most `.002 nat`.
- The range of the four `I` Shapley estimates is at most `.008 nat`.

## Strong null and decision

The strong null fires if A fails; pooled `I<=0`; pooled `I` is not top; at least two waves have `I<=T`; pooled
named transport Spearman is nonpositive; or auxiliary magnitude is at least the sum of named magnitudes.

A/B/C/D with null false confirms the interaction-weighted global projection as the next screen. Partial transport
with null false requires bootstrap/another corpus before changing the projector. A null retains the ordinary p448
program and blocks branch-targeted optimization. No result licenses a new compressor or adoption point by itself.

The subsequent interaction-weighted screen must remain global enough to share factors across branches and should
compare against ordinary p448 at identical rank and price. A residual-stream subspace jointly written by multiple
heads is a candidate metric, not a commitment to head labels.
