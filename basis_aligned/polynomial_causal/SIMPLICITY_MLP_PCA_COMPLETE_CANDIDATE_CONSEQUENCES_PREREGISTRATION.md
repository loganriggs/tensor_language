# Rung450 preregistration: complete mixed104 MLP-PCA teaching consequences

Status: registered before any MLP-PCA candidate is run on TEACHING. GPU execution must use the managed queue;
SEALED_CONFIRMATION is forbidden.

## Candidate family

Rebuild the exact seven rung445 complete candidates, not local projector-only substitutes:

- rank256 output projectors at layer pairs `{0,8}`, `{0,17}`, and `{8,17}`;
- fixed pair `{8,17}` at ranks384 and512;
- same-price `{8,17}` rank256 hybrids replacing32 or64 ordinary activation-PCA directions with directions selected
  by the frozen circuit-certificate loss gradients.

Each candidate includes the full mixed104 compiled program charged by its frozen price. The first, third, sixth, and
seventh candidates cost531,927,350 scalars; rank384 costs533,401,910 and rank512 costs534,876,470.

## Consequences

Use the identical96-row TEACHING role, original-native attention16 mean knockout, and physical14,984-value MLP16
partner as rung449. For each complete candidate P collect native `N`, native knockout `NKO`, native partner `Q`, P,
P+knockout, and P+Q per-token cross entropy. Removal normalized error is
`||(CE_PKO-CE_P)-(CE_NKO-CE_N)|| / ||CE_NKO-CE_N||`. Composition normalized error is the error of the additive
prediction `(CE_P-CE_N)+(CE_Q-CE_N)` for actual `CE_PQ-CE_N`. Report full data and fixed48-row waves.

Unablated, knockout, and partner conditions run as fresh child processes inside one managed parent so permanent
legacy compiler hooks cannot cross conditions. Every child must independently reproduce candidate layer/rank maps,
rank104 Q/K factors, and the complete active replacement set.

## Predictions and null

- **A — instrument:** all frozen hashes, seven IDs/prices/maps, three complete mixed104 identities, original native
  vectors, and exact intervention liveness hold; SEALED remains unopened.
- **B — fixed-rank-ladder consequence ordering:** within `{8,17}` rank256→384→512, both removal and composition
  normalized errors strictly decrease (three-point Spearman with negative error equals1).
- **C — teaching variation:** across all seven arms, both error spans are at least`.015`.
- **D — document stability:** across all seven arms, wave1-versus-wave2 ordering Spearman is at least`.70` for both
  removal and composition error.

**Strong null:** an identity/instrument clause fails; native removal, partner, or every candidate is inert; both spans
are below`.003`; or either seven-arm wave correlation is negative.

A/B/C/D with no null counts MLP-PCA as complete-candidate teaching family2/3. This does not fit a predictor, open
attention0, establish semantics/OOD, or adopt a compressed model. New deployed values are zero.
