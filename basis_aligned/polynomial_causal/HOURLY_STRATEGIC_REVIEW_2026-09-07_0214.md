# Hourly strategic review — 2026-09-07 02:14 UTC

## Circuit targets and controlling goal

A completed circuit must identify what is read, computed, written, and consumed; merge or split
native modules according to causal function; predict held-out and OOD effects; support extraction,
removal, swaps, and edits; compose shared and task-specific branches; and remain stable across
documents, constructions, controls, gauges, and starts. The controlling goal remains a smaller
transparent tensor program that is jointly predictive, composable, manipulable, and cheaper in
literal storage, compute, state, edges, and intervention price. Neither a low-dimensional basis nor
task loss alone is completion.

## What changed since 01:14

The gauge-safe weight pullback was converted into causal evidence. Complete L6/L7/L9 response
patches gave weight-versus-causal Spearman `.452/.330`, recovered known L9H1/H4, and found a
causal L7 split, but also exposed high-weight L6H7 as inactive. The outcome-new 32-arm writer
factorial then rejected a simple additive account: L7H8 failed standalone is/was transfer and the
union interaction reached `-.385/-.441`. Its exact lattice nevertheless selected a four-group
conditional program, L7H8 + L9H1/H4 + MLP7 + MLP9. On 28 disjoint sealed rows this program passed
all gates: mean four-cell canonical residual fell from `.13696` for L9 alone to `.06896` without
L7H8 and `.04442` with it; the frozen L7H8 increment reduced is/was mode-2 residual by `39.20%`.
This identifies L7H8 as a conditional member of a serial/distributive writer, not a standalone
semantic head.

The complete downstream atlas removed all 54 heads and six MLPs in layers 12--17 under that writer.
Static weight score modestly enriched causal necessity (`.335/.211` for modes 1/2) and correctly
identified L15H5 as a shared reader (roughly 5--9% signed necessity); L15H1 was consistently smaller
at roughly 1--2%. The preregistered novel L17H4 prediction failed decisively: its best positive cell
was `.0050` and its other effects were negligible or opposing. Causal mass instead concentrates in
MLP12--17, L15H5/H1, L13H6, and small mode-2 L17H2. Raw weights are therefore an incidence prior
requiring activation and finite-sensitivity conditioning.

The six-task constrained-DAS line also sharpened the user's overfitting diagnosis. Starting from
difference in means did not preserve its better quantifier extraction (`.824` DIM versus `.752`
DIM-start constrained), although different starts reached different projectors. A swapped-split
replication rejected apparent dative/quantifier complement closure. Cross-task inert controls do
improve specificity, but a weighted penalty may legally trade away target sufficiency and small
row splits give unstable complement bounds. The next objective must impose target retention as a
hard multi-family feasibility constraint before minimizing collateral; another lambda or KL sweep
is not licensed.

## Confounds and route decision

The highest-information route remains executed circuit composition. The ten-site downstream pool is
fixed solely by the complete atlas (`>=.005` absolute pooled necessity in behavior or either mode),
and all 1,024 cached-response subsets will be run. This directly handles nonlinear/serial
composition rather than adding singleton effects. Its result is discovery evidence only: the pool
and selected mask use the same opened rows and require genuinely fresh confirmation. Cached live
responses are an extraction interface, not yet a literal weight program; promotion requires weight
translation and a fresh end-to-end intervention.

Confounds audited:

- baseline subtraction and tiny denominators: task-mode vector residuals and signed projections use
  full writer-minus-base vectors; exact native/self/live replay gates are present;
- frame mixing and the 99.486%/0.513% mode imbalance: temporal and is/was cells remain separate and
  both task-balanced canonical modes must pass independently;
- nonlinear composition: all 1,024 subsets are executed with later model computation, never inferred
  from singleton sums;
- post-selection: the lattice is explicitly discovery; its selected subset cannot be identified
  until frozen on fresh rows;
- decode-versus-cause and dead weight incidence: L17H4 is preserved as a false positive, and causal
  response—not weight rank—sets the composition pool;
- native boundaries: complete head/MLP responses are current carrier units, with conditional L7H8
  already showing that semantic membership crosses and depends on native boundaries.

Alternatives remain causal-response/Hankel realization, predictive-state quotients, tangent/Sobolev
matching, and hard-constrained multi-environment DAS. Hankel modes already supplied the shared
interface; another basis fit cannot replace composition. The exhaustive ten-site lattice dominates
now because it directly decides extraction and grouping at modest cost. Hard-constrained DAS follows
only after this circuit receipt, and is killed if no direction simultaneously retains the frozen DIM
target floor and satisfies held-out family collateral constraints.

## Throughput and ranked next moves

Ten result artifacts landed between 01:14 and 02:14, including two exhaustive causal atlases, one
32-arm writer lattice, a sealed confirmation, and six DAS/head-set screens. Median adjacent result
spacing was about 133 seconds; GPU work remained serialized and continuously occupied. Bespoke
validation did not dominate scientific work; the 60-site atlas cost 71 forwards and completed in 22
seconds. The next 1,024-arm lattice is a promoted composition test, not a basic screen or compression
sweep.

`CIRCUIT_FOCUS: PASS` — the hour advanced causal writing, conditional grouping, sealed prediction,
downstream consumption, and selective response removal.

`CEREMONY_BUDGET: PASS` — shared capture/patch/gate machinery kept authoring and checks below the
scientific causal work; elevated 1,024-arm cost is justified by exact nonlinear composition of a
passed ten-site pool.

`NOVELTY_LESSON_GATE: PASS` — prior DAS, module-removal, and read-versus-write failures were applied;
L17H4's null changes selection from raw weight rank to activation-conditioned causal necessity.

Ranked moves:

1. **Execute the complete ten-site downstream response lattice.** Targets: extraction,
   cross-boundary grouping, and composition. Kill the pool if its full mask misses any frozen
   task/output residual; record a distributed chain if no <=8-site program passes.
2. **Freeze the selected upstream+downstream program on fresh construction rows.** Targets: held-out
   prediction, stable identification, and selective manipulation. Kill adoption on any failed
   task-mode or behavior cell, without reselection.
3. **Translate retained response components through activation-conditioned exact weights.** Targets:
   computational specification and reusable tensor-program execution. Kill a proposed edge when its
   exact local write and finite downstream sensitivity do not reproduce its cached-response effect.
4. **Test lexicographic/Pareto constrained DAS across complete families.** Targets: stable subspace
   identification and selective manipulation. Require target performance near DIM first, then
   minimize own-complement and cross-task collateral; infeasibility is the honest null.
