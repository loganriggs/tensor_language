# Native city value-branch factorial after fresh sign failure

Prior CITY_REMOVAL_ENDPOINT_FRESH_V1 pred_d failed: plain-note positive fraction
.75<.90; exact native inherited removal shares the failure. Do not discard cells
or loosen that gate. This is a different hypothesis about the full city write.

Panel: all20now-opened cells/40sequences/240rows from new-endpoint panel.
Native head8.2 route P=QK1(city)*QK2(city) frozen by the unedited forward.
d_i=-.5 W[P*lambda*V0(city)], d_c=-.5 W[P*(1-lambda)*V8(current_city)].
Four native attention8 arms: native, inherited(d_i), current(d_c), both(d_i+d_c).
All downstream layers recursively recomputed.160bodyforwards/300s.
No surrogate, donor, or fitted replacement. Same after-city destination support.

pred_a: native and inherited readouts replay prior fresh[0,1]<=1e-5maxabs;
finite, outsidezero,160forwards. Price160native forwards; no extracted-program
storage reduction claimed.
pred_b: BOTH removal attenuates>=.90of capable pairs and mean>=.02 in EVERY
family; >=18capable pairs per family with native cue contrast>=.1.
Opposing predictions: current-value source supplies the missing directional
signal versus full city source itself remains context-dependent.
pred_c: CURRENT removal targetRMS>=1e-5 and fourcontrol RMS<=.5target in EVERY
family. Record same controls for inherited/both descriptively.
pred_d: target interaction ||e_both-e_i-e_c|| / min(||e_i||,||e_c||)<=.35
EVERY family, both singles RMS>=1e-5. If it fails, preserve coupled semantics;
if it passes, this is near-additivity only until random-split specificity.

Report every cell's native margins and each arm's attenuation, especially cells8
and16 nominated by opened diagnostic; do not use them as independent confirmation.
No fresh/OOD/selectivity-null/composition promotion from this basic factorial.
