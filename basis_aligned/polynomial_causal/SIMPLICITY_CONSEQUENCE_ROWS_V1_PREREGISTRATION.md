# Rung447 preregistration: teaching and sealed consequence row authority

Status: registered before the row namespace exists. This is an outcome-blind CPU row remap. It loads no model,
candidate consequence, or candidate program.

## Purpose and scope

Rung445 froze45 candidate programs:35 candidates in three teaching families and10 attention0 candidates in a
sealed fourth family. Before measuring removal or composition, freeze document-disjoint row roles so the sealed
family cannot be evaluated on documents used to fit the consequence predictor.

The rows are fresh for these candidate-specific consequences, not globally unused. They come from authoritative
FineWeb row pools previously used only for unrelated MLP2 experiments. This limitation is explicit; a later OOD or
final adoption claim needs its own untouched corpus.

## Frozen sources and slices

- `TEACHING`: rows96:192 of `mlp2_rank512_refit_v1` EVALUATION.
- `SEALED_CONFIRMATION`: rows96:192 of `mlp2_trajectory_robust_r512_v2_physical_eval` EVALUATION.

Each role therefore contains96 rows of257 tokens and is divided into two fixed48-row reporting waves. Parent receipt,
file, and tensor hashes are embedded in the freezer source. The rung445 bank SHA must be `e35d5c0a…`.

## Frozen predictions and null

- **A — authority and closure:** exact bank/receipt/file/tensor hashes match; the freezer source is committed and
  byte-identical to its Git blob before execution; neither output cache nor receipt exists beforehand.
- **B — role separation:** each role is int64 `[96,257]`; full rows, first32-token prefixes, source-document IDs, and
  dataset indices are unique within role and disjoint across roles.
- **C — candidate-fit separation:** both roles have zero complete257-token and first32-token overlap with every
  FineWeb rowcache named as a required artifact in the rung445 candidate bank.
- **D — deterministic publication:** slice tensors and provenance exactly replay their registered parent slices;
  cache files and the receipt are create-only and hash-pinned; outcome access records model loaded false and
  candidate consequences loaded false.

**Strong null:** any failed input hash, pre-existing output namespace, role overlap, candidate-fit row/prefix overlap,
or source-closure failure. Under it, publish no row authority and register a new source pool.

A/B/C/D with no null licenses a separate consequence-harness preregistration with exact target, collateral, CE,
composition, and numerical bars. It does not license candidate scoring, predictor fitting, opening the sealed family,
compression, or adoption.

Literal price: zero deployed model values and zero native calls. Claim level: prospective row instrument.

