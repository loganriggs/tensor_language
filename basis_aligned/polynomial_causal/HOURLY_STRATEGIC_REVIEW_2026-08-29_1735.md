# Hourly strategic review — 2026-08-29 17:35 UTC

## Outcome first

The highest-priority safe action completed rather than remaining a plan: the missing
MLP2 FIT_SELECTOR calibration passed independent audit, executed 48 native forwards
in 16.13 seconds wall time, and published a receipt-last numerical artifact. The
physical K=512 validator is now scientifically and operationally unblocked for
implementation, though its execution still requires a separate frozen authority.

The special eight-hour window ended at 12:00 UTC and was not reopened.

## What fraction of the model is actually explained?

The strict ledger does not move because calibration is not a finite simplification:

| Currency | Current value | Largest gap |
|---|---:|---|
| Structural intervention surfaces | 36/36 | semantics and autonomous interfaces |
| Consequence-certified whole-program storage removal | 5.348245316% | 94.651754684% lacks a strict certificate |
| Named causal CE | 0.57968 / 5.30682 = 10.923302467% | most causal loss remains unnamed |
| Unexplained causal CE | 4.72714 nat = 89.076697533% | dominant quantitative gap |
| Complete terminal actions | 0/68 | extraction/removal/OOD chain entirely open |

The compiler lane now has a strong engineering frontier at both 16,110 and 5,419
coverage. At deployed coverage, `{MLP 768, attention 384}` costs 178.896M values,
22% less than the deployed design, while improving CE and top-1. That is a useful
rate--distortion result, but it is not a named causal mechanism and does not change
the strict ledger.

## Largest remaining gaps and confusing evidence

1. **MLP0--MLP2 compensation remains the largest local interface gap.** C512 is a
   strong executable MLP0 Down compression and composes with the L8 copy gate, but no
   small MLP2 program has survived finite whole-model validation.
2. **Tangent selection may not survive finite joint deletion.** SUFFIX is stable, but
   captures only 25.534% of its leverage score and improves that fit currency over
   LOCAL by only 6.6% relative. Removing 4,096 channels is far outside an
   infinitesimal neighborhood.
3. **The margin distribution has a fragile lower tail.** The 2% native-margin
   quantile is zero at bfloat16 resolution. A universal top-1 certificate may be
   weak even when CE/KL are good; this is why actual agreement and distributional
   metrics remain primary.
4. **Copy has an exact local route but no terminal action.** The L8 H3/H4 successor
   edge and rank-compressed gate are strong, yet fresh extraction/selective-removal/
   OOD validation has not closed any of the 68 terminal cells.
5. **Independent local simplifications still lack a common composition telescope.**
   C512 plus the copy gate is one positive pair. MLP1/MLP2 and broader early-stack
   combinations remain open.
6. **No semantic canonical basis exists.** Native product coordinates are gauge-safe
   under scaling/permutation, but stability is not meaning. SAE/HOSVD/dictionary
   atoms have not yet earned causal or compositional identities.

## Pruning

- More MLP1 native-gate selection remains pruned because it duplicates the completed
  global-gate negative.
- Nearby K sweeps are pruned until the single preregistered K=512 native-product test
  says whether this grammar works at all.
- Raw Down-space MSE, PCA, HOSVD energy, or SAE reconstruction cannot promote a
  program after Family F showed that better local reconstruction can worsen suffix KL.
- More compiler allocation or routing work is secondary: the router is unprofitable
  at both coverages, and the remaining rank-allocation gains are small engineering
  refinements rather than causal explanations.
- Reopening MLP0 token clustering is pruned unless the composition telescope points
  to a specific missing producer coordinate.

## Top five actions, ranked

| Rank | Action | Why it is highest return now |
|---:|---|---|
| 1 | Implement and execute physical MLP2 K=512 validation | Highest causal information gain; directly tests storage, execution, final CE/KL, cells, gauge replay, signed edits, and suffix composability at modest GPU cost. One run cleanly separates SUFFIX/LOCAL tie, SUFFIX win, and native-coordinate failure. |
| 2 | Cross any surviving MLP2 program with C512 and the compressed copy gate | Converts independent successes into a whole early-stack program and measures the compensation/interface terms that dominate the current gap. Depends on rank 1. |
| 3 | If every native K=512 arm fails, learn response-conditioned/balanced factors | Uses controllability-by-edit and suffix observability rather than local energy. It is the principled alternate coordinate system, but more expensive and assumption-heavy than first falsifying native atoms. |
| 4 | Fresh terminal validation of the exact copy-edge program | Best route to the first nonzero 68-cell action: extraction, selective removal, and OOD transport of an already localized exact tensor edge. |
| 5 | Cheap MLP3 native-Down K512 behavioral port | Tests whether checkpoint output directions generally beat locally refitted decoders. It is falsifiable and cheap, but less central than the MLP0--MLP2 compensation interface. |

Across expected information gain, causal relevance, whole-model composability,
falsifiability, and GPU price, rank 1 dominates. Rank 2 is conditional on a survivor;
rank 3 is conditional on collective failure. This prevents both premature basis
synthesis and an uninformative width sweep.

## Action executed and numerical receipt

The calibration published:

- 31,505 eligible FIT_SELECTOR positions across 192 documents;
- a 28-point exact margin-epsilon grid;
- 50,304 FIT_SELECTOR target-frequency counts;
- copy-positive/repeat-negative/nonrepeat counts of 2,420 / 11,224 / 17,861;
- 48 calls and returns, 48 calls at every one of 18 attention and MLP sites, zero
  backwards;
- native margin mean 1.58057, median 0.9375, 90th percentile 3.9375;
- internal runtime 13.3966 seconds and shell wall time 16.13 seconds;
- no validation/replication access, no finite candidate, no raw-logit publication,
  and no failure artifact.

Full hashes and interpretation are in `MLP2_CMR_V1_CALIBRATION_FINDINGS.md`. The next
safe action is physical-validator implementation under the now-authorized calibration
receipt; no user decision or external data is required.

