# Strategic review — 21 September 2026, 05:20 UTC

ACTIVE_TRACK: WEIGHT_FOLDING

The explicit two-day weights-first exploration continues to override hourly alternation. The complete goal remains predictive, extractable, selectively manipulable, reusable, simple and stably identified circuits. Previous turn: progress, because the requested overall review was rewritten and its index updated; no scientific result was added by that rewrite.

Since 04:17, source interchange supported a small quadratic implementation, but a balanced donor family falsified the shared16 relative-fidelity claim. Exact mixed-product blocks then simplified the two original source reads from 4,608 native products to 1,152, with a stronger independent-spectral baseline of 2,304. Native replay passed. Reusing this dictionary across additional reads failed; aggregate error hid a 44% component error. This directly changes the next action: fit the composed weak component separately rather than select by an aggregate score.

Current path: original MLP16 quadratic source reads -> native mode3 normalized final-MLP feature -> fixed writer. The next managed experiment jointly optimizes two rank16 source forms using an exact symmetrized quartic numerator coefficient loss. Native h and earlier normalized z remain inputs; RMS division remains explicit. Eight fits compare coefficient frames, learning rates and initializations. Winners are selected by coefficient loss before native-calibration diagnostics. Five planted structures and independent dense value/gradient checks validate the objective; none establish real-model circuit identification.

Interpretation targets: computational specification is explicit; cross-boundary folding is implemented for selected readers; fresh continuation evidence applies to the leading feature, not the new third component; extraction remains conditional on native inputs; source interchange supplies limited manipulation evidence; broader reuse failed its present screen; stable semantic identity across all fits remains open. The new fit addresses faithful extraction of one component and supplies a test of joint versus separate approximation, not a newly identified semantic circuit.

Confounds: covariance weighting is not the full moment operator or normalized functional MSE; augmented constant and RMS coordinates are constrained at execution; both metrics use calibration centering and parameter preconditioning. Fixed source signs and affine terms restrict the search. Training/calibration probes are not held-out evidence. A low numerator loss can coexist with a poor divided native function. Product counts exclude native input producers.

Alternatives ranked: (1) joint numerator fit now, because the objective is verified and tests a different optimization target; (2) if coefficient improvement fails to transfer, diagnose numerator-versus-denominator weighting and moment geometry on the same states before any promotion; (3) wider arbitrary DAG search remains valuable but is a larger implementation effort without a verified baseline for the present failure. Do not run native promotion for a candidate missing the per-component screen.

Throughput: five-structure/40-fit toy sweep took about 9 seconds; GPU replay jobs took seconds. Mathematical derivation, implementation and requested explanatory writing dominate elapsed time. Reuse the source executor and existing capture data rather than build another framework. Exact category time shares are unavailable and are not inferred.

TRACK_ALTERNATION: OVERRIDDEN by the user's two-day exploration instruction.
TRACK_PROGRESS: PASS — exact source sharing, verified reuse failure and joint quartic objective.
CEREMONY_BUDGET: bounded checks on a new contraction are justified; no new promotion framework.
NOVELTY_LESSON_GATE: PASS — retained donor failure, per-component scoring, accurate least-squares negative control and explicit metric distinctions.

Three-hour math/literature review was completed at 04:35; next due 07:35. The immediate continuation is the audited eight-fit job in the managed queue, followed by interpretation of all registered gates.
