# Rung451 preregistration: independent MLP-PCA consequence-reliability rows

Status: registered after rung450's removal wave-stability failure and before any candidate is evaluated on the new
role. CPU-only row freezer; no model or outcome access.

## Purpose and source

Rung450 remains failed at its fixed seven-arm removal-order bar. A distinct follow-up may ask whether continuous
effect magnitudes and statistically separated comparisons replicate without treating unresolved near ties as a total
order. Before defining that outcome test, freeze an independent192-document role.

Use the entire registry-authoritative EVALUATION role from
`mlp0_c512_mlp2_full512_composition_v2`: int64 `[192,257]`, semantic tensor SHA `ff34039b…`, file SHA `c340a75c…`.
These rows were used by an unrelated MLP0+MLP2 composition experiment, so they are candidate-consequence fresh rather
than globally untouched. Split them into fixed96-document waves only after the exact role is frozen.

## Predictions and null

- **A:** parent receipt/file/tensor, bank, existing teaching/sealed receipt, and committed freezer source hashes hold.
- **B:**192 unique rows, first32-token prefixes, document IDs, and dataset indices; exact int64 shape `[192,257]`.
- **C:** zero full-row, prefix, document, or dataset-index overlap with TEACHING and SEALED_CONFIRMATION, plus zero
  full-row/prefix overlap with every candidate-fit FineWeb cache named by the bank.
- **D:** create-only output and receipt exactly reproduce the parent role/provenance; fixed waves `[0,96)` and
  `[96,192)`; model/outcome access false.

**Strong null:** any hash, uniqueness, overlap, provenance, or create-only clause fails. Passing licenses only a new
tie/uncertainty-aware consequence preregistration. It does not change rung450, count MLP-PCA, fit a predictor, or open
sealed attention0. Price and native calls are zero.
