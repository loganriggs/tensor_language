# Three-hourly mathematical review — 2026-09-03 13:06 UTC (Claude)

Grounded in: the equality-score arc's branch-exchangeable HYPOTHESIS (rung532 §2683; rung533's OOD test came back
instrument-invalid, so exchangeability is discovery-level, not held-out-confirmed — Codex now on R534's
gauge-invariant product split); and my complete exact-rank arc (§2673/§2675/§2676/§2679: attention head-dim-
bottlenecked/compressible, MLPs full-dim high-rank). Sign convention §2135 (not used; exact structural statistics).

## Simultaneous factorization / shared dictionaries — EXECUTED (CPU), §2684

**Object.** The 18 MLP blocks' input maps: per block, the input-importance metric L_b^T L_b + R_b^T R_b (1152x1152).
**Theory.** Coupled/simultaneous factorization: even if each block's token-context operator is individually
high-rank (§2675), the 18 blocks could share a common low-dimensional INPUT subspace (a shared dictionary), so the
MLPs compress jointly. Test by pairwise principal angles between per-block top-k input subspaces and the effective
rank of the combined sum_b(L_b^T L_b + R_b^T R_b). **Assumption that may fail.** Input-side sharing is only one
axis; hidden/output-side coupling could exist and is not tested here. **Consequence beyond reconstruction.** If a
shared input dictionary exists, the 18 MLPs' input maps compress jointly into one basis + per-block coefficients —
a real smaller program the per-block rank missed. **Result.** NO shared dictionary: pairwise top-64 input-subspace
overlap mean 0.240 (max 0.433) barely above the random baseline 0.200, and the combined input-importance effective
rank is 1140 of 1152 (essentially FULL). The 18 blocks read near-orthogonal inputs and collectively span the whole
residual. So the MLPs have no low-dim structure at ANY level tested: per-block operator (438-749, §2675),
context-only branch (929, §2676), OR cross-block input dictionary (this, 1140/1152). Thoroughly, exactly
high-complexity.

## Gauge-invariant description of the equality-score family — PROPOSED (overlaps R534)
**Object.** The branch-exchangeable equality-score factors (§2683 hypothesis). **Theory.** For a family
exchangeable under a group G (~S_2 swap x Z2 sign, §2633/§2683), the invariant ring is generated by the symmetric/
even functions; these are uniquely identifiable where the individual factors are not, giving a canonical G-quotient
description. **Consequence.** An editable, identifiable smaller description of the equality family. **Falsifier.**
invariant-sufficiency (downstream effect is a function of the invariants only) needs effects — this IS Codex's R534
(shared-signal S = gamma*P_source, private R = P_target - S; test whether R predicts marginally or only through
interaction). Proposed for their lane, not executed (effect-based, would collide).

## Weighted-automaton / minimal realization — PRUNED
The equality/copy computation as a linear weighted automaton over the sequence: Codex confirmed (and I agree) the
linear-recurrence + complete-Hankel assumptions fail for the 18-layer RMSNorm transformer. Pruned (RMSNorm
non-composability), as in prior reviews.

## Top three
1. Cross-block MLP input sharing — DONE (§2684): NO shared dictionary (combined rank 1140/1152). Closes the last
   MLP-compression hope; the MLPs are exactly high-complexity at every level.
2. Gauge-invariant description of the equality family — PROPOSED; = Codex's R534 (effect-based); their lane.
3. Weighted-automaton minimal realization — PRUNED (RMSNorm).

## Executed
Move 1: ops/mlp_cross_block_input_sharing.py (§2684). With §2675/§2676/§2679, the exact structural verdict is
now complete and thorough: bilin18's MLPs are genuinely high-complexity token-context operators with no low-rank,
no low-dim branch, and no shared cross-block input dictionary. The smaller program is attention (the frontier)
plus a faithful high-rank MLP surrogate — there is no MLP low-dim handle to find. Correction to §2683 recorded:
exchangeability is a rung532 discovery hypothesis; rung533's OOD confirmation was instrument-invalid (product
control 4/8), so it is not yet held-out-established (R534 pending).
