# Semantic origin-field interchange v1

The prior-key L1→L2H2 path has selective task-effect support but is not sufficient
for native fidelity. A verified exported source-edit program now preserves exact
native computation and overlapping edits. Test whether the copied field denotes
the origin entity, not merely whether ablating its path damages the task.

Fresh two-chain generator seeds22909/22910,16 IID24-cycle and16 OODtwo12-cycle
worlds. Use arrangement0 only, then exchange complete binding pairs at slots11/19
of each recipient input to make chainA backward as well. ChainB is already backward.
This preserves each function and all query answers. Both selected L2 routes useH2.
Eight queryforks perworld. No selection by native output;32independent worlds.

For source binding u→v, the established subpath reads the L1 value projection at
its preceding key position u, routes it through that head's native key→value score
and O, then through the native L2 gain,V,H2 score andO into the later join record.
Replace only that L1 value read by the other selected origin's value read, separately
for each of the four L1 heads. Keep recipient L1 routing, L2 routing, both gains,
all endpoints, and the exact complement of this subpath unchanged. These are
explicit compiled-path interventions, not whole-token swaps or upstream-layer cuts.

If chains are a0→a1→a2→a3 andb0→b1→b2→b3, exchanging copied origins a1/b1 in the
completed join records predicts answers b3/a3 for hop3 queries a0/b0. The two
intermediate joins are editable variables; no claim that this edited relation must
remain the square of the original function. Lowerhop queries should stay intact.

Arms native,identity path replacement,swapA,swapB,bothSwap,swapA_cutB,
swapB_cutA,cutA,cutB. Cuts remove the whole selected native backward join write;
swaps replace only its prior-key subpath. Single transfer swapA_cutB should answer
queryB witha3; swapB_cutA should answerqueryA withb3. Their joint use tests reuse.
The two standalone swaps also permit a raw-query-logit composition check without
assuming that duplicate origin keys have a deterministic task answer.

A instrument: valid maps/answers, disjoint target supports, nonzero path andidentity
controls, explicit route contraction vs independent native L1 pattern/value hooks,
all-logit correspondence<=1e-9 andrelativeRMS<=1e-10 (floor1e-6), finite;
native hop3 accuracy>=.8 andown cut goldP loss>=.25 forboth queries/populations.
B semantic retarget: bothSwap desired-answer accuracy>=.8 andmean desired-answer
probability gain>=.5 overnative, forboth queries ineach population.
C reuse: corresponding single-transfer desired-answer accuracy>=.8 andprobability
gain>=.5 ineach population. Do not count same-context identity as field semantics.
D specificity/composition: abs mean original-gold probability change<=.10 for
bothSwap on each query/hop0..2; at unchanged finalqueries, joint raw-logit effect
minus sum of single-swap effects max<=1e-9. Full edited-distribution correspondence
is already required byA, independently of task retargeting.

The exported program forms all states/paths from tokens and its own387968 constants;
reference checkpoint access is only for an independent native intervention oracle.
No new numeric dictionary, donor tuning, alignment fit or best-head choice. A valid
retargeting null rejects this field interchange claim; do not redefine the target
as generic causal damage. Success identifies a manipulable field/interface while
all native background remains priced; it is not a smaller semantic model claim.
B4FP64,1800s,<256MiB/tensor,managedGPU. Primitive controls first, managed integration
next. Use native cached suffix as a timing baseline; sparse editor is not faster.
