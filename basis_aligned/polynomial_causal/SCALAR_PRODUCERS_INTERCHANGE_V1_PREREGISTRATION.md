# Scalar-variable interchange and conditional interaction reuse

2026-09-12, before model evaluation of this new intervention. Frozen physical producers and composed MLP response passed fresh email/letter removal/interaction tests. Removal alone does not establish donor interchange or general manipulations. Keep all current selectivity/wholehead-fidelity failures.

Use all48context-transfer rows, now explicitly reused. Opposite-cue donor i xor1 has the same template, spelling endpoint and city-pair identity, matched token positions/length. Native scalar fields a8,a9 are captured for both members before edits. No factor refit or donor choice from model scores. An interchange replaces a component's scalar field at all positions with its donor field, keeping its physical writer and all other native computations fixed except their recursive response.

Arms: native; interchange8; interchange9; interchange8+9; interchange8 with component9 held at its pristine recipient field; interchange8 with exact response-generated9field; interchange8 with direct+mixed response-generated9field; self-donor interchange8+9. For8, subtraction amplitude is a8_recipient-a8_donor, possibly signed, rather than a8_recipient. For9interchange use donor pristinea9 minus actual recursively recomputed a9. The two-node intervention intentionally overrides9 after8changes; this is explicit node interchange, not an assumption of additive effects.

The analytic response generator uses each recipient's pristine z8, biasfreeMLP8outputu8 andrawr9, with signed amplitude a8_recipient-a8_donor. It retains fulljointQK9 and the original token lookup. Exact/directedmixed generated9field replaces the actual component9field after8interchange; fullnativehead9background remains. These external native state inputs remain charged.

A EACHtemplate: native margins replay context-transfer native arm<=1e-4relative; self-donor margins/effects replay no-op<=1e-4relative (report absolute error as well); exact generated9 serial effect<=1e-4relative. Serial-effect reference is interchange8 minus interchange8-with-frozen-recipient9. Native capability must retain meanpairedcontrast>=.2 and>=10/12positive. Use an absolute1e-6margin guard only when the target serial-effect norm<1e-5, and report such a near-zero cell as noninformative for C rather than a positive.

B EACHtemplate: individual8/9 and jointinterchange each moves target margin toward donor on>=20/24rows; joint mean directed transfer>=50%native meanpairedcontrast; eacharm unrelated token-margin meanabs<=.5regionalmeanabs. This is a new interchange bar, not retroactive repair of prior wholehead fidelity.

C EACHinformative template: direct+mixed generated9 serial effect error<=10%relative; exact arm must satisfyA. This tests reuse of the operation under signed donor differences, beyond its earlier zero-removal input domain. If any template is noninformative, do not claim a general Cpass.

Price384bodyforwards48*8, <=32tokens,180seconds, no optimization. Cache one matched pair at a time to keep state memory small. Managedlane1. A pass is controlled interchange/composition evidence on known text, not newOOD, full input extraction, global service selectivity or a complete model decomposition.
