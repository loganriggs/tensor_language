# Fresh FineWeb confirmation of folded MLP8 value mediation

Freeze the six-piece native-weight MLP8→head9.8 value formula and the MLP8 subset left_cross+right_cross+quadratic+MLP8_normalization. Opened same-boundary specificity passes, but reversed-group control leakage remains. No further term selection, fitting, strength tuning or removal of failed documents.

Use first20new eligible FineWeb sample-10BT documents, same frozen preposition/city selector and fixed six spelling probes, excluding every prior document hash and exact input. One original and one paired city substitution;40sequences/240probes, no capability filtering. This is fresh relative to mediator selection; no claim of pretraining-disjointness or structural-template generalization.

Parent intervention is exact native head8.2 city interchange with recipient queries and paired donor K1,K2,complete V. It is NOT the approximate packed-prefix intervention. Capture native and fullswap contexts in two batched forwards, including raw MLP8 input, both native mixed9 states and both native MLP8 outputs. Folded mediator uses z8,swapdelta,mixed9_native,mixed9_edited and fixed program; compare against independent Wv9.8(MLP8_edited−MLP8_native)*lambda9*(1−mu9)/rho9_edited. Native normalizer context remains explicitly external.

20arms total:native,fullswap,fullswap minus native-reference MLP8value correction,fullswap minus folded correction,16norm-matched random128Dcorrections at same head9.8value boundary and fullswapbackground. Reuse first2capture scores;18additional forwards. 800sequence-equivalent forwards/360block calls,2CPUthreads,600second cap. Nullseed18102000+1000*k+context_id,pairedopposite sign;match each sequence/position norm of folded correction. Keys and otherheads unchanged; fullsuffix recomputed.

pred_a:20documents/40sequences/240probes/no prioroverlap;folded correction<=1e-4relative versus native reference per sequence;finite/support0;nullnorm<=1e-5;unchangedotherheads;correct subtraction<=1e-5relative;counts/CPU.
pred_b:folded correction target-effect change from swap predicts native-reference correction change with<=.05relative L2 error;targetchangeRMS>=1e-5;>=90native-capable pairs(nativeUK-USmargin>=.1).
pred_c:all four control RMS changes caused by correction<=.5correction target RMS AND all four control RMS changes of total corrected swap versusnative<=.5total corrected target RMS.
pred_d:corrected swap positive attenuation>=.90 of capablepairs and mean>=.02.
pred_e:folded correction targetchangeRMS>=2randommedian and beats all16randomcorrections.

Report native fullswap direction, corrected direction, perdocument signs, untouched/substituted prediction errors and reversed/other subgroup controls without reselecting rows. Gate failures retain the exact fold but block fresh selective mediator promotion. No independent composition or smaller whole-model claim follows; four native tensor inputs include the intervention delta, and the full prefix/suffix remain external.
