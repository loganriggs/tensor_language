# Real 36-site shared-output RRR v2 recovery

**Status:** prospective recovery after v1 terminal implementation failure and before
any v2 authority, row tensor, model, result, failure, or receipt exists.

V1 published an authority and then failed at the first evaluation-role metric lookup:
the evaluation token IDs were on CUDA while the immutable covered-token Boolean mask
remained on CPU.  No arm metric, result, or receipt was published.  V1 authority and
failure are immutable parents, respectively file SHA256
`4ac2839267abc99179fda32f161ec33eabe091384053b53ec78dca5f6b54e122` and
`1162039e29635f35978a267324b6b9f1da3333ec01b410d04e61b614664bfd88`.

V2 changes exactly one execution boundary: before indexing the coverage mask by token
IDs, the mask is placed on the token device.  The v2 wrapper must hash-bind and
semantically replay both v1 parent artifacts, and source-close this amendment, wrapper,
test, corrected base runner, original preregistration, core, facade, and model sources.

Every scientific object remains frozen exactly as in v1: rows and roles, covered token
definition, raw embedding input, native one-token output targets, scale-relative ridge,
24 arms, ranks, exact-price allocation, direct 494/481 comparison, legacy anchors,
whole-program CE, thresholds, conjunctive controls, autonomous all-36 replacement,
call schedule, and resource ceiling.  No validation/final authority exists.  V2 uses a
fresh create-only authority/result/failure/receipt namespace and receipt-last lifecycle.

The recovery is an execution repair, not a new hypothesis or another attempt selected
using model outcomes.  A v2 failure spends v2.  A v2 receipt supports only the original
discovery claim: an in-process priced compression grammar.  Because factors are hashed
but not serialized, even a pass is not yet an immutable deployable model, a
generalization result, or semantic-coordinate evidence.
