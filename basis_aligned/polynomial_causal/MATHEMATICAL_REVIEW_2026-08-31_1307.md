# THREE-HOURLY MATHEMATICAL REVIEW — 2026-08-31 13:07Z

Convention (§2135): CE numbers are damage above the real model; LOWER IS BETTER.

## Fresh facts this review answers to
The corruption economy is measured (§2241/§2243): motif-grain attention reads carry ~1.07 and tail-grain
~1.01 of the frontier's +1.95 census (overlapping); retrieval classes transit attention; punctuation goes
NEGATIVE under splices (frame overshoot). The paradox: oracle stream fidelity halves aggregate damage yet
certificates stay 0/62 under every idealization tried. Motif α is proven structural (§2220 frame-refit
no-op; §2244 class-α nets +0.01); the attn5 cliff is modest in the modern frontier (§2245: 0.122 at 5.3M).

## Top three mathematical moves

### 1. Bias–dispersion decomposition of certificate failure → EXECUTED (rung 150, queued)
Object: each circuit's certificate statistic, member mean|dCE|. Split into BIAS (signed member mean —
fixable by ~62 stored scalars if dominant) and DISPERSION (what |·| adds — §2209's quadrature-of-orthogonal-
drifts prediction). Dispersion dominance would EXPLAIN the census/certificate decoupling: aggregate is a
signed mean, certificates are abs means with a dispersion floor. Assumption that may fail: heavy tails make
medians misleading (full per-circuit stats kept in the receipt). Consequence beyond reconstruction: the
branch picks the certificate repair — bias → per-circuit offsets; dispersion → variance-reduction (move 2).
Falsifier: rung 150, ~4 min, zero price.

### 2. Variance-reduction estimation for dictionary fits (falsifier ready behind 150)
Object: every fitted dictionary (aXL CV/LW, tailE). Averaging B bootstrap-refit parameter sets cuts
ESTIMATION variance ~1/B at zero runtime/storage cost (average the parameters, don't ensemble). If
certificate dispersion is partly estimation noise this moves it; if intrinsic (drift-induced) it does
nothing — cleanly falsifiable. Cheapest test: B=4 bootstrap halves at the aXL dicts, member abs-dCE.

### 3. Frame-consistency as gauge structure (recorded; no run)
§2224/§2241/§2243's overshoots (punctuation NEGATIVE under splices; the span-edit explosion) instantiate one
rule with a SIGN: components fit downstream of corruption OVERSHOOT when the corruption is removed. This
upgrades the §2227 type system with a corollary — any upstream fidelity improvement requires downstream
refits to cash in fully (the build pipeline refits by construction; oracle splices do not, which is exactly
why the cheap classes pay under them).

## Pruned
Invariant-theory quotients (gated on rung 90); Hankel/automata retrieval modules (stream fidelity dominates
retrieval classes — deferred); sensitivity weighting (closed §2229); simultaneous factorization (moot at
the front); IB (no handle).

## Executed
§2244/§2245 written first (nulls preserved). Rung 150 preregistered, built, dryrun-clean, queued — after two
builder anchor failures (a stale two-line gate anchor, then a CINFO assumption this variant lacks; both
rebuilt against the actual runner text).
