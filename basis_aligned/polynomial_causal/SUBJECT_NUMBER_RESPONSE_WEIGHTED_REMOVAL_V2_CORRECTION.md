# Subject-number response-weighted removal V2 correction

V1 is preserved as a valid mixed null. Candidate removal passed its causal gate:
native residual ratio `.32420`, versus `.52788` for symbolic-law removal and
`1.00956` for the median equal-norm random removal. Four of five collateral
ratios were below `.187`. The registered selectivity gate failed because
`work/jobs` was `.250333` against a `.25` ceiling.

The label “unrelated reader” was wrong for that pair. `work/jobs` contrasts a
singular-form verb with a plural-form noun and is therefore structurally entangled
with the grammatical-number feature being removed. V2 does not relax the bar or
discard the V1 receipt. It prospectively replaces only that pair with the unseen
same-category color contrast green/black `(4077,2042)`. The remaining four readers,
all causal arms, random directions, seed, coefficients, thresholds, prices, and
instrument checks are unchanged. V2 is the sole correction.
