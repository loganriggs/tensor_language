# Hourly strategic review — 2026-09-08 04:15 UTC

## Circuit interpretation targets

The target remains a circuit that specifies what information is read, the operation performed,
the written variable, and its downstream users; groups and splits computation across native
heads/MLPs when causality requires it; predicts held-out and OOD behavior; executes through a
clear interface; supports selective removal, swap, and edit; composes and reuses subcomputations;
and is stable across splits, texts, and admissible gauges.  The program-level goal is still a
smaller transparent tensor program that is jointly predictive, composable, manipulable, and
literally simpler in storage, compute, edges, and state.  Rank, reconstruction, or a task fit is
not a substitute for these properties.

## What changed since 03:15

The hour moved the v15 construction-conditioned rank-two program through manipulation and three
increasingly explicit routing falsifiers.

1. Final-state projection removal loses `1.106-1.212` of the live effect and projection-only
   sufficiency recovers `1.133-1.198`; the analytically frozen direct path gives exactly `1.0`.
   P/C remain off.  This establishes selective necessity and sufficiency of the transported span.
2. Whole A1/A2 payload swaps preserve direction and cosine (`>=.995`) but have reciprocal amplitude
   error.  Opposite-fold state-norm gains repair all frozen-direct swaps to `.807-1.234`, while the
   live suffix still overshoots two cells to `1.342/1.389`.  The dominant payload is reusable up to
   construction gain; the 13-20% suffix correction is not yet reusable.
3. A frozen tied-embedding router fails in distribution: it finds all v15 targets but routes every
   held P row to A2 (macro `.8333`), then sends all v16 targets and P off.
4. Pooling both v15 directions in the six-scalar contextual Gram router does not repair the strict
   gate.  LOGO macro is `.9479` with three control false positives.  On v16 A1/A2 are almost wholly
   exchanged, P leaks `9/11` rows, macros are `.1458/.125`, and oracle folds disagree.
5. A prospectively frozen projective response-shape spectrum gets all v15 targets right but leaks
   five controls (macro `.9479`).  V16 macros are `.25`; all A1 rows become A2, A2 splits, P leaks
   `13/11`, and folds disagree.  Runtime sign invariance closes within `3.40e-6`.

The last two nulls change strategy.  Direction reversal was a real confound in the earlier raw
linear router, but removing it is insufficient.  Coarse response moments, projective response
shape, and tied lexical geometry all fail stable branch identity.  Their prospective decisions now
close contextual centroid feature engineering rather than licensing more features or thresholds.

## Is this still the highest-information route?

No.  The routing branch has now produced three independent representation failures while the v15
state program itself has strong extraction and manipulation evidence.  Another hand-designed
router representation would have low information value and high post-selection risk.  The highest-
information missing property is composition/reuse between the independently established temporal
and is-was task programs.  Before executing a joint intervention, a zero-forward authority audit
must determine whether their released command states share an operational final-residual boundary
and whether an unused dual-command capability population exists.  If either is absent, the next
experiment is a capability builder, not a causal run on incomparable contexts.

## Confound audit

- **Baseline subtraction:** removal, sufficiency, and swap all use the same native-off state and
  exact final decoder; no independently normalized effects are added.
- **Frame mixing:** opposite-parity bases and gauge-invariant whole-span operations are retained.
  The two v16 oracle-coordinate systems are reported separately and their disagreement is a failed
  gate, not averaged away.
- **Nonlinear composition:** live and frozen suffix swaps are separate.  The final RMSNorm,
  unembedding, and soft cap remain exact; the live 13-20% correction is not treated additively.
- **Shared-token difficulty and leakage:** v16 text/native outcomes had been opened previously and
  are labeled `OOD_TEXT_REUSE_NEW_INTERVENTION`; all router parameters still use v15 only.  V16 C is
  excluded because token lengths differ, so no claim extends to it.
- **Dead knobs:** the embedding router has no optimizer; Gram and shape routers have fixed features
  and centroid rules.  Whole-group LOGO and two oracle folds are live falsifiers.
- **Precision/noise:** exact prices and finite values pass.  Shape sign-invariance error is
  `3.40e-6`, far below the scale of the categorical failures.
- **Post-selection:** the shape object, bars, and kill rule were committed before the Gram outcome
  opened.  It failed and is preserved; no threshold or feature was changed afterward.

## Distinct next approaches, ranked

1. **Joint temporal/is-was command composition at an audited common boundary.**  Inventory the
   released task command tensors, gauges, backgrounds, and populations; then build a dual-command
   capability set if needed.  A registered 2x2 empty/temporal/is-was/both intervention should test
   whether the two state writes and their final-head effects compose.  This advances composition,
   reuse, extraction, and prediction.  Kill it if no common operational boundary exists, no joint
   population is natively capable, or the both arm is not predicted by the registered interaction
   law.
2. **Native semantic-routing circuit localization.**  Instead of another fitted feature vector,
   full-patch A1/A2/P at the known L8/L9/L11 writer core and split only a causally selective module.
   This advances computational specification and stable identification.  Kill it if complete
   modules cannot separate target from answer-preserving temporal paraphrase before any subspace fit.
3. **Regularized multi-environment causal operator.**  Revisit optimized DAS only after at least two
   training construction families and one sealed third family exist.  Use noise/KL regularization
   and optimize target plus complement together, reflecting the user's overfitting diagnosis.  Kill
   it if hard target feasibility is lost or construction-held controls fail; do not select on v16.
4. **More centroid, rank, or static-weight routing.**  Rejected.  It changes no circuit target after
   the embedding, Gram, and projective-shape nulls.

The route therefore changes from router feature engineering to joint composition/reuse, beginning
with a zero-forward compatibility audit.  This is not rank drift: the decisive measurement is a
joint causal factorial and its prediction, not reduced dimension or reconstruction.

## Throughput and systems checkpoint

From 03:15 to 04:15, five new result receipts landed on this branch: final-state manipulation,
gain-calibrated swap, tied-embedding routing, pooled contextual-Gram routing, and projective-shape
routing.  Result spacing was roughly 7, 7, 10, 24, and 6 minutes (median 7); the 24-minute interval
was dominated by a pre-existing 25-minute managed GPU job, while the branch used that wait for the
zero-forward Gram replay and the preregistered successor.  Model executions for this branch were
2-6 seconds.  Scientific design/interpretation and reusable contracts dominated; focused validation
used 4-8 tests per unit, and there were no outcome-bearing retries.  No lane was launched directly
or preempted, and both managed runners remained healthy.

`CIRCUIT_FOCUS`: **PASS** — every unit advanced selective manipulation, reuse, OOD identification,
or a circuit-level falsifier.

`CEREMONY_BUDGET`: **PASS** — checks were smaller than design plus execution; the only long wall-time
segment was serialized GPU work already owned by another valid circuit run.

`NOVELTY_LESSON_GATE`: **PASS** — prior router failures were searched and bound, direction/sign
confounds were tested explicitly, and the repeated null now closes the object instead of widening it.

Immediate continuation: audit whether the released temporal and is-was command tensors can be
installed at one final-residual interface on a genuinely joint population, and register the minimal
capability/factorial successor from that receipt.
