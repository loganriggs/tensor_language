# Hourly strategic review — 2026-09-08 21:22 UTC

## Circuit interpretation targets

The live target remains a computational specification of what is read, computed, written,
and consumed; cross-boundary grouping and within-module splitting; held-out/OOD prediction;
extraction or sufficiency; selective removal/swap/edit; composition and reuse; and stable
identification across corpora and gauges. The program-level goal remains a smaller transparent
tensor program that is jointly predictive, composable, manipulable, and cheaper in literal
storage, compute, edges, and state. Rank, variance, reconstruction, and CE alone do not satisfy it.

## What changed since 20:22

1. The exact v22 ten-route effect game landed. Its A1/A2/P rows were aligned and valid, but the
   canonical C endpoints were not position-compatible, so the original global instrument verdict
   remains invalid. The bounded valid result was preserved: the four heads recovered about 72% of
   behavior/Q with low P leakage, while six broad MLP routes were redundant and failed P selectivity.
2. A history-disjoint v23 corpus repaired that defect by making all 64 base/donor token lengths and
   semantic endpoints equal. Native capability passed (A1 16/16 jointly capable, A2 14/16).
3. The frozen `L8H1/L9H1/L9H4/L11H3` program passed the aligned confirmation with exact zero
   closure error: behavior recovery `.7259`, Q recovery `.6701`, cosine `.9952/.9768`, all signs
   correct, P leakage `.1003/.0834`, and C leakage `.0040/.0064`. All four singleton heads and all
   four split-stable Shapley allocations passed. This is an identified selective distributed writer
   program on fresh lexical data, not merely a screen.
4. The confirmed subspace was folded into literal head and M11 weights. The exact restricted tensor
   passed formula error `8.8e-7`. Factor support is nearly shared across heads (median score cosine
   `.9854`, median participation `3.96/4`) but signed head maps are not one common mode (top head-mode
   energy `.3012`). Literal factors are diffuse (top 256/4608 score mass `.2809`).
5. The frozen activation overlay then showed the distinction the user proposed: weight reader-mode
   energy is broad (`.362/.215/.213/.210`), while realized causal response uses one frozen mode at
   `.9714`, stable across reporter halves at cosine `.99998`. Directly composing head `W_O` with M11
   fails badly, including adjacent L11H3, because live RMS normalization is part of the transport.

The correction matters: the weight tensor describes possible computation at the normalized M11
interface; it is not an end-to-end head-to-M11 path tensor. Causal patching identifies the route,
and the occupancy overlay identifies the narrow realized slice.

## Throughput and workflow audit

Authoritative artifacts from 20:22–21:22 contain six circuit-relevant outcomes: v22 capability,
the v22 game/audit, v23 capability, v23 causal confirmation, restricted-weight characterization,
and the occupancy/transport falsifier. This meets the six-per-hour screen-or-honest-null rate,
although only v23 is a newly promoted circuit. Approximate serial allocation was 45 minutes on
scientific design/implementation, 8 on focused validation, 15 of managed compute overlapping the
authoring blocks, 7 on interpretation/commits, and under one minute idle. Shared capture/patch and
weight-atlas functions prevented safeguards from becoming the largest bucket.

- `CIRCUIT_FOCUS: PASS` — every artifact changed identification, selective control, weight anatomy,
  occupancy, or transport of the same circuit.
- `CEREMONY_BUDGET: PASS` — focused tests and hash gates were smaller than design plus execution;
  the extra aligned-control repair addressed a demonstrated invalid instrument.
- `NOVELTY_LESSON_GATE: PASS` — prior v1–v22 rows/results were searched; post-selection, raw-H,
  activation-reconstruction, and control-family failures were applied rather than repeated.

## Confounds and direction check

- Baseline subtraction and nonlinear composition: exact native differences and all 16 four-head
  coalitions were used; Shapley efficiency closed.
- Frame mixing and padding: fixed by equal semantic endpoints on every v23 row; exact donor closure
  is zero.
- Shared token difficulty: target metrics use the capability-frozen 30 jointly correct rows.
- Leakage: P and C are separate normalized controls; neither was used to choose routes or bars.
- Post-selection: the four heads were frozen from v15/v21 before v22/v23; no v23 subset/dose/rank
  was selected.
- Gauge/scale: reader-subspace claims concern its projector and unfolding spectra; raw H/R norms are
  not compared. The newly exposed scale catastrophe is physical RMS normalization, not gauge noise.
- Dead knobs/noise: all patch hooks, reader gradients, formula checks, exact prices, and split signs
  were live. Closure is exact, well above numerical ambiguity.

The current route remains highest information, but its object changes. A raw HOSVD, SAE, or sparse
weight fit is now demoted: the exact weights are diffuse while causal occupancy is already one-mode.
The missing computation is the input-dependent transport from each head write through normalization
and intervening blocks into that occupied M11 reader mode.

## Ranked next moves and kill evidence

1. **Normalization-aware adjacent L11H3 transport tensor.** Derive the exact RMS-normalization
   Jacobian and a finite-dose remainder, then test its frozen prediction of L11H3 M11 factor response
   on v23 halves/P/C. This advances computational specification and held-out prediction. Kill a
   tangent-only description if held-out cosine is below `.90` or finite-dose residual remains above
   `.25`; retain an exact finite normalization operator instead.
2. **Layerwise L8/L9 transport localization.** With the frozen occupied mode, patch/capture after each
   intervening attention/MLP boundary to locate rotations or nonlinear conversions. This advances
   cross-boundary grouping and within-module splitting. Kill a boundary if removing it changes neither
   mode orientation nor magnitude beyond the registered noise floor.
3. **Selective necessity and reader rescue.** Remove the four-head occupied mode, then rescue at M11
   or its downstream reader while testing P/C and another task. This advances manipulation and
   extraction. Kill the claimed interface if removal is nonselective or M11-mode rescue cannot recover
   the signed effect.
4. **Multi-task shared-subspace tensor.** Once a second independently confirmed circuit overlaps this
   projector, concatenate task-indexed restricted tensors and test shared/private modes. This advances
   reuse and composition. Do not start from activation similarity alone; kill sharing if interchange
   across tasks fails despite geometric overlap.

The immediate action is item 1. It is not a compression sweep: it decides whether the identified
writer-to-reader edge is predictable from a local tangent plus a bounded normalization remainder, or
requires a genuinely finite, state-dependent transport program.
