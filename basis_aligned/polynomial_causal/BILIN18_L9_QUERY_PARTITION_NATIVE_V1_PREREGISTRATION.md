# Native query interaction partition and complete query interchange

Registered10 September2026 before native bank capture. Follow the original
bilinear handoff/pilot and the0206 hourly review. Previous atlas: no single
query source suffices; individual omission effects do not add to joint cuts.
The executed toy shows normalization alone can create source/rest interaction.
This run distinguishes that algebra from the trained computation and asks
whether the complete contextual query carries the paired tense change.

Unchanged72 opened evalpairs, four panels, native errors retained. No fits,
source selection, ranks or new datasets. Earlier MLP4 routing/local-V tests
condition on one producer change; full contextual Q/Q2 interchange is a
different intervention. Do not equate a query-cut effect with tense mediation.

Capture native19-source banks, keys and mixed values at L9H1/H4 using the
completed parent primitive. For each source/rest partition z_s,z_r, measure
I=y(all)-y(s)-y(r)+y(0). Use full-gain query denominator D_full to define
C=(N(all)-N(s)-N(r))/D_full, then R=I-C. Fold native O into every vector
before comparing I,C,R. All comparisons share the same1152 residual-write
frame; do not subtract a local read from a final-logit effect. Report both
endpoint interaction vectors and their donor-minus-base differences.

Also swap the COMPLETE native normalized attention input at Q/Q2 projection
ports only, selected heads1/4, semantic query endpoint. Each receiving run
retains its native keys, values, other heads/tokens and residual background.
Head RMS and RoPE recompute normally. Run native/identity/swap for both
endpoints of every panel:24forwards/432sequence evaluations. The source
norm supplied to query ports is the donor's native normalized input, not a
cached denominator applied to some different source. Identity uses receiver.

A instrument: completed parent/source/row/backend/checkpoint hashes; CPU
algebra controls; native source-lineage/read and identity logits maxabs<=1e-3
AND relative Frobenius<=1e-5; local partition closure<=1e-8 AND<=1e-9;
query projection FP64 oracle<=1e-3 AND<=1e-5; unselected query outputs
unchanged, hooks restored, exact counts, allfinite, parentnativecontrastreplay.
No negative source Gram norms clipped. Both task banks and all sources saved.

B complete query tense-carrier screen: for both swap directions in every
panel, signed projection onto native donor-minus-base change >=.10 for
BOTH centered full logits and paired answer-minus-foil margin. Reverse
direction flips the target sign. Also report relative effect norm, cosine,
and row-level margin effects; no native-error filtering. A pass licenses
finer query-source mediation testing, not sufficiency/identification/adoption.
A miss closes the strong common query-carrier hypothesis at these sites;
query background computation may still matter and per-task misses are kept.

C numerator-cross explanation: ||R||/||I||<=.10 in EVERY panel, separately
for all endpoint vectors and donor-minus-base interaction vectors, stacked
over all19 sources. Zero targets require absoluteR<=1e-8. This is a fixed
local reference decomposition; correlated/cancelling components are not
variance percentages. Report signed projections and component cosines.

D live query manipulation: both swap directions in every panel must change
centered logits by norm>1e-8. D failure is a dead-query-edit diagnostic,
not permission to change dose or source. A failure is invalid; B/C misses
are retained. No posthoc source/head/position/denominator rescue.

Nativeweights545902902 retained, zero real weight saving/model updates.
The per-context conditional bank includes source-score factors, norm/Gram
matrices, nativevalues and selectedoutputweights; count all saved entries.
It is not independent token-to-logit extraction. Save immutable result JSON
and compressed NPZ using allow_pickle=False-compatible numeric arrays.
Managed enqueue only,600-second watchdog. Next step follows B/C outcomes.
