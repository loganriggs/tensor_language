# Hourly strategic circuit review — 2026-09-06 21:14 UTC

## Circuit interpretation target

The target remains a compact tensor program that predicts fresh behavior, supports selective
causal interventions and removals, composes across tasks, and exposes literal weight reads and
writes. A trained scalar actuator, geometric overlap, or a list of large patch effects is not by
itself a circuit.

## What changed since 20:14

- The frozen temporal H3-Q8 operation transferred zero-fit to a text-disjoint v11 bank. Q8 carries
  99.93%/94.68% of full-H3 behavior; the causal suffix carries 100.03%/100.32% of Q8 behavior.
  Value transport remains dominant, pattern change opposing, interaction small, and pre-subject
  value terms exactly zero.
- Translating Q8 through the actual H3 output weights exposed a shared is/was component: the
  selective cDAS axis has squared overlap .1332, 19.18x isotropic chance. Installing that component
  causally carries .270--.480 recovery across four held-out panels with low P/C collateral; its
  orthogonal remainder supplies the rest and the two pieces exactly compose.
- A 32x32 source-command by target-reader matrix is predicted zero-fit by the same Q8 coordinates
  at cosine .9999966--.9999997 in every within- and cross-task quadrant. The finite family resolves
  eight command dimensions and four reader dimensions, establishing a finite 4--8 state bracket,
  not global Fliess minimality.
- A complete 36-module is/was writer atlas found many overlapping singleton effects. Raw weight
  incidence correlates only moderately with causal magnitude and is miscalibrated across attention
  and MLP module types.
- Greedy complete-module composition rejected the apparent distributed sum: MLP8 alone was best;
  every added module worsened the objective, and the all-eight union overshot behavior to 276%.
- On 29 independently capable fresh-v6 rows, exact source-position patching localized MLP8's useful
  write to the post-cue span. That span carries 63.15% of complete-MLP8 behavior and improves Q8
  coordinate cosine from .782 to .839. Prefix is exactly zero; the cue contributes only 7.70%.

## DAS red team and confounds

- **Memorization:** regularization makes learned axes more stable, but the decisive evidence is the
  zero-fit v11 operation transfer and cross-task causal reuse. No v11 or is/was outcome selected Q8.
- **Complement-target cheating:** the old scalar complement objective identifies a useful answer-
  margin actuator but not a general state. Independent full-vocabulary effects, causal composition,
  and weight-derived Q8 coordinates remain the acceptance tests.
- **Singleton/additivity error:** eighteen material module patches did not imply an eighteen-module
  additive program. Prospective greedy composition exposed strong overlap and selected one module.
- **Weight-ranking calibration:** Frobenius incidence across unlike module shapes is not comparable.
  The next contraction is within one MLP's exact Down map, where the singular modes have a common
  mathematical meaning.
- **Numerics:** the source atlas first failed serialization because an exactly zero prefix produced
  undefined cosine. Replacing that diagnostic with finite zero alignment changed no intervention,
  data, threshold, or outcome; the independent rerun passed all gates.

## Throughput audit, 20:14--21:14

The hour advanced from a downstream Q8 weight reader through a transferable upstream operation,
cross-task geometry, selective causal reuse, a finite causal realization, full-module localization,
failed additive composition, and exact source-position localization. The queue remained focused on
one causal boundary at a time. Engineering invalids were isolated from scientific evidence and
repaired without threshold adjustment.

`CIRCUIT_FOCUS: PASS` — every valid run tested transfer, shared-state causality, composition, or
literal writer localization.

`CEREMONY_BUDGET: PASS` — capability gates and exact closures prevented post-selection claims while
11--39-forward tests kept evidence cost small relative to interpretation gained.

`NOVELTY_LESSON_GATE: PASS` — the workflow explicitly rejected scalar-target DAS as semantic proof,
response transport as mediation, singleton effects as additivity, and cross-type norms as calibrated
weight rankings.

## Different next objects, ranked

1. **MLP8 post-cue Down/product modes.** Contract frozen Q8 with MLP8 Down, take its rank-at-most-eight
   hidden-product row space, causally install rank prefixes and complement, then split the product
   into left-change, right-change, and interaction. This is the direct requested translation from
   shared subspace to actual writer weights.
2. **Fresh-bank confirmation of the minimal weight-mode prefix.** If a prefix below eight closes on
   v6, freeze it and test a new v7 construction before simplifying the program.
3. **Reader/writer incidence calibration within module families.** Normalize attention and MLP tensor
   contractions against activation-conditioned nulls before comparing architectures again.
4. **Further constrained DAS optimization.** Demoted: use only if it predicts an independent vector-
   valued causal object that the zero-fit tensor program does not already explain.

The highest-information next object is the already preregistered MLP8 post-cue Down/product split.
It can either expose an eight-mode literal writer with an inert complement or falsify the claim that
the shared Q8 coordinate system captures the natural upstream computation.
