# Subject-number embedding-to-L11H3 small mediation fresh V1

Registered after the opened-panel discovery found that full L11H3 restoration
recovers `9.55%` of embedding-coordinate removal damage and the frozen rank-one
axis recovers `9.05%`, while the axis reproduces the full-head rescue at `.0615`
relative error. The discovery missed its original `10%` substantial-mediation
floor and remains a registered null. This test freezes the distinct, narrower
hypothesis that L11H3 carries a stable small path.

The authority is a fourth disjoint 16-pair vocabulary panel (eight irregular,
eight regular), with both forms crossed with two untouched templates and
opposite-number attractors: 64 rows. No behavior on these rows has been opened.

Apply the same frozen same-norm embedding-coordinate removal. At L11H3, compare
no rescue, full native head-output restoration, projection of that restoration
onto the previously frozen rank-one writer axis, and 16 row-wise norm-matched
random-axis rescues. The native L11H3 state remains an explicit donor port; this
tests a path, not donor-free extraction.

Instrumentation requires decoder accuracy `1.0`, native agreement accuracy at
least `.75` in every number/template and regularity cell, target decoder residual
and norm error at most `1e-5`, repeated removed-head captures within `1e-5`,
finite values, and exactly 20 forwards over 1,280 sequences.

Predictions fixed before execution:

1. Fresh embedding removal is live: damage RMS at least `.50`, with positive
   damage on at least `.75` of rows overall and in every number, template, and
   regularity cell.
2. Full-head rescue is a stable small mediator: cosine with removal damage at
   least `.75`, positive fraction at least `.70`, and least-squares recovery in
   `[.05, .15]`.
3. Rank-one rescue retains that full-head path: cosine with full rescue at least
   `.97`, relative L2 at most `.20`, norm ratio in `[.75, 1.25]`, and recovery of
   original removal damage in `[.04, .14]`.
4. Rank-one rescue has lower relative error to full-head rescue than every one of
   16 matched random axes and effect RMS at least 10 times their median.

Passing does not erase the discovery's failed `10%` claim. It establishes OOD
reuse of a quantitatively small rank-one mediation edge. Failure localizes the
discovery to its opened vocabulary or shows that the head/axis relationship is
not stable enough to be a graph edge.
