# Current versus inherited value provenance in O framing transport

At the all-query-fixed, recipient-routing boundary, split head9.8 mixed values
exactly as `v=(1-mu)V_current+mu*v_first`. After 48 native captures, swap the
paired donor's framing values in three ways: both value sources, current values
only, and inherited first-layer values only. Queries and keys remain recipient
tensors. This is 192 body forwards under 180 seconds.

- `pred_a`: current plus inherited factors and donor deltas recompose within
  `1e-10`; native and full-value-swap target/work-jobs scores replay the frozen
  interaction V2 artifact within `1e-5`; all values are finite, both source
  deltas are live, and exactly 192 forwards execute.
- `pred_b`: native cue is positive for at least 10/12 pairs and full-value-swap
  target-effect RMS is at least `1e-5` in each template.
- `pred_c` (current-value hypothesis): current-only paired cue error versus full
  value swap is at most 20%, while inherited-only error is at least 50%, in each
  template.
- `pred_d` (inherited-value hypothesis): inherited-only error is at most 20%,
  while current-only error is at least 50%, in each template.
- `pred_e`: the sum of separate current-only and inherited-only paired behavioral
  effects differs from full value swap by at most 10% in each template.

Four frozen unrelated control ratios are reported. This tests cross-boundary
value provenance with no fitting. Native upstream generators and suffix remain;
no upstream necessity, corpus OOD, compression adoption, or quantization claim.

