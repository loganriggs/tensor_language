# Hourly strategic circuit review — 2026-09-06 20:14 UTC

## Circuit interpretation target

The target remains a smaller transparent tensor program that predicts fresh behavior,
supports selective interventions and removals, composes across causal boundaries, exposes
literal weight-level reads/writes, and is simpler than the native model. Rank, activation
overlap, or downstream response magnitude alone are not circuit evidence.

## What changed since 19:14

- Raw-response SVD was a valid wrong-object null despite explaining 87.97% of fit response
  energy. Exact H3-output-to-reader weight contractions produced the useful nested family.
- Rank seven passed its selection bank but strictly missed sealed v9 A2 sufficiency at 89.33%.
  A tensor-anchored discarded-normal optimization used full-vocabulary sufficiency/complement
  KL, tangent noise, four-environment balance, and an anchor. All three restarts agreed, yet
  every learned move worsened held-out KL; step-zero rank seven remained best.
- Rank eight then passed wholly new v10: 93.78%/91.29% full-H3 behavior, 6.47%/8.89%
  complement, and 96.75%/96.98% L15H5/H1 response transport. It is the current stable,
  label-free manipulable H3 variable.
- L15H5/H1 was falsified as a behavioral reader despite that transport. Restoring all L15
  responses changes rank8 behavior by only +0.007%/-1.00%.
- A complete MLP11-through-MLP17 dynamic removal atlas found no stable >=10% module mediator.
  Jointly restoring all thirteen module outputs changes behavior by only +0.84%/-3.03%.
  Head splitting and greedy module selection are therefore not licensed.
- The residual route was compiled directly from weights. H3 Q8 coordinates pass through the
  exact attention11 output matrix and frozen block12--17 residual gain 1.51363148. Installing
  that write at resid18 matches the live all-module-clamped route within 2.86e-6 logits and
  retains 99.16%/103.03% of rank8 behavior.
- Contracting those eight residual modes with the analytic final RMS-norm/unembedding reader
  predicts per-row causal margin effects at cosine 0.99999985/0.99999991 and relative RMSE
  0.056%/0.047%. The largest coordinate contributes only 46.6%/44.5%, so the interface is
  compact but genuinely distributed.

## DAS red team and confounds

- **Memorization:** noise, KL, environment balance, and tensor anchoring did prevent drift;
  restart agreement rose to >=0.976. They did not improve the held-out objective. The remaining
  failure is capacity/objective mismatch at rank seven, not merely unregularized fitting.
- **Target cheating:** answer-margin complement DAS previously beat DIM on its trained scalar
  but lost badly on centered full-vocabulary causal effects. Full-vocabulary KL repaired the
  target but still could not compress rank eight. Complement loss is a falsifier, not proof of
  semantic correctness.
- **Response/causality confusion:** 97% L15 response transport and earlier module response
  rankings did not imply mediation. Every claimed reader now requires dynamic response removal.
- **Post-selection:** rank seven was not rescued after the v9 miss. Rank eight was frozen from
  the v8 nested sweep and confirmed only on new v10.
- **Numerics:** direct-route v1 used the wrong matrix orientation for `nn.Linear`; it was
  released invalid. The source-verified `F.linear` repair changed no scientific condition and
  passed exact route agreement. The invalid output is not counted as evidence.

## Throughput audit, 19:14--20:14

Eight relevant terminals landed: regularized-rank7 null, v10 capability screen, rank8 paired
confirmation, L15 mediation null, full module-atlas null, one engineering-invalid matrix
orientation, corrected direct-weight route screen, and analytic reader screen. GPU executions
were 2--131 seconds; the scientific chain advanced continuously from subspace selection through
fresh confirmation, causal downstream localization, literal weight compilation, and row-level
prediction. The only avoidable work was the one orientation error, caught immediately by the
registered route-agreement control and repaired from source rather than by threshold changes.

`CIRCUIT_FOCUS: PASS` — every valid result changed manipulable-state quality, causal mediation,
or predictive weight-level circuit structure.

`CEREMONY_BUDGET: PASS` — one capability gate and exact controls prevented false promotion;
implementation time remained below interpretation time, and the invalid transpose cost one
18-forward run.

`NOVELTY_LESSON_GATE: PASS` — prior DAS, response-SVD, weight atlas, L15, original-module atlas,
claim ledger, and fresh-bank outcomes were checked. Activation transport was explicitly not
reused as causal evidence.

## Different next objects, ranked

1. **Compile the upstream writer into Q8 coordinates.** Pull the H3 Q8 covectors backward
   through attention11's exact subject-value operation and value weights, then test whether the
   already causal L9H1/H4/H7 responses predict the eight H3 coefficients row by row. This would
   join the proven upstream component circuit to the now proven downstream tensor reader.
2. **Cross-task subspace reuse atlas.** On independently capable temporal/number tasks, measure
   overlap in the same H3 coordinate system and test shared-coordinate interventions before
   calling any shared subspace. Reuse actual residual modes and weight readers, not retrained DAS.
3. **Exact nonlinear coordinate pruning.** Only if a simpler interface is needed, perform
   prospective leave-one-coordinate and nested exact resid18 interventions. The analytic
   contribution table prioritizes coordinates, but rank seven's prior fresh failure prevents
   post-hoc deletion without a new bank.
4. **More regularized constrained DAS.** Demoted. The best regularized run selected its tensor
   initialization unchanged; new penalties without a new functional object are low information.

The highest-information next object is the upstream Q8 writer compilation. It directly answers
which weights and causal components write the same state that the downstream weight reader uses.
