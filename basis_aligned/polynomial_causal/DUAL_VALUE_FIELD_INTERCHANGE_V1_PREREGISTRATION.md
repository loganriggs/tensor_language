# Original value versus computed endpoint at one record

The random-layout endpoint family failed its hop2 consumer claim. Mechanical
correspondence passed; hop2 task effect was small despite a substantial logit
response. A fixed opened-case final-reader audit finds distributed H0/H3 effects,
not an isolated consumer to adopt. Preserve both receipts without filtering.

New hypothesis: one postL2 record carries an original value and a composed
endpoint, with different query computations using each. Isolate the literal
original-embedding residual path E/8 at initial binding-value positions. Its
coefficient is exact after three lerp(.5) layers. The other field is the already
compiled sum of forward endpoint messages rho*phi(e). Retain all other producer
terms, native prefix, original gates/gains and downstream readers. These are
overlapping state edits; recompute the final source RMS normalization exactly.
Do not assume their effects add linearly in logits or probabilities.

Fresh25909/25910 random-layout IID24-cycle/OODthree8-cycle worlds;16 each,
all24 query entities×hops0..3. No fixed slots, fit, native-error filtering or head
selection. Use the existing structural last-two-edge orientation labels.
Each world supplies a random24-cycle sigmaR. Set sigmaJ=sigmaR composed with
itself: both maps have no fixed points and differ on every entity. Replace raw
E(e)/8 by E(sigmaR(e))/8; replace joined phi(e) by phi(sigmaJ(e)). This distinguishes
which field supplies the answer without fitting or selecting endpoint maps.

Prospective consumer rule: hop0 keeps q; hop1/2 use the raw field. For hop3,
forward final-two-edge orientation uses the joined field, while backward
orientation uses the later record's raw endpoint (its computed origin was the
separate backward route). Thus backward hop3 is a control for join-only edits,
but a target for raw-only edits. The raw E/8 path may be too weak or redundantly
encoded; that is a valid null, not an instrument failure.

Arms native,identity,mapRaw,mapJoin,mapBoth,removeRaw,removeJoin,removeBoth.
Individual maps should affect only their registered consumers; joint mapping
should preserve the distinct predictions. Removal deletes only the named field.
Native input function is unchanged; the edited internal records need not define
one coherent new function. No coefficient or dose tuning follows a failure.

A mechanical: CPU residual-producer identity and live overlapping edits; token
maps/labels valid; full candidate/native intervention logits max<=1e-9 and relative
RMS<=1e-10(floor1e-6), identity<=1e-9, finite, all scored groups nonempty. Use
independently exported program for candidate and native hooked reference for
endpoint messages. Verify raw E/8 by the exact captured prefix-producer identity.
This gate deliberately excludes scientific success requirements, unlike the
previous receipt's broad instrument gate; preserve that older outcome unchanged.
B raw field: native accuracy>=.8 in each raw-consumer population/hop group;
removeRaw goldP loss>=.25; mapRaw desiredaccuracy>=.8 and desiredP gain>=.5.
On forward hop3 (join consumer), abs mean original-goldP change<=.10.
C joined field: native forwardhop3accuracy>=.8, removeJoin goldP loss>=.25;
mapJoin desiredaccuracy>=.8 and desiredP gain>=.5 in each population.
On each raw-consumer group, abs mean original-goldP change<=.10.
D joint composition: mapBoth accuracy>=.8 and desiredP gain>=.5 for each
population/consumer/hop group under its own distinct field map; mean original
goldP change on hop0<=.10 for each individual/joint map. Full edited-distribution
correspondence is required byA even when the semantic rules fail. Report joint
minus single raw-logit interaction magnitudes; no zero-interaction bar applies.

All3072 variants retained. Report every gate separately; no validity-driven
suppression of semantic measurements. Any failed semantic bar rejects the dual
field abstraction as specified. In particular, do not silently enlarge the raw
field with contextual terms or retune its scale. All387968 native constants,
derived dictionary/cache and remaining opaque computations stay charged. Zero
structural coefficient saving claimed. B8FP64,1800s,<256MiB/tensor, managed GPU.
