# Hourly circuit review — 13 September2026,04:12UTC

Targets: explicit reads/operations/writes/consumers; cross-boundary grouping and within-module splitting; held-out/OOD prediction; extraction/sufficiency; selective manipulation; composition/reuse; stable identification. The full goal is a simpler predictive, composable, manipulable model. The corrected bilinear handoff governs, not stale better-math goal wording.

## Evidence and direction

The last-block conditional predictor was closed over three raw input trajectories and exported; its local effect prediction is1–1.5%error while original total nonadditivity remains61–69%unexplained by that local piece. The inherited-effect telescope directed work toblock10. Its MLP dominates local regional interaction; the direct cross-product suffices within roughly1%of that MLP effect. Residual-only input products fail in two groups; the residual–attention partner is necessary. This is one progressively specified interaction family, not many independent circuits.

The MLP9 four-vector finite response was folded into residual inputs, then full attention10 raw projections were included with both QK factors, native roundedRoPE, first values and normalization. Synthetic controls and a two-prefix nativeCPU pilot passed. A registered160-prefix CPU test now predicts the full-product local regional effects within0.095–0.215%, all96 target signs, using generated intervention inputs and joint norm. FineWeb CE2.2–7.6%errors and sign/zero discrepancies remain; the executed unfiltered audit reports absolute magnitudes. No freshOOD or autonomous extraction is established.

Next highest-information step: generate the additive output background, which still comes from native C/R trajectories. Compute predicted preMLP10 states from pristine context plus generated changes, then execute the actual MLP10 to build predicted C/R outputs. Test background and final-effect preservation separately. This changes extraction dependency, not rank or aggregate reconstruction alone. Reject if small state error creates material target-effect error; do not conceal it with effect subtraction against a different background. If successful, evaluate original N/C/P/R causal interaction with explicitly generated branches, then fresh-context transfer. Full opaque attention/MLP/suffix weights remain priced.

Alternatives: finer head/source attribution, new-corpus OOD, or another factor fit. Defer further attribution until the complete conditional predictor has a clean dependency inventory; OOD becomes higher value after that bounded closure. General rank/CP/Tucker fits do not solve the observed background dependency. CPU control also verifies a ten-product conditional response basis, useful only if repeated-strength amortization is measured; no global low-rank semantic claim.

## Confounds, red team and throughput

The intervention is the existing regional child/remainder family; rows are reused, conditional additive-background effects differ from original total interaction, and CPU-derived outputs are not GPU-equivalence evidence. First values and scalar fields are supplied. Tiny effects can change sign; absolute magnitudes are reported but a fullFP64suffix precision discriminator remains unrun. Successful identities do not provide stable semantic factor identification.

The hour includes several short GPU interaction screens and CPU algebra/result audits; peer681 has occupied lane1 since03:46:53. Authoring and repeated short result publications were avoidable overhead. A measured1.64second two-prefix CPU pilot enabled the full160-prefixCPU confirmation in105.53seconds during that wait. This is an actual scheduling improvement, not a speculative speed claim. Phase timestamps have gaps; do not invent a numerical labor split. Native screens refine one family; do not count arms as circuits.

CIRCUIT_FOCUS: PASS — cross-layer computation, conditional prediction and extraction dependencies advanced.

CEREMONY_BUDGET: PASS with repair — CPU execution now replaces repeated queue polling; use the prepared shared scorer and existing helper rather than another audit framework. Short repeated turn updates should not trigger full reports or more ceremony.

NOVELTY_LESSON_GATE: PASS — MLP10 and specialist-head dossiers checked; residual-only failures and supplied-background limits preserved. No new whole-head identification or fitting sweep.

Next hourly05:12; next mathematical review05:29. Execute the small generated-background CPU control while retaining the already queued GPU experiment unchanged.
