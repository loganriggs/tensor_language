# Hourly strategic and circuit-throughput review — 2026-09-07 23:15 UTC

## Circuit interpretation targets and full goal

A useful circuit must provide: (1) a computational specification of what is read, computed, and
written; (2) cross-boundary grouping and within-module splitting rather than assuming native heads
or MLPs are semantic atoms; (3) held-out and out-of-distribution causal prediction; (4) an isolated
executable circuit or explicit sufficiency interface; (5) selective removal, swapping, and editing
with redundancy and interactions measured; (6) predictable composition and reuse; and (7) stable
identification across documents, constructions, gauges, and fitting restarts.  The full objective is
still a smaller transparent tensor program that is jointly predictive, composable, manipulable, and
simpler under literal storage, compute, edge, state, and program price.  Rank one is only a matched
projector family here; it is not circuit evidence by itself.

## What changed since 22:15

The target-feasible v15 DAS result was interpreted rather than reduced to its machine label.  It
validly improves cross-fit A1 transfer over matched difference in means (`.8719` versus `.7250`) and
is geometrically stable (minimum fold cosine `.8474`), but fails construction-general selectivity:
sealed A2 is `.6489 < .75`, and one P fold has mean KL `.01945` plus one flip versus DIM's `.00678`.
A complete 30-configuration comparison found that the registered noise/Jacobian settings alter
selection score by less than `.0005` and never improve flips.  This closes another coefficient grid:
the optimization objective can find a stronger target direction, while local smoothness does not
supply missing construction observations.

A history-disjoint v16 construction bank was authored and frozen.  Its capability-only executor is
hash-bound first in managed lane 1 behind a continuously verified live v236 job; it opens exactly two
native forwards and no patch or causal outcome.  A first enqueue rejection exposed missing exported
prediction metadata and was repaired before model access.  V16 A1/A2/P are rowwise token aligned;
its inherited C panel is not and is excluded from every future patch claim.

The multi-construction decision was converted into shared machinery rather than another copied
optimizer.  `multi_environment_projector_contract.py` fail-closes missing/nonfinite panels, gives
per-construction target feasibility strict priority, and scores the worse P/C control rather than a
pooled average.  The formal rank-one prior and reusable smooth fit core are committed.  A thin causal
executor is implemented through the sealed boundary: it fits v15 A1/A2 with separate P/C controls,
compares factor-SVD, joint-DIM, and frozen-A1 starts, freezes both fold projectors, and only then
constructs v16 A1/A2/P.  Twenty-three focused tests and the experiment gate pass.  The only deliberate
placeholder is the not-yet-landed capability receipt hash, so premature execution is impossible.

The user's weight-tensor route also produced a concrete diagnostic.  Each selected head direction
was mapped exactly through its output weight and backward through its value weight.  Fold cosines are
`.9368-.9923`; every source ranks L15H5 q/k/q2/k2/v among its top ten downstream interfaces; and
value pullbacks recover `L8H1 -> {L9H1,L9H4} -> L11H3` at `.9725-1.0` percentiles.  The immutable
first artifact remained invalid under its absolute `1e-7` closure bar; a preregistered zero-model
audit obtained relative error `2.5796e-8`, below eight float32 epsilons, without changing any score.
This is an explicit reader/writer compatibility hypothesis, not causal proof.

## Confound audit and path decision

- Baseline subtraction and nonlinear composition are controlled by exact base/donor references,
  ascending absolute clamps, complete attention-15, and identity parent replay.
- Frame mixing is addressed by requiring A1 and A2 separately; an average cannot hide a failed
  construction.  P and C likewise remain separate worst-control panels.
- Shared token difficulty is checked by native capability first and rowwise alignment.  No failed
  v16 text will be repaired after outcome access.
- The zero-effect complement cheat is infeasible under hard target bars and fixed unit dose.
- Dead regularization knobs are removed: rank, noise, and Jacobian coefficients are not searched.
- Precision floors are represented relative to the write norm in the completed weight audit; the
  causal executor retains absolute parent-replay tolerances where logits have fixed scale.
- Post-selection leakage is prevented structurally: the v16 bank is constructed only after v15
  selection, with a regression testing source-code order and a missing capability hash blocking run.
- Fold stability is necessary but not construction stability.  Exact weight alignment is a screen
  for interfaces and cannot substitute for reset/rescue or sealed causal transfer.

The current path remains the highest-information route.  It directly tests whether the same
head-local coordinate survives complete construction shifts.  The alternatives rank as follows:

1. **Fixed rank-one multi-construction causal transfer.**  Changes held-out/OOD prediction, stable
   identification, within-head splitting, and selective manipulation.  Kill it if no moved v15
   checkpoint satisfies both target constructions, or if a v15-selective checkpoint fails sealed
   v16 relative to the frozen A1-only projector.
2. **Input-conditioned projector or finite routed mixture.**  Changes computational specification
   and cross-construction composition.  Promote it only after the fixed operator fails; kill it if
   the routing rule itself cannot transfer to a sealed construction without target labels.
3. **Weight-predicted L15H5 reset/rescue.**  Changes reader identification and extraction.  Promote
   it only for a construction-stable causal coordinate; kill the edge if resetting the predicted
   interface does not selectively remove and rescuing it does not restore the coordinate's effect.
4. **Further rank or regularization sweeps.**  Rejected as primary work because they would change
   capacity or local fit without resolving any circuit interpretation target.

## Throughput and systems audit

Authoritative commits show the following serial intervals: v16 capability design through valid
queue receipt took about six minutes; the exact weight-interface prior through first result took
about three minutes, with two execution-only bugs repaired in another three minutes; its zero-model
audit and publication took about eight minutes; and the formal multi-environment prior through a
gated sealed executor took about eight minutes.  The temporal lane therefore produced one deep DAS
receipt, one exact weight diagnostic plus a repaired audit receipt, one capability candidate in the
managed queue, and reusable selection/fit infrastructure during the hour.  Scientific design and
implementation dominated; focused validation ran in seconds.  GPU waiting did not create idle time:
the fit contract, executor, tests, weight translation, and audit all advanced while v236 remained
live.  The broad lane separately queued v247 and v249 without displacing the temporal order.

The two weight-runner defects (a misplaced scalar conversion and label-set comparison) were small
but show that even zero-forward diagnostics need focused algebra/ranking tests.  Those regressions
now exist.  The multi-construction implementation reused the parent patcher and shared selector
instead of copying a second 900-line optimization harness, repairing the prior ceremony-budget
failure.

CIRCUIT_FOCUS: PASS

CEREMONY_BUDGET: PASS

NOVELTY_LESSON_GATE: PASS

## Immediate action

Keep the verified v236 handle and the hash-bound v16 capability job in place.  When v16 lands, score
its registered capability predictions without text adaptation.  On a screen, bind the immutable
receipt hash into the already tested multi-construction executor, run its no-model/gate suite, and
enqueue it through the managed runner.  On a capability null, preserve it and author a different
construction family before any causal access.  No rank/noise grid or weight-only adoption step is
licensed.
