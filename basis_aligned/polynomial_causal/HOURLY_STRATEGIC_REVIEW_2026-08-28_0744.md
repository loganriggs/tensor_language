# Hourly strategic review: preserve attention's tensor program

Date: 2026-08-28 07:44 UTC

Status: local evidence consolidation plus safe CPU implementation. This document
grants no new data-role, selection, final, or promotion authority.

## How much is actually explained

- Topology is inventoried at 36/36 attention and MLP sites.
- Named behavioral explanation is $32.1\%\pm6.4\%$.
- Named causal recovery is $10.923\%$.
- Strict admitted executable recovery is still 0% of the distinct $+0.8976$ ship gap.
- Within the separate discovery compiler, the best local table-plus-linear class
  recovers 36.6% of the $+1.7460$ one-at-a-time sum and leaves 63.4% as a class and
  composition shortfall. This is not interchangeable with the strict ship denominator.

The cost balance is worse than previously reported: the rank-8 point contains 0.664M
learned factor values but 224.737M active table values. The current implementation
allocates 2.084B dense table values and still executes/falls back to the native model.
It is a causal probe, not an executable compression point.

## New localization

The final-CE hybrid oracle restores either native tensor class while compiling the
other. Exact attention restoration gains $1.33851$ held-out nat; exact MLP restoration
gains $0.16135$ nat. Attention is $8.30\times$ larger in this context. Interaction is
$-2.17559$ nat, so the halves are strongly redundant and their effects cannot be
summed. Controls pass. This localizes the dominant missing primitive to the squared,
content-routed attention contraction.

The concurrent downstream-objective run moved the current local class from $+0.38578$
to a selected $+0.58315$ held-out nat. It rescued the lag-1-plus-prefix-mean class from
$-0.80166$ to $+0.31212$, but that richer class remains worse than the current-state
class despite twice the factor count and a slightly larger complete table-plus-factor
description. Thus objective mismatch was real, but it does not rescue the proposed
nonlocal grammar. The completed GPU interval was used for CPU-side executable work.

## Pruned candidate space

- More rank in the output-map class is pruned by the rank-32/rank-128 tie.
- More coordinate passes are pruned by an exact zero-change fixed point.
- Fixed lag/prefix features are pruned: final-CE training rescues their sign but still
  leaves them $0.27103$ nat below the simpler current-state class at greater cost.
- Factor-only nats/M is pruned as a simplicity score because it omits the dominant
  tables and native fallback.
- More isolated token clustering and semantic correlations are pruned until they
  predict a downstream consequence or executable interface.
- Generic CP/TT compression that deletes RMSNorm, RoPE, the value bus, or the squared
  QK product is pruned even if local MSE is low.

## Top five actions after pruning

1. **Zero-native-call tensor-preserving attention compiler.** Highest information gain,
   causal relevance, and composability; it directly instantiates the winning oracle
   class. First test ranks 384/512 for routing and values while keeping the tensor
   contraction and output map exact and fully priced.
2. **Shared QK input dictionary and gauge-canonical storage.** A shared encoder plus
   four typed decoders can reduce QK storage beyond six independent low-rank maps and
   produces an identifiable interface for circuit extraction. It is falsified by
   joint CE and gauge-replay instability.
3. **Downstream-weighted bilinear MLP product selection.** Attention dominates, but
   MLP restoration remains positive. Rank exact products by suffix consequence and
   require equal-$K$ improvement over the 29.4% local selector.
4. **Execute the sealed MLP0 predictive quotient when lifecycle authority exists.**
   This targets editability, selective removal, and OOD semantic transport rather than
   the immediate whole-model CE bottleneck.
5. **Consequence certification harness.** Compare equal-fidelity programs on complete
   bits/FLOPs, data-doubling stability, OOD CE, extraction, selective removal, and
   collateral damage. It validates a simplicity definition by what it enables.

## Safe action executed

Implemented `tensor_preserving_attention.py` and froze
`TENSOR_PRESERVING_ATTENTION_PREREGISTRATION.md`. The kernel directly evaluates all six
stored projections, per-head RMSNorm, RoPE, the two QK contractions and their product,
causal mask, first-layer value bus, value contraction, and output projection. It has
total input support, no token tables, no native fallback, and reports storage plus
multiply-adds. Focused CPU tests pass 5/5, including dense formula identity and execution
after every native projection has been made uncallable.

This is the first compiler object in the current arc whose simplicity denominator can
legitimately exclude the native attention module. It has not yet earned a behavioral
frontier point; the exact 18-layer identity gate and registered rank arms remain to run
now that the nonlocal downstream job has closed.

## 07:50 addendum: the local lexical table also has a small shared code

The registered table-rank sweep completed after the review was drafted. Compressing
the 36 covered-token tables jointly per site to rank 64 changes complete conditional
table-plus-factor storage from 225.442M to 15.886M values (7.05%) while held-out
recovery rises from $+0.38578$ to $+0.54064$ nat. Rank 16 costs 4.531M and still recovers
$+0.46878$, falsifying the prediction that it would fall below half the full-table
fidelity. All controls pass and cost efficiency rises monotonically over the tested
ranks.

This is strong evidence for the earlier “shared lexical code plus continuous context
refinement” account: the token-dependent mean outputs across 36 sites live in compact
site-specific rank-$r$ dictionaries, and a small continuous current-state correction
adds context. It is not yet a standalone program: the hook still runs/falls back to
native modules on uncovered tokens, about 24% of scored positions, so 15.886M is a
conditional description rather than executable whole-model cost.

The updated architecture hypothesis is therefore hybrid:

1. rank-16--64 lexical dictionaries plus continuous corrections for token-local MLP
   structure;
2. the exact squared-attention tensor operator with compressed/shared typed
   projections for content-routed transport;
3. exact bilinear MLP products added only where downstream consequence requires them.

Priority remains the attention identity/composition test because the hybrid oracle
assigns it the largest behavioral shortfall. In parallel, the table program must gain
total vocabulary support and zero native calls before its excellent conditional curve
can count as executable compression. The first joint frontier is the factorial crossing
of those two independently executable pieces, not either local curve alone.
