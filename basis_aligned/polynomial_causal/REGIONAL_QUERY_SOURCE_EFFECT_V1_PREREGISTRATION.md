pred_a baseline relative<=1e-5 against saved native margins.
pred_b source-only signed margin-effect relative<=.1 against full query+source swap EACH8cells.
pred_c query-only same. Null: opposing query effect remains functionally material.
0bodyforwards96cachedrows7suffixarms180sec,~2MB; native background fixed.

Saved CPU hybrid writes are frozen. Full replacement swaps both ports using donor branch output; source-only swaps all source inputs with recipient query; query-only swaps query with recipient sources. No scalar repair or data fitting. All eight cells scored; signs and nonadditivity descriptive.
