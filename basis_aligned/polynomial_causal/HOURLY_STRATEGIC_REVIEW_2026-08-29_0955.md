# Hourly strategic review — 2026-08-29 09:55 UTC

## Update this hour

The highest-priority prerequisite for Entry Point 4 now exists as a real artifact.
The outcome-blind terminal-copy row freezer published four fresh, mutually separated
roles and their token-level copy/control labels:

- 192 fit documents from natural text;
- 192 selection documents from natural text;
- 192 final-replication documents from natural text; and
- 192 source-code files as the repository OOD role.

The natural roles contain 576 distinct source documents. The code role contains 192
distinct files. The selection role has 303 positive and 303 matched-negative token
positions; the final role has 247 of each; the code-OOD role has 1,294 of each. Each
role passes the prospective minimum of 48 positions and 24 documents per polarity.
The receipt is
`basis_aligned/bilinear_quotient/terminal_copy_induction_v2_rows_receipt.json`
(SHA-256 `aea52a94c643906ef822a7c6ddb37a371b4315507a1a0a79acd539a19ae7f5c8`).

This is not yet an E4 model result. The freezer is incapable of loading the checkpoint,
and its receipt records zero model imports, zero checkpoint loads, and zero forward
calls. It removes the row/label blocker so the causal attention screen can now be built
and run without choosing examples after seeing effects.

The first v1 attempt failed safely because an old authority referred to a row tensor
that its own failed transaction never produced. V2 did not weaken the registry census:
it pins that exact authority, its internal hash, the exact `$.outputs.rows` pointer,
the exact failure, the original source closure, and the absence of rows, manifest,
receipt, lock, and staging files. It skips only that one pointer while loading every
other reference normally. An independent audit passed 59 tests; the real census loaded
112 registry JSON files and 29 prior row tensors and found exactly one lawful omission.

The separate S1912 discovery result also completed. Model confidence and compiled-
program confidence are only weakly correlated (Spearman 0.109--0.120), yet each
roughly doubles model/program agreement. Program-confident positions have about
9.84--10.69x agreement enrichment versus 4.95--5.82x when the program is unsure,
even inside the model's highest-confidence quartile. This suggests a precomputable
per-token reliability variable, but it changes no strict explanation ledger. S1913 is
testing that consequence now and should not be duplicated.

## How much of the model is actually explained?

There is no honest single percentage because the project has four different ledgers:

1. **Removal-certified storage:** 29,196,288 of 545,904,054 stored values can be
   removed under the current certificate: **5.3482453%**. The remaining
   516,707,766 values are 94.6517547%. Earlier prose saying 5.3481% is a rounding/
   arithmetic inconsistency; 5.3482% is the four-decimal value of the authoritative
   counts.
2. **Named causal cross-entropy:** 0.57968 of the 5.30682-nat reference CE is assigned
   to named causal mechanisms: **10.9233025%**. The unnamed remainder is 4.72714 nat,
   or 89.0766975%.
3. **Structural access:** all 36/36 intervention sites can be intercepted. This says we
   can manipulate every interface, not that its computation is understood.
4. **Terminal practical actions:** 0/68 registered extraction, selective-removal, and
   OOD-transport actions have passed. E4 is intended to create the first one.

## Largest remaining gaps

- The 4.72714-nat unnamed causal residual is the largest behavioral gap.
- 516,707,766 stored values still lack removal certification; dense MLP banks alone
  contain 286,675,200 values.
- No circuit has yet passed the complete extraction/removal/OOD lifecycle.
- A closed native-free program for uncovered tokens is missing. The E1 linear stream
  closure costs 1.08978--1.27276 nat, and refitting on its own closed inputs worsens
  this to 5.49867--5.61939 nat.
- A composable low-dimensional state interface is missing. In E3, even a destination
  oracle at rank 64 has 0.2709 normalized error; direct and chained transport errors
  are 0.4861 and 0.4520.
- The Family-F polynomial port failed its registered local fit gate
  (NRMSE 0.70275--0.78860). The promising native-Down measurement remains a fit-only
  diagnostic until its fresh two-sided causal port is run.
- E2 found an equal-price compression signal at tight storage, but no global or typed
  basis passed the semantic and large-budget gates. It therefore does not yet supply
  interpretable shared coordinates.

## Ranked next actions after pruning

1. **Finish the E4 attention-only dispatcher and causal streaming lifecycle, then run
   selection.** This is now the highest-return action: the rows, labels, adapter,
   candidate grammar, and simultaneous bootstrap exist. It directly tests a named
   behavior and can lead to extraction/removal/OOD evidence. GPU cost is moderate and
   the result is sharply falsifiable.
2. **Repair and run the fresh two-sided native-Down finite port.** This asks whether the
   locally faithful native Down factorization has a causal, composable advantage over
   Family F. It is nonredundant with Family F's failed local-NRMSE fit.
3. **Fit a native-free nonlinear fallback only on uncovered current tokens.** Covered
   token tables are already essentially exact; E1 showed that another global linear
   closure is poor. Attention-conditioned or sparse nonlinear state should therefore
   be tested only where the present program fails.
4. **Run one bounded rank-64/128 shared-private or causal-weighted allocation.** This
   tests whether reuse plus site-private directions can improve the exact tight-budget
   frontier. Large-rank revivals are pruned because E2 already made them uncompetitive.
5. **Turn S1912/S1913 reliability into a prospective allocation or deferral test.** A
   per-token confidence table is useful only if it predicts which compiled outputs can
   safely be trusted on fresh/OOD rows or improves allocation at fixed price. Another
   retrospective enrichment plot would be redundant.

Ideas pruned for now include more global rank-512 linear closure, more HOSVD using the
failed fixed projectors, local MSE-only gate refits, and unsealed diagnostic sweeps.
They either duplicate decisive negatives or do not compose into a whole-model causal
program.

## Immediate execution state

The E4 v2 row/label authority is complete and awaits a post-publication independent
replay. The next active CPU task is the physical per-candidate attention dispatcher and
receipt lifecycle. The GPU is occupied by S1913's nonredundant token-reliability run,
so no competing GPU job should be launched until it releases the device. E4.1--E4.3
remain unchecked until model interventions actually run; a row cache is a prerequisite,
not evidence.
