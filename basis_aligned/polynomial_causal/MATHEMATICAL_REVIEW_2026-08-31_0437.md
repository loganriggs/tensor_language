# Mathematical review — 2026-08-31 04:37 UTC

Convention (§2135): damage = CE added above the real model; lower is better. Grounding: §2184–§2189 (scalar
merge, corrected twin table, DC dominance, circuit-grain verdict), rungs 97/98 in flight.

## The unifying observation (executed analysis)

Every construction attempted in the last 36 hours sorts cleanly by one property — whether it is built from
**document-gauge-INVARIANT quantities** or requires **gauge-COVARIANT coordinates**:

```
  construction                      quantity class          outcome
  scalar gain on a twin (§2184)     invariant (amplitude)   WORKED (55%)
  mean-vector stand-in (rung 98)    invariant (μ is doc-independent by construction)   pending, predicted to work
  norm-ranked pruning (§2140/44)    invariant (norms)       WORKED (the only frontier improvements)
  fixed cross-twin linear maps      covariant               FAILED twice (0.106, −1.03)
  pointer/induction/bigram reads    covariant addressing    FAILED (≤ +0.011)
  class dictionaries at retrieval   position-local covariant FAILED (worse than deletion at a16)
  Fisher-direction selection        covariant directions    FAILED on the frontier (§2125)
```

**Candidate law (sharpening §2181):** the compilable subalgebra of bilin18, so far, is exactly the
gauge-invariant one — means, amplitudes, norms, spectra; everything requiring coordinates needs per-document
binding first. The DC finding (§2187) slots in: a module's output splits f(x) = μ + g(x), with μ invariant
(load-bearing at the front: the assembler pair's +9.07 joint knockout was μ-compounding) and g gauge-bound.
This predicts rung 98's outcome in advance (the μ stand-in should hit its bar) and prescribes where to look for
the next working constructions: invariants only, until the gauge is quotiented (rung 90 instrument still the
gate).

## Top three mathematical moves

1. **Causal-abstraction certification at circuit grain (bisimulation made operational).** Object: any compiled
   config vs the real model. Definition: the config is a valid abstraction at tolerance τ iff no battery
   circuit's member mean|dCE| under the config exceeds τ (interventions commute with the abstraction within τ).
   §2189 already ran the machinery once in 16 seconds. Measurable consequence: the coverage ledger becomes a
   per-circuit certificate; "replaced component" claims get a pass/fail per circuit. Cheapest experiment:
   certify the CURRENT best frontier config (§2144 assembly hooks under the census rows, all 62 circuits) —
   preregistered as rung 99. Assumption that may fail: census rows are FW-adjacent; a config fit on FW may look
   better there than on fresh text — report both framings.
2. **The invariant/covariant dichotomy as a selection rule** (above, executed as the table). Falsifier: any
   covariant construction that works without per-document binding, or any invariant one that fails — rung 98 is
   the live test of the second kind.
3. **The affine skeleton** (f = μ + g modelwide): the all-means model's CE is a measurable floor and the DC
   ledger (rung 97, running) prices the μ-terms per component; if DC shares are high modelwide, a two-part
   grammar (36 μ-vectors + signal terms) becomes the front's canonical form. Cheapest next: one all-means arm
   appended to a future rung.

Pruned: everything coordinate-based until rung 90's document-grain instrument exists; spectra-conjugation
retest at row grain (§2183: unidentifiable); further pointer variants (§2161 pause holds).
