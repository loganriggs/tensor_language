# Hourly strategic review — 2026-09-08 09:22 UTC

## Circuit targets and full goal

The interpretation targets remain: (1) specify what is read, computed, written, and consumed;
(2) group across native modules and split native modules where downstream use differs; (3) predict
held-out and OOD effects; (4) extract an executable circuit or explicit interface plus background;
(5) support selective removal, swap, and edit; (6) predict composition and reuse; and (7) identify
units stably across text, controls, and gauges. The program goal remains a smaller transparent
tensor program that is jointly predictive, composable, manipulable, and simpler under literal
storage, compute, edge, state, and program prices. The present partial edge is not completion.

## What changed since 08:15

Two decisive outcomes landed after the long unrelated v254 queue predecessor cleared.

1. The prospectively weight-ordered greedy screen passed A--E. Original FIT selected temporal P3
   (`L7H7+L9H4+L9H1`) and is-was P2 (`L7H7+L9H4`), exactly matching the 08:39 pre-outcome
   forecast. The identities validate without reselection on original HOLDOUT and OOD. This reduces
   five causal candidates to a shared two-head physical core plus temporal L9H1.
2. Exact writer-to-formula mediation passed authority, replay, algebra, price, and selectivity, but
   falsified one shared end-to-end edge. The source-local L11H3 formula mediates `.783-.799` of the
   temporal writer effect with cosine >=`.9978`, but only `.165-.203` of is-was despite cosine
   >=`.9764`. Every direction is correct. Formula/head/logit error is <=`9.54e-6`, replay and
   causal-zero errors are zero, and collateral <=`.00203`. C/D are therefore scientific failures,
   not instrument failures.

The earlier localization repair remains scoped: the original receipt is still invalid, and only
its separate like-for-like audit licenses B--E. The present result does not retroactively turn the
postcue region into the full upstream route.

## Strategic decision and genuinely different routes

The highest-information next experiment is a nested L11H3 route-closure ladder using the same
selected writers and captures:

1. source-local induced value under recipient-native routing (replay of the current mediator);
2. full-causal-prefix induced value under recipient-native routing;
3. the full observed L11H3 head-3 preprojection delta, including writer-induced q/k/q2/k2 changes.

The opposing predictions are sharp. If arm 2 rescues is-was, the compact writers spread task value
outside the donor-local postcue region before L11. If only arm 3 rescues it, writer-induced routing
is essential. If arm 3 remains low, most selected-writer behavior bypasses L11H3 and the circuit
must split the apparent shared writer into an L11 source branch and a different downstream branch.
Temporal provides a positive control: all three nested arms should preserve its already held
source mediation.

Ranked next moves:

1. **Nested L11H3 route-closure ladder.** Changes computational specification, within-module
   splitting, extraction, reuse, and stable identification. Kill source spreading if full-prefix
   value remains near `.20`; kill L11H3 closure if the full head remains below `.75` recovery.
2. **If full L11H3 head closes is-was, factor induced routing versus value.** A q/k/q2/k2 factorial
   then names the operation. Kill it if no routing arm predicts the full-head increment on
   held-out/OOD rows.
3. **If full head does not close, downstream destination atlas for L7H7/L9H4.** Search exact weight
   readers followed by causal head/module patching, not activation reconstruction. Kill each
   candidate if its mediator does not predict held-out/OOD writer effects.
4. **Join temporal source edge to the licensed H4 program.** Retained but demoted until the is-was
   shared-edge ambiguity is resolved; otherwise composition could hide two different routes.
5. **Regularized DAS.** Still deferred. Noise/KL may address memorization, but a fitted subspace is
   lower information than the exact nested physical mediators now available.

The route-closure ladder survives alternatives because it distinguishes three causal graphs in one
small intervention and directly tests the user's requested cross-boundary splitting. It is not a
rank, reconstruction, compression, or variance-preservation sweep.

## Confound audit

- **Baseline/frame mixing:** every mediator is scored against the same directly replayed selected
  writer effect; command-gold collateral remains a separate metric.
- **Nonlinear composition:** the ladder executes nested interventions; it does not infer route
  closure by adding scalar recoveries.
- **Shared difficulty/leakage:** selected identities are frozen from original FIT and cannot change;
  original HOLDOUT and OOD remain validation-only.
- **Post-selection:** the three mediator definitions and route decisions are fixed before outcome;
  no threshold or source position can be tuned on OOD.
- **Dead knobs/coverage:** source replay, full-prefix value, and full-head arms need distinct live
  tensor checks plus native/reference replays and exact shape/call inventories.
- **Precision/noise:** the current exact algebra error is `~1e-5`; routing decisions require effect
  differences orders of magnitude larger and retain the existing `1e-4` equality bar.
- **Native-boundary overclaim:** L11H3 is a physical site, not presumed semantic. A full-head pass
  would still require factor splitting; a failure explicitly licenses a cross-module bypass search.
- **Complement/DAS overfit:** no learned complement participates in this decision, so training-task
  memorization cannot manufacture closure.

## Throughput and systems checkpoint

From 08:15 to 09:12, the only GPU lane was occupied by the pre-existing v254 family job. Circuit
work used that wait to finish mediation authority hardening, tests, a pre-outcome prefix forecast,
the canonical dossier, and the Astra restart handoff. Once the lane cleared, the greedy result ran
09:12:36--09:12:42 and the mediation result ran 09:17:56--09:18:01, with the intervening five
minutes used by another queued batch. Both circuit experiments took about six seconds of GPU time.
Their scientific interpretation, binding, tests, publication, commits, and pushes took about nine
serial minutes total. The two promoted screens therefore landed six minutes apart, beating the
ten-minute operating target once queue contention ended.

Repeated authoring was limited to one result note and dossier update per promoted outcome. The
only failed check was a useful state-transition test: it assumed the binding must be absent and was
repaired to verify bound/unbound parity plus exact authority. Safeguards did not dominate the
scientific screen; GPU contention dominated the hour.

CIRCUIT_FOCUS: PASS

CEREMONY_BUDGET: PASS

NOVELTY_LESSON_GATE: PASS

Immediate continuation: preregister and implement the nested source-value/full-prefix-value/full-
L11H3-head route-closure ladder, reuse the existing row builders and accounting, then enqueue it
through the managed runner. No rank/compression or DAS fitting is opened.
