# Bracket cascade source-component factorial V1

The exact L13H8 donor source transfers on the sixth and seventh constructions
even after removing its full downstream MLP13--17 response. The frozen
donor-free rank-two source transfers on the sixth construction but narrowly
fails one seventh-construction ordered pair. This localizes the unresolved
boundary inside the source generator.

On the already frozen seventh cascade rows, split the source term into its two
multiplicative attention-score factors and projected payload. For each factor,
use either the exact donor value or the frozen donor-free rank-two type-table
approximation. Run the complete eight-corner factorial while retaining the
native recipient query and all native suffix computation. Compare every corner
with the exact-donor behavioral effect, including each ordered pair and
controls. Compute the three-way behavioral inclusion-exclusion residual without
fitting.

Choose the most compressed passing corner in fixed order: all three
approximations; both keys; key1 plus payload; key2 plus payload; payload only;
key1 only; key2 only. Require overall cosine `.90`, relative L2 `.40`, sign
`.90`, and norm ratio `.60`--`1.40`; every ordered pair requires cosine `.75`,
relative L2 `.50`, and sign `.85`; controls must be at most `.50` of exact
target RMS. Exact effects must be positive on `.90` of every pair, native replay
must be within `1e-5`, and all frozen program ranks must equal two.

This is a diagnostic source-factor localization. Exact donor components are
ceilings and are not an executable donor-free program. No coefficient, table,
rank, query, row, subgroup, construction feature, fit, gradient, update, gain,
bar change, or quantization is allowed. Exact price is 10 forwards, 1,440
sequences, zero fits, backwards, and updates.
