# Join payloads: exact original-token and prefix-producer split v1

Two disjoint L2 join writes pass selective native removal and exact joint
restoration/reuse across query forks, but not interchangeable contextual writes.
The opened-case factor audit points primarily to transported V dependence;
replacing scores alone still misses quantitative causal-effect fidelity.

Hypothesis: forward H1 transports the source edge's current value token; backward
H2 transports its origin key, written by earlier attention. Identify the native
producers of these fields instead of fitting another local-record head model.

Use the same two-chain/context generator algorithm as JOIN_CONTRIBUTION_CONTEXT_V1
but fresh IID/OOD seeds18909/18910,16 independent worlds each, two arrangements and
eight queries perworld. Use recipient context only; donor cases are not outcomes
for this test. Preserve both chain facts and original source masks and heads.

The architecture gives exactly x2=E/4+Y0/4+Y1/2, where Y0,Y1 are native attention
output projections before residual mixing. Compute all three through the native
prefix. With g=the live original L2 RMS gain, split native V linearly as

 V=Wv(g E/4)+Wv(g Y0/4)+Wv(g Y1/2).

Use the original complete L2 routing scores and O, restricted to the previously
identified four source→destination cells perroute. Three writes sum to its exact
native contribution. This is an explicit compiled V-term intervention, not removal
of an entire prefix attention layer; g and routing remain live native quantities.
No cached donor activations, learned dictionary or fit. All native parameters and
computations remain charged; discovery of producer dependencies is the target.

Evaluate native; remove each route/both; restore all8 E/Y0/Y1 subsets to each
individually removed route; and jointly restore the fixed semantic candidate:
E-only for forward H1, (Y0+Y1)-only for backward H2. Subsets are a complete exact
factorial for interpretation, not a best-subset search. Restore all terms jointly
as exact execution control. Recompute all downstream normalized nonlinear readers.

A instrument: synthetic nonzero three-term closure, native x2 producer identity,
selected-write sums and joint all-term logits<=1e-9; finite; native hop3 accuracy
>=.8 and own selected-route cut goldP loss>=.25 each population/arrangement/query.
B proposed fields: fixed E-only forward and Y0+Y1-only backward each restore>=.8
of native-minus-cut own-hop3 gold probability; complementary writes restore<=.2.
C fixed joint candidate vsnative: full29-way distributions meanKL<=1e-3,p99<=1e-2
on all positions and final query separately, each population/arrangement.
D quantitative single/joint effects of these fixed candidates: centered full-logit
(restored-cut) vs(native-cut) relativeRMS<=.01; absolute<=1e-8 below nativeRMS1e-6;
all positions/query separately, each population/arrangement.

B/C/D failure preserves exact factorization but rejects the corresponding semantic
sufficiency/quantitative prediction claim; no posthoc subset selection or fit.
A pass identifies an explicitly produced field and its consumers, not whole-model
extraction or lower description cost. B4 FP64,1800s,<256MiB per tensor, managedGPU.
