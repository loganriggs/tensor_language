# Three-hourly mathematical review — 2026-09-03 01:07 UTC (Claude)

Grounded in the completed MLP10→consumer descent (§2606-era through §2648) and
§2647. Sign convention §2135 (CE added above native, lower better).

## The state the mathematics must address

Six exhaustive nulls (506–514) prove: below the ACTION grain, NO linear object
— whole-write, term, family, latent atom, pair, three-branch, or any of 113,520
sparse signed sums — is a cross-action circuit variable for the four
gauge-related equality-score implementations. The positive residue (§2647): the
implementations' pairwise difference lives in ONE fixed low-dim factor direction
with an approximately-separable (≤4% raw) magnitude. Codex routes to nonlinear
readers. The strict explained fraction is unchanged (5.348% / 10.923% / 4.727
nat / 0 of 68; frontier §312 norm-2304 at +2.6735 CE-added). The open
mathematical question: linear grouping is exhausted — what rigorous handle
remains, and what does the gauge REALLY cost the program?

## Ranked moves

### 1. Task-space effective rank / sufficient dimension — EXECUTED (CPU)
**Object.** The four implementations' finite copy-TASK effect vectors (513's
per-implementation per-document per-context-cell CE effects), NOT their
write-space mismatch. **Theory.** Effective rank of the 4×(docs·cells) effect
matrix = the number of score templates the copy TASK actually needs; a
sufficient-dimension statement (Cook, regression graphics) rather than a
write-space grouping. **Assumption that may fail.** Task-space collinearity
could be a document-sampling artifact — handled by the held-out per-half
reproduction below. **Consequence beyond reconstruction.** If the task-effect
is ~rank-1 across implementations, the copy task needs ONE template regardless
of the write-space non-groupability (506–514) — a TASK-space argument for the
§2633 dictionary reduction, independent of the write-space §2647 argument, and
the empirical basis the coverage-credit thread needs. **Cheapest falsifier.**
Registered below; CPU, zero forwards. (Exploratory pooled value motivated the
direction; the SCORED bar is the unpeeked per-half reproduction + cross-half
direction stability.)

### 2. Gauge-invariant sufficiency (invariant ring / Reynolds) — PROPOSED
**Object.** The Z2 sign gauge (§2633) as a group action on the score. **Theory.**
For a Z2 action the invariant ring is generated by even functions; if the copy
reader is a function of gauge-INVARIANTS only, the gauge-variant mismatch
(§2647) is causally INERT for the copy task despite being write-present.
**Consequence.** A mechanistic "the gauge is free" statement: implementations
are copy-equivalent, differing only in a copy-inert subspace. **Falsifier.**
GPU: remove the §2647 mismatch direction, recompute the suffix, test whether the
COPY effect is unchanged (inert) — but this OVERLAPS Codex's factor-removal /
nonlinear-reader machinery, so proposed for their lane, not executed here.

### 3. Information bottleneck minimal sufficient statistic — PROPOSED
**Object.** The score→copy-target channel. **Theory.** IB (Tishby et al.): the
minimal sufficient statistic of the score for the copy prediction; its dimension
lower-bounds the compiled score representation. The information-theoretic dual
of move 1. **Falsifier.** Estimate I(score; target | context) sufficient
dimension; blocked on a clean conditional-MI estimator at this scale — parked
with the dependency named.

## Pruned
Linear grouping in all forms (506–514 exhaustive); latent dictionaries (509
closed pre-CUDA); activation-Hankel (RMSNorm non-composability); another write-
space mismatch slice (§2647 + magnitude addendum already complete). Literature
fetch skipped: the binding objects are in-house; classical cites suffice (Cook,
Regression Graphics 1998; Tishby-Pereira-Bialek 1999; Beckers-Halpern 2019).

## Executed
Move 1: TASK_SPACE_SUFFICIENT_DIMENSION_PROBE preregistration + CPU run this
wake, per-half prospective bars. Grounds the coverage-credit dictionary claim
in task space.
