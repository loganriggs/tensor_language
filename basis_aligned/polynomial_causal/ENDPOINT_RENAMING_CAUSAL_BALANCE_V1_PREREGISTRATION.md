# Does remaining computation compensate changing endpoint-removal effects?

Result-dependent continuation after the cross-layer path screen. That screen
finds hop3 sign compensation but rejects both conditional path transport and
physical-removal prediction. Use its saved native query logits L and actual
endpoint-write removal vectors R. No model execution, new field, fitting or
additional example selection. All384 paired comparisons and all population/hop
groups remain, with the existing32 opened worlds.

The base/both tokens are related by the known middle/foil label transposition pi.
Align donor output coordinates with pi derived from metadata, not a fitted
rotation. This matters for hop2, whose correct answer itself is renamed. Assert
the known answer correspondence. Do not choose between raw/aligned metrics
after seeing results. The grouped-path null remains as originally registered.

Let B=L-R be the actual query logits after removing the endpoint write. In the
common label frame, compute the exact identity

    delta L = delta R + delta B.

This uses physical removal, not the previous frozen path. Report centered RMS
of each vector, their cosine, and projections of delta R and delta B onto
delta L (with denominator floors explicitly counted). No projection is an
independent causal variance fraction. The exact identity is an accounting
check, not evidence that a meaningful circuit has been extracted.

Opposing predictions: a context-invariant causal field has relative RMS
delta R/R_base <=1% in every population/hop group (floor1e-6); a changing
removal effect with compensating remaining computation fails this test while
delta R and delta B oppose each other and delta L is smaller. Report all groups
even when neither pattern holds. Native full-distribution similarity alone
does not imply similar intervention behavior.

Also compute paired native query Jensen-Shannon divergence. This is the minimum
average teacher-KL attainable by a single common distribution for that paired
label frame. A group mean above .001 rejects that restricted output-invariant
abstraction at the stated query-KL bar. It is not an all-token lower bound,
general nonlinear impossibility, or rejection of explicitly context-dependent
circuits. Reuse the established paired-JS calculation and its prior derivation;
no literature claim or new theorem is required here.

Instrument: source rows SHA256
50374731a2cbdb44169c2c3c67dceac958235548eb5dbf497206f608029c7498,
full row pairing/answer checks, finite quantities, and accounting closure
abs<=1e-9/relative<=1e-10 through the shared scorer. CPU threads2, alarm180s,
no model/GPU, tensors below256MiB. All source cohorts are opened diagnostic
evidence. All native coefficients remain priced. No fitted gain/sign, best
head, hop subset or expanded endpoint-field rescue after either null.

This claim begins the next action: the exact causal accounting and fixed
coordinate contract are specified before opening the derived metrics. Execute
the bounded saved-row calculation next, then choose whether the missing
interface is context-sensitive amplitude, compensation in other computation,
or a larger mismatch that invalidates this proposed invariant semantic unit.
