# Hourly strategic review — 2026-09-08 08:15 UTC

## Circuit targets and full goal

The interpretation target remains: (1) specify the information read, operation performed, value
written, and downstream use; (2) group across native modules and split native modules when causal
interfaces demand it; (3) predict held-out and OOD effects; (4) extract an executable circuit or
explicit interface plus priced background; (5) support selective removal, swap, and edit; (6)
predict composition and reuse; and (7) identify the units stably across texts, controls, and
gauges.  The program-level goal is still a smaller transparent tensor program that is jointly
predictive, composable, manipulable, and simpler under literal storage, compute, edge, state, and
program prices.  A passing causal rung is not completion.

## What changed since 07:15

Four immutable circuit outcomes and one live successor materially advanced the L11 route.

1. The L11H3 q/k/q2/k2 factorial established a stable routing invariant.  Is-was donor routing
   changes the selected-value parent by only `.056-.081`; temporal donor routing changes it by
   `.081-.123`.  The recipient-native routing is therefore the stable carrier, not a newly fitted
   command subspace.
2. The explicit recipient-native contraction
   `(1-lambda_11) sum_j ((q_i·k_j)/128)((q2_i·k2_j)/128) delta_v_j` reproduced the independently
   patched L11H3 head tensor to `9.54e-6` maximum error and its direct-interface downstream effect
   to `8.59e-6` selected-logit error on original and OOD text.  This is an exact executable
   interface, not an activation reconstruction.
3. Translating the stored task tensors through physical L11H3 `W_v` produced stable original/OOD
   weight rankings over all 111 earlier writers.  It nominated the shared heads L6H7, L7H7,
   L9H1, and L9H4, plus task-specific L5H1/L3H4 candidates.
4. The frozen causal source-position factorial validated the weight prediction.  L9H1 and L9H4
   each pass the full shared cross-task singleton gate.  Both top-five unions pass, and singleton
   sums predict every simultaneous union with maximum relative L2 `.17425` and minimum cosine
   `.98560`.  The is-was union overshoots its L11 parent by `2.30-2.91`, showing that causal
   sufficiency alone would retain an unnecessarily large circuit.
5. The registered additivity branch fixed nested weight-ordered prefixes before outcome.  The
   26-forward greedy runner is the sole queued successor behind the live unrelated v254 job.
   Original FIT alone selects; original HOLDOUT and OOD cannot alter the prefix.  In parallel, a
   conditional writer-to-formula mediation test and implementation were frozen and built so the
   queue delay does not become research idle time.

Two prospective instrument corrections remain explicit.  The earlier source-localization result
stays invalid despite its separate target-frame repair audit.  This hour, the first conditional
mediation price omitted the live L11 parent reference needed to replay parent-relative writer
metrics.  V1 remains preserved unexecuted; v2 adds only four reference forwards, changing the
price from 14 to 18 without changing an intervention, bar, or null.

## Strategic choice and opposing routes

The highest-information next question is no longer which earlier head correlates with the source
tensor.  It is whether the selected writers' causal effect actually flows through that exact
tensor.  The frozen mediation test captures the writer-induced L11H3 value change, contracts it
through recipient-native bilinear routing, installs the formula directly at L11 preprojection,
and predicts the complete writer effect on held-out/OOD rows.  Passing closes an explicit
writer -> value tensor -> behavior edge; failure exposes a parallel-route residual rather than
inviting threshold tuning.

Ranked next moves:

1. **Greedy prefix outcome and exact formula mediation.**  Changes computational specification,
   cross-boundary grouping, extraction, OOD prediction, and literal edge count.  Kill compactness
   if no FIT-selected prefix validates; kill dominant L11 mediation if formula recovery is below
   `.75`, cosine below `.95`, or templates fail without reselection.
2. **Join the mediated source edge with the licensed H4 joint/removal program.**  Changes
   composition, selective manipulation, and explicit-background sufficiency.  Kill the join if
   source-writer and H4 interventions have unpredicted interaction or lose their established OOD
   selectivity.
3. **Operational causal-response grouping of the mediation residual.**  Use only if mediation is
   partial.  This could split native writers by downstream use.  Kill it if FIT response groups do
   not predict held-out/OOD interchange above shuffled/site controls.
4. **Full top-five subset enumeration.**  Demoted: the registered additivity gate passed, so the
   interaction-aware branch is not licensed.  It may return only if greedy order fails under its
   already frozen null, not because a hand-picked subset looks better.
5. **Regularized DAS.**  Still deferred.  Noise/KL may reduce memorization, but complement loss
   cannot both train and validate the intended mechanism, and the exact physical route now offers
   a stronger falsifiable object.

The mediation route survives because it directly tests downstream use of an exact polynomial
term at modest price and has a clear partial-route null.  Rank, reconstruction variance, and
compression work remain outside the active circuit loop.

## Confound audit

- **Baseline/frame mixing:** writer arms replay against a separately rerun L11 value parent;
  command-gold collateral is stored separately.  The added four reference forwards prevent a
  scalar-report reconstruction from silently changing frames.
- **Nonlinear composition:** the causal head union passed exact simultaneous-versus-singleton
  comparison, but `.17425` relative error is not zero.  The greedy test therefore executes every
  prefix rather than summing singleton scalars.
- **Shared difficulty/leakage:** weight rankings were identical original/OOD, candidates and order
  were frozen before causal outcomes, and only pooled original FIT may select a prefix.
- **Post-selection:** original HOLDOUT, OOD FIT/HOLDOUT, and every template remain validation-only.
  The mediation claim excludes original FIT except for exact replay.
- **Dead knobs and coverage:** writer, L11-factor, value-mediator, and direct-formula hooks have
  explicit call/shape inventories; algebra equality and literal forward counts are live gates.
- **Precision/noise:** exact source-term errors are at BF16-scale `1e-5`; the mediation algebra bar
  remains `1e-4` and is not relaxed after outcome.
- **Native-boundary overclaim:** heads are treated as physical intervention sites.  Semantic
  identity is the task-typed source tensor and its operational reader/writer behavior, not the
  native head label alone.
- **Generic recency:** exact weight reach could favor nearby writers.  The causal factorial already
  falsifies a purely geometric reading by showing large L9H1/L9H4 effects but near-zero L6H7 and
  small L7H7 effects for temporal despite all four having strong weight enrichment.

## Throughput and systems checkpoint

Authoritative receipts landed at 07:15 (QK factorial), 07:29 (source-term extraction), 07:38
(111-writer weight atlas), and 07:49 (six-head causal factorial): four outcomes in 34 minutes,
with successive intervals of roughly 14, 9, and 11 minutes.  The median remains about 11 minutes,
near but slightly above the ten-minute target because these were promoted identification/extraction
rungs rather than basic screens.  The scientific GPU executions took about 2--10 seconds each;
most serial time went to specifying and implementing genuinely different contraction, atlas, and
causal instruments.  Reused builders and accounting kept focused tests/gates to about 1--3 minutes
per rung.

Since 07:55 the GPU has been occupied by the unrelated 26-behavior v254 separability job, whose
first four members each require about 199--244 seconds.  The greedy circuit job remains uniquely
queued and hash-bound; it was not duplicated or bypassed.  During that verified wait, the next
mediation prior, price correction, 18-forward runner, and three focused tests were completed and
pushed.  Queue contention, not safeguards or unstarted eligible work, is now the dominant latency.

CIRCUIT_FOCUS: PASS

CEREMONY_BUDGET: PASS

NOVELTY_LESSON_GATE: PASS

Immediate continuation: keep the managed greedy job queued, bind its immutable result into the
already frozen mediation authority when it lands, execute that mediation next, then use its
registered pass/partial-route decision to choose joint H4 composition versus residual grouping.
