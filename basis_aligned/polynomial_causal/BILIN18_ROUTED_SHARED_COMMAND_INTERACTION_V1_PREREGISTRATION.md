# Where does the shared read acquire its two-command interaction?

The exact token-incidence certificate proves U11=0 for every token-only
shared-value producer on all32 registered dual-command worlds. The final
shared-removal response has a live mixed mode. Distinguish its presence
already in native routing x payload multiplication from later propagation.

Keep L9H1/H4,L11H3,L15H5, all128 opened sequences, both command positions,
both phases and every template. No head/query selection based on outcomes.
Capture native shared reads before and after each head's fixed output
projection. Let P_ab be the native signed causal attention pattern for one
head/query and U_ab its sourcewise lambda*v0 projected by W_Oh. Then
S_ab=P_ab U_ab is a D1152 residual write. Layer/head indices remain separate;
do not sum writes at different layers and call them one intervention.

Using the dtype-preserving balanced command transform, compute
S11=P11 U00+P01 U10+P10 U01+P00 U11. Products contract source positions.
Keep all four terms for closure, checking the final term is zero. Define
joint_router=P11 U00 and crossed=P01 U10+P10 U01. These are exact native
read terms, not independently performed input-token interventions.

A: source authorities and32world pairing; native shared-read reconstruction
against the original attention contraction abs<=1e-3/relative RMS<=1e-5
in deployedFP32; FP64 four-term convolution abs<=1e-9/relative<=1e-10 using
live sums; U11 zero<=1e-9; no future-iswas mixed read at temporal query
abs<=1e-8; finite values, restoration and call counts. Small controls must
include a planted joint-router-only read, a crossed-only read, independent
payload incidence, signed mixed terms, and a live omitted-term negative.

B native consumer interaction is material: at the later iswas query in
each phase, pooled M11 write RMS across the fixed typed layer/head entries
is >=.01 times their complete four-cell shared-write RMS. Report each head
and temporal query too, but do not choose a winning head. This is a read
coupling screen, not proof that these writes explain all final-output coupling.

C crossed single-command terms suffice: in the same two pooled late-query
phase panels, RMS(crossed-S11)/max(RMS(S11),1e-6)<=.01. Failure retains the
joint-router term; it does not license a fitted coefficient or a different
head set. No structural or causal-transfer promotion follows from closure.

Shared production is one token-local function reused by contextual consumers.
Native P still depends on the full prefix and all arbitrary Q/K weights;
charge this context and all545902902 native parameters. This is an exact
read-stage diagnostic before proposing a reduced program. No replacement,
rank search, baseline averaging or output selection.

ManagedGPU only, batch4,length<=27.32 native full forwards/128 sequence
evaluations, with at most192 additional local attention contractions for
shared/context read replay (three selected layers x two extra x32).
No gradients, fits or training, alarm600s, tensors<256MiB. Store only
source-derived mode/read statistics needed for the fixed verdict. Reuse the
existing native forward, balanced transform and source-read machinery.
