# Two-start degree-balanced shared-interface fit

Native graded kernel checks must all pass before execution. Rank128 common producer coordinates, full-U two-branch coefficient metric, equal relative weight for producer-degree groups1..4. Initializations: prior spectral128, independent Gaussian QR seed73142. No text or behavioral effects in optimization. Retain rank and all external ports/price from the shared-interface screen; no validation-guided reader reweighting.

Grassmann gradient descent uses horizontal gradient grad-P(P^T grad), QR retraction and Armijo sufficient decrease (1e-4), initial step4, up to20halvings, accepted step increased1.5x capped100. Each arm has540seconds/max10000updates, then records exact final gradient. Local convergence requires horizontal gradient norm<=1e-5; time/iteration/line-search limits are not convergence or an absent-structure result. Continue from final frames if optimization remains informative; do not relabel a budget stop. Total watchdog1320seconds, no native bodyforward, small overwritten progress snapshot every50steps. Preserve terminal frames and losses for continuation.

A: finite monotone loss (tolerance1e-10), final orthogonality<=1e-10, both fits meet gradient<=1e-5. B: spectral arm reduces initial balancedloss by at least10%. C: both frozen arms preserve bothbranches' native swaps on3activefreshfamilies with symmetricrelativeRMS<=.1/sign>=.9/live>=4 and meanabs removalCE disagreement<=.02. Report quotedcontrol separately without discarding it. C is not repaired by one arm passing. Native128endpoint validation uses exact background/RMS16/RMS17 and FP32stored interface maps; no datafit. Report full write errors and both arms rather than selecting by validation outcome.

Null: optimizing the coupled formal coefficient object fails to yield a converged useful shared circuit interface. Exactness is for independent formal background and producer coordinates under the declared graded coefficient metric, not the fully symmetrized eighth-order native input tensor or language distribution. No global recovery or four-property adoption claim from convergence. Circuit targets are shared cross-boundary computation, executable conditional interface and preservation of signed interventions; loss reduction alone does not identify a circuit.
