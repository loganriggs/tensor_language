# Frozen recurring node: broader context confirmation

The continued graph's coefficient-selected node passes all developmental write,
swap and removal bars. Freeze that exact arm, frame, node and671incident edges.
No factor or threshold changes. Evaluate the existing512pairs/1024endpoints in
PRODUCER_METRIC_CONTEXT_HOLDOUT_V1:32cells,16pairs each, eight contexts across
four morphology families. This cache was inspected for other programs; call it
broader context validation, not an untouched holdout or corpus OOD test.

Compare the frozen incident-edge write with its exact full quadratic star.
Keep normalization/background fixed at the local write intervention. Use native
FP32 U/RMS/tanh and shared scorer. The previous development node's reference
effects cannot be replayed on different rows; duplicate exact reference here.

- pred_a: cache row binding, node/edge identity, physical/compiled write replay
  <=1e-8, duplicate reference effects<=1e-5 and all effects finite.
- pred_b: all32cells swap relative RMS<=.1, sign>=.9, live>=4.
- pred_c: all32cells mean absolute removalCEdisagreement<=.02.
- pred_d: all32cells relative write error<=.05.

Report native answer-versus-foil capability in both sides of every cell, but do
not exclude weak cells or let capability change fidelity bars. Even a fidelity
pass is not proof of selective useful morphology or a specified semantic task.
Zero body forwards/zero newtext; cached1024boundary vectors;300second alarm.
Conditional node cost14,481,792floats plus1342edge indices and native background.
