# Full-panel generated original-interaction validation

Frozen13September2026 before managed execution. The preceding eight-prefix CPU screen passes its regional bars; its FineWeb row96 target sign reverses at sub-micro absolute scale. This full160-prefix historical panel is promotion, not fresh OOD confirmation.

Object: original parent minus child minus remainder plus native final output, with all three changed post-MLP10 states generated from the pristine context using the shared exact MLP9 response/attention10 generator. Unlike the earlier local-cross test, the parent uses the response at a+b, retaining inherited mixed computation. Generated z10 is rounded toFP32 before native MLP10; final suffix is nativeFP32. No changed native state is a candidate input. Native branches are references only.

A: every post10 branch state relative error <=1e-5.
B: each of four24-prefix regional groups has original-interaction target error <=2% and control error <=5%.
C: every branch absolute endpoint discrepancy <=1e-4; each of four16-prefix FineWeb groups has target error <=10% and control error <=15%.

Null: local algebra correctness does not preserve original-interaction outputs under native precision. Report individual scores and signed effects, including small effects and any sign reversals, regardless of aggregate verdict. Preserve previous local FineWeb failures; this is a different target.

Price:160 pristine prefixes,480 native changed branches,480 generated branches,1280 suffix evaluations,180second GPU cap through lane1. Pristine state, scalar-field generators, dense native maps and suffix remain charged; no claim of whole-model compression or independent extraction. Dependencies and source are bound in COMPOSED_JOINT_NATIVE_FULL_V1_BINDING.json. Shared regional-cue validation runs before scoring. Prior actual-weight synthetic control and eight-prefix CPU control provide positive references; no fitting or selected restart.
