# Does the mixed value preexist normalization?

Use all32 original/fronted worlds in THIRD_NOUN_CAUSAL_ROLE_ALIGNMENT_V1. Preserve
all previous structural, gender, and independent factor-transfer failures. This
screen traces the identified partial producer; it is not another normalization
approximation or a rank/variance scan.

Capture the actual raw residual x entering layer9 attention RMS and normalized
input u. Native u=g*x, g=(mean(x²)+epsilon)^(-1/2), with explicit epsilon equal
to torch.finfo(native raw dtype).eps (FP32). Compute in FP64 and validate against
the unchanged native normalized input. Causal recurrence capture must reproduce
that input bitwise through native RMS before the FP64 comparison.

Conditional on the remaining factors, split
u_oh = g0*x_oh + (g_oh*x0 + go*xh + gh*xo) = R+N.
R is inherited raw mixed state times the conditional mean normalization scale;
N contains interactions generated by variable normalization. Both include all
context-dependent factor terms and all three mature source positions. Reuse the
existing exact projectors; no coordinate, norm or rank threshold selection.

Map each part through native W_V, conditional-mean native routing P0, W_O and
local scale1.65625 to the common to/action write. Remove full R+N, R alone or N
alone from native attention outputs, then recompute the full native suffix.
Q/K and other native producer factors remain unchanged. This is a branch removal
at the value-producer/write interface, not direct alteration of raw residuals
which would also change routing. No across-layout swaps are performed here.

Perworld4 baseline/local captures plus6forwards for full/R/N removals; total
320forwards5120sequenceinstances,0fits,0offline decoder,600s cap.

A instrument: bound files, finite/hookcleanup, exactcounts; inherited native
factor controls pass, raw recurrence RMS replay bitwise, FP64 normalized-input
relative error<=1e-6, mixed partition closure<=1e-10, eachbranch puremixed<=1e-10.
Full normalized-value write versus observed common write<=1e-4relative. Native
and full-removal output grids replay COMMON_SUFFIX_V1 baseline/common-removal
with BOTH maxabs<=1e-3 and relativeFrobenius<=1e-5. Native routing/firstV unchanged;
incoming attention patch equals captured native output bitwise. Existing synthetic
controls separately plant normalization-only and inherited-only interactions.
Small scientific effects fail denominator gates rather than instrument validity.

All scientific requirements apply to EVERY world and both readout objects:
- B raw-origin fidelity: R-removal effect differs from full-removal effect by
  <=.10relative on the Qoh correct margin and centered three-reader vector.
- C normalization-origin fidelity: the same criterion for N alone.
- D composition: full effect versus sum of R/N effects differs by<=.10relative
  on correct-margin and centered three-reader full tables.
Zero denominators fail. Report otherfactor spill and natural signed projections
as diagnostics, without promoting a branch from raw norm dominance or changing
the failed structural/gender controls. If neither B/C passes, retain both parts;
do not pick the lower error as a discovered independent producer.

All545902902weights, native counterfactual inputs and downstream model remain;
structural savings0. A passing R identifies an upstream source question; it does
not independently generate x_oh or identify its semantic operation.
