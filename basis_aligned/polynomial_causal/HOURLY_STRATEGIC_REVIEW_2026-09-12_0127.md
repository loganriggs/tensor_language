# Hourly review — 12 September 01:27 UTC

The circuit targets remain explicit reads/operations/writes; cross-module grouping and within-module splitting; held-out/OOD prediction; extraction/sufficiency; selective manipulation; composition/reuse; and stable identification. The requested endpoint also requires a literally simpler executable program. No new circuit has been promoted. The bilinear handoff and later weight-first directions supersede the stale goal-record reference to better_math_ideas.

## What changed

Two completed optimizer endpoints received native-effect scoring. V3 CG gains4.15% coefficient capture over V2; LBFGS gains11.83% from that same start. Native write errors worsen from22.8% to24.1% and26.7%; swaps/removal still fail. Both remain unconverged. Independent probes establish about10.8% coefficient coverage for V2 and12.1% for LBFGS V1, within the selected full-input two-output target, not the full unembedding or model.

Three independent planted starts recover exactly enough, but a fourth is stationary at66% error. Twelve local perturbations fail to escape; replacing the weight-selected weak node recovers2/4 times. Small gradient, local perturbation failure and a well-conditioned output solve therefore do not establish a representation limit. The residual-scored native replacement test is queued, not completed.

Learned-bank isotropic mixing, harmonic fitting with exact lower corrections, and a generic two-output real pencil representation were executed with numerical controls and narrow red-team checks. They fail native write fidelity. The pencil's full representation is exact, but both outer-space refits and actual upstream folding fail to rescue the tested32-reader truncation. These negatives concern specific banks, objectives and selection rules; they do not establish absent structure.

The original mixed-core test used an initial bank. Its learned-bank repeat is queued. Both queued comparisons start the frozen LBFGS V1 endpoint, not the still-running longer continuation. This distinction matters for interpreting comparisons. Current methods and historical checkpoints are now separated in WEIGHT_ONLY_METHODS_INDEX.md.

## Confounds and criterion status

Discovery still uses weights only. The repeatedly inspected128-endpoint panel is developmental validation, not fresh/OOD evidence. Native background, radius and MLP17 denominator remain explicit. Exact pair or harmonic identities do not close those dependencies. A shared output coordinate or sparse block is not a semantic variable. Cancellation invalidates treating individual node energies as additive variance shares. Lower coefficient error is not native effect fidelity; signed CE improvement is not preservation. Matched parameters and output subproblem convergence do not prove joint convergence.

Extraction and selective manipulation have direct failing measurements. OOD prediction, stable identification, and useful semantic composition/reuse remain unproved. Improved optimization is supporting work, not a substitute for those properties.

## Direction and alternatives

Finish the already authorized long weight-only continuation and score its frozen endpoint. Do not claim convergence from its time/step boundary or automatically launch another identical continuation merely because coefficient capture increased. Compare the residual replacement and learned mixed-core results against their own registered frozen baselines. Their independent hypotheses are escaping a bad feature choice versus allowing cross-products among learned features.

If these also fail fidelity, the next decision must distinguish a restricted outer core, inadequate optimization across independent starts, and a metric/target mismatch. Nonlinear joint mixed-core fitting or a distinct native start would change those first two uncertainties; another exact linear refit in an already-tested bank would not. Changing to data-weighted discovery remains deferred under the user's instruction, and no inference of exhausted weight structure is warranted. Do not spend another block compressing the failed harmonic correction or tuning the same norm-ranked pencil truncation.

## Throughput and workflow

This hour produced two terminal optimizer native-effect screens and three distinct native-write negative extensions, plus optimization controls. Toy controls and documentation are not additional circuits. The completed native fits took roughly17 and14 minutes; the ongoing longer fit deliberately exceeds the basic-screen budget to address the user's convergence request. Small exact screens took seconds, but end-to-end reasoning, implementation and interpretation took minutes. Do not hide those costs behind their GPU execution times.

[Recorded phase accounting](HOURLY_PHASE_ACCOUNTING_2026-09-12_0127.json):26.40 minutes implementation,3.91 science,2.18 validation,22.51 review and5.00 publication. These are actual coarse phase marks, not a reconstructed time study; GPU execution overlaps them.

- **CIRCUIT_FOCUS: PASS for the user-directed discovery/extraction work.** Native intervention checks and dependency-preserving representations constrain circuit claims. Zero promotions.
- **NOVELTY_LESSON_GATE: PASS.** Prior congruence/LL1 work, original-bank failures and component dossiers were checked; new tests isolate changed assumptions and preserve misses.
- **CEREMONY_BUDGET: PASS, narrowly.** Science plus implementation totals30.31 minutes versus29.69 for validation/review/publication. Publication dropped from13.1 to5.0 minutes. Review overhead remains substantial; avoid more per-rung essays and duplicate scorer implementations.

The concrete workflow improvement is completed: quartic_native_effects_v2.py handles square, full mixed and sparse mixed programs with the same native background and thresholds. Its square reports reproduce the published receipt exactly; full/sparse completed-program write and reference-effect replays pass. Use this existing scorer for the queued results. Do not add more controls without a concrete new validity concern.

Next hourly review02:27 UTC; mathematical review remains due01:56 UTC. The active goal is unfinished. Long LBFGS PID3158512 and two audited queued structural comparisons provide the current continuation.
