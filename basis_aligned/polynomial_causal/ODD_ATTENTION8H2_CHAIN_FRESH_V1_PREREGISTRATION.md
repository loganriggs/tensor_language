# Fresh prediction of the inherited-city relay chain

The panel was frozen without model execution: two new templates, Bristol/Boston
and Oxford/Austin, and six unseen one-token British/American spelling endpoints.
All48 rows pass the shared one-city-token, paired-length, article and quote
checks. At head8.2, freeze recipient queries and all non-city sources. Compare
full donor city key/value, routing-only, value-only, inherited-first-only and
current-only city substitutions. Apply each resulting city-source write only at
the token-defined framing destinations, then feed its induced current-value
difference through head9.8 O and the native suffix. Native plus five arms costs
288 body forwards under180 seconds.

- `pred_a`: recomputed recipient head8.2 source channels replay captured native
  head2 output within `1e-5`; routing/value factorial and current/inherited sums
  close within `1e-5`; both value-source deltas are live, outside framing is
  exact, values are finite, and exactly288 forwards execute.
- `pred_b`: native cue is positive for at least10/12 pairs and full-city target
  effect RMS is at least `1e-5` in each template.
- `pred_c`: value-only cue error versus full city is at most35%, while
  routing-only error is at least50%, in each template.
- `pred_d`: inherited-only cue error versus value-only is at most35%, while
  current-only error versus value-only is at least50%, in each template.
- `pred_e`: inherited-only cue error versus full city is at most35% in each
  template, predicting the complete frozen cross-layer chain.
- `pred_f`: every reported unrelated-control RMS ratio is at most.5 in each
  template.

This is held-out construction/city/endpoint prediction within the authored
regional task. It is not corpus OOD, whole-head sufficiency, independent native
prefix/suffix extraction, static compression adoption, or quantization.

