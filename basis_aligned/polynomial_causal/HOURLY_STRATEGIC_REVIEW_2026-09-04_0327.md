# Hourly strategic review — 2026-09-04 03:27 UTC (Claude, lane 1: bilinear_quotient ledger) — PIVOT HOUR

## Explained fraction (unchanged)
Strict ledger 5.348% / 10.923% / 4.727 nat / 0 of 68. Nothing this hour installs into the §312 frontier (norm-2304, 2.6735 CE added above the real model; LOWER IS BETTER).

## What the hour established (§2805–§2807) and why it closes the lineage
- §2805: per-block gate compression composes linearly (.0072 per block; full ten-block program .0722).
- §2806: one pooled 256-dim core frame gates the whole late stack at .0442 (own frames + .011 at every rank).
- §2807: that frame IS the core's variance frame (PCA_256 .0485 vs .0442; overlap .853) and CE cost = fraction of pooled cross-term energy discarded, to ≤ .002 at every rank 32–512. Smooth spectrum; no discrete gate.
- User (03:21Z): the 768 core / 384 tail split is a cut on a smooth eigenvalue decay — "not much of a story of a discrete channel"; the lineage is a diversion; focus back on circuits, in depth; "what's preventing 20 high-quality circuits in the next 12 hours?" Relayed to Codex (board 03:22Z). §2790–§2807 stand as measurements; no mechanism named; CLOSED.

## Largest gaps — restated in circuit terms
1. Circuit throughput: 6 behaviour circuits + 1 subroutine in the campaign, none fully validated; Codex lands ~1 experiment/hour (00:19, 01:04 failed, 02:55; R593 still freezing), each closing ≤ 1 of 8 acceptance gaps on one circuit. 20 circuits × 8 gaps = 160 bespoke experiments at this pace.
2. No reusable battery: each experiment rebuilds rows, freeze, audit, float64 decomposition (hours) for 30–60 s of GPU. GPU ≈ 5% utilised over the night.
3. My lane produced 0 circuits in 12 h (late-tail continuum).
4. The acceptance bar itself (compiled_weights, learned_interchange for every circuit) is not reachable at 20 circuits/12 h by any agent; a defined "high-quality" tier equal to the induction template (mechanism in plain language; each edge intervention-tested; held-out replicated; selectivity control) is needed.

## Candidates (pruned by information gain / falsifiability / GPU cost / redundancy)
- A. Reusable behaviour-first battery (`ops/circuit_fast_lib.py` + one runner per behaviour): templated answer-changing counterfactual pairs + answer-preserving controls; capability; component activation patching (36 sites, then 9 heads of the top sites, then the sub-head channel into the answer direction); resample-interchange selectivity; cross-family control; two-half replication; automatic cross-behaviour component-sharing table (composition / re-use). One GPU script per behaviour, ~2–5 min. Bars preregistered ONCE for the battery.
- B. Run the battery over ~20 behaviours the model plausibly has (induction/copy, bracket/quote closing, numbered list, numeric +1/+2, month/day/alphabet succession, capitalisation after period, subject–verb agreement, unit after number, URL/HTML tag closing, "either…or", comparatives…), throughput-first, each producing a record Codex can red-team.
- C. Depth items on Codex's queue (bracket pending-opener source-region split; numbered-list final-label term split) — Codex's, heavy; I red-team on request.
- D. PR2 parameter accounting of the late-tail program — dropped (lineage closed).
- E. G2 frame canonicality — dropped (continuum object).

## Ranked top five
1. A (unblocks everything; reusable code is the user's explicit ask). 2. B (the deliverable count). 3. C (only on Codex's request). 4–5. none — all remaining continuum items dropped.

## Executed
A: building `ops/circuit_fast_lib.py` (forward with per-head/per-MLP write cache and position-/head-level patching, tokenizer, behaviour-family generators, battery scorer) with unit tests; first behaviour through the battery this hour. Board 03:22Z carries the directive and the claim protocol (I take unclaimed campaign items; post claims before starting).
