# Frozen current/first value sectors and the selectivity gap

2026-09-12 before native sector outcomes. Original scalar8.2/9.8pair supports removal, contextual transfer and donor interchange but fails one natural newline max-error control. Source readers have an exact architectural split: a_i=gamma_i*v_current_i(x)+gamma_i*v_first_i(token). Both sectors retain the SAME complete jointQK1*QK2 and physical writer d_i. This is not a split of tasks betweenQK1/QK2 and not a new factor fit.

Four fixed elements are8current,8first,9current,9first. Evaluate all16binary masks (mask0native, bits in that order), each subtracting the chosen sector field at its native producer while later states recompute. Mixed masks therefore include actual8->9interactions. Use48email/letter rows and32natural newline rows, explicitly reused. The frozen V2meanhead8control on natural rows remains available; independently reproduce it once per natural row to ensure capability/control is live.

A: current+first scalar/native executor replay<=1e-5relative; mask0regional margin and newline CE replay<=1e-4relative; fullmask15regional removal and newline effects replay prior physicalpair results<=1e-4relative. Natural meanheadcontrol replay<=1e-4relative. Report change denominators and absolute error. Source-sector CPU control is not end-task validation.

B: at least one nonempty mask, on EACHregional template, reduces paired cue contrast in>=10/12pairs, mean reduction>=50%nativecontrast, and unrelated token-margin meanabs change<=.5regionalmeanabs. Native capability retains meancontrast>=.2,>=10positive. Report all15masks, not just winners.

C: at least one SAME Bpassing mask also satisfies meanabsnewlineCEchange<=.02,maxabs<=.1 on EACHnatural16-row half. Native and meanheadcapability/control retain V2bars. This is a15candidate exploratory screen, not identified selectivity; any passer requires fresh natural/context confirmation. The original fullmask15failure remains failed even if a smaller mask passes.

Null: current/first value sectors do not supply a selective regional unit; both tasks share parts of these fields or routing. No amplitude/rank tuning or fit on these outputs is allowed as a rescue. Sector effects may cancel, so individual magnitudes are not additive shares of behavior. Report full tables and component nonadditivity.

Price80rows*16native masks+32mean-controls=1312bodyforwards, max179tokens,300seconds; no optimizer. Model/background stillnative. Managedlane1. Preserve earlier fullcomponent, countryaudit andwholehead-fidelity misses.
