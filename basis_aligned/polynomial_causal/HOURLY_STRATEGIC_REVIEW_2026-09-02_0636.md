# Hourly strategic review — 2026-09-02 06:36 UTC

## Goal check

The target is still a decomposition into computations that predicts held-out/OOD behavior, supports selective
removal and extraction/interchange, preserves unrelated circuits, and exposes reusable composition. Lower rank,
fewer stored numbers, or lower reconstruction error does not establish any of these properties. Ranks may summarize
a measured response tensor, but they are not circuit labels and do not determine the next experiment.

The active-goal continuation invariant is now explicit and exercised: 472→473→474→475→476 each ran through the
managed GPU lane, and rung477 is claimed before this review. A result, null, explanation, clean commit, or empty queue
is not a stopping condition; the successor must be registered and actually started.

## Evidence from the last hour

- Rung472 identified the query-position parts of MLP8/9/12 as exact selective causal components across held-out code
  and natural text. Query-only removal predicts full-prefix removal at70.8–92.8% per-token correlation and beats
  earlier-prefix removal everywhere.
- Rung473 showed that pair/triple removal terms are register-dependent and pairwise-only prediction is insufficient.
- Rung474 supplied the crucial coordinate control: every singleton is exactly invariant, but higher-order allocations
  change with the intervention coordinate. Only the singletons are identified objects at this grain.
- Rung475 used the existing62 downstream behavioral circuits and found no stable equivalence between complete MLP
  writes; best pair cosines are moderate and source/half dependent.
- Rung476 tested the narrower code-selected product groups. Its exact selected+complement bridge reproduced the full
  parents at0.0 nat, but selected cross-MLP similarity became weaker, not stronger: native best residualized cosine
  .373 versus parent .490, while matched controls win under the transplanted source. The strong null closes this old
  split for grouping.

## Main strategic correction

Do not select a decomposition by the four code context averages and then ask whether it happens to organize all
behavior. That route has now failed cleanly. Let downstream behavior propose the partition, while reserving complete
circuit families and documents for exact causal validation.

The 62 tags are also not62 statistically independent mechanisms: they are nested under top-level `r.<family>` roots.
The next split therefore holds out whole top-level roots, not individual descendant tags. This prevents a parent and
its nearly overlapping child from leaking across discovery and validation.

## Ranked next directions

1. **Rung477 per-product response graph.** Measure member-minus-matched-control first-order response profiles for all
   13,824 MLP8/9/12 product coordinates on32 discovery tags, two sources, and two document halves. A multi-view
   mutual-nearest graph must beat alignment-destroyed controls. This tests duplicated native product computations,
   not rank.
2. **Exact held-out-family group removal and interchange, only if the graph passes.** Open the30 reserved odd-root
   tags on documents500:1000, require selective effects and preservation of unrelated tags, then physically swap the
   proposed shared scalar products.
3. **Sparse mixed-product response directions if native coordinates fail.** A negative graph would show that native
   bilinear coordinates are too arbitrary, much as head boundaries were. Learn sparse mixtures in response space,
   but choose them by held-out circuit prediction/removal rather than reconstruction rank.
4. **Input semantics after a causal group exists.** For a validated group, explain when its left×right products fire
   in token-only, token×context, and context-only variables. This supplies the human-readable rule; it should not be
   optimized before the causal group is stable.
5. **Return to attention shared QK branches as an independent family after this MLP decision.** The exact polynomial
   branch factorization remains a different route to shared computation and prevents this program from overfitting
   its methodology to MLP products.

## Risks and kill conditions

- First-order term responses are proposals only. No group is identified until exact held-out removals and swaps work.
- The product coordinates retain bilinear-factor gauge. Failure at this basis routes to mixed response directions;
  it does not refute a finer MLP decomposition.
- A large mutual-nearest graph can arise from searching millions of pairs. Rung477 requires16 independently
  circuit-coordinate-permuted graph counts and a fourfold excess over their95th percentile.
- Do not reopen archetypal/convex dictionaries as a generic fix: the existing family is0-for-6. Only a response-
  anchored version with a new held-out causal consequence could justify reconsideration.
- Do not tune term counts, ranks, similarity thresholds, or the rung467 split after outcome access.

## Decision

Proceed with rung477 exactly as registered. It is the highest-information missing measurement because it tests the
user's proposed downstream-interaction basis directly, keeps complete behavioral families held out, and routes both
positive and negative outcomes without turning compression into the goal.
