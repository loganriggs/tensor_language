# Frozen interaction predictor under paired donor-field replacement

12 September2026. The completed donor novelty audit shows21–31% displacement residual beyond the best recipient-specific global gain. This test reuses72 longer contexts with a different manipulation, not new text OOD.

First compute every native head8 scalar from its pristine prefix. For each recipient, substitute its paired donor's field, at matching token positions. The removed amplitude is a_recipient minus a_donor. This varies across positions and can change sign. Both predicted response maps take that amplitude plus pristine recipient z8/u0/raw9. They never see changed recipient states. Donor IDs, one-token pairs, and lengths use the existing shared validator.

Six arms: native; swap8 only; swap8/remove actual updated9; swap8/remove predicted64_9; swap8/remove frozen pristine9; swap8/remove predicted full-map9. Head9 is removed in the latter arms, not itself donor-swapped. Signed interaction remains actual-updated9 margin minus frozen9 margin, now conditional on the head8 donor edit. Native background and suffix remain.

A: native baseline replays the earlier longer-context result within1e-6relative; each family capable (mean native paired contrast>=.2, >=10/12 positive); full-map interaction error<=1e-4 with reference norm>1e-6 in each family. B: A and rank64 interaction error<=5% eachfamily. C: B and total joint-edit effect error<=1% eachfamily. No rank reselection or fitting. Record control margins descriptively, with no new broad selectivity claim.

432full body forwards plus72eight-block pristine prefixes,120second managed GPU cap. The scalar8 runtime reads normalized input to block8 attention, so donor cache needs no block8 MLP or native suffix. Null: the frozen approximation only handles uniform edits, not donor transfer. If full-map control fails, resolve instrumentation/numerical error before interpreting compression.
