# Hourly circuit review — 12 September 08:36

Targets remain explicit reads/operations/writes; cross-boundary grouping and within-module splitting; held-out/OOD prediction; extraction with declared ports; selective removal; composition/reuse; and stable identification. The full objective is a simpler executable model with the four behavioral properties. No new circuit was identified or adopted this hour.

The coefficient/context permutation test limits the earlier selective-removal claim: global shuffling retains much of the effect, while within-group coupling has evidence. The upstream source tests then locate the existing branch's computation mostly before attention16, with substantial MLP15/background interactions. A common128-coordinate producer interface strongly beats a random interface on both branch writes but fails the joint intervention-fidelity criterion. The predeclared512 diagnostic preserves branch8's tested swap effects while branch3 still misses an active-family threshold. These are changes to the computational specification and conditional extraction boundary, not extra identified circuits.

The coupled-product objective fixes a real mismatch in the separate-reader fit. Its raw metric has99.9976%ofnorm in pureproducer terms, so an explicitly degree-balanced weight-only metric was introduced before optimization. Dense/native identities and gradients pass. The managed two-start fit is live. Its first arm reached540seconds without the registered1e-5gradient criterion; it remains unconverged. No behavioral conclusion is available until the terminal validation runs. CPU controls prove that even local convergence can reach a strict bad minimum, and demonstrate a chosen-plane escape method.

Confounds remain material: source coordinates are formal rather than naturally independent; the metric is not the fully symmetrized eighth-order native-input norm; RMS16/RMS17 and background remain external; high write fidelity can coexist with poor pair changes; arbitrary output priors can mimic removal selectivity; and repeated inspection of these construction families is development evidence. No validation-derived reader weights, token selection or repaired bars were introduced. Direct coefficient and derivative replay separate arithmetic errors from these modeling assumptions.

The next action is to finish and interpret the current two-start fit. Convergence continuation has priority over calling a time-limited fit a negative. A bounded plane audit and a fixed-rank coefficient lower bound can distinguish local-optimization limitations from limitations of this representation. Neither is a semantic success metric. Once optimization has an adequate receipt, signed intervention fidelity decides whether to pursue the common upstream interface; coefficient gains alone cannot justify another cascade of refinements.

Alternatives ranked next: (1) convergence continuation using existing manifold methods, with gauge-aware transport; (2) the already controlled nonlocal plane audit; (3) an objective that respects repeated native producer inputs rather than formal independent source slots. More text collection and outcome-guided reader reweighting are lower priority and remain outside discovery. The current route survives because it tests a concrete shared computation across MLP15–17; it should be redirected if adequate optimization still fails native effects.

Throughput: attention-source claim to receipt was9m27s; the preceding-MLP source and common-interface screens were under2minutes each. These are repeated tests of the same candidate, not three new circuits. Native arithmetic controls took seconds; the sustained fit is deeper confirmation. [Recorded phase occupancy](HOURLY_PHASE_ACCOUNTING_2026-09-12_0836.json): science21.12minutes, implementation15.43, review19.40, publication4.05. GPU work overlaps these categories, and missing fine-grained validation labels prevent interpreting them as exclusive labor times.

- CIRCUIT_FOCUS: PASS. Source computation, cross-boundary reuse, conditional extraction and signed-effect fidelity advanced; no rank-only promotion.
- CEREMONY_BUDGET: PASS WITH LIMITATION. Recorded science+implementation exceeds review+publication; shared scorer/geometry kernels were reused. The current source/objective controls address demonstrated failures. Phase timing is not precise enough to claim a separate validation-time budget; no retrospective relabeling.
- NOVELTY_LESSON_GATE: PASS. Late-module dossiers, previous producer metrics, quartic contraction and manifold optimizers were checked. New source-coordinate claims are distinguished from known folding algebra.

Next hourly09:36, mathematical review11:00. Follow the user's bilinear handoff and weights-first corrections over the stale goal text mentioning better_math_ideas.
