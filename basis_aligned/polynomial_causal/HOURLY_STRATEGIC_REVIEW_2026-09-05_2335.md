# Hourly circuit-only review — 2026-09-05 23:35 UTC

## Circuit interpretation targets and full goal

The required account still must specify the information read, the computation performed, the write, and the downstream consumer; group and split native modules by operational equivalence; predict held-out and OOD activations and behavior; yield an executable interface/program; support selective removal, swapping, and editing with collateral measurement; predict composition and reuse; and remain identifiable across prompts, constructions, controls, and gauges. Native heads and MLPs remain intervention handles rather than presumed semantic atoms.

The full goal remains a smaller transparent tensor program that is jointly predictive, composable, manipulable, and literally simpler under storage, compute, edge, state, and program prices. This hour converts Task14's upstream source decomposition and fixed readers into a prospectively installed ten-vector program, demonstrates selected manipulation, bounds collateral and cross-task scope, locates downstream consumers, and compresses their operational response to six scalars. It does not yet prospectively validate those six mediator gains on a fourth authority, replace native MLP15/17 computation, establish joint reuse with a second identified circuit, or constitute a whole-model replacement. The durable goal remains active.

## What changed since 22:35

The fixed-reader-guided edit passed all gates: on preselected support, an already frozen reader chose gains that achieved a requested signed `0.04` margin change under native-tail execution. This established manipulation rather than another passive correlation.

A held-row upstream-prototype diagnostic then rejected an overlarge 32-vector direction×exact-subset table. It predicted native displacement effects at cosine `0.82138`, but exact subset identity reduced SSE only `2.52%` versus direction×cardinality. The search therefore selected ten fixed direction×cardinality vectors rather than preserving a factor-label table.

Those ten 1,152-D vectors were exported before a third corpus was opened. On 32 disjoint noun forms and unseen near/beyond syntax, all native-capability cells scored `1.0`; the frozen vectors were installed across all 512 row×background conditions. They substituted native exact displacement effects at cosine `0.86301`, relative L2 `0.52605`, and sign agreement `0.93945`. Existing fixed readers predicted their installed effects at cosine `0.96927` with perfect signs. Cardinality reduced SSE `29.72%` over a two-direction upstream-vector control. This is now a prospective executable upstream+readout screen, not merely a retrospective prototype.

Literal installations of all ten writes on two unrelated behaviors preserved all registered numbered-list and bracket decisions. The first receipt was instrument-invalid solely because FP32 projected addition missed a `1e-5` exactness bar by `2.94e-5`; a frozen numerical-only audit used `5e-5`, with no scientific changes or GPU rerun. Worst median normalized collateral was `0.000715`, with zero answer flips. The scope remains narrow to these two behaviors.

Adjacent possessive agreement supplied a valid cross-task null after a native-gate-only repair. The Task14 cardinality-0 write had median donorward change `0.000499`; one direction×construction cell was at chance signs and correct-versus-swapped advantage missed. The write is therefore Task14/copula-specific at this interface, not a generic adjacent grammatical-number state. Other cardinalities and scales remain closed on those opened rows.

Composition advanced directly. An exact four-corner intervention showed that clamping MLP15+17 to matched base outputs removes `21.63%` of full program-effect norm with cosine `0.95849` and perfect signs, recurring in every direction×template and cardinality group. A singleton factorial then showed an approximately additive distributed pair: MLP15 accounts for `29.52%` and MLP17 `92.02%` of joint-mediation norm, with a cancelling interaction of `21.61%`. Both are material in all four direction×template groups and all five cardinalities; MLP17 dominates but MLP15 cannot be dropped under the registered rule.

Finally, row-held-out scalar composition predicted those mediator effects from the already frozen reader. A 30-scalar direction×cardinality gain law passed its component and joint prediction bars, but improved joint SSE only `0.1018%` over a six-scalar direction-only control and was rejected. The six-scalar model was promoted without reopening outcomes: joint mediation cosine `0.95337`, relative L2 `0.30185`, perfect signs; MLP15 cosine `0.84466`, MLP17 `0.97260`, interaction `0.97451`. This is the simpler current operational program.

## Throughput and systems audit

From 22:35 through 23:35 there were 34 commits, with median inter-commit gap `1.35` minutes, mean `1.74`, and maximum `7.20`. Thirteen new result/license/prediction artifacts landed in the Task14 chain, including prospective capability, sealed prediction, causal validation, collateral, cross-task reuse, two mediator factorials, and two scalar-law diagnostics. Managed GPU stages generally completed in 3–13 seconds; the two large within-batch mediator experiments used 9 and 17 physical forwards and completed in 9 seconds each. The GPU runner serialized all model work; CPU-only audits did not enter the queue.

The main efficiency gain was intervention batching: base, program, replay, and clamp corners shared one batch, and base-arm mediator outputs supplied clamp values within the same forward. This reduced grouped mediation from a naive capture-plus-intervene schedule to 9 physical forwards. Reusing immutable causal evidence made the scalar-law comparison CPU-only. The main avoidable cost was two claim/preregistration timestamp repairs and two capability/numerical gates that were stricter than their frozen authorities. Each was preserved append-only and repaired only at the instrument layer.

`CIRCUIT_FOCUS: PASS.` Every scientific stage altered manipulation, prospective program execution, collateral, cross-task reuse, downstream mediation, or operational compositional simplification. No rank, reconstruction, or generic representation sweep was opened.

`CEREMONY_BUDGET: PASS WITH TWO INSTRUMENT REPAIRS.` The numerical collateral and possessive capability repairs reused immutable effects and required no GPU rerun. Validation remained smaller than the scientific loop. Timestamp mistakes remain preventable: future preregistrations should derive UTC mechanically before hashing.

`NOVELTY_LESSON_GATE: PASS.` Prior-art searches and claims preceded each object. Failed larger tables were not tuned: exact-subset upstream prototypes yielded to cardinality, cardinality-specific mediator gains yielded to direction-only, and possessive reuse remained a null.

## Confounds and evidence boundary

- The prospective upstream program was fixed before third-corpus causal outcomes; its ten vectors consume neither third-corpus exact displacement nor target causal effect.
- Fixed-reader predictions were sealed before the 512 third-corpus installations. No scale or offset was fit after outcomes.
- Mediation uses direct activation interventions on both program write and mediator outputs. It is an operational controlled-clamp effect, not an observational or natural indirect effect.
- Every mediator factorial includes exact no-clamp, base-replay, and grouped-result replay; all maximum errors are zero at stored precision.
- Direction, syntax template, background cardinality, and individual rows are retained before pooling. Both mediator singleton effects recur across all registered groups.
- The six-scalar law excludes every cell sharing the held row during each fit. Its remaining error is retained; no cardinality extension is allowed after its `0.1018%` improvement.
- Collateral is established only for frozen numbered-list and bracket panels. Possessive reuse is explicitly null. Universal selectivity or shared grammatical-number semantics is not licensed.
- Literal simplicity is still partial: 13,824 upstream-vector scalars, 2,304 reader scalars, and six mediator gains are fixed, but native tokenization, embedding, L11 projection context, and tail execution remain in the causal test harness.

## Alternative routes and decision

1. **Prospective six-gain mediator transfer — selected.** Freeze the six direction gains, the ten upstream vectors, and the two readers; build a fourth Task14 authority with disjoint noun forms and unseen syntax; seal component and joint mediator predictions before clamp outcomes. Kill on capability failure, replay failure, joint cosine/error/sign failure, or direction×template failure.
2. **Joint composition with another identified circuit — second.** Cross the Task14 program with an independently identified bracket or induction write and test the exact mixed finite difference plus both task endpoints. This directly addresses reusable composition, but follows fourth-corpus validation so the Task14 mediator law is not tested and composed simultaneously.
3. **Broader collateral — third.** Add multiple unrelated behavior families with native capability and test all ten writes plus the two mediator clamps. This strengthens selectivity but does not improve the current executable computation.
4. **Native MLP15/17 feature reconstruction — deferred.** Reconstructing their internal vectors or fitting low-rank bases could reduce native compute, but current evidence supports only scalar task-effect composition. Opening generic rank now would violate the circuit-first priority.

The next action is the fourth-corpus authority and temporal seal for the six-gain mediator predictions. It changes data and syntax, not thresholds or model capacity.
