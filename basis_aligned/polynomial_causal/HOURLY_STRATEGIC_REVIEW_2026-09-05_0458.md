# Hourly circuit and systems review — 2026-09-05 04:58 UTC

## Interpretation targets

A finished circuit should specify what it reads, computes, and writes; group equivalent parts across native modules and
split native modules that do different jobs; predict held-out and OOD behavior; be sufficient or executable with a
stated background; support selective removal, swaps, or edits; compose with other circuits; and remain identifiable
across data splits and reasonable coordinate changes. Storage and compute matter for the eventual transparent tensor
program, but compression, rank, reconstruction error, and activation variance do not establish any of these properties.

The full program goal remains a smaller tensor program that is predictive on fresh text, composable when replacements
are installed together, manipulable under registered interventions, and literally simpler in storage and computation.

## What changed in the last hour

- Two screens completed: Claude's numbered-list alternative-P test and Codex's Task14 SELECT transfer test.
- Task14 head 11.3 transferred subject number across syntax on unseen nouns/templates: mean recovery 0.6273, weakest
  direction 0.5039, and direction fraction 1.0. It is held-out transfer, not selectivity.
- The FIT/SELECT runner was shared instead of copied; publication now binds SELECT phase, exact rows, and checkpoint.
- A novelty-audit wording error was found: the frozen prior-art receipt said “different lexical group,” but the executed
  rows use a matched noun group. The immutable receipt has a correction record; the result itself is unchanged.

## Throughput and failure controls

Repository timestamps show 54.2 minutes of circuit work and 1.3 minutes of operations work. Scientific screens ran at
04:25 and 04:46, so this hour produced two terminals but still missed the ten-minute serial target. The main Codex delay
was turning the first held-out use of a phase into a canonical, checkpoint-bound record. That shared path is now reusable;
the next donor-profile variation should require only a small relation builder and protocol declaration.

`CIRCUIT_FOCUS=pass` — all work produced circuit evidence or reusable circuit publication/execution machinery.

`CEREMONY_BUDGET=pass` — the scientific design and evidence dominated; focused tests took 2.62 seconds and no bespoke
compiler or backup suite was added.

`NOVELTY_LESSON_GATE=pass_with_correction` — prior dossiers and ledgers were searched, and no rank/reconstruction work
was opened. The audit caught one incorrect prior-art sentence and preserved it with an explicit correction rather than
silently changing a hash-bound record.

## Highest-information next move

The strongest cheap alternative is a deterministic cross-noun donor profile on the same SELECT phase and the same two
FIT-selected sites. It changes only the donor matching rule. If head 11.3 still transfers, its subject-number role is less
likely to depend on lexical matching; if the effect collapses, the present claim must be narrowed to matched lexical
contexts. This advances held-out prediction and stable identification. It is preferred over a projector or rank sweep.

After donor robustness, run selective removal/sufficiency with unrelated behavior controls. A same-phase donor-profile
test is not a substitute for those deeper identification requirements.
