# Structural transfer of the complete regional typed face

Use the frozen 240-row/40-sequence panel: five structural variants, two template
families and two city pairs, six endpoints. Base templates are opened; no_colon,
no_quote, neither, and short_frame prefixes are fresh relative to earlier regional
row manifests. Short frame changes wording as well as clause position: no pure
position claim. Cities/endpoints appeared previously; this is structural transfer.

Keep the prior destination rule: every position strictly after the city and
strictly before the final token. Do not retarget to quote boundaries. Native
recipient queries and non-city sources remain. Parent substitutes both donor
city key factors, current value and inherited value. Frozen face substitutes
both key factors and inherited value but leaves current value recipient.
Propagate only through the established head9 odd-value branch and native suffix.

Arms: native,parent,face,midpoint (half face),16random directions. Each random
vector lives in head8.2 output space, norm-matched to midpoint per destination;
same seed per context/cue pair, opposite sign across city cues. Seeds fixed to
17092300+1000*null_index+context_id. Four controls unchanged. 800 body forwards,
300seconds. No fit/support choice from these outcomes.

Score every condition separately (24 native endpoint pairs / four contexts).
pred_a: frozen CPU schema passes, all outputs finite, outside mask zero,
max null norm discrepancy<=1e-5, face channel identity<=1e-5,800forwards.
pred_b: native UK-minus-US paired cue>=.1 for at least18/24pairs in EVERY condition;
parent effect RMS>=1e-5 in each. A capability failure makes transfer there
inconclusive; do not substitute rows.
pred_c: frozen face effect predicts parent effect with relativeL2<=.35 in EVERY
condition and every endpoint aggregate; zero-effect prediction is the fixed
baseline (error1). No fitted predictor null or complete OOD certification.
pred_d: midpoint attenuation positive on>=75%of capable pairs, mean>=.02 per
condition. pred_e: midpoint targetRMS >=2*median16nulls and beats>=15/16 per
condition. pred_f: all four controlRMS/targetRMS<=.5 percondition; also report
full per-control null distributions (bounded collateral, not zero collateral).

Report all failures. No retuning, source omission rescue, composition promotion,
whole-model sufficiency or new token-only extraction follows from a pass.
