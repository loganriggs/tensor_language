# Hourly strategic review — 2026-08-30 23:35 UTC

Convention (§2135): L2 = CE added above the real model; LOWER is better.

## The hour: the tail price got a name — retrieval

- **§2155/§2156:** attn14's head map is diffuse (six heads carry, one negative); skip-a14 saves +0.2327 and the
  two skips compose sub-additively to −0.3222 (interaction in the shared refits, per §2154's rule). The
  two-ledger separation is now explicit in the ledger: tail dictionaries are pure coverage spend.
- **§2157:** the retrieval-vs-energy dichotomy died — a14L's damage is ALSO ~97% on retrieval classes. Both
  expensive dictionaries fail on the same thing; layers differ only in HOW they read.
- **Rung 64 (first pass, artifact pending rerun):** the pointer-linear ind primitive recovers only ~+0.011 of
  a16L's ~0.21 window cost — positive on 8/8 and well above the 0.0003 noise floor, but an order below the
  +0.05 bar: one linear map of the last occurrence's stream is NOT what attn16 computes. (Scored artifact from
  the rerun; the first run crashed at a runner tripwire after both arms completed.)
- **Rung 65 (queued):** is m16 retrieval-shaped — the coupled-retrieval-unit test for block 16.
- explanation_2335.md written (price map / two ledgers / retrieval price / instrument lessons).

## Strict ledger and gaps
Unchanged: 5.348% / 10.923% / 4.727 nat / 0 of 68. Named open object: BLOCK 16 (attn16's 3–4 concentrated heads
+ m16); the pointer result says its retrieval read is richer than last-occurrence-linear.

## Ranked next
1. Consume rungs 64/65 (write-ups; if m16 is retrieval-shaped, block-16 becomes one named unit in the registry).
2. Pointer v2 candidates ONLY with a registered mechanism: (i) pointer + current-position features jointly
   (bilinear read), (ii) max-over-occurrences instead of last, (iii) pointer at the VALUE stream of the three
   big heads. Pick ONE after seeing rung 64's per-class residual (cheap addition to the rerun? no — keep the
   artifact clean; a separate attribution if needed).
3. The 'other'-class decomposition (0.32–0.38 share at both layers) — what un-seen-token structure is being
   read? One descriptive pass.
4. attn5's write — still parked pending a mechanism.
5. Registry sync of the coverage-vs-price separation after 64/65 land.
