# Carry-only control following failed necessity prediction

Degree-split result passed both numerator omissions8/8; the prediction that linear numerator was necessary failed. Test whether the attention mixed edge's residual carry alone explains composition within the same weak-effect budget. No new fit, same opened rows and frozen edits. Reuse exact native fold, linear-only, quadratic-only and background joins. Add boundary10.95 zeroing the entire numerator; retain residual W beta only. Its denominator evaluates but multiplies zero; a deployed carry-only path needs no MLP response compiler or normalization statistics.

pred_a_instrument: inherited counts/anchors/local/readout checks and exactfold8/8. Counts12prefix16native36joined,24explicitMLP11calls.
pred_b_carry_sufficient: all8carry-only cells meet number<=.1 and eachcontrol<=.05 normalized by weaker singleton number effect.
pred_c_linear_more_precise: linear-only maximum output error is lower than carry-only maximum output error. This is accuracy, not necessity.

If carry-only passes, retract any suggestion this boundary's MLP response is required under these gates. Native background MLP and suffix remain; deleting their response correction is not deleting the module. Next investigate amplitude generalization or fresh contexts using this simpler interaction route. Native background source generation and per-input oracle edits still prevent a full extraction claim. Carry uses existing upstream edge (31296coefficients/context); no extra learned response coefficients required.
