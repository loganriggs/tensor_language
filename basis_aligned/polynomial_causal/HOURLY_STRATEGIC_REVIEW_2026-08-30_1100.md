# Hourly strategic review — 2026-08-30 11:00 UTC

## What fraction of bilin18 is actually explained

The strict whole-model ledger is unchanged:

- certified removable parameter storage: **29,196,288 / 545,904,054 =
  5.348245316%**;
- named deletion cross-entropy effect: **0.57968 / 5.30682 =
  10.923302467%**;
- unexplained deletion cross-entropy effect: **4.72714 nat = 89.076697533%**;
- registered actions passing extraction, selective removal, and OOD together:
  **0 / 68**.

These currencies are deliberately not added. Storage asks how much executable model
state is replaceable. Deletion cross-entropy asks how much measured predictive damage
has a named causal account. A response factorization or a clustering result changes
neither ledger until it composes into an executable intervention and passes its held-
out tests.

The A8 grouping confirmation finished during this review. Four of five optimization
seeds show the registered learned-direction separation; one seed shows essentially
nothing. This supports a reusable A8 geometry, but it is not a terminal circuit and
does not explain additional whole-model CE by itself.

## Largest remaining gaps and confusing results

1. **The causal-response values are collected but still lawfully sealed.** The exact
   55.5 MB FIT bundle contains 496 rows across 343 documents, but the production
   training transaction has not received an independent GO.
2. **The repaired transaction failed a second independent audit in seven ways.** All
   ordinary tests passed, which is precisely why the adversarial audit matters. The
   failures included stale protected-state publication, lock replacement, non-create-
   only directory rename, post-publication exceptions, authority-only partial state,
   stale failure provenance, and a production-path alias through the synthetic loader.
3. **The preregistered dense SVD control is mathematically infeasible at matched
   two-coordinate price.** One dense observation basis vector costs 4,802 persistent
   values, while every structured candidate costs fewer than 4,802. Thus its exact
   matched rank is zero at all 17 rank pairs. Amortizing storage over 229 documents
   would admit rank one or two in a few cases, but that is a different simplicity
   definition and may not be substituted silently.
4. **No whole-model early-MLP composition certificate exists.** MLP2 compensation
   means that independently good MLP0/MLP1 replacements can fail jointly.
5. **No semantic/editable meaning for continuous atoms is established.** A low-rank
   fit alone is coordinate-dependent; tree ranks, quotient nullity, sparse use, and
   new interventions must earn the stronger claim.

## Candidate actions pruned and ranked

The ranking uses expected information gain, causal relevance, whole-model
composability, falsifiability, compute cost, and duplication with finished work.

1. **Close the seven reproduced transaction attacks and obtain a fresh independent
   audit.** This is the only action that unlocks the already-collected causal tensor
   without invalidating it. The prospective repair is implemented and locally green;
   a self-audit cannot authorize execution.
2. **After exact GO, publish the sanitized 229-document training input.** This is the
   missing lawful interface. It exposes no validation or EVAL response values.
3. **Freeze an admissible control amendment and run the now-free GPU production-shape
   timing.** Retain rank-zero as the literal dense control; add a separable/random
   tensor null under the same persistent/code coordinates, and report any amortized
   dense control only as a third price view. Timing precedes any 2,000-step real fit.
4. **Run the frozen 51 structured training fits and mean/null controls.** Preserve all
   three seeds and every failed optimizer. Do not select with validation yet.
5. **Apply tree-cut ranks, quotient-Jacobian nullity, and signed sparse tensor
   alternatives to the training frontier.** Only survivors advance to frozen
   validation, pair composition, fresh amplitude/direction interventions, and OOD.

Rejected now: opening the FIT bundle directly; counting A8 grouping as an explained
circuit; another local activation-MSE SAE; full powersets before sparse interaction
gates; or silently pooling persistent and per-document prices to rescue the SVD arm.

## Highest-priority action executed

The independent NO-GO is immutable at audit commit `367334c0`, audit SHA-256
`d4c10c7441ea20c576ee65332302e89253b69f4e2b9708b3a712cf858b8c7dc5`, with 72
ordinary tests plus seven reproduced attacks and `outcome_access=false`.

Prospective Amendment 7 now:

- treats the owner lock as part of a twice-replayed final protected aggregate;
- makes that aggregate replay immediately adjacent to publication;
- installs the complete receipt/failure directory with Linux
  `renameat2(RENAME_NOREPLACE)`, so even an empty rival directory cannot be replaced;
- performs no filesystem work after successful installation and makes later owner
  cleanup non-propagating;
- retains the attempted authority identity before linking, allowing a complete
  failure pair after a post-link authority replay error;
- binds receipt/failure to the exact protected-aggregate digest; and
- rejects dot-dot or symlink-equivalent production artifacts on the synthetic loader
  surface, including mixed synthetic/production path sets.

The focused loader/lifecycle suite passes **22/22**. The widened factorization,
adapter, parent, input, FIT-bundle, and hierarchy closure passes **82/82 in 43.55 s**.
No response value, validation value, or EVAL value was read. The next gate is commit,
push, and another independent exact-source audit.
