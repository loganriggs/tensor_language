# Exact key-output coordinate reuse: native validation

Frozen original-weight coordinate program SHA256
10fa245ee4786e98218feb8ba2f10b335d1537d2823bd39ed8ad651e0991ed1a.
No sparse approximation remains in either head's basis. The scalar is algebraically
the original circuit, using already-required full key outputs as its input coordinates.

Reuse the632-forward selective-pair harness and original72regional/32newline panel.
A capability/instrument, B regional removal/donor/selectivity, C newline controls,
D effect preservation retain their previous thresholds. E compares complete scores
against the ORIGINAL uncompressed artifact<=1e-6relative, explicitly not the sparse
approximation. F compares original scalar and new scalar on every actual invocation,
including changed downstream contexts, <=1e-10relative. Full count300seconds cap.

Program5,414,601bytes, smaller than both previous original and sparse packages;
common-FP32payload5,064,704bytes. All maps, normalizers, values, writers and suffix
dependencies remain. Derived256KiBadapters are resident but not serialized. No
fresh/OOD, full-model saving or GPU speed claim. Historical source/result files
remain unchanged. New candidate is frozen before native outcomes.
