# Hourly review: interaction compression and solver cost

13 September 2026, 05:12 UTC; previous strategic review 04:12.

The interpretation targets remain explicit computation; cross-boundary grouping and within-module splitting; held-out/OOD prediction; extraction; selective manipulation; composition/reuse; and stable identification. The full goal is a simpler predictive, composable, manipulable executable program. The user's renewed compression directive explicitly overrides the older prohibition on compression as the primary task. Compression alone does not establish these circuit properties.

## What changed

The existing regional interaction's input generator passed the 160-prefix CPU panel, with roughly 0.095–0.215% regional local-effect error. A small generated-background screen and precision discriminator exposed substantial suffix-rounding effects on its eight fixed prefixes. The previously queued MLP9-to-MLP10 fold passed its separate native registration; these are conditional circuit improvements, not compression evidence.

The user then requested three compression settings. Their concrete targets and costs are in `explanations/for_logan/interaction_compression_settings_2026-09-13.md`.

Setting 1's fixed input-subspace and native-product selection baselines are weak. Executed spectral/support bounds limit those classes; they do not exclude sparse arithmetic graphs, learned reader frames, or simpler producer-constrained functions.

Setting 2 has a small exact retained-expression simplification: two fewer gate products, only 0.51% of total core products, exact on 120 cached writes. The learned output-block run screened ten starts and refined three to intrinsic-gradient convergence. Its 0.85% squared-loss gain masks worse spelling-contrast preservation. A separate unfolding rank bound explains why near-exact recovery is impossible at that block-rank budget. Neither result proves the actual retained producer path is incompressible.

Setting 3 now has a shared/global plus group-private function solver. Three of four favorable planted starts recover its computation; one reaches an incorrect stationary point and survives a targeted group-exchange attempt. This directly motivates restarts. The native two-capacity timing registration completed at 05:10:29 in 14.95 seconds, all bars held. First-sweep full coefficient errors are 85.08% and 76.49%, versus exact matched-storage global errors 85.85% and 77.74%. Sweeps cost 2.60 and 8.32 seconds. These gains are small and not converged.

## Alternatives and next decision

The highest-information next action is adequate multistart optimization at both declared capacities, followed by whole-function and per-token error analysis. This changes the prior comparison from one global low rank to an output graph with shared and private edges, allowing full total span. Its outcome can select candidate shared token functions for later frozen validation. It is explicitly a weight-structure screen.

A useful optimization improvement is already controlled: eliminate the common global span once during group assignment. Synthetic native-width CPU execution is 4.81 times faster for that encoder, with identical assignments and prediction error 7.73e-16. This does not imply a 4.81-times faster full GPU fit; local SVDs remain. Use an actual native replay before the improved encoder enters the next bound job.

If grouped fitting gives only modest gains, prioritize selective producer-constrained interaction graphs in settings 1 and 2, rather than increasing global ranks indefinitely. A good full-U fit still must beat meaningful native factored baselines and demonstrate more than dense quadratic functions with sparse token codes. Previous sparse dictionaries already exhibited this distinction.

Confounds retained: arbitrary-port coefficient norm differs from actual model-domain behavior; dominant token means can hide contrast loss; local stationarity is not global recovery; fixed group topology may miss overlapping parents; basis changes can inflate apparent sparsity; compilation and all normalization/background dependencies must be charged. No fresh OOD or selective-removal claim arose from these compression experiments.

## Throughput and workflow audit

There are multiple method screens, but **zero newly identified semantic circuits** from the compression block. The two recent targets belong to the same regional circuit family. Do not count optimizer starts or arms as separate circuits.

Authoritative timed runs include 105.53 seconds for the full CPU integrated panel, 33.45 seconds for queued native folding, 140.70 seconds for the learned output-block fit, 3.54 seconds for full-U feasibility, and 14.95 seconds for the new native timing run. Solver controls take subsecond to low-second CPU runs. Actual GPU compute is small relative to the elapsed hour; writing distinct implementations and interpreting negative results dominate. This supports reusing the new solver and accelerating its repeated projection, rather than creating another control framework.

Phase telemetry before 04:59 contains mostly turn-boundary markers, so it cannot honestly assign the preceding hour into science, authoring and review totals. Do not treat those spans as measured publication time. Recent explicit implementation intervals are 05:03:30–05:06:51 and 05:08:58–this boundary, with mixed testing not separately timestamped. Fix future phase boundaries prospectively.

- `CIRCUIT_FOCUS: PASS under explicit user override.` Compression targets declared circuit interactions and full-U control; no replacement of the four-property goal.
- `CEREMONY_BUDGET: FAIL TO VERIFY.` Incomplete phase boundaries prevent a defensible time split. Repair now by marking actual implementation, validation and publication transitions. Reuse existing solver/control machinery; no new audit framework.
- `NOVELTY_LESSON_GATE: PASS.` Prior mean clusters, sparse dictionaries, native products and output-block attempts were checked; recovery failures have executed discriminators and narrow claims.

Next mathematical review remains due 05:29 from the 02:29 review. Next hourly strategic review is due 06:12. Proceed with the reusable multistart controller and measured timing, preserving all local-convergence limitations.
