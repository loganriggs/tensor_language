# Three-hourly mathematical review (Claude lane) — 2026-09-02 04:10 UTC

Grounding: rungs 459–463 (the shared-matcher arc), my 463 audit's subtractive-gate reading, and Codex's rung 464
registration (3×3 matcher×correction interchange). Convention: CE effects in nat, added above native — lower is
better for damage; here "effect" = restoration of the native stake.

## A correction to my own 463 falsifier, before anything else
I proposed testing the gate by correlating mediated per-cell effects with direct over-restoration. That test is
PARTIALLY TAUTOLOGICAL: with the additive identity full = direct + mediated + interaction and interactions
measured small (±.013–.067), mediated ≈ −(direct − full) BY ARITHMETIC, so the negative correlation is forced.
The non-circular tests are (a) the smallness of the interactions themselves — already scored in 463 — and
(b) CROSS-SOURCE invariance of the correction, which is exactly what 464 registers. Withdrawn as proposed;
464's design is the right falsifier.

## Executed move: WHICH ROUTE CARRIES THE CONTEXT LAW (receipt-only, no identity circularity)
Direct and mediated per-cell effects are independent measurements (no identity links them to each other without
full). Computed from 463's receipt over the four context cells:
- direct route: .1153/.1979/.1999/.1330 — range ratio only 1.73×; var(direct)/var(full) = .137
- mediated route: −.1388/+.0384/+.1093/−.1206 — r(mediated, full) = **.9996**; var(mediated)/var(full) = 1.053
- full (native law): .0437/.2228/.2845/.0650 — range ratio 6.52×
READING: the direct equality signal is nearly CONTEXT-FLAT; the distributed suffix carries essentially the
entire context law (its profile matches the native context profile at r .9996 and full variance). The mechanism
is not "gate corrects a varying signal" but "flat carrier + suffix-resident context modulation."
CAVEAT stated plainly: r over 4 cells is descriptive, not inferential — 464's halves/interchange data will give
it degrees of freedom; treat this as the interpretation to test, not a result.

## Ranked mathematical moves
1. (executed above) Route attribution of the context law — flat-carrier + suffix-modulation hypothesis, now
   quantified and handed to 464 as the sharp expected pattern: matched corrections should reproduce the
   near−/far+/one+/multiple− profile under BOTH matcher sources if the modulation is source-independent.
2. 464 as causal-abstraction commutation (endorse, advisory): the 3×3 grid factorizes as main effects +
   interaction; the single cleanest statistic is the interaction share of the CE table (n=2 Möbius over the
   grid), complementing the registered ≥75% retention bars. Noise floor for grid dividends: 2σ ≈ .006.
3. Compile target for later (flag to direction authority): if 464 confirms source-independent modulation, the
   gate reduces to a two-argument scalar function (log-distance, predecessor-count) — a sufficient-statistic
   reduction with a literal price of tens of parameters and testable within-cell residual bounds. That is the
   executable-program payoff of this whole arc, priced honestly when its time comes.

## Pruned: Hankel/minimal realization (§2560), spectral certificates (22:10 memo), MDL (2233), archetypal/convex
family (0-for-6), common congruence (§2555), rank framings (00:47 direction).
