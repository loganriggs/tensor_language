# Hourly strategic review: validate the shell, then measure the MLP1 interface

Date: 2026-08-28 12:27 UTC

## Outcome

The explanation denominators did not move, but the consequence frontier became more
honest. The admitted rank640 complete tensor program passed new KL, task-accuracy, and
rare-target tests while failing a preregistered 98% argmax-agreement gate at
95.78/96.08%. This is not the context-free program's failure: rank640 retains
99.44/99.67% of live top-1 accuracy and essentially all rare-target accuracy. It does
show that 5.3481% storage removal is not near-exact token-choice replay.

The highest-priority safe action was this forward-only audit because the earlier
context-free work demonstrated that CE alone can flatter a replacement. It was frozen,
committed, and pushed before execution. The first invocation failed before model load
because the repository root was absent from `PYTHONPATH`; the identical committed
source then completed in 55.1 seconds. No metric, row, or threshold changed.

An independent artifact audit verified all numeric outputs, parent/row hashes, and
declared source hashes. It also found that the source enumeration omitted three
executed transitive modules. The source commit pins those contents, but strict
fail-closed provenance requires explicit enumeration; the result is therefore recorded
as bounded measured evidence rather than strict authority.

In parallel, the missing MLP1 paired-probe collector is now implemented. It directly
binds both rank640 parent hashes, exact rows/documents/tokens/positions, the frozen MLP1
geometry, disjoint probe halves, aggregate-only publication, create-only authority and
result namespaces, program buffer identity, and complete source closure. The analyzer
now publishes a per-context scalar audit ledger but no raw responses, frames, or
projectors. Thirty-six focused tests pass after lifecycle hardening. GPU measurement remains closed until the
new collector receives independent lifecycle review and a no-outcome authority receipt.

## Explanation balance

| denominator | explained or certified | remaining gap |
|---|---:|---:|
| structural inventory | 36/36 sites | none at ownership level |
| exact standalone ownership | 545,904,054 / 545,904,054 values | semantics/minimality not implied |
| consequence-certified storage reduction | 29,196,288 values = 5.3481% | 94.6519% retained |
| rank640 expanded behavioral gates | 5/6 | exact argmax agreement fails |
| named semantic behavior | 32.1% ± 6.4% | 67.9% unnamed |
| named strict causal recovery | 0.57968 / 5.30682 = 10.923% | 4.72714 nat = 89.077% |
| dense MLP simplification | 0 / 286,675,200 removed | 52.51% of model storage |

No single row is “fraction reverse engineered.” The strongest global summary is:
complete execution is solved; 5.35% removal preserves CE, KL, accuracy, rare-target
behavior, and a causal intervention distribution, but not 98% argmax identity; most
semantic and strict-causal behavior remains unnamed.

## New rank640 behavioral result

For native distribution $p$ and program distribution $q$, the audit measures

$$
\mathrm{KL}(p\|q)=\sum_v p_v\log\frac{p_v}{q_v},
$$

top-1 agreement $\mathbf 1[\arg\max p=\arg\max q]$, and task accuracy
$\mathbf 1[\arg\max p=y]$. These are different functions: two models may disagree
with each other while both being wrong, or exchange which one is correct.

| role | CE harm | KL | agreement | accuracy change | fit-frequency-0--4 accuracy change |
|---|---:|---:|---:|---:|---:|
| skip31000 | +0.005532 | 0.005114 | 0.95782 | -0.002279 | -0.000923 |
| skip35000 | +0.004449 | 0.004526 | 0.96077 | -0.001356 | +0.000099 |

All earlier CE values reproduce exactly. The expanded result therefore neither
invalidates nor upgrades the old currencies. It adds a failed axis. A separately
preregistered margin audit could ask whether disagreement is concentrated where the
live top-two logit margin is small, but that must characterize rather than erase this
failure.

## Largest missing interfaces

1. **MLP1 response geometry.** Equal-RMS edits assign 94.90--95.44% of early cut-3
   Fisher energy to MLP1, while the prior 16-probe assay saturated its measurement
   rank in 94/96 cases. Probe noise versus genuinely context-varying geometry remains
   unresolved.
2. **Physical MLP product selection.** Each MLP still evaluates 4,608 gates
   $h_n(z)=(\ell_n^\top z)(r_n^\top z)$. Dense MLPs hold 52.51% of storage. We have
   no consequence-weighted sparse selection of actual executable gates.
3. **Finite composition.** Tangent geometry does not explain the 0.458-nat MLP0/1/2
   interaction surplus or certify that retained gates survive finite replacement.
4. **Gauge and semantics.** A response factor $H_c=D_cE_c$ is unchanged under
   $D_cG^{-1},GE_c$. Neither the rank640 routing coordinates nor prospective MLP
   response coordinates have an invariant named gauge.
5. **Off-manifold robustness.** Rank640 passed 8/8 natural but only 6/8 synthetic
   intervention fixtures. Two synthetic failures and the new 4% argmax disagreement
   remain concrete boundary cases.
6. **Selective editability.** No current compressed component has a certified
   extraction/removal operation with a simultaneous collateral bound across circuits.

## Candidate pruning

The candidates were compared by expected information gain, causal relevance,
whole-model composability, falsifiability, GPU cost, and redundancy.

- More token tables, lexical clustering, bigram arms, and context-free rank sweeps are
  pruned. Concurrent discovery results sharpen this to a **fit-support/coverage**
  boundary rather than generic frequency: frequent targets absent from the fit table
  are still almost unreachable. They also found and corrected a target-favouring
  leave-one-out bigram tie-break; against the leak-free arm the context-free program
  leads by 1.1--1.4 accuracy points overall and 5.1--6.2 points on the high-coverage
  head. This rescues a real lexical advantage, but the program still has zero
  cross-position state and does not move a whole-model denominator.
- A standalone repeat of the full MLP0/1/2 tangent cut campaign is pruned. The existing
  negative already localizes roughly 95% of early response to MLP1.
- Immediate context-to-frame regression is pruned. A positive response bundle would
  not identify an encoder gauge and should first compete against sparse physical gates.
- Local-MSE PCA and independent per-module low-rank fitting remain pruned because they
  repeatedly fail joint RMSNorm/residual composition.
- Semantic naming before finite interchange/removal is pruned: a readable rotation
  without a new consequence prediction is not additional understanding.

## Top five

1. **Independently audit, freeze authority for, and run the MLP1 paired-probe assay.**
   It directly resolves the remaining measurement confound at the dominant interface.
   Its outcomes sharply prune $\le16$ local state, admit only a response bundle, or
   reject the tangent route as unstable. Cost: 256 backwards.
2. **Freeze a separate MLP1 physical product-gate leverage protocol.** For downstream
   write gradient $g_{c,a,q}$, native gate $n$ has exact global-scaling response
   $E_{(c,a),n}=\sum_q h_n(z_{c,q})d_n^\top g_{c,a,q}$. Selecting actual columns could yield a
   sparse executable polynomial program and avoids an arbitrary rotated latent basis.
   It should share a VJP engine but have a separate prospectively authorized sink.
3. **Characterize rank640 argmax disagreements under a frozen margin/calibration
   assay.** Test whether the 4% flips are concentrated at small native top-two margins,
   whether both models are wrong, and whether named token classes concentrate. This is
   cheap and falsifies the inference that the failed agreement gate is benign.
4. **Run fresh-row finite gate-pruning and MLP0/1/2 composition factorials.** Compare
   selected gates with matched random, response-norm, norm-preserving, and refitted-Down
   controls under CE, KL, rare accuracy, causal recovery, and collateral effects.
5. **Jointly gauge-fix surviving producer/consumer factors by conditional description
   length.** Minimize producer bytes plus downstream consumer bytes and sparse operation
   count over invertible latent gauges, subject to the full consequence vector. This is
   the principled route from a compressed subspace to semantic, editable coordinates.

## Executed action and next decision

The rank640 forward audit is complete, create-only, and has result SHA-256
`031e2631c3c1e2564e9e34f3b7a2e59cf1fb6e35b46c918187e429a3c49bfb37`.
Its numeric content is independently reproduced, with the transitive-source caveat
above. The reusable paired MLP1 collector and scalar ledger pass 36/36 focused tests.

The lifecycle re-audit passed. A no-outcome authority receipt was frozen, independently
audited, committed, and pushed before measurement. The authorized 256-backward run
then completed in 92.94 seconds with status `no_admitted_local_bundle` and result hash
`0d4314f34c4bf55c3542bac5ac0a7ebec593841669d3b5a484f0a9f3a47cdcdd`.

MLP1 response energy has $r_{95}=10$--17, but every energy-plus-gap selected rank is
zero and rank-16 same-context projector distance is 0.5621 against the frozen 0.15
ceiling. Stable fractions are 0/16 overall and 0/12 in the promotion cohort. Although
cross-context rank-16 distance exceeds same-context distance by 0.1343 with bootstrap
lower bound 0.1202, the same-context frames are too unstable to admit the context
bundle. (The registered 12-context contrast is 0.1327 with lower bound 0.1180; the
numbers just quoted are the all-16 diagnostic.) The next priority is therefore exact physical product-gate leverage followed
by finite gate interventions, not a context-to-frame compiler. No FineWeb, checkpoint,
CUDA, queue, or `rspd` blocker exists.

## Post-result priority ranking

1. **Exact MLP1 product-gate leverage.** Reuse the VJP engine but score the 4,608
   native bilinear products with the trajectory-complete sum over all token positions,
   whose columns have direct executable meaning. Fit paired leverage/response-energy
   selectors and test response capture on independent probes and fresh documents. This
   has the highest information gain about the 52.51% dense-MLP storage gap.
2. **Fresh-row finite gate removal and composition.** Turn any sparse leverage set
   into a causal claim by measuring CE, KL, coverage-stratified accuracy, recovery of
   named interventions, and collateral damage with matched controls.
3. **Rank640 margin/calibration audit.** Explain rather than erase the observed 4%
   argmax disagreements; separate near-ties, both-wrong flips, and class concentration.
4. **Joint producer/consumer gauge and MDL optimization.** Only after physical gates
   survive finite tests, search for a sparse shared coordinate system whose cost counts
   both the producer and all downstream readers.
5. **Bounded probe-convergence diagnostic, conditionally.** On a small frozen context
   set, measure projector distance, spectral-gap ratio, cross-half energy capture, and
   shrinkage-covariance distance at 64/128 probes. Run this only if it can distinguish
   estimator error from a genuinely smooth spectrum; do not reopen an unrestricted
   search for a canonical rank.
