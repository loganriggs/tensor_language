# Integrated MLP9–attention10 input generator: native test

Prepared before observing the queued residual-fold outcome. Submission is conditional on interpreting that predecessor; failures are preserved and any revised plan needs a new registration.

Use the existing160-prefix panel (96 regional,64 FineWeb) and retain all17 previous arms. Add arm17: full direct MLP10 product from generated child/remainder inputs and generated joint denominator. Add arm18: the same product excluding the attention/attention term, compared with the observed joint residual-plus-mixed arm14.

The new helper receives pristine z9, bias-free MLP9 output, pristine raw10/z10, shared first values, scalar edit fields, fixed-writer response matrix and attention10 weights. It receives no observed child/remainder input changes or their joint denominator. The additive output background and native downstream suffix remain external, so this is conditional generation, not autonomous circuit extraction.

- pred_a: previous17-arm outcome replay and state partition relative errors <=1e-4; input product partition <=1e-10.
- pred_b: combined generated child/remainder input relative L2 <=1% and joint-denominator relative L2 <=0.1%, separately on regional and FineWeb panels.
- pred_c: full generated-product effect versus observed full cross-product <=2%, and generated residual-plus-mixed effect versus observed joint residual-plus-mixed <=2%, in each of four regional groups.

Report both endpoints, every sign/zero discrepancy, absolute errors and FineWeb descriptive errors without filtering. No fitting, fresh corpus or OOD claim. Strongest null: finite-precision and upstream prediction errors amplify through QK normalization/products or the final behavioral readout. CPU controls establish implementation plausibility only. If a native state or behavioral bar fails, localize input/denominator/endpoint error before changing assumptions.

Price:3040 full body forwards,160 extra MLP10 evaluations,480 FP64 full attention10 evaluations;160 prefixes <=248 tokens;300-second run bound. Full model, response matrix, attention weights and supplied background generators remain charged. Binding/dry-run review will occur after the predecessor artifact is available. Do not enqueue this unbound preparation.
