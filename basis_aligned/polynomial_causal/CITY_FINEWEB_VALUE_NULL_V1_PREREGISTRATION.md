# Same-boundary specificity of the MLP8-mediated value correction

Opened promotion screen after registered factor mediation. Test whether subtracting the folded MLP8 response is distinguished from equally large random value corrections at head9.8. Do not compare against the earlier attention8-write null: its boundary and dimension differ.

19arms:native, exact head8cityswap, same swap minus MLP8-mediated head9.8 value response,16random128-dimensional value subtractions. Match each sequence/source-position norm of the actual MLP8 response; pairedcontext seed18101000+1000*k+context_id, opposite British/American sign. Same destination support;keys/otherheads and full suffix untouched. Fixed opened FineWeb rows; no fitting/selection. CPU2threads,760sequence-equivalent forwards/342block calls,180second cap, no capture forward needed.

pred_a:native/swap/real-correction score replay prior mediation abs<=1e-4 ANDrel<=1e-5;random position norms<=1e-5relative;support0;unselectedheads unchanged;subtraction<=1e-5relative;finite/counts/CPU.
pred_b:real correction target RMS change relative to swap background>=2random median and greater than every16random corrections.
pred_c:every unrelated-reader RMS change from swap<=.5real target RMS change globally.

Report reversed/other group selectivity and all null effects separately without reselecting documents. Prior reversed-group control/target ratios can exceed.5; do not describe a global pass as uniform subgroup selectivity. A pass remains opened and requires fresh intervention consistency; a fail retains local causal-necessity evidence without specificity promotion. Native regional directional failures and composition failures remain unchanged.
