# Subject-number L11H3 subject-value upstream Möbius V1

Registered after the exact source-factor assay localized the coordinate-removal
response almost entirely to the contextualized subject source. This opened-panel
assay decomposes the dominant `subject:U` atom; it makes no fresh behavioral or
OOD claim.

Propagate the L11 pre-attention residual as an exact sum of native components,
then group them into four open ports: embedding recurrence, layer 0–3 writes,
layer 4–7 writes, and layer 8–10 writes. Keep the inherited layer-0 first-value
bus as a fifth port. For every one of the 32 base/removed corners, reconstruct
the raw L11 state, apply native RMS normalization and L11H3 value/output weights,
and multiply by the frozen base subject-to-subject routing score. Compute the
complete five-port Möbius transform and project simultaneously onto the frozen
writer axis and 16 frozen orthogonal readouts.

Instrumentation requires exact raw-component reconstruction within `1e-5`,
native subject-value replay and complete Möbius closure within `1e-4` absolute
and `1e-5` relative error, exact prices, finite values, and an independent
synthetic five-factor closure within `1e-12`.

Select exact Möbius terms without fitted coefficients by greedy residual
reduction through four terms. A compact upstream candidate requires relative L2
at most `.25`. Writer specificity requires at least `.10` improvement over the
median four-term error of the 16 matched random readouts. Report every term's
RMS, aligned recovery, order, and exact port subset, including negative and
high-order terms.

Passing licenses freezing the upstream terms for fresh vocabulary/context
testing and causal reinstallation at L11H3. Failure preserves the exact
decomposition but leaves the contextualized subject value as an unresolved
native port.

Price: four partial forwards over 256 sequences, 32 exact corners, 31 Möbius
terms, 17 readouts, no logits, fitting, gradients, or parameter updates.
