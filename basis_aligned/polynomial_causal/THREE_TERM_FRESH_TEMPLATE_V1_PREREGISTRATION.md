# Three-term interaction compression on fresh templates

13 September2026. New fixed48-row panel: four new templates, six existing spelling contrasts, British/American cue pairs. Exact token sequences are disjoint from1,037sequences across48prior row-inventory files. This is a constructed template shift, not unseen vocabulary or corpus-wide OOD.

Candidate: existing frozen three-term rational numerator executor. Comparator: exact five-bank executor and native changed-prefix branches. Generate child and parent scalar fields using the assembled upstream executor and live native states; do not load cached fields for new rows. The initial prefix and downstream suffix remain native dependencies.

Before evaluating fresh rows, replay historical anchor fields at indices0,24,48,72 against existing child/parent artifacts. PredA: relative child and parent errors<=1e-4, and prepared h9 state versus normal native prefix<=1e-6. A failure invalidates the fresh instrument and must be resolved before behavioral interpretation.

PredB: each fresh family compressed original-interaction error<=2%target and<=5%control. PredC: each family incremental compressed-versus-five-bank interaction error<=1%target and<=2.5%control, with reference equal to the exact five-bank interaction norm. PredD: native mean paired regional-cue margin difference is positive in every family; failure limits any task-level interpretation even if numerical prediction passes.

All48rows and all failures are retained. Native, exact and compressed each evaluate pristine/child/remainder/parent states, with shared pristine state. Preserve scalar output errors and signs, including material threshold1e-5. Price52prefixes including4anchors, at most576fresh suffix readouts plus anchor checks;180seconds managedGPU. No data fitting, rank selection or adaptation after viewing this panel. The test validates the full original two-edit interaction conditional on declared native state ports, not just a small local numerator error.
