# Head8.2 value/routing input cache

96 reused prefixes; no activation fitting or new OOD claim. Candidate first-layer value sector comes from existing specialist-head dossier, but target is the newly localized H input of the Q7/MLP8/head9 interaction.

A: native endpoint margin relative error <=1e-4 and reconstructed head8.2 four readings <=1e-5. B: their donor field reproduces prior physical path field <=1e-4. C: first-layer generated H donor field alone approximates full head8.2 near-quote field <=0.20 relative. Null: current values or routing/value interactions prevent this simplification.

Capture joint product of both native normalized/rotated QK scores. Capture current/first values using native FP32 projections and learned mixing, then project into four readers. No alteration of native forward outputs. Store routing [96,26,26], source values [96,26,2,4], generated readings and fields. About 1 MB; 96 full forwards, 120-second runtime cap. Native background weights and contextual inputs remain required.

Source-valued H sectors both include native routing; donating a generated sector is not the same as swapping only source values with recipient routing. Subsequent CPU corner analysis will distinguish those inputs.
