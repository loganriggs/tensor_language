# One-input full city-removal package certificate

Export unchanged CITY_FULL_FRESH_V1 approximate formula with53-token exact weight
tables, head8.2 matrices and full MLP8 factors. Input residual7[B,T,1152], tokenIDs,
city position, destination mask, strength=.5. No donor state. Output block9 delta;
full suffix remains external. Preserve old paired package.

CPU and isolated copied-package40fixture replay: relative write error<=1e-4,
zero-strength zero, unknown token rejected, no imported repository modules.
Native120forwards: native, native8fullcityhalfremoval, packaged approximate write.
pred_a native/native8 anchors replay CITY_FULL_FRESH_V1[0,1]<=1e-5maxabs,
packaged write vs saved candidate<=1e-4relative, finite/outsidezero,120forwards.
pred_b candidate readouts<=1e-4absolute AND1e-5relative vs savedfresh[3].
pred_c eachfamily/each target or control effect<=1e-3relative.
pred_d isolated40fixture conditions above. All rows nowopened for implementation;
no new independent freshness, composition, token-only or matched-effect simplicity.
