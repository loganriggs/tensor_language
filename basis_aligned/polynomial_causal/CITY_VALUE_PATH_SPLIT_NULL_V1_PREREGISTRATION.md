# Specificity of direct/MLP8 value-path composition

Real split passes absolute composition on opened40FineWebsequences:globalinteraction.20018,reversed.15235,other.20125. Test same-full-write random partitions before any independent-path promotion.

A=direct value response;B=MLP8-mediated value response;S=A+B, all at head9.8 value with exactnativecityswap background. For each sourceposition choose random A'=proj_S(A)+u*||A−proj_S(A)||, where u is a random unit vector orthogonal toS. Then B'=S−A'. This preserves S and both piece norms (and their mutual angle) in128dimensions. S=0 uses A'=||A||u,B'=−A';zero-responsepositions stayzero. Sample16fixed nulls with seed18103000+1000*k+context_id and opposite pairedcue sign. No outcome conditioning or alternate real split. This conditional-orientation null differs from historical coordinate-mask splits: it additionally controls norm/cancellation geometry.

37arms:native,swap,swap−B,swap−A,swap−S,32randomsinglepieces. Reuse same joint arm for every null.1480sequence-equivalent forwards/666blockcalls,2CPUthreads,600secondcap. No new data or fit; fullsuffix and bothkeys/otherheads retained.

pred_a:first5scores replay CITY_VALUE_PATH_COMPOSITION_V1<=1e-4abs AND1e-5relative;randompiece norm errors<=1e-5relative;randomA'+B'closure<=1e-6relative;offmask0;otherheadsunchanged;subtraction<=1e-5relative;finite/counts/CPU;each randompiece targetRMS>=.01jointtargetRMS.
pred_b:real interaction<=.35smaller single globally and in both frozen subgroups;both real singles>=.10joint globally.
pred_c:real normalized interaction<=median of16random normalized interactions globally. This follows the existing source-split specificity gate; report its modest strength and all ratios/percentile. Also report each subgroup's comparison; no hidden group exclusion.

A c failure blocks independent composition promotion despite passing absoluteinteraction. A pass nominates the split for freshjoint confirmation and is not a uniqueness/gauge-invariance theorem. Original swap direction failures, mediator subgroup leakage and matched-effect simplicity remain separate limitations.
