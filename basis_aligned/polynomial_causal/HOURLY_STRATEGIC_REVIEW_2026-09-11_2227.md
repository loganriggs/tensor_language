# Hourly research review — 11 September22:27 UTC

Circuit targets remain explicit reads/operations/writes; grouping across modules and splitting within them; held-out/OOD prediction; extraction or sufficiency; selective removal/interchange; composition/reuse; and stable identification. The full goal is a simpler executable explanation satisfying these together. Follow the bilinear handoff and subsequent user corrections, not the stale better_math_ideas text in the durable goal.

The last hour confirmed the frozen output split on new words/constructions, while its full-CE-preservation and verb-retention bars failed again. Exact-native comparison supported its conditional signed-effect prediction. Upstream tracing found little attention17-mediated input change for that selected component. MLP16 and the remaining incoming residual interacted substantially; exact-native and normalization accounting showed that the quadratic interaction survives with normalization fixed, while input normalization substantially offsets it for verbs. These findings narrow the interpretation; they do not establish standalone extraction or fully selective removal.

The user then explicitly redirected the discovery object toward composed interaction paths, including full unembedding→MLP17→attention17 or MLP16, mixed interactions and subsequent sparse/shared structure. The requested proposal is in for_logan. Prior-art review found earlier exact derivations and output-map spectral/head-block measurements, but no completed jointly optimized sparse path comparison. We therefore implemented that comparison rather than presenting old folding algebra as new research. The user's weights-first discovery direction justifies the current structural optimization phase; it does not license calling coefficient fits circuits.

The first full-U, equal-price path pilot completed22:25:24. Shared features capture2.9343/2.7719% versus independent2.7262/2.7461%, at147456fitted floats each. Instrument and shared-incidence tests hold; the original10%relative advantage misses both seeds. Three arms still change edge support after four cycles; only the second independent arm meets full convergence. There is no optimization-controlled structural verdict yet. The selected-edge incidence is a property of the proposed computation, not semantic or causal reuse.

An exact executable now handles both artifact conventions, full physical output writes, explicit input normalization, edge removal, shared-node removal and overlapping joint removals. Synthetic tests pass. A new active-edge kernel preserves the same objective/gradients while avoiding the computation of inactive output coefficients during fixed-support steps. CPU errors are below2.59e-16. A longer continuation from the final pilot artifacts has passed managed enqueue, retaining every original fit/convergence bar. No restart or change of objective is being used to conceal the first miss.

The deeper two-MLP route now has a symmetric four-linear coefficient oracle, preserving the constrained upstream product computation. It reuses existing quartic symmetrization as an independent small-tensor control. Native4096Gaussian+4096random-sign probes completed22:25:30: estimated norm-squared3.6549e20/3.6482e20, estimated relative errors0.291/0.255%, about1.45seconds per distribution including the pair-partition reference. This makes the coefficient objective practical without text fitting or a huge tensor. The approximately1/3norm ratio after three-pair symmetrization is compatible with generic near-orthogonality of pairings; executed accounting prevents misreporting it as discovered learned sparsity. Actual sparse quartic fitting has not run.

Confounds and limitations checked: formal source coordinates versus reachable states; invertible output-map rank invariance; coordinate gain versus learned alignment; symmetry of shared input slots versus distinct query/key slots; input versus final normalization; fixed-support versus support-search convergence; optimizer time/line-search stops; output-group edge sparsity versus a learned output-mode sparse core; exact computational overlap versus causal reuse; and probe-training versus independent probe-audit error. No text data fit was introduced. A native norm uncertainty estimate is not a rigorous finite-sample bound. Bias/lower-degree terms and other normalized paths remain outside the homogeneous quartic oracle.

Throughput: this hour includes fresh output validation, upstream tracing, exact CPU interaction analyses, a requested long report/proposal, the matched sparse path pilot and the quartic native price check. They are not independent identified circuits; no new circuit is promoted. The new path-kernel claim22:04:15→pilot22:25:24 takes21.2serial minutes, including13.7minutes of native fitting. This exceeds the old basic-screen10minute target because the user specifically requested adequate factorization optimization, not because an extra review framework was added. A parser-only enqueue rejection and a failed board append were corrected and recorded; neither silently modified executed code. Existing contraction, manifold and execution helpers were reused.

Tool-boundary phase accounting from21:27:43 to22:27:51: science18.95min, implementation24.21, validation4.84, review2.64, publication9.49. These are coarse intervals, including concurrent GPU execution, not hardware profiling. Scientific/implementation work43.16min exceeds review/publication/validation16.97min.

- **CIRCUIT_FOCUS: PASS for the user-directed discovery infrastructure.** The new object supports cross-boundary interaction grouping and explicit interventions. No new behavioral circuit has been certified this hour; coefficient gains remain method evidence.
- **CEREMONY_BUDGET: PASS.** Core/optimizer machinery was reused, and the checks remained small. No new publishing or audit framework was added. User-requested reports account for much of publication time.
- **NOVELTY_LESSON_GATE: PASS.** Existing dossiers, prior output pullback, producer studies and quartic symmetrization were checked. Convergence and original behavioral misses remain explicit. The small1/3symmetrization ratio is not promoted as learned structure.

Ranked next steps: (1) finish same-objective support convergence with the active-edge continuation; if it still misses, inspect stationarity/support cycling rather than declare absent structure; (2) compare genuinely composed quartic sparse/block/DAG candidates using the now-priced oracle, retaining lower-degree and normalization dependencies; (3) freeze viable structural candidates and run native execution plus new FineWeb/causal validation. Another post-hoc suffix nuisance projection is lower information than these user-directed alternatives. Output-map-only composition cannot lower exact tensor rank under invertible maps; the real opportunity is constrained producer composition and cheaper shared computation.

Continuation is concrete: completed pilot interpreted, active-edge CPU checks executed, longer managed continuation accepted, quartic native result interpreted with executed pairing accounting. Full goal active. Next hourly review23:27UTC; mathematical review remains due22:51UTC. No full-goal completion or behavioral adoption claimed.
