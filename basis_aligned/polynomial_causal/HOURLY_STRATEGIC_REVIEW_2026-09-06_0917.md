# Hourly strategic review — 2026-09-06 09:17 UTC

## Circuit interpretation targets and full goal

The circuit standard remains: (1) identify reads, operations, writes, and downstream users; (2) group causally equivalent pieces across native modules and split native modules where functions differ; (3) predict held-out and OOD activations and effects; (4) extract a sufficient executable interface; (5) support selective removals, swaps, and edits; (6) predict composition and reuse; and (7) remain stable across corpus, gauge, and fitting choices. Rank, compression, storage, and compute count only when priced together with those causal properties.

The durable goal remains a smaller transparent tensor program that is prospectively predictive on fresh/OOD text, composable under joint replacement, manipulable under registered counterfactuals, and simpler under literal state/storage/compute/edge price. The current aspectual and tense-auxiliary circuits cover bounded paired-choice behaviors and still depend on a native transformer background. The full goal is not complete.

## What changed since 08:17

- The capability-authorized `has`/`had` writer-to-`is`/`was` readout test produced only intermediate transfer: A1 0.296/0.279, A2 0.241/0.237, P 0.106/0.090, C below 0.001. A2 sits in the frozen 0.20–0.25 inconclusive band, so the result was not promoted and no threshold moved.
- A selective rank-one `is`/`was` direction was independently fitted at `resid:18`. It has strong native held-out effects (A1 0.964–0.992, A2 0.560–0.562, P 0.096–0.123, C below 0.009), but cosine with `q_has` is only 0.244 and reciprocal `is/was`-writer-to-`has/had` transfer is weak. This identifies a distinct selective writer rather than one shared reader/writer direction.
- An exact projected-response factor audit separated controllability from observability. All twelve linear response predictions correlate at least 0.9988 with finite interventions. Projecting `q_has` into the `is/was` local geometry retains 0.758 normalized readout observability but only 0.540 displacement gain; projecting `q_is` toward `has/had` retains only 0.282 observability and 0.266 gain. This explains the asymmetric additive cross-readout effect without licensing a shared-writer claim.
- A fixed-budget donor-free `q_is` actuator passed all A and C bars but narrowly failed v2 P reflection at 0.727 versus the frozen 0.75 bar. The immutable endpoint audit located the bottleneck at global-budget saturation: v2 endpoints average 0.688 while interiors average 1.003. This is a budget-bounded null, not failure of the direction.
- An alternative P surface was stopped at capability-first validation: one native population passed only 4/8 rows. No causal grid or outcome was opened.
- A prospective fourth, disjoint lexicon then tested a budget-free dyadic-bracket/bisection actuator frozen before its rows were generated. All eight capability cells passed and all six causal predictions passed: A1 mean recovery 1.031, A2 1.092, P reflection 1.000, direction agreement at least 0.938, and C 0.014. It used two model forwards but 3,776 exact head evaluations.
- The next claimed screen replaces those 3,776 evaluations with a four-scalar direction-affine map from a local `resid:10` `is/was` contrast to root alpha. It is calibrated on immutable A1 roots and causally tested on A2/P/C with no donor, final-margin, grid, or outcome access.

## Strategic decision

The highest-information move is the already-claimed `resid:10`-margin-to-root-gain screen. It directly tests the missing upstream computation for the newly identified `q_is` actuator and prices simplification: four scalars and one local read can replace thousands of head evaluations if the map predicts causal roots. This advances the executable read–compute–write interface rather than changing rank.

If the screen passes, the coefficients must be frozen immediately and tested prospectively on a fifth disjoint lexicon. Only that prospective test can establish stable identification beyond the v4 calibration population. If it fails, this particular scalar reader is killed without feature, rank, or threshold rescue; the root actuator remains valid, but its controller is not yet compressed into a local circuit computation.

## Confound audit

- **Shared writer versus shared readout:** matched projected interventions and the response-factor audit explicitly separate displacement gain from output observability. `q_has` can be visible to `is/was` without being its native writer.
- **Capability:** every new population is checked before causal outcomes. The alternative P family was rejected at that gate, while the fresh v4 family passed all eight cells.
- **Budget saturation:** the fixed 7,218-unit actuator null is retained. The budget-free root result does not overwrite it; it identifies the endpoint saturation mechanism.
- **Selection and leakage:** all v4 rows enter calibration or causal testing by frozen family. Root alphas are immutable, and the affine controller cannot access donor activations, final margins, causal outcomes, or row IDs.
- **Nonlinear head:** exact RMS normalization and soft-cap are used. The local `resid:10` exactness check compares selected-pair and full-head values on the same intermediate state, not an invalid intermediate-to-final replay.
- **Precision:** projected finite-versus-first-order responses agree at correlation at least 0.9988 and route errors remain below 7e-7, far below effect bars.
- **Postselection:** no failed threshold was relaxed, no endpoint removed, and the failed alternative P surface did not open causal outcomes.

## Ranked next moves and kill evidence

1. **Run the bounded four-parameter upstream controller screen.** Killed by either direction's calibration R² below 0.50, A2/P direction agreement or effect below 0.75, C above 0.20, or any integrity/count failure.
2. **On pass, freeze coefficients and run one prospective fifth disjoint lexicon.** Killed by capability failure or any frozen A2/P/C bar; no refit is allowed.
3. **On null, diagnose the root-alpha dependency with registered scalar candidates only if they correspond to explicit local circuit operations.** Do not open a generic feature/rank sweep.
4. **After a prospective pass, compile the distinct `q_is` read–compute–write program and test joint `q_has`/`q_is` composition.** Killed if joint replacement is not predicted by the separate programs or creates large unrelated-control effects.
5. **Return to raw-text variable extraction and wider OOD scope.** These remain necessary for the durable goal but are lower-information until the second controller is resolved.

## Circuit throughput and systems checkpoint

The hour produced seven terminal scientific outcomes or audits: selective `q_is` localization, matched projected response, exact response-factor audit, fixed-budget actuator invalid, zero-forward population repair/null, alternative-P capability invalid, and prospective budget-free root actuation. The substantive receipts arrived roughly every four to eight minutes; GPU execution remained seconds, while scientific authoring and integrity checking dominated. The capability-first gate prevented an invalid family from consuming its causal grid, and the zero-forward repair avoided repeating an expensive run.

The main efficiency opportunity is now explicit: the valid root actuator uses only two model forwards but 3,776 head evaluations. The claimed controller screen caps work at 16 forwards/240 examples and, if prospectively stable, reduces runtime control to a local scalar read plus four stored coefficients. The two avoidable integrity issues this hour—a too-strong rowwise capability implementation and a lexicon overlap caught before model execution—were preserved and corrected without rerunning causal outcomes.

`CIRCUIT_FOCUS: PASS` — every completed unit localized a writer, separated response factors, bounded a control law, established donor-free manipulation, or targeted an upstream circuit computation. No rank/compression sweep was opened.

`CEREMONY_BUDGET: PASS` — validation caught two defects cheaply, capability-first screening saved a causal run, and the next experiment directly attacks the dominant 3,776-evaluation cost. Bespoke runner authoring remains the largest systems burden and should be factored after the frozen result.

`NOVELTY_LESSON_GATE: PASS` — earlier claims and controls were searched, invalid and null evidence was retained, all bars remained frozen, and the new controller is a mechanistic consequence of the controllability/observability review rather than a relabeled prior screen.

The next hourly review is due near 10:17 UTC. The next three-hour mathematical review is due near 11:30 UTC.
