# Subject-number L11H3 expanded graph fresh OOD V1

## Authority and frozen graph

Use the previously frozen rank-one confirmation authority: 32 recipient prompts
from 16 noun pairs disjoint from all earlier authorities, crossed with the new
nine-token templates `behind_beside` and `among_behind`. The authority validates
one-token noun forms, prompt novelty, two-attractor layout, and native answer
continuations.

Apply the frozen nine-edge expanded graph from
`SUBJECT_NUMBER_L11H3_EXPANDED_BEHAVIORAL_GRAPH_V2_RESULT.json` with no term
selection, reselection, or fitted coefficient. Its five native ports are
embedding recurrence, layer 0–3 writes, layer 4–7 writes, MLP8, and MLP10.
Compute all 32 corners and the exact behavioral Möbius expansion at the longer
prompt's subject position.

Run 16 new equal-L2 edits at the same pre-L11 subject site, orthogonalized per
row to the all-five target edit. Report `can`/`will` collateral. Tests are
reported overall and in the four direction-by-template cells.

## Registered gates

- Instrument: finite; frozen nine masks reproduce the bound parent; native
  corner-0 replay at most `1e-5`; upstream aggregation error at most `1e-10`;
  numerical-gauge correction relative L2 at most `1e-5`; random norm mismatch
  at most `1e-5`; absolute and relative Möbius closure at most `1e-10`; exact
  prices/checkpoint.
- Capability: native agreement accuracy at least `0.75` in every cell.
- Frozen OOD prediction/composition: nine-edge relative L2 at most `0.15`,
  cosine at least `0.98`, and positive aligned recovery in every cell.
- Removal: target damage RMS at least `0.10` and positive fraction at least
  `0.60` in every cell.
- Selectivity: overall target damage is at least twice the median equal-L2
  random damage, and `can`/`will` collateral RMS is at most half target damage.

Passing is strong lexical, construction, length, and position OOD evidence for
the frozen native-port graph. It is still not token-only extraction because the
five upstream port values are supplied by native computation.
