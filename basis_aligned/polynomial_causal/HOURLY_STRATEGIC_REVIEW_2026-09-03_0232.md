# Hourly strategic review — 2026-09-03 02:32 UTC

## Circuit interpretation targets

A useful decomposition must eventually provide all seven properties:

1. **Computational specification:** what information is read, what operation is performed, what is written, and what
   later computation uses it.
2. **Grouping and splitting across native modules:** merge parts of different heads/MLPs when downstream computation
   treats them as one variable, and split one native module when its parts serve different jobs.
3. **Held-out and OOD prediction:** predict activation and signed behavioral effects on unseen documents, tasks, and
   shifted data.
4. **Extraction or sufficiency:** an executable isolated circuit, or a precise interface plus background, reproduces
   the target computation or causal effect.
5. **Selective manipulation:** removal, swapping, or editing changes the intended behavior while preserving unrelated
   behavior, with redundancy and interactions explicitly tested.
6. **Composition and reuse:** shared subcomputations serve multiple tasks/modules and combine predictably with
   task-specific branches.
7. **Stable identification:** units survive data splits, gauges, and fitting restarts, or are defined by operational
   equivalence under downstream readers.

The full goal remains a smaller transparent tensor program that is predictive, composable, manipulable, and cheaper
under literal storage, compute, edge, state, or program pricing. Rank, reconstruction, aggregate CE, or variance alone
cannot substitute for these circuit properties.

## What changed since 01:32

Rung516 showed that rung515's exact consumer-term pairs disappear at the task-effect comparison before the32 circuit
coordinates can explain the rejection:97 pairs pass task gates in one document half,8 in the other,0 in both. This
closed the long equality-consumer descent.

Rung517 then made a genuine basis change at MLP0. It summed across heads and split attention0 sources into SELF,
PREVIOUS, NEAR, DISTANT_SAME, and DISTANT_OTHER, evaluating all32 subsets through native MLP0. After preserving and
repairing a structured-row shape failure, the valid run passed exactness and split stability but rejected both sparse
semantic hypotheses and consumer specificity. Prose Shapley shares are12.9/30.0/26.4/3.6/27.0%; structured shares
are12.8/30.8/26.2/3.4/26.8%. PREVIOUS, NEAR, and DISTANT_OTHER each recover about79--82% of the full context benefit
when used alone, yet removing them from full context loses only5.64/.80/.67%. Stable source anatomy therefore exists,
but it is diffuse and redundant rather than five additive circuits.

Rung518 is now preregistered to cross nine heads with those five relations, yielding45 exact pieces. The same signed
scale must explain each pair's singleton and removal effects on four fixed tasks plus32 circuit families, transfer to
the other30 circuits/documents, and survive bidirectional physical replacement. The pair detector recovers four
planted relations exactly in8/8 problems, and the exact45-piece constructor and response-reduction arithmetic are
CPU-tested. The model path remains fail-closed while the collector is completed.

## Is rung518 the highest-information route?

Yes, conditionally. It is not another decomposition sweep because the proposed units are accepted only when actual
downstream task-and-circuit effects identify them, not because their weights or activations are similar. It can change
targets2,3,5, and7: merge cross-head pieces, split a head by source role, predict held-out circuit families, and license
physical swaps. A full pass still would not achieve extraction/composition or simplicity; those require a later
executable grouped implementation.

The route should be killed immediately if no small relation passes the fixed32-circuit discovery test or if the
planted/exact instrument fails. It should not be rescued with rank, extra distance bins, a larger pair list, or softer
cosine thresholds.

## Confound audit

- **Baseline and nonlinear interactions:** singleton and leave-one-out backgrounds remain separate; one scale must
  predict both. This directly addresses rung517's redundancy and avoids treating Shapley allocation as additivity.
- **Native-head gauge:** the45 pieces use heads only as an overcomplete starting vocabulary. Cross-head physical
  equivalence plus a within-head separation is required before claiming improved semantic boundaries.
- **Shared token difficulty:** circuit coordinates are member-minus-matched-control CE effects, not raw loss.
- **Leakage and post-selection:** all990 pairs, fixed32/30 circuit identities, two document halves, a1--16 cap, and16
  coordinate permutations are frozen. Confirmation cannot open without discovery.
- **Dead knobs and precision:** every SINGLE/DROP edit, exact45-piece sum, FULL/native replay, task/circuit support,
  and calls are prediction-A gates.
- **Frame mixing:** every atom is installed at the same MLP0 context boundary; no attention0 direct residual path is
  silently removed.
- **Multiplicity:** the real discovery count must strictly beat the permutation95th percentile as well as absolute
  gates; a zero-control floor will be reported as vacuous if the real count is also zero.

## Genuinely different alternatives

1. **Rung518 operational atom quotient:** current first choice. Kill on an empty or non-transferring relation.
2. **One-circuit exact interaction atlas:** choose a documented circuit, expand a later bilinear MLP over all earlier
   writes, and use finite removals after gradient triage. This targets computation and selective manipulation; kill
   if no small source-pair set predicts held-out circuit effects or if unrelated circuits move equally.
3. **Task-defined state transition:** test whether coarse equality implementations can be exchanged under a closed
   later-action set. This operates above the failed exact-term grain; kill if centered task effects or later actions
   distinguish the proposed state.
4. **MLP0 token-semantic grouping:** use the complete token table, but define groups by interchangeable effects on
   multiple circuits and new contexts. Do not fit another activation SAE. Kill if token swaps fail downstream.
5. **Attention factor vocabulary across heads:** decompose each selected attention piece into shared Q/K/Q2/K2/value
   factors only after a task-conditioned atom is identified. Kill if factor sharing does not predict finite swaps.
6. **MLP16/attention5 frontier gaps:** revisit only with a circuit-conditioned interaction or executable replacement,
   never a standalone rank or quantization result.

## Ranked actions

1. Finish and CPU-test rung518's existing62-circuit collector; then build one no-outcome managed smoke.
2. If the smoke passes, run and score discovery exactly. Open confirmation and physical swaps only through the frozen
   gates.
3. If discovery is empty, pivot to the one-circuit exact interaction atlas rather than refine the45-atom vocabulary.
4. If a relation confirms physically, build its grouped executable and test joint composition before pricing it.

Rung518 survives the alternatives because it uses already-paid circuit labels to answer the user's precise grouping
and splitting question at moderate cost. Its value comes from downstream operational equivalence, not dimensionality.
