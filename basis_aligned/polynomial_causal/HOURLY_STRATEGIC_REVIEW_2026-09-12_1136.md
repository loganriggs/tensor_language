# Hourly circuit review — 12 September 11:36 UTC

Targets remain explicit reads/operations/writes; cross-module grouping and
within-module splitting; held-out/OOD prediction; extraction with declared inputs;
selective removal; composition/reuse; and stable identification. The full goal is
an executable simpler model with OOD prediction, extraction, removal and reuse.
No complete circuit or whole-model success is claimed.

This hour moved from a weight-derived shared quadratic source factor to native
regional-spelling evidence. FineWeb execution preserved the component. The first
two-source semantic screen failed; corrected grammar plus all-source coverage
passed, then four new geographic/spelling families passed. Four-port interchange
refuted parent cue dominance and localized transfer to two linear children;
their role generalized. Independent fits agreed on native effects within1%.
MLP16-only and embedding-dominance hypotheses failed. Exact residual unrolling
now traces the same two readers to distributed earlier updates. The selected
attention8/9/13 group transfers43/56%of the child effect on discovery templates.

Decision: test that fixed producer group on the existing four held-out families,
then inspect its output-to-reader weight composition before any new fit. This
advances grouping, upstream extraction and stable prediction. The managed runner
has accepted REGIONAL_PAYLOAD_ATLAS_OOD_V1; its producer outcomes were uninspected
when the>=40%per-family bar was frozen. Do not replace a miss by selecting a new
best group on these rows. Alternatives: broaden selectivity controls (needed for
promotion); identify shared parent semantics (unresolved); repeat generic tensor
fitting (lower immediate information than closing this measured dependency).

Confounds: the held-out rows already supplied whole-block results; this is
producer-role holdout only. Numerator swaps hold recipient normalization and
other downstream ports; they are not full-network module ablations. Broad
lexical probability preservation and independent extraction remain untested.
Group choice is task-informed localization of a frozen weights-first discovery,
not evidence of unsupervised upstream group selection. Native module names are
attribution boundaries, not semantic units. Existing middle-pooling dossier's
collective redundancy findings caution against declaring individual layers causal
origins. Source position coverage materially changed an earlier verdict.

Throughput: nine named native receipts completed this hour: FineWeb preservation;
regional V1, V2 and OOD; factor V1 and OOD; cross-start native; MLP16 mediation;
and producer atlas. The eight post-preservation runs took1.85–5.53seconds each,
with typical claim-to-verdict intervals of2–5minutes. These are stages of one
increasingly specified component, not nine identified circuits. CPU paired-role,
cross-start and joint-group checks reused saved arrays.

- CIRCUIT_FOCUS: PASS. New native removal, interchange, held-out role and
  independent-fit evidence; upstream localization changes the extraction plan.
- CEREMONY_BUDGET: FAIL TO VERIFY. Recorded occupancy is linked below, but
  implementation markers were omitted in some blocks (including the current
  adaptation). No backfilled times. Repair now: explicit markers before the next
  implementation, reuse the atlas and receipt scorer, no new audit framework.
- NOVELTY_LESSON_GATE: PASS. Prior MLP16 folds and middle-pooling/specialist
  dossiers checked; distinct downstream readers, no repeated whole-layer rank
  claim. Preserve each failure and its discriminating source/producer audit.

[Recorded occupancy](HOURLY_PHASE_ACCOUNTING_2026-09-12_1136.json).
Next hourly12:36UTC; mathematical review14:00UTC. Full goal remains active.
