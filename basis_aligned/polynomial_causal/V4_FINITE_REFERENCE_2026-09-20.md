# Finite-reference oracle passes, shared static amplitudes remain weak

Supplying measured signed finite unitB effects to the existing bounded LP produces16/16 selective native cells on openedv4. Minimum retention1.05220 and maximum collateral ratio.07833 meet unchanged.8/.1 gates. The previous missed congruent close_to plural subject cell reaches1.05220/.07395. Native frozen-bank and reference effects replay exactly;12prefix28suffix,1.398s runtime. Prediction still passes only5/16 cells, so this is not a finite-effect predictor.

The only change is the reference in the linear objective/control constraints. Dictionary23, amplitude box[-1,1], gamma cap2, outputs, native metrics and gates remain fixed. Primal/dual checks retain1e-7 tolerances. Planted tests verify default==explicit tangent reference, target doubling and sign reversal. The shared solver's optional target_effect parameter preserves default behavior for earlier callers. Native reference computation and recipient derivatives are explicitly charged; this is opened oracle selection, not independent extraction or new OOD validation.16/16 shows feasible selective edits in this dictionary for these tested cells, not that the dictionary is a stable semantic circuit.

## Executed next CPU consequence: how much selection can be shared?

V4_FINITE_REFERENCE_SHARING_V1.json solves the same finite-target tangent LP with one amplitude vector shared across all48inputs per role, or24per role/template. Certified worst per-row linear retention ceilings:

| Sharing | Subject | Attractor |
|---|---:|---:|
|One vector per role|.23590|.00399|
|close_to, one vector per role|.29262|.00880|
|next_to_subject, one vector per role|.36199|.02038|

These use all opened inputs and are diagnostic ceilings, not held-out fits. The constraints demand per-input linear control limits and worst-case target retention, which are stronger/different than family-aggregate native acceptance. Therefore they do NOT prove that every constant vector fails the nonlinear native metric, nor that a contextual shared computation cannot exist. They show that replacing the successful contextual oracle by a single vector under this exact surrogate is not a viable simplification.

Literal amplitude prices:96independent23-vectors=2208values for tested inputs; two role vectors46values; role/template vectors92values. These prices exclude the large native reader/source/reference generators, which dominate independence and executable simplicity. Low array storage alone is not circuit identification.

Direction: stop interpreting first-order reference feasibility as finite-effect capacity, and avoid another static-vector sweep. The demonstrated success establishes a contextual, finite-reference oracle baseline for later weight-defined feature extraction. Exact row+column attention supplies one reusable local primitive, but independent context production, nonlinear signed prediction, reusable selection and stable minimal features remain missing. No complete circuit is claimed.

Primary native receipt v4_finite_reference_v1_result.json; choices V4_FINITE_REFERENCE_CHOICES_V1.json; shared baseline audit_v4_finite_reference_sharing_v1.py. Previous failures remain in V4_SOURCE_ORACLE_AUDIT_2026-09-20.md and FIXED_QUERY_FRESH_V4_2026-09-20.md. Mathematical review deadline remains16:36UTC.
