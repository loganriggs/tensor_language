# Native price and derivative check for coupled source projection

No text fitting or native body forwards. Rebuild exact MLP15 coefficient metric H and frozen A0/A3/A8. Formal source T=[I,Hhalf]; producer projection J=diag(I,PP^T) applies to all four source slots. Use full unembedding writer Gram W^T U^T U W for the two branches, without per-reader normalization or dropping the common-logit component. Exact quartic retained norm uses K=I+HhalfPP^THhalf. Normalize loss by full norm minus purebackground norm, retaining all producer-dependent and mixed terms. Bias is absorbed into background; actual RMS denominators remain explicit ports outside this coefficient objective.

Rebuild previous shared128/shared512 spectral starts before evaluating this new objective. A: CPU dense/gradient controls held, native finite-difference tangent derivative relativeerror<=1e-5 at epsilon1e-4, fullrank retainednorm relativeerror<=1e-8, finite producer-dependent energy>0. B: one registered normalized tangent step at shared128 (initialstep.1, up to12halvings, first strict decrease) improves producer-dependent normalizedloss by>=1e-6. C: mean forward+gradient evaluation<=2seconds and peak CUDA allocation<=10GiB. Rank512 objective/gradient are reported as a capacity diagnostic, no optimization/adoption verdict. Maximum300seconds,0bodyforwards. Null: exact coupled objective is numerically uninformative or too costly to optimize with this implementation.

Persist exact coefficient forms/root/writerGram and both spectral starts, about50MBFP64 for reuse without native reload; these are experiment inputs, not an adopted circuit. Small-step improvement is neither convergence nor a behavioral result. If the cost/derivative gates hold, next work is a bounded Grassmann optimization with convergence reporting and frozen native intervention validation. No outcome-tuned frame, row filtering or repaired earlier thresholds.
