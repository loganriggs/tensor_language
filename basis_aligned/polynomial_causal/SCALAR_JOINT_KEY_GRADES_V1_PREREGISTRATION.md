# Complete inside/mixed/outside joint-key grade screen

New hypothesis after the preserved120candidate screen miss: a coherent source-subspace path includes interactions between its inside and outside coordinates. Freeze the same leading64D keysubspace of eachhead, no refit, boundary/rank change or threshold relaxation.

For joint-key scores split source state into P x and(I-P)x while retaining actual full normalization. Three exact numerator grades: inside-inside (oldpair0), mixed (oldpairs1,2,3, bothslotassignments), outside-outside (oldpairs4..9). Their sum is complete routing. Apply to8.2first-statevalue and9.8current-statevalue only. These sixpieces define all64on/off masks. This is a new exploratory grouped-path screen, not retrospective rescue of the failed one-path screen.

Regional65arms:64masks plus originalfullproducerpair. Natural66arms adds actualmeanhead8.2control. Same48email/letter and32natural prefixes, all retained. Total5232bodyforwards,360second cap, managedlane1. Everycandidate reported. Physical output subtraction and native later recomputation preserve serialdependence; denominator/input backgrounds remain charged.

A: native/sum check<=1e-5relative; baseline, both-inside(mask9), fullsector(mask63), originalfullproducer(mask64), meanhead(mask65) replay priorpathscreen<=1e-4relative. Nonzero reference checks; no replacement of effectrelativeerror with largebaseline normalization.

B: EXISTSnonempty mask with>=50%nativecuecontrast coverage,>=10/12positivepairedreductions andunrelatedmargin ratio<=.5 EACHregionalfamily. C:SAMEBmask meanabsnewlineCE<=.02,maxabs<=.1 EACHnaturalhalf, with originalnativecapability/meanheadpositivecontrol bars unchanged. Fullsector/producer failures and oldinside49.44%miss remain. Null: completejoint-key sourcegrades stillcannot isolate selective regionalcomputation.

Strongest alternatives: scalar sourcegrades need not align with behavioral usage; further query/path structure may matter; basis remains a probe and controls reused. No additionalsearch masks after scoring. If anypass, select maximum minimumregionalcoverage, then minimummaxnewlinechange, then lowestmaskid. Prospective72regional/32newline panel remains unscored and can be used under a new explicit confirmation binding; originalone-pathconfirmation was conditional on a passer and is not activated by this new registration automatically.
