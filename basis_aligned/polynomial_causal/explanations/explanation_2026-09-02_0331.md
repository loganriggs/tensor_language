# The first task-conditioned mechanism: a shared equality matcher — 2026-09-02 03:31 UTC

*(Claude-lane companion to Codex's 0303; covers rungs 457–460, the first complete arc of the corrected direction.
Convention: CE damage = cross-entropy ADDED above the native model — lower is better.)*

## The story in four experiments

Your redirect said: stop treating rank as the objective; group pieces of different heads when they implement the
same downstream computation. Four experiments later, that program has its first causally-proven mechanism.

**1. The factorial (457).** Four heads carry an exact equality/copy service. Running all 16 subsets showed every
head contributes (Shapley .10–.17 nat), the early pair {L5H5,L7H3} and the late pair {L8H3,L8H4} partially
substitute for each other across depth (interval-resolved redundancy), and the terms matter most at LONG range and
unique-predecessor contexts. But which pieces are "the same"? Not resolvable at whole-head grain.

**2. The reader screen (458).** If two heads compute the same variable, some later module should read their
outputs interchangeably — on task. Screened 72 candidates: correlations exist but are identical on-task and
off-task (the generic MLP16 interface). Registered null: whole terms are the wrong unit. The structure, if real,
lives below head grain — exactly your granularity claim.

**3. The split (459).** Each term factors exactly as (equality relation) × (QK matcher scalar) × (value payload).
Transplanting L5H5's MATCHER forward into L8H4's term — three layers away — restored the task effect almost
perfectly (recovery 1.12, bootstrap lower bound .99), read task-conditionally at MLP9, with within-pair swaps 8.5×
safer than the cross-pair control. All five registered predictions passed. The matcher is a shared, portable
object; the payloads are private. This is "one shared matcher, task-specific output branches" as heldout causal
fact — the first full pass of the new era.

**4. The code test (460).** On a sealed code corpus, frozen without refit: causal recovery .92 (lower bound .90),
interchange separation 15.5, and the control transplant actively anti-correlates. One clause missed — the
response-similarity margin thinned (.032 vs .05) — so the cross-corpus claim is conservatively withheld while the
natural-text identification stands. The causal evidence transported; the similarity margin, the weakest
instrument in this family, did not clear its bar.

## Honesty notes
Codex corrected two numbers in my 457 audit (an interval-semantics trap and a sign I read backwards — the terms
are strongest FAR, not near); both accepted, verdict unchanged. I independently re-derived the weight-audit
premise for the next test (all twelve QK slices full-rank, branches non-proportional) — it holds. Next: swapping
the two QK branches between heads to learn whether the matcher's halves are themselves shared.
