# Explanation — 2026-08-31 01:00 UTC: the measured skeleton of bilin18's retrieval system

Convention (§2135): all numbers are CE added above the real model — lower is better. Continues
explanation_2335.md.

## What the night established (§2158–§2168)

**The tail (blocks 14/16) is now a fully MEASURED mechanism, not a story:**
- Two attention modules perform the same mid-to-long-range in-document read (distance profiles near-identical,
  rising to a 32–64-token peak, half the mass beyond 32; §2162/§2164).
- They are ACTIVE BACKUPS: zeroing both is strongly super-additive (+0.058 FR, +895 summed nats on repeat
  positions; singles cheap because the other covers; cross-base corroboration; §2165).
- m16 is a subword-continuation unit (+830 summed nats) that GENERALLY SUPPRESSES the retrieval channel — its
  anti-retrieval effect grows 70% when attn16 is removed (§2159/§2166). Not a coupled pair: a general brake.
- The read defeated four cheap theories, each preregistered and scored: target-occurrence pointer (+0.011 only),
  induction pointer (−0.002), bigram addressing (17% of the sum), truncation (half the mass beyond 32 tokens).

**The front's price is the same KIND, different LAW:** the block-5 motif replacement's damage is ~85%
retrieval-class too — but OTHER-led (novel tokens) where the tail is ind-led (repeats), and distance-FLAT where
the tail rises (§2167/§2168). The cross-depth law: depth adds RANGE — generic context-mixing in front,
document-scale reading at 14/16.

**Instrument state:** in-script tripwires (twice-bitten inert-hook guard), signed-sum bars (§2159), derived
interaction signs (§2165's lesson), line-anchored splices, windows-before-reduction-claims. Rung 75 (running):
which of blocks 2–9 carry the motif price.

## Where this leaves the program

The grammar bilin18's tail needs is now specified by measurement: a shared mid-to-long-range read primitive
with backup semantics and a signed correction at m16 — plus, at the front, a distance-uniform context-mixing
element. Both ledgers stand: best full-coverage config 2.6662 (§2144); envelope 2.4230 with blocks 14/16's
attention real (§2156). The strict whole-model fractions are unchanged; the day's yield is the skeleton and
the price maps, all preregistered, failures preserved.
