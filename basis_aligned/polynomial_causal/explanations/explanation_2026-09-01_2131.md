# What happened between ~17:30 and ~21:30 (2026-09-01)

*(Plain-language companion to ledger §2537–§2553. Convention throughout: CE damage = cross-entropy ADDED above
the native model — lower is better.)*

## 1. The evening the attention0 arc turned positive
For most of the day this arc produced disciplined nulls: heads share structure diffusely, but every attempt to
name discrete shared pieces (partitions, one-sparse codebooks, low-rank carriers) died against its controls.
Tonight three positives landed, each immediately replicated on data it had never seen:

- **The 6/6/32 block (424/425).** A coupled continuous read-out of attention0's edge computation — rank-6 scores
  per branch, rank-32 payload — reproduces held-out downstream computation almost losslessly: +.0002 nat vs the
  deranged control's +.55. My fresh-row guard replicated it bit-for-bit on the bridge and within .001 off-rows.
- **The global sparse vocabulary (426/427).** One 512-atom sparse code over all heads' query/key factors beats
  18 per-head codes while costing 18.84% fewer literal bytes; every atom serves ≥5 heads. Document-stable on 96
  never-used documents — though I recorded that its CE margin narrows 4× off-docs. Atoms work as a GENERATOR;
  every attempt to read them as MECHANISM (stable identities, product-specific coupling) failed (430).
- **Generation is harder than read-out (431/433).** The same budget that reads scores near-losslessly cannot
  GENERATE them from token tables through a direct bilinear form (+1.03 nat, family closed by strong null). The
  autopsy blamed the omitted token-dependent RMS denominators — and the necessity test proved it causally:
  freezing just those 36 denominators to constants costs +.32 nat. The normalization IS mechanism.

## 2. The head census — a four-rung story with two dead hypotheses of mine
I registered that head 6 (the shared-geometry outsider from the cross-arc unification) was the positional head.
Wrong within eight seconds: head 6 has nearly ZERO positional dependence; the positional head is **head 3** —
which ties its whole profile together (position-driven scores are exactly the kind of shared input geometry the
carrier sees, while its output service stays private). Confirmed on a third sample and a population transfer.
Then my rank-taxonomy explanation for head 6 ALSO died — after I discovered my own control was confounded
(heavy-tailed scores fake concentration), rebuilt the statistic as matched-control excess, and predicted my own
hypothesis's failure as the registered outcome. Final census: head 3 positional; heads 0/1/2/5 token-identity;
heads 7/4 interaction-led; heads 6/8 intermediate. A closing echo from tonight's normalizer table: heads 3 and 6
carry the MOST token-dependent normalizers — the two "special" heads are special there too.

## 3. Process notes (kept honest)
Three of my control bars this evening were mis-derived (a floor, a window edge, a ceiling) — each scored FALSE
as written, none retro-passed, and the lesson is now a standing rule plus a lint check (preflight #4). Codex
matched that hygiene from the other side: it rejected its own instrument repair as a Goodhart risk before
running it, and re-registered a scale-aware clause instead. The failed receipts all still exist.

## 4. Where this leaves the program
Attention0 now has: a near-lossless continuous non-head basis (read-out), a real-but-modest discrete vocabulary
(generator only), a causally-necessary token-dependent normalization (head-private tables routed), and a
complete confound-corrected head census. The registered next object is the mathematics of whether the 36
normalizer quadratic forms share a common diagonalizing structure (RJD falsifier), which decides between shared
and head-private program forms before any factorization is priced.

## 5. 21:36 update — the common quadratic basis is now ruled out

The registered mathematical falsifier has run. For each of the 36 normalizers we formed the exact symmetric matrix

`A_j = W_j^T W_j / 128`, so `d_j(x)^2 = x^T A_j x + epsilon`.

First, the combined range is not small. It occupies all **1,152** residual dimensions; retaining 99% of its squared
singular-value mass still needs **1,106** dimensions. Therefore an exact shared model with 256 squared features is
mathematically impossible in full weight space.

Second, we tested the stronger structural condition. After whitening by the mean matrix, slices generated from one
common congruence basis should approximately commute and should become nearly diagonal in one shared basis. The real
median commutator is `.031670`; controls that independently permute every slice's residual coordinates are
`.031109–.031163`. The real/control ratio is **1.018**, so trained alignment is slightly worse than destroyed
alignment. The best shared-basis off-diagonal energy is `.86138`, versus `.85599` for the most favorable control,
again no advantage. The registered strong null fires.

This prevents a wasted rank-256 fit. It does **not** say the denominator functions are random: rung433b already found
real shared structure over the finite vocabulary. It says that finite-token similarity is not produced by one small
global squared-feature basis in the complete weight matrices. The honest next forms are head/map-private normalizers,
or a token-manifold evaluator that must transfer to unseen contextual states. Any such program still has to be judged
by complete cost, OOD prediction, extraction, selective intervention, and composition—not reconstruction alone.
