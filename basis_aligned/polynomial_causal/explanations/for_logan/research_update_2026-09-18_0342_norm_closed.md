# Native RMS closure and the remaining composition failure

The MLP8-mediated value correction now has an executable interface that generates the edited RMS9 scalar from native weights and token-derived initial states. Local algebra, isolated execution, and installed full-model replay pass. The direct attention path and MLP8 path compose globally on the opened panel, but their fresh joint test fails in the previously reversed subgroup; the two paths remain coupled for promotion purposes.

| Claim | Evidence | Evaluation | Result | Status |
|---|---|---|---|---|
| Generate edited RMS9 from native factors | fold | opened fresh-mediator fixtures | max relative RMS error 1.7e-8 | passes at declared interface |
| Generate the value correction | fold | opened fixtures | max relative value error 2.1e-5 | passes local gate |
| Standalone reduced interface | isolated replay | 40 fixtures | exact replay; invalid tokens and batches rejected | passes |
| Installed readout equivalence | edit/replay | 40 fresh-mediator fixtures | max readout error 6.2e-5 | passes |
| Direct and MLP8 paths compose | edit | fresh 20-document panel | global interaction .107; reversed subgroup .491 | fails subgroup gate .35 |
| Same-full-write specificity | edit | opened | global interaction .200 vs random median .210; reversed .152 vs .064 | global pass is weak and subgroup fails |

The norm-closed program stores 16,536,963 FP32 values and 402 token entries. It takes one native z8 array and one upstream delta array; token IDs generate the initial table contribution and edited RMS9 internally. The derived Wv·Down cache is priced separately. This removes the externally supplied counterfactual norm but increases literal storage relative to the 11,206,658-value supplied-norm mediator. It is an interface closure, not a compression win.

The fresh joint test used the same frozen direct and MLP8 value paths on 20 new FineWeb documents. Globally, the joint suppression is 0.79 of the parent swap effect and 97% of capable pairs remain positive. The reversed subgroup has interaction .491, control movement 1.12 times the joint target, and therefore fails the registered subgroup preservation/composition gate. Keep the two terms coupled; do not promote the global average.

The four-property accounting remains explicit: fresh effect prediction and declared-boundary extraction are supported for the mediator; selective manipulation passes for the fresh mediated correction but the original city removal/swap direction failures remain; composition is not established for the two paths; matched-effect simplicity is untested. The norm-closed expansion has a separate fresh confirmation registration pending.

## Receipts

- [Norm-closed registration](../../CITY_MLP8_NORM_CLOSED_V1_PREREGISTRATION.md), [local result](../../CITY_MLP8_NORM_CLOSED_V1_CPU_RESULT.json), [installed result](../../CITY_MLP8_NORM_CLOSED_INSTALLED_V1_RESULT.json), [isolated result](../../CITY_MLP8_NORM_CLOSED_V1_ISOLATED_RESULT.json), [package manifest](../../extracted_circuits/city_mlp8_norm_closed_v1/manifest.json).
- [Fresh joint composition](../../CITY_VALUE_PATH_FRESH_V1_RESULT.json), [registration](../../CITY_VALUE_PATH_FRESH_V1_PREREGISTRATION.md), [matched split result](../../CITY_VALUE_PATH_SPLIT_NULL_V1_RESULT.json).
- [Prior fresh supplied-norm mediator](../../CITY_VALUE_MEDIATION_FRESH_V1_RESULT.json) remains separately scoped; its prediction cannot be silently borrowed as fresh evidence for the norm-closed expansion.
- Fresh joint CPU run: 200 sequence-equivalent forwards, 90 block calls, 10.46 seconds. Norm-closed local construction: 0 full forwards, 0.85 seconds; installed replay 80/36, 2.92 seconds; isolated package replay 0.80 seconds.
- FineWeb selector cleanup: explicit iterator cleanup alone still reproduced the interpreter abort; the isolated validating/flushing worker exits cleanly and reproduces the frozen rows. The underlying dataset-library shutdown bug remains documented, not hidden.
