# The remaining upstream port is explicit

The coupled direct-plus-MLP8 operator is now installed and replayed through the full suffix, but it still consumes native unnormalized `z8` and an upstream attention8 intervention. The existing attention7/head8 export cannot be silently chained into it: its RMS8 is approximate, and its FineWeb removal direction is only 88/102 positive. That route is retained as a falsified upstream candidate.

The next fold is registered to close this port exactly from residual6 and token IDs. Its four gates are fixed in [the preregistration](../../CITY_COUPLED_UPSTREAM_PORT_V1_PREREGISTRATION.md): fresh prediction, declared extraction, null-controlled selectivity, and composition of the generated upstream write with the coupled value operator. An empty reversed subgroup will remain untested rather than being counted as a pass.

This keeps the current four-property status honest: the coupled object passes prediction, extraction, and fresh selective manipulation at its declared boundary; reuse is a candidate across two panels; the upstream port and independent internal composition remain open.
