# Fresh confirmation of the norm-closed MLP8 value mediator

The norm-closed MLP8 value mediator now has a fresh FineWeb confirmation. The expanded token table was built from native checkpoint weights for 20 new documents selected after the prior mediator panel; no model outcomes were used in selection. The result closes the edited-RMS port on fresh rows while preserving the existing scope: native `z8` and the upstream intervention remain inputs, the later suffix remains external, and independent direct/MLP8 composition is still a separate question.

| Claim | Evidence | Evaluation | Key result | Status |
|---|---|---|---|---|
| Fresh panel is outcome-blind and source-disjoint from frozen row documents | fit/selection receipt | 20 FineWeb documents, 40 sequences, 240 probes | 1,526 documents scanned; 0 document overlap; no model calls | passes |
| Generate edited RMS9 from native factors | fold | fresh 40 sequence fixtures | max relative error `1.5e-8` | passes |
| Generate the MLP8 value correction | fold | fresh 40 sequence fixtures vs independent native MLP8 reference | max relative error `3.4e-8` | passes |
| Fresh value-path prediction and controls | edit | fresh 240 probes, 115 capable pairs | interaction `.12`; joint/parent `.88`; controls ≤ `.18` globally | passes |
| Fresh direction; same-boundary null is inherited separately | edit | fresh panel plus prior null receipt | swap positive `114/115` (99%); the 16/16 null result is from the prior mediator panel, not this fresh confirmation | direction passes; fresh null pending |
| Isolated executable package | replay | 40 fresh fixtures | exact; no outside-mask movement | passes |

The fresh composition screen remains informative about scope. The reversed subgroup contains only 12 rows: its interaction is `.25`, but one control reaches `2.2×` the joint target, so subgroup preservation is not uniformly clean. The direct and MLP8 terms therefore remain coupled for promotion, even though the expanded norm generator itself generalizes cleanly.

The fresh program stores 16,518,531 FP32 values and supports 386 token IDs. It removes the supplied edited RMS9 scalar, but it is larger than the 11.2-million-value supplied-norm interface. This is an interface closure, not a simplicity win. The four-property status is: prediction passes at the declared mediator boundary; extraction passes with native `z8` and intervention ports; selective manipulation passes for the fresh mediator with the stated subgroup limitation; composition of direct and MLP8 pieces remains unresolved; simplicity remains unestablished.

## Receipts

- [Fresh row manifest](../../CITY_MLP8_NORM_CLOSED_FRESH_V1_ROWS.json), [binding](../../CITY_MLP8_NORM_CLOSED_FRESH_V1_BINDING.json), and [row audit](../../CITY_MLP8_NORM_CLOSED_FRESH_V1_ROW_AUDIT.json).
- [Fresh fold result](../../CITY_MLP8_NORM_CLOSED_FRESH_V1_CPU_RESULT.json) and [isolated replay](../../CITY_MLP8_NORM_CLOSED_FRESH_V1_ISOLATED_RESULT.json).
- [Fresh direct/MLP8 screen](../../CITY_MLP8_NORM_CLOSED_FRESH_V1_RESULT.json) remains a separate full-suffix composition receipt; it does not replace the norm-closed fold result.
- [Fresh registration](../../CITY_MLP8_NORM_CLOSED_FRESH_V1_PREREGISTRATION.md).

The source selector still has a FineWeb streaming-library shutdown bug: the ordinary process writes and validates the artifacts, then aborts during interpreter finalization. The worker workaround and hashes make the row artifact auditable; this runtime issue does not change the selected rows.
