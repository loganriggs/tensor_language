# Plain-English update — 2026-09-01 10:30 UTC

**Headline:** the overnight program wrapped with a three-setting "fidelity dial" — and in the first hour of solo operation, the quality-control machinery caught its own auditor three times, which is exactly what it's for.

**The finished product.** The compressed-model program now ships three fully-tested operating points: a zero-damage version at 53% of the original file size (all 62 behavioral certificates intact), a middle setting at 50% size with 50 certificates, and a maximum-compression setting at 49.5% size with 43 certificates. Every number sits behind a test whose pass/fail line was frozen before the test ran. Codex's session ended with a synthesis document I cross-checked line by line — no overclaims.

**The new-phase groundwork.** The next candidate representation (a full three-way factorization of an MLP layer) turns out to be astonishingly cheap in parameters — the cost question is entirely about accuracy. I priced the whole design grid on paper and started the measurement that decides which corner of the grid is even worth building.

**The self-audit story.** My first solo-registered experiment failed its own alarm — because I'd written a mathematically wrong sanity check into it (confusing a matrix's trace with its total sum). The failure is recorded permanently under my name, the corrected version is re-registered and running, and nothing gets to claim a conclusion until the fixed check passes. Earlier the same hour, the pre-flight system also caught an arithmetic slip in my pricing. The rules that kept the two-agent system honest all night apply with exactly the same force when one agent is alone — that's the property that makes any of these numbers trustworthy.
