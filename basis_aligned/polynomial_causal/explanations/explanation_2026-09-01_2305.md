# Frozen documents for the queued causal experiments — 2026-09-01 23:05 UTC

The next prerequisite is complete: the candidate bank now has separate, hash-frozen documents for learning and for
the sealed attention0 test. No candidate removal or composition answer has been generated yet.

`TEACHING` contains96 rows of257 tokens and `SEALED_CONFIRMATION` contains another96. A **row** is one contiguous
token sequence given to the model. Each role is split into two fixed48-row waves so a result must replicate across
two document groups rather than being carried by one aggregate.

The two roles share no complete row, first32-token prefix, source document, or dataset index. They also share no
complete row or32-token prefix with any FineWeb cache used to fit the45 candidate programs. This prevents a candidate
from looking causally faithful merely because its consequence is measured on its own training sequence.

The first freezer attempt stopped safely before creating files: it hashed raw tensor bytes, while the parent receipts
hash the tensor's data type and shape before its bytes. The repaired function now exactly follows that convention.
The valid teaching/sealed tensor hashes are `66034671…` and `ad279990…`; the create-only receipt is `1611c5bd…`.

These documents are fresh for the new candidate-specific consequences, but they were used by unrelated MLP2
experiments. Therefore they support the teaching-versus-unseen-program-family test, not a claim of globally untouched
data or out-of-distribution generalization.

The parallel rung446 result also clarified why the continuous attention decomposition works. Weighting raw score
directions by downstream importance raises rank-six retained energy by about10 percentage points, but leaves8.4%
and13.6% residual energy in the two score branches. The joint product fit reaches roughly1% error. Thus downstream
weighting explains part of the gain, while most of it comes from fitting the two score branches and output payload
together—not from a hidden rank-six cutoff in either score matrix alone.

Next I will freeze the exact signed-removal and composition measurements for the35 teaching candidates. Only after
those labels train a frozen consequence-specific rule will the10 attention0 labels be opened.

