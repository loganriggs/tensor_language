# What from the 20:54 plan was actually implemented — 2026-09-01 22:05 UTC

## Short answer

The 20:54 explanation contained good proposals, but it blurred three statuses: completed experiments, conditional next
steps, and research designs that had not yet been run. Two important items were indeed left as prose:

- the Archetypal-SAE comparison intended to address unstable sparse Q/K atoms;
- the historical and prospective experiments for learning a useful definition of simplicity.

They are now reinstated. The Archetypal experiment has completed as rung439b. The simplicity experiment
is being built next as a separate leakage-audited backtest; it will not be treated as completed merely because its
protocol document exists.

## Implementation audit

| 20:54 proposal | What it was supposed to test | Current evidence | Status now |
|---|---|---|---|
| Strict Archetypal SAE | Whether atoms anchored to the convex hull of real token functions are more identifiable | No prior run | Completed negative in rung439b |
| Relaxed Archetypal SAE | Whether a small departure from the hull retains fidelity while improving stability | No prior run | Completed negative at the frozen 25% relaxation |
| Signed symmetric hull | Whether signed contrasts fit Q/K better than a positive hull | No prior run | Completed: geometric specificity positive, computational consequences negative |
| Observable-profile anchoring | Judge sameness through attention scores rather than private Q/K coordinates | Rung430 used score loss, but matched restarts with private decoder cosine | Completed: stability improved relatively but missed absolute identification bars |
| Consequence validation of the hull | Require more than restart stability | No Archetypal consequence test | Completed on previously unopened FINAL; hull arms failed score/write/reader/CE noninferiority |
| Direct continuous score generator | Replace the accurate six/six readout with an executable smaller generator | Rung431 completed and hit the strong null | Completed negative result; raw linear-bilinear generator closed |
| Add the missing normalization functions | Explain why the direct generator failed | Rung433b showed constants cost `+.32122` nat; rung435 rejected a global common quadratic basis | Completed diagnosis; next continuous generator must retain private/blockwise or token-manifold normalization |
| Learn simplicity from solved circuits | Predict OOD, extraction, removal, and composition from candidate structural measures | Protocol written, no dataset or fitted predictor | Reinstated as the next distinct experiment |
| Optimize a new program under the learned rule | Test whether the rule can bootstrap a better decomposition | Impossible to justify before the predictor passes a sealed-family test | Correctly conditional, not yet implemented |
| Archetypal fresh/OOD extraction and removal | Establish causal semantics after identifiability | No hull candidate had earned this | Conditional on rung439 A/B/C/D; not silently dropped |

## What rung439 computes

For every rung430 query or key decoder atom `d`, rung439 solves an approximate convex projection using Frank–Wolfe:

`min_c ||c-d||^2`, with `c` in the convex hull of all FIT token rows.

Every Frank–Wolfe step scans the complete FIT set, chooses one real token row, and updates nonnegative mixture weights
that sum to one. There is no hidden nearest-neighbor pool. Because sparse coefficients can absorb positive scaling,
the deployed decoder stores the normalized direction `c/||c||`, while the unnormalized `c` and its mixture weights
are retained as the hull certificate.

The registered arms are an unsigned hull, a signed hull, a fixed relaxed signed hull, and an entry-permuted-token
hull. The last control preserves each head/branch marginal distribution but destroys the fact that all entries came
from one token. A real geometric result therefore requires lower projection error than that matched control for both
query and key sides.

Atom identification is not scored with raw Q/K decoder cosine. Each atom is converted into its exact score vector
against frozen real token anchors and rotary offsets. Independent restarts are matched in this observable profile
space. The run also applies legal phase rotations that commute with rotary position encoding; score profiles must be
unchanged. This prevents a private-coordinate convention from masquerading as identifiability.

Finally, the hull decoders retain rung430's exact sparse supports, coefficients, biases, and 15,583,320-byte deployed
price. Their full score pattern, attention write, six downstream responses, and suffix cross-entropy are measured on
the 96-row FINAL role that rungs426 and 430 did not open. Stability alone cannot pass the experiment.

## The learned-simplicity experiment that still needs implementation

The protocol in `SIMPLICITY_CONSEQUENCE_VALIDATION_V1.md` is an experiment design, not a result. Its first executable
version will create one row per *candidate program*, not one row per document. Each row has:

- pre-outcome structural measurements: complete producer/decoder/index price, operations, interface dimension,
  graph edges, gauge-safe dimension, locality, reuse, conditioning, and validation causal distortion;
- separately stored consequences: OOD transport, extraction accuracy, signed removal fidelity, non-target
  collateral, and composition prediction error;
- circuit family, module, grammar, seed, chronological availability time, and whether the experiment was selected
  adaptively.

The first backtest will freeze a historical cutoff, fit only on candidate results available before that time, and
predict later whole-circuit outcomes while holding out entire module/grammar families. It must beat bytes alone,
rank alone, sparse-edge count, and shuffled labels at matched validation causal distortion. Because historical
experiments were adaptively chosen, a positive backtest is only a filter. It may freeze a rule for one prospective
circuit-family prediction; it cannot establish the rule by itself.

The anti-Goodhart separation is physical: structural features and outcome labels will be different files, and the
sealed-family label file will not be loaded by the training/selection program. A rule revised after seeing a sealed
failure becomes a new generation and needs a new sealed family.

## Rung439b result

The first receipt failed only its gauge instrument because one side used BF16 rotary tables. It remains preserved as
invalid. A separately registered repair recomputed both sides of the mathematical gauge identity with exact float64
rotary tables, reducing the discrepancy to `2.22e-16` without changing any scientific value.

The real-token signed hull is genuinely different from the destroyed-token control. Mean squared projection residual
is `.7055` versus `.8938` on queries and `.7062` versus `.8923` on keys—about a 21% improvement on both sides.
This confirms that whole-token convex geometry exists.

It is not a useful causal basis. On the unopened FINAL documents:

| arm | score-product error | attention-write error | mean six-reader error | CE damage |
|---|---:|---:|---:|---:|
| unchanged score-trained `U54` | `.1892` | `.3407` | `.2305` | `+.02534` nat |
| strict signed hull `SH54` | `2.4681` | `1.4582` | `.5294` | `+.05539` nat |
| 25%-relaxed hull `RSH54` | `1.2980` | `.9432` | `.4188` | `+.04119` nat |

Observable restart cosine improves from `.288/.313` to `.470/.512` for query/key atoms, but misses the registered
`.60` floor. Projections from disjoint FIT halves reach `.751/.769`, below `.80`. Thus the hull makes atoms somewhat
more conventional and data-grounded, but not stable enough to identify and far less faithful to the computation.
A/B pass, C/D fail, and the strong null fires. The conditional extraction/removal gate is not run.

This closes convex anchoring of the raw folded factor rows at this 512-atom/k27+k27 object. It does not close a
dictionary whose atoms live directly in a downstream causal-response quotient; that is a different representation.

## Current queue and next work

The GPU queue is empty after the valid rung439b receipt. Its registered routing forbids the fresh extraction/removal
follow-up. The learned-simplicity backtest is now the active CPU/data experiment; the next GPU object must either live
directly in downstream-response space or include the exact normalization operation in a continuous generator.

## Clarification: “dropped” means queued, not abandoned

The two unimplemented 20:54 ideas were not being discarded. I now use four distinct status words:

- **queued**: we still plan to run it, but a prerequisite or an earlier experiment comes first;
- **conditional**: it runs only if a stated earlier gate passes;
- **closed**: it was actually tested and rejected at the registered object and budget;
- **withdrawn**: we deliberately no longer think the experiment addresses the project goal.

Under those definitions, the Archetypal Q/K experiment moved from queued to completed/closed at rung439b. The
learned-simplicity experiment remains active, but rung440 showed that it needs an arm-level data repair before fitting.
The extraction/removal test for Archetypal atoms was conditional, not dropped: rung439b failed the condition, so it
was correctly not run.

## Rung440: why the simplicity predictor is not being fit yet

A **receipt** is one JSON file summarizing an experiment. A **candidate program** is one specific replacement being
compared inside that experiment. These are not the same unit: one receipt can contain several arms, such as a real
dictionary, a permuted control, and an equal-price baseline. Treating that whole receipt as one training example
would erase which structure and price belonged to which outcome.

Rung440 froze receipts from rungs300–436 and ran two physically separate passes. The feature pass stored only
pre-outcome structure—source-code counts, module/grammar family, declared arms, prices, ranks, widths, atom counts,
and hashes. The label pass separately stored registered prediction and null Booleans. The files join through a hash;
the feature file was finalized first and contains no CE, loss, error, fidelity, accuracy, cosine, correlation,
certificate, damage, predicate, or null field.

The archive has enough breadth:130 canonical receipts,8 module families,7 grammar families,100% source/chronology
linkage, and registered coverage for OOD transport, extraction/identification, and composition. But only22/130
receipts (16.9%) explicitly expose their separate arms and only76/130 (58.5%) expose a machine-readable structural
price. Both miss the frozen70% bar. Therefore A/B/D pass, C fails, and the strong null does not fire: the archive is
salvageable, but fitting now would be invalid.

The next queued step is a hand-reviewed manifest with one row per candidate arm, a complete price for that arm, and
common definitions of the four consequences. Only after that manifest passes the same separation and coverage gate
will I run the historical chronological/held-family predictor. A positive historical result would still be only a
filter; the learned rule must then make a frozen prediction for a new prospective circuit family.

## Rung441: the repaired arm-level dataset and what is still missing

I completed that hand review rather than leaving it as another prose item. Generation1 contains45 distinct candidate
programs across four families. Repeated measurements of the same program are merged; control programs remain present
and labeled as controls; every row has a positive scalar or byte price and hash-pinned source receipts. Structure and
consequences are still different files.

The consequence coverage is:

| consequence | candidate programs | independent program families | fitting license? |
|---|---:|---:|---|
| cross-corpus/OOD CE | 28 | 2 | no—needs at least3 families |
| extraction/certificates | 12 | 2 | no—also needs at least20 programs |
| signed removal/intervention | 2 | 1 | no |
| composition prediction | 0 | 0 | no |

Here, an **independent program family** means a genuinely different construction, such as a vocabulary factorization
versus an MLP0 shared-input replacement—not five ranks of the same construction. Holding out a whole family tests
whether the proposed simplicity rule transfers to a different kind of program. Randomly holding out ranks from one
family would be much easier and would exaggerate generalization.

Rung441 therefore passes its manifest, OOD, and extraction coverage screens, but fails removal/composition coverage.
No learned rule is fit. This does not say simplicity cannot be learned; it says the historical experiments were not
designed as a balanced dataset for that question. The next queued experiments must create prospective removal and
composition arms in independent families, with their outcome labels sealed until the structural rule is frozen.
