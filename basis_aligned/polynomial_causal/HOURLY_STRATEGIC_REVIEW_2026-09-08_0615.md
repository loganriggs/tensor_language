# Hourly strategic review — 2026-09-08 06:15 UTC

## Circuit targets and full goal

The target remains a circuit account that supplies all seven properties: (1) a computational
specification of what is read, transformed, and written; (2) cross-boundary grouping and
within-module splitting instead of assuming native heads are semantic atoms; (3) held-out and OOD
prediction; (4) extraction or sufficiency through an executable interface plus stated background;
(5) selective removal, swap, and edit; (6) composition and reuse across commands; and (7) stable
identification across documents, gauges, and restarts. The program-level object must still be a
smaller transparent tensor program that is jointly predictive, composable, manipulable, and
literally simpler in storage, compute, state, and edges. Recovery, rank, or weight enrichment alone
cannot close that goal.

## What changed since 05:15

The shared-interface causal route passed its two important discriminators while preserving one
registered null.

1. The prefix-preserved bank licensed all 32 dual-command cells without filtering. Minimum native
   accuracy is `.875` and mean accuracy is `.9961`; the earlier temporal answer is protected by
   exact prefix identity and causal triangularity.
2. The complete head/module factorial returned the registered singleton/module null: no complete
   L9/L11/L15 module reaches `.50` recovery, so the original B/C claims remain false. It nevertheless
   exposed a distributed causal law. L9H1+L11H3+L15H5 recovers temporal `.706/.752` and is-was
   `.523/.491` on FIT/HOLDOUT, with cosine `.991-.998`, direction `1.0`, collateral at most `.00288`,
   and union-minus-singleton-sum error `.024-.057`.
3. The exact `00/10/01/11` test licensed that H3 program jointly. Simultaneous recovery is unchanged;
   temporal interaction is exactly zero, is-was interaction is `.00345-.01129`, and additive cosine
   is at least `.999936`. This is composition evidence for two task-typed commands through one
   distributed physical interface, not merely static overlap.
4. The frozen greedy test considered only the two remaining fold-stable material writers. FIT chose
   the smallest qualifying augmentation, L9H4. H4 reaches temporal `.833/.848` and is-was
   `.696/.669`; L8H1 was not added because H4 already passed the prospective quality bars. The H5
   result is reported but not selected.
5. The no-reselection H4 simultaneous confirmation is hash-bound in lane 1. A second frozen job now
   splits the two weight-nominated readers inside the causal program: L11H3:v over the command
   prefix and L15H5:q/q2 at the command query, each scored against exact complete-head parents.
   Earlier entry-12 DAS factor tests were searched and are not reused as evidence; they targeted a
   different fitted state and returned a bypass null.

## Strategic assessment and alternative routes

This path remains the highest-information route because it has progressed from shared physical
weights to causal transfer, distributed grouping, improved recovery, and simultaneous composition.
The next missing property is computational specification inside the heads. The Q/Q2/V test is a
direct native intervention with no fitted parameters, so it also avoids the task memorization that
confounded complement-loss constrained DAS.

Genuinely different alternatives remain live but lower-ranked:

1. **Reader-factor causal split (current):** can establish within-head read operations and a
   cross-boundary L11-value/L15-pattern subprogram. Kill it if immutable head replay passes but the
   nominated factors fail parent-relative FIT or HOLDOUT bars.
2. **New-template/OOD H4 confirmation:** can change held-out/OOD prediction and stable identification.
   It becomes the next promotion test after factor scoring; kill stable identification if the frozen
   H4 effect geometry or selectivity fails on unselected lexical/templates.
3. **Selective removal and extraction:** reset the licensed head/factor program and execute its
   explicit interface against native background. This changes manipulation/sufficiency; kill a
   sufficiency claim if swap success does not predict signed removal or isolated execution.
4. **Causal-response basis across tasks:** group physical sites by held-out intervention response
   rather than projected weight energy. This is appropriate only if Q/Q2/V nominations fail; it is
   killed if a fit-fold response basis does not predict the other phase/template better than
   site-label and shuffled controls.
5. **Regularized DAS:** remains deferred. Noise/KL can reduce memorization, but another optimized
   subspace is lower information than the direct native-factor falsifier. It reopens only with a
   construction-held-out objective and an independent native causal target, not complement loss
   reused as both justification and evaluation.

## Confound audit

- **Baseline subtraction and margin frame:** every effect is native-subtracted and uses fixed
  will-minus-had or is-minus-was margins across both toggle directions.
- **Frame mixing:** head and factor interventions use absolute native donor activations in the live
  model. No frozen fitted DAS coordinate is subtracted after an upstream state change.
- **Nonlinear composition:** both H3 and H4 use the exact Möbius four-cell remainder. Q/q2 likewise
  receives an explicit two-factor remainder rather than assuming additivity.
- **Shared token difficulty/leakage:** FIT alone selected L9H4; HOLDOUT was opened afterward. No row,
  template, or command cell was filtered, and the factor sites were frozen from the complete atlas.
- **Dead knobs and tripwires:** the factor runner asserts exact pair involutions, equal lengths/query
  positions, all hook calls and shapes, complete-head replay, H4 replay, and literal forward count.
- **Precision and temporal drift:** the enqueue gate warned that absolute replay can be brittle. The
  `1e-5` immutable-parent gate is intentionally retained because this immediate hook lineage has
  reproduced exactly; any actual failure makes the receipt invalid and cannot pass by bar editing.
- **Causal ordering:** later is-was interventions must have at most `1e-5` effect on every earlier
  temporal margin. Earlier-to-later collateral remains separately measured.
- **Post-selection:** neither H4 joint confirmation nor the reader split contains a selection path,
  fitted subspace, optimizer, noise schedule, or tunable regularizer.

## Throughput and systems checkpoint

Authoritative result timestamps show four circuit receipts after the previous boundary: native
license at 05:24, full head/module factorial at 05:31, H3 joint composition at 05:40, and greedy H4
selection at 05:48. From the first to fourth receipt this is about one result every eight minutes.
The two newly authored tests took roughly four minutes (H3 joint), four minutes (greedy), three
minutes (H4 joint), and six minutes (reader split) from claim to ready commit. Reused builders,
accounting, and managed hooks kept validation below the scientific design/execution block; there
were no outcome-bearing retries or changed thresholds.

The GPU is currently occupied by the pre-existing unrelated `v252` 16-control run; eight control
families have completed and its live PID remains present. This is verified queue latency, not idle
time. H4 confirmation and the reader-factor split are both content-hash-bound behind it, in that
order. Both Supervisor runners are healthy. The operating ten-minute basic-screen target was met
during the completed causal chain; deep composition and factor promotion are appropriately longer
confirmation stages.

CIRCUIT_FOCUS: PASS

CEREMONY_BUDGET: PASS

NOVELTY_LESSON_GATE: PASS

Immediate continuation: score H4 joint composition on landing, publish its immutable verdict, then
score the already-queued Q/Q2/V factor split. A factor pass routes to source-position/key
localization plus new-template confirmation; a factor null preserves H4 at head granularity and
routes to held-out causal-response grouping rather than DAS retuning.
