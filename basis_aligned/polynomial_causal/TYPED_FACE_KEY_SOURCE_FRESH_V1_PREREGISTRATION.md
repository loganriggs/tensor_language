# Fresh contextual transfer of native key-source rule V1

Freeze previous source sufficiency hypotheses before opening outcomes. FineWeb
skip11000 cache contains11eligible unused source rows after excluding all8earlier
regional corpus-transfer source documents. Select8 by fixed SHA ordering, one
city occurrence per source. City offset changes from12to8, with24tokens starting
at the city, total32tokens unchanged. Six endpoints/two city cues:96rows,
16unique sequences,8distinct contexts. Cached corpus is not pretraining-disjoint.
Fresh means unused in this inherited-city route's selection/causal panels;
broader historical model-compression work has used this cache.

Exact token-generator expansion: compile native first-layer values for all20
city tokens in the already declared ten-pair vocabulary, without fitting or
outcome access. Other head8 weights are frozen unchanged. Save as a separate
program, retaining the original eight-token package/hash. Report old/new table
rounding differences; no new city chosen using scores.

Run native, all8carry/MLP7/initial source corners with inherited donor held fixed,
direct inherited-only, direct full face, and both-key-denominators frozen:
12arms*16prefixes=192forwards,300seconds. Both RMS boundaries carried in source
factorial; same conditional head9 odd-value descendant and native suffix.

pred_a: source/current closure<=1e-5; source0/direct-inherited and source7/direct
full margins <=1e-5 maxabs AND1e-6 relF; at least36/48native city/endpoint pairs
have British-minus-American margin difference>=.1; key-increment RMS>=1e-5.
pred_b: frozen carry-only rule predicts full key increment within .35 relativeL2
in both cues and all endpoints. pred_c: carry+MLP7 rule within .20 in same cells.
pred_d: both-frozen head-key denominator edit predicts full-face effect within
.10 relativeL2 in same cells. This confirms or rejects the earlier opened
normalizer screen; failure prevents omission, without altering earlier verdicts.
pred_e: complete source Mobius closure<=1e-12, finite/live effects and all source
document/sequence exclusions verified. Four unrelated readers are reported;
there is no new source-selectivity or complete-circuit claim without its null.

No support/threshold change, fitted proxy, random-split rescue, or broader OOD
claim. The existing physical-write composition specificity failure remains.
Price20x128table plus20token indices, same head maps and two native state ports.
