# Derivative-only minimax response fit

Frozen six-atom dictionary from SHARED_FIVE_SOURCE_SPARSE_RESIDUAL_V1_RESULT.json. No plane, support, rank or outcome-label optimization. For each context group and each output, fit quadratic coefficients to the exact analytic response -g.a -0.5 a.H.a over the18 predeclared arms, minimizing the maximum group residual divided by the analytic number response norm per arm. Retain g exactly. Score afterward against native outcomes, number10% and modal5% of native number budget in every cell. Calibration/construction-heldout labels retained; both sets already opened. Modal-null arm construction remains derivative-based. All context and derivative production costs charged. This is not prospective OOD or a complete circuit. Verify fitted coefficients are unchanged if native outcome arrays are replaced before scoring. Preserve oracle result as oracle only.
