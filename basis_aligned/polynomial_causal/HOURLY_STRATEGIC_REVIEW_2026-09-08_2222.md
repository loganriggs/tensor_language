# Hourly strategic review — 2026-09-08 22:22 UTC

## Circuit interpretation targets

The circuit target remains: specify what information is read, what computation is performed,
what is written, and which later operations consume it; group across native modules and split
within them when operational evidence warrants; predict held-out/OOD effects; extract a sufficient
interface; selectively remove, swap, and edit it; compose reused subcomputations; and identify it
stably across corpora, gauges, and fitting restarts. The program-level goal remains a smaller
transparent tensor program that is jointly predictive, composable, manipulable, and cheaper in
literal storage, compute, edges, and state. Rank, reconstruction, variance, and CE alone are not
completion evidence.

## What changed since 21:22

1. Exact RMS transport identified L11H3 as a state-dependent normalization/tangent program. The
   exact input closure is below `2e-6`; the full-dose tangent has cosine `.99998`, signed projection
   `.9924`, and residual `.00945`. This replaced the invalid raw `W_O -> M11` composition.
2. Layerwise patching separated the earlier writers. L9H4 is already transport-ready after block 9;
   L9H1 needs block-10 rotation and block-11 attention gain. L8H1 is progressively oriented by
   blocks 9 and 10 and also needs block-11 attention gain. Thus the four native heads are not four
   copies of one local edge.
3. Reverse removal confirmed necessity of `L8H1/L9H1/L9H4/L11H3`: target recovery `.7215`, cosine
   `.9940`, direction `1.0`, P/C leakage `.1186/.0042`, and positive `.7257/.7177` halves. Complete
   M11-factor rescue recovered only `.1511` of that effect. The leading occupied weight mode explains
   `.8738` of the small M11-local rescue, but its direct removal reaches only `.1820`. The original
   terminal label was audited: this is a necessary four-head program with a minor M11 branch, not a
   dominant or multidimensional M11 mediator.
4. An exact residual-versus-M11 block-output factorial was preregistered and queued. It separately
   restores the post-attention residual delta, the M11 contribution, and their exact sum. A reciprocal
   downstream complete-module atlas was preregistered outcome-blind, and its reusable capture/transfer/
   reset helper now has three focused passing tests.
5. The user's weight-tensor grouping proposal produced a useful falsifier. Contracting the exact
   four-head tensor with the frozen occupied reader mode gives only `.2972` leading head-mode energy,
   `.0486` median literal-map cosine, and `.0620` median top-8 private-coordinate overlap. A gauge audit
   then corrected the naive interpretation: flattened maps and right spaces are not invariant to
   independent head-local rotations. Gauge-invariant context-Gram similarity is high (`.9245`) and
   the leading context direction overlaps around `.96`, but full-map Procrustes similarity is only
   `.6860`, rank-8 context overlap `.3961`, and rank-32 overlap `.2626`. The live hypothesis is a
   shared low-dimensional context-read core with head-private adapters/tails, not one shared complete
   writer map. No rank was selected after seeing this ladder.

## Throughput and systems audit

From authoritative timestamps, this hour produced five causal/transport results or corrections
(L11, L9, L8, necessity/M11 rescue, gauge-corrected weight grouping), two successor preregistrations,
and one reusable reciprocal patching helper. Median scientific receipt latency remained below ten
minutes until the externally owned four-hour family-separability job occupied the GPU. Our exact
factorial is hash-bound in the managed queue behind that job and one Shapley job; PID `1454327` is
live and computing, so this is a verified serialized wait, not an idle or missing runner.

Approximate serial allocation was 39 minutes scientific design/execution/interpretation, 9 minutes
focused validation and authority gates, 8 minutes shared-helper implementation, and under 4 minutes
queue/status handling. Safeguards did not dominate. CPU tensor work continued while GPU access was
serialized.

- `CIRCUIT_FOCUS: PASS` — every owned action changed the writer computation, cross-boundary grouping,
  selective manipulation, or downstream-reader plan for the confirmed circuit.
- `CEREMONY_BUDGET: PASS` — exact closures and the gauge audit addressed demonstrated high-risk
  boundaries; tests/review remained smaller than scientific work.
- `NOVELTY_LESSON_GATE: PASS` — v15/v17/v20-v23 mediation and reader atlases were searched; the work
  avoided repeating activation reconstruction and explicitly caught a head-coordinate gauge error.

## Confounds and direction check

- Baseline/nonlinear composition: all effects are native paired differences; the queued joint block-11
  correction must algebraically restore the donor state.
- Frame/padding: v23 retains equal endpoints and exact self-patch closures.
- Shared difficulty/post-selection: only the frozen 30 jointly capable target rows are inferential;
  P/C and reporter halves remain separate, and neither ranks heads or modes.
- Leakage: reverse necessity passes both controls. Residual and M11 branches will be rescored separately.
- Gauge: the first occupied-weight grouping result used literal private head coordinates. The immediate
  gauge-invariant audit preserved that result only as a checkpoint-specific diagnostic and changed the
  semantic claim to partial shared context structure plus private adapters.
- Precision/dead hooks: tensor replay is exactly zero, rotation-invariance error `2.2e-16`, and the
  downstream helper fails closed on shape, position, and hook-count changes.
- Queue pressure: the current GPU occupant is unrelated circuit-corpus work and is estimated by its own
  preregistration at roughly four hours. Reordering or direct GPU launch would violate serialization;
  CPU analysis and binding-ready implementation remain available.

The current route survives alternatives. A PCA/SAE on examples could name occupied activation patterns
but would not resolve whether heads share a computation. The exact weight contraction plus gauge audit
already answers that at the capability level, while the pending factorial and reciprocal module patches
answer actual mediation. A generic tensor decomposition is therefore demoted until a causal shared core
is prospectively isolated.

## Ranked next moves and kill evidence

1. **Exact block-11 residual/M11 factorial.** This changes computational specification and extraction.
   Kill residual dominance if its rescue projection is below `.65`; invalidate the instrument if joint
   restoration is not exact. The hash-bound job is already queued.
2. **Reciprocal downstream module atlas.** This changes downstream-consumer identity and module splitting.
   Kill a singleton reader unless both transfer sufficiency and reset necessity validate on frozen halves.
   The prior and tested shared helper already exist; bind the executor when the factorial lands.
3. **Prospective shared-context-core intervention.** This changes cross-head grouping. Freeze a rank or
   analytic spectral gap without using the opened ladder, install common context-side components with
   separate head-local Procrustes adapters, and require held-out causal interchange. Kill grouping if it
   does not outperform private-map controls or leaks to P/C. This remains behind actual residual-reader
   localization so weight capability does not outrun causal use.
4. **Direct residual-to-final-reader program.** If no downstream module passes reciprocal tests, freeze all
   suffix writes to the removed path and test the recurrent lambda-product state law plus exact final
   RMSNorm/unembedding. Kill direct carry below `.75` behavior projection or on control leakage.
5. **OOD and cross-task reuse.** Once the branch and reader are fixed, predict fresh lexical/construction
   effects and test joint installation with another circuit. Failure preserves a task-specific circuit
   record and blocks adoption.

The immediate bounded action remains the exact factorial already in the managed queue; during its wait,
the downstream atlas implementation and gauge-safe grouping test can advance on CPU without opening or
reordering another GPU claim.
