# Folded cubic trust-region pilot

12 September 2026. Circuit target: reliably discover shared source computations feeding the regional consumer, with complete-function agreement and later native intervention validation. A coefficient gain alone is not a circuit or adoption result.

The two existing failed native source-cubic endpoints are reused. For each, compare raw reader coordinates and the exact fixed-separation secant coordinates, with the same rank16 source-function family, full joint QK1 × QK2 × value weight objective, producer heads8/9/13 and four downstream readers. No text fitting. The secant code expands one pair into eight tied products, not eight independently fitted factors. Both representations have110592 nonlinear scalar parameters. Source factor gauges remain; stationarity is measured after pullback to unit-reader coordinates.

Use SciPy trust-krylov with exact autograd Hessian-vector products, a Newton trust-region method rather than claiming Gauss-Newton. Common objective divisor is the larger of the two saved native captures. Per arm:64iterations/60seconds; whole process420seconds. Optimize original relative position7; score position0 without fitting/selection there. No model body forwards. Best finite evaluated candidate retained, explicitly including trial points.

A: initial raw/secant objective agreement <=1e-6 relative and no objective worsening beyond1e-7. B: at least one secant arm gains>=10% coefficient capture AND has unit-reader gradient<=1e-6. C: select secant arm only by training capture; held-position capture must not worsen relative to its parent. Missing B with a large gradient is unfinished optimization, not absent structure. Compare all four arms, costs and termination reasons. No semantic naming from coefficient factors.

Prior art: existing raw/QR native audit and fixed secant rewrite; earlier dense shared-reader trust-region polish failed on a different small planted group model. New work differentiates the exact secant parameterization and applies matrix-free curvature to native110592-variable objective. The new ten-start dense cubic test recovers3/10 with trust-region least squares versus0/10 L-BFGS within fixed budgets; this does not establish native solver superiority.

Controls: CUBIC_PROJECTION_CURVATURE_V1_CONTROL.json (dense independent Hessian and native finite-difference HVP errors<=1e-4), CUBIC_SECANT_SOLVER_V1_CONTROL.json (same initial residuals). Native timing on CPU: gradient~1.29s, HVP~2.24s. GPU time/memory measured by this pilot; do not extrapolate toy timings. After terminal interpretation, choose adequate continuation or diverse starts using numerical evidence before any structural negative; later frozen native intervention tests are required for circuit promotion.
