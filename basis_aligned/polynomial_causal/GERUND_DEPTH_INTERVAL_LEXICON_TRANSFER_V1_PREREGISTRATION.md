# Gerund depth interval: held-out lexicon and smaller-band transfer

14 September 2026. The corrected depth-interval discovery used the sixteen
A1/A2 verbs. It found a fixed blocks7–16 plus attention17 band that reproduces
the recipient-fixed MLP17 context gate and final norm moment, and a cumulative
onset between blocks7 and11. It also failed the G collateral control. This
follow-up does not repair or relabel that failure.

Use the existing R panel's sixteen different bare/-ing token pairs and can/are
frame. These rows were used by earlier gerund work, but no depth-band outcome
has been measured on them. For each base row, the next base row is the cyclic
lexical donor; the final token is identical. Keep the recipient's actual
answer-minus-foil context reader fixed. Run base, an own-cache no-op, and the
six already fixed arms: prefixes7/11/13, complete pre-MLP17, band7-pre17 and
band11-pre17. No boundary, reader, row or threshold is selected from R.

Predictions:

* **A — instrument:** exactly8 bodies/128 sequences/288 module-hook visits; all
  16 native base endpoints capable; cyclic gate and norm references live;
  no-op logits maxabs<=1e-3 and relative L2<=1e-5; complete pre-MLP17 state,
  gate and final-state donor restoration exact at the V2 tolerances; all hashes
  fixed.
* **B — held-out band7 gate:** transfer>=.75 and relative error<=.50.
* **C — held-out onset:** prefix7 transfer<=.50, prefix11>=.50 and
  prefix13>=.65.
* **D — held-out band7 norm:** transfer>=.50 and relative error<=.75.
* **E — smaller interaction interface:** the prospectively promoted
  13-site band11-pre17 has gate transfer>=.75/error<=.50 and norm-moment
  transfer>=.50/error<=.75.

Band11 uses13 complete 1,152-vector output ports versus35 for the full upstream
prefix, a62.86% conditional port reduction. Native weights, donor generation,
other positions, MLP17 and suffix remain external. Passing validates the fixed
interaction interface across a held-out lexicon; it does not restore
selectivity, prove pretraining OOD or establish static compression. Failure
closes the smaller band without boundary/head/rank/denominator rescue.
