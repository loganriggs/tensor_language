# Three-hour mathematical, organization and efficiency review

## Mathematical objects and current evidence

Native model:18blocks, residual1152, bilinear channels4608, vocabulary50304,9attention heads of128. B(x)=D[(Lx)*(Rx)]+bias. Attention multiplies two QK products and values, with causal masking, input/head RMS, RoPE and first-layer value cache. Its unnormalized numerator is degree5. RMS and final softcap remain explicit nonlinear operations; the model is not one fixed polynomial tensor.

The current circuit interface is five named pre11 source directions at one nominated subject/attractor position. Native suffix F produces four signed logit contrasts. At baseline G[o,i],H[o,i,j] give -G a-0.5a^T H a. H decomposes exactly into local MLP11–17, attention11–17 and readout curvature transported through native Jacobians/adjoints. Self and A/B cross terms are explicit; dropping all A/B crosses fails27%number. Local normalized MLP folds retain quadratic numerator and denominator. Full native source/Jacobian/reader generation remains charged.

The folded path object q B12(lambda12 B11(K a)) is one homogeneous numerator quartic H[o,i,j,k,l], shape4x5x5x5x5 per context. It excludes RMS/background/bias and attention12 paths explicitly. Raw versus symmetric representatives agree on repeated a but can differ in tree rank. Pair-tree HT with identity leaves exposes quadratic features and a bilinear root; factor basis rotations and inverse scalings remain gauges, so sparse cores need normalized factors and shared feature accounting.

Five-property score: simple — conditional coefficient/storage gains only, some folds lose price; predicts OOD — earlier conditional predictors have limited prospective evidence, latest routes use opened data; extracted — polynomial consumers exact but native generators retained; selective — five-source modal-null low leakage yet usually low target retention; composes — some finite source sums pass, joint omission/signed/strengthened reuse failures remain. No complete identified circuit.

## LITERATURE_SEARCH

Actual searches: "site.proceedings.neurips.cc Gradient Surgery for Multi-Task Learning 2020" and "site.arxiv.org Geiger causal abstractions neural networks interchange interventions 2021". Opened primary abstracts:

- [Yu et al., Gradient Surgery for Multi-Task Learning](https://proceedings.neurips.cc/paper/2020/hash/3fe78a8acf5fda99de95303940a2420c-Abstract.html). Gradient projection motivates separating competing first-order objectives. Our intervention variables are five source amplitudes, not training parameters; our bounded LP imposes three exact modal-gradient constraints rather than the paper's update rule. Its empirical training results do not imply finite intervention selectivity or identifiability here.
- [Geiger et al., Causal Abstractions of Neural Networks](https://arxiv.org/abs/2106.02997). Their intervention-based verification makes the missing requirement precise: feature alignment must preserve counterfactual behavior. Our source edits and derivatives have no validated semantic abstraction map yet; coefficient replay is insufficient. No theorem of unique recovery is imported from the abstract.

Immediately preceding this review, the actual query "Grasedyck hierarchical singular value decomposition tensors 2010 hierarchical ranks matricization" and opened [primary institute abstract](https://www.mis.mpg.de/publications/preprint-repository/article/2009/issue-27) supported the native pair-tree baseline. Its compression assumptions concern small unfolding ranks. They do not guarantee sparse interpretable cores or compact full normalized circuits. No access failures occurred for these abstracts; full proofs were not newly reviewed.

Executable mathematical consequence: for number gradient n and modal gradient matrix M, Ma=0 eliminates modal first-order change, leaving O(||a||^2) when local second derivatives are bounded. Number retains n.a+O(||a||^2) if nonzero. A scaled ordinary edit has both effects at first order. This suggests a matched-number-strength finite control, not a guarantee. Solve l*t+q*t^2=target from the frozen quadratic; require native strength match before crediting collateral reduction. CPU MATCHED_STRENGTH_AMPLITUDES_V1.json executed:192choices,26without root in[0,1], amplitude range0.0140–1.0, worst predicted mismatch0.0850. This directly changes the native test's interpretation: some cells may be unmatchable under the preregistered bounds.

## BASELINE_COMPARISON

See DECOMPOSITION_BASELINES_2026-09-20.md. Same-interface dense5source response80values/context; per-output spectral2 uses68 and passes opened finite edits. Common plane and sparse6atom fits fail overall; oracle outcome fit cannot replace predictive baseline. Native two-MLP quartic raw2500values, canonical exact280, rank8tree656 at9.35%best worst-case coefficienterror, rank2tree116 at36%error. Thus HT has not won this small-interface price comparison. Native whole-path generators are counted separately; no tensor-to-whole-model savings claim. The next causal baseline is scaled unit-B at matched target strength.

## REDTEAM_POSITIVE

Strongest successes are exact contraction and conditional prediction. Exact contractions exclude or explicitly retain nonlinearities by scope; they do not prove native causal fidelity. Grouped minimax partial success vanished in independent-input testing; budget changes were controlled separately. HT symmetric rank8passes10% but loses to exact canonical storage. New low-collateral intervention success must survive matched-strength controls and weak native-capability cells; original80%retention failure remains.

## REDTEAM_NEGATIVE

Shared feature failures survive fullbasis/planted recovery and joint-LS controls, but oracle minimax passes all32numbercells at7.49%, refuting insufficient-capacity inference. Independent primaldual tests handle solver status failures. HT toy symmetrization rank inflation does not imply native raw representative must approximate better: symmetric rank8wins here. Keep original dtype/export failures as instrument failures. A missing root in bounded analytic matching is a control-design limitation, not evidence against causal selectivity.

## organization and efficiency

Registries and module dossiers cross-link the exact source, quartic, shared-feature and minimax receipts. A bounded repair already consolidated minimax evaluation through injected fitter/output arguments; preserve it. Native experiment should reuse native_source_observables and frozen derivatives, avoiding new autograd/LP work. Repeated small runners still duplicate native row-loading; a larger refactor now would delay the missing causal control. Actual timestamps show short GPU execution versus substantial CPU authoring; exact category minutes are unavailable. Do not report inferred idle time or ceremony ratios as measured.

CIRCUIT handoff: execute matched-strength finite interventions and preserve unmatched controls. WEIGHT_FOLDING handoff: exact cores/HT baseline remain, but target reduction of native context generators or reusable nonlinear structure; no further promotion by opened coefficient fits alone. Next three-hour deadline is three hours after this receipt's timestamp. Goal remains active.
