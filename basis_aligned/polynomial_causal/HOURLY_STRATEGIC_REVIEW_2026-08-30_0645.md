# Hourly strategic review — 2026-08-30 06:45 UTC

## New outcome this hour

The lawful causal-response program now has a frozen, source-document-disjoint split.
This is an executed metadata result, not an unrun collector:

- `1,000` census rows exactly match the committed curated-row tensor;
- those rows originate from `688` source documents, at most two rows per document;
- SHA-256 seed `184` assigns `343` documents to direction fitting and `345` to
  response evaluation;
- cross-role document overlap is exactly zero;
- the six components are `a8`, `a16`, `m16`, `a3`, `m14`, and `m13`;
- they contain `49` usable circuits;
- every circuit has member positions in at least **149 distinct source documents in
  each role**.

Thus support is not the blocker.  The next collector can fit directions and score their
causal responses without document leakage or small-cell excuses.

The frozen artifact is `causal_response_tensor_document_split.json`; its parent hashes
bind `census_state_diverse.pt`, `curated_rows.pt`, and `circuits/BATTERY.json`.
`causal_response_tensor_split.py` reconstructs it, and two deterministic split tests
pass.

## Strict project balance

No new model outcome from this lane was opened, so the strict balance remains:

- certified storage: **5.348245316%**;
- named causal CE: **10.923302467%**;
- unexplained CE: **4.72714 nat = 89.076697533%**;
- terminal actions jointly passing extraction, selective removal, and OOD: **0/68**.

The useful distinction is increasingly clear:

- many behaviors are localized;
- some tensor primitives are exact and extractable;
- almost none are yet independent, composable, and selectively removable.

The missing object is a split-safe causal interaction language, not another catalogue
of high-cosine directions.

## Running jobs and partial evidence

`substrate_geometry_census.py` is actively using the RTX 5090.  It is the other owner's
authorized ratio-only geometry census over six components.  I did not interrupt it or
launch a competing GPU job.

Its current log contains only partial, unpublished values:

- all 16 a8 circuits: shared variance `0.9309`, mean full cosine `0.9250`, residual
  cosine `0.2162`, and 10%-margin selectivity `5/16 -> 9/16`;
- all 13 a16 circuits: shared variance `0.4887`, mean full cosine `0.4271`, residual
  cosine `0.2255`, and selectivity `7/13 -> 6/13`.

These values are not banked until the registered six-component run publishes its
artifact.  If they survive, they strengthen the descriptive difference between a8 and
a16.  They still cannot supply additive response cells because the run stores absolute
concentration ratios.  A separately registered six-component geometry-versus-causality
replication is waiting on the census artifact.

Newline's freezer remains source-complete (`68/68` tests, `23/23` exact source blobs)
but has no independent authority/audit.  Bracket and successor likewise await fresh
independent audits.  Those are exact execution blockers, not scientific failures.

## Largest remaining gaps

1. **Signed response collection:** the split and schema exist, but the GPU response
   tensor has not yet been collected.
2. **Composition prediction:** MLP0 has large pair effects; independent MLP
   replacements interact; no library-level model predicts held-out combinations.
3. **Shared-service boundaries:** equality matching is exact and OOD-predictive but
   supports several use branches, defeating induction-only collateral.
4. **Executable routing:** MLP1's sparse dictionary still pays for native gates before
   selecting atoms.
5. **Terminal circuit diversity:** only previous-token and equality copying have mature
   executable programs; three other candidates are audit-blocked.
6. **Certified propagation:** no bound composes local approximation error through
   residual additions, RMSNorm, attention, and final softmax/CE.

## Candidate actions and pruning

### Kept

1. **Collect the signed response tensor on the frozen document split.** For every
   phase/source/target/document, retain signed and absolute member/off sums plus counts.
   This is the immediate prerequisite for causal CP/BTD and bootstrap uncertainty.
2. **Independently audit and execute newline, successor, and bracket.** They are the
   shortest path to more comparable executable programs and a real composition panel.
3. **Fit component-conditioned causal BTD after collection.** Compare global,
   independent, shared-only, and shared-plus-private models on held-out documents and
   held-out source-target cells at matched stored degrees of freedom.
4. **Run four-circuit Möbius prediction after terminal closure.** Predict withheld
   subset interventions and collateral rather than exhaustively fitting a powerset.
5. **Factor equality matcher from payload/use branches.** This targets an observed OOD
   collateral failure rather than optimizing another local reconstruction.

### Pruned or delayed

- CP/BTD on the existing concentration ratios: not additive and therefore not a tensor
  program interface.
- Another row-level split: source document IDs exist, so row parity would accept
  pseudoreplication unnecessarily.
- More geometry-only hierarchy searches: M16 already reverses geometry-to-causality;
  the registered six-component replication should finish before any new geometry work.
- More flat rank sweeps/local MSE fits: completed results already show enrichment
  without causal sufficiency.
- Ten-circuit powerset: too expensive and mixes readiness levels; first require a
  predictive four-circuit gate.
- Reopening the expired eight-hour queue: its failures and incomplete cells remain
  preserved, but its deadline passed and its branches have already been pruned.

## Ranked priorities

1. **Signed response collection** — highest information gain and now fully supported by
   a frozen split; directly enables causal factorization and edit prediction.
2. **Independent terminal audits/runs** — highest near-term causal utility and required
   for a behavior-diverse program library.
3. **Held-out causal BTD** — tests the user's shared/private/DAG idea with the correct
   object and an explicit price.
4. **Four-circuit Möbius composition** — most direct whole-program composability test,
   conditional on priority 2.
5. **Equality service/use factorization** — most targeted route toward selective
   removal and reusable primitives.

## Action executed

The highest-priority safe CPU portion of priority 1 was completed while the GPU was
occupied:

- found and bound the true source-document IDs absent from the compact census state;
- proved exact equality between curated and census rows;
- selected the document split using membership metadata only, without model outcomes;
- froze all parent hashes, role counts, components, circuits, and support counts;
- ran the deterministic split tests and generated the receipt.

The GPU collector is not launched concurrently.  The exact blocker is physical GPU
ownership by `substrate_geometry_census.py`; its registered successor also depends on
the published census artifact.  The next safe launch is a serialized signed-response
collector after that chain releases the device, using this immutable document split.
