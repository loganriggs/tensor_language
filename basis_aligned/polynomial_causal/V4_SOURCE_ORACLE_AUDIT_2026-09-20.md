# Contextual oracle recovers15/16, with a finite-reference mismatch

Per-input native readers and the unchanged23-port bounded LP improve openedv4 selectivity8/16→15/16. All8attractor cells and7/8subject cells pass; all16collateral limits pass. The remaining congruent close_to plural subject cell retains.75830 versus required.8. Do not round to a pass. Exact native gradients still predict finite effects within the registered bars in only6/16cells. Instrument replays frozen-bank and unitB native effects exactly;12prefix4double36reverse28native suffix,2.85s runtime. Readers, sources, source states and orientations are saved in v4_source_oracle_audit_v1_tensors.pt; their native generation is charged.

This strongly implicates contextual selection in most frozen-bank failures but does not establish full dictionary sufficiency, a reusable selector, or independent prediction. LP primal/dual certificates validate the stated tangent optimization; they do not certify the finite nonlinear intervention.

## Executed CPU audit of the remaining failure

V4_ORACLE_FINITE_GAP_V1.json compares tangent predictions with finite effects and the finite unitB reference, with no refitting. In the missed cell, selected tangent effect projects to.57252 retention against finite unitB; actual selected effect reaches.75830. Endpoint-minus-tangent adds+.18579 projected retention. Thus curvature helps this edit; it is incorrect to explain this miss as curvature reducing a predicted finite pass.

The tangent of unitB itself projects to only.60408 of its finite effect and has41.32% relative number-effect error. The LP defines reference strength from that tangent; the native acceptance gate uses the finite unitB effect. Those are different reference objects. The current implementation matches its preregistered objective, so the negative is not discarded as a software bug. It exposes an objective/measurement mismatch that must be addressed explicitly before interpreting feasibility as finite dictionary capacity.

Next discriminating diagnostic: hold dictionary, amplitude bounds and full native gates fixed, but supply the measured unitB signed finite target to the LP reference constraints and retain native validation. This charges the unitB evaluation and is an opened oracle experiment, not an extracted rule. It may fail because a linear objective still cannot guarantee finite behavior; such a miss remains. A half-radius test would instead diagnose curvature but does not directly resolve the mismatched reference objective.

Exact row+column attention replacement remains validated at strict.1%effect error; it cannot solve this selection issue. For the original goal, reusable contextual feature generation and signed finite prediction remain missing. No complete circuit is claimed.
