# Trim unused native context from the certified interchange formula

Fresh CITY_FULL_INTERCHANGE_V1 passes its five registered gates. This is an opened
implementation follow-up, not fresh behavioral evidence or a new fitted operator.
By causal attention, donor city fields need only tokens/residual6 through the city.
Recipient query fields need only the prefix through the last edited destination.
Generate each prefix with unchanged full-nine-head/five-reader weights, then pad the
resulting delta to the requested output length with exact zeros outside destinations.

The interface remains TWO logical residual6 contexts, with corresponding token IDs,
city index and full output destination mask. For original T32/city12/destination13..30,
recipient prefix31 plus donor prefix13 means50,688native floats versus73,728previously.
No unknown-state imputation; no change to full query/key/value interactions, RMS
approximation, rotary positions or native MLP8/suffix. Weight count unchanged.

CPU local and isolated fixtures:40now-opened fresh interchange sequences; prefix
output relativeerror<=1e-4 versus frozen generated swaps on every sequence, exact
support0,finite,unknown tokens rejected, imports outside repository. Batch1contract.
The prior untrimmed package/input interface remains reproducible.

Installed CPU certificate: native and trimmed generated swap, two padded full-model
batches/80sequence-equivalent forwards/36blockcalls,2CPUthreads,180second cap.
Native scores match prior native and trimmed scores match prior generated swaps at
abs<=1e-4 ANDrel<=1e-5; all five readout effecterrors<=1e-3. Requiredcallcounts,
finite/support0,CPUonly. This preserves earlier evidence under a declared precision
contract; it cannot improve failed source composition or establish simplicity versus
a matched-effect random component. Price weights and state separately.
