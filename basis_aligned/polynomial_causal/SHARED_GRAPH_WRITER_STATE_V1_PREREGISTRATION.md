# Fixed writer-state compilation of the same shared graph

Use the same frozen shared-parent union and selected pair4/5 as the completed
feature-frame compilation. Keep19active physical group writers W. Input readers
are the12parents and25folded private partners; they need not lie in span(W).
Maintain h=alpha*x0+W*a. Precompute initial reader values, W^T x0, inputnorm,
initial fullU readout, F*W, W^T W and U*W; all are charged. Retain exact native
RMS scale/epsilon and residual reentry. No original intermediate hidden state.

The products and sparse dependencies are the original graph's mixed and shared
pairs, counted once. Implementation may store a dense19x42mixing map, priced as
such. No refit, support change or new native behavioral claim.

A: closed versus direct shared graph physical-state/full-logit/norm errors
<=1e-10 on72saved preMLPinputs, allfour parent-removal arms. B: single/joint
signed-logit-effect error <=1e-8; interaction error <=1e-6 and norm>1e-8.
C: total compiled core+output-adapter coefficients below the56-frame version,
with immutable per-input cache no larger. Retain fullU encoding cost in both.
Also report compactDAG baseline; a gain over a redundant frame alone is not
discovery or a whole-model simplification. CPUonly, no nativebodyforward.
