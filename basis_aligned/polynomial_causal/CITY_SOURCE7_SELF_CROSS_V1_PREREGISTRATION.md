# Diagnose the source-group direction failure at its product terms

The fresh source-group direction gate failed:80%positive versus90%, with the same
four reversing documents under native and generated group removal. The full city
removal and complement were positive in every capable contrast. Do not retune the
source grouping or discard reversals. Treat this20document panel as opened now.

Split the reference MLP7-present city numerator group into two exactly defined
parts, with full native query factors and denominators fixed:
self = K1(M)*K2(M)*(V(M)+inherited), where M is weighted native MLP7;
mixed = present-self, including all terms involving M and another residual source.
MLP7 internal input cross terms remain inside M. This is city-side source algebra,
not an MLP7-input self/cross split. Assign tiny native-reference rounding residue
to mixed so self+mixed exactly reproduces the registered present write.

CPU preflight replays native block7 once on the saved residual6 batch (one40sequence
block call, no full forwards), reconstructs mixed8 and reader factors, and requires
full city write and MLP7-present write replay<=1e-4relative per sequence against
fresh captured references. All source pieces finite/support0 and exact sum closure.
Save three writes: present,self,mixed. No fitted coefficients or outcome-selected terms.

Next installed diagnostic: five arms native,present,self,mixed,self+mixed;
200sequence-equivalent full forwards/90block calls,2CPUthreads,180second cap.
Reference replay native/present and present-vs-joint abs<=1e-4 andrel<=1e-5;
callcounts/finite/support unchanged. No new random-null/selectivity promotion.

Opposing predictions, scored without changing them:
a. Instrument and algebraic replay pass.
b. Mixed removal accounts for >.5 of reference-present target-effect L2 norm
   in each of the four previously reversing documents4,8,11,19, and its paired
   attenuation is negative at all six endpoints in each of those documents.
c. Self removal attenuates at least90%of120capable contrasts overall.

If b/c fail, reject the proposed mixed-term explanation rather than rename the
components after seeing effects. Report source-present minus self minus mixed
behavioral interaction relative to the smaller single effect, separately on reversing
and other documents. This diagnoses head products versus native-suffix interactions;
no independent-composition claim follows from exact write closure or one small ratio.
