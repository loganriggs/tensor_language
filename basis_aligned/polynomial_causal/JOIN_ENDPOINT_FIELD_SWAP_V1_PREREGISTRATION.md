# Forward endpoint-field interchange v1

Backward origin-field interchange failed its fixed semantic bars. Its saved-output
diagnosis finds broad errors, with only two joint/single correctness differences
among64 hop3 queries. Do not tune that intervention. Test the already localized
forward endpoint producer instead: its original embedding term recovered
.969–.999 of task effect in the prior producer experiment. That evidence did not
establish semantic interchange or isolate the value-position term alone.

Fresh seeds23909/23910,16 IID24-cycle and16 OODtwo12-cycle worlds,8 query forks.
Use arrangement0 of the two-chain generator and swap complete binding pairs at
slots13/21, making both chains forward. Keep all maps and answers unchanged.
Both selected routes use L2H1, from earlier record S to later join record J.
The source value token is the composed endpoint a3 or b3. Freeze native L2 scores
and native source RMS gains. Exchange only E(source-value)/4 within each route's
V input; leave the key-position E, Y0/4, Y1/2 and all other paths unchanged.
Candidate forms every state from tokens and exported program weights.

For chain a0→a1→a2→a3, its join now carries b3 in the endpoint field, so querya0
hop3 should answerb3. Unlike the backward-origin swap, a single endpoint swap
should retarget its own query and preserve the other query. No removal of the
other join is needed to define its answer. Intermediate edited joins need not
be F² of any new token-level function.

Arms native, identity, swapA, swapB, bothSwap, cutA, cutB. Cuts remove complete
selected L2 route writes. No fit, donor selection, source/head/threshold sweep.
Opposing prediction: the field depends on contextual producer terms or receiver
encoding, so swapping bare endpoint embeddings will not reliably retarget.

A instrument: valid maps/answers, causal disjoint destinations, live swaps,
restored hooks; explicit embedding-path contraction vs independent native L2
pattern/value-input hooks; all-output max error<=1e-9, relative RMS<=1e-10 with
floor1e-6; identity<=1e-9 and finite. Native hop3 accuracy>=.8 and own complete
route cut goldP loss>=.25 for each query/population; no filtering.
B joint semantic retarget: bothSwap desired-answer accuracy>=.8 and desired
probability gain overnative>=.5 for each query/population.
C reuse/selectivity: each single swap meets the same bars on its ownquery/hop3,
and abs mean original-gold probability change<=.10 on the otherquery/hop3.
D lowerhop/composition: bothSwap abs mean original-gold probability change<=.10
for each query/hop0..2; unchanged-finalquery rawlogit joint effect minus summed
single effects max<=1e-9. Do not use nonlinear probability additivity as a bar.

All387968 independent constants and contextual background remain charged.
Passing identifies an editable endpoint interface, not a smaller semantic model.
Independent native checkpoint is an intervention oracle only; candidate uses
EXACT_SOURCE_EDIT_V1_PROGRAM.pt. B4FP64,1800s,<256MiB/tensor, managed GPU after
CPU controls and source binding. Preserve a valid null without adaptive repair.
