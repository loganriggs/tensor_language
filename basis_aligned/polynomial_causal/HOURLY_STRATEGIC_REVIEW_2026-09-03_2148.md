# Hourly strategic review — 2026-09-03 21:48 UTC

## Goal and success criteria

The goal is a smaller transparent tensor program for bilin18 whose parts have behavioral meanings. A proposed circuit
must improve evidence for: (1) a precise computation, (2) grouping across native modules and splitting within them,
(3) held-out and out-of-distribution prediction, (4) executable extraction or sufficiency, (5) selective removal or
editing, (6) reuse and composition, and (7) stability under data splits and internal changes of basis. Literal storage,
compute, edges, states, and program length are prices of an already meaningful computation. Rank reduction,
reconstruction, or low cross-entropy damage alone does not establish any of these circuit properties.

## What changed since 20:48

- R588 independently audited R584's FIT-only result. All twelve exact downstream-MLP response pieces are active, but
  none passes the full selective-successor gate. This is a real coarse-decomposition null, not a dead-intervention null.
- R585's first exact implementation review found six prospective execution flaws before any outcome was opened. The
  builder repaired independent remainder computation, the exact 20,736-operation census, realized bootstrap identity,
  deep held-evidence checks, finite-value checks, and staged receipt-last publication.
- Parent reproduced 92 acceptance passes, three strict tests of the obsolete bytes, all 16 live versions of the first
  blocker contracts, deterministic producer and managed dry runs, and clean static gates. Candidate commit
  `27e4beaaf` nevertheless remained unexecuted pending a fresh review.
- That fresh review blocked `27e4beaaf` with three new executable attacks. A 459-forward scientific null could omit all
  evidence; internally consistent invented endpoint/direction IDs could pass the held validator; and the managed
  adapter rejected partial output paths before the producer's crash recovery could quarantine them. Result, receipt,
  and evidence paths remain absent.
- The two-agent loop is therefore doing its intended job. The critic reviewed immutable Git blobs while the builder
  began a second prospective repair. The failures are being converted into exact tests rather than relaxed after an
  outcome.
- The three-hour mathematical review derived a block-term tensor view of the crossed score/value factors. It could
  group pieces across native head boundaries when its uniqueness conditions hold. The current sparse, structured data
  do not yet satisfy those conditions, and algebraic uniqueness would still require downstream causal tests.
- Separate late-MLP rank/program probes continue in the other research lane. They may price compact implementations,
  but they do not displace the causal circuit route because they have not yet grouped a behavior, split a mixed module,
  or shown selective manipulation.

## Is R585 still the highest-information experiment?

Yes, conditional on repairing the evidence boundary. At each of four fixed attention sites it crosses a recipient and
donor equality score with a recipient and donor projected value:

$$
t_h^{xx}=\sum_r e_h^x(r)u_h^x(r),\quad
t_h^{yx}=\sum_r e_h^y(r)u_h^x(r),\quad
t_h^{xy}=\sum_r e_h^x(r)u_h^y(r),\quad
t_h^{yy}=\sum_r e_h^y(r)u_h^y(r).
$$

These are replay, score-only, value-only, and joint substitutions. Selector edits and payload edits predict opposite
single-factor effects, while nonzero answer-preserving controls test broad damage. This can distinguish a distributed
selector-by-payload computation from generic activation importance. The latest blockers concern whether a null or
interrupted run is auditable; they do not weaken the scientific contrast, but they correctly prevent execution.

## Confound and failure audit

- **Authority membership:** internal hashes are insufficient. Endpoint rows, directed intervention rows, factor rows,
  and operation rows must equal the frozen authority set in exact order for the splits actually opened.
- **All completed terminals:** a held result and every completed scientific null need terminal/phase-appropriate raw
  evidence. FIT-only nulls have smaller exact arrays than FIT+SELECT holds; neither may use fixture-only summaries.
- **Hard-crash recovery:** an interrupted partial package must be quarantined non-destructively before the unused-path
  guard, while a complete prior result remains immutable and blocks rerun.
- **Factor meaning:** recipient and donor score/value factors remain frozen before intervention, semantic positions are
  token-derived, and the two layer-8 heads share one pre-modification state.
- **Multiple mediators:** joint effects are measured directly rather than inferred by adding separate patch effects.
- **Broadness:** active unrelated controls use logit-scale comparisons in the same units; intervention norm only proves
  that a control edit was nonzero.
- **Native-boundary bias:** even a held four-site result does not prove that attention heads are semantic atoms. Subset,
  downstream-equivalence, and block-term tests remain necessary.

## Alternative routes

1. **R585 score/value factorial:** retain as first priority; kill if the joint term fails transfer, the two single-factor
   arms do not separate selector from payload, or active controls move comparably.
2. **Pending-opener downstream-use split:** separate the broad projected value into pending-delimiter state versus
   generic punctuation/context effects. Kill finer work at any site lacking a full-state ceiling on both constructions.
3. **Numbered-list successor downstream-use split:** distinguish visible-label identity, successor action, and
   copy/arithmetic-conflict use. Kill a proposed successor piece if its active removal changes copy or +2 controls.
4. **Block-term factor recovery:** after valid factor evidence, construct the complete donor×recipient×readout tensor
   and test deterministic/generic identifiability plus held-out stability. Kill if no small stable cross-head blocks
   survive downstream interventions.
5. **Predictive-state causal quotient:** merge internal pieces only when registered downstream continuations cannot
   distinguish their interventions. Any separating continuation kills a merge.
6. **Rank/PCA/Fisher coordinates:** keep only as implementation-price baselines or candidate coordinates for a named
   circuit. Do not promote them from cross-entropy preservation alone.

## Ranked actions now

1. Finish the second CPU-only R585 repair against all three fresh attacks and rerun the full old and new test suites.
2. Commit exact new hashes, then require a second fresh immutable-byte review; do not reuse either blocked approval.
3. Only after an explicit APPROVE, enqueue the hash-pinned adapter through `ops/enqueue.sh`. Interpret FIT first and let
   the frozen terminal determine whether SELECT opens.
4. Promote authority-membership, phase-complete evidence, and reachable hard-crash recovery into the shared handoff
   contract and playbook.
5. Start the two distinct-circuit wave—pending opener and numbered-list successor—with those improved tools while the
   parent retains R585 as the deep reference.

The active action is step 1. No R585 scientific output exists, no gate has been loosened, and no rank result is being
substituted for circuit evidence.
