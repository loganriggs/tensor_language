# Hourly circuit review — 10 September 2026, 04:14 UTC

Interpretation targets: specify inputs, operations, writes and consumers;
group across modules and split within them; predict held-out/OOD behavior;
extract an executable circuit with explicit background; selectively remove,
swap or edit it; predict composition and reuse; and identify stable units.
The full goal also requires reduced structural description with all arbitrary
weights, state and adapters charged. No identified circuit or structural saving
was established in this hour.

## Evidence and throughput since03:14

Authoritative managed logs show:

| Receipt | Start | Exit | Executor time | Status |
|---|---|---|---:|---|
| Shared MLP4 products | 03:24:26 | 03:24:31 | 2.071s | Valid null |
| Semantic square V1 | 03:37:32 | 03:37:38 | 2.987s | Invalid numerical controls |
| Semantic square V2 | 03:42:18 | 03:42:24 | 3.544s | Valid null, unchanged candidate |
| Raw interaction origin | 03:56:44 | 03:56:50 | 2.289s | Valid null |

Three valid scientific receipts, not four circuits. Valid-receipt gaps are
17m53s and14m26s; median16m09.5s misses the10-minute operating target.
Claim-to-valid-result times are approximately4m31s (shared claim header corrected
from03:23toactual03:20),8m24s (square including repair),3m50s (raw origin).
Thus scientific implementation itself has stayed below10minutes, while the
between-screen derivations, interpretation and publication extend serial gaps.
GPU executor total10.891s; no GPU-capacity bottleneck. Exact attention-minute
splits between thinking, authoring, validation and publication were not logged.
Claude's v465/v467 used the same managed lane; they were not interrupted.

CIRCUIT_FOCUS: PASS with direction change — direct native source effects,
interchange instruments and exact support rules advance specification and
localization. The weak is/was mixed correction now rules out treating further
MLP4 interaction decomposition as the primary cue-circuit search.
CEREMONY_BUDGET: FAIL to establish for the whole hour — precise proportions are
unavailable and valid-receipt gaps remain too long. Numerical repair had a
concrete risk (FP64/FP32 operation and readout geometry), but repeating full
explanation/startup narratives after every basic null is not the experiment.
Repair now: reuse the existing batches/capture/scorer/managed preflight, write
one bounded first-attention token producer, and append its basic result to the
current explanation instead of authoring another long report/framework.
NOVELTY_LESSON_GATE: PASS — prior MLP4 operand factorial, five-MLP hidden
interfaces, shared first-value payload and separate lookup-model endpoint
program checked. No native-neuron semantics, variance/rank promotion, changed
thresholds, or unknown control families silently passed.

## Scientific decision

Shared128products beat random but miss70–82% of the full source effect.
Local semantic-square products miss60–85% of context correction. Raw
attention4-only and earlier-only corrections both fail, and the correction
itself is only5–7% of the is/was A2 full-vector cue effect. This branch cannot
currently deliver the desired main computation. Close it without another
anchor, normalization, subset, rank or gain rescue.

The new first-attention support rule is exact for token-local factors and the
actual no-softmax kernel. Single-cue change uses only edges touching the cue;
mixed change uses opposite-variable edges. It is an implementation/localization
consequence, not evidence of learned semantic importance. Use it first to test
the MAIN first-layer cue message, not to start another weak-interaction atlas.

Ranked next moves:
1. Native first-attention cue-carrier screen with independently token-derived
   factors/write differences, all9heads, existing72unfiltered evaluationpairs.
   Full native write swap must have>=.10 signed effect projection in both
   vocabulary and margin frames/allcells. Kill further local interpretation
   at this interface if the carrier fails. Exact execution alone is not enough.
2. If carrier passes, test selective source-edge manipulation and transfer to
   a separately registered fresh context family; do not infer these from algebra.
3. If carrier fails, use the broader circuit ledger to choose an already causal
   shared producer/reader interface. Demote more first-layer mixed-edge anatomy,
   MLP4 sparse supports, or generic coefficient-rank studies.

Confounds: baseline-subtracted vectors use the same centered vocabulary frame;
small correction denominators are tested for materiality; final-logit readout
geometry is controlled; native firstV payload must remain separate from the
attention0write; input/output precision is explicit. The data are opened,
not pristine OOD. Exact support does not identify meaningful edges, selective
controls are not present unless actually run, and all native weights remain.

Concrete action underway: first_attention_token_program.py is implemented and
its tiny controls executed. Independent factors and full writes replay exactly,
cue delta error4.86e-17, installed write5.56e-17; original receiving firstV and
hooks preserved. The next native protocol/runner use this producer. No new
forward backend or head/edge selection. Next clocks05:14hourly and04:49math.
