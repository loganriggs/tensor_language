# Upstream producer routing/value split

Frozen group attention8,9,13; original32regional rows,2families. No new fit.
Each producer's joint QK1*QK2 pattern is one port; the two-dimensional folded
current+shared-first value reading is a second port. Evaluate four vertices:
recipient both, donor routing, donor values, donor both. Sum the three producers
before inserting their numerator deltas into the fixed child tuple, retaining
recipient RMS17, parent, downstream routing/writers and native remainder.

A: each native projected producer, downstream attention, recipient block and
saved all-three-producer donor write replay<=1e-5 relative. B: A and fullgroup
transfer>=40%earlier wholechildtransfer and>=5/8positive EACHfamily. C: A/B and
value-only transfer>=90%fullgrouptransfer, unrelated meanabs<=.5regional EACHfamily.
Null: route or interaction dominates instead. This is a prospective role test
on previously inspected group rows; failure is preserved, not renamed success.
Report interaction=full-routing-values on the final nonlinear contrast as well.

Native rounded rotary and nativefloat32 QK projections/RMS are retained; contraction
and folded value maps evaluated FP64. BothQK scores move together, never separate
tasks assigned toQK1orQK2. Data annotates/intervenes on frozen weights only.
Cost7bodyforwards32rows lengths4–12,4suffix arms,3selected-producer routing
recomputations perbodyforward,180sec cap. Save ~1.5MBwrites/margins, no logits.
