# Hourly research review —12 September07:36 UTC

Targets remain explicit reads/operations/writes; cross-boundary grouping and within-module splitting; held-out/OOD prediction; extraction; selective removal; composition/reuse; stable identification. The full user goal is a simpler executable model/circuits with allfourbehavioral properties, not a compressed local fit. No circuit is promoted this hour.

Progress: matched weighted SVD repairs most but not all node approximation misses. A frozen ing-readout branch has lexical/construction transfer, a matched quote control, independently recurring whole function (cos0.998855), conditional joint-effect prediction and a compact executable local input fold (7.98MB). Natural FineWeb/Pile removal predictions and control/composition bars hold, but Pile native capability fails. Original unmatched quote and threecross-start write-bar misses remain. Document audit rules out one-outlier explanations. [Primary account](COMPOSED_WEIGHT_COMPARISON_V1_RESULTS.md).

The highest-information gap is now whether the input computation matters beyond the frozen ing-favoring output direction. Predominantly positive scalar coefficients can make many interventions appear selective on tokens used to characterize that writer. Token-list coherence and native loss averages do not identify the reader. Before expanding ranks, datasets or semantic labels, hold the writer and coefficient distribution fixed, shuffle the coefficient/context pairing, and compare both removal-selectivity statistics and replacement CE. Include within-spelling-group document shuffles so a mere class-level average cannot win by construction. This is validation, not data-guided fitting.

Opposing predictions: meaningful context coupling makes actual coefficients superior to mismatched coefficients on held natural contexts; an output-prior explanation retains the earlier selective-removal score under shuffling. A miss narrows the computational interpretation rather than denying the weight structure or extraction identity. The sample is24documents/corpus and spelling-based, so confidence and role claims remain limited.

Other next directions: independent-start natural intervention fidelity; trace the partner quadratic into attention16/residual source pairs; larger corpus validation. Rank these after the coefficient/context control because none resolves the output-direction confound as directly. Full input-state production and end-to-end OOD remain open. Do not change the dataset or capability gate to obtain promotion.

Throughput: several2–7second managed screens, one42second branch screen, CPU audits, and a two-minute storage interruption. Implementation and publication dominate elapsed time; the exact executor is a real research deliverable, but many near-duplicate wrappers remain costly. [Recorded phase accounting](HOURLY_PHASE_ACCOUNTING_2026-09-12_0736.json) has explicit attribution limitations and does not add overlapping GPU time.

- CIRCUIT_FOCUS: PASS. Identification, local extraction, selective intervention, corpus transfer and composition advanced.
- CEREMONY_BUDGET: FAIL. Repeated small wrappers/publication and imprecise phase labels remain. Next block uses only cached endpoints and one small tail scorer; no new framework or full-body collection.
- NOVELTY_LESSON_GATE: PASS. Prior spelling-pair marginal controls, native producer folds and output aliases checked; no generic SVD/dense-fold novelty claim.

Next hourly08:36, mathematical review08:00. User weights-first discovery and bilinear handoff remain authoritative over stale goal text. Execute the registered coefficient/context control next.
