# Hourly circuit review — 2026-09-09 21:58 UTC

Circuit targets: explicit inputs, operation and consumers; grouping across native
boundaries and splitting within them; held-out/OOD prediction; independent
extraction; selective removal/interchange; composition/reuse; stable identification.
The full goal additionally requires reduced structural description, counting
adapters and opaque weights. The bilinear reconstruction handoff's appended
criterion controls. This hour did not identify a structurally smaller circuit.

## Evidence and decision

Eleven scored receipts refine the four-layer lookup model's endpoint computation.
Local gate, physical field and grouped-path invariance failed. Overlapping joins
are not independent, and neither common RMS alone nor a key-only interface
predicts their full joint/removal effects. Direction changes and normalization
counteract each other. The previous-key producer O supports both query origins;
the proposed direct-value/previous-key two-address split fails.

An exact origin-key x endpoint-value interaction I has selective effects, but
its fixed nomination fails IID target loss .48058<.5. OOD .66782 and all control
groups pass; this is not permission to relax the bar. The exact complementary
endpoint read J shows backup:16/63 initially correct targets survive either
single-node knockout but fail the joint. These are expanded-read node edits,
not physical source removals, and J is not yet an advance-then-read algorithm.

Pure query-lineage assignments fail. Entity/task and entity/document mixed
terms both matter. A four-corner bound now rejects every additive root
bipartition for the forward-hop3 I/J groups: some binary root intervention
must incur7.8–9.4% RMS error relative to the fixed native route reference.
Across all query/hop groups,84/96 partition bars are ruled out; no reference
was floored. This bound is broader than one failed fitted candidate but is
restricted to this Q-port root interface. It does not reject a joint nonlinear
query operation or a different variable map.

Stop finer root-dominance probes. The next bounded action asks whether a
coordinate change can make the query computation independent squared channels
while preserving its norm and every output reader. The root Gram G and all
route forms A_k are3x3. For G>0, simultaneous congruence diagonalization is
equivalent to commutation after whitening; an exact dyadic obstruction is
A_i adj(G) A_j-A_j adj(G) A_i !=0. The relevant result is Theorem3.2/3.3 of
[Jiang and Li](https://arxiv.org/pdf/1507.05703). The registered first context
is fixed, all58 centered route/output forms are included, and any one exact
obstruction rejects that grammar even locally. Four planted/assumption controls
already pass in simultaneous_congruence_reference.py; trained forms are next.

This is an operational circuit-splitting decision, not a rank or variance
search. A positive local result would still require a common basis across
contexts, causal correspondence and literal pricing; no identification follows
from diagonalizing one context. A negative result stops that grammar without
adding channels or fitting a better context. Alternatives are further root/port
subsets (demoted by repeated failures), semantic swaps (existing origin and
global-label nulls remain binding), or a general coupled bilinear operator
(still allowed, but needs a concrete sharing claim beyond native rewriting).

## Confounds and scope

Gold probabilities are nonlinear: small single knockouts can coexist with a
large joint loss despite exact logit additivity. Lineage is conditional payload
transport, not an input Jacobian, raw token intervention, or proof of entity
identity. Native gates may encode the document in a query-token-root payload.
The new lower bound uses a fixed native-route denominator, not the earlier
per-intervention denominator. Its RHS is FP64 evaluated, not interval-certified;
the4e-9 contrast-slack check is numerical robustness only. All387968 native
export constants and opaque backgrounds remain charged. The current toy-model
results do not establish an18-layer bilin18 reduction.

## Throughput and coordination

Authoritative result mtimes:
21:01:09 cross-layer path;21:06:15 causal balance;21:12:46 overlap composition;
21:16:48 ports;21:22:52 key direction/gain;21:28:35 address producers;
21:34:34 interaction;21:36:51 route census;21:44:18 query lineage;
21:48:51 mixed inputs;21:52:39 additive bound.
Median interval5.41 minutes across all receipts; about5.98 minutes across the
eight native screens. Native CPU runtimes1.65–5.22 seconds, saved-row audits
.025–.062 seconds. No GPU queue wait or duplicate launch in this block.
Exact thinking/authoring/validation/publication shares were not instrumented.
Repeated orchestration and documentation still dominate computation. Reuse the
existing lineage, Q-port, interaction and scorer modules in the next audit;
do not build another framework or launch a mode/rank sweep.

CIRCUIT_FOCUS: PASS — inputs, cross-head interaction, selective-node tests,
backup behavior and constraints on legal grouping; structural goal outstanding.
CEREMONY_BUDGET: PASS on the observable sub10-minute screen-latency proxy after
the2058 helper/executor repair. This is not a measured cognitive-time fraction;
repeated runner boilerplate remains a limitation to reduce.
NOVELTY_LESSON_GATE: PASS — source/port/instruction/lineage nulls retained;
the next test changes coordinates and supplies a whole-class decision, without
post-selection or a rank-only success criterion.

Supervisor bqrunner/bqrunner2 are healthy. Concurrent Claude commits v441/v443
address construction-specific pooled coverage; the21:49 health commit records
a30-minute authoring block. The shared throughput directive still applies to
that work: use its receipts and remove repeated authoring before another long
bespoke block. Preserve its files and queue ownership. No scientific verdict
about those runs is inferred solely from commit messages.

Next hourly22:58, mathematical22:49 UTC. The present literature check does not
reset the three-hour clock. Concrete continuation: registered
QUERY_COMMON_SQUARED_CHANNELS_V1 and implemented exact congruence primitive;
compile and test the first fixed trained context next.
