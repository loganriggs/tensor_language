# Opened FineWeb interchange using the packed prefix

FineWeb removal failed directionality at88/102 capable probes; native and packed effects agree. Preserve this as a failed cross-corpus removal gate. Test the already supported complete-city interchange counterfactual on these now-opened20documents, using the physically packed eight-head prefix. This is not fresh confirmation or a rescue of removal.

Recipient Q1/Q2 remain from its own context. Replace city K1,K2 and complete current/inherited V with the paired donor context; subtract recipient city write. Full native MLP8/suffix recomputed. Same frozen weights and token tables as CITY_ATTENTION7_DROP3_FINEWEB_V1; no new selection, fit or changed norm formula.

19arms: native, exact-native swap, packed-generated swap,16per-position norm-matched random swaps with paired opposite signs. Seed18100000+1000*k+context_id. Capture40equivalents plus760installed:800sequence-equivalent forwards/360block calls,2CPUthreads,600second cap.

pred_a:40sequences/240probes;native query reference replay<=1e-4;native full-swap and self-removal factor replay<=1e-4;generator self-removal consistency<=1e-10;finite/support0;null norms<=1e-5;native score replay abs<=1e-4 ANDrel<=1e-5;CPU/counts. Candidate queries can change; compare native query reference using full nine-head weights.
pred_b:target-effect relative L2 error<=.05,targetRMS>=1e-5,>=90capable pairs(native margin>=.1).
pred_c:four unrelated control RMS movements each<=.5target.
pred_d:positive paired attenuation>=.90 and mean>=.02.
pred_e:targetRMS>=2random median and beats all16nulls.

Report document signs, each recipient direction, untouched/substituted errors, all failures. No selective-removal or independent composition claim follows from interchange. An opened pass requires fresh cross-corpus confirmation before promotion; a failure retains a conditional coupled operator and stops corpus-specific adjustment.
