# Strategic checkpoint — 2026-09-09 16:51 UTC

Circuit targets remain explicit read/operation/write/consumer specifications;
cross-module grouping and within-module splitting; held-out/OOD prediction;
executable extraction with a declared background; selective manipulation;
composition/reuse; and stable identification. The full objective is a simpler
predictive, composable, manipulable tensor program, including every opaque
coefficient and adapter. Task recovery or native path localization alone cannot
satisfy it. The bilinear reconstruction handoff overrides stale goal wording.

## Evidence since1550

Six managed research runs completed: middle matching, shared-kernelv1 (invalid),
coverage-repaired sharedkernel (valid rejection), two-join context interchange,
value producer factorial, previous-key origin path. Two CPU diagnostics separated
support/amplitude failure and score/value contextual dependence. An additional
CPU algebra fixture proves/controls a restriction on intervention interactions.
These are eight empirical receipts plus mathematical controls, not eight circuits.

Middle matching remains causal, but its whole-head local-record extrapolation
fails badly. One calibration-coverage repair is preserved separately from the
valid scientific rejection. No further feature/role/head/regularization rescue.

Two independently removable join writes coexist in the same context; own joint
replay and query reuse are exact. Their transfer across shuffled surrounding facts
fails full quantitative fidelity. Exact score/value factor diagnosis localizes
most contextual variation to transported payloads. Forward current-token E and
backward prefix-produced origin fields pass fresh task-effect criteria, yet their
fixed joint simplification misses the distribution/effect bars. The backward field
comes mainly from the second attention output. Its immediately preceding key→value
subpath recovers.883–.988 task effect on another fresh panel; full-distribution and
31–40% query-effect errors still reject quantitative sufficiency.

## Full-goal audit and change of direction

Computational specification, cross-layer producer/consumer linkage, selective
removal and same-context reuse advanced. Independently executing a substantially
simpler model with faithful OOD/joint outputs did not. All400640 native parameters
remain in the accepted native-reference programs. Physical361776-parameter kernel
exists only as a failed candidate. There is no adopted compact replacement.

The repeated pattern—high task recovery, failed causal vector fidelity—means we
should not keep narrowing source/head paths while treating those failures as
peripheral. Context and normalization are part of the computation. The next move
changes the grouping criterion: analyze dependency supports of edited normalized
source states, rather than identify a native head or one additive producer term
with an independently composable unit.

The1648 mathematical review maps the exact final-layer restriction to anchored
function decomposition/Boolean Mobius inversion. Disjoint post-L2 source supports
force degree<=2 in edit bits and no pair interactions at unchanged query positions.
Overlapping E/Y0/Y1 edits at one normalized state may have third-order interactions.
This is a precise composition guarantee under stated architecture assumptions;
it does not reduce native parameters or establish a semantic quotient by itself.
The next trained audit is preregistered and implemented in part; synthetic exact/
FP64 controls pass. Prefer this discriminating structural test over another fitted
lookup or source-sufficiency sweep. Semantic key-edit verification follows only
with a clear intervention interface, not another favorable accuracy metric.

## Confounds and alternatives

All native-normalizer and downstream states are recomputed where the registered
intervention changes them; compiled V-path edits explicitly retain the original
routing/gain and are not mislabeled as whole-layer ablations. Donor references are
charged and never called recipient-token execution. Context-shuffle and producer
factor diagnostics are marked reused/opened, not fresh confirmations. No row is
filtered by native correctness. Untrained-position outputs remain in all declared
full-fidelity gates. Intermediate norm and exact replay tolerances are unchanged.

Raw-logit composition and nonlinear KL/CE composition differ. The origin-path test
has identical raw query effect vectors with an independent route present/removed,
while its distribution errors differ. The new test explicitly checks this issue.
Correlated factor RMS ratios are not variance fractions. Native head boundaries
are neither semantic units nor guaranteed independent normalized states.

Alternatives ranked: (1) prove/audit interaction support and derive legal grouping;
(2) fresh explicit origin-field interchange with priced contextual inputs;
(3) revisit generic state reduction only if a new closure certificate exists.
Another rank, dictionary, absolute-position, head or regularization sweep is not
licensed by the current findings. Full-model application of the interaction law
is also not licensed: more suffix layers, source softmax or final normalization
can invalidate its low-order premise.

## Throughput and systems

Authoritative Git/board/runlog times: shared-kernel claim16:04→invalid16:15→repair
16:18→interpreted dossier16:22 (~18min including one instrument repair); context
claim16:25→run16:28→diagnosis/dossier16:34 (~9min); producer claim16:33→run16:37→
dossier16:43 (~10min); origin claim16:41→run16:43, interpretation now. These are
approximate serial claim-to-evidence bounds, not full cognitive-time measurements.
The shared-kernel block missed the10min target; ordinary subsequent blocks are
near it. Compute across the six GPU receipts was about17s, so GPU speed is not the
main latency. Scientific specification/implementation dominates; documentation,
provenance and the due mathematical review consume additional time. Focused CPU
controls take around1s percall; no broad test reruns or custom audit framework.
The next block reuses the native contribution executor, scorer and generators.

CIRCUIT_FOCUS: PASS — explicit fields, cross-layer paths, selective interventions,
reuse and grouping/composition evidence; no compression-only primary task.
CEREMONY_BUDGET: PASS with latency caveat — small shared controls, substantive
science/implementation dominate; kernel repair and repeated runner authoring are
visible overhead, not hidden as GPU time.
NOVELTY_LESSON_GATE: PASS — prior claims searched, fixed-kernel nulls preserved,
new object is interaction support rather than another fitted semantic feature.

Both managed runners remain healthy; another agent's queued runs and shared dirt
are preserved. No unmanaged GPU process. Hourly next after17:51; mathematical
next after19:48. Durable goal remains active; complete the trained structural
audit and interpret it before choosing the next reduction or semantic-edit step.
