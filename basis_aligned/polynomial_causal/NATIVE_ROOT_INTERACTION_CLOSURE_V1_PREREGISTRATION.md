# Native final-readout closure

Frozen opened two-site OODv2 quartet,64mixed cell-settings. Capture native final states for all17coordinates in fourcontexts. Evaluate R(x_s+x_t-x_0) with exact final RMS/unembedding/softcap. For baseline-minus-edit output convention, root generation is R(xs)+R(xt)-R(x0)-R(xadd), transported interaction is R(xadd)-R(xjoint). Their sum equals total interaction exactly.

Add measured root generation to the stored sum of14individual module-removal effects. Strong prediction: all64number interaction predictions within10%relative or1e-8absolute norm. This would support a finite additive response approximation on this opened panel, not an independently extracted program or literal joint-removal test. If it fails, omitted root alone does not explain the gap. Report original module-only and root-only controls. Modal errors remain separately recorded; no broad-selectivity claim.

Counts12prefix68suffix32additive readouts. Priorfloat64output replay<=1e-10, exact rootsplit<=1e-10. No fitting or new native derivative calls. Native state generators remain charged. Follow-on circuit track must broaden preservation controls beyond the three modal contrasts.
