# Hourly strategic review

ACTIVE_TRACK: CIRCUIT

Actual UTC: **2026-09-19T16:43:27Z**. Next deadline: **2026-09-19T17:43:27Z**. Previous review: [15:41 WEIGHT_FOLDING](HOURLY_STRATEGIC_REVIEW_2026-09-19_1541.md), 1h01m52s old. This is one live review, not an offline backfill. Historical `/workspace` references were resolved against this checkout. No GPU model, agent, job, queue, service, timer, commit, push, external contact, experiment/result, or `TYPED_FACE_EXTRACTION_V1` implementation was changed. The sole bounded CPU/process repair was the truthful 16:43 phase mark.

The previous review declared `WEIGHT_FOLDING`; this review therefore alternates to `CIRCUIT`. The folding handoff—complete residual6 -> attention7/MLP7 -> head8.2 -> norm-closed MLP8 -> head9.8 exact delta replay with all ordered source terms, all seven `QK1*QK2*V` products, and a deduplicated price—did not run and is preserved unchanged for the next folding hour.

## Seven circuit targets and full goal

1. **Computational specification:** identify what is read, the operation/composition, what is written, and every downstream user.
2. **Cross-boundary grouping and within-module splitting:** group pieces across native modules when one reader treats them as a variable, and split a native module when its pieces serve different computations.
3. **Held-out and OOD prediction:** predict activations and behavioral effects on unseen inputs, task variants, and shifted corpora.
4. **Extraction or sufficiency:** execute an isolated circuit at a declared boundary with every retained port and background named.
5. **Selective manipulation:** removal, swap, or edit changes the target against matched controls while preserving unrelated behavior and accounting for redundancy/interactions.
6. **Composition and reuse:** shared subcomputations serve multiple tasks/modules and their joint behavior follows a tested composition law.
7. **Stable identification:** units survive document/corpus splits, plausible gauges, and fitting restarts, or are defined by downstream operational equivalence.

Goal: a smaller transparent tensor program that **predicts held-out/OOD behavior, is extracted at a declared boundary, supports selective manipulation/removal, and composes/reuses**. Simplicity is the fifth, separately priced property: independently stored scalars/bytes, product nodes, readers/writers, ports/states, edges, peak state, compute, and runtime. Lower error, rank, storage, variance, or CE cannot substitute for a missing behavioral property.

| Property | Current evidence | Verdict |
|---|---|---|
| Held-out/OOD prediction | Regional intermediate extraction covers 20 distinct FineWeb document/context cells and Pile tests, but packed FineWeb direction is `88/102`; no frozen end-to-end regional formula passes structurally varied cells. | **PARTIAL** |
| Extraction at a declared boundary | Residual6-to-attention8 and norm-closed MLP8 packages replay with declared ports; complete head9.8 plus suffix remains open. | **PASS only at intermediate boundaries** |
| Selective manipulation/removal | Fresh regional edits beat same-site nulls, but the complete path lacks one frozen equal-norm, three-reader, matched-split, and full-logit battery. | **PARTIAL** |
| Composition/reuse | Regional key/value `interaction/smaller=2.3448`; additive lower bound `.5862`; matched random-split specificity fails. A closed Mobius face is accounting, not small or specific interaction evidence. | **FAIL overall** |
| Measured simplicity | Local package prices and exact full-vector rank floors exist; no deduplicated residual6-to-suffix price or matched-effect advantage exists. | **LOCAL ONLY** |

Claims remain separated as **fold/edit/response/fit** and **fresh/opened/replay**. Actual distinct `(template, city pair, endpoint)` context cells—not row counts—are the unit for transfer claims.

## Progress, failures, confounds, and alternatives

Since 15:41, no fold, circuit screen/null, causal run, scientific result, commit, or circuit/path/module dossier update appeared. The declared complete regional fold therefore made no progress. The only execution was routine runner health: canaries at 15:50:37–15:51:00 and 16:21:04–16:21:22. Both queue files are empty; the `bqrunner` and `bqrunner2` processes are live. The historical `/home/loganriggs/.../supervisord.conf` path is absent on this instance, consistent with the current-instance correction; no service action was attempted.

The last substantive receipt remains the 15:29 weight-only rank certificate: exact rank 128 for head9.8 through seven immediate block10 readers. It rules out a generic exact linear width `<128` at those tested boundaries, but says nothing about regional reachability, task use, extraction through the suffix, OOD prediction, selective causality, composition, or favorable price.

Confounds still capable of reversing a circuit verdict are baseline subtraction and frame mixing; nonlinear downstream loss composition; shared token difficulty; opened-panel/post-selection leakage; dead single arms; RMS/precision floors; treating shared residual inputs as independent ports; and treating a closed Mobius identity as small relative to the smallest live piece or specific against matched random splits. Full `QK1*QK2*V` interactions and forward responses must be retained.

Ranked alternatives:

1. **Frozen regional forward-response census (chosen):** one edited forward decomposed by downstream module/head establishes which suffix readers actually use the complete city delta before any suffix is proposed. Kill a candidate reader/group on unstable sign, negligible aligned effect, or failure to replay the full recursive response within the preregistered residual budget.
2. **Full-suffix five-arm factorial:** baseline, routing single, value single, product-suppressed both, and true joint directly separate the head product from suffix curvature. It is next, but only after the census names the suffix and backgrounds. Kill composition on dead singles, `interaction/smaller` above gate, or matched-split nonspecificity.
3. **Late-group fold toward tokens or a task-specific quotient:** potentially closes ports or evades the full-rank lower bound, but is lower information until an actual downstream regional reader is declared. Kill on fresh-cell prediction or matched-effect selective-control failure.

The regional route remains the highest-information path because it is the deepest exact extraction with distinct-cell fresh evidence and same-site causal controls. Breadth into another behavior would ignore the explicit depth-first authority and the open suffix/composition failure.

## Chosen CIRCUIT action

Run the **frozen forward-response census of the complete regional city edit**, from the declared head9.8 output-vector boundary through blocks10–17, final RMS, unembedding, and softcap, before naming a compact suffix. Use the existing corrected residual6/MLP8/head9.8 objects; do not change `TYPED_FACE_EXTRACTION_V1`.

Preregister the parent response and every per-module/per-head response with signed aligned fraction, change-norm ratio, replay residual, and target regional logit effect on outcome-blind structurally varied cells. Keep all seven head9.8 delta products and native RMS/background dependence. Record actual distinct cells. Include at least three unrelated readers and full logits/CE as collateral measurements, but reserve equal-norm directions and matched random splits for the immediately following factorial. A suffix is nominated only if its summed responses replay the full recursive response and its components have stable signs across cells; otherwise the honest result is a null and the fold remains an extracted intermediate path, not a circuit.

This action can change targets 1 (downstream use), 2 (cross-module grouping/splitting), 3 (frozen fresh prediction), 5 (localising an edit's effect), 6 (identifying where composition must be tested), and 7 (cell-stable reader identity). It does not by itself establish extraction of the suffix or independent composition.

**Concrete connection between tracks:** the folding hour's seven-product expansion supplies the exact product-suppressed head arm; this circuit census identifies the downstream readers allowed to consume those terms, permitting the next folding hour to omit causally unused terms rather than compress an arbitrary full module.

## Registry and dossier audit

The circuit registry, computation-path registry, `CIRCUIT_GRAPH_REGISTRY_V1.md`, MLP dossier index, and relevant regional/head9.8/MLP7/MLP8 dossier slices were checked. `PATH-REGIONAL-CITY-001` still ends at the head8.2 write even though later MLP8/head9.8 evidence exists. The graph registry inventories 49 packages but omits `city_mlp8_norm_closed_v1`, and its two `four_trait_verified` labels do not price the fifth simplicity property.

Highest-priority missing cross-link remains: connect `PATH-REGIONAL-CITY-001`, its regional circuit record, `city_mlp8_norm_closed_v1`, the complete seven-product head9.8 endpoint, the head9.8->block10 rank certificate, and the pending response/factorial, explicitly marking suffix prediction, selectivity, composition, and total price open. The computation-path registry is concurrent dirty state, so this review did not edit it.

## PAST_HOUR_TIMING

Window: **2026-09-19T15:41:35Z–16:43:27Z**. End-to-end serial candidate latency (`prior-art -> counterfactual specification -> native capability -> causal screen -> dossier/null`) is **unknown/not observed** because no candidate traversed the pipeline.

- **Design/scientific thinking:** unknown; no science phase boundary or receipt.
- **Implementation:** none observed; active duration unknown. No data build, indexing, shared-helper, runner, or refactor work appeared.
- **Validation:** no scientific validation.
- **Model execution:** no research GPU execution. Health canaries occupied 23s and 18s, **41 serial seconds total**; these are health costs, not evidence.
- **Interpretation:** unknown; no new scientific result.
- **Documentation/review:** this scheduled Codex process started at 16:41:59; completion time is not yet available, so duration is unknown. The 16:43:27 phase mark is a boundary, not labor duration.
- **Idle/blocked:** unknown. No time is inferred from absent receipts or commit gaps, and parallel durations are not summed as wall time.

The authoritative `RESEARCH_PHASES_2026-09-19.jsonl` contains sparse hourly-review boundary marks through 16:43 and no science, implementation, validation, or publication boundaries in this window. Coverage is inadequate for labor accounting; each mark is an event, not uninterrupted activity. No phase time was backfilled.

## PROCESS_IMPROVEMENT

Ranked by likely saving/cost: (1) add `started_utc`, `finished_utc`, evidence/evaluation labels, distinct-cell IDs, and literal-price fields to the existing receipt path—low cost, likely minutes saved per candidate/review; (2) add the canonical regional cross-link—low cost, likely 5–10 minutes saved per handoff; (3) consolidate sibling interventions behind the existing declarative machinery—largest cumulative saving, but medium collision and equivalence-test cost.

No shared helper, runner, schema, registry, or result mutation is justified in this bounded review: no candidate exposed a new repeated operation, the relevant registry is concurrently dirty, and changing shared schemas without equivalence tests would risk concurrent work. The sole safe repair was the truthful 16:43 phase mark; it improves boundary coverage but does not repair missing scientific instrumentation.

## Explicit verdicts and handoffs

- `TRACK_ALTERNATION: PASS` — previous `WEIGHT_FOLDING`; current `CIRCUIT`.
- `TRACK_PROGRESS: FAIL` — the declared complete regional fold produced no exact-replay/path receipt or honest falsifier during the prior hour.
- `CEREMONY_BUDGET: FAIL` — there was no scientific execution against which review/preflight could be smaller; 41 seconds of canaries are maintenance, not science.
- `NOVELTY_LESSON_GATE: FAIL` — no new candidate duplicated prior science, but the declared action did not apply the registered depth/response-before-suffix lesson, and the known regional cross-link remains missing.

**Chosen action:** run the frozen complete-city forward-response census and nominate no suffix until its per-reader responses close and transfer across distinct cells.

**WEIGHT_FOLDING handoff:** exact-replay and price residual6 -> attention7/MLP7 -> head8.2 -> norm-closed MLP8 -> complete head9.8, retaining every ordered source cross term and all seven `QK1*QK2*V` delta products.
