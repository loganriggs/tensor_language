# Does the backward join provide an address shared by both consumers?

The normalizer-only composition screen failed. Removing L2H2's backward join
also changes the forward query's gold probability by .176 IID/.351 OOD; these
are not independent selective units. Existing nonoverlapping source-port tests
nominate joint K1/K2 use for the backward write. Test that specific address
hypothesis in the overlapping setting, without selecting a better port subset.

Same opened32 worlds, two overlapping orders, two query origins, four hops,
512 requests from overlapping_join_normalizer_reference.populations. Reuse
source_port_interchange_reference exactly: derive all final K1/K2/V projections
from postL2 state with the backward write removed, then install each of the
eight subsets while leaving query projections and skip native. All seven
nonempty port combinations are diagnostic; K1+K2 (subset3) is the fixed address
candidate, V (subset4) its prespecified contrast. These are physical native
projection-hook interventions, not claims of a residual-space edit producing
the same hybrid. Full-port final query must match the physical write removal.

A: every subset agrees with independent native projection hooks abs<=1e-9,
relative<=1e-10. Empty subset equals native; full subset final query replays
the saved physical backward cut from OVERLAPPING_JOIN_NORMALIZER_V1, rows SHA
fb74b8938f81f3f000fbdc5be8e35d431632bfaaaeb2fd8d111d8311db0769e0.
Query metadata and reuse of source states across both consumers are checked.

B shared-address nomination: for every population and both hop3 query origins,
the signed gold-probability effect of K1+K2 divided by that of the full cut is
in [.9,1.1], while |V-only effect/full effect|<=.1. Denominator below .001
invalidates that nomination panel rather than dropping it. Report the complete
factorial and signed key/value interaction; probabilities do not add linearly.

C sufficiency is separate: subset3 must predict the full-cut centered query
effect to1% relative RMS, floor1e-6, in every population/query/hop group, and
its teacher query KL versus the full cut must have mean<=.001, p99<=.01.
A B-only pass is a task-level shared-address nomination, not a sufficient
extracted circuit. No alternative subset, head, hop, scale or fitted adapter
can replace a failed candidate. Both candidate outputs and native full outputs
remain saved. No structural savings from port attribution are claimed.

CPU threads2, batch8 FP64, alarm180s, no tensor>=256MiB, no GPU/model training.
Existing exact-source and port executors, cohort and scorer are reused. All
native387968 export coefficients plus native routing remain charged. This is
a downstream-use test for an already observed cross-query interaction, not a
rescue of invariant endpoints or shared-normalizer-only composition.
