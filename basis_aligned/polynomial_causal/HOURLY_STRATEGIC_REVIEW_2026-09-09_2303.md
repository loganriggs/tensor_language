# Hourly circuit review — 2026-09-09 23:03 UTC

Circuit interpretation targets: explicit inputs/operation/consumers; grouping
across native boundaries and splitting within modules; held-out/OOD prediction;
independent extraction; selective removals and interchanges; predictable
composition/reuse; stable identification. The full goal also requires a smaller
structural description charging adapters and opaque weights. The bilinear
reconstruction handoff's appended criterion controls. No new circuit meets
all these properties this hour.

## What changed and what to do

Seven scored receipts since21:58. Small-model exact coefficients reject common
squared channels, a proper common norm/reader block, and one shared linear
gate at the fixed query interface. These are narrow grammar exclusions, not
whole-model impossibility. We stopped enlarging that family and returned to
an actual shared producer in full bilin18.

Its fixed four-head contextual payload cannot be replaced by shared first
values: KLmean .0487/.0424. A factorial shows the shared and contextual
command effects do not oppose each other, despite negative mixture weights.
Neither dropping shared inputs alone nor adding the measured single-cut
effects meets the registered full-output bars. Independent contributions
fail; this does not imply nonlinear circuits cannot compose.

The exact four-cell response transform excludes a command-independent shared
contribution even with an arbitrary world/token-specific baseline: minimum
all-token relative error .374/.373. An exact integer token-incidence audit
then proves the shared token-only producer has zero mixed command mode at
all795 aligned positions. The removal response has a mixed mode, so the
interaction originates downstream. Additive-command responses have a7.2–7.3%
all-token error floor,13.7–15.4% at the later query. These are finite-domain
L2 statements, not KL or OOD guarantees.

The next highest-information action is native reader convolution. At each
fixed head/query, S11=P11U00+P01U10+P10U01, since U11=0. This separates a
joint-command router from crossed single-command routing/payload operations.
Its registered32forward screen changes computational specification and reuse
claims: test whether native shared reads already have material interaction,
and whether crossed terms predict it to1%. A fails on native/convolution
oracle mismatch; B fails if the typed pooled late-query interaction is below
1% of complete shared writes; C fails if crossed-only error exceeds1%.
Keep every fixed head/query and charge full native routing-state production.
Do not merge different-layer writes into a fictitious single intervention.
Eight planted algebra controls pass; native capture/scoring is next.

Alternative1: further global shared/context masks. Demoted: they cannot locate
the operation producing the interaction. Alternative2: fit an invariant or
additive response. Closed on this cohort by exact projection bounds; fitting
cannot beat those bounds. Alternative3: derive a complete new producer or
query representation directly from weights. Potentially more structural,
but no concrete reduced interface survives current evidence. First locate
the native interaction, then nominate a reduced operation with a falsifier.
Fresh/OOD interchange is reserved for a candidate passing this basic screen.
No variance or rank target substitutes for a circuit decision.

## Confounds and corrections

All32 worlds are opened; historical FIT/HOLDOUT labels are not fresh validation.
All native errors and50304 outputs remain, with centered-logit gauge fixed.
Teacher KL and response RMS answer different questions. A consumer-local cut
is not global producer deletion. Zero payload interaction does not prove
one native reader explains the final response. Source-incidence equality is
exact integer arithmetic; response bounds are FP64 numerical measurements.
Causal order supplies an exact future-bit zero at the earlier query.

One enqueue initially rejected two prediction keys; exposing the already
registered causal-order check as C met the three-key convention without
adding a scientific gate. The next primitive's planted crossed effect had
norm exactly1, failing a strict>1 omission tripwire; the planted amplitude
was doubled before trained execution and the threshold preserved. No trained
outcome, selected heads or scientific bars were changed. Method/hook restoration
and direct-head-zero controls pass in the completed intervention study.

## Throughput and shared work

Authoritative result mtimes:22:09:38 common squares,22:12:06 block bound,
22:19:21 shared gate,22:29:19 shared-only native screen,22:39:23 component
factorial,22:47:23 response modes,22:50:44 producer incidence. Median interval
about7.6 minutes across these seven receipts. Between the three full-model
screens the intervals are10.07 and7.99 minutes (median9.03). Actual GPU runtimes
3.228,3.238,1.765seconds; CPU algebra/incidence .004–1.061seconds. Authoring,
interpretation and publication dominate. The last native run waited behind
Claude v445 until22:47:18; this was shared queue coordination, not GPU failure.
Cognitive and review-time fractions are not instrumented, so the latency proxy
cannot establish their exact allocation. Reuse the current native forward,
command transform and scorer; add only scoped capture, not another framework.

CIRCUIT_FOCUS: PASS — producer/consumer dependency tests and explicit interaction
algebra; full structural discovery remains outstanding.
CEREMONY_BUDGET: PASS on the observable sub10-minute median screen-latency
proxy; actual cognitive fractions unknown. Keep controls and authoring shared.
NOVELTY_LESSON_GATE: PASS — failed simple query grammars closed, no head/rank
rescue; new study moves from global effects to a native routing operation.

Both managed runners are running. Claude's latest published v447 interpretation
withdraws v445's exclusive token-count explanation; preserve those files and
its followups. Its health commit records prolonged authoring, so the shared
one-screen-per10-minute target and reuse-before-bespoke-authoring rule still
apply; evaluate its own receipts, not queue occupancy or our median.

Continuation: board claim plus registered reader-convolution protocol and
implemented, controlled algebra primitive; integrate native capture next.
Three-hour mathematical2249 is complete with primary-source mappings and an
executed incidence consequence. Next clocks: hourly00:03 Sep10, math01:49 Sep10.
