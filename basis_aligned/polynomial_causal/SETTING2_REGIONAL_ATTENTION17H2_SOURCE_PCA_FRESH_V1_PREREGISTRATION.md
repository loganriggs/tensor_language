# Second-panel confirmation of the exported rank-eight source-PCA port

The competing-cues test rejected the response projector but its predeclared
rank-eight source-PCA comparator passed response, installation, removal, signs,
and every preservation control. Treat that as post-hoc discovery. Freeze the
already-exported source-PCA projector and test it without reselection on all 48
`REGIONAL_SOURCE_BLOCK_OOD_V1_ROWS.json` rows, which use shorter contexts,
unseen spelling endpoints, geographic cues, and templates relative to the
original regional source panels.

Evaluate source PCA, response rank 8, the same four random rank-8 projectors,
and the unprojected eight-edge program. Predictions: exact artifact/instrument;
source-PCA response replay; bidirectional causality; preservation; retained
source norm <=65%; minimax score at least 10% below the response projector; and
score plus `.10` no larger than the random median. No panel selection, gradients,
fits, gains, or parameter updates.

Price: 48 rows, 20 native/edited executions over 96 sequences, seven fixed
candidates, 672 candidate suffix sequences plus 96 complete-corner suffix
sequences, zero backwards, fits, or updates.
