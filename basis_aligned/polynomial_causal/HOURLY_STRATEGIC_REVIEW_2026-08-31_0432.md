# Hourly strategic review — 2026-08-31 04:32 UTC

Convention (§2135): L2 = CE added above the real model; LOWER is better.

## The hour: the invariant law earned its keep

- **§2188/§2189:** the scalar merge is assembler-specific (14/16: 19%; b4/b5: 7% — §2187's DC story predicted
  it), and at circuit grain it helps every one of the 62 circuits but repairs none (zero circuits worsened by
  the scaling; the DROP breaks ~77% — the merge is an aggregate trade). Circuit grain adopted as a standing
  16-second validation layer.
- **§2190:** the DC stand-in WINS — "attn3 = its mean vector" costs +0.0466 at 1,152 values, beating the
  scalar merge (+0.107) and the drop (+0.237); the a2-gain adds nothing on top. The pair's full rate curve
  (price vs damage) is measured at four points. Instrument note kept: the mean is estimator-dependent (~0.02);
  state the estimation population.
- **One frame void, cleanly caught:** rung 97's assembly-frame DC ledger was structurally inert for
  motif-replaced components (a8 zeroing at −0.008 was the tell; the tripwire aborted pre-scoring). Rebuilt in
  the census frame (97b, running) with an anti-inertness bar.
- **The invariant/covariant selection rule (math review 0437)** now has two substantive confirmations (scalar,
  mean vector) and seven covariant failures behind it.

## Strict ledger and gaps
Unchanged: 5.348% / 10.923% / 4.727 nat / 0 of 68. In flight: rung 97b (does DC dominance generalize — the
affine-skeleton decision), rung 99 (the frontier config's per-circuit abstraction certificate).

## Ranked next
1. Consume 97b/99 (§2191/§2192). If DC shares are high modelwide → the affine-skeleton rung (all-means floor +
   per-component μ pricing) becomes the front's canonical form; if the certificate shows retrieval-concentrated
   failures → the coverage ledger gets its per-circuit table and the registry syncs.
2. DC stand-ins at the OTHER cheap positions the rate curve suggests (a14? — its mean was §2187's meanA at
   +0.037 on skip-1416; a mean stand-in for attn14 on the best config could shrink the envelope's coverage
   retreat to a 1,152-value object). One registered rung after 97b.
3. Rung 90 (document-grain gauge instrument) — still the gate on all covariant compilation.
4. Consolidated explanation + registry sync at the chapter break.
5. attn5 cliff mechanism — parked.
