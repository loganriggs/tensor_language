# Physical three-write composition and random-split null V1

Object differs explicitly from the failed two-input-port additive hypothesis.
Retain all three actual head8.2 writes: routing delta times recipient mixed value,
recipient routing times inherited delta, and routing-delta times inherited delta.
No cross-term omission, coefficient fit, support selection, or retroactive rescue.
The prior1.737input-port interaction and43.4%additive lower bound remain true.

Compare all eight masks of the three physical writes through the same head9 odd
value descendant and native suffix. Random-split null: sixteen seeded orthogonal
128D head-space bases; partition the same total head-space write into43/43/42
coordinates and rotate back. Every three-piece split sums to the same total.
Seeds17092130..17092145, CPU float64 QR; no outcome access in split generation.
Native/full are common:104arms*8prefixes=832forwards,300seconds. The four
existing contexts are opened, not fresh confirmation. Six endpoints/four controls
share each final-logit vector. Equal total write, not equal per-piece norms.

pred_a: float64 synthetic component closure<=1e-12, native joint-write/state
closure<=1e-5; full/native readout anchor maxabs<=1e-5 and relF<=1e-6. Live
canonical singleton target RMS>=1e-5; zero effects invalidate ratios.
pred_b: unscaled sum of the three singleton behavioral target effects predicts
the joint within .10 relative L2 in both cues and every endpoint.
pred_c: every pairwise Mobius interaction <=.25 times the smaller singleton
L2 norm in both cues and every endpoint. Also report the full third-order term.
pred_d: canonical maximum normalized pairwise interaction across all registered
cells <=.5 times the median of the same statistic across sixteen random splits.
Null: apparent additivity is generic downstream near-linearity, not evidence
that this three-piece decomposition identifies special compositional units.

All predictions must pass for a positive decomposition-specific screen. Even a
pass is opened-panel evidence; fresh selective tests and simplicity comparison
remain necessary. A failed pred_d must not be described as a special composable
circuit merely because pred_b/c pass. No whole-path extraction or removal claim.
