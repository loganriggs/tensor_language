pred_a native/compiled/child write relative<=1e-5, partition<=1e-6.
pred_b native target mean>=.2 and >=10/12 positive; removal coverage>=.1,
>=10/12 positive and unrelated meanabs<=.5 regional EACH clause order.
pred_c native and component target response meanabs>=2*distractor EACH order.
Null: lexical regional priming without role selectivity. 6bodybatches48rows
lengths21-24,7suffixarms180seconds; frozen package, no fitting.


Two fixed clause orders cross the editor's city (Manchester/Seattle) with a tourist's city (Liverpool/Denver), for six previously used spelling concepts. These are new contexts, not new spelling tokens or unseen cities. Target effects average British-minus-American editor city with tourist city held fixed; distractor effects change tourist city with editor city fixed. Both have 12 comparisons per order. No model-based row selection. Native capability and role selectivity are measured separately, so a missing native ability cannot support an absent-circuit claim. The candidate and thresholds are frozen before scoring. A is numerical validity; B removal coverage and controls; C role selectivity. Preserve all misses.

Crossover: now the editor uses Liverpool/Denver and the tourist Manchester/Seattle. Thresholds are unchanged. Combine with prior48 to average each city pair over both roles. The original B/C misses remain. Report native and component target/distractor effects separately by order and city assignment; a twofold semantic role advantage requires the bar in each order on combined data, not selection of a favorable assignment.
