# Shared quadratic dictionary: optimization red-team

The common two-feature plane plus three named residual pairs uses six quadratic atoms per output. Previous sequential fitting failed finite response gates. We retained exactly the same plane, supports, native gradients, contexts and44-value/context cost, then changed only coefficient selection.

Joint coefficient least squares reduces its coefficient objective but still fails: maximum heldout number error13.39%, modal1.73%. Joint least squares under the fifteen positive single/pair amplitude design also reduces its own objective but fails all-arm number26.36%, modal2.11%. These are analytic-Hessian fits, without finite-outcome labels. Planted-span recovery and full15-coordinate recovery are below3.11e-15; dictionary condition2.46 and design-dictionary condition9.07. Thus elementary rank, conditioning, sign or projection errors do not explain these failures.

The next check asks whether the fixed dictionary lacks capacity at all. Allow arbitrary six coefficients independently for every context, keeping the exact analytic linear term. For each of32 construction/role/number/panel cells, minimize the maximum across18 measured arms of the number-effect residual norm divided by that arm's native number-effect norm. The eighteen arms are15 singles/pairs plus unit-B/full-null/half-null. This deliberately fits native outcomes and therefore supplies only an oracle feasibility diagnostic.

A weighted least-squares bound initially excludes no cell (largest lower bound5.409%). We then solve the convex minimax problem in SVD-whitened coordinates. Let E_a = U_a c - Y_a denote normalized residuals. The primal value is max_a ||E_a||_2. A dual witness Z satisfies U_b^T Z[:,b]=0 for each example b and sum_a ||Z[a,:]||_2<=1; its objective is -<Z,Y>. Nonnegative active-arm weights generate a candidate witness, then nullspace projection and normalization enforce dual feasibility independently of the optimizer success flag.

**All32 cells are feasible below10%; the worst optimum is7.492%.** Maximum primal-dual gap3.27e-8. This is a floating-point convex certificate, not an interval-arithmetic proof. It refutes the inference that the previous number failures establish inadequate fixed-dictionary capacity. It does not prove that analytic-derivative-only coefficient selection will work, that modal effects are preserved, or that the features transfer to fresh contexts. The native source and derivative generators remain necessary.

Primary receipts: SHARED_QUADRATIC_JOINT_FIT_V1_RESULT.json, SHARED_DICTIONARY_LOWER_BOUND_V1_RESULT.json and SHARED_DICTIONARY_MINIMAX_V1_RESULT.json. Executors use corresponding audit_shared_*.py files. Original shared-plane and sequential sparse-fit failures remain unchanged.

Next test: replace native finite outcomes in the minimax objective by the frozen full quadratic response and its predicted number-effect budgets. This selects coefficients from analytic derivatives only. Score against the already opened native outcomes afterward, including modal outputs. If it works, prospective validation is still required; if it fails, the gap between a locally quadratic target and finite native behavior remains material.
