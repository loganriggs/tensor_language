# Longer natural text contexts for the composed interaction executor

Use fixed natural pairs(0,1) and(16,17) from the existing confirmation panel.
Truncate each pair to its shorter prefix; target the actual next token from the
original prefix, or its original newline target when not truncated. Each of the
four prefixes receives the other pair member's routing/value ports. Donors are
different natural texts, not controlled semantic cue changes.

Four native capture passes plus4×25×2 native/compiled comparisons =204forwards,
180second cap. Same strength grid{-1,0,.5,1,2}² and mixed strength a*b.
A: post9/post10 states and full scores relative errors<=1e-5 per case.
B: baseline-subtracted next-token CE and newline-minus-comma margin discrepancies
<=1e-5+1e-4abs(native effect), every case; zero-strength effects exactly zero.
C: charged combined runtime payload<=independent payload per prefix. The price
forecast may predict failure; preserve it and do not choose shorter replacements.

Original weights, prefix/context generators and later native blocks remain.
No score filtering, fit, quantization, corpus independence or semantic circuit
claim. This tests a longer natural-context distribution; it does not imply
universal accuracy or static model compression even if individual gates pass.
