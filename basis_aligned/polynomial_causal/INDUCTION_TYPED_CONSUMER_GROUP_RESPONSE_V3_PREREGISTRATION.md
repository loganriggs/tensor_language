# Induction typed consumer group response V3 predicate correction

V2 correctly bound and executed 33 forwards / 1,056 sequences, but its instrument Boolean retained the parent singleton runner's literal `counts == [66, 2112]`. It therefore finalized invalid despite matching its corrected binding and output price exactly. No V2 candidate outcome is scientific evidence.

V3 changes only that instrument comparison to `counts == [33, 1056]`. It reuses the V2 price, the original 96 immutable rows and split, all eight architectural groups, intervention semantics, metrics, thresholds, selection rule, and null boundaries. No candidate result or ranking motivated the correction. Any other instrument failure remains terminal.
