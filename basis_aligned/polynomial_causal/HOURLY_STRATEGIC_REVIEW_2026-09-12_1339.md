# Hourly circuit review — 12 September 13:39 UTC

Targets: explicit computation; grouping across modules and splitting within them;
held-out/OOD prediction; extraction with declared inputs; selective removal;
composition/reuse; stable identification. The full goal remains a simpler
transparent executable model satisfying all four requested circuit properties.
Conditional prediction of one branch does not complete that goal.

The article correction is complete and shared validation now checks all affected
panels. Corrected selective first-value transfer persists. Short-prefix key
replay passes, but native-type update families and half-sized shared update
supports fail key/routing fidelity. Downstream composition filters much of that
error: three geographic families pass and one misses at17%. An exact mixed-QK
numerator/normalizer analysis predicts all four within1.2–3.1%, without fitting
a scalar. These remain conditional results using native correction information.

The stronger recursive extraction test fails73–103%write fidelity despite an
accurate all-update control. The selected updates depend on omitted producers;
their opaque native graph closure costs24–26updates and347–371million scalars
before remaining query/downstream computation. Whole-native-update pruning is
demoted as the independent extraction method. A norm-aware feature criterion
and native synthetic witnesses now reject the fixed four-feature all-real-input
interface, while preserving the possibility of text-domain or nonlinear closure.

Highest-information next action: implement and test a truly closed feature
executor with RMS and embedding re-entry, then require future learned programs
to use that interface rather than original hidden state. A planted model will
provide positive controls for multi-layer extraction and combined edits; it is
not a native circuit result. The input/writer common-subspace assumption must be
explicit, charged and tested before applying it to native weights.

Alternatives: continue native module subset search (demoted by recursive failure
and cost); add semantic panels (low information without closed generators);
fit arbitrary data-conditioned correction matrices (contrary to weights-first
instruction); or jointly search producer/readout feature structure with norm
constraints (next substantial native modeling route after the executor check).

Confounds: repeatedly inspected geographic panels, support selection using
original examples, finite prefix vocabulary, query/background dependencies,
numerator/normalizer cancellation, synthetic off-manifold counterexamples, and
unpriced native generators. No claim of fresh-corpus validation, global minimum,
absent structure or a discovered closed circuit follows from the latest results.

Nine relevant native receipts completed since12:36, generally1–4seconds each.
They are stages of one component, not nine identified circuits. The published
units span roughly6–8serial minutes each; most time was authoring, reasoning and
publication, not GPU computation. Seven Codex commits preserve those units.

- CIRCUIT_FOCUS: PASS. Tests directly distinguish conditional prediction from
  extraction and expose hidden dependencies. Pure reconstruction is not adoption.
- CEREMONY_BUDGET: FAIL / insufficient trustworthy phase attribution. Several
  science/implementation intervals retained a previous review/validation label;
  the [recorded audit](HOURLY_PHASE_ACCOUNTING_2026-09-12_1339.json) cannot measure
  their real allocation. No labels are backfilled. Repair immediately by marking
  implementation, validation and publication at their actual boundaries, using
  the existing helper, and keep the next known-answer test in one shared executor.
- NOVELTY_LESSON_GATE: PASS with a preflight lesson. Article validation and prior
  dossier checks were applied. Prefix-count overlap and literal result-key syntax
  were caught before native execution; freeze union counts and use the existing
  literal-key convention. No new queue framework or native rerun is needed.

The discarded1340 phase audit requested a future end time; use the1339 receipt
ending at the actual13:38:53 boundary. Preserve the former only as audit history.
Next hourly14:39UTC; mathematical review14:00UTC remains separately due.
