# Bracket cascade-pending live-amplitude confirmation V1

After the fixed six-scalar suffix law failed, a model-facing feature audit used only opened fifth/sixth rows and selected the lowest-complexity cross-transferring law: one global through-origin coefficient `0.0005561862682518956` multiplying the donor-free L13H8 source delta's direct donor-minus-recipient LM-head projection. The seventh construction was generated only after that selection artifact was committed.

`BRACKET_CASCADE_PENDING_OOD_V1_ROWS.json` contains 36 answer-changing target rows and 36 answer-preserving controls across twelve new lexical groups and a new cascade surface, balanced to twelve endpoints in each directed closer pair. Load the previously serialized rank-two key/payload source program and the selected scalar coefficient unchanged.

For each recipient, construct the donor-free source term from live recipient queries/payload and donor-type rank-two key/payload tables. Its native-suffix causal effect is compared with the exact donor-term ceiling. Independently predict the effect without executing the suffix as `0.0005561862682518956 * dot(delta_t, W_d-W_r)`. Compare this one-parameter prediction against both exact and donor-free program effects. No fit, coefficient/rank change, construction feature, intercept, donor state in the program arm, update, or quantization is allowed.

Price is four forwards over 144 endpoints: native, exact factor replay, exact donor-term ceiling, and donor-free program. This is 576 sequences, zero fits/backwards/updates.

- **A — instrument/capability:** replay error at most `1e-5`, program ranks all two, exact 4/576 accounting, and each target/control native cell has accuracy at least `0.75` with positive mean margin.
- **B — exact parent:** exact effects are positive on at least `0.90` of each pair.
- **C — donor-free source:** program versus exact effects has cosine at least `0.90`, relative L2 at most `0.40`, sign at least `0.90`, norm ratio `[0.60,1.40]`; every pair has cosine at least `0.75`, relative L2 at most `0.50`, sign at least `0.85`.
- **D — suffix-free prediction versus exact:** the same overall and pair bars.
- **E — suffix-free prediction versus program:** the same overall and pair bars.
- **F — controls:** program control RMS at most `0.50` of exact target RMS.

All predicates passing yields `live_amplitude_behavior_confirmed`. If the source program holds but D or E fails, the one-parameter suffix law is a prospective null and must not be refit on these rows.
