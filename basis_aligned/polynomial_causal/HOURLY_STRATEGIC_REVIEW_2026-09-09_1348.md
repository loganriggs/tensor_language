# Restart strategic review — 2026-09-09 13:48 UTC

Circuit interpretation targets: explicit inputs/operation/consumers; cross-head or
cross-module grouping and within-module splitting where justified; held-out/OOD
prediction; executable extraction with accounted background; characterized removal;
joint composition/reuse; and identification stable under appropriate input changes.
The full goal remains a simpler predictive, composable, manipulable tensor program.

The last hourly review was 03:40. Codex was inactive after the 04:12 pilot commit;
these are not eight hours of performed reviews. The previous status-only turn made
no research progress. The goal is now explicitly active. Next active-work checkpoint
is at the first safe boundary after 14:48 UTC, mathematical after 16:48 UTC.

The completed reconstruction pilot established full numerical execution but no
nontrivial sharing: local contextual bank rank128/128 and all screened product factors
distinct. Do not resume the same local span or duplicate-factor search. The user
clarified the handoff, not the empty better_math_ideas.md, is the authority.

Chosen successor: a fixed nonlinear equality/type rule for the first-layer joint
QK1*QK2 router on the same existing small checkpoint. Its first-layer inputs are
fully generated from tokens, so no native activation cache hides the mechanism.
Four independent head coefficients consume one equality/type predicate; all value
and later contextual operations remain explicit and charged. A full table is only
a baseline. The 37-case restriction is justified by a specified entity-renaming
group, not selected because rank37 fits well. No trained-model outcome opened yet.

This changes the circuit decision: do arbitrary entity-specific routes matter to
the full prediction and to single/joint removals, or does an explicit shared rule
suffice? KL over all29 outputs prevents answer-minus-foil recovery from standing in
for extraction. Function-topology OOD and renaming controls distinguish distribution
transfer from a within-distribution symmetry. Native and compiled edge removals have
the same semantic map, including interactions and redundant effects.

Alternatives: joint product decomposition of contextual MLP reader banks remains
plausible but needs a more specific operation grammar; large-language-model grammar
extraction has the highest direct relevance but currently lacks recursive dependency
closure. This inexpensive fixed-hypothesis test uses established small-model tooling
to reject or license a concrete reusable operation before larger execution. It cannot
satisfy the full goal by itself. If it fails, inspect the counterexample and change
the computational boundary, not thresholds or the evaluation population.

Confounds: normalizer values must remain live; rotary FP32 table reassociation needs
an unreduced numerical control; the four heads must not be conflated because they
share values; head0/head1 are fixed before results; native task correctness cannot
filter examples; the changed function topology does not change vocabulary or context
length. All239 input positions and48 query positions are scored separately. The
remaining native parameters and35520 learned positional/orbit coefficients all count.

CIRCUIT_FOCUS: PASS — new explicit routing hypothesis and causal full-output test.
CEREMONY_BUDGET: PASS at restart — reused loader, model, executor design and shared
preflight; no new audit framework or broad parameter search. Record actual runtime.
NOVELTY_LESSON_GATE: PASS — searched equality/permutation/routing prior work, read the
earlier Z2 gauge proposal and the hop mechanism retractions. This tests S24 token
renaming in the first-layer joint kernel, not the old hidden-coordinate sign gauge.

Concrete continuation: board claim, preregistration, executable orbit projector and
eight passing CPU control checks. Managed GPU full-output runner is implemented and
being gated. Claude's frame-capacity experiments and all unrelated files remain owned
by their lane. No candidate is promoted by this review.
