# Hourly strategic review

ACTIVE_TRACK: CIRCUIT

Actual UTC: **2026-09-19T18:47:12Z**. Next deadline: **2026-09-19T19:47:12Z**. Previous review: [17:45 WEIGHT_FOLDING](HOURLY_STRATEGIC_REVIEW_2026-09-19_1745.md), 1h01m43s old. This is one live review, not an offline backfill. Historical `/workspace` paths were resolved against this checkout. No GPU model, agent, job, queue, service, timer, commit, push, external contact, experiment/result, existing registry/dossier, or `TYPED_FACE_EXTRACTION_V1` implementation was changed. The sole bounded CPU/process repair was the truthful 18:47 phase mark.

The previous review declared `WEIGHT_FOLDING`; this review therefore alternates to `CIRCUIT`. Its declared complete residual6-to-head9.8 replay did not land, but the 18:33 opened replay census is a useful on-track partial receipt: collectively omitting the four order-at-least-two `QK1*QK2*V` terms costs **0.2853** relative local-head L2, and their norm is **1.8837x** the smallest target single across 20 actual context cells. The folding handoff is preserved unchanged for its next hour.

## Seven circuit targets and full goal

1. **Computational specification:** identify what is read, the operation/composition, what is written, and every downstream user.
2. **Cross-boundary grouping and within-module splitting:** group pieces across native modules when one reader treats them as one variable, and split a native module when its pieces serve different computations.
3. **Held-out and OOD prediction:** predict activations and behavioral effects on unseen inputs, task variants, and shifted corpora.
4. **Extraction or sufficiency:** execute an isolated circuit at a declared boundary with every retained port and background named.
5. **Selective manipulation:** removal, swap, or edit changes the target against matched controls while preserving unrelated behavior and accounting for redundancy/interactions.
6. **Composition and reuse:** shared subcomputations serve multiple tasks/modules and their joint behavior follows a tested composition law.
7. **Stable identification:** units survive document/corpus splits, plausible gauges, and fitting restarts, or are defined by downstream operational equivalence.

Goal: a smaller transparent tensor program that **predicts held-out/OOD behavior, is extracted at a declared boundary, supports selective manipulation/removal, and composes/reuses**. Simplicity is the fifth, separately priced property: independently stored scalars/bytes, products, readers/writers, ports/states, edges, peak state, compute, and measured runtime. Lower error, storage, rank, variance, or CE cannot replace a missing behavioral property.

| Property | Current evidence | Verdict |
|---|---|---|
| Held-out/OOD prediction | Regional intermediate extraction spans 20 fresh FineWeb context cells and scoped Pile tests, but packed FineWeb direction remains `88/102`; the new 20-cell census is opened replay. | **PARTIAL** |
| Extraction at a declared boundary | Residual6-to-attention8 and norm-closed MLP8 packages replay with declared ports; complete head9.8 plus suffix is open. | **PASS only at intermediate boundaries** |
| Selective manipulation/removal | Scoped fresh edits beat same-site nulls; the complete path lacks one frozen equal-norm, three-reader, matched-split, full-logit battery. | **PARTIAL** |
| Composition/reuse | Regional key/value `interaction/smaller=2.3448`; the new local omission error is `.2853`; matched/source splits fail specificity. A closed Mobius face is accounting, not smallness or specificity. | **FAIL overall** |
| Measured simplicity | Local prices and restricted rank floors exist; no deduplicated residual6-to-suffix bill or matched-effect advantage exists. | **LOCAL ONLY** |

Claims remain separated as **fold/edit/response/fit** and **fresh/opened/replay**. Transfer units are distinct `(template, city pair, endpoint)` context cells, not row counts.

## Progress, failures, confounds, and alternatives

Since 17:45, the only scientific receipt is [`HEAD9H8_SEVEN_TERM_INTERACTION_CENSUS_2026-09-19_1833.json`](HEAD9H8_SEVEN_TERM_INTERACTION_CENSUS_2026-09-19_1833.json): CPU-only replay of stored float64 contributions, no model load or forward, 0.0147 seconds. Pair-plus-triple omission exceeds 10% in 16/20 cells (median `.3002`, range `.0263-.9176`); 19/20 cells exceed `.35` interaction/smallest-single, though tiny denominators make that ratio unstable. The triple alone is only `.2023` of the smallest single, so dropping all higher-order terms from the triple alone would have been wrong. This is **fold/opened-replay**, not fresh prediction, edit, response, matched-null specificity, suffix fidelity, or adoption. No complete regional replay, forward-response census, causal screen/null, commit, or registry/dossier extension appeared.

Likely verdict-changing confounds remain baseline subtraction and frame mixing; live RMS and other native backgrounds; nonlinear suffix/loss composition; shared token difficulty; opened-panel/post-selection leakage; dead single arms and tiny smallest-piece denominators; precision/noise floors; shared residual inputs masquerading as independent ports; and semantic claims drawn from a closed Mobius identity without matched random splits. All seven `QK1*QK2*V` terms, both ordered attention/MLP cross terms, forward responses, actual distinct cells, full logits/CE, and unrelated readers must remain explicit.

Ranked alternatives: (1) **chosen:** the already frozen complete-city forward-response census through blocks10-17/final readout, killed as a useful localization instrument if signs/readers are unstable across distinct cells or its module responses fail to replay the recursive delta; (2) the subsequent five-arm baseline/routing/value/product-suppressed/true-joint factorial, killed as a reusable grouping by smallest-piece interaction or collateral no better than matched splits/equal-norm directions; (3) template-boundary controls before deeper source attribution, promoted if response location moves with colon/quote structure; (4) a new behavior or fitted suffix, demoted because it abandons the deepest path or introduces an unclosed fitted port. The regional route survives because it has the deepest declared extraction, actual distinct-cell evidence, scoped causal tests, and a newly measured nonadditivity that directly determines the next intervention.

## Chosen CIRCUIT action and cross-track connection

Run **one frozen complete-city forward-response census** before naming any suffix component. Endpoint: the installed complete head9.8 seven-term write delta through native blocks10-17, final RMS, untied unembedding, and softcap. Inputs retain the declared regional residual6/token boundary, norm-closed MLP8 context, all seven head products, live normalizers, causal mask/rotary phases, learned mixing/re-entry, biases, and unchanged native background. Evaluate structurally varied outcome-blind cells and report signed regional effect, per-module/head response vectors, full logits/CE, and at least three unrelated readers, with actual distinct-cell counts. Exact check: summed downstream module responses plus residual route must replay the recursively recomputed final delta within a frozen numerical gate and preserve zero/off-scope bookkeeping. Stop rather than propose a suffix if response signs or users are unstable.

This directly advances targets 1, 2, 3, and 7 by identifying downstream users and possible cross-module groupings; it only nominates later tests for extraction, selectivity, and composition. The follow-up five-arm experiment, not this census, must supply fresh matched-null causal evidence. **Track connection:** the folded seven-term census falsifies an additive three-single representation locally, while the circuit response census determines which downstream reader can use those exact terms; only that reader-specific result can license a later fold, grouping, or omission.

## Registry and dossier audit

`COMPUTATION_PATH_REGISTRY.md` still ends `PATH-REGIONAL-CITY-001` at head8.2 despite the later norm-closed MLP8 and complete head9.8 evidence. `CIRCUIT_REGISTRY.md` and `MODULE_DOSSIERS.md` contain the principal regional head8.2/head9.8/MLP7/MLP8 receipts but do not cross-link the current full endpoint or the two rank certificates by the canonical path ID. `CIRCUIT_GRAPH_REGISTRY_V1.md` inventories 49 packages but omits `city_mlp8_norm_closed_v1`, and its two `four_trait_verified` labels do not price the fifth simplicity property. The MLP index links the MLP7 regional addendum and generic MLP8 dossier, but not one canonical residual6-to-head9.8-to-suffix record.

Highest-priority missing cross-link: one canonical extension joining `PATH-REGIONAL-CITY-001`, its regional circuit record, MLP7/MLP8 and head8.2/head9.8 dossier entries, `city_mlp8_norm_closed_v1`, the seven-term census, both rank certificates, and the pending response/factorial, with evidence type, evaluation status, distinct-cell manifest, open ports, and deduplicated price. The path registry is concurrent dirty state, so this review did not mutate it.

## PAST_HOUR_TIMING

Window: **2026-09-19T17:45:30Z-18:47:12Z**. End-to-end serial candidate latency (`prior art -> counterfactual -> native capability -> causal screen -> dossier/null`) is **unknown/not completed**: the local fold census is not the declared complete candidate pipeline.

- **Design/scientific thinking:** unknown; no phase boundaries.
- **Implementation/data building/indexing:** active duration unknown. The new script's filesystem mtime is 18:33:29; no authoritative start mark exists.
- **Validation:** duration unknown. Shape/hash assertions, independent recomputation, and AST validation passed, but have no separate phase timestamps.
- **Model execution:** no research model execution. The CPU replay ran from 18:33:33.739 to 18:33:33.754 (**0.0147s**). Health canaries ran 17:52:16-17:52:38 and 18:22:43-18:23:00 (**39 serial seconds total**); they are maintenance, not evidence.
- **Interpretation:** unknown; no separate boundary.
- **Documentation/review:** the 17:45 review reached disk at 17:47:12 and the 18:34 mathematical review at 18:35:34; those are boundary-to-file elapsed spans, not proven uninterrupted labor. This review remains in progress.
- **Idle/blocked:** unknown. Commit gaps and absent receipts are not converted into idle or active time; parallel durations are not summed.

The actual-date `RESEARCH_PHASES_2026-09-19.jsonl` contains sparse hourly marks through 18:47 but no science/implementation/validation/publication marks in this window. Coverage is inadequate for labor allocation; marks are events, not continuous intervals. The last repository commit remains `9e0c03df3` at 12:21:10 UTC. Both queues were empty; the configured local `supervisorctl` binary named in the recovery note is absent, so runner liveness was not inferred from that failed control call. Recent runner logs show successful canaries through 18:23.

## PROCESS_IMPROVEMENT

Ranked by likely saving/cost: (1) canonical regional path cross-link plus uniform evidence/evaluation, cell, timing, port, and price fields—low implementation cost, likely 5-10 minutes saved per handoff; (2) consolidate sibling intervention runners behind the existing declarative executor—larger cumulative saving, medium collision and equivalence-test cost; (3) add science/implementation/validation marks to existing runners—small code cost but only useful if consistently invoked.

No shared registry/helper/runner mutation is safe in this bounded review: the path registry and runner state are concurrently dirty, and retrofitting heterogeneous receipts requires equivalence tests. The one bounded repair was the 18:47 phase mark, which improves boundary truthfulness without pretending to measure prior work.

## Explicit verdicts and handoffs

- `TRACK_ALTERNATION: PASS` — previous `WEIGHT_FOLDING`; current `CIRCUIT`.
- `TRACK_PROGRESS: FAIL` — the 18:33 seven-term census is a substantive on-track partial fold receipt, but the declared complete residual6-to-head9.8 replay and deduplicated price did not land.
- `CEREMONY_BUDGET: FAIL` — measured scientific execution was 0.0147s while review artifacts were materially larger; unknown design/implementation time cannot establish the opposite.
- `NOVELTY_LESSON_GATE: PASS` — the census reused the registered artifact, counted real distinct cells, retained all seven products, distinguished replay from fresh causal evidence, and produced a new falsifier rather than another rank/reconstruction sweep. The canonical cross-link remains debt.

**Highest-value action:** run the frozen complete-city seven-term forward-response census through the native suffix on structurally varied distinct cells; identify users before proposing a suffix or launching the matched-null factorial.

**WEIGHT_FOLDING handoff:** exact-replay and deduplicately price `residual6 -> attention7/MLP7 -> head8.2 -> norm-closed MLP8 -> complete head9.8`, retaining every reader-permitted self/ordered-cross term and all seven products; the 18:33 local interaction census does not substitute for this replay.
