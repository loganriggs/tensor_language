# Source-token census of the explicit head9.8 cross term

Frozen 15 September 2026 before source-level values are inspected. Reuse the
opened 48-row composition panel and its fixed late-touching QK1 group and
half-strength head8.2-induced current-value change. At the final query, decompose

$$
-R\Delta v=\sum_{k\in\mathrm{framing}}-R_{qk}\Delta v_k.
$$

Report exact description/instruction sums and rank relative-to-quote source
offsets by summed head-vector squared norm within each template family. Select
the first four offsets in that outcome-blind vector-norm ranking; this is a
compression discovery on opened rows, not a causal or fresh confirmation.

- `pred_a_exact_source_partition`: source vectors sum to the explicit cross term,
  and carry, attention, midpoint, and joint audits close within
  $2\times10^{-6}$ over exactly six native batches.
- `pred_b_top4_offsets_compress`: the four selected offsets replay the complete
  cross-term vectors with relative error at most `.35` in each family.
- `pred_c_cross_family_offset_transfer`: offsets selected in either family replay
  the other family's cross vectors with relative error at most `.45`.

Also report projection through the native head9.8 output weight and local UK/US
unembedding reader. There are no fits, behavior-guided choices, gradients,
updates, recursive subset interventions, or quantization.
