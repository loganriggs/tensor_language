# Hourly circuit review — 2026-09-09 15:50 UTC

Circuit interpretation targets: explicit inputs/operation/consumers; grouping and
splitting across native module boundaries; fresh/OOD prediction; executable extraction
with declared background; selective removal/interchange; joint composition and reuse;
stable identification. The full goal remains a simpler predictive, composable and
manipulable tensor program under the reconstruction handoff's appended criterion.

Eight research receipts since1449: upstream producer atlas; MLP token/context terms;
stronger-model cold capability audit; cold final-source test; opened-panel order
diagnostic; counterbalanced suffix-source test; upstream writer test; head/port
factorial. An additional integer reference verifies1296 fact-subset/order cases.
These are not eight completed circuits. The middle-entity matching test is now
implemented and queued; its outcomes have not been used to write this review.

The weak checkpoint's simplification branch is closed. Its shared MLP dependency
does not reduce to the mixed token/context term; full fidelity requires the other
terms too. A stronger existing four-attention checkpoint, with the same400640
parameters, passes fresh cold multi-hop capability without any earlier answers.
The larger cold panel lowers the initial hop3 estimate to .906/.813 IID/OOD,
so the initial16-example100% result is retained with its sample-size limit.

On that checkpoint, direct final-answer-binding sufficiency fails. Binding order
instead nominates a two-edge composition at the later of B2/B3. A fresh6-order
counterbalance confirms its causal source: all12 order/topology groups have J
removal loss .856–.978, other-chain absolute loss<=.019.32 fixed-point controls all
fail, mean goldP.00082, as the proposed distinct-edge join predicts. This supports
specific failure prediction, not arbitrary unseen-input full-distribution closure.

The corresponding explicit candidate operation is J_i=E_i C_prev+C_prev E_i,
with C accumulating earlier facts. Sum J_i=F²-sum E_i². It completes both joining
orientations at the later source and excludes same-edge self-composition. The exact
reference distinguishes original-fact deletion, which recomputes later writes,
from deletion of one completed source write. This is a proposed native algorithm,
not an inference from rank or the mere existence of an algebraic identity.

Causal writer tests localize the S→J computation to prefix layer2. Head/port
factorial then passes all4 predictions: L2H1 is the forward-value writer, L2H2
the primary backward-key writer. The smaller H3 backward contribution remains
reported (.107–.201 loss); it is not dropped from a purported two-head model.
Forward V-only rescue≈1, KK-only≈0; backward KK-only≈1, V-only≈0. Both K1/K2
are needed for the strong backward rescue. All-port final-query replay is exact,
using native source ports reused across queryhops from the same world's hop0.

Status against the goal:

- Operation/consumers: a concrete two-edge join hypothesis, directional native
  writers, and final value versus two-factor key consumers. Middle matching itself
  is the next causal test, not assumed from port names.
- OOD: source and writer/port predictions transfer to two12-cycle worlds and
  counterbalanced orders; fixed-point failure is predicted. Scope remains this
  small permutation task, not full bilin18 or unrestricted function graphs.
- Extraction: exact source decomposition and native-reference port rescue exist.
  These keep opaque prefix/routers and do not constitute an independently compiled
  semantic join. The old generic final-matrix fold is not new semantic savings.
- Removal/composition: targeted edges show large hop3 effects and small matched/
  lower-hop controls; complete source-port restoration gives exact query logits.
  Logits at altered source positions are outside that restoration claim.
- Reuse/identification: source ports are causally query-independent and reusable;
  the two orientations converge on a composed key/value interface. Fresh data,
  counterbalanced order and interventions replace old attention/probe stories.
- Structural reduction: unfinished. All400640 weights remain in writer tests;
  a smaller native replacement with counted adapters/background is still required.

Critical fidelity correction: keeping J+query gives100% task accuracy on384
positive order variants but fails native-distribution KL(.0043–.526). It deletes
native errors/background too. Likewise joint-prefix-cut port rescue recovers≈100%
behavior but fails distribution tails. Only the later L2-only all-port intervention
has exact query closure. Do not substitute task improvement for predicting the model.

Next moves ranked: (1) finish the valid-permutation middle-entity intervention now
queued, testing the complete joint score and its full-query causal effect; (2) if
that passes, derive/implement a joint read-route-write replacement rather than another
localization atlas; (3) transport the operation/method to bilin18 only after the
small-model writer/input interface is clear. More rank, token-state vocabularies,
head thresholds or linear probes are lower-information choices at this point.
If matching score and causal effect diverge, keep their value/context dependency
explicit rather than declaring a standalone equality detector.

Confounds audited: no per-example capability filtering; world/order/hop/case forks
are correlated and independent-world counts are separate; native RMS gains and
absolute RoPE remain live; negative-control lag differs by2 tokens and is disclosed;
new middle-label edits preserve permutation validity but change four occurrences,
not just one local token; source activations in rescue are labeled intervention
references, not an extracted runtime cache. Predicting self-loop failure does not
erase it from OOD reporting.

CIRCUIT_FOCUS: PASS — clear movement from capability to causal operation, writer
and consumer interface. Next work must move toward replacement, not endless tracing.
CEREMONY_BUDGET: cadence≈8 receipts/hour; authoring/reasoning and shared-queue waits
dominate seconds-long small-model runs. No precise overhead fractions were logged.
Shared controls/loaders/scorers are reused, and per-example logits now use a compact
tensor artifact rather than huge pretty-printed JSON. Do not build a new framework.
NOVELTY_LESSON_GATE: PASS — old TT/X/CC identity and attention/probe retractions were
read and respected. The new claim is a tested causal source/writer/port correspondence,
not novelty of matrix multiplication or recurrence.

Both managed runners are healthy. Next strategic checkpoint after16:50 UTC;
mathematical review remains due after16:48 UTC. Concrete continuation: committed
head/port result plus prospective middle-match generator/controls and an audited
managed experiment. Full goal stays active; no completion or blockage claim.
