# Frozen output-factor preservation of the composed regional interaction

13 September 2026. Use only the new `COMPOSED_LAST_BLOCK_STATES_V1` cache from the already tested 120 prefixes, preserving its additive background, full-head, compact-head and MLP-only states. This is a historical conditional circuit-effect check, not a new circuit, fresh/OOD panel or fit.

Freeze both completed grouped artifacts and their exact weight-only matched global baselines, ranks78 and167. Evaluate two reader replacements per program: only the bias-free final bilinear output contribution, and the whole unembedding. Keep final RMS from each unchanged state and apply separate token softcaps. For the additive background, its bias-free contribution is the additive combination of native branch contributions, as defined by the cache's matching linear part.

For every row, compute the British-minus-American target margin and the existing unrelated control margin. Report full-head minus background, compact-head minus background, and MLP-only minus background effects. Compare compressed effects with the same predictor under native readers, not with a newly chosen reference. Report baseline output drift separately, so cancellation cannot hide it.

Registered checks per capacity:

- `pred_a`: FP64 native-reader outcomes replay cached FP32 margins within1e-4 maximum absolute error; full and compact target-effect vectors replay within5% relative error in all five groups.
- `pred_b`: grouped **quadratic-only** full and compact target-effect vectors preserve native-reader effects within10% relative error in all five groups.
- `pred_c`: those same grouped quadratic-only variants change each unrelated control effect by at most1e-4 absolute margin units.

Keep zero/opposite-sign counts, both endpoints, all five groups, full/compact/MLP-only variants and both replacement routes. The whole-U route is a declared diagnostic beyond the folded fit objective; no compression adoption follows from passing only the conditional quadratic route. Compare matched global baselines descriptively under exactly the same targets and arithmetic. Preserve the prior convergence and FineWeb failures regardless of this outcome.

No body forwards in scoring. The cache collection is separately managed and registered. Score only after its completed receipt exists. Artifact hashes are recorded; no factor selection, coefficient refitting, task labels in discovery or post-hoc changes to groups.
