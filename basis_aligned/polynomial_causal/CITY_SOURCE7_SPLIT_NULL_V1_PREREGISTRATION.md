# Specificity of the source7 composition screen

The MLP7-dependent/complement partition passes the absolute interaction bar:
.34571131394446597 <= .35. Test whether this is better than arbitrary partitions
of the SAME complete city-removal write. No selection of alternate source groups.

Sixteen fixed random partitions use a seeded permutation of1152 native output
coordinates (seed18094900+k); the first576 form pieceA, the remaining576 pieceB.
Each mask is shared across every token, sequence and cue. Pieces sum exactly to
the original complete city-removal write. Inject each at attention8 output and
recompute MLP8 and the entire suffix. This is a basis-specific random-split null,
not an equal-norm edit null or gauge-invariance test.

Arms: native,full,MLP7-group,complement,32random pieces;1440forwards/300seconds.
All original40opened sequences and readouts remain. No fitting or new data.

- a: first four arms replay CITY_SOURCE7_EDIT_V1<=1e-4absolute AND<=1e-5relative;
  finite readouts; exactly1440forwards; group+complement sum<=1e-10relative;
  each random piece target RMS>=.01 of full effect (live normalization).
- b: real partition interaction RMS divided by smaller-piece target RMS<=.35.
- c: real normalized interaction<=median of sixteen random normalized interactions.

Report all random ratios, real percentile and piece effects. Passing b alone does
not establish composition specificity; c failing preserves that distinction.
Even if all pass, fresh source-group manipulation/composition and port closure
remain untested. Prior inherited/current composition failure is not superseded.
