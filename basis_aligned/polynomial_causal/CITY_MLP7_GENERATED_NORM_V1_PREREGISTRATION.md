# Generate the city-side normalizers from the folded interface

The folded city readings are k1,k2,v after the mixed8 input RMS. Each key RMS
can therefore be calculated from its128coordinates; external key RMS scalars
are redundant (up to native floating-point order). Keep the native epsilon.

For the remaining input RMS, test an explicit unfitted approximation:
rho_hat=sqrt(mean(other_city_sources²)+epsilon). This omits the MLP7 output's
self and background-cross contributions to the scalar denominator only.
All MLP7 contributions to both key numerators and current-value numerator stay
fully generated by the folded readers. Query factors remain externally native.

CPU screen on all forty opened source fixtures:
- a: generated key normalizers WITH supplied native mixed8 RMS reproduce the
  complete native city write<=1e-4relative on every sequence.
- b: generated key normalizers WITH rho_hat instead reproduce the complete
  native city write<=.10relative on every sequence.
- c: abs(rho_hat/rho_native−1)<=.10 on every sequence.
No row selection, fitted correction or changed gate after inspection. Preserve
per-sequence norm ratios and write errors. Passing is a local approximation
screen only; full native suffix, selectivity/nulls and fresh confirmation are
required before adopting the normalization approximation.

Scope/price: two1152-dimensional source vectors, two native query fields, token
value/table, known scalar lambda8 and mask. Exact mode still takes one RMS
scalar; approximation mode takes none. This does not close upstream states or
query generation. Same frozen reader weights; no added learned parameters.
