# Opened v4 source dictionary vs selector audit

The frozen role-bank selector fails8/16 cells even in the full native model. Exact single-token attention replacement now matches full effects<=.001. Test whether the same23-port dictionary permits selective native edits when supplied actual recipient derivatives. This is an oracle diagnostic, not independent extraction or fresh OOD.

Four openedv4 contexts. Compute9 native suffix readers with respect to rawpre11, contract each nominated role's23directions, solve the existing bounded shared_selective_source_lp.choose per input with unchanged unitB reference and.08linear collateral budget. No change of dictionary, amplitude box, outputs or finite gates. Native full baseline, unitB, frozen-bank edit replay and oracle edit for each role:12prefix4double36reverse28native suffix. Old frozen-bank full and unitB effects must replay<=1e-8. Native oracle must retain>=.8number and<=.1maxcollateral in all16cells; report per-output linear/finite prediction errors10%number/5%controls. Primal/dual certificates retain existing solver checks. Save raw reader/source factors for future weight-defined contextual reduction; do not count their per-input production as free. A pass localizes the bottleneck to contextual selection; a miss requires distinguishing local-linear failure from dictionary capacity, not declaring impossibility.
