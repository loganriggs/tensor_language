# Induction typed consumer group response V2 price correction

V1 is preserved as invalid because its price omitted the three native endpoint-capture batches. It executed 33 forwards and 1,056 sequences against a frozen 30/960 count; self reproduction was exact, all producer and restore arms were live, and transaction observations passed, but no V1 scientific group outcome is registered.

V2 changes only the preregistered and runtime-checked price to 33 forwards and 1,056 sequences. The 96 rows, DISCOVERY/CONFIRM split, eight architecture-defined groups, native-oracle restoration semantics, metrics, 25% vocabulary-RMS bar, CE/choice bars, selection rule, and no-fallback policy remain byte-equivalent in meaning. If the corrected instrument passes, V2 may interpret the group results; otherwise it remains invalid.
