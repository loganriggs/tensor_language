# Hourly strategic review — 2026-09-08 05:15 UTC

## Circuit target and program goal

The seven required circuit properties remain: (1) a computational specification of what is read,
computed, and written; (2) grouping across module boundaries and splitting within modules when
causality demands it; (3) prediction on held-out and OOD text; (4) extraction or sufficiency through
a clear interface; (5) selective removal, swap, and edit; (6) composition and reuse; and (7) stable
identification across folds, texts, and admissible gauges. The final object must still be a smaller
transparent tensor program that is jointly predictive, composable, manipulable, and literally
simpler in storage, compute, edges, and state. A low-dimensional basis or a large static weight
score alone is not a circuit.

## What changed since 04:15

The previous review correctly closed router feature engineering and redirected to composition.
That redirection has now produced four immutable outcomes and the missing executable contracts.

1. The zero-forward compatibility audit confirmed one exact 1152-dimensional final-residual
   decoder boundary, but correctly refused composition because physical bases and simultaneous
   command rows were absent.
2. The common-gauge capture stored temporal Q8 and both cross-fitted is-was bases byte-exactly.
   Is-was is fold-stable (`.9050/.8087` principal cosines), but not contained in Q8
   (`.6752/.2465` and `.6520/.1941`); the union rank is 12. The proper state model is a task-typed
   direct sum, not one shared subspace.
3. The first same-sequence native bank was an informative null. Is-was and future temporal cells
   were perfect, while past temporal cells were `0/8` in one template and `4/8` in the other in both
   phases. No rows were licensed and no causal result opened.
4. Translating the stored physical bases through every exact weight tensor succeeded. Is-was
   top-20 fold Jaccards are `.8182` for readers and `.7391` for writers. Six interfaces exceed
   fourfold isotropic enrichment for both task spaces: L9H1/L11H3/L15H5 output writers,
   L11H3:v, and L15H5:q/q2. Shared weights therefore act on distinct task-typed state spaces.
5. A second native bank now preserves each previously qualified temporal prefix exactly at the
   earlier answer. Causal masking makes those temporal logits invariant to the appended clause;
   only the later is-was command remains uncertain. It is queued without filtering behind the live
   managed job.
6. The conditional successor is already frozen and staged: full-head swaps for L9H1/L11H3/L15H5,
   all nine-head parent-module swaps, and the three-head union, separately for each command while
   scoring both margins. Canonical will-minus-had and is-minus-was orientations prevent the paired
   toggle sign error that a correct-answer-relative margin would introduce.

## Strategic assessment

This remains the highest-information route. The physical basis result answered the user's proposed
cross-task subspace question more precisely than expected: the states do not collapse to one shared
basis, but exact weights expose a small shared interface set. The right next question is causal use
of those weights under full head/module patching. It simultaneously tests computational
specification, within-module localization, stable cross-task reuse, selectivity, and whether a
greedy union is justified. It does not reopen the failed semantic-router branch.

The first native null does not justify editing or filtering its rows. The prefix-preserved successor
is a distinct population with an analytical causal guarantee for the earlier token and one remaining
empirical coexistence question. If it fails, same-sequence composition remains capability-blocked
for these constructions and the staged causal runner stays sealed.

## Confound and implementation audit

- **Gauge:** all tensor contractions consume physical orthonormal bases stored in the same ambient
  gauge. Is-was folds remain separate; neither is selected or averaged after outcomes.
- **Rank versus circuit:** rank 12 describes the state union only. The advancing evidence is the
  exact named-weight inventory and prospective causal head/module swaps.
- **Static alignment:** the six interfaces are explicitly nominations, not causal readers/writers.
  Complete parent-module arms protect against falsely closing a layer when one head fails.
- **Autoregressive position:** the successor preserves the complete earlier temporal prefix; later
  text cannot affect that logit. Every role-specific donor pair is an involution with equal query
  positions.
- **Margin orientation:** the staged factorial uses one canonical vocabulary orientation across
  both toggle directions. Correct-answer-relative margins would reverse the donor coordinate and
  have been invalid; a focused test now guards this.
- **Nonlinear composition:** the three-head union is compared with the actual sum of singleton
  effects. A relative error above `.25` forbids greedy composition and routes to interaction-aware
  subsets.
- **Selectivity:** every patched forward scores both answers. Later-to-earlier effect must be
  numerically zero; earlier-to-later collateral has a fixed relative bar.
- **Post-selection:** sites and bars were frozen from the complete weight atlas before the native
  successor outcome. No row, site, direction, template, or fold may be dropped.
- **Execution:** all GPU work remains hash-bound behind Supervisor bqrunner. Both lanes are healthy;
  no direct process or preemption occurred.

## Ranked next moves

1. **Score the queued prefix-preserved native capability receipt.** If and only if all 32 cells
   pass, insert its immutable hash into the already-staged 15-forward factorial, rerun focused tests,
   commit, and enqueue. If it fails, preserve the null and leave causal outcomes sealed.
2. **Execute the full head/module factorial.** Require a material parent route before interpreting
   a head, use cross-role collateral to distinguish shared interface from shared computation, and
   allow a greedy head union only if its preregistered distributivity gate passes.
3. **Run the task-typed final-state `00/10/01/11` composition law.** This becomes eligible only after
   native capability and causal interface evidence. Use the exact Möbius/additivity contract already
   implemented; do not infer final composition from static weight overlap.
4. **If head additivity fails, interaction-aware subset factorial.** Test the seven nonempty subsets
   of the three frozen heads without expanding the site bank.
5. **Router/DAS optimization.** Deferred. Any return requires genuinely new construction variation
   and a joint target/complement objective with noise or KL regularization, not another v15 feature
   or threshold sweep.

## Throughput and systems checkpoint

From 04:15 to 05:15 this branch landed four results: compatibility audit, physical basis capture,
first joint capability, and the complete exact-weight atlas. It also produced two candidate banks,
two native capability gates, a joint Möbius contract, a 1,026-interface weight contract, a causal
factorial accounting contract, and the staged full head/module runner. The science jobs themselves
took about 3–4 seconds each; the roughly 26-minute gap to the basis result was serialized behind a
valid unrelated managed job. Queue latency was used for prospective design and tests. There were no
outcome-bearing retries and no threshold changes. The queue currently contains the prefix-preserved
capability followed by another valid owned job behind live `v250`; bqrunner and bqrunner2 are both
RUNNING.

CIRCUIT_FOCUS: PASS

CEREMONY_BUDGET: PASS

NOVELTY_LESSON_GATE: PASS

Immediate continuation: keep the staged causal runner closed, score the queued capability at its
first safe boundary, and either hash-finalize/enqueue the full head/module factorial or publish the
second native null without filtering.
