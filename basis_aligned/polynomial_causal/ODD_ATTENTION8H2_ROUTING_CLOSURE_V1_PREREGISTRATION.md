# Close the head8.2 city-routing port from block8 state

Export head8.2's four128x1152 Q/K maps and compute the exact product of its two
normalized, BF16-rotated city-source dot products from the normalized block8
current state. Compose that routing with the frozen inherited-city rank-one
writer. Validate on all96 rows of the four-template union and save one native
representative per family. Exactly96 native body forwards under180 seconds; no
fitting. The first CPU control's bitwise-equality gate failed at5.59e-9; it is
preserved. Correction-only V2 uses a prospective native FP32 `1e-7` bar and
passes without changing the formula.

- `pred_a`: standalone routing agrees with native head8.2 city routing within
  `1e-5`, and the composed rank-one write agrees with direct native inherited
  city projection within `1e-5` on every row.
- `pred_b`: exported value/output maps and mixture exactly match the bound
  rank-one program; all exported tensors are finite and exactly96 forwards run.
- `pred_c` (unfavorable standalone price): if normalized block8 current must
  cross this edge's interface, `T*1152 + 2*1152 + 1` runtime scalars are at
  least the dense `T*1152` write size at every observed length.
- `pred_d` (favorable standalone price): the same closed runtime interface is
  at most50% of the dense write size at every observed length.

The shared-state interpretation may amortize the block8 current port across
other consumers, but this receipt prices the edge alone. Block8 state generation,
head9.8 O, and the native suffix remain external; no full-model claim follows.

