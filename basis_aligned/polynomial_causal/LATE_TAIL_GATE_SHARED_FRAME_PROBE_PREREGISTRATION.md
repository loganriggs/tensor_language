# Preregistration — late_tail_gate_shared_frame_probe (Claude, LANE 1 CUDA) — Registered Registered 2026-09-04 03:12Z (box clock) (box clock)

Sign convention (§2135): every CE number is CE ADDED ABOVE THE REAL MODEL on held-out docs 0–63 (FRESH split; second moments from docs 96–191) — LOWER IS BETTER. Every CE arm below is expected ≥ 0 (gating a cross term by a projector can only lose information relative to the exact block) and each prediction states its operands' signs. Absolute CE bars STATE THE WOBBLE THEY INCLUDE (rule adopted after §2805's two at-the-bar passes): the reproduction wobble is ~.003 per arm; a bar on a difference of two arms includes 2 × .003.

## Question
§2804–§2805: each late MLP's tail read is controlled by a gate on the core, and that gate truncates to 16–128 whitened modes per block; the compression composes (full ten-block program .0722). The frames were fitted PER BLOCK. Is the gate frame SHARED across the late stack — one core subspace that every late block reads to control its tail read (a reusable component: "the late stack's tail-read controller is a 128-dim core subspace") — or does each block gate on its own directions? Weight-side: the read spaces span(M_c^{-1/2} V_k) of adjacent blocks either overlap far above a random subspace (64/768 = .083) or do not. CE side: ONE pooled frame applied at all ten blocks either costs about what the per-block 128-frames cost or approaches the removal cost.

## Instrument
Parent late_tail_gate_program_composition_probe (§2805): same exact weight-side Gram G_l (kept per block this time), whitening by the block's core second moment M_c,l, Gate(l, k) with own modes; GateP(l, Pi) = the same block with a SUPPLIED core projector Pi. Read space of block l at k: R_l^k = orth(M_c,l^{-1/2} V_l[:, :k]); adjacent overlap ov(l, l+1; k) = ‖R_l^T R_{l+1}‖_F² / k (in [0, 1]; random-pair value k/768). Pooled frame: M̄_c = mean_l M_c,l; Ḡ = Σ_l G_l / tr G_l; Ḡ_w = M̄_c^{1/2} Ḡ M̄_c^{1/2} → V̄; Pi_shared(k) = M̄_c^{1/2} V̄_k V̄_k^T M̄_c^{-1/2}. Retained cross-term energy of block l under Pi: tr(G_l Pi M_c,l Pi^T) / tr(G_l M_c,l) (equals the block's own captured[k] when Pi is its own frame — frame check ≤ 1e-6). Arms (CE added): SPLIT8_1024; GATE_EXACT; UNIFORM_128 (own 128-frames at all ten blocks — reproduces §2799's .0627); SHARED_32/64/128/256 (Pi_shared(k) at all ten blocks); TRANSFER_17_128 and TRANSFER_8_128 (block 17's / block 8's own 128-frame applied at all ten); RANDOM_128 (Pi = M̄_c^{1/2} Q Q^T M̄_c^{-1/2}, Q a seed-0 random orthonormal 768×128). Weight-side: ov(l, l+1; k) for k = 16, 64, 128 and the random-pair reference; retained energy per block under every frame; own-64 read space's fraction inside the shared 128 / 256 read space. Price: 96 fit + 64 × 11 ≈ 800 GPU document-forwards (~35 s).

## Predictions (scored exactly as written) — hypothesis value / null value / operand signs / wobble included
- pred_a_instrument: |baseline − 3.0322401| ≤ 1e-4; |SPLIT8_1024 − .0374| ≤ .015; |GATE_EXACT| ≤ 1e-3; |UNIFORM_128 − .0627| ≤ .015; Gram check ≤ 1e-6; frame check ≤ 1e-6.
- pred_b_adjacent_read_spaces_overlap: median over the nine adjacent pairs of ov(l, l+1; 64) ≥ .5. Null: ≤ .2. Operands in [0, 1]. Worked example: if the late blocks read one shared control subspace, adjacent 64-frames overlap ≈ .6–.8; a random pair reads .083; block-specific frames with a small common part read ≈ .15.
- pred_c_shared_128_frame_prices_near_own_128: SHARED_128 − UNIFORM_128 ≤ .05 (includes 2 × .003 wobble). Null: SHARED_128 − UNIFORM_128 ≥ .10. Signs: both positive (UNIFORM_128 ≈ .063). Worked example: if the frame is shared, the pooled 128-frame retains ≈ .55–.65 of each block's cross-term energy against ≈ .69–.77 for the own frames (§2799) and the ten-block cost rises from .063 to ≈ .09 (+.03); if each block gates on its own directions, a 128-dim shared frame retains little more than a random one and the cost approaches the removal cost .3668 (+.30).
- pred_d_shared_beats_random: RANDOM_128 − SHARED_128 ≥ .05 (includes 2 × .003 wobble). Null: ≤ .01. Signs: both positive. Worked example: a random 128-frame retains ≈ 1/6 of the energy → ≈ .2–.3 CE; shared ≈ .09 → margin ≈ .15; if the pooled frame is no better than random the margin is ≈ 0.
- pred_e_shared_256_frame_covers_the_stack: SHARED_256 − UNIFORM_128 ≤ .02 (includes 2 × .003 wobble). Null: ≥ .05. Signs: both positive. Worked example: if the ten own-128 read spaces lie in a common ≈ 200-dim subspace, the shared 256-frame retains at least what each own 128-frame does → ≈ .06–.07 (+.00 to +.01); if they are block-specific, 256 shared dims cover ten disjoint 128-frames poorly → ≥ +.05.

Bars: {"ce_tol": 1e-4, "repro_tol": 0.015, "exact_tol": 1e-3, "gram_tol": 1e-6, "frame_tol": 1e-6, "floor": 0.002, "b_ov": 0.5, "c_margin": 0.05, "d_margin": 0.05, "e_margin": 0.02}. Null bars: {"b_ov": 0.2, "c_margin": 0.10, "d_margin": 0.01, "e_margin": 0.05}.

## What each outcome means
b, c, e TRUE: the late tail-read controller is ONE reusable core subspace — the late-stack program becomes "ten tail reads gated by a shared 128–256-dim core frame" (one frame, not ten), and the frame's directions are the next object (what core features they are; whether they are the §2803 linear-quarter directions). b TRUE, c FALSE: the read spaces overlap but the pooled frame is a poor common frame — the gate is shared in subspace but block-specific in weighting; a joint (CE-fitted) frame is the follow-up. b FALSE with null met: block-specific gates — the compression stays per block and the program's parameter count stays 10 × (768 × k). d FALSE with null met would mean the pooled construction is broken (a control on the instrument, not the science); TRANSFER arms say whether the shared frame is dominated by the end blocks. Nothing installs into the §312 frontier; the strict explained fraction is unchanged by any outcome.
