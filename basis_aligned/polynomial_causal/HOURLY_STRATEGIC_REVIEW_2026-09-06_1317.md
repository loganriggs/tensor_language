# Hourly circuit strategy review — 2026-09-06 13:17 UTC

## Circuit interpretation targets

The live decomposition must specify what each circuit reads, computes, writes, and feeds downstream; group operationally shared pieces across native modules while splitting cohabiting variables; predict held-out and out-of-distribution behavior; support an executable sufficiency interface; permit selective removal, swap, and edit tests; explain composition and reuse; and remain stable across populations, gauges, and fitting restarts. Literal storage and compute count only after these circuit claims hold.

The program-level goal remains a smaller transparent tensor program that is jointly predictive on fresh and OOD text, composable under installed replacements, manipulable under registered counterfactuals, and simpler under literal storage, compute, edge, state, and program prices. A screen, a useful null, or low loss is not completion.

## What changed since 12:17

Ten Codex claims reached terminal status: eight valid scientific receipts and two preserved instrument-invalid receipts. The valid sequence localized the will/had temporal signal from a broad late reader to an exact fresh lexical path. The initial L9H1/H4 source partition found subject onset dominant but did not identify where that state was written. A residual-depth sweep placed useful subject state late, at boundary 9 rather than by boundary 5. The first block-8 component cube was invalid and retained as such; its native-arithmetic repair was valid but null because block entry, attention, and MLP jointly assembled the state rather than one component meeting the dominance bar.

The block-8 head inventory screen was also first invalid because the preregistration assumed 12 heads while the checkpoint has 9. The immutable failure was recorded, the generic runner was corrected to read the model inventory, and the clean repair identified H1: it retains 98.23%/97.56% of all-head A1/A2 recovery, while every other singleton is at most 1.3%. Exact source decomposition then showed that the literal temporal cue alone supplies 99.3%/100.7% of H1's effect at the two unchanged subject-onset tokens; prefix and local sources are negligible.

That writer survived a genuinely fresh authority using unseen `Later`/`Previously` cues, new target nouns, and a new note-frame construction. Native capability passed in all eight family/direction/side cells. H1 retained 91.8%/97.1% of full block-8 attention, the cue retained 98.2%/96.2% of H1, and noncue sources remained below 0.0045 absolute recovery. The identified OOD writer operation is therefore temporal cue -> block8 H1 -> two subject-onset destinations.

The first composed writer-reader prediction was a useful null. Clamping the exact block9 H1/H4 subject-source bank removes only 44.69%/48.85% of the fresh writer effect, while clamping its complement sources removes exactly zero; the bank is perfectly source-selective but only a partial downstream route. An exact block-9 subject-state cube then found no single bypass component sufficient at the registered bar. Boundary-10 recovery is 0.09205/0.05783. Entry is largest at Shapley 0.06422/0.03465 but retains only 69.38%/59.01%; attention contributes 0.01096/0.00629 and MLP contributes 0.01687/0.01689. The bypass is cumulative carried-plus-update processing, with MLP9 especially material in A2.

An independent retraction during this interval established that final-residual selectivity and DAS merely rederive lm-head/readout geometry. None of the new writer evidence relies on resid18 subspace selectivity; downstream work will use explicit causal boundary and native-module interventions.

## Throughput and systems audit

The ten terminal claims landed in 56 minutes. Eight were valid circuit screens/nulls and two were explicit invalids that prevented silent result rescue. Valid managed GPU jobs generally took about 1.2-2.5 seconds; queue wait was not the bottleneck. The interval added three reusable, tested primitives: arbitrary attention source-to-destination intervention, composed writer-reader mediation, and exact residual block-component cubes.

Implementation ceremony still dominates. The five most recent experiment runners total 1,463 lines (72, 303, 346, 339, and 403), compared with 484 lines in the three reusable evaluators, even though the corresponding GPU executions take seconds. Hash closure, dry-run pricing, repeated batching, endpoint scoring, and publication remain replicated around a small causal kernel.

`CIRCUIT_FOCUS: PASS` — every experiment localized a writer, source, head, downstream edge, component split, OOD prediction, or an instrument failure that materially constrained those claims.

`CEREMONY_BUDGET: FAIL` — runner authorship remains orders of magnitude larger than GPU execution. Before another GPU job, extend and test the existing residual-boundary evaluator so it can capture a declared upstream source write and score that state across multiple downstream boundaries. The next runner must call this reusable operation rather than reimplement capture, patch, and scoring loops.

`NOVELTY_LESSON_GATE: PASS` — the interval searched the existing aspectual suffix chronology, corrected the checkpoint's nine-head inventory before inference, preserved both invalid terminals, used fresh cue/construction authority, and incorporated the final-residual readout-geometry retraction instead of extending it.

## Confounds, alternatives, and decision

The fresh path has exact token-level source and destination semantics, unchanged destination tokens, row-matched native logit endpoints, full-head and source reconstruction closures below 5e-6, no fitted scalar, and direct OOD replication. Remaining confounds are downstream multiplicity and boundary mixing: a subject-position state can be read by several later layers, and a boundary patch measures all suffix consumers rather than naming one module. Nonmonotonic recovery is possible because later blocks can amplify or cancel the same state. Therefore the boundary sweep is an onset/offset locator, not itself a module identity claim; any crossing must receive a preregistered module/head/source decomposition.

Ranked next moves:

1. Sweep the exact fresh block8H1 cue-written subject state across residual boundaries 10-18. Boundary 10 must recur the measured bypass, boundary 18 must be inert, and the latest material boundary localizes the last downstream consumer window. This directly narrows the missing half-path.
2. Decompose the crossing block into carried, attention, and MLP terms, then localize heads/sources only if attention is material. Earlier aspectual work makes block11 H3 and block15 H5 informed alternatives, but their source banks cannot be assumed for this new construction.
3. Test joint mediation through the known block9 bank plus the newly localized later consumer. This is the decisive completeness test: their union should account for substantially more than the current 51-55% retention under the block9 clamp.
4. Only after causal closure, compile the fresh writer and reader operations into the shared typed program and test selective cue edits. A basis/rank fit remains rejected as the immediate action because it would not identify the missing native consumer.

The highest-information next action is the downstream residual offset sweep. It turns the current partial-mediation null into a bounded search over nine causal boundaries and has an explicit dead endpoint at boundary 18. The ceremony failure forces a reusable evaluator repair and CPU tests before preregistration or GPU execution. The next mathematical review remains due around 14:30 UTC; the next hourly strategic review is due around 14:17 UTC.
