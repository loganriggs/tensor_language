# Hourly strategic review

ACTIVE_TRACK: CIRCUIT

Actual UTC: 2026-09-19T01:55:26Z. Next deadline: 2026-09-19T02:55:26Z.
Previous Codex review: [2026-09-18 02:57 WEIGHT_FOLDING](HOURLY_STRATEGIC_REVIEW_2026-09-18_0257.md), 22h58m old at this review's clock sample. This is one current review, not backfill for offline hours. The live checkout is `/workspace/tensor_language`; historical `/workspace` paths were resolved here. Supervisor, not the stale workstation systemd path, reports `bqrunner` and `bqrunner2` running with empty queues. No GPU model was loaded.

The regional city path now exactly generates the head8.2 city-removal write from residual6 and token IDs through attention7 and MLP7 at the attention8 boundary. The important correction is that full MLP7 enters the RMS8 residual once while its five folded readers supply the numerator channels; adding it to both had produced 1.34 relative error. Corrected replay is about 2e-6, including a fresh outcome-blind FineWeb boundary panel. This is extraction and boundary prediction, not yet a full-suffix circuit: FineWeb behavioral direction transfer previously failed, independent key/value composition fails, and matched-effect simplicity remains absent.

## Seven circuit targets and full goal

1. **Computational specification:** identify what is read, the operation/composition, what is written, and the downstream consumers.
2. **Cross-boundary grouping and within-module splitting:** group pieces across heads/MLPs when a reader treats them as one variable, and split native modules when pieces have different roles.
3. **Held-out/OOD prediction:** predict activations and behavioral effects on unseen inputs, task variants, and shifted corpora.
4. **Extraction or sufficiency:** execute the circuit at a declared boundary and background, with every remaining native port visible.
5. **Selective manipulation:** removal, swap, or edit changes the intended behavior against matched controls while preserving unrelated behaviors and accounting for redundancy/interactions.
6. **Composition and reuse:** shared pieces serve multiple tasks/modules and joint effects are predictable from their registered composition law.
7. **Stable identification:** units survive document/corpus splits, gauges, and restarts, or are defined by downstream operational equivalence.

Goal: a smaller transparent tensor program that predicts fresh and OOD behavior, is extracted at a declared boundary, supports selective manipulation/removal, composes and is reusable. Price simplicity separately in stored scalars/bytes, readers, products, writers, tables, states, ports, edges, adapters, and execution. Lower error or storage alone cannot replace any behavioral property; quantization is excluded. This applies the user-designated five-property definition: simple, predicts OOD, extracted, selective, composes.

## Progress, failures, and five-property score

| Property | Current evidence | Verdict |
|---|---|---|
| Held-out/OOD prediction | The frozen packed eight-head path predicted removal on 20 fresh Pile documents, but FineWeb behavioral direction was only 88/102 capable pairs, below the 90% gate. The corrected full upstream generator predicts its write on 20 new FineWeb documents / 40 sequences / 240 probes with max relative error `1.61e-6`; those are 20 distinct document contexts, not 240 independent contexts. Full-suffix behavior on that panel is untested. | **PARTIAL; whole-behavior cross-corpus gate failed** |
| Extraction at a boundary | Corrected residual6→attention8 execution replays 40 opened and 40 fresh sequences with max error `1.97e-6` and `1.61e-6`, zero off-support write. Residual6, token IDs, city/destination specification and the native suffix remain the interface/background; blocks 0–6 are not extracted. | **PASS at the declared intermediate boundary; incomplete end-to-end** |
| Selective manipulation/removal | Packed Pile removal beat 16 equal-site nulls and controlled four unrelated readers. The fresh FineWeb exact-upstream receipt checks prediction/support only; it does not install the write through the suffix. | **PARTIAL; no fresh full-suffix pass for the corrected exact path** |
| Composition/reuse | Coupled key/value interchange has interaction/smaller `2.3448` at the head and `1.0746` through the suffix; head-cross omission error is `.4186`. A closed Möbius identity does not make these small relative to the smallest piece or show random-split specificity. | **FAIL for the tested split** |
| Measured simplicity | The packed eight-head program saved 884,736 FP32 values (3.8% versus equal-vocabulary nine-head execution), but the exact corrected path retains all nine attention7 heads and full MLP7/RMS machinery. No matched-effect random component, whole-program speedup, or five-property adoption price exists. | **MEASURED LOCALLY; NOT ESTABLISHED AS SIMPLE** |

Evidence tags remain distinct: the upstream identities are **fold** evidence; packed/full-suffix interventions are **edit** evidence; `CITY_FINEWEB_RESPONSE_V1` is **response** evidence; no new fit supports the exact path. Fresh/opened/replay labels above are relative to the relevant selection step.

Since the previous review, the regional route produced a fresh FineWeb direction failure rather than hiding native reversals; an exact head9 value fold rejected routing-only sufficiency; an MLP8 value mediator passed a fresh causal screen at a narrower native-state boundary; and the residual6→attention8 closure was corrected from a double-counting failure to exact replay. The separate circuit lane generated many unit-level screens through v286, including reusable MLP8 number detectors and several honest transfer/selectivity failures, but those are not evidence for the regional city path. Its lane explicitly stopped and idled after its bounded program ended. `TYPED_FACE_EXTRACTION_V1` remains primary-owned and was not modified.

## Highest-value CIRCUIT action

Use the already frozen corrected upstream generator as an intervention, not another attribution: on preregistered structurally varied rows with actual distinct `(template, city-pair/document)` cells, install its generated city-removal write before the existing coupled MLP8→head9.8 value operator and recompute the full native suffix. Freeze before outcomes:

- target effect-error and sign/attenuation gates against the matching native intervention;
- at least three unrelated-reader preservation gates;
- 16 equal-norm, same-site random-direction nulls;
- native-upstream only, value-correction only, additive singles, and true joint arms, pricing the Möbius interaction relative to the smaller live single;
- separate new-endpoint and boundary-changing templates (colon/quote/moved-sentence controls), with context-cell count reported separately from probe rows.

This directly tests targets 3, 5, and 6 and can kill the route if the frozen write misses its prediction gate, if collateral is not below its preregistered bound/nulls, or if joint effects remain non-composable. It does not propose a new suffix: the forward response census already rejected early/head9 sufficiency and selected the existing coupled value reader. No run is enqueued by this bounded review.

Alternatives ranked by information/cost:

1. **Installed exact-upstream + coupled-value factorial** above: highest value because it converts an exact fold into fresh causal prediction/selectivity/composition. Kill on any frozen gate.
2. **Structural-template test of packed removal alone:** cheaper, but leaves exact port closure disconnected from the causal route. Kill if boundary effects track punctuation position.
3. **Another source/head partition:** demoted; MLP7 direction, key/value independence, destination partition, and random split evidence already reject this search style. Reopen only with a downstream-reader-derived partition and preregistered matched null.

Concrete track connection: folding found that the five MLP7 reader channels plus full RMS residual are jointly necessary and removed the double count; the circuit test now asks whether that exact boundary object causes the intended suffix effect selectively. Conversely, the failed independent key/value intervention tells the next folding hour to retain the full QK1×QK2×V coupling rather than compressing its singles.

**WEIGHT_FOLDING handoff:** preserve the corrected residual6→attention8 exact-RMS operator, all ordered residual6/initial7/attention7 self/cross terms, and complete QK1×QK2×V coupling; after the circuit factorial, fold only the source terms the surviving downstream reader actually uses.

## Confounds, alternatives, and organization

Audit baseline subtraction and intervention equivalence, frame/position mixing, RMS and softcap nonlinearities, suffix loss composition, shared token/document difficulty, Pile/FineWeb selection history, capability conditioning, leakage, dead configuration knobs, BF16/FP32 noise floors, and post-selection. The fresh exact-upstream receipt corrected an earlier counterfactual mismatch (city removal versus donor replacement); those must stay separate. Native and generated sign reversals do not prove semantic ownership. A local exact equality proves neither text-reachable small interactions nor whole-model sufficiency.

Registry/dossier audit:

- `CIRCUIT_GRAPH_REGISTRY_V1.md` reports 49 packages, 44 manifests, 33 boundaries, and two historical four-trait records; it does not certify five-property completion.
- The circuit registry records packed removal, FineWeb failure, value mediation, and composition failure. MLP7/MLP8 dossiers preserve the ordered terms, normalization, ports, and primary ownership.
- The computation-path registry had no current record for the corrected city path. **Bounded repair executed:** added `PATH-REGIONAL-CITY-001`, linking the exact fold, circuit packages, dossiers, failures, and next causal falsifier.
- Remaining missing cross-link: the generated graph inventory does not list the untracked `city_mlp8_norm_closed_v1` package. It was not regenerated because that package and related result set are concurrent untracked work; primary ownership must settle it first.
- The explanation index's first pointer stops at the 03:26 mediator update and does not point to the later exact-upstream correction. That is navigation debt, but a second repair was outside this review's one-repair budget.

## PAST_HOUR_TIMING

Window: 2026-09-19T00:55:26Z–01:55:26Z. Times below are process/receipt/file authorities, not commit-gap estimates.

- Claude review 33 states its own 00:32–01:32 bucket as 2 minutes lint/review and 58 minutes deliberately idle under `STOP`; therefore at least 37 minutes of this review window (00:55–01:32) are explicitly idle. It produced no scientific receipt.
- Review 33 and its ops index commit ended at 01:32:58. Its design, implementation, validation, and interpretation buckets are zero for science; documentation/review is included in its declared 2 minutes, but exact overlap with this window is unknown.
- Managed canary files changed at 01:37:37–01:37:39. They are infrastructure checks, not candidate model execution, and overlapped no registered scientific receipt.
- The Codex process associated with the 01:50 commit started at 01:46:50; the commit was at 01:50:09. This bounds the process-to-publication interval at 3m19s but does **not** measure active work or divide it among design/implementation/interpretation. The corrected exact replay itself reports `0.7700s` CPU runtime. The fresh 20-document result was timestamped 2026-09-18T14:56:01 and reports `1.9377s`; its late commit is not new execution this hour.
- Serial candidate latency from prior-art check through scored dossier is **unknown**. No authoritative start/end pair covers that whole chain. Scientific design **unknown**; implementation **unknown**; validation measured only for the `0.7700s` replay (executed before this hour); GPU/model execution **0 this hour**; interpretation **unknown**; documentation/review **2 minutes in the lane's own 00:32–01:32 bucket plus an unclassified ≤3m19 process-to-commit interval**; idle/blocked **at least 37 minutes measured**.
- The newest matching phase log is `RESEARCH_PHASES_2026-09-18.jsonl`, ending with a science boundary at 03:00:38. It has nine sparse marks for that date and none for 2026-09-19. Coverage is stale for this hour; the absence prevents category reconstruction and does not imply uninterrupted labor or inactivity outside the independently declared STOP interval.

## PROCESS_IMPROVEMENT

Ranked by likely time saved versus cost:

1. Canonical current-city path record with receipts/dossier/circuit links: likely saves roughly 5–10 minutes of repeated registry search per review or candidate; cost under 5 minutes. **Executed and `git diff --check` passes.**
2. Resume sparse phase-boundary marks with the existing helper: potentially saves a similar retrospective audit cost, but changing another process's instrumentation during a bounded concurrent review risks misleading ownership; not executed.
3. New runner/helper work: low value because the queues, shared executor, lint, derive, and wait helpers already exist; rejected as ceremony.

The review itself creates no new framework. The dirty worktree contains extensive concurrent/untracked results and runlogs; they were preserved.

## Explicit verdicts

- `TRACK_ALTERNATION: PASS` — previous Codex review was `WEIGHT_FOLDING`; this review is `CIRCUIT`.
- `TRACK_PROGRESS: FAIL (immediate prior hour)` — the declared prior track made substantial exact-fold progress since the last Codex checkpoint, but the measured 00:55–01:55 window produced no new scientific receipt; it late-published an older 0.77-second replay. The bounded repair is the canonical path record, and the next action is the causal factorial above.
- `CEREMONY_BUDGET: FAIL` — measured scientific execution this hour was zero while documentation/infrastructure and at least 37 minutes of declared idle time dominated. No elevated scientific risk justifies that ratio; reuse existing machinery for the next test.
- `NOVELTY_LESSON_GATE: PASS` — circuit/path/graph registries and MLP7/MLP8 dossiers were checked; source-partition, direction-transfer, composition, response-census, normalization, and matched-null lessons constrain the selected action.

