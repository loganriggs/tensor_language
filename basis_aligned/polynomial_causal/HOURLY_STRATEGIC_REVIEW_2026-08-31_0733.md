# HOURLY STRATEGIC REVIEW — 2026-08-31 07:33Z

Convention (§2135): all damage numbers are CE added above the real model; LOWER IS BETTER.

## Where the program stands
Goal: reverse-engineer bilin18 into a predictive, manipulable, simpler tensor program.
Strict ledger: 5.348% / 10.923% coverage-credit variants, 4.727 nat, 0 of 68 modules fully closed (registry-
grade; unchanged pending the CP-front global result).
Since the 0632 review, two chapters advanced:
- COMPOUNDING PHYSICS closed (§2207–§2211): excess is a progressive ladder; aggregate is K-buyable
  (all-3456: +0.119) and refit-buyable (seq_traj: +0.437 at zero extra values, a Pareto win) but certificate
  validity is pinned ≤ 2/62; drift composes in QUADRATURE (orthogonal site drifts) while CE superadds —
  the damage is directional, not magnitudinal; attention is only a 31% conduit.
- COMPOSITION CALCULUS built (§2212–§2213): the full Möbius lattice of the CP front is measured
  (singles +0.235 / pairs +0.442 / triples +0.221 / quartic −0.068); K₃ follows ADJACENCY; order-3 predicts
  any front subset to ~±0.07.

## Largest gaps
tail dictionaries/coverage credit; m16 remainder; attn5's write (price cliff); the DIRECTION problem
(certificates measure directions; no repair has moved validity past 2/62 for any multi-site config).

## Candidates (pruned by information gain / falsifiability / GPU cost / redundancy)
1. **CP-front GLOBAL config (rung 122)** — swap tables→CP-3456 inside the full §2144 config, tail dicts
   refit under the swapped front. If the front carried ~1.75 of the census +2.855, this is the largest
   single aggregate move available AND makes the config 184M values cheaper. → EXECUTED (queued).
2. Rungs 120/121 in flight (cross-type lattice out-of-sample; pair-grain certificates).
3. Direction-preserving repair design — blocked on 121 (where validity dies) + 115's direction insight;
   candidate instrument: per-circuit member-dCE vectors under each single, testing whether failures are
   low-rank in circuit space (CPU-able from saved cev tensors if we start saving them — note: start saving).
4. FR fresh-window L2 of the swapped global config vs the §312 frontier 2.6735 — descriptive print lands
   with rung 122; a registered frontier-claim rung only if 122's aggregate holds.
5. Registry/memory sync + explanation doc — explanation due (last 0537); done this review.

## Decision
122 queued (depth 2 behind 120/121). The three in-flight rungs answer: does the calculus cross element
types, where does validity die, and does the global config break the program's aggregate record. Next review
integrates all three; the direction-repair instrument (candidate 3) is the standing design task.
