# Pack the previously selected attention7 drop-head3 candidate

Engineering handoff for the next WEIGHT_FOLDING hour. Selection is frozen by CITY_ATTENTION7_OMISSION_V1, on opened rows. This is not a new circuit, fresh confirmation or independent composition claim.

Store only the eight retained 128-channel head maps for q1,k1,q2,k2,value and output; retain the full first-value table because downstream head8 also reads it. Keep native mixture, RMS, rotary, all five MLP7 readers and head8 factors. No fit, quantization, new selection or scalar proxy.

CPU replay on the same40 fixtures; zero full-model forwards. pred_a: generated edits match the existing head3-omission executor to <=1e-4 relative on each fixture. pred_b: literal FP32 count is 22,441,606, exactly884,736 fewer than full generator. pred_c: finite outputs, exact zero outside declared destinations, CPU only. Count saved parameters, not tensor views sharing a full original storage. Stop on failure. Fresh registered removal/selectivity/null confirmation is required before adopting the subset; none is borrowed from the nine-head interface.
