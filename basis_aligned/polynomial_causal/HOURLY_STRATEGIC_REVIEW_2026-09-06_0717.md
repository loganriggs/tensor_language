# Hourly strategic review — 2026-09-06 07:17 UTC

## Circuit interpretation targets

The circuit standard remains: (1) a computational specification of reads, operations, writes, and downstream users; (2) operational grouping across native modules and splitting within them; (3) held-out and out-of-distribution prediction; (4) extraction or sufficiency; (5) selective removal, swapping, and editing; (6) predictable composition and reuse; and (7) stable identification across corpus, gauge, and fitting choices. Literal storage and compute matter only together with those causal targets.

The full goal is still a smaller transparent tensor program that is jointly predictive on fresh and OOD text, composable under joint replacements, manipulable under registered counterfactual edits, and simpler under literal state/storage/compute/edge pricing. The aspectual path is not the whole model, unrestricted syntax is not covered, and no completed rung ends the program.

## What changed since 06:17

- The rank-one `resid:18` carrier became directly manipulable without a confirmation donor. The target-guided v10 actuator recovered 1.086–1.171 across lexical and fresh A panels, reflected P margins at 0.995–0.999, and moved C by only about 0.011 normalized units.
- Three explicit-variable interchanges held: construction transfer, A1/A2 family transfer, and reporter/period derangement. Whole-variable transfer spans 0.761–1.328 and group swaps 0.877–1.152 with perfect direction. Program v11 now names the tested operational quotient: initial `resid:10`, source-attention deltas, and MLP states. This is a tested bounded equivalence class, not a raw-text state machine or whole-model replacement.
- A prospective syntax-OOD attempt was instrument-invalid because four A2 native-capability sides failed. It sets a capability boundary; its causal arms are not evidence.
- The two-scalar direction prototype is an honest scientific null. It kept all signs and near-zero C collateral but recovered only 0.253/0.257 on fresh A1/A2 and 0.295 on P, about 22% of v10. Its registered terminal remains invalid because an unused C-donor capability condition failed; a zero-forward audit isolated that condition and preserved the outcome separately.
- Changing the object from a direction mean to an input-conditioned gain succeeded on the design-seen population. Four frozen affine coefficients read the source state's fixed `has`/`had` confidence. They produced A1/A2 recovery 1.172/1.325, P reflection 1.023, and C effect 0.00097—4.62x/5.15x/3.46x the scalar prototype and at least as much A recovery as v10 without a per-row grid. The result remains formally invalid because six unrelated C controls fail an irrelevant requirement that their `has`/`had` probe be positive; the audit shows their actual `night`/`day` margins all pass. Scientific scope is only a design-seen causal screen because the old fresh alpha relation informed the predictor form.
- A prospective 64-row lexicon shift is now sealed: 16 new agents and 16 new periods, no old fresh row-ID overlap, no inspected model outcomes or target-guided alphas, unchanged coefficients and bars.

## Strategic decision

The source-conditioned gain route is currently the highest-information path. The two-scalar null and affine screen jointly identify a concrete computational distinction: carrier direction is stable, while required gain tracks a live property of the source state. The next run can promote or kill held-out lexicon prediction with only 25 forwards. This advances prediction, sufficiency, selectivity, and stable operational identification; it is not a rank or compression sweep.

The strongest live confound is that the predictor reads the same fixed `has`/`had` output contrast that the A/P intervention changes. P reflection establishes counterfactual control rather than mere passive correlation, and C protects unrelated output, but the rule could still be a local output-controller rather than the model's upstream aspectual computation. A new-lexicon pass is therefore not enough for a full computational specification. The next discriminant after it must compare this readout rule with a predictor built from upstream v11 quotient variables or test a construction where the aspectual variable has a different surface readout.

## Confound audit

- Baseline subtraction and frame mixing: recovery uses explicit base-to-donor margin denominators; construction/family/lexicon interchanges separately test variable reuse. No CE-added sign convention enters these actuator scores.
- Nonlinear composition: RMS normalization and soft-cap readout make alpha effects nonlinear. Every claimed effect is evaluated through the exact selected head, not inferred from linear alpha proximity.
- Shared token difficulty: live concern. The fixed feature and A/P outcome both involve `has`/`had`; prospective lexical stability cannot alone prove an upstream semantic state.
- Leakage and post-selection: the first affine intervention is explicitly labeled design-seen. Its coefficients use only 16 lexical rows, but inspection of prior fresh target-guided alphas informed the functional form. The sealed v3 lexicon is the first prospective test.
- Dead knobs and capability semantics: two otherwise interpretable runs were made formally invalid by capability conditions on states/contrasts that their scores did not use. This is a workflow defect, not grounds to rewrite terminals. The prospective runner must use family-specific source capability: A base+donor, P source, C actual-task base; C probe sign remains a free predictor input.
- Precision/noise floor: exact-head replay errors of `3.81e-6` are far below the `1e-3` bar. Effects are orders of magnitude larger.
- Confirmation-donor leakage: absent from both prototype and affine alpha selection. The affine rule reads only the source state, fixed token IDs, direction, and four inherited coefficients.

## Ranked next moves and kill evidence

1. **Frozen affine actuator on sealed new lexicon.** Highest information/cost. It dies if native source capability fails, A1/A2 or P falls below 0.75, or C exceeds 0.20. A pass promotes lexicon-held-out prediction only.
2. **Upstream quotient-to-gain predictor.** Read `resid:10`, attention, or MLP operational variables rather than the output head. It dominates mechanistic interpretation if it predicts new-text alpha with comparable causal recovery. It dies if held-out recovery falls below 0.75 or requires a high-capacity row lookup.
3. **Different-surface-readout construction.** Test whether the same operational variable controls an aspectual contrast not scored by `has`/`had`. It kills the “semantic controller” reading if the rule cannot transfer after an explicitly mapped readout change.
4. **Analytic exact-head root/derivative controller.** This could remove grid search entirely, but it remains a target-readout controller and ranks below upstream prediction. It dies if nonlinear root estimates fail P reflection or C selectivity.
5. **Scalar-state lower bound.** Search for pairs with nearly identical fixed has/had confidence but materially different required causal alpha. Such a collision would certify that no one-dimensional margin rule can be sufficient and force a richer predictive state. Do this as a diagnostic, not a compression objective.

No further rank, sparsity, reconstruction, or frontier sweep is warranted. The next three-hour mathematical review remains due near 08:30 UTC.

## Circuit throughput and systems checkpoint

Authoritative timestamps since 06:17 show seven Codex candidate/release cycles: v10 donor-free actuation (~3.5 minutes from prior to result), syntax OOD (~2.3 minutes), cross-construction interchange (~2.6 minutes), cross-family interchange (~3.4 minutes), lexicon derangement (~5.1 minutes including pairing audit and reusable engine), two-scalar prototype (~5.0 minutes through audit), and affine gain (~4.4 minutes through audit). Median serial latency is about 3.5–4 minutes, below the 10-minute target. GPU computation was typically seconds; scientific design and authoring dominated. There was no idle GPU wait. The repeated custom capability logic, not computation, caused both latest invalid terminals and the largest avoidable review cost.

`CIRCUIT_FOCUS: PASS` — every unit advanced manipulation, operational grouping, held-out prediction, or a valid capability/null boundary.

`CEREMONY_BUDGET: FAIL` — safeguards were not the largest time bucket overall, but two unnecessary capability conjuncts forced post-outcome audits. The repair is bounded: use one family-specific capability table in the prospective executor and never interpret an arbitrary predictor-feature sign as native task capability.

`NOVELTY_LESSON_GATE: PASS` — prior artifacts and claims were searched; the work changed from fixed amplitudes to a task-conditioned causal predictor, preserved invalid/null receipts, avoided rank drift, and marked the design-seen limitation before promotion.

The circuit corpus also gained the independent possessive-number DAS result and its failure-use diagnostic in the other lane; these are not counted as Codex serial throughput or as evidence for the aspectual actuator.
