# Hourly strategic review

ACTIVE_TRACK: CIRCUIT

Actual UTC: **2026-09-19T12:35:54Z**. Next deadline: **2026-09-19T13:35:54Z**. Previous review: [11:33 WEIGHT_FOLDING](HOURLY_STRATEGIC_REVIEW_2026-09-19_1133.md), 1h02m50s old. This is one live review, not an offline backfill. Historical `/workspace` paths were resolved against this checkout. Evidence cutoff is v597 at 12:21:34 plus the 12:25 mathematical review. No model was loaded; no agent, job, queue, service, timer, experiment/result, registry, dossier, commit, push, external contact, or `TYPED_FACE_EXTRACTION_V1` implementation was changed. The sole bounded CPU/process consequence was the truthful 12:35 review phase mark.

The preceding review declared `WEIGHT_FOLDING`; this review therefore alternates to `CIRCUIT`. The exact regional residual6-to-head9.8 fold was not executed. Concurrent work instead continued circuit breadth through gender, person, aspect, and temporal routes. Its useful receipts are audited below, while the exact fold is preserved unchanged for the next folding hour.

## Seven circuit targets and full goal

1. **Computational specification:** identify what is read, the operation/composition, what is written, and which downstream computations use it.
2. **Cross-boundary grouping and within-module splitting:** group pieces across native modules when a reader treats them as one variable, and split native modules whose parts serve different computations.
3. **Held-out and OOD prediction:** predict activations and behavioral effects on unseen inputs, task variations, and shifted corpora.
4. **Extraction or sufficiency:** execute an isolated circuit at a declared boundary with every retained port and background named.
5. **Selective manipulation:** removal, swap, or edit changes the intended behavior against matched controls while preserving unrelated behaviors and accounting for redundancy/interactions.
6. **Composition and reuse:** shared subcomputations serve multiple tasks/modules, and their joint behavior follows a tested composition law.
7. **Stable identification:** units survive corpus/document splits, gauges, and fitting restarts, or are defined by downstream operational equivalence.

Goal: a smaller transparent tensor program that **predicts held-out/OOD behavior, is extracted at a declared boundary, supports selective manipulation/removal, and composes/reuses**. Simplicity is the fifth separately measured property: independent stored values/bytes, product nodes, readers/writers, open ports/states, graph edges, and runtime. Lower replay error, rank, storage, variance, or CE cannot substitute for a missing behavioral property.

## What changed since 11:33

The strongest result is the pronoun-gender grouping, but it is not yet a five-property circuit. **Edits** v568-v570 reject an additive pattern/value or positional account: pattern-only is `-.072`, pattern+values is `.745` rather than the `.878` answer-write effect, and intermediate-position writes add `+.125` while the all-position intervention falls to `.745`. The thread correctly stopped that decomposition. v571-v576 identify heads 8.1 and 6.1 as post-noun copiers rather than noun self-copies; their post-noun writes carry `.256`, and combined with the readers' noun values close `.898`. This grouping replays at `.899` on 64 Pile pairs (v577) and `.891` on 63 later outcome-blind FineWeb pairs (v582). Those are useful claim-relative transfers, but the receipts report rows rather than distinct context cells and lack matched random splits, equal-norm removal controls, a standalone boundary, and a literal price. A closed joint face is not evidence that its interaction is small relative to the smallest piece.

The person route is an honest concentration failure. On 43 opened natural pairs all cue values close `.992`, four named readers close `.361` at the cue and `.412` at the answer, and early copies plus cue values close `.563`. On 36 Pile pairs the four writes close `.375` while all writes close `.917`. The preregistered seven-head set reaches `.549 < .60` (v593), so the route is correctly classified **spread** at the block-census depth limit. This is a screen/null, not extraction or a compact circuit.

The hour also sharpened comparisons: number reader values are current-state `.411` versus token-only `.075`; distant verb readers are `.347` versus `.027`. v595 first crashed on a stale `SETS_W` guard and was repaired, costing one failed managed execution. Aspect `since/by` was parked when sign constancy was only `.531`, correctly applying the lesson that an in-place swap needs syntactically aligned pairs. Temporal `will/had` passes sign constancy `.935` but fails the registered reader/carry/additivity predictions (`all=.789`, named readers `.254`, low/high `.664/.302`), so it is not a promoted route.

Evidence types remain separate: **fold** is native algebraic attribution, **edit** is a causal intervention with the recursive suffix, **response** allocates the response after an edit, and **fit** is predictive only. Fresh/opened/replay status is claim-relative; rows do not stand in for distinct context cells.

| Property | Current evidence | Verdict |
|---|---|---|
| Held-out/OOD prediction | Regional intermediate extraction has distinct-cell FineWeb/Pile evidence but packed FineWeb direction is `88/102`. Number transfers on 62 later documents. Gender composition replays on Pile and a later FineWeb set; person writes replay on Pile. None is a frozen end-to-end formula from declared inputs, and new receipts omit context-cell manifests. | **PARTIAL** |
| Extraction at a declared boundary | Regional residual6-to-attention8 and norm-closed MLP8 packages execute at declared intermediate boundaries. Complete head9.8/suffix and current number/gender/person routes remain open. | **PASS only at intermediate regional boundaries** |
| Selective manipulation/removal | Regional scoped fresh edits beat same-site nulls. Gender has low but nonzero number collateral (`.063` previously); current gender/person groupings lack equal-norm directions and three unrelated-reader controls at a complete boundary. | **PARTIAL** |
| Composition/reuse | Regional interaction/smaller is `2.3448` and random-split specificity fails. Gender copier+reader grouping transfers across three sets, but has no smallest-piece Mobius gate or matched random splits; person composition is local and incomplete. | **FAIL overall** |
| Measured simplicity | Regional components have local prices and a restricted head9.8 rank certificate. No deduplicated full regional price exists; number/gender/person have no program price. | **LOCAL ONLY** |

## Highest-value CIRCUIT action

Run the already identified **regional full-suffix five-arm factorial** before another behavior or unit census. The declared object is the complete city delta delivered to head9.8 after residual6 -> attention7/MLP7 -> head8.2 -> norm-closed MLP8, with the native blocks9-17 suffix, final RMS, unembedding, and softcap recomputed. First perform the forward response census so the suffix is not guessed. Then score: (1) baseline, (2) routing/key-only delta, (3) value-only delta, (4) both singles installed with their explicit product terms suppressed, and (5) the true joint native `QK1*QK2*V` delta containing all seven nonempty finite-difference products. Use structurally varied distinct context cells frozen before outcome, full logits/CE, at least three unrelated readers, 16 equal-norm same-site directions, and matched random splits. Normalize the Mobius interaction by the smallest **live** single and report the random-split distribution.

This action can change targets 1, 2, 3, 5, and 6. It passes only if both singles are live, the frozen signed prediction transfers, target movement is specific against the reader/full-logit controls, and the true interaction is small and specific relative to the smallest live single. Kill the grouping if signs fail, either single is dead, collateral breaches its frozen gate, or the matched random splits are indistinguishable. Exact algebraic closure alone is not a pass.

**Concrete cross-track connection:** gender edits show that a reader grouping becomes predictive only when its noun-value input and post-noun copier writes are composed; pattern/value and position-only partitions fail. That causal grouping tells the next regional fold which producer-to-reader transport terms must stay explicit. Conversely, the complete regional seven-product fold supplies the product-suppressed arm needed to decide whether routing and value form one circuit component or correlated alternatives.

**WEIGHT_FOLDING handoff:** next folding hour resumes the unchanged exact residual6-to-head9.8 output-delta program, retaining every reader-permitted attention/MLP self term, both ordered attention/MLP cross terms, MLP8 normalization/background, and all seven `QK1*QK2*V` delta products, with exact replay and a deduplicated price.

## Confounds, alternatives, and decision

The factorial must preserve baseline subtraction and replace-versus-zero semantics; native source order; frame/position/city/endpoint mixing; token/document difficulty and FineWeb/Pile leakage; RMS denominators, rotary rounding, causal masking, learned value mixing/re-entry, MLP bias, suffix curvature, final RMS/softcap; live-delta tripwires; numerical floors; and post-selection. Gender/Pile and person/Pile are not automatically OOD for every selection step. v582 has 63 rows but no receipt-level distinct-cell manifest. The duplicated `text`/`verb_slot` payload in v570/v576/v577 remains a semantic-schema warning.

Ranked alternatives: (1) the chosen regional factorial; kill it on dead singles, nonspecific interaction, or transfer/selectivity failure. (2) Freeze and test the gender copier+reader grouping with distinct-cell manifests, matched random splits, equal-norm removals, and unrelated readers; kill compactness if interaction/smallest-piece is not specific. (3) Canonicalize number/gender/person paths before new breadth work; this improves stable identification and prevents stale four-head claims but is documentation, not behavioral evidence. (4) Predictive-state quotients or signed composition bases wait until multiple fresh response environments exist. (5) New behaviors and opened head censuses are demoted. The regional route remains highest-value because it is the authority-designated depth target and directly attacks the program's current composition failure.

## Registry and dossier audit

`COMPUTATION_PATH_REGISTRY.md` still has `PATH-REGIONAL-CITY-001` ending at head8.2 and lacks canonical current number, gender, and person paths. `CIRCUIT_REGISTRY.md` retains stale four-head number/gender summaries beside newer narrative facts. `CIRCUIT_GRAPH_REGISTRY_V1.md` is still a 49-package generated inventory, omits `city_mlp8_norm_closed_v1` and all current number/gender/person routes, and labels two rows `four_trait_verified / next gap: none` without separately pricing simplicity. `MODULE_DOSSIERS.md` and the MLP dossier index preserve regional MLP7/8 prior art but do not cross-link number hub units 829/953/1030, gender copiers 8.1/6.1, or person readers/writers to claim-specific receipts. Primary ownership of `TYPED_FACE_EXTRACTION_V1` was respected.

Highest-priority missing cross-link remains **`PATH-SUBJECT-NUMBER-002`** (v338-v595, five pronoun readers, five verb readers, copier 4.5->5.3, hub/detector units, current/token branch comparison, 62-document transfer, invalid-run corrections, and missing extraction/composition/price gates). The new **pronoun-gender path** should link v549-v582, distinguish noun-value and post-noun copier roles, preserve failed pattern/position/magnitude hypotheses, and label each dataset claim. A **person spread/null record** should point to v579-v593 without implying a compact circuit. None was written during concurrent work.

## PAST_HOUR_TIMING

Window: **2026-09-19T11:33:04Z-12:35:54Z**. Twenty-nine successful receipts v568-v597 finished from 11:36:18 to 12:21:34. Their authoritative `serial_seconds` sum to **58.604s**, median **1.970s**, with **351 declared forwards**. Result cadence is about **97.0s per successful candidate** over the 46.3-minute result span. This is cadence, not active labor. v595 also occupied about two runner seconds in a failed stale-guard execution before repair. Parallel durations were not summed.

- **Design:** unknown; registered predictions exist, but no science boundaries were marked.
- **Implementation/data building/indexing:** unknown; 29 sibling result-producing runners plus the v578 row build appeared. The third gender set reports 63 pairs; build runtime and distinct-cell count are absent.
- **Validation:** 58.604s of successful receipt runtime includes model execution checks; one approximately two-second failed v595 run is separately visible. Active repair time is unknown.
- **Model execution:** 58.604s successful serial runtime plus the failed v595 attempt; runner logs give start/end seconds. No model was loaded by this review.
- **Interpretation:** unknown; board claims and Review44 are timestamped, but duration is not instrumented.
- **Documentation/review:** unknown; Review44 committed at 12:07 and the mathematical review began at 12:25, but commit gaps are not labor time.
- **Idle/blocked:** unknown. No interval is labeled idle from a commit or receipt gap.

The current work-date phase log is `RESEARCH_PHASES_2026-09-19.jsonl`. Before this review it contained only sparse hourly review boundaries through 11:33 and covered none of v568-v597's design, implementation, validation, model execution, interpretation, or publication. The helper appended one truthful **12:35:54 review** boundary. Sparse marks are not continuous labor, and no missing category was inferred or backfilled.

## PROCESS_IMPROVEMENT

Ranked by likely time saved versus cost: (1) a declarative intervention runner with row loader, semantic arm schema, scorer, slot assertions, cell manifest, price fields, and receipt writer could plausibly remove much of the roughly 97-second candidate cadence and would prevent stale guards/duplicated payloads; medium cost and requires cross-family equivalence tests. (2) canonical number/gender/person path records likely save 5-10 minutes per handoff at low documentation cost. (3) mandatory `started_utc`, `finished_utc`, evidence status, and distinct context-cell fields are low cost and would make timing/generalization claims auditable.

No shared code or registry was changed: the shared runner/library and narratives are under concurrent modification, while a new ad hoc checker would worsen duplication. The recent 12:25 mathematical review independently reached the same conclusion. The bounded nonconflicting repair was the truthful phase mark; it costs negligible time and improves future review-boundary coverage without pretending to measure the unmarked work.

## Explicit verdicts

- `TRACK_ALTERNATION: PASS` — previous `WEIGHT_FOLDING`; current `CIRCUIT`.
- `TRACK_PROGRESS: FAIL (TRACK DRIFT)` — the preceding hour produced substantial circuit edits, transfers, honest nulls, and a useful gender grouping, but no concrete residual6-to-head9.8 folded-path receipt as declared.
- `CEREMONY_BUDGET: PASS WITH MEASUREMENT CAVEAT` — 29 scientific receipts and 351 forwards dominate visible output; successful model execution was under one minute, but design, authoring, validation, interpretation, and review labor are unmeasured, so their proportions are unknown.
- `NOVELTY_LESSON_GATE: FAIL` — sign-constancy screening correctly parked the malformed aspect pairs and the person kill bar stopped a weak compact claim, but the hour still opened new behaviors during the regional depth mandate, produced another stale-guard crash, extended the one-runner-per-candidate family, and left canonical paths/cell schemas unrepaired.

**Highest-value action now:** execute the preregistered regional forward-response census and full-suffix five-arm routing/value factorial on frozen distinct cells with matched nulls, smallest-live-piece interaction pricing, unrelated-reader/full-logit controls, and all seven native `QK1*QK2*V` products represented in the true-joint arm.
