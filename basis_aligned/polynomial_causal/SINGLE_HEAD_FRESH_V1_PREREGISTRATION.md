# Fresh confirmation of the frozen single-head normalization formula

Freeze before model outcomes: SINGLE_HEAD_FRESH_V1_ROWS.json (20 cells,40
sequences,240 endpoint rows). Ten authored constructions vary colon/quotes,
unquoted prose, line breaks and indirect speech. Liverpool/Boston and
Glasgow/Dallas were not used in normalization selection; not globally new cities.
The six spelling endpoints are unchanged. CPU prior-prefix overlap is zero.
No prompt selection on native capability: failures remain in the panel.

Candidate is typed_face_single_head_norm_v1.py as frozen by the opened screen:
g2=mixed residual8 + head8.2; s2=mean(g2²)+epsilon; delta9=d +
D[((Ld)*(Rg2)+(Lg2)*(Rd)+(Ld)*(Rd))/s2]. Preserve native block8 reentry,
full QK1*QK2*V donor-retained write d at strength .5, and recursive native suffix.
Token tables are extended exactly from checkpoint weights. No fitting.

Arms: native, full native8 half-write, exact full-response reentry, candidate,
and16 Gaussian block9 residual-direction nulls =800bodyforwards/300seconds.
Null seed=17092500+1000*k+context_id; per-position candidate write norm matched;
paired UK/US signs, same support and same site. Four unrelated readers unchanged.
Use HEAD2_MLP8_CROSS_FRESH_V1_BASELINES.json unchanged: cue-constant and fixed
14-feature OLS, trained earlier on native8 half-write effects. Candidate has more
native information and compute than those text predictors; disclose asymmetry.

pred_a: full reentry vs native8 <=1e-4 max absolute AND <=1e-5 relative L2;
local full-response delta error<=1e-4; finite; outside-mask zero; random norm
relative error<=1e-5; exactly800forwards. Candidate target is full native8 effect.
pred_b: every family has >=18 of24 capable pairs (native UK-US margin>=.1),
candidate target RMS>=1e-5 and target prediction error<=.35relative L2.
pred_c: each of4 reader RMS changes<=.5 candidate target RMS EVERY family.
pred_d: >=.75 of capable pairs attenuate and mean attenuation>=.02 EVERY family.
pred_e: target RMS>=2times random median and beats>=15of16 nulls EVERY family.
pred_f: candidate error<=.8times EACH frozen baseline error EVERY family.

No gate relaxation or averaging failed families away. Report effective cells,
per-family values, seeds, prices, all failed gates. Freshness ends once outcomes
are inspected. Passing supports prediction and selective paired manipulation;
not donor-free removal, new endpoints/corpus, independent composition or a
complete token-only circuit. Two residual7 native inputs and suffix remain.
The candidate evaluates one attention head and omits two baseline MLP readers;
current full instrumentation bundle retains full arrays, so no storage reduction
claimed until a pruned standalone package is built and replayed.
