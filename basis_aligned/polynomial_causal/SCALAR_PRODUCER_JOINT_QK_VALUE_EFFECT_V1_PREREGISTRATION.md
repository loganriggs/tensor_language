# Signed end-effects of the joint-QK/value generation split

2026-09-12, before evaluation. CPU four-corner scalar audit shows template-dependent routing/value interaction; general value-only hypothesis misses. Test final effects rather than assuming intermediate error has identical behavioral significance.

Reuse48regional rows and original physical components. Same five-forward harness as signed MLPbridge: native, native dynamicjoint removal, jointly generated9field removal, value-only generated9field removal, routing-only generated9field removal. In every edited arm component8 is physically removed and all later states recompute. Scalar9fields come from already frozen SCALAR_PRODUCER_JOINT_QK_VALUE_V2_ARTIFACT.pt (joint1,value2,routing3). Both QK1 and QK2 always form the routing port together. Native full head9 background remains unchanged by this substitution; this tests generation of the subtracted component, not direct editing of wholeheadQK matrices.

Reference D=dynamicjoint margin minus previously frozen-pristine9joint margin, EACHtemplate24signed rows. A: baseline/dynamicjoint replay<=1e-4relative and jointly generated field's D error<=1e-4relative. B: value-only D error<=10%relative in EACHtemplate. C: routing-only D error>=25%relative in EACHtemplate. Report all errors and effects; Bmiss is expected from one intermediate family but is not proof without execution. C is a falsifier for treating routing alone as the explanation.

Price240single-prefix forwards<=22tokens,120seconds, no optimization. No newOOD or input closure, no revision of newline failure. An exact-interface success plus value-only miss retains joint routing in the conditional interaction program.
