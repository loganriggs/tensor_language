# Rung449 preregistration: complete mixed104+MLP0 teaching consequences

Status: registered after rung448's object/price audit and before any complete-candidate TEACHING forward pass. GPU
execution must use the managed queue. SEALED_CONFIRMATION is forbidden.

## Correction and object

Rung448 remains a valid local-MLP0 diagnostic, but it cannot label rung445 rows whose IDs and prices denote complete
compiled candidates. Rung449 keeps the five frozen ranks, documents, intervention, partner, and metrics, while
installing the **entire** mixed104+MLP0 candidate used to derive each frozen price.

The full candidate contains mixed104's exact learned front/middle/tail program and rank104 Q/K factors at layers2:18,
plus the rank-r context-input MLP0 program. It is rebuilt by the hash-pinned historical harness and source algorithms.
Prices are exactly531,632,438 /532,959,542 /533,623,094 /534,286,646 /535,613,750 scalars for ranks256/384/448/512/640.

## Exact computations

On96 TEACHING rows, collect per-token cross entropy for native `N`, native attention16-mean knockout `NKO`, native
plus the physical14,984-value MLP16 partner `Q`, complete candidate `P`, complete candidate plus the same knockout
`PKO`, and complete candidate plus partner `PQ`.

Removal compares `R_P=CE_PKO-CE_P` with `R_N=CE_NKO-CE_N`. Composition compares the actual joint change
`J_P=CE_PQ-CE_N` with the additive prediction `A_P=(CE_P-CE_N)+(CE_Q-CE_N)`. Report cosine, normalized error,
norm ratio, CE damages, and fixed rows0:48 versus48:96 waves exactly as rung448 did. The target top quartile and
collateral bottom half are frozen from `|R_N|` before candidate metrics.

The full harness is executed in three fresh child processes—unablated, knockout, partner—inside one managed queued
parent. This prevents permanent legacy compile hooks from leaking between conditions. Child processes are descendants
of the managed job, not independent GPU launches. Each condition must independently rebuild the same rank maps,
rank104 Q/K identity, and complete active replacement set.

## Frozen predictions and null

- **A — complete-candidate instrument:** all bank/row/fit/factor/local-receipt/compiler/producer hashes hold; native
  replay max is zero; all five exact prices/ranks are recovered; all three builds reproduce the same mixed104 identity;
  knockout and partner each fire exactly120 final-candidate calls; complete candidate CE differs materially from the
  local rung448 candidate; SEALED is never loaded.
- **B — structural ordering:** Spearman of rank with negative removal normalized error and negative composition
  normalized error is at least`.50` for both.
- **C — learnable variation:** removal and composition normalized-error spans are each at least`.015`.
- **D — row stability:** the two fixed48-row waves give candidate-order Spearman at least`.70` for both errors.

**Strong null:** any complete-candidate identity/instrument clause fails; native removal, partner, or every candidate
is inert; both spans are below`.003`; or either wave correlation is negative.

A/B/C/D with no null makes MLP0-context one eligible complete-candidate teaching family. It fits no predictor, opens
no sealed outcome, and licenses no semantic, compression, OOD, or adoption claim. Literal new deployed values are zero.
