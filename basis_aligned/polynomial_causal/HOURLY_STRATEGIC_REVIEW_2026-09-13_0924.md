# Hourly research review — 13 September2026,09:24UTC

Circuit interpretation targets: explicit read/compute/write specification; cross-boundary grouping and within-module splitting; held-out/OOD prediction; extraction/sufficiency; selective manipulation; composition/reuse; stable identification. The full goal remains a simpler executable model/circuit program with prediction, extraction, removal and reuse. The user's interaction-compression direction continues to override the older compression-primary prohibition.

## What changed since08:24

The shared-output-subspace fit fails own-term regional preservation. A balanced paired-output objective converges across three starts and improves contrast fidelity, but still fails one behavioral group and worsens total coefficient error. Shared features plus sparse private corrections improve substantially under joint fitting, yet remain worse than the rotated sparse-entry baseline at matched bytes. These are narrow representation failures, not absence-of-structure claims.

The sparse candidate's favorable retained-port regional result does not extend to the full native head. Matched full-head six-pair effect error is10.24%on regional text and20.51%on FineWeb, with material sign reversals. Designated-output and magnitude-slice counterchecks do not rescue it. This separates text, port and output scope; FineWeb is not a fresh OOD corpus here.

Respecting the earlier circuit's rational amplitude coefficients exposes an exact conic relation: diagonal/self products use five vectors instead of six. Full normalized-branch execution passes the160historical-prefix native panel, preserving attention, mixed terms, bias and normalizers. But a matched full-branch cost check loses to direct MLP: the three main dense contractions remain. The identity is valid; that execution schedule is not an adoption win.

One response writer is fixed across contexts. Caching its projections and self-product gives real repeated-preparation savings after correcting a non-contiguous-input slowdown. Combined cached five-bank preparation is1.27–1.35times faster after setup. A split bank stores one shared fixed node plus four varying vectors per context; at128contexts bank-plus-entire-cache storage falls18.44%. These are local representation/preparation improvements, not whole-model savings.

Removing that shared fixed node from all changed branches fails preservation materially: regional target effects change8–18%, FineWeb targets18–33%, and controls also change. The corresponding1152-vector component is exported with a checkpoint-free amplitude/normalizer executor. It is a port-conditioned computational primitive, not an independent text-level or selectively semantic circuit.

## Confounds and interpretation

Counterchecks addressed objective imbalance, joint versus sequential fitting, tiny references, all-pair versus designated-output probes, retained versus full-head ports, exact source-support versus numerical noise, warm versus cold execution, tensor layout, per-product versus aggregate numerical error, actual storage aliasing and shared versus copied constants. The old failed bars and unfinished initial fits remain recorded.

An executed signed-contribution audit now adds an important interpretation: the fixed node's removal-defined contribution has negative projection onto the regional target interaction in all four groups, while aligning positively with the controls. Regional target cosines range−0.136to−0.867; control cosines0.842to0.962. These are projections, not additive percentages of causal explanation. The node is not simply a supportive spelling feature; compensating paths matter. FineWeb target alignment varies. Removing a material component is not automatically beneficial or selective.

## Throughput and workflow

Authoritative evidence includes18completed owned commits since the prior review, native full-head transfer tests (64and96forwards), the160-prefix five-bank fidelity test, and the160-prefix node-removal test, plus CPU fitting and counterchecks. Managed execution for those four native tests took2.05,2.15,11.27,12.61seconds. No newly identified semantic circuit or full four-property completion was produced. The exported component is a narrower extraction receipt with explicit supplied ports.

CIRCUIT_FOCUS: PASS under the compression override. Native transfer limited candidate scope, and node removal/extraction connected an exact shared computation to behavioral effects.

CEREMONY_BUDGET: FAIL TO VERIFY. Phase transitions remain incomplete, so exclusive scientific versus implementation/review time and full prior-art-to-dossier latency cannot be reconstructed honestly. Native execution time is not serial research latency. Repair is procedural: use the existing phase marker before each next science, implementation, validation and publication block; no new framework. Keep detailed results in one primary note rather than append essays to every pointer.

NOVELTY_LESSON_GATE: PASS. Earlier sparse support, module dossiers, response bases and fixed-writer interfaces were checked. Existing exact source-star results warn that materiality is not reusable semantic identification. The new extracted node is conditioned on a specific upstream writer/amplitude interface, not a rerun of those unconditional groupings.

## Next actions and stopping evidence

1. Prioritize understanding the material fixed node's signed contribution and its consumers over further preparation microbenchmarks. The first CPU signed audit is already executed. A supportive/selective label is rejected by the current control and signed-alignment evidence; retain its precise arithmetic specification.
2. Test scoped consumer interventions and prediction on a genuinely held-out panel using the existing amplitude generator. Failure of prediction or large collateral effects would block semantic promotion, while still allowing exact computational reuse. Supplied normalization/background must remain visible in extraction claims.
3. Pursue further producer-conditioned compression only when it removes an expensive contraction or a meaningful dependency. More output-frame tuning or global dense-map compilation is lower priority given current prices and failed transfer.

Next hourly review:10:24UTC. Latest mathematical review is08:29; next is11:29. Both managed runners are alive and the GPU queue is empty at this review; no completed job should be restarted. The durable goal remains active.
