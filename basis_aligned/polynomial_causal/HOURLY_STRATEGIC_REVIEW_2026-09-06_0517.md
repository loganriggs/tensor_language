# Hourly strategic circuit review — 2026-09-06 05:17 UTC

## Circuit-first reminder

The target remains a predictive, composable, manipulable, simpler transparent tensor program. A new experiment is worthwhile only if
it removes a runtime dependency, resolves a material term into explicit tensor operations, tests transfer, or fails closed on a real
boundary. Screens are not a license to hide native computation behind a circuit label.

## Material progress since 04:17

- Compressed the material suffix attentions on a frozen 16/16 split. Four-head sets retain 96.23% at block11 and 101.57% at block15;
  unopened singleton confirmation then shows H3 retains 81.44% and H5 retains 83.22% of those compact-set increments.
- Localized the two dominant heads to three-role banks. The first scientific artifact terminated invalid because its projection audit
  compared a query-only delta over the full sequence. An independent frozen diagnostic found query errors of 7.6e-6/4.8e-6 and all
  large error off query. A zero-forward evidence-class audit released `11H3 <- {determiner,period,self}` at 90.34% retention and
  `15H5 <- {period,determiner,self}` at 87.03%, explicitly labeling the post-outcome instrument repair.
- Released executable program v3 with typed role partition, suffix source-term, selected-head projection, and suffix crossing APIs.
  It passed 33 deterministic equation/rejection cases and stores no fit.
- Factorized MLP11/15 into exact Left-change, Right-change, and bilinear-interaction responses on a split design. Postexecution review
  caught that v1 supplied all six source roles instead of the preregistered three-role banks. V1 and the dependent program-v4 release
  were append-only superseded as invalid; a corrected design audit and sole-correction v2 were run rather than narratively rescuing them.
- Corrected MLP v2 passes all five gates in the exact source contexts. MLP11 Left+Right retains 101.16% of its all-three increment;
  MLP15 Left+interaction retains 110.05%. Both have positive A1/A2 increments.
- Released valid executable program v5 directly from valid v3 plus corrected MLP v2, explicitly excluding the invalid v1/v4 lineage.
- Tested the simplest resid10-to-resid18 sparse recurrence: checkpoint lambda carry plus only corrected block11/15 writes. This is a
  valid null at 78.99% of dense recovery versus the frozen 85% bar. Both block11 and block15 remain necessary in A1 and A2.

## Efficiency and integrity accounting

The source screen used one model load for both heads and both splits. The MLP screen likewise shared captures across two boundaries;
the corrected v2 changed only the source-bank argument after an exact audit, leaving rows, arms, selection, thresholds, and price fixed.
The sparse suffix test cost 18 forwards instead of six independent boundary factorials. No fit, backward, update, threshold relaxation,
or post-outcome row change was used. Invalid artifacts and their dependent release remain immutable and explicitly superseded.

The sparse null prevents an overaggressive claim that blocks11/15 alone replace the suffix. It also makes the next experiment sharper:
the unexplained portion is a six-block new-write bank, not an undefined native suffix.

## Current strongest valid program and honest boundary

Executable v5 is the current valid release. It resolves:

1. the two-term MLP4 writer at `last`+period+determiner;
2. attention5 H7/H1/H6/H8 source transport;
3. carried/attention/MLP crossings through blocks6-9 and attention9 H1/H4;
4. block11 H3 from determiner+period+self plus MLP11 Left+Right;
5. block15 H5 from period+determiner+self plus MLP15 Left+interaction.

It remains paired-causal and checkpoint-dependent. Native new writes at blocks10,12-14,16-17 and the final readout are not yet replaced.
The sealed new-construction capability failure remains binding; there is still no standalone, free-form, native-margin, full-logit, or
whole-model claim.

## Next one-hour decision

Run one frozen split singleton/leave-one-out screen over the omitted block set `{10,12,13,14,16,17}` while keeping the corrected
block11/15 program fixed. Select two blocks on the first 16 rows and test them on the disjoint 16. Require both 70% recovery of the
missing increment and 85% of the all-omitted total program. If it screens, factor only those two blocks; if null, retain the dense
suffix recurrence and reconsider whether three-block selection or a direct low-dimensional recurrence is the simpler path.

Do not launch six independent component factorials before this compression result. The next three-hour mathematical review is due
around **2026-09-06 05:30 UTC**; the next hourly strategic review is due around **06:17 UTC**.
