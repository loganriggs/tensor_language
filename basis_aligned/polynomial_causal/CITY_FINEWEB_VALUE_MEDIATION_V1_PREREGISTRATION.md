# Causal screen of the folded MLP8→head9.8 value response

Opened FineWeb panel, no fresh or null promotion. Six-piece native fold passes replay but fails omitting MLP8 and omitting quadratic magnitude gates. Test those nominated terms as explicit interventions before further suffix pruning.

Five arms: native; exact head8.2 full-city swap; same swap minus folded MLP8-mediated value response in head9.8; same swap minus its quadratic value term; same swap minus direct attention-edit value term. MLP8-mediated sum is left_cross+right_cross+quadratic+MLP8_normalization. Block9 normalizer response is retained. At head9.squared_attention input, subtract the nominated128-channel value difference from head8 only; both native key factors and all other heads remain unchanged. Preserve head8 swap and full later suffix. This is path-specific value mediation, not native MLP8 ablation.

Use saved exact-swap delta and six-piece native fixtures from CITY_FINEWEB_MLP8_VALUE_FOLD_V1. No coefficients fitted. CPU2threads,200sequence-equivalent forwards/90block calls,180secondcap.

pred_a:native/fullswap scores replay previous<=1e-4abs AND1e-5relative;captured removal is supported only at original destinations;unselected head values unchanged exactly;requested subtraction replay<=1e-5relative;finite/counts/CPU.
pred_b:subtracting MLP8-mediated value response changes target RMS by>=.20fullswap effect RMS on frozen reversed-document group, with target RMS>=1e-5.
pred_c:subtracting quadratic changes target RMS by>=.10fullswap effect RMS on BOTH reversed and other groups.
pred_d:subtracting direct value response changes target RMS by>=.20fullswap effect RMS on BOTH groups.

Report target/control movements, error against fullswap, capable-pair attenuation and per-document signs for allarms. These are necessity screens at declared factor boundary, not proof of independent composition, selective removal, native-module ownership or fresh portability. Predicates may fail despite large attribution norms; preserve that distinction. A convincing effect needs separate registered norm-matched value-boundary nulls and fresh confirmation before promotion.
