ACTIVE_TRACK: CIRCUIT

The previous07:26review was WEIGHT_FOLDING. This hour produced exact native attention-factor controls, derivative-path decomposition, and a prospectively validated smaller modal reader. Signed-source testing then exposed a number-branch limitation. Handoff for the next folding hour: compare backward quadratic observables against repeated compressed-state propagation; paired-block composition improves but does not resolve the signed failure. Do not start another rank sweep or promote native grouped execution as compression.

The full goal remains a smaller transparent computation with a specification of what it reads/writes, cross-module grouping and within-module splitting, held-out/OOD prediction, extraction, selective intervention, compositional reuse, and stable identification. We have conditional output prediction/extraction and some source reuse, not a standalone generator, unique feature dictionary, or installed residual-state replacement.

TRACK_ALTERNATION: PASS — CIRCUIT follows WEIGHT_FOLDING.
TRACK_PROGRESS: PASS — concrete folded attention/reader paths, native controls, CPU extraction and failed composition tests are recorded, not just a rank/reconstruction sweep.
CEREMONY_BUDGET: PASS with measurement limits — checks stayed focused (small CPU identity tests and shared enqueue checks). The available timing record does not support an exact split of authoring, interpretation and validation time; no invented category totals are claimed.
NOVELTY_LESSON_GATE: PASS — current registries and source/derivative dossiers retain failed state reuse, native-dependency prices, normalization and signed-intervention limitations.

PAST_HOUR_TIMING: `HOURLY_TIMING_2026-09-20_0828.json` inventories18receipts,17available explicit claim-to-receipt intervals, median20.70s, total57.59s reported inner experiment runtime. These intervals exclude earlier preparation and subsequent interpretation; they are not complete candidate lifecycles or proof that the remaining time was idle. Eight-row export uses24source cases, not24independent texts. No other-agent job was displaced.

Changes: exact finite readers did not rescue a fixed width8 state basis. A baseline-gradient modal branch plus sparse nonlinear number passed fresh source tests. It was CPU-exported with native context/gradient/input costs. Attention12 plus all token-local MLP derivatives subsequently passed a second fresh text-combination panel. Carry-only and other simpler cuts failed. Signed/mixed source testing fails on negative B and A-B; restoring dense quadratic pairs still fails. Native projection every two blocks reduces worst error to11.47%; removing all internal projection reaches5.49%, with an exact oracle replay.

Confounds checked: original native targets match exactly across sparse/dense/group controls; independent scoring matches; output projection was separated from initial-interface effects; corrected versus uncorrected number branches were distinguished; source-budget normalization exposes cancellation sensitivity; modal derivative pruning is not a primal module ablation. Old copied runner scope/price fields are metadata debt, not authority over frozen row hashes and executed arms. v693/694 repair those fields for new receipts; old receipts stay intact.

PROCESS_IMPROVEMENT: the shared selected-reader helper now computes six MLP pullbacks at only the readout token, followed by one attention pullback. CPU unused-port perturbations verify dependencies. Full native baseline generation and number-branch contexts are still charged; do not count this as an end-to-end speedup. New controls must use explicit current scope and measured runtime tensor counts rather than inherit stale prose.

Next CIRCUIT action: matched random interventions at the same post11 boundary, with each token's perturbation norm and support matched to source A/B. Existing random controls were pre11 joint-source controls, so they do not establish this specificity. Freeze8seeds, test native target/random effect separation and prediction of control effects. The model should not receive a selectivity claim simply because modal effects are small on one intervention family. If native source specificity fails, the source definition needs refinement; if prediction alone fails, retain the source evidence but restrict the conditional predictor's validity.

Alternatives: another rank/calibration sweep repeats prior failures; deeper native grouping supplies an upper control but no simpler circuit. A quadratic observable baseline may directly address lost products and is preserved for the next folding hour. The current circuit control gives more information about selective manipulation at low cost.
