# Generated MLP8 context boundary certification

Freeze native attention8 generator: all9heads, six dense maps,74-token first-value
table generated from normalized embedding/block0 initial reentry/c_v0. Preserve
full QK1*QK2*V, rounded rotary, RMS and inherited/current mixture.
Inputs current8,donor_city8,rho8,token IDs and prior scalar/mask arguments.
Context g=rho8*current8+attention8(current8,first_values(tokens)). No native g.
Three arrays remain, but one is scalar perposition:38,048native state floats atT32.
Full23,975,681float program charged. No compression claim.

40opened fixture sequences, native/native8/compiled =120bodyforwards plus40
local full-attention8 generator calls,300seconds. Same half-write/masks.
pred_a: native/half-write reference replay<=1e-5abs, native jointdelta error
<=1e-4relative, outside zero, finite,120bodyforwards.
pred_b: compiled readouts<=1e-4maxabs AND<=1e-5relativeL2 versus native8.
pred_c: target and four controls effect errors<=1e-3relative in everyfamily.
pred_d: isolated copied package reproduces40native jointdelta fixtures<=1e-4
relative each, zero strength zero, unknown token rejected.
No fresh/corpus/OOD or causal-composition promotion. Token vocabulary explicitly
bounded; a future extension must compile new values from weights before outcomes.

Only change: head8.2 maps are views of fullattention8 maps; no duplicate stored maps. Same gates.
