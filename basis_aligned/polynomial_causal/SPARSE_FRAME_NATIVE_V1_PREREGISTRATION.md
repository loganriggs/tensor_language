# Frozen graph native screen

Prepare while the two-start weight fit runs; bind/queue only after its terminal
receipt is interpreted. Select the arm with highest coefficient capture, never
native-effect quality. Preserve its convergence/stability failures. The existing
128endpoint morphology cache is developmental, not fresh/OOD validation.

Primary: compare the frozen optimized4096edge graph and initial spectral4096edge
graph against the exact bias-free MLP16producer/MLP17producer-producer write.
Use full native U, FP32 RMS and30tanh in quartic_frozen_native_score_v2. Replay
the earlier complete-path reference effects. No common-logit invariance assumed.

- pred_a: compiled reader/write and old reference replay<=1e-8/1e-5;
- pred_b: every family's swap relative RMS<=.1, sign agreement>=.9,live>=4;
- pred_c: every family's mean absolute removal CE disagreement<=.02;
- pred_d: every family's relative residual-write error<=.05.

Secondary, separately reported: select exactly one node by maximum incident
coefficient energy in the frozen graph. Compare its selected incident edges with
the exact full quadratic star on that same reader. If z=H^(-1/2)p and a is its
unit frame column, the reference is B(p)-B(p-H^(1/2)a(a^Tz)), divided by the
unchanged native denominator. This is local node removal, not a global upstream
ablation. Apply identical fidelity bars to this separate component; a pass cannot
repair the primary graph verdict or establish task semantics. No testing other
nodes conditional on its outcome. Initial=reference for the secondary replay.

Report signed reference effects, price and locality. Wholegraph conditional
compiled cost20,643,840floats+8192indices, plus native U/background. A compact
node graph pays native L16/R16 plus4608times active reader count plus1152times
incident edge count, and edge indices. No fullmodel compression or OOD claim.
