# Hourly strategic review — 2026-09-07 14:14 UTC

## Circuit interpretation targets

The program must identify what information is read, what operation is performed, what is written, and which downstream readers consume it; merge cross-module pieces and split native modules when the computation requires it; predict held-out/OOD behavior; execute a sufficient interface or isolated circuit; support selective removals/swaps/edits; compose and reuse shared subcomputations; and remain stable across corpora, gauges, and fitting restarts. The end state remains a predictive, composable, manipulable, and literally simpler transparent tensor program. Compression, rank, or variance alone cannot substitute for these targets.

## What changed since 13:14

Six terminal circuit receipts landed by 13:47, at a median inter-receipt gap of about 6.2 minutes: Claude's v202-v205 behavior screens plus two temporal/is-was hidden-support screens. The activation-conditioned screen showed that top25 hidden units beat equal-size static-weight support, but even top50 failed temporal behavioral sufficiency (`.681`). The exhaustive 32-arm five-site lattice then found the unique minimal functional mask: top50 at MLP0 and full support at MLP1/2/3/6. It recovers `.7512/.9131` task behavior with response RSE `.01465/.01452`, median control KL `.00084`, and zero flips. No arm with at most three full sites passes.

The important correction is structural: maximum higher-order Mobius energy is only `.00138`. The support is broad but almost perfectly additive, so an executed greedy combination is justified; the result is not evidence for a hidden conditional interaction. MLP0's discarded half changes temporal behavior by only `.00059`.

The first construction-holdout execution was instrument-invalid before evidence because a CPU-native response cache met CUDA task modes. The retry moves only the eight cached response tensors, adds no forwards, and is live at the unchanged 25-forward cap. A gauge-safe helper now converts paired task subspaces into canonical shared/contrast axes and scores actual hidden writer weights; 3/3 tests pass. The user's DAS regularization hypothesis is now quantified and a four-arm whole-family tournament is preregistered and implemented, but remains behind the primary circuit queue.

## Throughput and systems audit

Serial path this hour: activation hidden screen (39 forwards) -> complete five-site factorial (60 forwards, 127.5 seconds) -> construction-holdout confirmation. Scientific design/execution produced two substantive circuit receipts. Validation/review did not dominate, although one avoidable cache-device mismatch cost a failed 25-forward setup. The shared runner stayed serialized; Claude's v206 intentionally occupied about 19 minutes for a 15-member family screen, then the mask30 retry began immediately. There was no idle GPU gap attributable to an empty queue.

`CIRCUIT_FOCUS: PASS` — the hour split a native MLP internally, established additive cross-site composition, added gauge-safe weight/subspace grouping machinery, and expanded the behavior-screen inventory.

`CEREMONY_BUDGET: PASS WITH ONE INSTRUMENT DEFECT` — safeguards were smaller than the scientific work, live forward accounting held exactly at 60/60, and the device failure was repaired without another compiler or audit layer. Future native-cache consumers must normalize device at their interface.

`NOVELTY_LESSON_GATE: PASS` — prior results and active claims were searched; activation reconstruction was not treated as function; weight scores remain candidate generators requiring causal patching; and the DAS design uses complete-family selection rather than another row split.

## Confound audit

- The `.75` temporal bar is barely crossed by mask30, so stable identification is unproven until the live construction-family confirmation lands.
- Tiny Mobius interaction is established only on the selector population; a shifted population could change interaction structure even if mask30 transfers.
- Per-unit participation energy omits cross-unit cancellation. It ranks writers but does not certify an additive unit decomposition; complete subspace patches remain required.
- Exact `Down^T Q` maps are gauge-safe, but source task modes must also be defined operationally and cross-fit before calling a direction shared or task-specific.
- DAS complement inertness can be memorized. KL's large full-vocabulary improvement and noise's reader improvement are positive evidence, but only whole-family cross-validation can adjudicate them.
- Control KL is small and flip-free, but answer-margin thresholds do not cover every unrelated behavior; promoted circuits still need broader collateral panels.

## Competing routes and ranking

1. **Finish frozen mask30 construction holdout.** Changes stable identification and OOD prediction. Kill it if mask30 loses function or diverges from full by more than `.03` on either task.
2. **Canonical task-pair source directions pulled through exact hidden weights, followed by full patches.** Changes within-module splitting, cross-task reuse, and computational specification. Kill it if shared/contrast rankings are not cross-fit stable or their executed patches do not selectively transfer the corresponding task response.
3. **Whole-family no-reg/noise/KL/noise+KL DAS tournament.** Changes stable subspace identification and selectivity. Kill the current objective if pooled step zero still wins both complete-family folds; call rank one inadequate if no moved checkpoint remains target-feasible.
4. **Finite causal-response modes/Hankel interface.** This remains the fallback if activation/DAS coordinates are unstable: operational equivalence can identify states without unique neuron ownership. It dominates only if task-mode weight patches fail stable prediction.
5. **Further hidden percentile sweeps.** Demoted: they would refine a compression curve without changing circuit semantics.

The current route survives because the factorial supplied a rare strong simplification: broad source support is additive, and one internal MLP split is already dispensable. The highest-information next measurement is its no-reselection family transfer, followed by shared/contrast weight-coordinate patching rather than finer arbitrary sparsity.
