# Native all-degree repeated-input metric audit

Use the frozen converged interfaces and exact mixed_repeated_contraction_v1 kernel. For producer degree k=1..4, sample2048independent tuples of4-k background vectors and2k producer-input vectors, Rademacher seed73210, batches64. Background absorbs producer bias but remains a formal external variable; all producer occurrences share the actual quadratic MLP15 map. No text or fitting.

A: every native graded diagonal agrees with existing source-gain polynomial coefficients<=1e-8; grade4 oracle agrees with independently implemented eighth contraction<=1e-8. Grade1 estimated relativeerror agrees with its formal Gram prediction within.02+3estimatedSE inbotharms, because with one producer factor its coefficient Gram suffices exactly. B: at least one grade2/3 error differs from formal prediction by>.05+2SE in either arm. C: relativeSE<=.1 for all8error estimates and runtime<=180seconds. All estimates/grades reported; grade4 repeats the prior purepath comparison on new probes and does not repair the mixed-degree B bar. Watchdog240seconds,0bodyforwards.

Save per-probe reference/error energies. Full-U writerGram is fixed. Compute paired delta-method ratio uncertainty, with no capability or row selection. The grouped polynomial coefficient contains binom(4,k), so its raw squared norm equals binom(4,k) times the formal slot-count grade norm before additional producer-input symmetrization; that multiplicity cancels from relative errors. A miss narrows the fixed-frame metric explanation, not all possible optimizer outputs or background dependence. This is an instrument/metric audit, not a circuit promotion.
