# Hourly strategic review — 2026-08-30 10:45 UTC

## Whole-model position

The strict ledger has not changed:

- certified removable storage: **5.348245316%**;
- named deletion-CE: **10.923302467%**;
- unexplained deletion-CE: **4.72714 nat = 89.076697533%**;
- terminal actions jointly passing extraction, selective removal, and OOD: **0/68**.

The completed signed causal-response FIT is a new measurement interface, not yet an
explained fraction.  Its 55,475,273-byte bundle and receipt are preserved.  Response
values remain sealed because the first full training transaction audit is exact
NO-GO.  The active a8 two-extra-seed confirmation still owns 5.86 GiB of GPU memory;
seed `20260833` completed its 16 directions in 778 seconds and the job continues.

## Largest gaps

1. **No lawful factor result yet.**  The response tensor exists, but no value may be
   used until the repaired transaction receives an independent GO.
2. **No validated whole-model composition.**  Local and independently installed early
   MLP simplifications interact strongly; MLP2 can compensate conditionally.
3. **No terminal circuit quality.**  Equality copying is exact/extractable but fails
   the narrow collateral certificate; none of 68 registered actions jointly passes
   extraction, selective removal, and OOD.
4. **No semantic meaning for continuous codes.**  Rank or response reconstruction does
   not identify editable atoms.  Gauge stability, sparse use, and fresh interventions
   remain necessary.
5. **The majority of deletion CE is unexplained.**  The certified storage and named CE
   fractions cover different currencies and must not be added.

## Candidate actions considered and pruned

- **Open the FIT bundle directly:** rejected; it would bypass the independent NO-GO
  and invalidate later selection claims.
- **Run another local MLP MSE/SAE fit:** rejected as redundant and causally weak.
- **GPU-time the accelerated optimizer now:** deferred because a8 is using the GPU;
  contaminated timing would not answer production cost.
- **Generic token Hankel or information bottleneck:** rejected; prior token splices
  were OOD and neither supplies the missing executable causal interface.
- **Full ten-circuit powerset:** rejected before the smaller sparse interaction and
  pair-composition gates.
- **Treat the new hierarchy toy as bilin18 evidence:** prohibited; it is a planted code
  gate only.

## Ranked top five

1. **Repair and independently re-audit the training transaction.**  Highest
   information gain because it unlocks the already paid-for causal response tensor;
   CPU-only, falsifiable by the exact four previous attacks, and required by every
   response-factor branch.
2. **Publish the sanitized 229-document training input after exact GO.**  This is the
   missing interface between collected intervention evidence and mathematical program
   fitting.  It exposes no validation or EVAL values.
3. **On GPU release, time the exact production-shape accelerated fitter, then run the
   frozen 51-fit CP/shared-private grid plus price-matched SVD and mean controls.**  A
   timing receipt prevents another multi-day CPU mistake.
4. **Diagnose the fitted frontier with tree cut ranks, quotient Jacobians, and signed
   sparse NTF.**  These distinguish a genuine hierarchy, editable identifiable atoms,
   and sparse per-document use from mere low MSE.
5. **Freeze the frontier, then test validation and unseen pair composition.**  Use
   block-D-optimal calibration and the small polarization/Volterra pilot to expose
   compensation and interaction failures before EVAL.

## Highest-priority action executed

Prospective Amendment 6 repairs every independently reproduced blocker without opening
FIT values:

- success now replays authority, audit, all sources, parent, input, manifest, and the
  original lock immediately before publication;
- terminal plus receipt/failure are prepared as hardlinks inside a private directory
  and published by one atomic directory rename;
- failure records the current exact protected bytes as well as the originally attempted
  authority digest;
- production loading reads only the canonical authority and independently verifies its
  GO, source closure, ancestry, namespace, and FIT parent;
- the model facade and both transitive `jacclust` runtime files join the closure.

The original 67-test suite plus new attack tests now gives **76/76 passing** in 34.72
seconds; the focused repaired loader/lifecycle suite is **16/16 passing** in 9.70
seconds.  This is still not self-authorization.  The repaired source must be committed,
pushed, and independently audited before the no-argument lifecycle runs.
