# Frozen uniform-token length transport screen

September14 10:21UTC. Native historical transfer failed even under all12 raw
output energy (fit/baseline1.346). Contrast choice alone is insufficient.
Test length sensitivity before changing an optimization objective.

Freeze lengths5,32,128. For each length, four batches of4 sequences drawn with
seeds170229000..170229003:16contexts perlength. Same seeds are not assumed
paired sequences, because shape changes the random draws across rows.
Use the existing actual hierarchy and native injection site. Original sparse
and frozen producer-gradient fit only; no new fitting or program selection.

A: finite values and producer replay<=1e-10. B descriptive hypothesis:
fitted normalized totalerror energy<=.95baseline for everylength. Report
perbatch values, total and contrast ratios, ownmixed-relative error.
Failure suggests length sensitivity; a pass does not explain native transfer.
This small screen does not estimate a statistically precise population ratio.
CPU120s/two threads, no adaptive extension; timeout is inconclusive.
