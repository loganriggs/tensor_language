# Framing versus quoted-clause sources of O

Use the48 frozen article-corrected rows. Token ID366 is the unique opening-quote
boundary in every row. Without looking at model outcomes, split the already
validated post-city/nonfinal O sources into framing sources after the city
through the quote, and the three copied-clause sources after the quote before
the final self token. Preserve pre, city and self separately for exact accounting.

Six arms are native; remove framing, clause, their post union, self, or all O:
288 body forwards,180seconds.

- `pred_a`: source recomposition is at most `1e-10`; native, post, self and all-O
  arms replay `ODD_CONTEXTUAL_POSITIONS_NATIVE_V1` within `1e-5`.
- `pred_b` (framing relay): framing-only cue error versus all-O is at most35%
  and clause-only error is at least50% in each family.
- `pred_c` (clause relay, opposing): clause-only error is at most35% and
  framing-only error is at least50% in each family.
- `pred_d`: the winning B or C source class has control-effect RMS at most50%
  of its target-cue RMS in each family. If neither wins, D fails.

The first CPU receipt incorrectly demanded bitwise-zero post addition and is
preserved as failed; V2 applies the board's frozen `1e-10` tolerance and passes
at `5.47e-17`. Native registration uses V2. No token is deleted and upstream
states remain native. This screen distinguishes semantic source classes on the
existing panel; it does not establish unseen-template transfer, independent
input generation, a unique semantic unit, static compression or quantization.

