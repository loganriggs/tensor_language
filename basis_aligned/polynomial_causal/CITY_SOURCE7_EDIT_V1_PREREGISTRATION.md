# Causal test of the MLP7-dependent city-write group

Select the group once from CITY_SOURCE7_CONTRAST_V1: every ordered city-side
K1*K2*V term containing MLP7 in at least one slot, including inherited-value terms
whose key slots contain MLP7. Query states and all RMS denominators stay native.
This is a source-term intervention at attention8 output, NOT removal of MLP7 as a
module. Its complement contains every other term plus native rounding residue.
Group+complement equals the complete native city-removal write exactly.

All twenty opened Pile documents, forty sequences, six spelling probes and four
control readers remain. No fitting, row filtering or fresh confirmation. Arms:
native, complete city-value removal, MLP7-dependent removal, complement removal,
sixteen random directions matched to the MLP7-group norm separately at each
position and injected at the same attention8 output. Full MLP8 and later suffix
are recomputed for every edit. Null seeds18094500+1000*k+context_id, same paired
seed and opposite cue signs. Support is the existing destination mask.

Predictions:
- a: native and complete-removal scores replay CITY_FULL_STRENGTH_V1<=1e-4
  absolute AND<=1e-5 relative; finite readouts; null norm error<=1e-5; exactly800
  forwards; group+complement relative sum error<=1e-10; support zero outside mask.
- b: MLP7-group target RMS>=.15 of complete-removal target RMS; >=.80 positive
  attenuation on capable contrasts; all four reader RMS values<=.5 group target.
- c: RMS of joint effect minus both singles <=.35 times the smaller single's
  target RMS. This is a composition screen, not identification: random-split
  specificity and fresh-panel composition remain untested even if it passes.
- d: complement target RMS<=.85 of complete-removal target RMS. This tests the
  prediction that MLP7-group carries a meaningful fraction of this causal effect.
- e: MLP7-group target RMS>=2 times the random-null median and beats all16 nulls.

Opposing outcomes: the source group has a selective, material causal effect and
composes approximately with its complement; or native-write attribution does not
predict downstream service, selectivity, or interaction. Preserve each failed
gate independently. Report signed paired attenuation, effect fractions, all
controls and interaction. Price800body forwards,300seconds, existing weights and
native captured sources. This test alone closes no native input dependency.
