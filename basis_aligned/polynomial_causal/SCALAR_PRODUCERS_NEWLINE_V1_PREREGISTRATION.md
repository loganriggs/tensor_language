# Dedicated newline-service control

2026-09-12, before new panel model evaluation. The scalar producer pair passes fresh regional tests, but head8.2 already belongs to a newline-setter dossier. A newline endpoint on spelling prompts was not an adequate preservation test.

Freeze16new contexts:8lists and8forms. They contain line breaks and stop after a complete list item or field, before a potential next newline. Next-token endpoint is newline ID198 versus comma ID11; full-vocabulary newline cross-entropy is also measured. Fixed text only, no model-based filtering. No regional candidate refit or revision.

Native capability EACH8rowfamily: mean newline-minus-comma margin>=.2, >=6/8 positive margins, mean newline CE<=5nats. To demonstrate that the control engages the documented service, actually remove whole native head8.2 at all positions and rerun subsequent layers; require mean newline CE increase>=.02nats and >=4/8 positive increases EACHfamily. This wholehead intervention is a positive control, not the proposed regional component manipulation.

Candidate arms: remove the compiled head8.2 regional contribution, head9.8 contribution, or both at the four-reader regional interface, with original current/query/norm/background states retained as in the validated regional interventions. EACHarm/family must have mean absolute newline CE change<=.02nats and max absolute change<=.1nat. Report signed change and newline/comma margin alongside absolute preservation. Positive-control or capability failure makes service-preservation inference inconclusive, even if candidate damage is small.

Instrument: original compiled-read native replay<=1e-5 and no-edit full final logits/CE replay<=1e-4relative where nonzero; check fullhead write subtraction against exact value/output slice. Native capability and fullhead positive control use real model forwards; this is not a zeroed output-reader surrogate. Fullhead removal must propagate recursively into later layers. Budget32single-row body forwards (16native+16wholeheadcontrol), plus4suffix evaluations per row,180seconds; no fitting. Native GPU only through managed lane1. No new experiment has run at preregistration time.

A pass supports selective preservation of this tested newline service under the consumer-specific edge manipulation. It cannot certify global wholehead replacement, arbitrary unrelated services, independent extraction, or all-token OOD. Keep existing dossier evidence and all negatives.
