# Hourly circuit review — 2026-09-09 14:49 UTC

Circuit interpretation targets: explicit inputs, operation and consumers; grouping
across modules and splitting within modules; fresh/OOD prediction; executable
extraction with declared background; selective removal/interchange; joint composition
and reuse; stable identification. The full goal is a simpler predictive, composable,
manipulable tensor program, under the reconstruction handoff's appended criterion.

Since1348: equality-router v1 was instrument-invalid; exact-position v2 rejected the
fixed equality rule; its norm-gain diagnostic rejected the magnitude-only explanation;
local self/previous transport was a valid null. Contextual answer-history then passed
its four source-decomposition predicates, with full29-way extraction and a portable
program. Fixed-position fields missed the stronger payload/retargeting and factor
specialization bars. The narrower hop-state compiler also failed, despite physically
removing weights and passing instrument checks. These are seven main result receipts
(including two diagnostics/repair stages), plus export replay, and the immediate
query/key factorial. They are not seven identified circuits.

The new query/key factorial uses frozen books and already opened populations on CPU:
query-only KL .822–1.573, key-only1.497–2.624, both1.485–2.514. Neither side supports
the semantic code. The exact output interaction is49–56% of joint error; errors are
not independent. CPU/GPU parent query-KL disagreement is below9e-16. Stop symbolic
state guessing, including a one-sided rescue or a larger role/feature vocabulary.

The source decomposition is real progress: repeated higher-hop answers depend on
earlier matching answers, while novel hop1 depends on bindings. But the remaining
native contextual producers are opaque. A discovery of a compact operation has not
yet replaced those producers. The next highest-information test is to trace which
upstream additive producers supply the final Q/K/V factors, using exact live
normalization and full-model effects. This is a causal dependency question, not an
activation-rank or token-classification objective.

Alternatives ranked: (1) finite upstream-producer/factor ablation atlas to identify
the missing contextual dependency; kill a simple isolated path if effects remain
distributed or fail fresh source controls. (2) Return directly to full bilin18 with
the exact source/factor machinery; higher relevance but confounded by learned value
mixing, final norm/softcap and unresolved native-background closure. (3) More semantic
codebooks or rank searches: demoted because the recent failures and prior full-rank
certificate give no new operation hypothesis. An exact query/key error decomposition
already ruled out the obvious one-sided rescue.

Confounds: source-history controls originally differed in age; fixed-position tests
addressed it but missed their stronger bars. Their deterministic first-eligible foil
is mostly token0, limiting generality. Head0/3 nomination used observed sensitivity,
while compiler evaluation used fresh seeds. RMS gains remain live; RoPE uses cached
absolute positions. Logit composition is affine only at this final layer, not in
probability or arbitrary upstream edits. Small-model findings are not bilin18 findings.

CIRCUIT_FOCUS: PASS with corrective action — one causally supported source mechanism,
but failed compact compilers must not become an endless state-feature sweep.
CEREMONY_BUDGET: mixed — most elapsed time was reasoning/authoring, execution seconds;
eight receipts/hour meets the rough ten-minute screening cadence, but this excludes
the older inactive gap. No precise authoring-versus-validation timing was logged, so
do not claim a measured overhead fraction. Reuse loader, scorer, readout, runner and
controls for the next atlas; no new framework or artifact-packaging campaign.
NOVELTY_LESSON_GATE: PASS — incorporate the fresh lesson that field sensitivity does
not identify a sufficient semantic state, and that Q/K replacement errors interact.
Known local-span, radius, symmetry and state-code nulls stay closed.

Next strategic checkpoint after15:49 UTC; mathematical remains after16:48 UTC.
Both managed runners are healthy. Main GPU queue is empty after the1.84s hop test;
this is authoring-bound work, not a GPU blocker. CPU error-factorial completed9.49s.
Concrete continuation: claim and preregister the exact upstream-producer/factor atlas,
then implement its reusable live producer decomposition before any new model outcome.
