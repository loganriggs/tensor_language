# Key direction versus RMS gain in the backward join

The full overlapping source-port screen rejects a sufficient shared-key-only
replacement. Its K1+K2 intervention nevertheless damages both query origins,
while V-only partly compensates. This does not yet show that the backward
write supplies address information: the changed keys include both a direction
change and a shared RMS gain change. Resolve that confound before promoting an
address interpretation. Do not repair the prior port or normalizer-only nulls.

Same512 opened requests,32 worlds, two overlapping orders, both query origins,
four hops; no selection. Native source x, backward write v, gains
g0=RMSgain(x), g1=RMSgain(x-v). Keep final queries, values and skip native.
For both final key factors, execute the complete2x2 factorial:

    native       K(x) g0
    direction    K(x-v) g0
    gain         K(x) g1
    full keys    K(x-v) g1.

RoPE uses the original source positions. Keys refer to all four final heads;
no head/role subset. This can reuse source_port_interchange_reference.execute
with the existing final features: gain-only multiplies native keys by g1/g0;
direction-only divides changed keys by that ratio. Use independent native
projection hooks fed the corresponding raw-state/gain products as the oracle.

A instrument: four exact outputs abs<=1e-9/relative<=1e-10; native and full-key
queries replay OVERLAPPING_JOIN_PORTS_V1 rows SHA256
9816f643cb7f42c578ab9a7db1006fdeedb94a4670e81a2cd508660352228678.
Zero-write identity, equal-norm directional change, and radial-change
direction/gain cancellation controls distinguish the operations. Check finite
gains and unchanged source support; all hooks restored.

B fixed direction-dominance screen: for each population and each hop3 query,
direction-only signed gold effect/full-key signed gold effect in [.9,1.1],
gain-only absolute ratio<=.1. A full-key denominator below .001 invalidates
that panel; never drop it. Report full29-vector effects and the exact logit
interaction between direction and gain for every population/query/hop group,
including errors relative to the full-key effect with floor1e-6. No fitted
gain, best head/hop or choice of factorial cell follows a failed prediction.

A B pass would nominate directional matching as the main task-level key use;
it would not restore the failed full-write sufficiency claim. If B fails,
carry direction and normalization as coupled consumers in the explanation.
No native coefficients are removed; all387968 export constants and native
background remain priced. These are explicit projection-port interventions,
not a claim that a residual edit can realize every hybrid.

CPU threads2, batch8 FP64, alarm180s, tensors below256MiB, no GPU or model
training. Reuse the existing cohort, executor and scorer. This protocol begins
the next concrete action by fixing the exact factorization and falsifiers;
implement the small native-hook audit and execute it next.
