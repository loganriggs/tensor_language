# Frozen contextual confirmation of the paired producer-metric program

12 September2026. Register before any model outcome on the new contexts. Weighted program SHA11bf30b2d258e27ccd42a6568fc4973dccf9124e6d19ec008167c83985ed3c1c is primary; ordinary outer32 SHA090998427b7f3265c8c558fdf71bcd5190c3f9b84aef46f8df019ff4e91d9999 is a matched comparator. Both use32terms per output and the same explicit native producer/output interface. No fitting or selection among them from this test.

Rows are512pairs across8fixed surrounding contexts,4task families and16previously used lexical groups, with new core constructions. Authority digest4a9ff5762fc4d7d12385630ab84e67e4bf85f7329f4a3d60e3f57e7f63621f37. All32family/context cells must be reported separately;512pairs do not constitute512independent lexical samples. This tests new context/construction transfer, not new lexeme or corpus/pretraining OOD. Rows are generated before model evaluation and remain fixed if native capability is low.

Reuse the exact capture/scoring implementation from the previous panel, adding the frozen ordinary-program comparison.128batch8prefix forwards plus two first-batch physical/manual controls:130body forwards,1040sequences,lengths14–24. Managed lane1 only, bound sources/rows/checkpoint,600second outer limit. All actual counts checked. First-batch hooks replace the exact selected component with the primary approximation while retaining other native computation. Record exact reference and both approximate writes for audit.

Registered primary predictions:

- A: source/row identity and finite execution; physical/manual tail relative error<=1e-5, exact component algebra<=1e-8, actual130forwards/1040sequences.
- B: all32cells have swap relative RMS error<=.10, sign agreement>=.90 and>=4live pairs at1e-4 cutoff.
- C: all32cells have removal CE mean absolute disagreement<=.02nats.
- D: all32cells have physical-write relative error<=.05.
- E: all32cells have native answer-versus-foil capability>=.75 on each base/donor side.

Score the ordinary comparator under the same cell thresholds descriptively; its failure cannot rescue a primary failure. No averaged score replaces the all-cell requirements. CE added above native is damage, lower is better. A passing result supports contextual fidelity of this partial path, not independent extraction, selective linguistic naming, shared-factor identification, or the full goal. Both programs retain76,096 fitted values plus15,925,248 shared native parent values and the declared scale/normalization/background. If a cell fails, preserve it and diagnose the failure without deleting its examples or fitting to the panel.
