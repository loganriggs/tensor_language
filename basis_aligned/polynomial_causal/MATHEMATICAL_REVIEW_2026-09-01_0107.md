# MATHEMATICAL REVIEW — 2026-09-01 01:07Z

Convention (§2135): all damage numbers are CE added above the real model; LOWER IS BETTER.

## Grounding (what the night established)
The floor law: one model-wide, maximally delicate cancellation mechanism in the score maps' fine singular
band — any deficiency anywhere costs ≈0.052–0.062 with per-circuit fingerprints correlating at 0.993+
(§2348–§2363). Two-sided asymmetry (values smooth, scores binary, §2357). Top-k compute sparsity additive
at four anchors (§2362). Manipulability: knockouts transfer; the honest effect metric is under adjudication
(baseline-subtraction controls 273/274 in flight; instrument lessons §2359/§2371: mechanism tripwires and
live config tripwires are now mandatory).

## Top three mathematical moves
1. **The mechanism as a rank-one object in FUNCTION space (perturbation theory).** Object: the per-position
   damage vector d = cev − CBASE ∈ R^256k of each band-deficient config. The fingerprint evidence (rank
   correlations 0.993+ at circuit grain; identical class tables) suggests the stronger statement: the d
   vectors of DIFFERENT deficiencies are nearly COLLINEAR — the mechanism is a single direction in function
   space, δCE ≈ α(deficiency)·v. Theorem-shape: first-order perturbation with a near-degenerate dominant
   functional mode. Assumption that may fail: circuit-grain correlation without position-grain collinearity
   (mixture of a few modes). MEASURABLE CONSEQUENCE beyond reconstruction: if collinear, a SINGLE fitted
   correction direction (an in-frame refit at the OUTPUT site — legal under the exactness type system,
   §2227-family) could repair ALL band damage at once and legally break the 0.055 floor. CHEAPEST
   FALSIFIER: save cev vectors for two deficiency configs, compute cosine of their d vectors (bar: ≥ 0.95).
   EXECUTED: rungs 275/276 (cev-dump instrumented repro runs) queued; cosine analysis next wake.
2. **Signed-composition algebra for interventions (causal abstraction, properly).** The additivity dispute
   (§2370) is an artifact of composing UNSIGNED member means: |a+b| ≠ |a|+|b|. The correct object is the
   signed per-position dCE vector; intervention composition should be tested as vector addition in CE
   space. Falsifier: with cev dumps for {config}, {config+KO} and the real-KO row, test
   d(config+KO) ≈ d(config) + d(KO_real) position-wise (cosine of residual). Shares the rung-275/276
   instrument; extends the knockout receipts next.
3. **MDL/prequential certificate of the registered set** — a description-length statement for the eight
   Pareto points (bits of config vs nats saved). Honest but low action value now; PARKED.

Pruned: further gauge/invariant work (exercised §2309–§2319), Hankel/automata (no new object), band-subset
geometry (closed §2361/§2363), information-bottleneck variants (no composable interface here).

## Executed
Rungs 275/276: instrumented repro runs of the frontier (corner+tail-r96) and corner+tail-r120 configs
that SAVE their cev vectors — the shared instrument for moves 1 and 2. Preregistered as repro claims with
cev dumps; the collinearity bar (cosine ≥ 0.95) is registered HERE for the follow-up analysis.
