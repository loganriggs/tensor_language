# Hourly circuit review — 10 September 2026, 02:06 UTC

The interpretation targets remain: specify inputs/operation/outputs and
consumers; group across native modules and split within them; predict fresh
and OOD behavior; extract an executable circuit with explicit background;
predict selective removals/swaps; demonstrate composition/reuse; and identify
units stably across data splits and permissible changes of coordinates.
The full goal additionally charges all opaque parameters, state and adapters.

## What changed and throughput

The managed logs give four valid receipts in the preceding hour:
01:06:18 operand-identity null,01:25:36 shared fixed-query null,01:34:42
direct-embedding query null,02:00:21 query-source atlas. Inter-receipt gaps
are19m18s,9m06s,25m39s; median19m18s exceeds the10-minute target. Executor
times are2.083,2.284,2.180 and13.179 seconds. There is no GPU bottleneck.
Four receipts are four screens/diagnostic nulls, not four identified circuits.

The latest source-atlas claim at01:58 reached its managed result at02:00:21.
It reused rows, model loader, scorer, enqueue preflight and projection hooks.
The longer interval included restoring the original handoff/pilot, literature
checks and deriving the19-source normalized tensor at the due mathematical
review. We did not record attention-minute profiling, so cannot give exact
thinking-versus-authoring fractions. New code is one bounded math/capture
primitive plus a thin runner, not a second model-forward framework.

CIRCUIT_FOCUS: PASS — actual query-consumer edge edits and joint-effect
analysis clarify circuit inputs and the failure of additive source accounts.
CEREMONY_BUDGET: PASS for the latest basic screen — shared preflight and
focused algebra/tiny-native controls stayed within its roughly2-minute
claim-to-result block. The longer derivation is science, not a repeated
artifact-validation framework; precise whole-hour fractions are unavailable.
NOVELTY_LESSON_GATE: PASS — original pilot, previous fixed/direct query
nulls and older source-position weight tests checked; global rank scans,
native-neuron semantic assumptions and posthoc threshold repair rejected.

Throughput repair: reuse the same saved factorial cells for the next analysis
and existing source-gain representation for the next control. Do not author
another task dataset, full forward backend, or broad source subset atlas.
The CPU source/rest audit already converts all four saved arms into an exact
interaction identity. Next bounded CPU work separates numerator cross terms
from normalization-induced interaction before deciding another native run.

## Interpretation and confounds

No singleton source passes any full panel. Neither task has an omission
above .10 native paired-effect norm in BOTH its panels, so the preregistered
robust dependency-overlap hypothesis fails. Empty sets do not establish
different task mechanisms or absence of dependency. Sources are fixed native
query-input edges, not global upstream module removals. All native parameters
and all initialization remain charged; no adoption or fresh circuit identified.

Exact source/rest identities on saved paired margins close0. Summing separate
omissions misses joint query removal by73–81%. That is evidence against that
additive approximation; a correct nonlinear circuit can still compose.
The source/rest partition changes for each source, so these are not19-player
Shapley values. No additive source ranking or causal share is inferred.

Confounds: native paired baseline is explicitly subtracted; full logit
comparisons share one vocabulary frame; KL is not summed as causal evidence;
all source masks and zero controls are live; FP64 algebra, native unity and
independent raw MLP8 omission pass with native full-logit errors<=1.53e-5.
Opened rows and outcome-selected strongest-source examples are labeled.
No dataset filtering, refitting, selected-source threshold changes or
unmeasured OOD claims. Shared text difficulty could align task response
profiles; no profile similarity is promoted to a common operation.

## Ranked next moves

1. Separate bilinear numerator interaction from RMS-induced interaction
   at the fixed query-read interface. Same source tensor, no rank search.
   Changes computational specification and grouping: a shared normalizer
   and a shared bilinear feature are different operations. Kill a proposed
   bilinear cross-factor explanation if the interaction comes mainly from
   normalization on the registered domain. Start with an explicit toy
   counterexample now; native measurement remains separately unregistered.
2. Once the interaction source is localized, test a small mechanistically
   specified joint source program through both task consumers on fresh text.
   Kill it on paired-effect, unrelated-output or joint-edit failure; do not
   nominate an arbitrary source subset from this atlas as confirmed.
3. Reader-equivalence/invariant-subspace methods may group consumers, but
   must preserve norm dependence and declared source edits. Defer a new
   global CP/rank/commutator sweep: it cannot distinguish the immediate
   computational alternatives as cheaply as the existing tensor tool.

Both managed runners healthy. Next hourly03:06; next math04:49. Goal active.
