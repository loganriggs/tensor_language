# Native attention reader fold

Path: nine output contrasts backward through the native suffix11–17, retaining each attention's Q1/K1/Q2/K2/V factor paths and RMS radial derivatives. Reader at a pre-attention state equals residual reader plus five explicit adjoints; cached first-layer values remain fixed at this intervention boundary. No softmax. Shared factor weights, rounded RoPE and native FP32 RMS epsilon retained in FP64 evaluation.

Opened OODv2 four contexts, both nominated sites, 23 source directions, nine outputs. No training or thresholds selected from this result. Counts12prefix,4double suffix,36 joint reverse calls. Instrument: explicit attention forward and backward maximum errors<=1e-8; sum-six readers and previous pre11 source-gradient replay<=1e-8. Null: contextual reader is distributed across branches; no one frozen branch can be dropped while preserving all gradients. Screen candidate omission: each of six branches may be removed separately, report relative Frobenius source-gradient error for every output/role/template/panel; pass only if every cell<=10%. This is derivative preservation, not finite causal adoption. Opposing prediction is one whole branch removable at that bar. Do not infer neuron-level sparsity from a branch failure.

Literal price unchanged native attention weights; five derivative branches plus residual. Export6*9*1152 reader values per nominated site for diagnostics, charged as context-derived arrays, not fixed model parameters. Full native forward states and backward oracle generation remain charged. CPU planted oracle/finite-difference already passed. Reuse shared context builder and native observables. No circuit claim without independent extraction, finite intervention, fresh prediction and reuse.
