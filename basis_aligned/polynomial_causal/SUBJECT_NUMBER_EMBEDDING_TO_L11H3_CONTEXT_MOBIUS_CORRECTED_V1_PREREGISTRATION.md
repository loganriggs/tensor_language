# Subject-number context Möbius corrected replication V1

Registered after identifying an inherited indexing bug in the prior removal
authority: `attractor_pair[2-number_index]` selected the stratum string for
singular subjects and a plural noun for plural subjects. The prior factorial
results remain valid only as a lexical-context decomposition, not as an
opposite-number-attractor assay.

This replication uses `attractor_pair[1-number_index]`, asserts that all 128
attractors are atomic nouns with number opposite to the subject, and otherwise
keeps the same 32 subject forms and four fully crossed context templates.

Measure the same frozen-coordinate-removal L11H3 scalar and the same zero-sum
subject/context/Möbius decomposition. Run two 64-row batches (`near/behind` and
`under/above`) twice each. Instrumentation requires duplicate alpha maximum
error at most `1e-4`, removal geometry within `1e-5`, exact prices, and the same
synthetic additive and rank-one interaction fixtures within `1e-12`.

The additive hypothesis is sufficient only at raw relative L2 at most `.15`.
Otherwise interaction is material at RMS at least `5.0` and at least `1e5`
times duplicate-capture RMS. Rank one is a provisional reusable candidate only
at interaction-energy capture at least `.70`. A corrected qualitative
replication additionally requires additive-error drift from the flawed-context
assay at most `.05` and rank-one-energy drift at most `.15`; this comparison is
explicitly secondary to the corrected result.

Repeat the 256 within-subject context-column permutations as a specificity
control. Labels remain factorial indices, never licensed circuit inputs. This
assay makes no OOD or behavioral claim.

Price: eight partial forwards over 512 sequences, 256 CPU permutations, no
logits, fitting, gradients, or parameter updates.
