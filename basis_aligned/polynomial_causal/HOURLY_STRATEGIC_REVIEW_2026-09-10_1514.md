# Hourly circuit review: 14:14–15:14 UTC, 10 September

The interpretation targets remain explicit inputs, operations, writes and consumers; cross-module grouping and within-module splitting; held-out/OOD prediction; executable extraction; selective manipulation; composition/reuse; and stable identification. The full goal also requires fewer independently specified computations with all opaque parameters and adapters charged. No result this hour meets the complete criterion. Original bilinear handoff/pilot, not better_math_ideas, remains the authority.

Recorded at the first safe authoring boundary after15:14. Previous goal turn PROGRESS78b9c5b5e. The user requested a consolidated report and organization, then insisted on checking existing module dossiers, attention OV/QK input-sharing analysis, and both token and structured unembedding views. Those instructions explain the changed allocation of work; they do not turn documentation into a native circuit receipt.

## What changed

Two managed native receipts completed in this window:

-14:17:41: P/R output partition retained the two-behavior donor dissociation but failed selective mean removal and additive endpoint effects.
-14:59:01: the two-MLP token-reader fold predicted live MLP16-swap vocabulary effects within4.6–5.6%, with CE-change MAE below.0034nats. The fixed16-leaf unembedding shared-mean hierarchy left94–98% relative error. The target swap effect was slightly negative, so this predicts an opposing component rather than extracting the target circuit.

CPU evidence: loss cancellation was decomposed into mean harm and improvement; the exact trained OV pullback showed19/26 complementary per-head value-input spaces contain the saved scalar reader, with seven single-head exceptions; paired resampling confirmed the token/hierarchy error distinction. Per-head row-space membership is not full-block redundancy because head contributions can cancel.

The dossier audit found an important process failure: MLP17 calibration, quadratic readers and normalized context-gated response already had substantial evidence. The new two-consumer and backward-fold tests are narrower extensions. Canonical MLP16/17, module lookup, component records and the latest report now state this. No new claim may infer novelty from a missing local filename or the current experiment vocabulary.

## Throughput and overhead

The two native receipts are2480seconds apart (41.33minutes), exceeding the600second target. Their executor times were1.58 and1.34seconds. The bottleneck was authoring, analysis and requested reporting, not GPU capacity. The saved phase audit has75.44% coverage:1914.75seconds science-labelled,801.03review-labelled and884.21unrecorded. Mixed labels include report work; they cannot establish a precise research/ceremony labor budget.

CIRCUIT_FOCUS: PASS with scope limits—the tests address within-module splitting and signed causal prediction, but do not license extraction. CEREMONY_BUDGET: recorded review is smaller than recorded science; overall budget remains incompletely observed. NOVELTY_LESSON_GATE: prior omission found and repaired through canonical dossiers and startup lookup; avoid another generic MLP17/calibration/rank rediscovery. The next implementation reuses the native greedy forward and changes only factor exposure; no new general compiler or fitting framework.

## Next decision and falsifiers

1. Execute the now-registered full QK1/QK2/V donor/live factorial through both P and R. This addresses the user's specific suggestion that shared OV inputs can coexist with different score dependencies. Frozen54forwards864seq, all existing rows, no fit/head/rank/gain selection. Parent full-P/R and route/value bridges prevent silently changing intervention semantics. A failure of the opposite-score-half criterion closes a simple division by the stored global factor labels, not all head-specific modes. QK1/QK2 can be exchanged per head, so any passing fingerprint still needs gauge-aware head-level confirmation.
2. Use the explicit backward token-reader program to study operationally shared token-specific computation. The16-leaf mean-only predictor is closed; no cluster-count tuning on these outcomes. Its largest leaf contains71% of vocabulary, so do not conclude that all unembedding structure is absent. Shared arithmetic plus explicit residual readers may be useful, but without reduced structural cost it is only a representation.
3. Defer the immediate-MLP interaction draft. User steering prioritized input sharing and readout folds, and the module-specific novelty audit for a14-MLP sweep was incomplete. Do not enqueue that draft without resolving those prerequisites.

Confounds checked: task recovery versus full-vocabulary prediction; signed CE versus absolute preservation; native endpoint accuracy versus a positive donor denominator; exact fixed-attention replay versus live prediction; per-head input overlap versus cross-head cancellation; raw centroid means versus logits; recognizable clusters versus sufficient causal predictors; QK label symmetry; retained native background and coefficient costs.

Latest math review13:49 remains current; next mathematics16:49, next hourly16:14. No duplicated review or reminder stands in for executed research.
