# Hourly strategic circuit review — 2026-09-08 16:22 UTC

## Circuit interpretation targets and full goal

A successful decomposition must specify what is read, computed, written, and consumed; group
semantic variables across native boundaries while splitting mixed modules; predict held-out and OOD
effects; execute or expose a sufficient interface; permit selective removal/swap/edit under
redundancy; compose and reuse shared subcomputations; and remain stable across corpus, gauge, and
fit perturbations. The program-level goal remains a smaller literal tensor program that is jointly
predictive, composable, manipulable, and simpler in storage, compute, edges, state, and program
description. Rank or reconstruction alone does not satisfy it.

## What changed since 15:22

Four Codex circuit receipts advanced the temporal/is-was route.

1. A physically removed A11 reader causes stable `.222/.224` FIT/HOLDOUT loss and M11 causes
   `.110/.114`; restoring each module's complete output rescues exactly. Joint removal is mildly
   redundant rather than the positive interaction suggested by the Shapley allocation. This
   corrected credit assignment into directed physical mediation.
2. Within A11, H3 alone recovers `90.70%/90.32%` of the complete A11 reader loss, with exact parent
   closure. This is a genuine native-module split rather than a whole-attention label.
3. A FIT-defined shared U8 retains about `94.25%` of unseen reader-delta energy and causally recovers
   A11/M11 at `.993/.923` on HOLDOUT. Folding U8 into the checkpoint produced exact writer and H3
   contractions plus an exact `8x8x8` M11 tensor. Its frozen top-32 native factors causally recover
   `.809` on HOLDOUT. This is the first compact shared weight-space computation in this route.
4. A history-disjoint v18 capability bank was constructed. The novelty audit rejected duplicated
   v15/v16 cue families before model access; unseen This-year/Last-year and This-week/Last-week
   replacements then passed all eight native cells and both panels at 16/16 jointly capable rows.
   The no-refit U8/H3/top-32 transfer is preregistered with P/C selectivity controls.

No result was silently repaired. The reader joint-interaction prediction missed and is preserved;
the initially proposed duplicate constructions were rejected before registration; and v18 has not
opened a causal outcome.

## Confound audit

- **Baseline subtraction / frame mixing:** target effects are within-row writer-minus-native and
  reader-loss contrasts, separately reported for A1 and A2. P and C remain separate controls.
- **Nonlinear loss composition:** the A11/M11 joint discrepancy showed why Shapley credit is not a
  physical edge law. The transfer uses output-margin vectors and separately tests each reader.
- **Shared token difficulty:** v18 native capability is perfect, but that alone cannot identify the
  circuit; causal effects are still sealed.
- **Leakage and post-selection:** U8 values, H3, and the top-32 order are literal v17 artifacts.
  V18 permits no SVD, refit, factor reorder, dose calibration, or threshold change.
- **Dead knobs / tripwires:** the transfer will restore the stored basis byte-for-byte, bind every
  input artifact by hash, count hook calls, and independently replay the writer arm.
- **Precision floor:** exact closure bars remain `1e-5`; substantive causal bars are far above
  float noise.

## Route comparison

The frozen construction transfer remains highest-information. It directly distinguishes a reusable
weight-defined computation from a v17 activation-regime description and changes prediction,
stable-identification, cross-boundary grouping, selective-manipulation, and reuse targets.

Alternatives are lower priority now:

- Fit an SAE, hierarchical SAE, PCA, or another decomposition inside U8. This could name internal
  structure, but before transfer it risks explaining a dataset-specific occupancy pattern. Kill
  criterion for deferral: if the unchanged physical program fails both v18 constructions, a finer
  decomposition of the same fitted space is not justified.
- Expand to more tasks sharing U8. This tests reuse breadth, but construction transfer is the cheaper
  necessary gate. Kill criterion for current priority: if v18 passes targets but fails P/C, first
  separate target and generic control computation.
- Map additional upstream/downstream weight contractions. The maps already exist for the nominated
  writers, H3, and M11; more atlas breadth would not resolve whether those coordinates generalize.
  Kill criterion: a v18 failure localized to one boundary would justify expanding only that side.

Opposing outcomes are already registered: U8 failure means activation-specific occupancy; U8 pass
with H3 failure means the cross-module variable survives but the native endpoint changes; top-32
failure means the weight hierarchy is activation-regime-specific; P/C failure means nonselective
generic temporal or lexical transport.

## Throughput and systems audit

Authoritative timestamps show the reader result at 15:34, H3 result at 15:52, restricted-tensor
result at 16:04, and v18 capability result at 16:16: four useful receipts in 54 minutes, with three
being promotion-depth tests rather than basic screens. Serial intervals were approximately 18, 12,
and 12 minutes. GPU execution itself was seconds; most time went to scientific design and exact
intervention implementation, with one pre-outcome block-path repair. Shared runner machinery kept
validation small; there was no idle wait at a result boundary. The next engineering pressure is to
reuse the existing capture/patch helpers rather than author another bespoke framework.

`CIRCUIT_FOCUS: PASS` — every completed unit improved causal localization, splitting, weight
translation, or prospective generalization.

`CEREMONY_BUDGET: PASS` — tests and hash binding remained smaller than scientific design plus
implementation; the independent replay addressed a concrete otherwise-unmeasured registered bar.

`NOVELTY_LESSON_GATE: PASS` — full construction history was searched and caught the v15/v16
collision before the v18 authority was sealed.

## Ranked next moves

1. Implement and run the registered immutable v18 U8/H3/top-32 transfer with P/C controls. Kill the
   reusable-circuit claim according to its four explicit partial-null terminals.
2. If the program transfers selectively, use its exact restricted writer/reader tensors to define
   an operationally anchored within-U8 decomposition across multiple tasks, then intervene on those
   factors jointly. Kill any factorization that only improves reconstruction or lacks interchange.
3. If one boundary fails, localize the rotation or missing factor on that boundary only—H3 versus
   other A11 heads, or frozen versus construction-conditioned M11 factors—without refitting the
   already-passing boundaries.

Concrete continuation receipt: the v18 capability result is committed, and the hash-bound no-refit
transfer prior is committed before implementation or any v18 causal outcome.
