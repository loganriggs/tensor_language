# Hourly research review —12 September,05:33 UTC

Circuit targets: explicit input/operation/output; cross-boundary grouping and
within-module splitting; held-out/OOD prediction; extraction; selective removal;
composition/reuse; stable identification. The full goal remains a simpler
executable program with all four user properties. No new circuit is identified.

The direct full-output half-block search improved cuts but did not converge.
The normalized commutant eigensolve converged reproducibly but rounded to strongly
coupled halves. A star-graph control proves that this does not exclude cheap
shared arithmetic. The composed single-parent fit converged across four starts,
but its compact partner failed native interchange and removal. Prior comparison
then showed that the parent is largely a refinement of an existing direction.
Even its full matched partners failed interchangeability; retain both interfaces.
These results target extraction, grouping and reuse, and prevent false promotion.

Decision: demote more isolated-parent or half-block tuning. Check full-input
interaction sparsity under composition. The earlier sparse spectral core kept
128inputs and only6.406% of total centered energy before selecting any edges.
The new streaming calculation retains all1152inputs and all outputs, comparing
single-layer and producer-folded spectral frames with equal edge budgets.
Its exact dense CPU control passes at2e-16. This is a discriminating baseline
for interacting-reader discovery, not globally optimized Tucker or a circuit.
Unsupervised weight search precedes any frozen native tests, per user direction.

Confounds retained: paired coefficients differ from symmetric quartic/text norms;
spectral frames are not optimal sparse frames; normalization and common output
remain native background; similar parent readers need not be interchangeable;
reused morphology rows are developmental, not fresh/OOD confirmation. Native
partner-error accounting explains the observed swap failures without blaming
numerical precision or tail nonlinearity. No post-hoc rank repair is promoted.

[Recorded phase accounting](HOURLY_PHASE_ACCOUNTING_2026-09-12_0533.json):
5.44min science,15.45implementation,5.03validation,22.06review,18.10publication.
Labels include boundary attribution and background GPU execution: they are not
exact independent stopwatch measurements. Nevertheless documentation/review is
too large. The direct block run took18.56min; subsequent spectral and native
tests took seconds. Further record duplication is not justified.

- CIRCUIT_FOCUS: PASS. Failed extraction and interchange changed the route;
  numerical factors were not promoted as circuits.
- CEREMONY_BUDGET: FAIL. Review/publication dominates the recorded hour. Repair
  now by running the existing streaming kernel with reused contractions; keep
  its result in one primary receipt and skip new explanatory frameworks.
- NOVELTY_LESSON_GATE: PASS. Old shared parent and restricted128core checked.
  Their remaining scope is stated rather than represented as untried work.

Next action already begun: full-input sparse-core CPU implementation/control and
managed preflight. V1 preflight rejected keyword-style prediction keys before
execution; V2 changes only explicit prediction-key spelling, preserving V1.
After the fixed-frame result, optimize only if it resolves a remaining meaningful
restriction; otherwise change representation. Next hourly06:33; math08:00.
