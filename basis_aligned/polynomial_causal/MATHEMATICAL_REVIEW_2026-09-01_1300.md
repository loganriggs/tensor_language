# THREE-HOURLY MATHEMATICAL REVIEW — 2026-09-01 13:00 UTC
Context: rung 388 measured the function→census conversion at L16 (.8146 R² → +.0478 census, ~.0026 per 1% omitted; CE added above native, LOWER IS BETTER) and proved the function is JOINT three-mode structure. Codex must now choose L17-screen vs representation pivot. This review derives the missing calculus and executes it on receipts.

## Top three moves (ranked)

### 1. EXECUTED — Union-bound corner calculus (mode energies → function R², closed-form)
**Theorem-shaped statement:** for the importance-truncation core, the omitted function fraction is bounded above by the SUM of the three mode-wise omitted invariant energies (union bound over the three projection stages); measured efficiency η = (omitted function)/(additive omitted) ≤ 1 captures overlap of the lost subspaces.
**Calibration at the one measured corner (L16, r512/k576/p512, all receipts):**
- input omitted@512 = .0875 (366 fit-B), product omitted@576 = .1512 (384), output omitted@512 = .0067 (385) → additive = .2454
- measured omitted function = .1854 (388) → **η = .755**, and the union bound holds (.185 ≤ .245 ✓) — the calculus is CONSISTENT and conservative.
**Predicted surface over the measured grid (η=.755):** the best measurable L16 corner (r768, k3456, p512; price 8,553,600 = saving 7,372,800/layer) predicts omitted ≈ .755×(.0332+.0330+.0067)=.055 → **R² ≈ .945 → census ≈ +.014, certs ~40-45 by ray** — NEAR frontier grade (+.0125) but short of mid-tier (needs R² ≳.965). At L17 the modes are tighter (product .973@2304, output .997@512): an (r768,k2304,p512) corner predicts R² ≈ .955 — better, still marginal.
**Consequence:** the L17-vs-pivot decision is now quantitative: same-family corners plausibly reach ~+.012–.016 census at ~7M/layer savings — real but NOT clearly better than composing more proven input-mode cuts. Handed to Codex with the numbers.
**Assumption that may fail:** η is corner/layer-dependent (one calibration point); a second measured corner would pin it. The union bound itself cannot fail.

### 2. Certificate-weighted vector distillation (proposal, GPU)
Fit future cores minimizing ray-projected certificate damage directly (residuals weighted by the two-mode certificate basis pulled back through the linear output path), not Euclidean MSE. Differs from the falsified scalar reweightings (350/353): the weight is a fixed low-rank OUTPUT-space metric derived from the validated ray, not an input-density reweight. Falsifier: one fit at the measured corner; success bar = cert count above 22 at equal price. Codex's call.

### 3. Genericity nulls for product spectra (CPU, my lane later)
Compare per-layer product-mode spectra to random-weight nulls with matched marginals: is front-stack flatness just genericity (maximal border rank of generic bilinear maps) while late-layer concentration is the algebraically special, trained structure? Cheap; would tell WHERE training installed compressible computation. Not urgent.

## Pruned
Everything previously closed; plus "more ALS variants" (387 showed joint fitting adds ~.016 — optimization is not the lever).
