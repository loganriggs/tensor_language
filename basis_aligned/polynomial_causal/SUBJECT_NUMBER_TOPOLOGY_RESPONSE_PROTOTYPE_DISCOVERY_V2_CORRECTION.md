# Subject-number topology-conditioned response prototype V2 correction

V1 is preserved as invalid. It treated the complete opposite-minus-native MLP8
raw-state difference as though changing that one input also transported the
opposite subject's independently changed MLP4--7 writes through their separate
downstream slots. The native reconstruction passed, but the alleged opposite-head
endpoint missed by `61.7404`; therefore none of V1's attractive response or law
metrics is evidence.

V2 changes only the intervention definition and its exactness check. At each site,
construct the validated `YZ` hybrid with the existing exact MLP8 input decomposer:
recipient E/A/U/W and opposite-number MLP6/7 (`Y/Z`). The topology prototype now
models this grouped-source displacement, exactly matching the intervention family
for which the frozen response law was discovered. Other downstream MLP slots stay
recipient-native by design. Require hybrid reconstruction closure and native head
replay at most `5e-5`; an opposite-native head replay is neither expected nor
claimed.

Rows, folds, rank seven, ridge `1.0`, eight permutation controls, frozen axis,
frozen beta, fixed coefficient target, prices, and every scientific threshold in
V1 remain unchanged. V2 remains opened-authority discovery and cannot establish
fresh OOD behavior.
