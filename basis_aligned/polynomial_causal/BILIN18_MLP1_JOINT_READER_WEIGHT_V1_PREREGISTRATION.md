# Joint two-circuit MLP1 weight object: restore before proposing a reduction

User-directed mathematical follow-up to weight_tensor_two_circuit_math_2026-09-09.md.
The command-mode/first-value sequence is paused. Target MLP1, the lowest-index
shared MLP response site in the established temporal/iswas program. Native
module co-use is evidence for studying both readers jointly, not for assuming
one shared semantic variable.

Restore exactly the existing pooled rank8 output projector and complete task
modes using pooled_response_projector.fit and
run_temporal_iswas_rank46_task_mode_complete_rank_ladder_v1.fit_full_modes.
Take each task's first4 modes, as in the existing rank4 causal/noise protocols.
Do not use the older task-usage helper's default top2 modes or select a new rank.
This repeats an existing recorded construction; it is not a new optimization
for favorable tensor spectra. Bind recipe, model, cohort and parent-result
hashes before the managed run. Eight existing native capture forwards are
expected (four pooled plus two per task); count calls and fail on discrepancy.
Serialize both physical reader maps and their projector/recipe identities so
later CPU algebra does not repeatedly reload or refit the interface.

Let C stack the two task readers. Compile Q_k=sym[L^T diag((CD)_k)R] for all
8 reader coordinates from full MLP1 input dimension1152. Keep C*b separately.
NativeFP32 weight maps are cast toFP64 for analysis; native epsilon stays the
FP32 RMS epsilon. Use streamed matrix multiplication, never a1152^3 tensor.
Eight FP64 forms require~85MiB, below256MiB per tensor; report actual shapes.
The native model and these derived arrays are counted separately.

A: recipe/source hashes; finite orthonormal task maps, at most1e-4 projector
orthogonality error; exact-form factor/symmetric-tensor/normalized-reader replay
on17 fixed continuous input vectors and stored native normalized input states,
abs<=1e-8/relative<=1e-9 inFP64. The pre-normalized-state formula is evaluated
with the original epsilon, and bias is not divided by the norm. Compare native
FP32 MLP coordinates separately at abs<=1e-3/relative<=1e-5. Check serialized reader-map roundtrip and an orthogonal basis-gauge replay
within1e-4 Frobenius relative error of the corresponding projector. Do not
run an additional fit or confuse the distinct historical parity fits. No new
behavioral or OOD claim from algebra.

Record three different structural objects without conflating them:
1. Complete symmetric quadratic-function spans for temporal and iswas readers,
   including principal-angle intersection diagnostics. Compare against earlier
   weight-overlap evidence only where the actual reader definitions match.
2. Joint input support from ranges of all Q_k. Report the complete normalized
   singular spectrum and residual of any algebraically proposed discarded space.
   A numerical singular cutoff1e-10 relative is a diagnostic, not an exact-zero
   certificate. If every direction remains, close this exact linear-feature
   quotient candidate; do not claim a smaller input space from a truncated plot.
3. Literal factor and shared-linear-producer structure, distinguished from
   complete-polynomial overlap. An absent quadratic intersection does not rule
   out shared linear factors. No global factor-count sweep is authorized by this
   diagnostic protocol.

The exact normalized-observable theorem is conditional on Q_k=U H_k U^T.
Independent additive writer edits additionally require their norm cross-terms,
e.g. include their input directions in U, or explicitly supply equivalent norm
update data. Do not infer intervention closure just from the natural numerator
span. No whole-network state reduction follows from a module-level quotient.

This is a restored weight-object and feasibility receipt, not adoption. It has
one instrument prediction and descriptive structural outcomes; any runner
metadata requiring additional prediction keys must name predeclared structural
checks rather than invent new scientific bars. Subsequent factor candidates
require their own explicit operations, native removal/interchange mapping,
held-out/OOD both-task evaluation, joint intervention test and complete price.
Use existing atlas contractions and new norm/edit reference. No updates/training,
managedGPU only, alarm600s; small controls execute on CPU. Full native weights,
reader adapters and retained background remain charged.
