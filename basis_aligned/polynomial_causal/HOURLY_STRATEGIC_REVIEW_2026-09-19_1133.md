# Hourly strategic review

ACTIVE_TRACK: WEIGHT_FOLDING

Actual UTC: **2026-09-19T11:33:04Z**. Next deadline: **2026-09-19T12:33:04Z**. Previous review: [10:30 CIRCUIT](HOURLY_STRATEGIC_REVIEW_2026-09-19_1030.md), 1h02m55s old. This is one live review, not an offline backfill. Historical `/workspace` paths were resolved against this live checkout. Evidence cutoff is v567 at 11:28:55. No GPU model, agent, job, queue, service, timer, experiment/result, registry, dossier, commit, push, external contact, or `TYPED_FACE_EXTRACTION_V1` implementation was changed. The sole bounded CPU consequence was the truthful 11:33 review phase mark.

The preceding hour was declared `CIRCUIT`; this review therefore alternates to `WEIGHT_FOLDING`. The declared regional five-arm factorial was not run. Instead, circuit work finished the adjacent subject-number map, replayed two number readouts and their units on a third outcome-blind FineWeb set, then opened a pronoun-gender route. That useful but off-plan work is audited below; mandatory alternation preserves its next circuit handoff rather than extending it now.

## Seven circuit targets and full goal

1. **Computational specification:** identify what is read, the operation/composition, what is written, and which downstream computations use it.
2. **Cross-boundary grouping and within-module splitting:** group pieces across native modules when a reader treats them as one variable, and split a native module when its pieces serve different computations.
3. **Held-out/OOD prediction:** predict activations and behavioral effects on unseen inputs, task variations, and shifted corpora.
4. **Extraction or sufficiency:** execute the isolated circuit at a declared boundary with every retained port and background named.
5. **Selective manipulation:** removal, swap, or edit changes the intended behavior against matched controls while preserving unrelated behavior and accounting for redundancy/interactions.
6. **Composition and reuse:** shared subcomputations serve multiple tasks/modules and joint behavior follows a tested composition law.
7. **Stable identification:** claimed units survive document/corpus splits, gauges, and fitting restarts, or are defined by downstream operational equivalence.

Goal: a smaller transparent tensor program that **predicts held-out/OOD behavior, is extracted at a declared boundary, supports selective manipulation/removal, and composes/reuses**. Simplicity is the fifth separately priced property: independently stored scalars/bytes, product nodes, readers/writers, open ports/states, graph edges, and measured execution. Lower replay error, storage, rank, variance, or CE cannot substitute for a missing behavioral property.

## What changed since 10:30

The strongest advance is a cleaner generalization check for the subject-number route. The v544 miner scanned 12,327 later FineWeb documents outcome-blind and produced 64 rows in four cue/label cells from **62 distinct document contexts**. On these rows, **edits** v545/v546 replay the five pronoun writes at `.730` with remainder `.034`, and the five verb writes at `.599` with remainder `.085`; v547/v548 also preserve the hub/detector position split. These results improve held-out within-corpus evidence, but do not create an extracted number executor, matched composition null, or priced program. Receipt v545 still duplicates `text` into `verb_slot`, and v548's inherited docstring says 60 pairs although the lineage filters to 45 distant rows.

The adjacent number map v540-v543 rejected several priors: early attention is not the largest noun-site contributor; block12 is not the largest mid writer; MLP8 is large (`.295`) while MLP17 is small (`.052`); and the top30 MLP8 units reach `.232`, below the `.25` gate. The first v543 execution failed with a copied `MODES` dictionary and was repaired. These are honest nulls, but they continue the one-runner-per-candidate pattern.

The gender route is promising but mostly **opened**. **Edits/folds** v549-v558 show five pronoun readers close `.600` at the noun; self-copies 8.1/6.1 carry `.310`; the five heads add locally, but early/late groups do not; and the reader value splits into current-state `.291` and token-only `.314`. v553's fold attributes one representative reader's native value to embedding `.34`, attention `.15`, and MLP `.51`; the proposed compensation mechanism was refuted by v554-v557. v559-v562 correctly recover some weight-based ranking/sign information only after transport-sign repair, but three successive direct-magnitude formulas remain wrong by roughly fifty-fold even after final RMS and a lambda-chain adjustment. v563 resolves the main causal endpoint instead: editing the five readers' writes into the answer closes `.878`, with the rest of their blocks `.001`. The result transfers on 64 Pile pairs at `.893` (v565), but Pile is fresh for this gender edit only to the extent documented by its pre-existing row set, not a new mined cell manifest this hour. v566's number collateral is `.063` for the five writes while the native number/gender ratio `.117` misses its `.10` gate. v567 finds noun `.600`, next `.132`, and later `.231`, but all-position `.732 < .80` and the parts are nonadditive. The planned v568 pattern/value test had no result by cutoff.

Evidence remains typed: **fold** is native attribution, **edit** is causal intervention, **response** is downstream allocation after an edit, and **fit** is a predictive relationship. Fresh/opened/replay status is claim-relative; row counts are not context-cell counts.

| Property | Current evidence | Verdict |
|---|---|---|
| Held-out/OOD prediction | Number readouts and hub/detector replay on 62 distinct later FineWeb document contexts; gender answer-write edit replays on 64 Pile pairs. No frozen end-to-end formula predicts either behavior from declared inputs, and regional packed FineWeb signs remain `88/102`. | **PARTIAL** |
| Extraction at a declared boundary | Regional residual6-to-attention8 and norm-closed MLP8 boundaries execute with declared ports. Complete head9.8/suffix, current number route, and gender route are not standalone. | **PASS only at intermediate regional boundaries** |
| Selective manipulation/removal | Regional scoped fresh edits beat same-site nulls. Number channel separation transfers; gender's five-write edit has small but nonzero number collateral (`.063`) and lacks equal-norm same-site directions plus three unrelated readers at the complete boundary. | **PARTIAL** |
| Composition/reuse | Regional key/value interaction/smaller is `2.3448` and additive factorial RMS is at least `.5862` of the smaller live single. Number and gender have local additive pieces, but no fresh smallest-piece Mobius gate against matched random splits; gender positions are nonadditive. | **FAIL overall** |
| Measured simplicity | Regional components have local prices and a 128-product lower-bound witness. Neither the complete regional path nor number/gender routes have a deduplicated storage/state/node/edge/runtime price. | **LOCAL COUNTS ONLY** |

## Highest-value WEIGHT_FOLDING action

Complete one exact forward fold with endpoint **the regional head9.8 output delta at the residual10 boundary**:

`(native residual6, token IDs) -> all nine attention7 writes -> complete MLP7 and five head8.2 readers -> head8.2 city-write delta -> norm-closed MLP8 -> head9.8 QK1*QK2*V output delta`.

- **Exact native factors and shapes:** residuals and module writes are `T x 1152`; every head Q/K/V coordinate is `T x 128`; each causal QK score is `T x T`; `O` maps each head result to `T x 1152`; MLP `L,R` map `T x 1152 -> T x 4608`, elementwise multiplication stays `T x 4608`, and `Down` returns `T x 1152`. The standard fixture has `T=32`.
- **Included source terms:** retain all residual6, initial/re-entry, attention7, and MLP7 sources used by the five head8.2 readers. At MLP8 input `z+d`, retain `z*z`, ordered `z*d` and `d*z`, `d*d`, bias, skip/re-entry, and changed normalization separately. In each head9.8 QK factor expand reader-permitted attention-output × attention-output, MLP × MLP, attention × MLP, and MLP × attention terms separately. Retain all seven nonempty finite-difference products of `(dp1, dp2, dv)`: three singles, three ordered pairs, and the triple.
- **Omitted terms and background:** omit only the unchanged baseline head9.8 write from the delta program. Keep native residual6, unrelated query/source content, unchanged attention fields, all RMS denominators, rounded rotary phases, causal mask, learned mixing/re-entry, MLP biases, and the blocks9-17 suffix as explicitly priced inputs/background until folded. No live QK2, value, cross, or source term may be dropped: current composition and source-partition tests do not license it.
- **Nonlinearities:** epsilon-aware RMS, rounded rotary, two bilinear QK scores without softmax, their product with V, bilinear MLP products and bias, and learned residual re-entry are exact. Final RMS and `30*tanh(U*RMS(h)/30)` are outside the local endpoint replay but remain live in the later behavioral test.
- **Simplicity measure:** one deduplicated count of independent FP32 values/bytes, 4,608-wide MLP product nodes, live QK1×QK2×V products per causal cell, readers/writers, graph edges, open-state scalars, and measured CPU execution, charging shared factors once. Exactness alone is transparency/extraction progress; it is a simplicity pass only against the scoped native comparator.
- **Exact replay check:** freeze native captures and compare every intermediate plus the final head9.8 delta on 40 opened fixtures and 20 genuinely distinct FineWeb document/context cells. Require float64 closure `<=1e-10`, FP32 per-sequence relative L2 `<=5e-5`, exact off-support zero, and equality of named-source sums to their parent. Preserve any miss rather than relaxing a gate.
- **Later causal/predictive falsifier:** after freezing the fold, run baseline, routing/key-only, value-only, both singles installed with their explicit product suppressed, and the true joint native product through the full suffix. Require signed transfer on structurally varied distinct cells, at least three unrelated readers, full logits/CE, 16 equal-norm same-site directions, and matched random splits. Kill the grouping if a single is dead, signs fail, collateral breaches, or `joint - additive-without-product` is not specific and small relative to the smallest live single. Exact replay or a closed Mobius identity is not sufficient.

This advances targets 1, 2, and 4 and produces the frozen object needed for targets 3, 5, and 6. It outranks folding the newly opened gender route because the regional path is the authority-designated depth target, already has the deepest port closure and distinct-cell evidence, and its unresolved coupled interaction blocks composition.

**Concrete track connection:** gender **edits** reveal that half of the five readers' value effect comes from a token-only branch and that reader writes, rather than a guessed direct unembedding path, close the answer effect. That tells folding to retain consumer/transport factors and not infer effect magnitude from a locally large weight contraction. Conversely, the regional seven-product fold supplies the exact product-suppressed arm needed to determine whether routing and value should be one causal circuit component.

**CIRCUIT handoff:** return to the frozen regional full-suffix five-arm factorial on structurally varied distinct cells, with three-reader/full-logit controls, equal-norm directions, matched splits, and interaction normalized by the smallest live single. Preserve v568's gender pattern/value question for a later circuit hour; do not let it displace the regional test again.

## Confounds, alternatives, and decision

The fold and later factorial must preserve baseline subtraction and replace-versus-zero semantics; source ordering; frame/position/city/endpoint mixing; token/document difficulty; FineWeb/Pile leakage; every RMS denominator, rounded rotary phase, causal mask, learned re-entry, attention product, suffix nonlinearity, final RMS and softcap; live-delta tripwires; BF16/FP32 floors; and post-selection. The 62 v544 documents are actual distinct contexts but only four cue/label cells; the 64 Pile gender rows lack a new this-hour cell audit. Repeated `text`/`verb_slot` payloads show that semantic schema fields remain unsafe.

Ranked alternatives: (1) the chosen complete regional fold; kill the instrument on any intermediate replay failure and deny simplicity if the deduplicated price does not improve. (2) Fold the gender token-only generator through the five reader transports; kill if it cannot predict magnitudes on untouched documents or if number/control readers share the factorization. (3) Fold the number route separately by pronoun and verb consumer; kill if frozen groups fail on distinct cells or require opaque residual ports at every edge. (4) A predictive-state quotient or signed composition basis is deferred until several fresh response environments exist. (5) More opened unit/head censuses are demoted. The regional route survives because it tests exact port closure on the designated path rather than opening another post-selected behavior.

## Registry and dossier audit

`COMPUTATION_PATH_REGISTRY.md` now contains a useful uncommitted `PATH-REGIONAL-CITY-001` row and keeps `PATH-SET2-001`, but it still ends the former at head8.2 and has no complete residual6-to-head9.8 record. `CIRCUIT_REGISTRY.md` still carries older four-head number/gender summaries and does not consolidate the five pronoun heads, corrected five verb heads, v544-v548 transfer, or v549-v567 gender decomposition. `CIRCUIT_GRAPH_REGISTRY_V1.md` remains a 49-package inventory, omits `city_mlp8_norm_closed_v1` and both live number/gender routes, and labels two packages `four_trait_verified/next gap: none` without separately pricing simplicity. `MODULE_DOSSIERS.md` and the MLP index preserve regional MLP7/8 prior art but do not cross-link this hour's MLP8 number/gender facts. Primary ownership of `TYPED_FACE_EXTRACTION_V1` was respected.

The highest-priority missing cross-link remains **`PATH-SUBJECT-NUMBER-002`**, now linking v338-v548, the 62-context third FineWeb set, five pronoun and five verb readers, copier 4.5->5.3, hub 829/953/1030, detector 701/2059, position-conditioned endings, corrections/invalid runs, module dossiers, evidence status, and missing extraction/composition/simplicity gates. A separate **pronoun-gender path record** is also now warranted for v549-v567; it must not overwrite the older MLP17 gender committee and must distinguish the token-only value branch, noun/answer sites, opened FineWeb versus Pile replay, number collateral, and failed magnitude formulas. Neither cross-link was written during concurrent work.

## PAST_HOUR_TIMING

Window: **2026-09-19T10:30:09Z-11:33:04Z**. Twenty-seven successful model receipts v540-v567 (v544 is a separate CPU row build) finished from 10:31:07 to 11:28:55. Their declared internal serial runtimes sum to **51.423s**, median **1.910s**, range **1.645-2.174s**, with **230 declared forwards**. Successful model-result cadence is **133.4s per candidate**. Do not confuse cadence with active labor; it includes unknown design, authoring, interpretation, publication, queue gaps, and repairs. Parallel durations were not summed.

- **Design:** unknown; predictions are recorded but no science phase boundaries exist.
- **Implementation/data building/indexing:** unknown. Twenty-seven sibling model candidates plus v544's miner were authored. v544 scanned 12,327 documents and wrote 62 distinct-document contexts, but its wall runtime is absent; commit gaps are not labor time.
- **Validation:** successful receipt runtime is included above. v543 had one failed managed execution caused by inherited `MODES`; the v544 interpreter emitted a teardown error after safely writing the artifact. Active repair time is unknown.
- **Model execution:** 51.423s successful internal serial runtime. Runner occupancy is generally 2-3s each; the failed v543 occupied about 2s. Scheduled canaries are separate.
- **Interpretation:** unknown; board claims follow each receipt and Review43 summarizes v542-v564, but no interpretation marks exist.
- **Documentation/review:** unknown; Review42 at 10:40 and Review43 at 11:24 are timestamped, but authoring duration cannot be inferred. This review began at the truthful 11:33 boundary.
- **Idle/blocked:** unknown. The longest successful-result gap, v540->v541, is about 6m23s; no evidence licenses calling it idle.

The newest work-date phase log is `RESEARCH_PHASES_2026-09-19.jsonl`. Before this review it held only seven sparse hourly review marks from 04:04 through 10:30 and covered none of v540-v567's design, implementation, validation, model execution, interpretation, or publication. The helper appended one truthful **11:33:04 review** boundary. Sparse boundaries are not uninterrupted active labor and no missing phase was backfilled.

## PROCESS_IMPROVEMENT

Ranked by expected time saved versus cost:

1. **Declarative intervention runner plus semantic receipt schema**: one shared row loader, intervention specification, scorer, actual-batch price, slot assertions, nonduplicated payload check, finite/nonzero template checks, and receipt writer. Likely saves roughly 45-80s of the observed 133s cadence and would have prevented v543's copied-dictionary failure plus repeated stale `verb_slot` payloads; medium cost with equivalence tests.
2. **Canonical number/gender path records** after the live sequence freezes: likely saves 5-10 minutes per handoff and prevents stale four-head summaries; low documentation cost.
3. **Required receipt timestamps and cell manifest** (`started_utc`, `finished_utc`, evidence status, unique document/context/cue-label cells): low cost and would make data-building and active-time audits measurable.

No process code was changed. The best shared-runner repair overlaps an actively changing sibling-runner family and needs multi-intervention equivalence tests, so it is not a safe micro-edit inside this bounded review. A tiny ad hoc checker would add another bespoke layer. The sole nonconflicting CPU repair was the append-only truthful phase mark; scientific priority remains the exact regional fold.

## Explicit verdicts

- `TRACK_ALTERNATION: PASS` — previous `CIRCUIT`; current `WEIGHT_FOLDING`.
- `TRACK_PROGRESS: FAIL (DECLARED-ACTION DRIFT)` — the preceding hour produced substantial circuit receipts and honest nulls, including transfer on 62 distinct FineWeb contexts and a Pile gender replay, but did not execute its declared regional full-suffix factorial; it also opened gender before the authority-designated regional path met the five-property definition.
- `CEREMONY_BUDGET: PASS WITH MEASUREMENT CAVEAT` — 27 model receipts plus one data build dominate visible output and model execution is cheap, but design, implementation, validation, interpretation, and review minutes are uninstrumented, so their active-time proportions are unknown.
- `NOVELTY_LESSON_GATE: FAIL` — earlier causal lessons were used and failed predictions were preserved, but another 27 sibling candidates, a copied-dictionary crash, duplicated semantic payloads, absent start times/cell schemas, and missing number/gender canonical paths show that shared-helper and registry lessons remain unrepaired.

**Highest-value action now:** complete and exactly replay the fully coupled residual6-to-head9.8 regional output-delta program, retaining all ordered source terms, both ordered MLP cross terms, all seven `QK1*QK2*V` delta products, exact opened/fresh replay, and one deduplicated program price.
