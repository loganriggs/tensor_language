# Preregistration — late_tail_gate_per_block_probe (Claude, LANE 1 CUDA) — Registered 2026-09-04 02:57Z (box clock)

Sign convention (§2135): every CE number is CE ADDED ABOVE THE REAL MODEL on held-out docs 0–63 (FRESH split; second moments from docs 96–191) — LOWER IS BETTER. Every fractional bar below is written with the denominator FLOORED at .002 (the CUDA-atomics wobble) and an additive floor on the right-hand side, so a negligible denominator cannot manufacture a pass or a fail; all block costs are expected ≥ 0 (a cross term's removal is expected to hurt), and the sign assumption is stated per prediction.

## Question
§2799 closed the small gate for the late STACK (256 of 768 modes to get within .03 of exact) but its per-block spectra are very unequal: block 17's whitened gate has effective rank 4.8 (16 modes = 93% of its cross-term energy), block 16's 24 (64 modes = 79%), blocks 8–15 68–159 (64 modes = 56–67%). If CE follows the gate energy per block (as it followed variance per direction in §2803), the two last blocks' tail reads are compressible to 16 and 64 gate modes — the lineage's only parameter-reducing item, at the level of a sub-MLP component (the cross term of one block). This rung prices that, and produces the per-block tail-read importance vector (each block's cross term removed alone) against §2799's joint .3668.

## Instrument
Parent late_tail_gate_mode_rank_probe (§2799): same exact weight-side Gram, whitened gate modes, Gate(l, k) head (MLP(c) + J(Π_k c) t + MLP(t); k = 0 removes the cross term; None = exact), same fit docs. New: one(l, k) = block l at rank k, the other nine late MLPs exact. Arms: SPLIT8_1024; GATE_EXACT; B{l}_GATE_0 for l = 8..17; B17_GATE_{4,16,64}; B16_GATE_{16,64}; B15_GATE_64; TWO_BLOCK_B16_64_B17_16. Price: 96 fit + 64 × 20 = 1376 GPU document-forwards (~50 s).

## Predictions (scored exactly as written) — each with the value under the hypothesis, under the null, and the operands' assumed signs
- pred_a_instrument: |baseline − 3.0322401| ≤ 1e-4; |SPLIT8_1024 − .0374| ≤ .015; |GATE_EXACT| ≤ 1e-3; Gram direct check ≤ 1e-6 at blocks 8 and 17.
- pred_b_block17_cross_term_matters: B17_GATE_0 ≥ .02. Null: ≤ .005. Sign: expected positive. Worked example: the joint removal costs .3668 over ten blocks (§2799) and block 17 is the largest late tail writer to the readout (§2797/§2800: W17 .027 of .113) and the largest late tail READER by §2788's gain curve; its own cross term removed alone should cost .03–.08; under the null block 17's cross term is inert (≤ .005) and its compressibility is moot.
- pred_c_block17_rank16_gate_recovers_most: B17_GATE_16 ≤ 0.3 × B17_GATE_0 + .002. Null: B17_GATE_16 ≥ 0.6 × B17_GATE_0 − .002. Signs: both ≥ 0 expected. Worked example: 16 modes hold 93% of block 17's cross-term energy; if CE follows energy the residual cost is ≈ 7–15% of B17_GATE_0 (≈ .005 of .05); if the CE-relevant part of the gate sits in the energy tail (as the stack-level GATE_64 .0886 vs energy 62% might suggest), ≥ 60% of the cost remains.
- pred_d_block17_rank4_gate_recovers_half: B17_GATE_4 ≤ 0.5 × B17_GATE_0 + .002. Null: ≥ 0.8 × B17_GATE_0 − .002. Worked example: effective rank 4.8 → 4 modes hold ≈ 70–80% of the energy → cost ≈ 20–30% of B17_GATE_0 (≈ .012 of .05); null: the top four modes are the mean gate's neighbourhood and buy little (≥ 80% of the cost remains).
- pred_e_block16_rank64_gate_recovers_most: B16_GATE_64 ≤ 0.4 × B16_GATE_0 + .002. Null: ≥ 0.7 × B16_GATE_0 − .002. Worked example: 64 modes = 79% of block 16's energy → ≈ 21–30% of its cost remains (≈ .01 of .04); null ≥ 70%.
- pred_f_two_block_program_is_cheap: TWO_BLOCK_B16_64_B17_16 ≤ .015. Null: ≥ .04. Worked example: additive costs ≈ .21 × .04 + .07 × .05 ≈ .012 if CE follows energy per block and the two blocks do not interact; ≥ .04 if either block's CE sits in its energy tail or the two truncations compound.

Bars: {"ce_tol": 1e-4, "repro_tol": 0.015, "exact_tol": 1e-3, "gram_tol": 1e-6, "floor": 0.002, "b_cost": 0.02, "c_frac": 0.3, "d_frac": 0.5, "e_frac": 0.4, "f_cost": 0.015}. Null bars: {"b_cost": 0.005, "c_frac": 0.6, "d_frac": 0.8, "e_frac": 0.7, "f_cost": 0.04}.

## What each outcome means
c, e, f TRUE: the last two late blocks' tail reads are a 16- and a 64-mode gated linear form — the first sub-MLP program item in the lineage that survives a CE test; the program would replace block 17's cross term (its dependence on 768 gate coordinates) by 16 gate scalars and block 16's by 64, at ≤ .015 CE. c or e FALSE with null met: gate ENERGY is not gate CE per block — the §2799 spectra do not license compression even at the low-rank blocks, and the "no small gate" conclusion holds block by block. The per-block GATE_0 vector plus its sum against the joint .3668 says whether the late tail reads are independent (sum ≈ joint) or interacting. Nothing installs into the §312 frontier; the strict explained fraction is unchanged by any outcome.
