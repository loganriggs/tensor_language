# Hourly strategic review: certify the shared routing interface

Date: 2026-08-28 09:20--09:35 UTC

Status: discovery consolidation plus executed matched control. No new final or promotion
authority.

## Honest model accounting

- Structural inventory remains 36/36 attention and MLP sites.
- Named behavioral explanation remains $32.1\%\pm6.4\%$.
- Named causal recovery remains 10.923%.
- Strict fully compiled recovery remains 0% of the distinct +0.8976 ship gap because
  the MLP/local half still has no admitted zero-native executable.
- Attention now has a source-closed executable operator and two replicated compressed
  points: independent weighted rank-384 recovers 99.84% at 77.78% of dense attention
  storage; shared weighted rank-384 recovers 99.44% at 61.11% of storage.

These denominators are deliberately not added. The attention result is an executable
subsystem certificate; the behavioral/causal percentages describe named whole-model
claims; the strict ship fraction requires a complete replacement program.

## New result and corrected story

The matched independent-Q/K control closed the optimizer confound. Independent weighted
rank-384 reaches 99.8412%/99.8428% recovery. Shared rank-384 replays at
99.4635%/99.4342%. Thus most of the improvement over the historical 97.1% independent
arm came from using the correct activation-weighted objective. Sharing itself costs
0.3777--0.4086 percentage points, below the frozen 0.5-point threshold, while removing
21.43% of complete program storage and 18.75% of its multiply-adds relative to matched
independent factors.

The defensible claim is therefore not “sharing improves fidelity.” It is: one common
384-dimensional routing interface is almost fidelity-free and materially cheaper than
four independently optimal interfaces under the tested distribution.

## Missing interfaces and failures

1. The local compiler is still a post-forward hook. Native MLP/attention modules execute
   before replacement and the native attention v1 bus survives.
2. The first standalone-fallback assay scored only covered current-token positions,
   where the two arms are identical by construction. Its 0.00% loss is not a standalone
   or total-distribution result. This failure is preserved on the board; a corrected
   all-position output assay is currently running, but it remains post-forward.
3. No attention/local factorial executable exists. The hybrid oracle measured
   -2.17559 nat interaction, so separate gains are not composable.
4. The 384 routing coordinates are gauge-dependent and have no stable semantic names,
   OOD transport test, or editing certificate.
5. Remaining MLP residual CE has not been assigned to a minimal set of exact bilinear
   products under suffix consequence.

## Candidate pruning

- More historical ordinary-SVD routing sweeps are pruned: the matched control proves
  estimator mismatch was material.
- More conditional table/correction ranks are pruned until a pre-execution local bank
  exists; otherwise fallback and native calls dominate the completeness question.
- Hook-level “standalone” cost claims are pruned regardless of CE until native objects
  are poisoned and v1 ownership is explicit.
- Coordinate semantic correlations are pruned until gauge stability and causal/OOD
  consequences are measured.
- Generic tensor decompositions that alter RMSNorm, RoPE, squared QK, the causal mask,
  or residual sequencing remain pruned despite parameter savings.

## Revised top five

1. **Source-closed MLP/local program bank.** Implement pre-execution dispatch, poison all
   native MLP objects, score all positions, and price complete support. This is the
   missing interface blocking any strict whole-model executable fraction.
2. **Factorial whole-model cross.** Combine the shared-QK attention bank with the first
   admitted local bank and measure attention/local interaction directly. Highest causal
   relevance once item 1 exists.
3. **Gauge-canonical shared routing frontier.** Sweep shared ranks below 384 and fix a
   canonical covariance-weighted gauge. This tests minimality, extraction stability,
   and executable cost without reopening operator structure.
4. **Consequence/OOD validation at matched CE.** Compare independent and shared routing
   programs on OOD prediction, circuit extraction, selective removal, collateral damage,
   and data-doubling stability. This operationally validates the simplicity measure.
5. **Suffix-selected bilinear MLP products.** Use downstream adjoints to add exact
   polynomial products only where the remaining CE requires them; falsify against
   equal-count and equal-cost controls.

## Highest-priority action executed

The matched weighted independent-versus-shared control was preregistered, implemented,
tested 25/25, committed before rows were opened, and executed after the prior GPU owner
released the device. Runtime was 102.4 seconds. Parent replay, fidelity-free sharing,
and complete-cost dominance gates all pass. The next safe CPU action is specifying the
source-closed MLP bank while the corrected all-position output assay owns the GPU.
