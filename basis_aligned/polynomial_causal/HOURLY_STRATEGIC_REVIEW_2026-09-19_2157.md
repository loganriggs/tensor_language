# Hourly strategic review

ACTIVE_TRACK: WEIGHT_FOLDING

Actual UTC: **2026-09-19T21:57:22Z**. Next deadline: **2026-09-19T22:57:22Z**. Previous review: [20:55 CIRCUIT](HOURLY_STRATEGIC_REVIEW_2026-09-19_2055.md), 1h01m49s old. This is one live review, not an offline backfill. Historical `/workspace` paths were resolved against this checkout. No GPU model, agent, job, queue/service/timer, commit/push, external contact, existing experiment/result/registry/dossier, or `TYPED_FACE_EXTRACTION_V1` implementation was changed. The sole bounded process consequence was the truthful 21:57 phase mark.

The prior track was `CIRCUIT`, so this review alternates to `WEIGHT_FOLDING`. Preserve the revised joint response/curvature census for the next circuit hour; unfinished work does not override alternation.

## Seven circuit targets and full goal

1. **Computational specification:** identify reads, operations/compositions, writes, and downstream users.
2. **Cross-boundary grouping and within-module splitting:** group pieces across native modules when a reader treats them as one variable, and split modules when pieces serve different computations.
3. **Held-out/OOD prediction:** predict activations and behavioral effects on unseen inputs, task variants, and shifted corpora.
4. **Extraction or sufficiency:** run an isolated circuit at a declared boundary with every retained port and background named.
5. **Selective manipulation:** removal, swap, or edit changes the target against matched controls while preserving unrelated behavior and accounting for redundancy/interactions.
6. **Composition and reuse:** shared subcomputations serve multiple tasks/modules and their joint behavior obeys a tested composition law.
7. **Stable identification:** units survive corpus/document splits, gauges, and fitting restarts, or are defined by downstream operational equivalence.

Goal: a smaller transparent tensor program that **predicts held-out/OOD behavior, is extracted at a declared boundary, supports selective manipulation/removal, and composes/reuses**. Simplicity is the separately measured fifth property: independently stored scalars/bytes, contractions/products, readers/writers, ports/states, edges, peak memory, and runtime. Lower error, storage, rank, variance, or CE cannot replace a missing behavioral property.

| Property | Current evidence | Verdict |
|---|---|---|
| Held-out/OOD prediction | Regional intermediate extraction has 20 fresh FineWeb context cells and scoped Pile transfer, but packed FineWeb direction is `88/102`; the newest control is opened replay. | **PARTIAL** |
| Extraction at a declared boundary | Residual6-to-attention8 and norm-closed MLP8 packages replay with declared ports; the complete residual6-to-head9.8 executor is absent. | **PASS only at intermediate boundaries** |
| Selective manipulation/removal | Scoped fresh edits beat same-site nulls; the complete joint path lacks equal-norm, matched-split, three-reader full-suffix controls. | **PARTIAL** |
| Composition/reuse | Regional key/value interaction is `2.3448x` the smaller piece; suffix curvature is `.4583x` total and often opposes the explicit head term. | **FAIL overall** |
| Measured simplicity | Local packages and restricted rank floors are priced; no deduplicated end-to-end regional bill exists. | **LOCAL ONLY** |

Claims remain separated as **fold/edit/response/fit** and **fresh/opened/replay**. Counts use actual distinct `(template, city pair, endpoint)` context cells, not rows alone.

## Progress, failures, confounds, and alternatives

Since 20:55, one substantive regional **response-algebra/opened-replay** receipt landed. CPU-only replay of the existing five-arm artifact proves exactly

`total interaction = explicit head-product response + native-suffix mixed curvature`.

On the target, curvature is `.4583x` total, the head response is `1.2525x`, their cosine is `-.6784`, and optimally rescaling the head response still leaves `.3367` relative L2. Across 20 actual context cells, curvature/total has median `.4054` and range `.0440–1.1736`; it opposes the head term in 12/20 cells. Thus the pending circuit census cannot use the local head product, its transmitted response, or the old QK1-only census as a suffix surrogate. This advances the prior track by falsifying that instrument, but it does not complete the requested per-module joint census, fresh test, matched null, selective intervention, extraction, or composition claim.

The concurrent temporal lane produced `v598` and `v599`. `v598` is an opened screen: head9.4 is `.123`, head9.1 `.059`, and blocks7–8 `.295`; one preregistered head claim failed. `v599` is an honest compact-set null: the named writers reach `.4756 < .60`, while all writes reach `.8285 < .85`, and sign constancy is only `.516`. These circuit screens are not the user-designated regional depth action and do not alter this folding choice. The board's late 21:36 report of `v597` is not new model execution in this window: its primary receipt is timestamped 12:21:34 UTC.

Confounds remain live RMS/background dependence, frame mixing, baseline subtraction, nonlinear suffix/loss composition, shared token difficulty, opened-panel/post-selection leakage, dead knobs, precision/noise floors, positional colon/quote structure, shared residual inputs masquerading as independent ports, and tiny smallest-piece denominators. Exact Mobius closure is accounting, not smallness or random-split specificity. The new result adds a specific confound: downstream mixed curvature can cancel the explicit head interaction, so local attribution magnitude and full-suffix effect need not agree.

Ranked alternatives are: (1) **chosen**, exact-replay and price the complete regional residual6-to-head9.8 native delta; kill the proposed simplification if the complete expansion fails replay or deduplication yields no smaller independently specified program; (2) fold backward from one block10 reader, but the seven rank-128 certificates rule out an exact linear width `<128` quotient there and make this lower information until attainable task-specific structure is identified; (3) fold from the unembedding/MLP17, rejected now because it abandons the deepest regional port-closure path; (4) another rank/sparse fit, rejected because it would be a probe/compression result without identification. The current path survives because it is the only route joining existing fresh intermediate extraction, all native interaction terms, and a later selective full-suffix falsifier.

## Highest-value folding action

**Endpoint:** the complete head9.8 output-write delta `ΔA9.8 in R^(T x 1152)` produced from token IDs plus the declared residual6 port, through attention7/MLP7, head8.2, norm-closed MLP8, and both QK factors plus V of head9.8.

**Native factors and shapes:** residual/write states `[T,1152]`; every attention Q1/K1/Q2/K2/V map `[128,1152]`, output map `[1152,128]`, causal score fields `[T,T]`, and head values `[T,128]`; MLP7/8 `L,R=[4608,1152]`, `Down=[1152,4608]`, bias `[1152]`; learned residual/re-entry scalars, causal mask, rounded rotary maps, and token-only value branches.

**Included terms:** expand residual inputs into named residual6/initial/attention/MLP sources. In MLP7 and MLP8 retain every reader-permitted attention×attention and MLP×MLP self term and both ordered attention×MLP/MLP×attention cross terms, including `B(z,δ)`, `B(δ,z)`, and `B(δ,δ)`. In head9.8 retain all seven nonempty delta products: `dQK1*QK2*V`, `QK1*dQK2*V`, `QK1*QK2*dV`, the three pairwise products, and `dQK1*dQK2*dV`; recursively expand both QK bilinear forms into ordered earlier-source pairs. **Omit only terms identical between baseline and intervention from the delta**, while retaining their native states as explicit background ports. Do not drop the four higher-order head terms: doing so already costs `.2853` local relative L2 on 20 opened cells.

**Nonlinearities/background:** preserve native RMS with epsilon `1.1920928955078125e-7`, MLP bias, learned skips/re-entry, signed value mixing, causal masking, rounded rotary semantics, and unchanged query/source states. Final RMS, untied unembedding, and `30*tanh(logit/30)` are outside this folding endpoint but remain the later causal-test background.

**Simplicity measure:** deduplicate shared arrays and report independent FP32 scalars/bytes, contractions and score/value products per causal cell, nodes/edges, open ports and cached states, peak bytes, and measured CPU runtime versus executing the corresponding native path. Price gauges/adapters and token tables. A smaller bill alone does not promote the path.

**Exact replay check:** on the bound opened fixtures and the existing 20-cell fresh FineWeb manifest, compare every intermediate and final `[T,1152]` delta against a direct native reconstruction; require seven-term closure, zero off-support write, FP64 offline algebraic closure, and a preregistered native-FP32 maximum relative error no weaker than the existing upstream `1.97e-6/1.61e-6` envelope. Any looser tolerance must be justified before seeing outcomes, not tuned afterward.

**Later causal/predictive falsifier:** install the frozen complete delta on fresh structurally varied cells and decompose the exact joint response module-by-module through blocks10–17, final RMS, unembedding, and softcap. Separately record key/value singles, summed mains, explicit head-product increment, and suffix mixed curvature; require full-vector closure, target and full-logit/CE prediction, at least three unrelated readers, equal-norm same-site directions, matched random splits, and smallest-live-piece normalization. Kill grouping/reuse if consumer identities/signs are unstable or true interaction/selectivity is no better than nulls.

**Connection between tracks:** circuit evidence selects the complete regional write and its downstream readers; folding shows that the candidate semantic object must contain all seven head terms. The new response result further says any cross-module circuit grouping must include separately localized suffix curvature rather than equating the folded head term with behavioral interaction.

## Registry and dossier audit

`PATH-REGIONAL-CITY-001` still ends at the head8.2 city-removal write. It does not join norm-closed MLP8, complete head9.8, the seven-term census, two rank certificates, the new curvature receipt, the pending joint response census, matched-null factorial, or a deduplicated price. `CIRCUIT_REGISTRY.md` and `MODULE_DOSSIERS.md` retain regional head8.2/head9.8 and MLP7/8 evidence, but no canonical residual6-to-head9.8 record. `CIRCUIT_GRAPH_REGISTRY_V1.md` still inventories 49 packages while omitting `city_mlp8_norm_closed_v1`; its two `four_trait_verified` labels also leave separately measured simplicity unpriced. The MLP index links the MLP7 addendum and generic MLP8 dossier, not the complete regional path.

Missing cross-link: one canonical `PATH-REGIONAL-CITY-001` continuation should connect circuit/module aliases, `city_mlp8_norm_closed_v1`, the seven-term and rank receipts, `REGIONAL_FIVE_ARM_CURVATURE_DECOMPOSITION_2026-09-19_2137.json`, the pending joint-response/factorial receipts, evidence/evaluation labels, distinct-cell manifests, ports, and a deduplicated price. Concurrent dirty registry/board work prevents that repair here.

## PAST_HOUR_TIMING

Window: **2026-09-19T20:55:32Z–21:57:22Z**.

- **Serial regional candidate latency:** the declared complete circuit census did not start. The curvature consequence ran at 21:38:22 for `.001851s` internal CPU time; design-to-receipt latency is unknown because no design/implementation phase boundaries were recorded.
- **Temporal candidates:** authoritative runner marks put `v598` at 21:37:08–21:43:58 end-to-end (**6m50s**) across a successful run, a failed wrong-row/price retry, and the final successful run; model-process intervals total about **9s**, not 6m50 of active labor. `v599` ran 21:44:58–21:45:00 (**2s** process; **1.963s** receipt runtime), about **1m02s** after the final `v598` completion. These are off the regional handoff.
- **Design/scientific thinking:** unknown; no phase boundaries.
- **Implementation/data building/indexing/refactoring:** unknown. The curvature script mtime is 21:38:18, but file timestamps do not measure active implementation.
- **Validation:** `.001851s` is measured for the exact stored-array control; other validation time unknown.
- **Model execution:** approximately **11s** across the three `v598` attempts and `v599` by runner start/end marks. No regional model forward ran. Maintenance canaries ran 21:25:07–21:25:27 and 21:55:42–21:55:59: **37s serial**.
- **Interpretation:** unknown.
- **Documentation/review:** the mathematical review was modified through 21:47:07; its start and active writing time are unknown. This review began at the authoritative 21:57:22 phase mark.
- **Idle/blocked:** unknown. Commit gaps and unmarked intervals are not converted into idle labor; parallel durations are not summed.

The actual-date `RESEARCH_PHASES_2026-09-19.jsonl` contains sparse hourly-review boundaries at 20:55 and 21:57, but no science, implementation, validation, mathematical-review, or publication mark in this window. It therefore covers only review boundaries and cannot allocate labor. Commits `899baad5f` (21:36:42), `dbea5f970` (21:37:33), and `067603198` (21:44:45) record the temporal work; the regional curvature files remain uncommitted. Both queue files were empty when inspected.

## PROCESS_IMPROVEMENT

Ranked by likely time saved versus cost: (1) a declarative row/arm/null manifest over the existing temporal/regional executor, likely avoiding the observed `v598` wrong-row/price retry and saving roughly 5–7 minutes per such failure at low-to-medium implementation cost; (2) the canonical regional path cross-link, likely saving 5–10 minutes per handoff at low cost once ownership clears; (3) consistent science/implementation/validation/publication phase marks, low cost and valuable for timing but not scientific throughput itself.

No code or registry repair is justified inside this bounded review: the collaborator is actively modifying the same runner/registry family, the path registry and board are dirty, and a rushed shared-helper edit risks collision. The truthful 21:57 mark repairs only this review's future boundary coverage; it does not backfill the missing categories. The next safe shared repair is the manifest after current temporal work settles, not another compiler or publisher.

## Explicit verdicts and handoffs

- `TRACK_ALTERNATION: PASS` — previous `CIRCUIT`; current `WEIGHT_FOLDING`.
- `TRACK_PROGRESS: PASS` — the prior circuit hour produced a substantive regional response-algebra receipt that falsifies the head-only suffix surrogate, although the declared per-module joint census remains undone.
- `CEREMONY_BUDGET: FAIL` — measured regional science was under one second while review/health ceremony was materially larger; unknown labor cannot establish the opposite.
- `NOVELTY_LESSON_GATE: PASS` — circuit/path/graph registries, regional module dossiers, the five-property authorities, seven-term/rank receipts, and the existing five-arm artifact were checked; the plan preserves exact port closure, full `QK1*QK2*V`, distinct cells, matched nulls, and response-before-suffix.

**Highest-value action:** exact-replay and deduplicately price the complete `residual6 -> attention7/MLP7 -> head8.2 -> norm-closed MLP8 -> head9.8` native delta with every reader-permitted self/ordered-cross term and all seven head products.

**CIRCUIT handoff:** run the joint per-module response/curvature census through the full native suffix, then apply fresh structurally varied cells, matched splits/equal-norm controls, three unrelated readers, and full logits/CE before naming a cross-module suffix.
