# Native installation of the conditional source-column executor

Replacing attention11 forward with the explicit source-column update passes the registered instrument and all16 opened fidelity/selectivity cells. Zero-edit baseline error4.77e-6 absolute logits; largest effect discrepancy versus the joint-query-frozen native path4.77e-6 absolute and1.413e-4 relative to full number-effect norm. Counts12prefix,4baseline attention preparations,16suffix calls;1.34s reported run. This validates native integration at the stated precision and intervention boundary.

The replacement does not call native attention11 on edited inputs. It computes only the nominated source's K1/K2/V projections, native head RMS and native rounded RoPE, then evaluates the two routing products and output-projected writers. It retains all heads and interactions. Baseline output and query/key/value context are produced by native attention; source input RMS and later suffix remain native. The adapter currently uses the uncached old-writer path, correctly priced at2d² output-projection multiplies per edit. No parameter savings or full-model extraction is established.

Positive-result limitations: selected amplitudes came from per-input oracle gradients, input panel is opened, and a single-token perturbation limits query changes to one row. Full native suffix predicts final effects; this is a conditional local executor, not an independent behavioral predictor. Zero-edit and reference replay support implementation correctness but do not prove semantic mechanism.

Next CPU consequence executed: SOURCE_OOD_V4_*_ROWS.json freezes48 new exact texts (six noun pairs, close_to and next_to_subject structures, congruent/opposite number) and96 nominated sites. Tokenizer checks enforce unique single-token noun positions, answer orientation and disjointness from the prior listed control/OOD panels. No model outcomes used. Freshness means new text/construction relative to these panels, not a claim that every noun was unseen anywhere in the repository. The source-column implementation and fit-only role bank will be hash-bound before evaluation. All nine outputs and both roles remain required; native capability and selectivity failures will be retained.

Primary result fixed_query_native_install_v1_result.json, managed log run_fixed_query_native_install_v1.log, preregistration FIXED_QUERY_NATIVE_INSTALL_V1_PREREGISTRATION.md. Executor native_fixed_query_source.py; conditional algebra fixed_query_source_column.py. Full goal active.
