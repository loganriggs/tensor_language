# Hourly strategic review — 11 September, 15:22 UTC

Circuit targets: explicit read/operation/write specification; grouping across modules and splitting within them; held-out/OOD prediction; executable extraction or sufficiency; selective manipulation; composition/reuse; stable identification. The full goal is a simpler executable model explanation with these properties. The user's weights-first direction remains authoritative.

The hour produced a discrete shared-parent graph move and joint weight refinement, followed by a stability red-team. LL1 groups disagree across starts, but one shared lower-level function survives. Checking sparse support and prior artifacts shows that it is almost entirely the previously known square150, not a new hierarchy. Its exact native reader/write pair is now durable. This prevents an inflated novelty claim and supplies a frozen interface.

On cached FineWeb, deleting the square selectively raises the chosen quote-token scores relative to four punctuation controls. Its prediction of whole-reader removal fails because that intervention also removes mixed branches. The proposed quote-context gate then fails: activity is higher at quote targets, and native quote-family top1 misses the registered bar. These are historical local screens, not OOD evidence or a quotation circuit. No weights were fitted to text. No new four-property circuit was identified during the hour.

The shared-square-bank kernel now supports joint optimization with reused nodes, but canonical eigen-square clustering yields essentially no native graph savings. An executed uv/uw counterexample shows why this proposal rule can miss exact shared linear computation. The alternative input-subspace census finds15/17 mixed-parent proposals; most connect two groups. Onlyone/zero connect at leastthree, missing the broader-sharing target. Some groups have multiple proposed parents, but these overlapping components cannot simply be added into an executable graph.

The highest-information optimization action is now live: projected LL1 with exact equilibrated output solves and explicit local stopping criteria. It changes the solver rather than appending identical short fits. Two wrapper-only enqueue schema failures are preserved; V3 retains the same mathematical kernels and registered bars. The managed run started15:21:15. At15:23 the first start was at iteration450 with11.807%capture and gradient2.09e-4, so convergence is not yet established. No result is inferred from the time budget.

Next actions: (1) interpret the projected run against its numerical, convergence, and group-stability predictions; (2) derive and execute correct joint accounting for overlapping input-parent proposals; (3) compare proposals from better-optimized fits before scaling graph search. Demote unchanged near-identical-square clustering, another quote-token narrative, and data-guided fitting. The graph question can change explicit computation and reuse; its measurement must include a valid joint executor, not only node counts or coefficient capture.

Confounds include postselection, historical data, arbitrary LL1 boundaries, partial cancellation, eigenbasis ambiguity, near-collinear parents, overlapping components counted twice, poor optimizer convergence, and output loadings mistaken for semantics. Repeated input readers can be shared even when their square eigenvectors differ. Whole-function cosine is not individual-unit stability. Earlier misses and the square150 alias remain authoritative.

Foreground phase labels14:22–15:22: implementation41.46min, science6.70, publication6.18, review5.66. Science plus implementation48.16min exceeds overhead11.84min. These are labelled elapsed intervals including recovery, not CPU timings, and GPU time is not added. ManagedGPU finished a peer job15:00:33 and began this run15:21:15; method/control authoring and enqueue-format repair occupied that gap. Do not hide it by counting many small controls as circuit throughput. The two local quote screens were fast to execute but do not meet the full circuit identification standard.

- CIRCUIT_FOCUS: PASS within explicit weights-first authorization. Shared computation, prior aliasing, and bounded intervention screens target identification/reuse; no compression-only adoption claim.
- CEREMONY_BUDGET: PASS on the labelled intervals. Preserve the prior hour's failure; reuse existing kernels and keep this checkpoint short.
- NOVELTY_LESSON_GATE: PASS. Existing square, shared-reader, pronoun, calibration, and module records were checked. The apparent new node was correctly linked to square150.

Concrete continuation: managed projectedLL1 V3 is live, and the shared-subspace parent census has actually run. No authority blocker. Next hourly16:22, mathematical16:51.

Recorded 2026-09-11T15:26:42.968840+00:00.
