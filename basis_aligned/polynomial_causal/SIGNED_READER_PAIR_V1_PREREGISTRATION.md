# Native signed reader-pair product and its output consumers

2026-09-10. Original bilinear handoff/pilot authority. Previous vocabulary-energy
formula is closed. Its post-result weight witness identifies a lost sign
interaction. This is a new specified bilinear operation, not a gain/rank sweep.

Recover e1,e2 from the frozen normalized-input pair in
CALIBRATION_READER_ENERGY_V1_WITNESS.pt. These are the two largest-eigenvalue
directions of the fixed centered-unembedding Gram, also the largest Q cross
coefficient in its eigenbasis. Selection used existing weights, not the following
native outcomes. No alternative pair is eligible after this screen.

For native normalized MLP17 input u, compute a=e1^T u,b=e2^T u and the actual
vector writer v=D[(Le1)*(Re2)+(Le2)*(Re1)]. The complete native MLP polynomial
contains a*b*v; its remainder is retained. Neither e1/e2 nor v is fitted to
activation outcomes. Native output bias belongs to the remainder.

The same input projections enter the residual-skip unembedding numerator as
rho*a*Ue1 and rho*b*Ue2, where pre-MLP residual=rho*u. This describes algebraic
reader sharing, not independent extraction of u or proof of a semantic variable.
The extracted term has3456 scalar coefficients in three1152-vectors, but the
full native MLP remainder/upstream model still remain and are charged.

Split v into v_cal=P_w v and v_other=(I-P_w)v using the OLD frozen calibration
direction only as a readout audit. Seven interventions plus native: remove full
a*b*v; remove its calibration projection; remove its complement; remove three
fixed random orthonormal reader-pair terms (seed9111236); remove old complete q*w
as reference. Recompute native final RMS and30tanh for every arm. No norm or gain
matching of controls after outcomes; report their natural native effects.

Predictions:

* A instrument:22bodyforwards86length256 sequence instances; finite outputs;
  e1/e2 orthogonality error<=1e-10; scalar weight correspondence
  w^T v/(w^T w)=2e1^T Qe2 within1e-10 relative; independent facade and online
  full/calibration/complement removals on first4rows eachcohort agree with
  formula <=1e-3abs and<=1e-5rel. Native formula samebar. CPU mixed-difference,
  polarization and sign-gauge controls pass.
* B calibration necessity: full-pair removal rare CE damage>=.10nat and
  frequent CE improvement>=.02nat on BOTH42FW and16Pile cohorts.
* C calibration projection sufficient for full-pair effect: centered
  vocabulary error ||delta_cal-delta_full||/||delta_full||<=.10 AND frequent/rare
  CE effect differences <=max(.002,.20*abs(full effect)), BOTHcorpora.
* D random specificity: mean absolute random rare CE changes <=.10 times
  full-pair rare damage (full damage must be positive), and mean absolute
  random frequent changes<=.05nat, BOTHcorpora.

Report complement effects and nonlinear interaction without assuming additivity.
Cfailure need not make the fullpair inert: it can indicate a writer with several
effects. B/Dfailure closes the proposed calibration screen; no pair/gain/rank
rescue. Earlier q positives and stability failures remain. Reused evaluation
texts, FineWeb document grouping unknown, Pile corpus shift only. Full545902902
nativeparams retained; <=900seconds managedruntime,8readoutarms over58rows.
15native evaluation batches+1facade+6online bridges=22forwards86sequences.
