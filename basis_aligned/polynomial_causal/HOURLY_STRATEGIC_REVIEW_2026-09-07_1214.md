# Hourly strategic circuit review — 2026-09-07 12:14 UTC

## Outcome and throughput

Circuit depth remained the active lane. Since the 11:14 review, six GPU experiments in the temporal/
is-was chain reached terminal receipts: corrected weight-edge atlas, joint source factorial, pairwise
Möbius pilot, ordered conditional deletion atlas, adaptive greedy deletion, and five-MLP source-subspace
test. Their GPU runtimes were approximately 111, 93, 102, 111, 120, and 125 seconds. This is one
substantive causal result about every 8–10 wall-clock minutes, with the GPU work itself near two minutes.
The mathematical review also landed at 11:26 and converted singleton overcount into an explicit
Boolean-lattice interaction object.

## Circuit state change

The hour reduced an ambiguous 46-component source graph to a much sharper hierarchy:

1. The task-specific atlas source sets are not task specific when installed jointly. Their shared eight
   MLPs reproduce both task-rank-four responses at approximately 1.0 and behavior at `.883/.896`.
2. Singleton contributions overcount drastically. Pair interactions are real and concentrated: the
   eight largest contain `.832` of pair-norm mass and almost all large pairs lie among MLP0–4.
3. Degree two is not executable. The full singleton-plus-pair reconstruction has projections
   `-.929/-.551` and RSE `4.23/2.91`; ordered higher interactions dominate.
4. Every one-module deletion from the shared eight remains functional. Ordered/full-context scoring and
   adaptive deletion reach the local five-MLP boundary `MLP0,1,2,3,6`, with response minimum `.954` and
   behavior `.7502/.8319`.
5. No further whole-module deletion is eligible, and controls remain non-selective (median KL `.0340`,
   top-one flips `.125`). Structural pruning is therefore exhausted.
6. Preliminary projected-source measurements show a sharp fidelity/selectivity tradeoff: rank-2 DIM
   yields negligible collateral (KL `.00013`, zero flips) but only `.447/.483` response projection and
   `.383/.433` behavior; per-row-mean SVD rank 8 reaches only `.627/.644` response while remaining highly
   selective. The DIM complement retains substantial causal response, so the live write is not a
   two-dimensional family mean.

The last measurements are not yet a valid scientific terminal because v1's monolithic authority gate
returned false without constituent diagnostics. An independent row audit establishes 54 training and
48 evaluation identities with zero overlap. A model-free atomic scorer correction is claimed and ready.

## Largest avoidable sink

Model runtime is not the bottleneck. The avoidable sink was **aggregate scorer design**:

- The pairwise v1 D gate called a mere `.10` RSE improvement “degree-two adequacy,” even though the
  reconstruction remained grossly wrong. That required an invalidity audit and v2 rescore.
- The source-subspace v1 A gate combined hashes, split identity, finiteness, and replay into one boolean
  and did not publish its constituents. Its false value is therefore not locally diagnosable despite
  independent recomputation of every preserved constituent.

These corrections consumed more authoring time than any GPU run. The bounded process change is now:

> Every authority/adequacy prediction must publish its atomic conjuncts, and a reconstruction called
> executable must directly bound both signed projection and relative error—not merely improve a worse
> baseline.

This applies immediately to the active v2 source-subspace scorer. Future runners will include an
`atomic_instrument` map and derive the aggregate prediction only from that map. This is not a tooling
detour: it prevents false scientific branches in the live circuit chain.

## Next circuit action

After the atomic v2 rescore, the likely next branch is a source-basis ladder fitted to **all position-
level deltas**, not per-row mean deltas. The current SVD fit captures `.98–.995` of per-row-mean training
energy but only about `.64` of fresh causal response, proving that averaging positions erased the
contextual write. Test ranks 8/16/32/64 under the same five physical modules, sealed targets, controls,
and complement arms. If a selective functional rank exists, validate it reverse-direction OOD and then
contract those source bases through exact downstream weight tensors. If no rank lies on that frontier,
fit weight-readable covectors from the frozen downstream rank-four response modes rather than optimize
another same-family complement loss.

Next hourly circuit review due after **2026-09-07 13:14 UTC**.
