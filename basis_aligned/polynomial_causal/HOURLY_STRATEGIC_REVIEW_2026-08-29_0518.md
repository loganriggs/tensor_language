# Hourly strategic review — 2026-08-29 05:18 UTC

## Bottom line

This hour produced two real outcomes, not merely runners.

1. Family-F reporting recovery succeeded, but the registered refitted programs fail
   the faithful-port gate.  Downstream consequence selects better MLP3 gates, yet the
   subsequent local decoder refit damages downstream behavior.
2. The map/table frontier completed on three document roles.  Rank-512 maps matter
   only when table rank is 256 or full; at table rank 64 or below they buy essentially
   no CE.

The strict whole-model balance sheet does not move: 36/36 structural component types
are inventoried, but only 5.3481% of model storage has a strict removal certificate,
10.923% of the named causal CE denominator is recovered, 4.72714 nat remains unnamed,
and terminal extraction/removal/OOD actions remain 0/68.  Family F is useful
mechanistic evidence but fails its registered port gate, so it earns no global credit.

## New evidence

### Family F

The v2 recovery reconstructed the exact spent v1 program and reran the frozen fit
report in 75.26 seconds.  It loaded 480 fit rows, zero validation/final rows, and no
ground-truth target tokens.  Result/receipt semantic replay passes.

At K512, the registered downstream-selected and locally refitted program has
document-balanced teacher KL `0.08476`, versus `0.10077` for matched random and
`0.08862` for activation-selected Family A.  Thus suffix consequence improves support
selection.  But its summed-write NRMSE is `0.70275`, far above the registered `0.20`
gate, so validation cannot open.

The informative anomaly is an objective conflict.  The same K512 support with native
Down has KL `0.05772` but NRMSE `0.86957`; local decoder refitting improves NRMSE to
`0.70275` while worsening KL to `0.08476`.  At K256, refitting similarly worsens KL
from `0.08041` to `0.14913`.  A cost-free affine fold reaches `0.07534`, still worse
than native Down.  Therefore local Euclidean reconstruction is not merely incomplete;
at this interface it actively moves away from the downstream objective.

### Rank-512 map/table interaction

The rank-512-over-rank-64 map CE gains for table ranks full/256/64/16/8/4 are:

| role | full | 256 | 64 | 16 | 8 | 4 |
|---|---:|---:|---:|---:|---:|---:|
| skip7000 | .04465 | .03582 | .00020 | .00000 | .00000 | .00000 |
| skip11000 | .04832 | .03968 | .00044 | .00000 | .00000 | .00000 |
| skip1200 | .04070 | .03558 | .00028 | .00000 | .00000 | .00000 |

All registered controls pass, including exact covered-arm invariance and reproduction
of all 18 prior rank-64 figures.  The reason is algebraic: maps are fitted to
rank-truncated table targets, so a map rank at least the table rank already spans the
target.  The practical rule is `map_rank <= table_rank`, with the empirical map ceiling
at 512.  Shared-output RRR should therefore be priced mainly at table rank 256/full;
claiming a universal 42.5M-map saving would be misleading.

## Largest remaining gaps

1. **Composition:** independently good local replacements still interact strongly,
   and no small state predicts unseen multi-component compositions.
2. **Semantic coordinates:** low-rank output bases are rotation gauges, not named
   features.  Compression does not yet enable selective edits.
3. **Objective alignment:** Family F shows that improving local NRMSE can worsen the
   actual suffix distribution.
4. **Fresh OOD evidence:** current discovery roles are repeatedly exposed.  Promoted
   candidates need new registry-excluding document roles and replication.
5. **Terminal utility:** no completed 68-action extraction/removal/OOD comparison
   exists, so no simplicity metric has yet proved it enables a selective edit.

## Candidate actions considered and pruned

- More recursive or self-fitted stream maps: pruned after two decisive failures.
- Rank-512 maps with table rank <=64: pruned by three-role exact-zero/near-zero gains.
- Validation of registered refitted Family F: blocked by its prospective NRMSE gate;
  opening validation would violate the experiment contract.
- Repeating the infinitesimal Fisher tangent panel: pruned; the completed split panel
  has full registered ranks and all stability gates fail.
- Generic HOSVD, weight SAE, hierarchy, or MDL scores without a held-out downstream
  consequence: deferred; they duplicate the local-reconstruction failure mode.
- Retrospectively promoting Family F's native-Down diagnostic: forbidden.  Its signal
  may only motivate a new prospective experiment.

## Ranked next five

1. **Real 36-site shared-output RRR.**  Highest whole-model reach and cheapest direct
   test of whether a gauge-invariant factorization halves the useful high-budget map
   storage while preserving CE.  Exact equal-storage independent allocation is now
   implemented and tested; the remaining work is the source-closed autonomous runner,
   audit, then one discovery execution.
2. **Prospective native-Down MLP3 finite-edit family.**  Highest new causal signal from
   this hour.  Freeze the native decoder, downstream-selected support, finite positive
   and negative perturbations, matched random/permuted controls, and a fresh document
   role.  This tests behavioral extraction/editability rather than relabeling it a
   locally faithful port.
3. **Finite gauge-transport triangle.**  Lifecycle-harden the unopened L8->L11->L14
   runner and test direct versus chained vector/CE transport on unseen compositions.
   This is the best E3.2 move because it uses finite same-forward interventions rather
   than repeating failed infinitesimal or token-splice Hankel objects.
4. **One behavior-anchored terminal circuit.**  Freeze matched positive, negative,
   off-target, and natural-text/OOD suites for one terminal behavior, then measure
   extraction and removal together.  It has the clearest utility metric but currently
   lacks the required prospective collector/reducer.
5. **Sparse gauge for a passing shared subspace.**  Conditional on E2.1, rotate only
   fit data with a fixed sparse dictionary objective, then require cross-role support
   stability and concentrated single-coordinate effects.  It is premature if the
   dense shared projector itself fails prediction.

The ordering favors expected information gain, causal relevance, composition, clear
falsification, and reuse of already frozen objects.  It demotes work that merely lowers
local MSE or repeats an exposed negative interface.

## Action executed this hour

- The highest-priority queued action, Family-F v2, completed and published receipt
  last; independent semantic replay passes.
- The exact matched-storage independent RRR comparator was implemented, tested 16/16,
  committed, and pushed as `95acfeb0`.
- The E2 real-data preregistration now freezes the physical input/output, scale-relative
  ridge, global/typed/independent grammars, exact-price allocator, equal-storage
  global494-versus-typed481 comparison, autonomous all-site dispatcher, discovery-only
  claim boundary, and fresh-role requirement for promotion.
- The earlier tangent result was audited into E3.1 as a bounded measured negative; it
  is not being rerun or miscounted as new execution.

Next safe work is implementation and adversarial testing of the real E2 runner while
the newly free GPU remains unused.  No E2 evidence cell is checked until real factors
and CE are published.
