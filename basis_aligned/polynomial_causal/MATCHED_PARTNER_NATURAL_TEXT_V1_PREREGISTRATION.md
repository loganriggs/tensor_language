# Frozen natural-text component validation

144endpoints,48document units:24FineWeb skip39000 documents and24historical Pile test documents. FineWeb is the model training distribution; Pile is reported as corpus shift. All921existing add_ing spelling pairs define the two groups; targets are not POS annotations. Within each selected eligible document use first ing target after128tokens, nearest base-form target and nearest other target. Random document selection seeds73101/73102, no model capability filtering. Nearby windows may overlap; uncertainty must resample documents jointly. Source caches were used historically by other studies, so do not call them globally untouched. The current factors are weights-only.

Evaluate actual native prefixes and compact frozen branches3/8 at their x16/den ports, plus joint removal. Full native unembedding/RMS/tanh scores actual next-token CE. Positive CE change is damage. Keep all endpoints on capability failure. Native-factor/packed write replay and full model-hook controls protect execution.

A:20body forwards/160sequences,128input tokens, full provenance and finite outputs; native and branch8 physical-hook logits versus manual tail relative error<=1e-5, MLP17input<=1e-6, compact/native-factor writes<=1e-5. B: in EACH corpus branch8 ing mean CE damage>=.002 and ing-minus-base damage>=.002. C: branch8 mean absolute CE change on other controls<=.02 in EACH corpus. D: actual next token native rank<=20 on>=50%of ing and base groups in EACH corpus. E: joint3+8 CE effect agrees with sum of separate effects to mean absolute error<=.02 in each of6corpus/group cells. Report signs, activation coefficients, output loadings and ranks even on failure.

Null: the branch's readable ing output and controlled-template transfer do not predict selective natural-text support. Spelling categories may mix grammatical roles, and signed input products can reverse output preferences. These are causes to audit, not reasons to remove observations or change bars. No fitting, rank selection or task-direction learning. Compact joint1,994,688FP32floats; native input-state production/U/background remain external. A local exact replica is not an end-to-end OOD model.
