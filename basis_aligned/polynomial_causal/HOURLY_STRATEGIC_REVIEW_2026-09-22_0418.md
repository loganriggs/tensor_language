ACTIVE_TRACK: WEIGHT_FOLDING

22 September 2026, 04:18 UTC. The user's two-day weights-first focus through 15:10 overrides hourly alternation. Prior hourly review 03:17; latest full mathematics/literature review 01:59, next due 04:59. The bounded Gaussian-noise literature lookup at 03:25 did not reset that clock.

The complete goal remains a simpler, executable, identifiable circuit with explicit reads/operations/writes, grouping across native boundaries, held-out and OOD prediction, extraction, selective manipulation, reusable composition and stable identification. Selected-output numerator reconstruction is not that goal. Preserve the broader midpoint path and its separate evidence; current quartic screens cannot supersede it.

## What changed this hour

Gaussian finite-response readout optimization did not meet its improvement criterion. Calibration-label regression improved text scores using more information than raw mean/covariance, but left substantial component errors. Response-oriented toy feature learning did not consistently beat value learning; the 250-versus-500-step comparison exposes a strong duration confound, especially for Muon. Do not interpret a short native optimization failure as representational impossibility.

The second graph stage is concrete: exact reassociation after reader sharing saves nine or ten additional nonlinear products; float32 finite-response replay passes. Whole-quadratic approximate sharing saves more products at equal reader count, but the primary 128-edit version fails response preservation. Its local Gaussian score understates text error. Joint edit interference is stronger on text; normalizing Gaussian probe radii changes errors only 4–8% and does not explain the 2.7–3.8-fold text discrepancy. These diagnostics support a measure mismatch, not a new valid circuit.

The decisive new capacity audit gives both current mixed-CP512 dictionaries evaluation labels and solves exact unregularized finite-panel least squares. All four value/response design matrices have numerical rank 512. Independent QR and SVD minima agree to 1.1e-16 in per-output relative error; normalized orthogonality residuals are below 6.2e-15. Every output 4–15 still has 26–49% value error and 26–46% response error. Thus these particular frozen feature spans cannot meet a 10% per-output criterion even on the measured states. This is not a population theorem or a lower bound on differently learned CP/Tucker/HT/DAG representations. Earlier root-1 capacity oracles used different dictionaries and do not contradict it.

## Decision and next actions

1. Keep the queued native output-local quartic learner as the primary next experiment. It changes feature directions and therefore addresses the now-demonstrated fixed-span limitation. Inspect optimization history, value and response metrics, both starts, and absolute errors. Its Gaussian measure and 250-step duration remain possible failure mechanisms. It protects coordinates 0–3 and cannot repair coordinate-1 removal failures.
2. Interpret the queued coordinate-1 removal-stage audit independently. Distinguish numerator error from normalization/readout amplification. Then run the prepared all-16-output cross-domain CPU audit on its cache. The native reference preflight passed; the FineWeb/stdlib documents are opened, so this is diagnosis, not untouched OOD confirmation.
3. Stop additional fixed-dictionary readout sweeps unless a question explicitly concerns a different feature class. The oracle audit now quantifies their limits. Stop further radial/graph-score microvariants unless they change the feature-learning objective in a way worth testing.
4. If new directions improve both values and responses, prioritize native replacement, per-document tails, fresh domain transfer and composition before further compression. If they fail, compare optimization traces against known-positive toys before changing the architecture. No semantic labels or circuit adoption from a lower loss alone.

## Confounds and throughput

The exact fold still targets the pure quartic MLP16→MLP17 contribution and 16 selected output directions, excluding other residual, attention, bias and cross terms. Empirical label-access oracle errors are explicitly in-sample diagnostics. Gaussian moment geometry is not coefficient Frobenius geometry or empirical eighth-moment geometry. Logical product savings are not measured runtime savings. The value and response oracles have different coefficients and cannot be combined into one claimed candidate.

Completed own commits since 03:17 include de9092979 (Gaussian responses), 8f6acee9b (calibration moments), 201190a0b (toy feature learning), 1ca8ba6c7/26f142b8b (exact graph/precision), 87b0f2abf (quadratic sharing), 1cb582761/24da30dad (interference/radius), and 4433d448e (cross-domain consumer). CPU scientific kernels include 68+14 seconds for learning toys, about 12 seconds for quadratic sharing, 13 seconds for radial probes and 2.6 seconds for the new oracle audit. Authoring/review dominated many short diagnostics; exact wall-time categories were not instrumented. This hour produced several related metric notes; consolidate them and stop that branch now that a stronger span test has resolved the actionable question.

The managed runner PID1070231 is still live on Pythia v762, started 03:49:49. Own residual and geometry jobs remain queued behind three other jobs, with hashes 8952631c... and 37674108... unchanged. No terminal own results exist. Do not reorder another agent's jobs or confuse queue waiting with experiment runtime. CPU follow-ups reuse the pending capture rather than add another GPU pass.

TRACK_ALTERNATION: OVERRIDDEN by explicit user authorization.
TRACK_PROGRESS: PASS, explicit graph reuse plus a finite-panel limit on current native feature dictionaries.
CEREMONY_BUDGET: NEEDS REDIRECTION: repeated short diagnostic notes were useful but no longer highest-information; stop variants and return to new feature learning/native transfer. No new testing framework or duplicate capture was introduced.
NOVELTY_LESSON_GATE: PASS, current mixed512/all16 oracle differs from the earlier coefficient400/root1 oracle and the calibration-ridge experiment; their scopes were checked before execution.

Organization: the 04:17 for_logan explanation supplies context, a flowchart and the exact restricted conclusion. Link the capacity audit from the path record and retain pending status for the cross-domain consumer. No circuit-registry promotion. Continuation receipt is the completed CPU capacity audit plus both immutable jobs verified in the managed queue. Goal active.
