# Successor fixed-pointer length-six confirmation V1

The frozen typed-group screen selected `pointer_all`, the ten complete attention/MLP responses at layers 8–12. This confirmation changes sequence length before reading any new native outcome. It uses every valid length-six window with a two-step backward and forward donor: month final indices 5–9 and digit indices 5–7. The coherent endpoint stays fixed. The early control swaps the first pair; the late incoherent arm swaps the penultimate pair. All 24 rows are generated and hash-frozen without loading the model.

Only the preselected `pointer_all` group is tested. Transplant its coherent final-query responses into aligned late and early recipients. Retain the V2 exact self/all-module ceiling, native capability, target, projection, cosine, and control bars unchanged. Confirmation requires projection at least .50 and cosine at least .70 in both families, with both control RMS fractions at most .50. No fallback to all-attention, subgroup, head, gain, row, or threshold change is allowed.

Price is seven forwards over eight sequences each, 56 evaluations, zero fits, gradients, ranks, or updates. A pass confirms a native-oracle typed response mediator across sequence length; extraction and weight compilation remain open. A miss is the final result for this fixed group grammar.
