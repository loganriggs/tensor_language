# Hourly strategic review

ACTIVE_TRACK: WEIGHT_FOLDING

Actual UTC: **2026-09-19T15:41:35Z**. Next deadline: **2026-09-19T16:41:35Z**. Previous review: [14:39 CIRCUIT](HOURLY_STRATEGIC_REVIEW_2026-09-19_1439.md), 1h01m55s old. This is one live review, not an offline backfill. Historical `/workspace` paths were resolved against this checkout. No GPU model, agent, job, queue, service, timer, commit, push, external contact, experiment/result, or `TYPED_FACE_EXTRACTION_V1` implementation was changed. The sole bounded CPU/process repair was the truthful 15:41 phase mark.

The previous review declared `CIRCUIT`; this review therefore alternates to `WEIGHT_FOLDING`. The circuit handoff—regional forward-response census followed by the full-suffix five-arm factorial—did not run and is preserved unchanged for the next circuit hour. The interval instead produced one off-track but relevant fold/weight-only receipt: the 1.26-second head9.8-to-block10 consumer-rank certificate.

## Seven circuit targets and full goal

1. **Computational specification:** identify what is read, the operation/composition, what is written, and every downstream user.
2. **Cross-boundary grouping and within-module splitting:** group pieces across native modules when a reader treats them as one variable, and split a module when its parts serve different computations.
3. **Held-out and OOD prediction:** predict activation and behavioral effects on unseen inputs, task variants, and shifted corpora.
4. **Extraction or sufficiency:** execute an isolated circuit at a declared boundary with every retained port and background named.
5. **Selective manipulation:** removal, swap, or edit changes the intended behavior against matched controls while preserving unrelated behavior and accounting for redundancy/interactions.
6. **Composition and reuse:** shared subcomputations serve multiple tasks/modules and their joint behavior follows a tested composition law.
7. **Stable identification:** units survive document/corpus splits, gauges, and fitting restarts, or are defined by downstream operational equivalence.

Goal: a smaller transparent tensor program that **predicts held-out/OOD behavior, is extracted at a declared boundary, supports selective manipulation/removal, and composes/reuses**. Simplicity is a fifth, separately measured property: stored scalars/bytes, product nodes, readers/writers, open states/ports, graph edges, peak state, compute, and runtime. Lower error, rank, storage, variance, or CE cannot replace a missing behavioral property.

| Property | Current evidence | Verdict |
|---|---|---|
| Held-out/OOD prediction | Regional intermediate extraction covers 20 distinct FineWeb document/context cells and Pile tests, but packed FineWeb direction is `88/102`; no frozen end-to-end regional formula passes structurally varied cells. | **PARTIAL** |
| Extraction at a declared boundary | Residual6-to-attention8 and norm-closed MLP8 packages replay with declared ports; complete head9.8 plus suffix is open. | **PASS only at intermediate boundaries** |
| Selective manipulation/removal | Scoped fresh regional edits beat same-site nulls, but the complete path lacks one frozen equal-norm, three-reader, matched-split, and full-logit battery. | **PARTIAL** |
| Composition/reuse | Regional key/value `interaction/smaller=2.3448`; additive lower bound `.5862`; matched random-split specificity fails. Gender transfer lacks the complete smallest-piece/null test and extracted price. | **FAIL overall** |
| Measured simplicity | Local package prices and exact full-vector rank floors exist. The new seven reader witnesses rule out exact width `<128` at tested block10 linear boundaries, but no deduplicated residual6-to-suffix price or matched-effect advantage exists. | **LOCAL ONLY** |

Claims remain separated as **fold/edit/response/fit** and **fresh/opened/replay**. A closed Mobius face is accounting, not evidence that interactions are small relative to the smallest live piece or specific against matched random splits.

## Progress, failures, confounds, and alternatives

Since 14:39, no circuit runner, causal screen, result, commit, or registry/dossier update appeared. The 15:29 **fold/weight-only** CPU certificate exactly composes `O_9.8 in R^(1152 x 128)` with the first 128 rows of block10 Q1/K1/Q2/K2/V and MLP Left/Right readers. All seven `128 x 128` witnesses have exact real rank 128 (mod-prime determinants nonzero); K1 is numerically ill-conditioned (`condition number 299,295`) but its conclusion is not threshold-based. This kills a generic exact linear width reduction at those immediate boundaries. It does **not** establish reachability, task use, extraction, OOD prediction, causality, composition, or favorable price, and it does not replace the promised full regional replay.

Confounds remain: native source fields share residual/RMS inputs rather than being independent formal ports; attainable activations may occupy a smaller nonlinear manifold; first-128-row witnesses are not identified regional users; later RMS, products, final RMS, and softcap are nonlinear; baseline subtraction or frame mixing can manufacture apparent additivity; shared token difficulty and opened-panel selection can inflate transfer; dead singles invalidate interaction ratios; noise/precision floors can hide small terms; and post-selection invalidates fresh labels. The regional path must retain actual distinct `(template, city pair, endpoint)` cells rather than row counts.

Alternatives ranked by information per cost:

1. **Complete regional residual6-to-head9.8 replay and price** (chosen): closes the exact native port and reveals whether the proposed algebra is executable. Kill or repair it on failed term closure, off-support write, or native-delta replay beyond a preregistered numerical tolerance.
2. **Task-specific/approximate quotient after the response census:** may bypass the exact rank floor, but is premature until actual downstream users and a norm are declared. Kill it on fresh-cell prediction or matched-effect causal/control failure.
3. **Fold the late group one step toward tokens:** valuable eventually, but lower information while the existing head9.8 endpoint is not closed. Kill a proposed source group if full-product attribution or a fresh donation edit is nonspecific.

The current regional route survives breadth alternatives because it has the deepest exact extraction, distinct-cell fresh evidence, and selective same-site interventions. The rank result narrows the plan: close the full 128-vector computation first; do not launch another generic rank/reconstruction sweep.

## Chosen folding action: exact residual6 -> head9.8 delta

**Endpoint:** the complete head9.8 output-vector delta at every causal destination, immediately after `O_9.8` and before residual re-entry/block10. The later circuit hour will determine which suffix readers use it.

**Exact native factors and shapes:** token IDs and `residual6 [T,1152]`; all attention7 Q1/K1/Q2/K2/V maps `[128,1152]` and output slices `[1152,128]`; MLP7 Left/Right `[4608,1152]`, Down `[1152,4608]`, bias `[1152]`; the five native head8.2 reader maps and head8.2 output slice with the same attention shapes; MLP8 Left/Right `[4608,1152]`, Down `[1152,4608]`, bias `[1152]`; and head9.8 Q1/K1/Q2/K2/V `[128,1152]` plus `O_9.8 [1152,128]`. Preserve learned mixing/re-entry, causal mask, and rounded rotary maps. For `T=32`, each score field is `[32,32]` on 528 live cells and each value field is `[32,128]`.

**Included source terms:** expand every chosen residual into named residual6/background, earlier attention output, earlier MLP output, and installed city delta. In each bilinear MLP/QK numerator retain attention×attention, MLP×MLP, attention×MLP, MLP×attention, all background/source ordered cross terms, and delta self/cross terms that the endpoint reads. At head9.8 retain all seven nonempty finite-difference products:
`F(dp1,p2,v)`, `F(p1,dp2,v)`, `F(p1,p2,dv)`, `F(dp1,dp2,v)`, `F(dp1,p2,dv)`, `F(p1,dp2,dv)`, and `F(dp1,dp2,dv)`.

**Omitted terms:** only the unchanged baseline `F(p1,p2,v)` and algebraically unchanged sources that cancel in the delta. No QK factor, ordered cross term, normalization effect, or product interaction is dropped for convenience; the new rank certificate licenses no smaller exact block10 interface.

**Nonlinearities/background:** live RMS `x/sqrt(||x||^2/1152 + 1.1920928955078125e-7)`, bilinear MLP product (no SiLU), two bilinear QK scores multiplied by V, rounded rotary semantics, causal mask, MLP bias, skips, learned re-entry, and native unchanged context remain explicit. The final RMS, unembedding, and `30*tanh(logit/30)` are outside this endpoint, not silently linearized.

**Simplicity measure:** one deduplicated bill of independently stored FP32 scalars/bytes, token tables, product nodes (including the seven head products), projections, readers/writers, open native ports/scalars, graph edges, live score/value/state memory, multiply-add/product count, peak state, and measured CPU runtime. Shared weights/projections count once. Compare with the unfolded native regional slice and a matched-effect control; exact width 128 at the tested consumer boundary is a lower-bound constraint, not a simplicity win.

**Exact replay check:** freeze the implementation and source manifest, then compare the sum of all named terms with a native recomputation of the complete head9.8 delta on opened fixtures and outcome-blind distinct fresh context cells. Report per-sequence relative L2, maximum absolute error, off-support zero, each intermediate term-closure residual, and the seven-product sum against `head_joint - head_baseline`; set numerical gates before opening the fresh cells. Any mismatch is an implementation failure, not an approximate result.

**Later causal/predictive falsifier:** after replay freezes the formula, run the preserved forward response census before proposing a suffix, then the five-arm full-suffix baseline/routing-single/value-single/product-suppressed-both/true-joint test. Score signed regional prediction, full logits/CE, at least three unrelated readers, 16 equal-norm same-site directions, and matched random splits on structurally varied distinct cells. Normalize interaction by the smallest live single. Dead singles, sign failure, collateral breach, nonspecific interaction, or failure to beat the matched controls falsifies the claimed grouping even if replay is exact.

**Cross-track connection:** the seven-product fold defines the product-suppressed factorial arm without mixing native head interaction with downstream curvature; the factorial then identifies which folded routing/value terms a regional reader causally uses and may license a smaller consumer-specific program.

## Registries and missing cross-link

The circuit registry, computation-path registry, generated circuit-graph registry, MLP dossier index, and relevant head9.8/MLP7/MLP8 dossiers were checked. `PATH-REGIONAL-CITY-001` still ends at the head8.2 write. `CIRCUIT_GRAPH_REGISTRY_V1.md` inventories 49 packages but omits `city_mlp8_norm_closed_v1`; its two `four_trait_verified` labels do not price the fifth simplicity property. Module dossiers carry the head9.8 QK/carry and MLP8 cross-term evidence, but newer number/gender/person routes remain noncanonical.

Highest-priority missing cross-link: connect `PATH-REGIONAL-CITY-001`, its regional circuit record, `city_mlp8_norm_closed_v1`, the complete seven-product head9.8 endpoint, the head9.8->block10 rank certificate, and the pending response/factorial; explicitly mark suffix prediction, selectivity, composition, and total price open. The path registry is concurrent dirty state, so this review did not edit it.

## PAST_HOUR_TIMING

Window: **2026-09-19T14:39:40Z–15:41:35Z**. End-to-end serial candidate latency (`prior art -> specification -> capability -> causal screen -> dossier/null`) is **unknown** because no such candidate traversed that pipeline. The rank-certificate execution itself was **1.2586 seconds**, finishing 15:29:06Z. Its source mtime (15:28:54Z) and receipt time do not establish active implementation duration.

- **Design:** unknown; no phase boundary.
- **Implementation/data building/indexing/refactoring:** active duration unknown; one new certificate script appeared at 15:28:54Z. No data build, index, or shared refactor was observed.
- **Validation:** 1.2586 seconds for the exact CPU certificate; no causal validation.
- **Model execution:** no GPU model execution. Two health canaries ran 14:49:44–14:50:01 (17s) and 15:20:06–15:20:33 (27s); their 44 seconds are health cost, not research evidence.
- **Interpretation:** unknown; the 15:29 mathematical review records the interpretation but not its active duration.
- **Documentation/review:** the mathematical review file landed 15:31:45 and its board entry 15:32:02; duration is unknown. This review has a 15:41 phase boundary only.
- **Idle/blocked:** unknown; no interval is inferred from absent receipts or commit gaps.

The authoritative `RESEARCH_PHASES_2026-09-19.jsonl` contains sparse hourly-review boundaries through 15:41 and no science, implementation, validation, or publication boundaries. Coverage is therefore inadequate for labor accounting; marks do not imply continuous work. No phase time was backfilled.

## PROCESS_IMPROVEMENT

Ranked by likely saving/cost: (1) add top-level `started_utc`, `finished_utc`, evidence/evaluation labels, distinct-context-cell IDs, and literal-price fields to the existing receipt path—low cost, likely minutes saved per candidate/review; (2) add the canonical regional cross-link—low cost, likely 5–10 minutes saved per handoff; (3) replace sibling intervention runners with one declarative factorial executor—largest cumulative saving but medium collision/validation cost.

No shared helper, runner, registry, or result mutation is justified now: no new intervention candidate exposed safe repeated code, the relevant path registry is concurrently dirty, and a schema change needs equivalence tests across existing families. The one bounded repair was the truthful 15:41 phase mark. It improves boundary coverage but does not cure missing scientific phase instrumentation.

## Explicit verdicts and handoffs

- `TRACK_ALTERNATION: PASS` — previous `CIRCUIT`; current `WEIGHT_FOLDING`.
- `TRACK_PROGRESS: FAIL (OFF-TRACK FOLD RECEIPT; DECLARED CIRCUIT ACTION ABSENT)` — the exact rank certificate is substantive weight-folding progress, but the prior circuit hour produced no response census, factorial, causal screen, or honest circuit null.
- `CEREMONY_BUDGET: FAIL` — measured scientific execution was 1.2586 seconds while the visible durable work was mathematical review/certificate documentation; uninstrumented work is unknown, but there is no evidence that review/preflight was smaller than science.
- `NOVELTY_LESSON_GATE: PASS WITH DOCUMENTATION DEBT` — the certificate explicitly extends the prior head9.8 rank object to seven native consumers, preserves full-product/response-before-suffix lessons, and avoids another generic rank sweep; the known regional registry cross-link remains missing because its file is shared dirty state.

**Chosen action:** exact-replay and price the complete residual6 -> attention7/MLP7 -> head8.2 -> norm-closed MLP8 -> head9.8 vector delta as specified above; do not pursue a generic exact block10 width reduction.

**CIRCUIT handoff:** run the frozen regional forward-response census, then the matched-null full-suffix five-arm factorial on structurally varied distinct cells, retaining all seven products.
