# Frozen rank-eight response basis on competing geographic cues

Load the exported rank-eight consumer-response projector without recomputing it
and apply it to the frozen eight-edge source addition on all 48 rows of
`REGIONAL_COMPETING_CUES_V1_ROWS.json`. These rows have zero text and row-id
overlap with the discovery panel and introduce a competing geographic role.

Evaluate exactly seven fixed arms: response rank 8, source-PCA rank 8, four
seeded random rank-8 projectors (`202609160612..615`), and the unprojected
eight-edge source program. Keep the complete factor corner as the causal target
and retain all response, family, installation, removal, sign, and preservation
thresholds. No fresh gradients, basis selection, gains, or parameter updates.

Predictions: exact instrument/artifact identity; fresh response replay; fresh
bidirectional causal fidelity; fresh preservation; rank-eight projection retains
at most 30% of source norm; minimax score at least 10% below same-rank source
PCA; and score plus `.10` no larger than the random median. All must pass for
promotion as a reusable extracted writer coordinate.

Price: 48 fresh rows, 12 native/edited full-model executions over 96 sequences,
seven fixed candidates, 672 candidate suffix sequences plus 96 complete-corner
suffix sequences, zero backwards, fits, or parameter updates.
