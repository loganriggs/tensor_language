# Hourly circuit and systems review — 2026-09-05 06:54 UTC

## Circuit interpretation targets

A useful decomposition must state what information is read, what operation is performed, what is written, and which
later computations use it. It may join pieces of different native modules or split one head/MLP when behavior requires
that. It should predict held-out and out-of-distribution inputs, support an executable extraction or sufficient
interface, allow selective removal/swap/edit, compose with other circuits, and remain stable across data splits and
plausible coordinate changes. Literal storage and compute matter later; lower rank or reconstruction error is not a
substitute for these circuit properties.

The full program goal remains a smaller transparent tensor program that is predictive, composable, selectively
manipulable, and simpler under literal storage/compute/edge/state prices. This hour's immediate agenda remains building
reusable machinery and organized evidence for hundreds of high-quality circuits.

## What changed since 05:54

- Task14's TEST success and OOD construction-general failure were published into its canonical dossier as claim v8.
  This prevents the next agent from repeating the same OOD test or treating head 11.3 as syntax-general.
- The exact numbered-list T3/T7 source factorial had one invalid managed attempt: legacy imports set a no-model flag.
  The log was preserved, import-mode restoration was tested, and the unchanged experiment was rerun. Its live-instrument
  null showed both terms help successor lists but have large opposite-signed effects on repeated-index controls; their
  joint downstream interaction was +0.763 [0.715, 0.811]. Native heads are not the semantic split.
- Claude replaced an impossible sentence-terminal control. The model then screened 55 sites in 6.9 seconds and returned
  a real selectivity null. The new answer-changing control was itself nonselective at the patched prediction position;
  the reusable lesson is to use same-answer controls when testing collateral at the same endpoint.
- The bracket L13H8 source-region BASIC_SCREEN returned a live-instrument generic null but exposed a construction split:
  PREFIX was zero for direct pending brackets and about -0.8 after completion/reopening.
- A fresh SELECT authority confirmed that split in both directions.
- A second narrow fresh authority confirmed that nearly all OPEN+POST recovery is the semantic opener payload itself:
  OPEN recovery was 0.956–1.391, POST only 0.013–0.033, and OPEN-by-POST interaction below 0.001. The same executor was
  reused for all three runs.

## Timestamp and throughput audit

Authoritative science completions in the interval were: 06:25 numbered-list source factorial, 06:30 sentence-terminal
control repair, 06:38 bracket region factorial, 06:46 bracket family confirmation, and 06:52 opener-payload confirmation.
The 06:20 numbered-list process was invalid before science and is not counted as a terminal. There were five scientific
terminals rather than the six-per-hour stretch target. After the repaired first result, serial gaps were 5, 8, 8, and 6
minutes; median gap was 7 minutes, within the ten-minute operating target. The main lost block was the initial empty
queue plus import-side-effect diagnosis, not GPU compute.

Approximate time allocation from commits and run logs:

- scientific design, evidence interpretation, and execution: about 39 minutes;
- focused preflight/tests and semantic review corrections: about 9 minutes;
- canonical dossier/explanation publication: about 10 minutes;
- idle GPU waiting after a frozen candidate: under 2 minutes after 06:25.

No bespoke compiler, crash suite, OOD pack, or independent audit was attached to the basic screens. The two final-review
corrections—native capability and explicit predictions—used already-computed values and added no forwards.

`CIRCUIT_FOCUS: PASS` — every new experiment either split a native head by causal source role, tested selective transfer,
or repaired shared circuit evidence/controls.

`CEREMONY_BUDGET: PASS` — scientific design plus execution dominated validation; all three bracket runs together used
44 forwards, while focused CPU suites took roughly one second each.

`NOVELTY_LESSON_GATE: PASS` — each bracket claim cited the canonical dossier and prior source-factor null; Task14 was
canonized; activation reconstruction, rank reduction, and duplicate site scans were explicitly excluded.

## Confound audit

- **Baseline and nonlinear composition:** normalized recovery uses the complete-head interchange on the same row as its
  denominator. Exact Boolean-lattice terms showed OPEN/POST interaction itself is near zero, but PREFIX has a real
  construction-dependent sign; it must not be averaged away.
- **Shared token difficulty:** all delimiter pairs are balanced and effects reproduce in both donor directions across
  three disjoint lexical authorities. Native capability is 100% in every family/direction.
- **Leakage and post-selection:** the generic BASIC_SCREEN null is preserved. Construction-specific and OPEN-only bars
  were frozen before fresh SELECT/confirmation outcomes, with zero endpoint-text overlap with earlier authorities.
- **Dead intervention:** replay error is zero, the whole-head ceiling is positive on every target row, and same-state
  controls are live.
- **Precision:** POST recovery is small but materially above numerical error; the conclusion is bounded by the registered
  0.10 effect bar, not a claim that POST is mathematically exactly zero.

## Alternative routes and ranked next moves

1. **Exact opener-payload removal on fresh held-out rows.** This directly changes the selective-manipulation and necessity
   targets. It dies if deletion does not damage both constructions or substantially changes same-state controls. This is
   the current highest-information step and is actively being frozen.
2. **Construction-shift OOD confirmation.** Vary nesting depth, prose template, and delimiter context while preserving the
   same pending-opener state. This changes held-out/OOD prediction. It is premature if exact removal is not selective.
3. **Downstream-reader localization for the exact opener vector.** Patch or delete the opener contribution and restore
   named later attention/MLP writes to find its consumers. This changes computational specification and composition, but
   should follow necessity so it does not map readers of a nonessential signal.
4. **Weight translation of the identified source term.** Contract the fixed opener token/value map through L13H8's value
   and output matrices, then test whether downstream weights treat it as one operational variable. This changes extraction
   and stable identification, not rank. It should follow causal removal and consumer evidence.

The current route survives the alternatives because it has produced a replicated below-head semantic unit with a direct
next necessity test. No rank, quantization, activation-energy, or reconstruction experiment is licensed by this review.
