# Selective head8-to-head9 interaction ports on changed cue channels

12September2026 before native cache or port outcomes. Reuse72frozen newcue rows,
originalrank64package and firstfour weight-ordered MLP8value eigenmodes. Two native
arms: baseline and removal of selectivehead8only; head9is observed, not removed.
144forwards120seconds. Cache exact nativez8/r9, head8amplitude, head9routingGamma,
value v andscalarGamma v. Use existing packagedscalar and verifiedrouting helper.

A: nativebaseline margins replay previousnewcuebaseline<=1e-4; routing×value
replays packagedhead9scalar<=1e-10; symmetric portsum replays its change<=1e-10.
For eachfamily evaluate last-query scalarresponses without padding.
B: baseline-routing times actual valuechange predicts full head9scalarresponse
within10%relativeerror in allthreefamilies, with response norm>1e-8.
C: firstfour weightmodes predict the exact symmetrically allocated value-port
response within5%relativeerror in allfamilies, with valueport norm>1e-8.
Predictor uses nativebaseline9denominator heldfixed, exactnativez8before/after,
and actual average routing. Thus C is conditional value-port prediction, not
prediction of the routing port or full native head/logits.

Symmetric allocation: Delta(Gamma v)=DeltaGamma*(v1+v0)/2 +
(Gamma1+Gamma0)*Deltav/2. Report bothsigned alignedfractions andnormratios;
cancellation can make fractionsnegative or>1. Existing broadercomponent value-only
failures are known; this tests the actualselectivecomponent and newcuechannels,
including the new four-mode predictor. No fitting or corpusOODclaim.
