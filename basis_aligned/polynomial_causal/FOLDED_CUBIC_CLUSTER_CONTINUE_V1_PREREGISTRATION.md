# Longer matched continuation with exact cluster coordinates

12 September 2026. Circuit target: robust discovery of shared source computations under the existing regional consumer; later complete-function replication and physical intervention evidence remain necessary. The original native scalar regional circuit is unchanged.

Native audit preserves the same full fitted functions at fit/held positions to about1e-12. Normalized feature Gram conditions improve11996→300.28 and8367→12.11. The registered requirement<=100 for both endpoints FAILED; retain this. Both improve by more than10×, and measured execution accuracy is several orders inside its bar, which justifies a new controlled optimization comparison without claiming that earlier condition criterion passed.

For each of the two completed secant endpoints compare unchanged secant coordinates against the exact three-product reader-cluster chart. Each retains16source features and110592nonlinear reader scalars. Cluster chart uses40tied product components internally, versus22for one-pair secant; this additional computation/memory is charged. No extra independently fitted features, output writers, language data or changed output metric. Native target remains full jointQK1×QK2×value for27heads8/9/13 folded into four downstream readings. Position7fit/0held, same earlier reference gates.

Four arms,300seconds/512iterations each,1500seconds whole process. Same exact-Hessian trust-krylov solver with common objective divisor=max of the two parent captures. A unit-reader gradient pullback removes raw/secant/cluster coordinate scaling from the stationarity test; stop once<=1e-6. Solver Euclidean gtol1e-12 is not the reported convergence criterion. Record best finite evaluated candidates, including trial points. Reset trust radius .1, max10; warm continuation does not preserve the old solver radius. Progress/checkpoints every20objective evaluations.

A: initial physical-objective agreement<=1e-6 and no loss of captured energy beyond1e-7. B: at least one cluster arm gains>=5% and satisfies unit-reader gradient<=1e-6. C: choose cluster candidate by fit capture only and require its held-position capture not to worsen relative to its parent. Independently compare cluster/secant gain, complete functions and compute cost. Failure to converge is unfinished, not absent structure. Do not choose a semantic label or circuit on coefficient gain.

Controls: independent polynomial/gradient/HVP equality for chart; native energy and synthetic execution replay; native HVP finite differences<=1e-4 and independent unit-reader pullback<=1e-10. CPU HVP3.79–3.84seconds for the cluster representation, approximately1.7×one-pair cost. Native GPU peak/time measured here. GPU only through managedlane1.

This bounded continuation addresses the actual new dependency and inadequate first duration before a larger diverse native restart campaign. Existing ten-start synthetic evidence does not count as ten native restarts. If these fits stay unstationary or disagree, preserve that uncertainty and compare further optimization/restarts against a richer shared-intermediate representation, with its changed family explicitly separated.
