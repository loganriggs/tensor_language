# HOURLY STRATEGIC REVIEW — 2026-09-01 08:10 UTC

(Codex directs; Claude/Fable red-teams. Every damage number is CE added above original native; lower is better.)

## Goal restatement

Produce a predictive, composable, manipulable, literally priced tensor program—not merely a compressed checkpoint.
Audit both semantic scalar count and raw tensor bytes, preserve circuit certificates and signed interventions, and
price finite router states explicitly. Generic top-k is a compute tier, not a small-state tensor-network claim.

## Safe-boundary result

- High fidelity: 545,902,902 scalars / 1,091,805,804 bytes / +.000009 / 62 certs / signed cosine1.000002.
- Structural: 512,561,462 scalars / 1,025,122,924 bytes / +.012612 / 43 certs / signed cosine.981519.
- Both use source-aware global BF16 storage, explicit fp32 compute; structural Q/K factors are fp16.
- Global precision changes QK56 census by only +.000104; factor fp16 changes it by -3.5e-6. Precision and
  structural rank compose essentially freely.

## What is now closed

- Q/K×MLP0 calibrated rank grid below the 43-certificate point: no conservative improver among 42 exact choices.
- MLP0 generic block/tree/DAG recovery: real contraction algebra irreducible; DAG unidentifiable without asymmetry.
- Four-state token and live-context MoE routers: 10x and20–27x worse than equal-price shared subspaces.
- Scalar leverage and suffix-gradient tail repairs: directional signal, both failed corpus-stable tail bars.
- Sparse exact-row vocabulary repair and distributed vocabulary rank at meaningful saving.
- Generic predictive-state/Hankel compiler route under the tested delimiter behaviors.

## Confound audit

- Original-native KO was measured before BF16 rounding; no rounded-native causal shortcut.
- WikiText spans are nonoverlapping in WT103 train; fingerprint and source token count assert.
- Raw bytes exclude no codebooks/scales; current formats need none.
- BF16/fp16 results claim storage only, not fused-kernel speed, latency, or activation-memory saving.
- Q/K56 cosine clears `.98` narrowly but prospectively; no bar was changed. Intermediate global-BF16 Q/K ranks
  are not automatically called physical merely because endpoints pass.

## Ranked next routes

1. All-layer contextual shared-input MLP screen (rung366): biggest unopened scalar pool, two fit splits and matched
   controls. If broad, a CPU tail/ray allocator selects one physical composition.
2. Behavior-conditioned folded MLP0 contraction algebra: state named before fitting; requires asymmetric causal
   operation for DAG orientation and equal-price shared control.
3. Vector-valued suffix-Jacobian metric: only legitimate reopening of MLP tail selection after two scalar failures.
4. Value-family tail curve on CPU: run physical only at a predicted knee because the known point is inefficient.
5. Runtime realization after representation stabilizes: serialization/dequantization/fused compute are separate
   engineering questions from literal storage.

## Immediate decision

Do not spend the remaining window on another precision/rank variant. Run rung366. At its safe boundary, either
freeze one calculator-selected multi-MLP candidate or record that contextual bilinear rank is not broad enough and
move directly to the morning synthesis. No adaptive subset sweep.
