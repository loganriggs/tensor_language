# Preregistration — late_tail_linear_read_spectrum_probe (Claude, LANE 1 CUDA) — Registered 2026-09-04 02:51Z (box clock)

Sign convention (§2135): every CE number is CE ADDED ABOVE THE REAL MODEL on held-out docs 0–63 (FRESH split; all fits on docs 96–191) — LOWER IS BETTER. recovered(model) = 1 − ARM / FINAL_ONLY. Sign assumptions for every bar are stated inline (§2802 rule extension).

## Question
§2802: the late tail write A at the final readout is three-quarters context-computed and one quarter a fixed linear image of the 768 core coordinates (ridge W, 768×384: recovered .244, R²_out .246). Is that linear quarter LOW-RANK — a few core→tail singular pairs that a program could install as a cheap correction of the readout — or spread across many directions like the readout itself (§2798: effective rank ≈ 260)? Side questions the same forwards answer: does CE recovery track variance recovery across ranks (if not, variance is the wrong currency for tail directions and every R²-based statement in the lineage needs a CE partner); is W's top tail direction the frequency direction d_f of §2801 (the one direction we already understand); does the core's linear predictability of the tail fall with context length (the context-computed part growing with context)?

## Instrument
Parent late_tail_token_table_probe (§2802): same consumer Box/CMLP/CAttn, fit pass on docs 96–191 collecting (core coords c = x·U_k, tail coords a = A·U_t) at the final site, ridge λ = 1e-2 × mean diag (identical to §2802 → LIN_768 must reproduce §2802's LIN_CORE .0854). SVD W = U S Vᵀ; W_r = top-r pairs; RANKS = (1, 2, 4, 8, 16, 32, 64, 128, 768). Arms: readout sees x − A + ((c − c̄) W_r + ā)·U_tᵀ. Out-of-sample R² accumulated per position inside each arm; position bands of 32. Per-token deltas by target class. d_f = normalize(WUᵀ u), u = centred unit log(1 + fit count) as in §2801. Price: 3 × 96 fit + 64 × (2 + 11) ≈ 1120 GPU document-forwards (~30 s).

## Predictions (scored exactly as written) — each with the value the arm takes under the hypothesis, the null value, and the operands' assumed signs
- pred_a_instrument: |baseline − 3.0322401| ≤ 1e-4; |SPLIT8_1024 − .0374| ≤ .015; |FINAL_ONLY − .1130| ≤ .015; |LIN_768 − .0854| ≤ .015.
- pred_b_rank8_keeps_half_of_the_linear_read: recovered(LIN_8) − 0.5 × max(recovered(LIN_768), 0) ≥ 0. Null: recovered(LIN_8) − 0.25 × max(recovered(LIN_768), 0) ≤ 0. Signs: recovered(LIN_768) is expected ≈ +.244 (pred_a pins it); recovered(LIN_8) in [−.05, .244]. Worked example: if the linear quarter is low-rank (dominated by ≤ 8 pairs), LIN_8 ≈ .1130 − .6 × .0276 ≈ .096 (recovered ≈ .15 ≥ .122 ✓); if it is spread like the readout (§2798: 32 of 384 directions kept 19%), LIN_8 recovers ≤ .06 → arm ≥ .106, null met.
- pred_c_top_tail_direction_is_the_frequency_direction: |cos(V[:, 0], d_f)| ≥ 0.8. Null: ≤ 0.3. Worked example: if the first thing the core linearly predicts about the tail is "rare vs frequent next token" (§2801: d_f is the unembedding's top tail direction and carries 7–8% of the channel), |cos| ≈ .9; a random unit vector in 384 dims gives ≈ .05, and a top direction unrelated to frequency gives ≤ .3.
- pred_d_ce_recovery_tracks_variance_recovery: max over r ≥ 8 of |recovered(LIN_r) − R²_out(LIN_r)| ≤ 0.05. Null: that maximum > 0.10. Signs: both quantities in [−.1, .3] for every r; the absolute difference is non-negative by construction. Worked example: §2802 gave .244 vs .246 at r = 768; if the readout is linear in A at this scale the same holds at every rank (gap ≤ .02); if the softmax weights directions non-uniformly (some high-variance directions carry little CE), gaps of .1+ appear at intermediate ranks.
- pred_e_linear_predictability_falls_with_position: R²_out(LIN_768) in positions 0–31 minus R²_out(LIN_768) in positions 224–255 ≥ 0.05. Null: |difference| ≤ 0.02. Signs: both R² values expected in [.1, .5]. Worked example: early in a document the core and the tail are both functions of few tokens and the linear map explains ≈ .35 of the tail; late, the context-computed part dominates and it explains ≈ .20 (difference ≈ .15); under the null the linear share is a fixed property of the map, flat across positions (≈ .25 everywhere).
- pred_f_rank64_reaches_90pct: recovered(LIN_64) − 0.9 × max(recovered(LIN_768), 0) ≥ 0. Null: recovered(LIN_64) − 0.75 × max(recovered(LIN_768), 0) < 0. Signs as in pred_b. Worked example: a moderately concentrated map (effective rank ≲ 30) has 64 pairs carrying ≥ 90% (LIN_64 ≤ .088); a map as spread as the readout (eff rank ≈ 260) has 64 pairs carrying ≈ 40–60% (LIN_64 ≈ .097–.102), null met.

Bars: {"ce_tol": 1e-4, "repro_tol": 0.015, "b_half": 0.5, "c_cos": 0.8, "d_gap": 0.05, "e_drop": 0.05, "f_share": 0.9}. Null bars: {"b_quarter": 0.25, "c_cos": 0.3, "d_gap": 0.10, "e_flat": 0.02, "f_share": 0.75}.

## What each outcome means
b and f TRUE: the linear quarter is a low-rank correction — ≤ 8 to 64 core→tail pairs, installable into the readout as W_U ← W_U + (U_t W_rᵀ U_kᵀ)-style terms; that would be the first sub-MLP program item in this lineage that survives a CE test. b FALSE, f FALSE with nulls met: even the linear part of the late tail is spread — the late tail is high-rank at every level (write, readout, and the core-linear part), and the honest program item stays the late MLPs. c says whether the one understood direction is also the linear map's leading one. d FALSE: variance is not CE; every R²-based statement in §2790–§2802 needs re-reading with CE partners (the lineage's CE arms already exist for the main claims — this would affect only the R²-only side statements). e TRUE: the context-computed part grows with context; e FALSE with null met: the split is a fixed property of the map. Nothing installs into the §312 frontier; the strict explained fraction is unchanged by any outcome.
