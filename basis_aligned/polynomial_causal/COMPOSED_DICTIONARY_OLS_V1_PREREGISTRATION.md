# Matched-cost composed product dictionary

The old native dictionary OLS tested the isolated MLP17, not its MLP16-producer
pullback. Use the same verified multi-output OLS on the paired producer metric,
with all centered-U output directions and exactly1365selected trained products.
Refit their output writers by exact conditional least squares. Greedy support
is not globally optimal; native slots are not assumed semantic circuit units.

Matched conditional price: native P16 maps15,925,248floats plus3*1152*1365
outer readers/writers =20,642,688, versus20,643,840for the frozen4096edge graph.
Both require U, native normalization and other background. Native product count
does not count all upstream product operations. One fixed random1365support
(seed120541) receives identical exact writer fitting as a control.

Freeze comparison against STREAMED_SPARSE_FRAME_NATIVE_V1 bestcapture9.08584298%
and SPARSE_FRAME_NATIVE_V1 cached graph writes. Later continuation does not change
these registered references. Centering is a metric choice, not common-channel
preservation. Same128developmental endpoints, full native U/RMS/tanh, no textfit.

- pred_a: old total/reference effects, recursive gain versus exact solve, linear
  solve residual and compiled write identity<=1e-8 (effects<=1e-5).
- pred_b: coefficient capture>=2times frozen graph capture.
- pred_c: eachfamily swapRMS<=.1/sign>=.9/live>=4, removalCEdisagreement<=.02,
  residualwriteerror<=.05.
- pred_d: selectedcapture>=1.25times randomsupportcapture.

Passing coefficient bars alone only exposes a representation/budget confound.
Native fidelity remains required for extraction, and fresh/OOD, selective controls
and shared computation tests remain unfulfilled even on a developmental pass.
Zero body forwards/zero newtext;1365greedy updates, two1365square Cholesky solves,
900second alarm. Preserve index/weight artifact and all counted dependencies.
