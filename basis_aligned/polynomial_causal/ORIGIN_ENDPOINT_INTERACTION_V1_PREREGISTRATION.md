# Candidate circuit: copied-origin keys multiplied by a forward endpoint value

The two-address producer split failed: the existing L1 previous-key path O
accounts for both query effects, while the direct source-value embedding E
does not explain the forward effect. Test a cross-head interaction as the unit.
For a->b->c->d with b->c latest, O arrives through L2H2 from a->b, and the
direct endpoint field F arrives through L2H1 from c->d. Both write into b->c.
Their key/value interaction could implement an address-a/content-d three-edge
lookup. This is a hypothesis; O contains contextual input and its prior
interchange null remains binding. No pure-origin semantic claim is assumed.

Same512 opened requests/32worlds/two orders/two query origins/four hops.
O is exactly join_origin_writer_reference.origin_write. F is exactly the
existing forward_endpoint_program_reference.messages with the selected
source-value column. No producer enlargement, head selection or fitting.

Define A as the final native attention read, B as its O-direction key-pair
cut, C as its F-value cut, and D as both cuts together. All use native source
gain, queries and residual skip. The interaction node is

    I = A-B-C+D
      = .5 sum_sources,heads (P_native-P_without_O_keys) V(F*g_native) O3 head.

With normalized scalar key reads a,b and O's reads da,db, its score factor is
da*b + a*db - da*db. The payload is linear in F. This term has explicit
producers and consumers spanning L1, two L2 heads and all final heads. It is
an additive node in the exact expanded native read, not a physical L2 source
removal. A circuit knockout executes native_logits-I. Never describe it as
removing the whole O or F source state. No source/query attention gate is fitted.

A instrument: explicit I matches the independent native projection-hook
four-arm contrast over all output positions abs<=1e-9, relative<=1e-10.
Compare live sums A+D and B+C+I to avoid a zero denominator. Empty fields and
disjoint supports give zero interaction; overlapping random fields are live;
payload scaling is linear. Native logits replay the prior cohort. Full
expanded-read knockout agrees with the independent contrast construction.

B circuit nomination: native hop3 accuracy>=.9 in each population/query group;
I-knockout lowers forward-query hop3 mean goldP>=.5. Every other query/hop
group must have mean absolute goldP change<=.05. Keep all cases including
native errors. Report complete distributions, mean/p99/max KL, signed gains
and centered effect vectors; generic damage or a selected group is not a pass.
No knob, head, path, scale or hop rescue after a null. No calibration/fresh
holdout claim for these opened cases. A pass requires a fresh intervention and
reuse test before identification, then structural pricing before adoption.

All387968 native export coefficients, native routing, O's contextual input,
and the exact remainder remain charged. Exact expression grouping alone is
not reduced structural description. This screen targets a new multiplicative
unit suggested by observed overlap, not a repair of the failed E/O split.

CPU threads2,batch8 FP64,alarm180s,tensors below256MiB,no GPU/training. Reuse
origin/endpoint, key-direction and exact-source primitives; implement one
small general key-write x payload-write read interaction and the native oracle.
