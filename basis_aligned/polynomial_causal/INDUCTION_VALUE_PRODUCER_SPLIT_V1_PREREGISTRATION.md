# Induction equality-message value-producer split v1

2026-09-10, before native execution. Authority: original bilinear handoff/pilot.
The existing induction equality service has OOD extraction evidence but fails
induction-only collateral certification. R593 donor coefficient/content interchange
is instrument-invalid and scientifically unresolved; this is a DIFFERENT operation
screen, not its repair, retry, or reinterpretation. Prior one-to-one score-factor
sharing failed R531. No rank, head, dose or factor-match rescue is proposed.

Use all72 FIT groups of frozen R578, all four core conditions s0p0/s0p1/s1p0/s1p1,
288 sequences total. SELECT, FINAL_TEST and OOD are excluded. The existing R586/587
capability evidence motivates this candidate; it is not fresh validation. Preserve
all native mistakes. Physical width30, batches32, original semantic query positions.

Fixed zero-index sites L5H5/L7H3/L8H3/L8H4. At each current query the registered
matching source is the payload position whose preceding token equals the query.
Each core condition has exactly one among A/C. The neutral control removes its
one N payload source, which has a nonmatching predecessor. These are matched source
counts, not proof that neutral edges are equally active.

Native mixed value is v=(1-lambda)v_local+lambda v_first. Native scores and both
current value producers remain live. Compute each equality-source message before
output projection with both normalized/rotated QK factors and their1/128² scaling.
Subtract shared-first, local, complete mixed, or summed shared+local contributions
only at the selected heads' final query. Other queries/heads stay bitwise unchanged.
Later layers recompute normally, including later selected heads' current queries
and values. First-layer value is captured from the same forward, never a donor cache.

Arms: native, literal-zero, shared, local, full, joint, neutral-full.63forwards,
2016sequences,162attention transactions,216selected-head transactions. Zero fitting,
gradients, rank searches or weight updates. Native545902902parameters remain charged.
FP64 analysis contracts current native factors; native arithmetic and final outputs
remain FP32. Preserve the small native value-mixture rounding residual in the audit.

- A instruments: counts above, cleanup, finite outputs, unchanged first-value bank
  and untouched head/query outputs; native/zero and full/joint final-output bridges
  maxabs<=1e-3 AND relative<=1e-5; mixture relative closure<=1e-6.
- B capability/materiality/specificity: in EACH of four condition panels, native
  answer-over-other accuracy>=.85, mean full-vocabulary gold-probability loss under
  full removal>=.10, and full-minus-neutral mean loss>=.05.
- C shared-first producer explains full effect: B plus relative effect error<=.10
  in every condition for answer-minus-other margin, centered three-reader vector
  (answer, other, neutral), and centered full-vocabulary logits.
- D local producer explains full effect: identical bars for local removal.
- E behavioral composition: norm(E_full-E_shared-E_local)/norm(E_full)<=.10 for
  the same three readouts and four panels. Undefined zero-denominator ratios fail.

E_mode=logits_native-logits_mode. Ratios aggregate all72worlds in a condition using
the Frobenius norm. They are effect-fidelity tests, not fractions of task accuracy.
Full/joint is an implementation bridge; E is a separate downstream nonlinear test.
Save per-row three-reader logits and full-vocabulary summary norms, not Python lists
of all50,304 logits. Native capability and each scientific predicate are separate
from A; instrument failure yields no scientific verdict.

Null: neither value producer alone explains the registered equality-message effect,
or the whole message is insufficiently specific/material. Do not promote the smaller
error or a subset of conditions/heads, tune thresholds, or reopen the old prefix
candidate. A pass nominates a value-producer role for fresh branch/consumer transfer;
it does not establish independent extraction, OOD, new structural savings, or repair
the broad induction-only collateral failure. GPU only through managed bqrunner,
hash-bound sources, create-only result,600-second guard. Seven CPU controls passed.
