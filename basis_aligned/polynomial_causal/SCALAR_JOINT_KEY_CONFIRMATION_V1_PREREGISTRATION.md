# Prospective confirmation of one joint-key path pair

Registered before SCALAR_JOINT_KEY_PATH_SCREEN_V1_RESULT.json exists. Rows are fixed in SCALAR_JOINT_KEY_CONFIRMATION_V1_ROWS.json. Samecity/endpoints but new diary/school templates and an explicit writer-versus-reader opposed-city challenge.72rows form36pairs,12perfamily. The challenge swaps exactlytwo matched citytoken positions; two single-city families pass the shared single-cue validator. No model-score/capability filtering.

32newline prefixes use unused cache indices, excluding all24mean rows and32priornewline evaluation rows; sameFineWebcache, not document/pretraining/corpus independence. Keep original meanhead8.2replacement vector from the old nuisance partition. Allnewrows remain whether capability passes or fails.

Select only if the frozen screen has instrument/capability and at least one joint regional/newline passer. Among those, maximize minimum regionalfamilycoverage, then minimize maximum newlineabsoluteCEdamage, then lowest arm ID. No alternate candidate tried on these confirmation outcomes. If no screenpasser, do not call any candidate confirmed or queue a promotion run.

After selection, freeze candidate and script hash before managed GPU execution. Regional arms: native, selected8pathalone if present, selected9pathalone if present, selectedjoint, full8first+9currentreference, selectedjoint oppositecue donor interchange, selfdonorjoint. Sevenarms72rows=504forwards. Natural arms: native, selectedjoint, fullsectorreference, actualmeanhead8.2control; fourarms32rows=128forwards. Total632bodyforwards,300seconds. Missing singleton path is explicit no-op, not another selected component.

A instrument: sum/fullreference identities<=1e-5relative; selfdonor native margin difference<=1e-4relative with1e-6absolute floor only for nearzero references. Native regional meancuecontrast>=.2 and>=10/12positive pairs EACHthreefamilies. Native newline meanCE<=5, meannewline-comma>=.2 and>=12/16positive EACHhalf. Meanheadcontrol meanCEdamage>=.02 and>=8/16positive EACHhalf. Failed capability marks family invalid; no rowreplacement.

B: selectedjointremoval>=50%nativecuecontrast coverage and>=10/12positivepairedreductions EACHfamily; unrelatedmargin ratio<=.5. Donorjoint>=50%nativecuecontrast transfer,>=20/24positive directedrows EACHfamily, unrelatedratio<=.5. Allindividual/fullreference outcomes reported; do not claim independent addition.

C: SAMEselectedcandidate jointremoval meanabsnewlineCE<=.02 andmaxabs<=.1 EACHnewhalf, with Acontrols held. Original discoverynewline worstcase andscreen results retained alongsideconfirmation, not replaced. This is selected frozen-candidate confirmation of contextualprediction/removal/interchange/selectivity. Full native states, routingnormalizers, remainingweights and suffix remain external; no independent wholecircuit extraction claim.

Runner integration depends on the actual preceding result and is not queued by this registration. Sourceindices/endpoints/thresholds are fixed in advance, not adapted after candidate scores.
