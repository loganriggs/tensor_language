# Hourly strategic review — 2026-09-08 07:15 UTC

## Circuit targets and full goal

The circuit interpretation target still has seven parts: (1) specify what information is read,
what operation is performed, what is written, and who uses it; (2) group across module boundaries
and split within native heads where causal behavior requires it; (3) predict held-out and OOD
effects; (4) extract an executable circuit or explicit interface plus background; (5) support
selective removals, swaps, and edits; (6) compose and reuse task-specific and shared pieces; and
(7) remain stably identified across documents, text shifts, gauges, and fitting restarts. The full
program goal remains a smaller transparent tensor program that is jointly predictive, composable,
manipulable, and simpler under literal storage, compute, edge, state, and program prices.

## Evidence and corrections since 06:15

Seven circuit receipts changed the H4 account.

1. The frozen four-head union passed simultaneous composition on original text, then passed the
   same test without reselection on fresh temporal-v12/is-was-v16 constructions and lexicons.
   Recovery remains about `.83-.85` temporal and `.67-.74` is-was, with interaction at most about
   1.1%, additive cosine above `.9999`, low collateral, and exact later-to-earlier causal zero.
2. Pair-symmetric midpoint removal passed on original and OOD banks. Every native-correct row moved
   toward a smaller correctness-aligned target margin, so H4 now has a prospective selective edit
   rather than swap sufficiency alone.
3. The within-head reader split identified L11H3:value over the causal prefix, but falsified the
   exact-weight nomination of L15H5:q/q2: q is anti-causal, q2 weakly positive, and their union
   cancels. Weight enrichment remains a candidate generator, not causal identity.
4. Source-region splitting localized the L11H3 value effect to the unchanged bridge for temporal
   and postcue for is-was. Its first receipt stayed `invalid` because replay mixed a parent-relative
   cosine with an older command-gold cosine. A separately preregistered three-forward audit replayed
   every like-for-like command-gold metric with zero error and reproduced the erroneous
   `.028743869178895265` comparison exactly. The original A and terminal remain unchanged; only the
   frozen B--E evidence is audit-recovered.
5. The 16-corner q/k/q2/k2 factorial passed authority, zero parent replay, exact Möbius accounting,
   validation, and selectivity, but not a shared-partner claim. Is-was is routing-invariant: the
   0000-to-1111 delta is only `.081` of the value-parent norm on FIT and `.056-.074` elsewhere.
   Temporal barely crosses the FIT relevance bar (`.101`) and selects k+q+q2; that subset predicts
   the full routing delta on validation (`.785-.952` recovery, cosine `.983-.998`), while the full
   routing delta itself falls to `.081-.088` of the parent on OOD. The correct interpretation is
   that recipient-native routing transports most of the command-specific value effect; donor QK
   factors are a small temporal modulator, not a reusable shared command variable.

The QK implementation also caught a prospective price defect before execution: the first 66-forward
registration could not recompute its requested parent-relative metrics because it omitted four live
full-prefix references. V1 was preserved unexecuted; v2 added only those reference arms and retained
all masks, bars, selection rules, and nulls.

## Strategic choice and alternatives

Another donor-factor split is now lower information. The result points to an exact native-routing
source-term object: for L11H3 at query `t` and locked region `R`, construct

`delta_z[t] = (1-lambda_11) sum_{j in R} ((q_t·k_j)/128)((q2_t·k2_j)/128) delta_v_j`,

then install `delta_z[t]` directly at the head's pre-projection interface. This tests whether the
transparent bilinear contraction predicts the already-observed downstream signed effect without
rerunning an internal value hook.

Ranked next moves:

1. **Exact native-routing source-term extraction.** This changes computational specification and
   extraction/sufficiency. Require the formula to reproduce the value-patch head delta numerically
   and the direct interface install to reproduce its downstream command effect on original and OOD
   rows. Kill the extraction claim if either tensor equality or held-out/OOD causal recovery fails.
2. **Upstream writer translation into the extracted value term.** Contract exact upstream output
   weights through L11H3 `W_v`, rank candidates in the physical residual gauge, then causally patch
   only prospectively nominated writers. This changes cross-boundary grouping and stable reuse.
   Kill a nomination if projected weight reach has no held-out causal transfer or is unstable across
   the two task routes.
3. **H4/L11 explicit-background executor.** Replace the donor lookup with the extracted term plus a
   stated native background and test joint commands/removal. This changes standalone sufficiency and
   literal price. Kill it if its signed effects do not compose or its background dependency cannot
   be stated and priced.
4. **Causal-response operational grouping.** If exact weight translation fails, group upstream
   components by held-out effects on the extracted term rather than activation reconstruction. Kill
   it if fit-fold groups do not predict held-out/OOD interchange above shuffled/site-label controls.
5. **Regularized DAS.** Noise/KL may reduce task memorization, but it stays deferred until an
   independent construction-held-out target exists. Complement loss cannot serve as both the
   optimizer target and proof that the optimized subspace is the intended circuit.

The source-term extractor wins on information, cost, and instrument validity. It directly evaluates
the polynomial attention computation and uses the already-identified source, while further QK masks
would mainly characterize a small off-diagonal perturbation.

## Confound audit

- **Baseline and frame mixing:** command-gold and parent-relative metrics are now named and stored
  separately. The localization error demonstrates why they cannot share one replay key.
- **Nonlinear composition:** joint commands and removals use exact Möbius remainders; the QK cube
  reconstructs exactly with Shapley efficiency error `1.11e-16`.
- **Shared difficulty and leakage:** no rows or templates were filtered. H4 and source regions were
  frozen before OOD. QK factor selection used only pooled original FIT.
- **Dead knobs:** five L11 factor hooks, all masks, region coverage, shape/call counts, and exact
  prices were live. Parent replay error was exactly zero.
- **Precision:** the managed gate's generic absolute-replay warning was monitored. The run replayed
  exactly; no tolerance change or retry occurred.
- **Post-selection:** temporal's `.101` FIT routing relevance is close to the `.10` boundary and
  falls below it on OOD. Report it as a small validated modulation, not a broadly material reader.
- **Off-manifold factors:** mixed q/k corners untie factors naturally written by one residual state.
  Their Shapley labels remain descriptive. The next native-routing contraction avoids basing the
  circuit identity on those hybrid corners.
- **Causal ordering and collateral:** all is-was-to-earlier-temporal effects are exactly zero; the
  maximum registered 0000/1111 arm collateral ratio is `.00474`.

## Throughput and systems checkpoint

Authoritative timestamps after 06:15 contain seven owned circuit receipts: H4 joint composition
(`06:27`), reader split (`06:27`), fresh native license (`06:27`), OOD H4 composition (`06:35`),
selective removal (`06:42`), source localization (`06:51`), and its replay audit (`07:01`), followed
by the QK receipt at `07:15`. That is eight outcomes in 48 minutes, roughly one every six minutes.
The core promotion chain took about 3--8 minutes per result; the invalid localization plus audit
consumed about ten minutes and produced a reusable target-frame lesson. The amended QK runner took
about eleven minutes from first registration to result, including the pre-outcome price correction;
its GPU execution itself took seconds.

Time was dominated by scientific factor/source design and implementation. Reused builders,
accounting, the managed runner, and the new pure four-factor contract kept tests and preflight
smaller. There was no GPU idle caused by an unstarted eligible job, no outcome-bearing retry, and
both Supervisor lanes stayed healthy. The median completed-result interval remains below the
ten-minute screen target despite the promoted OOD/removal confirmations.

CIRCUIT_FOCUS: PASS

CEREMONY_BUDGET: PASS

NOVELTY_LESSON_GATE: PASS

Immediate continuation: publish the immutable QK invariant result, then preregister and implement
the exact native-routing source-term extractor. Its formula-level equality and direct-interface
causal replay must both pass before any upstream-weight nomination is called a computational circuit.
