# MLP2 trajectory-robust rank-512 physical evaluation — v2 recovery

The v1 outcome-blind row freezer terminated before ordered-source inspection,
registry loading, harvesting, cache creation, model loading, or evaluation access.
Concurrent work had advanced local `HEAD` to `c10fe1e2` before the local
`origin/main` ref observed that commit, so the prospective ancestry gate refused it.

The exact committed v1 failure has SHA-256
`ef2ff4687fe057fb2d83db0c97429b67d46cc99dc0b422e9d4fa65edd4c4de81`.
Its cache, receipt, and lock are absent, as are every v1 evaluator output and lock.
The original source-only GO audit has SHA-256
`16b7d002040dd820d4e5fbe5a30209417c381b32e0ad69fe5558535def242e68`
and audited commit `b2d8b31fb2bbb0c69d9cb2d34a22bdd50dde6049` with no outcome access.

V2 is a one-change operational recovery. It uses new row and evaluator namespaces
and v2 schemas, and binds the exact v1 failure, original audit, original 24-source
science closure, and continued absence of every v1 output at all terminal callbacks.
It retains without change:

- FineWeb start index 120,000, one 192-document EVALUATION role, token length 257,
  scoring positions 64--255, and every registry-disjointness gate;
- the eight physical arms, exact C512/FULL/CONTINUE/ROBUST bytes and prices;
- the 10,000 common-document bootstrap draws, seed, nine contrasts, simultaneous
  quantiles, thresholds, prefix rule, optimization rule, and claim boundary;
- authority-before-row/model access, float64 sufficient statistics, exact call
  census, result semantic replay, guarded failure, and receipt-last publication.

The original v1 failure is never retried, removed, or reclassified. A new independent
source-only audit of the v2 recovery closure is mandatory before row selection.

