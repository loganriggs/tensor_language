# Task 14 subject–verb agreement capability-FIT compiler review

**Reviewed:** 2026-09-04 UTC. **Verdict: APPROVE exact commit
`fc586c1158ddeee7df8f4b502deec54189609c4c` for construction of a separate, hash-bound model-facing producer and
blocked managed adapter.** This review does not authorize execution, model/checkpoint/GPU access, queue/enqueue,
publication, localization, or any later phase. The future producer/adapter must receive a distinct immutable review
and authorization.

This was an independent CPU-only review performed from exact Git objects. I did not edit the implementation,
preregistration, authority, tests, or dryrun and did not create a producer, adapter, result, evidence, or phase
artifact. My only writes are this review and append-only board receipts.

## Immutable ancestry and prospective status

The requested object resolves exactly to `fc586c1158ddeee7df8f4b502deec54189609c4c`. Its ancestry is:

```text
e9686bc9bbb40f872d8e8320b30fab4f019e524d  repaired semantic authority
  ...
ea7efad782c088ba91a2ce338a9f740563c4e7c1  independent authority approval
f6e3ec265cd046b3c4e7625eb1e57101d65afbf3  intervening explanation-only successor
fc586c1158ddeee7df8f4b502deec54189609c4c  reviewed compiler target
```

Both `e9686bc...` and `ea7efad...` are strict ancestors of the target. The target's direct parent contains neither
the strict task-14 capability experiment ID nor any of the new authority/compiler/preregistration paths. The board
records the prospective claim at 07:45 UTC and completion at 07:58 UTC. The preregistration and implementation were
committed atomically, but the prospective scientific condition is still verifiable: the target contains no task-14
producer or adapter, no model-facing entry point, no strict task-14 result/evidence/outcome namespace, and no model
call. The source imports no Torch, model, checkpoint, CUDA, queue, runner, or science-execution module. Pre-existing
agreement results cited as provenance are not this experiment's outcome and do not supply its gates.

The preregistration fixes both alternatives before any possible task-14 execution. Capability pass is the conjunction
of its pooled, family, incongruent, coordinated-C, margin, and answer-relation clauses; capability failure is their
exact logical complement. There is no post-outcome exception, adjustable bar, family deletion, or fallback metric.

## Exact files and frozen closure

Raw SHA-256 recomputation directly from `git show fc586c...:<path>` gives:

| File | SHA-256 |
|---|---|
| FIT authority | `e88fd860c28c9b369abe4a8ec28372f93bb94b6e841265206c43e6929a25ac2f` |
| capability compiler | `98b2d263c5120c1a7b700dc4bb451f65cc9f9b338740d2cfbc7ae25a3ba5aab1` |
| compiler tests | `a013f9be7464a76b28286b130b99d8d3717299bd27462e2455f5c29566d87e00` |
| capability preregistration | `06a9747b4707999e11637a45cf83588bfd9cb8671d6b3a25790518af62900f8b` |
| checked-in dryrun | `9ee6bf94676befbb89101a254a902ebd84fb4a53a6bd3e748a0c9e36336e5636` |
| target board blob | `91cfb62679d714b9718eaca6f3c72f047133279a6853b5d64e3e30aac84c8a36` |

The compiler's frozen upstream roles also independently match the target bytes:

| Frozen role | SHA-256 |
|---|---|
| repaired generator | `33d7b62b3a0ffb4c798e75f085b7e96988e09b07be16667c5f9f8871c6339f94` |
| generator tests | `254fe3798efd8a4426f30e054fd8e5646a5bd6635df69815f376311ac2023694` |
| repaired design memo | `3cb4556d1ad2c1564f2708028e5d624c4519fbc4d52a38cac27b9d10d8312f68` |
| authority review | `7249991dd727f6385d3269cce23b0e5f83c588bcef3488dce33ae19dfd223fd1` |
| integration contract | `b36317f46127dc90d7b8d38c9aca85440c6ff46adb7087fe2c1fd7a2745cfa3e` |
| experiment spec | `64ba9b75d49dbc6129d592573fee454e27e2de661daef30ca35d457dbbbb093c` |
| artifact package | `6c8f81f16e3465b33c27abacd1114bd8ae7ce2fffa358c2a665f906a49f011cc` |
| managed entry | `1c5bfe6dc8435e767e0d05e4ccb415ce04feb3b7a6da50eb342695e6747dda81` |

The compiler itself is not yet a managed runtime artifact because no producer exists. A future producer/adapter must
freeze the compiler SHA above rather than import a mutable path.

## FIT authority and physical invocation

The only materialized strict task-14 authority artifact in the target is the FIT envelope. Its 128 rows reproduce
logical SHA-256 `3cf3315a77b3176418739e7a9357c0dbd9b95724d6b276038f53691b873377d1` and bind the reviewed full-authority SHA-256
`1cf6cf12668c7428719134bbee03ab84f57cc150f2653cc12ffc4a71566c8db1`. It contains exactly 32 linked groups and one
A1/A2/P/C row per group. All rows say FIT, and all 256 base/donor prompt strings are unique.

The managed spec exposes exactly one authority role, `fit_authority`. Its sole phase is FIT and explicitly forbids
SELECT, TEST, and OOD. The dryrun's generator-entry monkeypatch proves that neither full-authority generation nor the
panel generator is entered: rows are loaded only from the captured FIT bytes. No later-split authority path, artifact,
result, or generation flag appears; `later_split_generation=false` and `later_split_artifacts=[]`.

The independently reconstructed contract hashes are:

| Contract | SHA-256 |
|---|---|
| typed spec | `9cad5272e1f49712ba218ad54d577770dbb244c5e18065a592910f841895ec40` |
| physical call manifest | `4b4da44c5090914f87d52e018bc9a8d18b74a202bdb82667283a9f1564682e0e` |
| metric manifest | `5da9f66829156e352afe087c75f92a7a6a37f06fe1ec5177efeffd9442609dcc` |
| complete compiled contract | `84f8e1cf85323dba94d13c7c716afef448b8621bff6b534c2025715420e86a82` |
| logical dryrun | `3af0b62f3f8cdaa3eeab3f24e2e0537616baef33195ade025270489aa47e871d` |

Calls are exactly base A1, A2, P, C followed by donor A1, A2, P, C. Every call has one batch of 32, the correct
sequence length `[5, 8, 5, 8]` on each side, its exact 32 authority row IDs, native-only role, capability-only guard,
and answer/foil array contracts. For every call I independently joined row IDs back to the frozen authority and
reconstructed the side-specific prompt IDs, prediction positions, answer IDs, opposite-copula foil IDs, and
incongruence flags. Each metric entry matches that reconstruction. A1/A2/P have 16 incongruent rows in each side cell;
C has zero by definition. A1/A2 change the registered answer across sides, while P/C preserve it.

The only retained numeric arrays are `answer_logit` and `foil_logit`, each `float32[batch]` with `finite_policy=always`.
The physical package boundary permits exactly those two `.npy` names and checks request width, shape, dtype, and
finiteness. A one-dimensional saved NumPy array is C-contiguous on reload. The literal price therefore is exactly
8 forward calls, 256 distinct row-side evaluations, zero backwards, zero updates, and
`256 * 2 * 4 = 2,048` raw numeric bytes. Call/JSON headers are metadata, not learned numeric evidence.

## Gates, complements, and fail-closed decisions

I independently exercised every registered integer and strict-margin boundary on both base and donor sides:

| Gate | Last pass | First fail |
|---|---:|---:|
| pooled side at 0.85, 128 rows | 109 | 108 |
| each ordinary A1/A2/P cell at 0.85, 32 rows | 28 | 27 |
| each ordinary incongruent subset at 0.85, 16 rows | 14 | 13 |
| each C side at 0.75, 32 rows | 24 | 23 |

For all six ordinary side/family cells and both C cells, a constructed exactly zero mean margin fails. Positive mean
margin is strictly `> 0`, independently of the accuracy boundary. The native gate is one conjunction containing all
these clauses, so its Boolean negation is exactly the registered failure alternative rather than a posthoc failure
category.

A valid scientific miss produces `hard_abort` with instrument and answer-relation predicates true, native-capability
false, and all seven projection fields null. An invalid typed scalar produces an earlier instrument hard abort with
only `metric_evidence_contract=false`; NaN/Infinity are rejected still earlier as non-standard JSON. Thus invalid
instruments are distinguishable from scientific failures and cannot yield capability metrics. Duplicate, missing,
misrelated, or extra-field primitive rows fail before projection. A passing decision contains only base/donor and
family/incongruent aggregate accuracies/margins plus `capability_pass`; it contains no reader, writer, site, component,
head, MLP, localization namespace, or phase-opening field.

## Mutation and test evidence

The checked-in suite rejects mutations of authority bytes/metadata, future-split rows, local token/coordinate
semantics under a re-bound row digest, every frozen source/review/preregistration role, call/metric/primitive/price
surfaces, coverage, relation labels, forbidden localization fields, and the registered decision boundaries.

My separate in-memory exact-object battery made 81 successful assertions. It re-bound every call to authority tokens
and positions and rejected independently planted changes to source/review/preregistration/FIT hashes, authority
bytes, future split, call shape/order, array dtype/count contract, metric token/order, price, whole compiled hash,
primitive omission/duplication/relation/forbidden fields, scalar types, and nonfinite values. It also exercised all
pooled, ordinary-cell, incongruent, C, and zero-margin boundaries on both sides and distinguished instrument abort
from scientific hard abort.

With bytecode and pytest caches disabled and model/CUDA access excluded:

```text
focused capability compiler suite: 19 passed in 1.57s
task14 authority + capability + integration/compiler/spec-adversarial/result-contract: 126 passed in 8.06s
independent exact-object assertions and coherent mutations: 81/81 passed
```

Fresh dryruns equal the checked-in report and are identical across Python hash seeds. They record zero model forwards,
zero backwards, zero updates, and `queue_touched=false`.

## Approval boundary

**APPROVE exact compiler SHA-256
`98b2d263c5120c1a7b700dc4bb451f65cc9f9b338740d2cfbc7ae25a3ba5aab1` and FIT authority SHA-256
`e88fd860c28c9b369abe4a8ec28372f93bb94b6e841265206c43e6929a25ac2f` from commit
`fc586c1158ddeee7df8f4b502deec54189609c4c` for separate producer/blocked-adapter construction only.** The next unit
must freeze this compiler source, reconstruct inputs from the exact captured authority/call manifests, retain only the
two declared arrays, implement create-only/race-safe publication and runtime/model/checkpoint gates, and remain
blocked until its own independent review and explicit authorization. This review does not execute or enqueue it.
