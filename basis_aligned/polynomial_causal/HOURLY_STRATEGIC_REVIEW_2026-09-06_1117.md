# Hourly circuit strategy review — 2026-09-06 11:17 UTC

## Circuit interpretation targets

The live decomposition must ultimately specify what is read, computed, written, and consumed; group operationally shared pieces across native boundaries while splitting cohabiting variables inside modules; predict held-out/OOD behavior; provide executable sufficiency or a precise background interface; support selective removals/swaps/edits; explain composition and reuse; and remain stable across populations, gauges, and fitting restarts. Literal storage and compute matter only after these circuit claims hold.

The program-level target remains a smaller transparent tensor program that is jointly predictive on fresh and OOD text, composable under installed replacements, manipulable under registered counterfactuals, and simpler under literal storage, compute, edge, state, and program prices. A screen or low loss is not completion.

## What changed since 10:17

Ten Codex terminal receipts landed: one fit-free task-affinity null; H1/H4 cross-task reuse null on P selectivity; exact within-task source screen; one invalid aligned-P control; shared MLP4 response-factor screen; exact is/was MLP4-to-H1/H4 mediation screen; zero-forward typed-graph release; direction-matched task-state onset null; block9 component/head null; and unequal-length H1/H4 semantic-source screen. GPU science totaled seconds per candidate.

The substantive circuit revision is large. Both tasks reuse exact MLP4 left/right response algebra, task-indexed contextual carrier banks, and L9H1/H4. Is/was carrier mediation retains 49.5% of its MLP4 writer and 98.3% of all-source H1/H4. Direction-matched cross-task interchange shows answer-vocabulary identity onsets at resid10, while reliable has-to-is temporal member selection waits until resid12. The registered hypothesis that task identity lives outside shared temporal heads was falsified: H1/H4 retain about 92%/88% of full-attention task recovery; the seven-head complement retains only 5.6%/8.1%. Exact semantic grouping then localized 73.4%/70.2% of H1/H4 task transfer to contextual carrier banks, about 20% to prefix/cue, and under 5% to the shared final token.

Corrections were preserved. `moment`→`instant` was invalid because present-tense donor capability fell to 0.125; its descriptive P values were not promoted and no synonym rescue followed. The task-onset price was corrected before execution from 42/672 to 40/640, with the old claim abandoned and reclaimed. A proposed has/had rerun was dropped when prior search found the existing prospective H1/H4 holdout.

## Throughput and systems audit

Authoritative claim/release and commit timestamps show ten terminal receipts in the hour, exceeding the six/hour operating target; serial candidate cycles were generally about 3–6 minutes. There was no idle GPU wait. Scientific design and interpretation produced real within-head and cross-task decisions, but repeated 150–275-line runners consumed far more wall time than 2–4 second GPU executions. Exact hooks and hash/price gates caught two defects, yet evaluator duplication remains the main preventable sink.

`CIRCUIT_FOCUS: PASS` — every block changed circuit localization, semantic grouping, composition, or reusable evidence.

`CEREMONY_BUDGET: FAIL` — bespoke cross-task batch, four-logit capture, normalized endpoint scoring, and summary logic were repeated across three runners. The next bounded block must factor and test this kernel before the claimed pattern/value GPU experiment runs.

`NOVELTY_LESSON_GATE: PASS` — prior work was searched, a duplicate fresh mediation run was explicitly avoided, the incapable control was not rescued, and nulls changed the object rather than triggering rank/head sweeps.

## Confounds and alternatives

Task support uses exact mean soft-capped logits for the two answer vocabularies, not CE, and donor recovery is normalized by row-matched native endpoints. Occupation, temporal direction, and patched final token are matched. Remaining frame mixing is intentional—the task templates differ—and semantic source groups therefore establish an operational interface, not lexical invariance. Full-softcap nonlinearities are measured by actual suffix execution. The stable three-site onset rule prevents a one-layer spike. H1/H4 may still multiplex task and temporal information through different pattern/value factors; head identity alone is not semantic identity. P-selectivity remains control-family sensitive and unresolved.

Alternatives ranked by information value:

1. Factor the shared cross-task evaluation kernel, then run the already claimed exact carrier routing-mass/content factorial. This changes within-head computational specification; it is killed if aggregate routing masses are ill-conditioned or exact factor closure fails.
2. If content dominates, split its effective-value computation upstream; if routing dominates, trace query/key mass instead. Each is killed by low causal Shapley contribution or failed bidirectional recurrence.
3. Independently decompose resid11→12, where task identity is already present but has-to-is temporal correctness becomes reliable. This targets variable composition and is killed if no module selectively improves temporal member choice while preserving task support.
4. Function-space or causal-response bases remain secondary: they become useful only as matched controls or canonical summaries of the now localized variables. Rank/reconstruction sweeps are rejected because they would not decide a circuit edge.

The current route remains highest information because exact source terms now expose a falsifiable routing-versus-content split inside the shared heads. The forced workflow repair precedes that run; afterward the outcome, not compression metrics, chooses the next edge.
