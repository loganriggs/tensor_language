# Induction equality tensor FINAL/OOD v2 recovery

Status: prospectively frozen source design; numerical execution remains forbidden until
the fresh-row and execution source families each have an independent outcome-blind GO.

V1 remains NO-GO under `induction_equality_tensor_final_ood_independent_audit.json`.
Neither V1 role tensor may be opened, hashed, or used. V2 uses a fresh namespace and
three newly frozen roles: 192 label-only FineWeb documents, 192 disjoint natural FINAL
documents, and 192 repository-file-disjoint code OOD documents. Label-fit may construct
frequency strata and masks but may never be forwarded through the model. Registry
exclusion is metadata-only and binds every registry JSON; source-document IDs, dataset
indices, code paths, and normalized-code hashes must be disjoint. No prior row tensor is
deserialized by the freezer.

The candidate, heads, six arms, equality contraction, vocabulary-cycle null, scoring
slice 64:256, and SELECT anchors are exactly those in the v1 preregistration and bound
discovery result. Each role is one row per source document. All arms execute through
the hook-free observed-model facade and immutable one-use owners. For every batch and
arm the ledger must prove one outer return, one attention and MLP dispatch at every
site, native calls at every unmodified site, and zero native selected-attention calls
for all five analytical arms.

Raw logits are reduced immediately to document CE, native-to-arm KL, top-1, count, and
ordered-support hashes. The create-only ledger is the sole sufficient-statistics
artifact; no tokens, rows, masks, logits, or hidden states may be published. Point
statistics are evaluated once on the observed pooled document ledger, never as means
of bootstrap replicates. Use 20,000 shared within-role document draws and seed
`induction-equality-final-ood-v2:bootstrap:0`. Every nonpositive recovery stake in the
point estimate or any draw is a terminal integrity failure; no clamp, redraw, or repair
is allowed. Report simultaneous 95% lower and upper bands across every role-coordinate.

Natural FINAL gates: target-damage and specificity LCB > 0; extraction point >= .80
and LCB >= .60; off-target UCB <= .01 and point <= 10% of target point; deranged UCB <
half the extraction LCB; support >=200 tokens and >=30 documents for every cell; replay
and exact call/integrity gates pass. Code OOD gates are identical except target point
>=50% SELECT, extraction point >=.60 and LCB >=.40 and point >=50% SELECT, off-target
UCB <=.02 and point <=20% target. Both roles must pass for the bounded transport claim.

Authority is created before loading fresh role tensors or the model and binds the
pushed source commit, complete transitive source hashes, independent audits, discovery,
fresh-row receipt/files, checkpoint, and empty output namespace. An owned inode/nonce
claim protects execution. Sources, parents, rows, checkpoint, authority, and partial
artifacts are rechecked at every publication boundary. Ledger, result, and manifest are
create-only and semantically reloaded; one result receipt is the last fallible action.
Failure is create-only, mutually exclusive with the receipt, and binds protected and
partial-artifact snapshots. V1 files and its NO-GO audit are immutable parents only.

