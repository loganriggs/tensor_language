ACTIVE_TRACK: WEIGHT_FOLDING

The user's two-day weights-first exploration remains authoritative through September22 15:10UTC; hourly alternation remains overridden. Full target: a simple executable computation with specified readers/operations/writers, grouping across module boundaries, held-out/OOD prediction, extraction, selective manipulation, reusable composition and stable units. None is promoted from the new reconstruction fits.

**What changed since22:14.** Exact coefficient optimization now has two full-input-span baselines: CP512 and a144-by4 shared quadratic bank with512 selected root products. Five-family optimizer controls show spare width helps recover known tensors. On the native16-output MLP16→17 pure-quartic branch, both larger models remain globally inaccurate. The hierarchy improves coefficient/Gaussian metrics at lower deployed price but worse text fit. Exact Gaussian readout fitting removes probe interpolation; standardGaussian fitting harms text, and calibration mean/covariance fitting improves text to9.6–10.3% while damaging global coefficients. The guarded convex frontier preserves near10%text with99.4–99.6%sampled coefficient error, yet component tests fail. Registered integrity/tradeoff pass and component failure are retained.

**Circuit interpretation.** The fixed newline-associated output direction is an operational reader/writer, not an identified semantic unit. A new CPU response-capacity audit distinguishes capacity from transfer: evaluation-fitted oracles attain6.3–6.6%sensitivity error and reproduce the20independent same-token differences, but calibration-only sensitivity fits transfer poorly. No oracle is exported. This shows finite-panel response representability, not OOD or finite-removal fidelity. It also prevents misusing Gaussian convex optimality as a no-go theorem for component readouts.

**Next decision.** Stop readout-budget sweeps: this family already exposes the tradeoff. Highest-information weight-focused successor is joint feature-direction learning under a declared coefficient-plus-shiftedGaussian metric, retaining the fixed16-output target and explicit program costs. First benchmark the exact noncentral Gram backward on the actual512-feature shape with an independent small dense gradient reference; budget memory before a native run. Use the same two archived feature starts so any improvement can be attributed to changing directions rather than a new dictionary. Do not claim the fixed-feature convex guarantee for this nonconvex fit. A response-aware calibration diagnostic is secondary and must retain prefix-separated transfer; it must not replace the two-day weights-first experiment with task-label fitting.

**Falsifiers and alternatives.** If mixed feature learning lowers the objective without improving component transfer, the metric/feature identity remains inadequate; do not compensate with more coefficient-only steps. An adaptive shared topology remains a genuinely different route, but its fixed-support baseline is still weak. A full graph search would require a credible seed function. Oracle results do not remove the broad tensor capacity bounds or imply that a cheap globally faithful circuit exists. Repeated-index polynomial symmetry, data-informed output readers, reused evaluation panels and different loss geometries remain explicit limitations.

**Math and redteam.** Native constrained solver passes feasibility/KKT/export tests; independent SLSQP controls are preserved. Covariance alone is not the lifted eighth-moment metric. Full coefficient norm remains unavailable, so report exact loss differences and sampled normalized diagnostics separately. Gauge-dependent penalties require transported regularizers. The23:12 CPU row-hash assertion caught64-versus65-token hash conventions; source acquisition code confirms identical64state rows, and both conventions are now checked. No row or assertion was dropped. Small ridge effects cannot rescue Gaussian own-law fits (22:58 review addendum). Mathematical/literature review completed22:53; next due September22 01:53UTC.

**Throughput from receipts.** CP25step19.74s; CP400step259.65s; shared bank883.28s; isotropic readout7.23s; three-law readout17.79s; guarded frontier4.91s; latest CPU response-capacity audit about1.3s. Shared bank has lower deployment cost but about3.4times CP training runtime. The native queue was idle between the22:50 law comparison and23:10 guarded run while math, CPU diagnostics and the requested report were completed. Most controls were reused; exact wall-time categories beyond these receipts were not tracked. No new framework or redundant report needed.

TRACK_ALTERNATION: user override, weights-first.
TRACK_PROGRESS: PASS, exact native weight objectives, wider structures, metric tradeoff and response-capacity diagnosis.
CEREMONY_BUDGET: PASS for this turn: managed preflight reused,4.9s native and1.3s substantive CPU diagnostic; no extra infrastructure. Full-hour authoring/testing minutes not separately instrumented, so no claimed precise ratio.
NOVELTY_LESSON_GATE: prior sensitive readout/feature failures and synthetic-probe interpolation applied; latest oracle is a capacity diagnostic rather than a repeated candidate claim.

Continuation receipt: coefficient-guarded native result interpreted, followed by an executed fixed-dictionary CPU response-capacity and pair-constrained audit. The full goal remains active.
