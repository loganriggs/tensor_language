# Bracket key2 query-context scalar V1

The complete source factorial localizes most seventh-construction error to the
second query-key score factor, while full-vector native-relative transport makes
that factor worse. Compress the needed context directly at the scalar
interaction that the attention mechanism consumes.

Freeze the existing rank-two key2 type table. For every target endpoint define
`absolute` as live recipient query2 contracted with the frozen donor-type key2
prototype, `recipient` as the native recipient query2-key2 score, and
`recipient_prototype` as live recipient query2 contracted with the frozen
recipient-type prototype. The sole contextual feature is
`context = recipient - recipient_prototype`.

Using target activations from the first three frozen constructions only, fit two
through-origin activation laws: (1) anchored
`absolute + gamma * context`; (2) two-scalar
`alpha * absolute + gamma * context`. Select in that order using the opened
seventh construction only. Against the exact paired-donor factor2, require at
least `.05` relative-L2 improvement over `absolute`, cosine `.95`, relative L2
`.35`, sign `.90`, and norm ratio `.70`--`1.30`. No behavioral effect or answer
logit may enter either fit or selection. The eighth construction remains sealed.

No key vector reconstruction, coefficient by delimiter/direction/construction,
intercept, rank change, behavioral fit, gradient, update, gain, bar change,
donor state at execution time, or quantization is allowed. The memory-safe
execution price is five forwards over 720 sequences: one factor capture for
each of four panels plus one native replay reference on FIT. There are two
scalar activation fits, zero backwards and updates.
