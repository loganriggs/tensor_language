# Hourly strategic circuit review — 2026-09-08 13:22 UTC

## Circuit interpretation targets

1. **Computational specification:** identify what is read, computed, written, and consumed.
2. **Cross-boundary grouping and within-module splitting:** group operationally equivalent
   pieces across modules and split native heads/MLPs when only parts implement the variable.
3. **Held-out/OOD prediction:** predict causal effects on unseen text without reselection.
4. **Extraction/sufficiency:** execute an isolated circuit or an explicit interface plus background.
5. **Selective manipulation:** add, remove, swap, or edit the intended behavior with low collateral.
6. **Composition/reuse:** predict joint behavior and reuse shared subcomputations across tasks.
7. **Stable identification:** survive corpus shifts and legal gauges, or be defined by operational
   equivalence under downstream readers.

The full objective remains a smaller transparent tensor program that is jointly predictive on
fresh/OOD text, composable, selectively manipulable, and simpler under literal storage, compute,
edge, state, and program prices. The current two-stream interface advances that objective but
does not prove a complete semantic computation or full-program adoption.

## What changed since 12:22

No new circuit GPU receipt landed. The verified unrelated v258 process occupied managed lane 1
for the full hour and advanced from its early members to 17/22; the immutable P7 necessity audit
remains queued exactly once ahead of unrelated v269. Both Supervisor runners remain healthy.

The CPU work stayed circuit-related:

- the conditional M11 executor was completed with exact arm-local Left/Right/interaction factors;
- exact A11-head and M11-factor writes now contract through checkpoint `W_O` and `Down` weights;
- an RMSNorm-aware reader bridge and causal-order filter nominate only legal downstream readers;
- atomic binding now opens only prospectively eligible A11/M11 executors, and a direct seam test
  proves both frozen executor eligibility predicates accept emitted bindings unchanged;
- Logan's transferred source-conditioned Shapley idea was mapped to the immutable P7+identity
  result. The exact two-player dividend is `-.026619/-.028195`; Shapley credits are identity
  `.444852/.466224` and P7 `.555148/.533776`. This is mild replacement-stream redundancy or
  cancellation, not a directed edge, because the P7 outputs were clamped rather than recomputed
  conditional on identity.

## Confound audit

- **Baseline subtraction and frame mixing:** the frozen head/product executors use arm-local live
  baselines and exact complete-route replays; global-native factor frames remain prohibited.
- **Nonlinear composition:** finite task-margin vectors and exact intervention games are primary.
  Scalar Shapley values allocate a registered game but cannot turn an interaction into an edge.
- **Shared difficulty, leakage, and post-selection:** P7 order and OOD banks remain frozen. A11H3
  and M11 are the only prospectively nominated within-module claims; newly observed pieces need
  sealed transfer.
- **Dead knobs and precision:** one-call hook coverage, exact state/output closure, registered-logit
  replay, and finite-value checks remain live. No threshold changed after an outcome.
- **DAS memorization:** no fitted subspace is in the active branch. Constrained DAS remains demoted
  until a sealed, regularized transfer objective beats the zero-fit physical interface.
- **Shapley surrogate leakage:** no surrogate was fitted. Any future tensor-network approximation
  must establish held-out sup-norm error; R-squared or weight rank is insufficient.

## Alternatives and decision

1. **Current LOO -> physical head/product split.** This most directly advances within-module
   splitting, specification, OOD identification, and manipulation. Kill a branch if its parent
   replay or stable necessity fails on either OOD phase.
2. **Source-conditioned exact effect game.** This resolves redundant downstream routes that
   single deletion can miss. Open it only if multiple P7 members are LOO-null while full P7 remains
   sufficient; kill it if identity-conditioned coalition effects are below the precision floor or
   unstable across OOD phases. Seven players cost 256 identity-by-subset arms per population.
3. **Cross-module causal-response grouping.** Use reader-response equivalence to merge distributed
   pieces only after physical interchange/removal. Kill it if response similarity fails sealed
   transfer or the grouped replacement fails composition.
4. **Regularized DAS.** Noise/KL/Jacobian regularization could address memorization, but it remains
   lower information than exact physical splitting now. Revive only with sealed optimization and
   evaluation corpora, complement invariance, multiple restarts, and weight reader/writer tests.

The current route survives because its next receipt makes a discrete decision between stable
native-module necessity, distributed/redundant routing, and instrument failure without fitting a
coordinate system. Ranked next moves are: consume and score P7 LOO; bind/enqueue eligible A11 and
M11 splits; invoke the seven-player effect game only on the registered redundancy signature.

## Throughput and systems audit

The authoritative interval produced zero new circuit/null receipts, missing the one-per-ten-minute
target. The serial bottleneck was a verified live ~five-minute-per-member v258 run ahead of the
circuit job, not an idle GPU or missing queue entry. Approximately 20 minutes went to scientific
design/interpretation, 25 minutes to reusable implementation, 15 minutes to tests/review/handoff,
and the entire wall-clock hour overlapped managed GPU computation plus circuit queue waiting; idle
time was zero. Even though the helpers address requested weight translation and caught real causal
ordering/schema risks, implementation plus validation was the largest CPU bucket and no measured
circuit screen closed. The next bounded block therefore forbids another helper, framework, or
preregistration: consume the queued result and execute the already-frozen licensed branch.

`CIRCUIT_FOCUS: PASS` — all work advanced physical splitting, weight reader/writer nomination,
redundancy diagnosis, or safe circuit execution.

`CEREMONY_BUDGET: FAIL` — reusable implementation and validation dominated the CPU interval while
zero circuit receipts landed. Repair: no new infrastructure before the P7 receipt is scored and
an eligible split is actually run.

`NOVELTY_LESSON_GATE: PASS` — existing Mobius/Shapley utilities and prior factorials were searched;
the new contribution was restricted to source-conditioned effects, and reconstruction/rank,
global-frame, post-selection, and interaction-as-edge errors were explicitly rejected.

Concrete continuation: the reviewed P7 necessity runner SHA
`384a79a38294416204d5bde0e24fcee7008e391a6712de82ec59c627f7f1318a` remains hash-bound in the
managed queue behind the verified-live v258 process. Consume it immediately when it lands; do not
open new CPU infrastructure during the ceremony-budget repair block.
