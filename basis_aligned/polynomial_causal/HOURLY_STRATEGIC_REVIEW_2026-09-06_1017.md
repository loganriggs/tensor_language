# Hourly strategic review — 2026-09-06 10:17 UTC

## Circuit interpretation target and durable goal

The circuit standard remains causal and executable: name the reads, computations, writes, and downstream users; group causally equivalent pieces; predict held-out effects; expose a sufficient interface; support selective edits and composition; and stay stable across population, construction, gauge, and fitting choices. Rank and compression count only when priced with prediction, manipulation, reuse, and explicit failure scope.

The durable goal is still a substantially smaller transparent tensor program that predicts fresh/OOD behavior, composes without hidden interference, supports registered counterfactual manipulation, and improves literal state/storage/compute/edge price over the native transformer. The current result is a strong bounded two-behavior program that still consumes native `resid:10`, native `resid:18`, and the checkpoint head. It is not a whole-model replacement and the full goal is not complete.

## What changed since 09:17

- The four-scalar `is`/`was` upstream controller passed its calibration-design test: direction-specific A1 root-alpha R² values are 0.652/0.684; A2 recovery is 0.965, P reflection 0.946, and C 0.017. It replaces 3,776 root/head evaluations with one local read and four scalars.
- A first fresh v5 authority failed native capability before causal access. A separately preregistered, fully disjoint v6 authority passed all eight native cells; the unchanged controller then passed prospectively with A1 1.084, A2 0.977, P 1.040, and C 0.017. A zero-forward audit released the exact 1,156-value `q_is` program.
- Joint aligned `q_has + q_is` writes preserved both behaviors and were almost exactly additive: nonlinear residuals were at most 0.0012 target scales on has/had and 0.034 on is/was. The joint result was nevertheless a valid null because `q_has` adds 0.646/1.034 scale surcharge to is/was A2/P. The two programs are algebraically compatible but not task-isolated.
- Flipping only the partner direction label on a fixed state was invalid: partner effects reversed on only 0–0.438 of rows. Direction is a source-regime selector, not a live command bit under this off-support metadata intervention.
- The zero-forward paired-reader audit rejected one shared affine temporal scalar. Within-bank correlations were high, but C correlation was higher and all eight cross-bank transports missed R² 0.50, often catastrophically. Separate task-gated readers are required.
- A preregistered ten-scalar nearest-centroid gate over the two local contrasts was formally invalid because its has/had scale literal did not match the cited immutable authority. It was not repaired after outcome access. Descriptively, it also failed badly: held-out A2/P routing was 0.438 for has/had and 0.500 for is/was, with C abstention 0.625. That exact feature/classifier route is dead.
- A simpler fit-free raw-text selector was compiled from six literal grammar cues. Its zero-forward release audit passed 256/256 decisions, exact delegation to both released programs, strict ambiguity rejection, and identity abstention. This is an explicit interface branch, not an internal-circuit claim.
- Most importantly, the unchanged combined interface passed a new prospective matched-lexicon test. The two banks share the identical 16 novel occupations, so occupation identity cannot route them. All 16 native capability cells passed before causal access. Has/had A1/A2/P reached 0.901/0.916/1.085; is/was reached 1.056/0.959/0.968; direction agreement was at least 0.875; both C banks were exactly unchanged. Price was four forwards, 256 examples, 1.40 seconds, zero fitting/search/backwards/updates.

## Strategic interpretation

There is now a prospectively supported composed program with an explicit raw-text branch:

`raw text cue -> bank and direction -> task-local resid:10 contrast -> frozen affine gain -> selected rank-one resid:18 write -> exact head`.

The causal writers and scalar computations are stable across disjoint matched lexicons. Joint algebra is also nearly linear. The missing mechanistic object is the model's own branch computation: which internal scalar or small operation distinguishes the has/had construction from the is/was construction before selecting the appropriate reader/writer? The failed two-contrast centroid establishes that temporal command values themselves do not provide a stable task selector.

The highest-information next scientific candidate is a frozen, fit-free task-affinity read at `resid:10`: compare the absolute support assigned to the has/had answer pair against the is/was answer pair, rather than comparing the direction-signed within-pair contrasts. This separates task identity from temporal value. It must be preregistered on a fresh matched population, and it must route both writers causally without changing either basis, gain map, or rank. C abstention may remain the explicit raw-text safety boundary until an internal no-task signal is separately identified.

## Confound and validity audit

- **Capability and selection:** every fresh population remained capability-first. The v5 failure opened no causal outcomes; v6 and the matched dual population passed before intervention analysis.
- **Composition versus isolation:** tiny additive residual proves algebraic compatibility, while the frozen surcharge bar correctly prevents calling simultaneous writes task-isolated.
- **Off-support commands:** the opposing-label result is retained as invalid and supports no manipulability claim.
- **Task identity versus temporal value:** high paired correlations and failed cross-bank transport show why direction-signed reader contrasts cannot be treated as task identity.
- **Raw-text branch scope:** the selector is literal, auditable, and prospectively validated, but it is external to the transformer and restricted to registered grammars.
- **Postselection:** no failed bar was relaxed. The invalid centroid gate was not repaired or rerun after outcome access. The next affinity feature is a different mechanistic quantity—between-pair support rather than within-pair temporal contrast—and requires fresh data.
- **Price honesty:** the combined screen counts four full forwards/256 examples, eight inherited affine scalars, and 2,304 basis values. Native background states and head weights remain required.

## Ranked next moves and kill evidence

1. **Factor the repeated dual-program evaluation kernel before another GPU run.** One shared, tested library must own direction inference from row metadata, capability-cell aggregation, requested token IDs, scalar summaries, and exact price checks. This directly addresses the two avoidable integrity failures this hour.
2. **Preregister a fit-free internal task-affinity candidate on a fresh matched population.** Kill it if fixed sign fails at least 0.75 bank routing per family/direction or if causal dispatch misses either released program's 0.75 A/P bars. Do not tune a threshold or add coordinates after failure.
3. **If affinity passes, localize its upstream production with a registered path/mediation test.** The object is the task branch scalar, not a broader rank sweep.
4. **If affinity fails, retain the raw-text interface and test one explicitly motivated earlier-site task cue.** No generic classifier or dimension search.
5. **After internal branch resolution, widen construction/OOD scope and reduce dependence on native background states.** This is required for the durable goal.

## Circuit throughput and ceremony budget

This hour produced a calibrated `q_is` controller, one sealed capability failure, one prospective controller pass, one compiled release, an aligned-composition null, an opposed-command invalid, a paired-reader null, a learned-gate invalid, a raw-text combined release, and a prospective matched-lexicon combined screen. GPU execution remained seconds; the final combined screen cost 1.40 seconds. Scientifically, the hour moved from a second isolated writer to a prospectively validated automatic two-program interface and sharply isolated the missing branch computation.

The systems record is worse. The joint runner completed four forwards before a summary-key crash, and the learned-gate preregistration bound the wrong scale authority. Both were caught and preserved, but both were avoidable. Several 200–300-line bespoke runners repeated capability, direction, summary, and price logic. That ceremony now creates more risk than the GPU work itself.

`CIRCUIT_FOCUS: PASS` — every scientific action identified or tested a reader, controller, writer, composition law, branch mechanism, or executable causal interface. No rank/compression drift occurred.

`CEREMONY_BUDGET: FAIL` — repeated bespoke evaluator code caused two avoidable integrity failures and dominated the 1–4 second experiments. Before another GPU candidate, factor and test the shared dual-program evaluation kernel; the next experiment must consume that kernel rather than copy it.

`NOVELTY_LESSON_GATE: PASS` — prior results were searched, hashes and bars were frozen, invalid/null evidence was retained, capability gates prevented leakage, and the proposed affinity read targets the specific task-identity quantity left open by the failed within-pair reader gate.

The next hourly review is due near 11:17 UTC. The next three-hour mathematical review is due near 11:30 UTC.
