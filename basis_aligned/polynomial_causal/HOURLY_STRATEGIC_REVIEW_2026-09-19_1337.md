# Hourly strategic review

ACTIVE_TRACK: WEIGHT_FOLDING

Actual UTC: **2026-09-19T13:37:49Z**. Next deadline: **2026-09-19T14:37:49Z**. Previous review: [12:35 CIRCUIT](HOURLY_STRATEGIC_REVIEW_2026-09-19_1235.md), 1h01m55s old. This is one live review, not an offline backfill. Historical `/workspace` paths were resolved against this checkout. No GPU model, agent, job, queue, service, timer, commit, push, external contact, experiment/result, or `TYPED_FACE_EXTRACTION_V1` implementation was changed. The sole bounded CPU/process consequence was the truthful 13:37 phase mark.

The preceding review declared `CIRCUIT`; this review therefore alternates to `WEIGHT_FOLDING`. The declared circuit action—the regional forward-response census and full-suffix five-arm factorial—did not run, and no other scientific receipt or commit appeared after 12:35. Preserve that exact circuit handoff for the next circuit hour; do not repeat the circuit track now.

## Seven circuit targets and full goal

1. **Computational specification:** identify what is read, the operation/composition, what is written, and every downstream user.
2. **Cross-boundary grouping and within-module splitting:** group pieces across native modules when a reader treats them as one variable, and split native modules whose pieces serve different computations.
3. **Held-out and OOD prediction:** predict activations and behavioral effects on unseen inputs, task variants, and shifted corpora.
4. **Extraction or sufficiency:** execute an isolated circuit at a declared boundary with every retained port and background named.
5. **Selective manipulation:** removal, swap, or edit changes the intended behavior against matched controls while preserving unrelated behaviors and accounting for redundancy/interactions.
6. **Composition and reuse:** shared subcomputations serve multiple tasks/modules and their joint behavior follows a tested composition law.
7. **Stable identification:** units survive corpus/document splits, gauges, and fitting restarts, or are defined by downstream operational equivalence.

Goal: a smaller transparent tensor program that **predicts held-out/OOD behavior, is extracted at a declared boundary, supports selective manipulation/removal, and composes/reuses**. Simplicity is a fifth, separately priced property: independent stored scalars/bytes, product nodes, readers/writers, open states/ports, graph edges, and runtime. Lower replay error, storage, rank, variance, or CE cannot substitute for a missing behavioral property.

| Property | Current evidence | Verdict |
|---|---|---|
| Held-out/OOD prediction | Regional intermediate extraction has 20 distinct FineWeb document/context cells and Pile tests, but packed FineWeb direction is `88/102`; breadth routes are claim-relative and lack frozen end-to-end formulas/cell manifests. | **PARTIAL** |
| Extraction at a declared boundary | Residual6-to-attention8 and norm-closed MLP8 packages replay with declared ports; complete head9.8 and suffix remain open. | **PASS only at intermediate regional boundaries** |
| Selective manipulation/removal | Scoped regional edits beat same-site nulls, but the complete path lacks the frozen equal-norm/three-reader/full-logit battery. | **PARTIAL** |
| Composition/reuse | Regional `interaction/smaller=2.3448`; matched random-split specificity fails. Gender composition is promising but lacks the complete smallest-piece/null test. | **FAIL overall** |
| Measured simplicity | Local package prices and the restricted full-vector head9.8 rank certificate exist; there is no deduplicated residual6-to-head9.8 price or favorable matched-effect comparator. | **LOCAL ONLY** |

Evidence remains typed as **fold/edit/response/fit** and **fresh/opened/replay**. A closed Möbius face is accounting, not evidence that the interaction is small relative to the smallest live piece or specific against random splits. Rows are not distinct context cells.

## What changed since 12:35

No scientific result changed. Git has no commit after `9e0c03df3` at 12:21:10, the board has no claim after the 12:35 review, and no experiment/result/registry/dossier file was created or updated. Managed runner logs show only successful canaries from 12:48:17–12:48:35 and 13:18:39–13:18:57; both queues are empty and the two runner processes remain live. The documented workstation Supervisor path is absent on this corrected instance; the live runners are the checkout scripts managed by the container Supervisor. This review did not alter them.

The prior hour therefore neither advanced nor falsified the declared circuit factorial. Existing failures still govern: regional key/value composition is large and nonspecific; packed FineWeb direction transfer misses; the person seven-head set misses `.60` at `.549`; aspect `since/by` is syntactically misaligned; temporal `will/had` fails its reader/carry/additivity predictions. The latest 12:25 weight-only certificate still establishes full column rank 128 for head9.8's output map and a restricted independent-port trilinear CP-rank floor of 4,096 at the final position (67,584 over 528 cells at `T=32`), not a behavioral or causal result.

## Highest-value WEIGHT_FOLDING action

Build and exact-replay the **regional residual6-to-head9.8 output-delta program** before any new rank sweep or behavior. The endpoint is the complete `head9.8` attention output delta `Delta A9.8[t,o] in R^(T x 1152)` at the layer-9 attention boundary, especially the final regional decision position; blocks9–17, final RMS, unembedding, and softcap remain an explicit later behavioral suffix.

Native factors and shapes:

- `r6, initial7, attention7_h in R^(T x 1152)` for nine heads; MLP7 `L7,R7 in R^(4608 x 1152)`, `D7 in R^(1152 x 4608)`, bias and learned re-entry; five head8.2 readers `Q/K/V in R^(128 x 1152)` as applicable and `O8.2 in R^(1152 x 128)`.
- MLP8 `L8,R8 in R^(4608 x 1152)`, `D8 in R^(1152 x 4608)`, bias, token embedding table, learned re-entry, and the generated edited RMS. The existing opened package prices 16,536,963 stored FP32 scalars plus token indices; `WvD` is a derived cache, not independent weights.
- Head9.8 `Q1,K1,Q2,K2,V in R^(128 x 1152)`, `O9.8 in R^(1152 x 128)`, causal `(t,s)` cells, native rotary rounding, value mixing, and re-entry.

Included source terms: expand residual inputs into named residual/background, attention-output, MLP-output, and installed head8.2-write sources. For every bilinear MLP/QK reader retain attention×attention, MLP×MLP, attention×MLP, and MLP×attention terms separately wherever the reader can use them, plus all ordered background/edit cross terms, edit self terms, biases, skips, changed denominators, and learned re-entry. At head9.8 retain all seven nonempty finite-difference products:

`dp1*p2*v`, `p1*dp2*v`, `p1*p2*dv`, `dp1*dp2*v`, `dp1*p2*dv`, `p1*dp2*dv`, and `dp1*dp2*dv`.

Omitted terms: only the unchanged baseline head product when representing the delta, analytically zero/off-support terms, and source terms proven unreadable by the selected native reader. No QK2, ordered cross term, normalization, or value interaction is omitted merely because it is small on the opened panel.

Nonlinearities/background: bilinear elementwise MLP products divided by live RMS variance; bilinear QK scores, rounded rotary maps, causal mask, and the trilinear `QK1*QK2*V` head product. Native residual6, unrelated token/context content, unchanged module writes, all live RMS denominators, biases, token initial state, and the later native suffix are named background/open ports. The later softcap `30*tanh(logit/30)` is outside the intermediate replay but inside the causal test.

Simplicity measure: one deduplicated bill for independent static scalars/bytes, derived caches, product nodes, causal cells, graph edges, open-state scalars/ports at `T=32`, peak working bytes, and measured CPU/GPU execution. Shared weights count once. The full-vector rank floor forbids claiming a saving from rearranging the same 67,584 trilinear terms; any simpler retained subset must arise from exact source restriction or a declared consumer quotient.

Exact replay check: against an independently instrumented native forward, require the named terms to sum to the complete head9.8 delta with maximum per-sequence relative L2 at most `1e-5`, zero off-support write, and matching intermediate deltas after attention7, MLP7/head8.2, and norm-closed MLP8. Replay must cover the existing opened fixtures and the frozen 20-cell FineWeb extraction set without fitting or reselection. Failure localizes to the first mismatching boundary and stops behavioral promotion.

Later causal/predictive falsifier: freeze the program, first run its forward response census, then install it through the full native suffix on preregistered structurally varied distinct cells. Score signed regional prediction, full logits/CE, at least three unrelated readers, 16 equal-norm same-site directions, and the five-arm routing/value factorial with matched random splits. Kill the retained grouping if signs fail, either single is dead, collateral breaches its gate, or its Möbius interaction relative to the smallest live single is not smaller/more specific than random splits.

This action can improve computational specification, cross-boundary grouping/splitting, and extraction now; only the later frozen factorial can improve prediction, selective manipulation, or composition. Exact replay or a lower price alone cannot promote the path.

**Concrete cross-track connection:** the circuit-side factorial needs an explicit product-suppressed arm. The seven-product fold defines that arm without conflating missing native interaction with downstream curvature; in return, the factorial identifies which folded source terms the downstream regional reader actually uses and may license a smaller consumer-specific program.

**CIRCUIT handoff:** run the preserved regional forward-response census and full-suffix five-arm routing/value factorial with all seven products, distinct cells, smallest-live-piece normalization, matched random splits, equal-norm directions, three unrelated readers, and full logits/CE.

## Confounds, alternatives, and registries

The fold must preserve baseline subtraction and replace-versus-zero semantics; native source order; frame/position/city/endpoint mixing; shared token difficulty and FineWeb/Pile leakage; RMS, rotary rounding, causal masking, value mixing/re-entry, MLP bias and suffix curvature; live tripwires; precision floors; and post-selection. Existing panels opened during selection cannot become fresh by replay.

Ranked alternatives: (1) the chosen complete regional fold; kill it on replay failure or an unfavorable deduplicated price. (2) Contract the complete head tensor into a preregistered regional scalar reader to seek a smaller consumer quotient; kill it if unrelated-reader/full-logit preservation fails. (3) Fold head17.2's full QK1×QK2×V response; defer until the regional path closes because it would widen the depth target. (4) Canonical number/gender/person records are valuable organization work but not folding evidence. Generic rank, reconstruction, activation-variance, quantization, and new-behavior sweeps remain demoted.

Registry audit: `PATH-REGIONAL-CITY-001` still ends at head8.2 although the live fold extends through norm-closed MLP8 to head9.8. `CIRCUIT_REGISTRY.md` retains stale number/gender summaries. `CIRCUIT_GRAPH_REGISTRY_V1.md` remains a 49-package inventory that omits `city_mlp8_norm_closed_v1` and current number/gender/person routes; its two `four_trait_verified / next gap: none` rows do not price the fifth simplicity property. MLP7/8 dossiers contain the regional prior art, but module dossiers do not cross-link number units 829/953/1030, gender copiers 8.1/6.1, or person writers/readers.

Highest-priority missing cross-link for this track: extend **`PATH-REGIONAL-CITY-001`** from its head8.2 endpoint through `city_mlp8_norm_closed_v1` to the complete head9.8 seven-product endpoint, linking the head9.8 rank certificate and marking suffix prediction/selectivity/composition open. The broader missing `PATH-SUBJECT-NUMBER-002`, pronoun-gender path, and person spread/null record remain documentation debt. None was edited while the shared registry is concurrently dirty.

## PAST_HOUR_TIMING

Window: **2026-09-19T12:35:54Z–13:37:49Z**. Scientific serial candidate latency is **undefined: zero candidates started or finished**. The only authoritative process intervals are canaries at 12:48:17–12:48:35 and 13:18:39–13:18:57 (about 18 seconds each); these are health checks, not research/model evidence and are not added as scientific wall time.

- **Design:** unknown; no phase mark or receipt.
- **Implementation/data building/indexing/refactoring:** none observed; unknown uninstrumented labor.
- **Validation:** no scientific validation; two successful canary intervals only.
- **Model execution:** zero scientific model executions; canary execution about 36 wall seconds total.
- **Interpretation:** unknown; no new claim or receipt.
- **Documentation/review:** the prior review file and board append landed by 12:37:31, but active duration is unknown and commit/file gaps are not labor time.
- **Idle/blocked:** unknown; absence of receipts is not proof of uninterrupted idleness or a blocker.

Today's phase log had only sparse hourly review boundaries through 12:35 and no marks for intervening science. This review appended one truthful 13:37 review boundary. Sparse boundaries are not continuous labor and no category was backfilled.

## PROCESS_IMPROVEMENT

Ranked by likely time saved versus cost: (1) extend the canonical regional path record and deduplicated price schema, likely saving 5–10 minutes per handoff at low documentation cost; (2) require `started_utc`, `finished_utc`, distinct context cells, evidence type/status, and price fields in receipts, low cost and high audit value; (3) replace sibling runners with a declarative intervention executor, highest cumulative saving but medium cost and collision risk.

No shared registry/helper refactor was safe in this bounded review: `COMPUTATION_PATH_REGISTRY.md` and runner state are live dirty work, and a new ad hoc publisher would worsen duplication. The one bounded nonconflicting repair was the truthful phase mark, which improves review-boundary coverage without inventing missing labor.

## Explicit verdicts

- `TRACK_ALTERNATION: PASS` — previous `CIRCUIT`; current `WEIGHT_FOLDING`.
- `TRACK_PROGRESS: FAIL (NO CIRCUIT RECEIPT)` — the preceding hour did not execute the declared circuit census/factorial or produce another circuit screen/null.
- `CEREMONY_BUDGET: FAIL` — there was no scientific work against which review/preflight could be smaller; visible durable activity was the prior review/board update plus health canaries. Unmeasured labor remains unknown.
- `NOVELTY_LESSON_GATE: FAIL` — no duplicate candidate was launched, but the authority-designated regional action remained untouched and the already-known regional path/price/cell cross-link debt remained unrepaired.

**Highest-value action now:** exact-replay the complete residual6→attention7/MLP7→head8.2→norm-closed MLP8→head9.8 delta with every reader-permitted self/ordered-cross source term and all seven `QK1*QK2*V` products, then publish one deduplicated price and path dossier link.
