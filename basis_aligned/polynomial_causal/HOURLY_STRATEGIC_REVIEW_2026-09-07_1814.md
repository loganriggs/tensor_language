# Hourly strategic circuit review — 2026-09-07 18:14 UTC

## Circuit interpretation targets

A useful decomposition must eventually provide all seven: (1) a computational specification of
what each part reads, computes, writes, and feeds downstream; (2) cross-boundary grouping and
within-module splitting rather than assuming heads or MLPs are semantic atoms; (3) held-out and
out-of-distribution prediction; (4) an executable sufficiency interface or extracted circuit;
(5) selective removal, swapping, and editing with redundancy and interactions measured; (6)
predictable composition and reuse across tasks; and (7) stable identification across documents,
constructions, gauges, and fitting restarts.

The program-level goal remains a smaller transparent tensor program that is jointly predictive,
composable, manipulable, and simpler under literal storage, compute, edge, state, and program
prices.  Neither low rank nor a completed causal screen is that endpoint.

## What changed since 17:14

Three sequential causal localization receipts and one workflow repair landed.

1. The complete five-source-MLP atlas was an honest graph/readout null.  Old reader coordinates
   judged the native five-MLP patch nearly complete (`.928`), but behavior reached only `.399` and
   14/16 inherited controls flipped.  This established that the finite old reader family was
   under-observing the new construction.
2. A shared capability population contract now derives denominators and fractional thresholds
   from the built row inventory and rejects copied counts.  It directly repairs the preceding
   hour's ceremony failure.
3. The all-layer native atlas found the changed behavior primarily at attention 8 (`.40565`),
   attention 9 (`.38479`), and attention 11 (`.27203`), with several material MLPs.  The apparent
   input-inclusive prefix result was corrected and quarantined: patching the entire embedding
   sequence makes the prompt itself donor-identical.
4. The exact 27-head atlas split the three live attention modules.  `L8H1=.40075`,
   `L11H3=.25460`, `L9H1=.16310`, and `L9H4=.15853`; all have perfect causal direction and are
   conditionally necessary in their complete module parent.  Whole-minus-summed-singleton
   behavior gaps are only `.00370` at layer 8 and `.00047` at layer 11, but `.02936` at layer 9,
   so native execution is still needed for the layer-9 interaction.
5. The 174-forward adaptive cross-boundary combination is valid but returns the registered
   `no_small_selective_prefix` terminal.  The full 17-part bank reconstructs behavior and final
   residual almost exactly (`.958/.966` A1/A2 behavior; `.981/.988` residual projection) while
   catastrophically changing controls (29/32 flips; median KL `2.627`).  Along the frozen greedy
   path, behavior first exceeds `.80` at five pieces but already has 24 control flips and median KL
   `1.656`.

The terminal name must be scoped carefully.  It proves that **this greedy path** has no eligible
prefix, not that no small selective subset exists.  At stage one, two attention candidates are
individually exact-selective: `attn:15` gives `.1269` A1 projection with zero flips, and `L9H4`
gives `.0990` with zero flips.  `L8H1` and `L11H3` each incur only one flip at low KL, while the
complete MLP candidates cause most collateral.  Since selectivity can be restored by composition
(as MLP1 did in the earlier mask-26 lattice), a small exhaustive attention-component lattice is
the discriminating next test.

## Confound and validity audit

- **Baseline subtraction:** every target and control is measured relative to its native base;
  base-self closure is exactly zero.
- **Frame mixing:** A1 selects the greedy order and A2 does not.  However, A2 helped define the
  candidate pool in the earlier pooled atlases.  It is a no-reselection check, not pristine
  held-out identification.  A new family remains mandatory.
- **Nonlinear composition:** the greedy arms run jointly through the native suffix; singleton
  sums are not used as outcome evidence.
- **Shared token difficulty:** cue words and construction identity remain plausible shortcuts.
  P paraphrases and C controls expose large collateral for complete MLPs, but are themselves used
  during selection and are not held-out controls.
- **Leakage/post-selection:** candidate membership is postselected from the same v15 population.
  This licenses localization and composition screening only.
- **Dead knobs/tripwires:** authority hashes, derived candidate sets, exact 174-forward count,
  base-self closure, A1-only order, no-op exclusions, and P/C statistics all fired.
- **Precision/noise:** exact replay errors are zero; scientific effects are orders of magnitude
  above the `1e-4` instrument tolerance.
- **Observation mismatch:** behavior and final residual agree for the full bank, but this does not
  repair the old-reader mismatch or make residual reconstruction a semantic circuit criterion.

## Competing approaches and ranked next moves

1. **Exhaustive five-attention-component lattice** over `L8H1`, `L9H1`, `L9H4`, `L11H3`, and
   complete `attn:15`.  This directly changes within-module splitting, cross-boundary grouping,
   extraction, and selective manipulation.  Measure all 32 subsets on A1/A2/P/C.  Kill it if no
   subset reaches target behavior `.75` with zero control flips and low KL, or if a selected subset
   fails no-reselection A2 behavior.
2. **Control-conditioned within-MLP splitting** if the attention lattice cannot close behavior.
   Use task-defined causal response components rather than hidden-unit magnitude.  This changes
   within-module splitting and selective manipulation.  Kill it if complete-MLP target gain cannot
   be separated from P/C response in a held-out response block.
3. **Weight-tensor translation of a confirmed attention core.**  Pull the selected response
   covectors through native OV tensors and rank upstream QK/value readers by exact contractions.
   This changes computational specification and cross-boundary grouping.  Kill a proposed edge if
   its weight prediction fails causal source/destination interchange.
4. **Finite multi-environment causal-response DAS.**  Retain DIM/step zero and hard target
   constraints; add complete constructions and downstream-reader blocks.  This changes stable
   subspace identification and OOD prediction.  It remains behind direct circuit localization
   because the scalar-axis objective is already rejected and the current attention lattice is
   cheaper and more discriminating.
5. **Full 17-component exhaustive search** is rejected: `2^17` native combinations spend far more
   compute while mixing known nonselective complete MLPs.  The five-component lattice tests the
   live selectivity-cancellation hypothesis exactly.

The current route remains highest-information after narrowing from complete modules to attention
pieces.  It changes circuit evidence directly and is not a rank or compression sweep.

## Throughput and systems audit

Authoritative claim-to-result latency was about 14 minutes for the complete-source atlas, 16
minutes for the module atlas including its one hook repair, and 3 minutes for the head atlas;
median was 14 minutes.  The greedy follow-up took about 12 minutes from preregistration work to
runner start and 31 seconds of GPU execution.  Scientific design/implementation was the largest
bucket; model-free gates took under two seconds per enqueue.  Shared GPU waiting contributed most
of the module interval, while the input-hook correction was the only execution repair.  These are
promotion/localization rungs rather than independent basic screens, but the repeated bespoke
runner surface is now visible; the next 32-mask lattice should reuse the just-tested hook/report
machinery rather than add another framework.

`CIRCUIT_FOCUS: PASS` — the hour moved from a failed source basis to complete modules, four causal
heads, and an executed cross-boundary selectivity boundary.

`CEREMONY_BUDGET: PASS` — shared preflight was seconds; the elevated time was scientific native
intervention implementation, managed queue contention, and one concrete backend-hook repair.

`NOVELTY_LESSON_GATE: PASS` — prior greedy/module results were searched; complete modules were not
treated as semantic atoms; response reconstruction was kept separate from behavior; control-family
dependence and post-selection limits are explicit.

The immediate continuation is the preregistered exact five-attention-component lattice.  A result
boundary is not a pause: score it, then either confirm a selective core on a fresh family or begin
control-conditioned within-MLP splitting.

## 18:30 correction — padded shape did not imply semantic alignment

The lattice landed after this review, and its target-side result is exact: all five pieces recover
`.80536/.86829` A1/A2 behavior.  Its selectivity terminal is not evidence.  Inspection of the row
authority shows A1/A2 base/donor token lengths and semantic positions agree per row, whereas the
legacy P and C controls do not (sampled `6→7` and `15→13`).  The patch hooks copied responses by
absolute token index after checking only equal padded batch tensor shape.  Consequently the P/C
KL and flip measurements mix semantic positions.  Statements above calling those collateral
figures scientific are retracted; target localization remains valid.  The next bounded block is a
shared per-row alignment contract plus capability-qualified equal-length P/C controls, followed by
the same frozen lattice.  This is the `NOVELTY_LESSON_GATE`-required instrument repair.
