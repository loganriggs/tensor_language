# Preregistration — sign-gauge validation and the causally available reverse (parallel lane)

Date: 2026-09-02 20:00 UTC
Owner: Claude (parallel probe lane)
Status: frozen before any validation-document or reverse-direction negated outcome exists

## Question and lineage

The sign-gauge quotient (§2630, receipt eff94038…) passed in full on discovery
documents 0:500: both negated cross-family score transplants into L8H4
(L7H3→L8H4 and L8H3→L8H4) are edges under rung 501's verbatim criteria in all
eight cells, with in-run positive-scale arms reproducing the anti-alignment and
negated-payload controls staying weak. Per that prereg's registered ceiling,
the pass licenses exactly two things, which this rung runs:

1. **VALIDATION** of the two passed directions on documents 500:1000 (never
   opened for any negated-transplant outcome); and
2. **the causally available REVERSE direction.** SCOPE CORRECTION to my §2630
   ledger text: "reverse directions (L8H4→L7H3 etc.)" was over-broad — a
   later-layer donor into an earlier-layer recipient is not causally available
   under the factor machinery (donor factors must exist at the recipient's
   site), exactly why rung 501's vocabulary had seven pairs. The one available
   reverse is the same-layer **L8H4→L8H3** (cross-family; its POSITIVE-scale
   form failed in 501), tested NEGATED on both discovery and validation
   documents.

Full pass here licenses the mutual same-layer statement "L8H3 ↔ L8H4 up to
sign" (rung 501's typed-semantics rule: both directions independently) plus the
validated family gauge — the sixth-standing-claim language. Any failure leaves
§2630 as a discovery-documents result with the failed component named. Codex's
main line (504, 153-pair finite suffix screen) is untouched; rung 501's module
and my probe are imported/pinned hash-exactly.

## Arms (identical machinery, three directions, four quarters)

Directions: L7H3→L8H4, L8H3→L8H4, L8H4→L8H3 — each with NEG_SCORE (score_ratio
negated; scales recomputed in-run by 501's exact 24-forward fit pass),
POS_SCORE (in-run anti-alignment control), NEG_PAYLOAD (specificity control);
plus LATE_NATIVE/LATE_ABSENT under both early backgrounds (early term = the
donor's own; for L8H4→L8H3 the early-removed term is L8H4's). Documents
0:1000 in one unconditional collection — the validation docs' negated outcomes
are precisely what the §2630 pass licensed; 500:1000 CE was opened by earlier
rungs, so this is prospective for intervention type, not new-corpus OOD.
Quarters: 0:250 / 250:500 / 500:750 / 750:1000, scored per quarter.

Price per batch of 4: 2 shared + 3 directions × 9 = 29 forwards; 250 batches
+ 24 scale forwards = 7,274 total.

## Frozen predictions (edge = rung 501's `_partition_edge` verbatim, interval [.65,1.40])

### pred_a — exact, live instrument
501's suite: replay logit and MLP9 max-abs 0.0; factor reconstruction ≤ 1e-10;
scale pass exact (24 calls, all scales live); every intended edit nonzero;
calls exact (7,250 + 24); all task cells supported; reader token counts
positive; probe/501 hashes match; probe receipt shows pred_a/b/c all true and
strong_null false.

### pred_b — the two passed directions validate
L7H3→L8H4 and L8H3→L8H4 NEG_SCORE are edges in BOTH validation quarters
(500:750, 750:1000), each with its POS_SCORE control anti-aligned (task and
reader copy-cosines < 0) in both quarters. (Their discovery quarters are
re-measured and reported; pred_b is carried by validation.)

### pred_c — the reverse direction is an edge on discovery
L8H4→L8H3 NEG_SCORE is an edge in both discovery quarters (0:250, 250:500)
with POS_SCORE anti-aligned in both.

### pred_d — the reverse validates
Same for the two validation quarters.

### pred_e — interpretation (b ∧ c ∧ d, with a required)
The mutual statement is licensed: L8H3 and L8H4 realize one copy-score
computation up to a Z2 sign gauge (both directions, discovery + validation),
and the cross-family gauge for L7H3→L8H4 is validated. Registered claim
language: "each of the four equality scores can supply the L8-recipient
copy-score computation, with cross-family donors requiring exactly a sign
flip" — still NOT whole-head equivalence, an executable circuit, or
compression; the payload/output side remains distinct per §2627.

## Strong null and routing

Strong null fires if pred_a fails or pred_b fails. pred_b true with c or d
false: the forward gauge is validated but the mutual statement is NOT licensed
— report the reverse's failed cells, retain §2630 + validated forwards, and
close the reverse question (no re-registration with adjusted scales on any
route). No outcome licenses rank, compression, or scale sweeps. Bars are
501's, unchanged; nothing is dropped.

## Literal price

7,274 full-model forwards (~3 min), one phase, documents 0:1000 (500:1000
opened for negated-transplant outcomes as licensed by the §2630 pass); sealed
and final roles untouched. Sufficient statistics only. Zero deployed
parameters.
