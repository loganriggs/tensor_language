# Export the frozen rank-eight consumer-response projector

The preregistered response-basis frontier was a valid selector null, but its
rank-eight arm independently passed response, bidirectional causality, and all
preservation gates while retaining 19.9997% of source norm. Recompute the basis
from the same bound ODD discovery rows and export exactly the leading eight
multi-reader response eigenvectors, plus the leading eight source-PCA vectors
as a comparator. Do not inspect another panel or choose another rank.

The response Gram construction, six readers, two endpoints, per-row gradient
normalization, fixed eight source edges, and eigensolver match the parent runner.
Require 72 finite VJPs, projector orthogonality <= `2e-6`, and the parent receipt's
rank-eight arm to retain all three scientific gate classes. The binary artifact
is an extraction object, not a fresh scientific pass.

Price: 48 discovery rows, 12 native/edited executions, 72 VJPs, zero fresh-panel
sequences, fits, or parameter updates.
