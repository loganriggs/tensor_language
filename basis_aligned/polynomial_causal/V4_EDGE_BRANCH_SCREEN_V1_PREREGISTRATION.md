# Cross-head value-branch sufficiency screen

Head3 alone fails even with exactMLPresponse. Test a different, algebraically specified grouping acrossheads. The27writer interface contains9writers for each of three branches:0baseline currentvalue,1edited currentvalue,2cached first-layer value. Scalar coefficients incorporate query/key routing and exactnormalization; these are not independent unnormalized value-only writes.

Evaluate allthree singleton andallthree pair supports. Each inserts its partial mixed edge at additivepreMLP11 and executes the exactnative normalizedMLP11 andsuffix. This isolates branchselection from MLPresponse omission. Seven amplitude settings andeightcellgroups unchanged, frozen source selectors andcore, no refit. Fullcarry/full-edgeexact/linear controls retained. No freshtext claim; six-support choice is an opened screen.

pred_a_instrument:12prefix64native252joined, nativeunitanchors<=1e-8, fulledgeexact<=.001weakoutputerror, branchsum<=1e-10absolute. pred_b_single_branch_sufficient: someonesupport passesall56cells. pred_c_pair_sufficient: sometwobranchsupport passesall56cells. Gates10%number and5%eachcontrol relativeweaker singleton effect. Report allsupports and failures. No causalimportance inference from coefficient/state norms.

Literal edgecore price:shared189headrouting/normcoefficients+3sourceinputnormcoefficients, plus10368writercoefficients per retainedbranch. Singles10560,pairs20928,full31296values/context; optional zero-denominator dependencies may admit further savings only if implemented and audited.224explicitMLP11calls beyondsuffixes. Native source/background/suffix preparation remains charged. Success would define a crosshead partialinteraction candidate, not a full semantic circuit.
