# Hourly circuit-only review — 2026-09-05 21:35 UTC

## Circuit interpretation targets and controlling goal

The circuit targets remain: (1) a computational specification of what is read, computed, written, and consumed; (2) cross-native-module grouping and within-module splitting defined by downstream operational equivalence; (3) held-out and OOD activation plus behavioral prediction; (4) extraction or a precise executable interface with stated background; (5) selective removal, swap, and edit with collateral accounting; (6) predictable composition and reuse; and (7) stable identification across prompts, constructions, controls, and gauges. Native heads and MLPs are candidate boundaries, not semantic units. Rank, reconstruction, variance, and compression are not substitutes for these targets.

The program goal remains a smaller transparent tensor program that is jointly predictive, composable, manipulable, and literally simpler under storage, compute, edge, state, and program pricing. The present Task14 path has materially advanced the first three properties, but coefficient generation and literal implementation price remain open; the durable goal is not complete.

## What changed since 20:35

The repaired MLP6-versus-MLP7 factorial established that neither native layer is a stable unit: the pair is distributed, interacting, and direction-dependent, with a narrow lexical-specificity miss. An alternate lexical control was instrument-invalid and was preserved rather than used as a repair.

The grouped source then acquired an executable quadratic head law. A midpoint derivative predicted the full finite head effect; endpoint linearization failed; frozen endpoint/midpoint coefficients predicted new source gains `-0.5, 0.5, 1.5` after native-tail installation. This improved computational specification and manipulation, but not semantic uniqueness.

OOD-fronted intervention transfer preserved the quadratic geometry (minimum cosine `0.999770`, maximum relative error `0.021516`) while revealing a direction-by-E/A/U/W task-use gate. A complete E/A/U/W-by-X lattice identified the gate as distributed and signed rather than owned by one native factor: E/A contribute roughly 29–31% each, W roughly 24%, U roughly 16%, and all signs reverse with grammatical direction. The exact universal factor-order prediction failed in one matched singular-to-plural template because W exceeded A; this correction was retained.

The normalized gate profile survived matched templates and fronted OOD syntax (minimum comparison cosine `0.97326`). More importantly, profiles frozen in a different cell plus only target empty/full endpoints predicted 140 intermediate causal conditions with worst normalized MAE `0.06397`, then leave-one-row-out profiles predicted 224 prompt-level conditions with median normalized MAE `0.05488` and a `33.63%` squared-error improvement over uniform interpolation.

A new intervention was then opened on 32 previously used but MLP6–7-intervention-new `behind/near` and `beyond/under` prompts. A clean native-only license passed all 12 cells at 8/8. The causal lattice passed all gates: frozen matched coefficients predicted the four new cells at normalized MAE `0.04001–0.05922`; signed direction shifts were `-0.10752/+0.09327`; template profile cosines were `0.99149/0.99828`; lexical effects stayed absolutely small (`0.00694`). This is intervention-held-out construction transfer, not pristine-text evidence.

Two workflow corrections are explicit. A preregistration v1 with a metadata timestamp 59 seconds later than its claim was abandoned before analysis and reissued unchanged as v2. Two causal price calculations were corrected before their outcomes in hash-bound amendments. Neither changed rows, predictions, bars, or evidence, but exact price arithmetic should move into tests for the next implementation.

## Throughput and systems audit

Authoritative result timestamps from 20:37 through 21:31 show ten terminals or staged receipts: one honest invalid lexical-control instrument, eight scientific causal/CPU screens, and one native capability license. Consecutive landing gaps were approximately `10, 4, 13, 6, 4, 6, 3, 4, 4` minutes; median serial latency was four minutes and only one gap exceeded the ten-minute target. The GPU jobs themselves took about six to seven seconds; useful time was dominated by circuit design, scorer implementation, and interpretation rather than queue wait. Focused test bundles generally completed in under two seconds. The managed GPU lane never had a competing direct process.

Safeguards did not dominate this hour: each new intervention reused the same authority/license, exact-decomposition, chunked-installation, and managed-runner machinery. The avoidable overhead was clerical chronology/price repair, not an expanding audit framework. The next runner must derive counts from compiled specs and assert them in tests before enqueue.

`CIRCUIT_FOCUS: PASS.` Every terminal changed grouping, computational specification, composition prediction, prompt/construction stability, or causal manipulation evidence.

`CEREMONY_BUDGET: PASS WITH PRICE-TEST REPAIR.` Shared gates stayed smaller than scientific design/execution, but two manual price mistakes require derived-count assertions in the next bounded block.

`NOVELTY_LESSON_GATE: PASS.` Task authorities, claims, prior-art receipts, failed instruments, results, canonical records, and the board were searched before each candidate. The work kept MLP6–7 grouped, preserved lexical failures, treated opened text honestly, and avoided rank/reconstruction drift.

## Confounds and evidence boundary

- Baseline subtraction is explicit: every q(S) is exact-X minus base-X under the same row/background, and all downstream effects use the same native row baseline.
- Frame mixing is prevented by retaining direction and template cells. Averaging is used only after per-cell gates pass.
- Nonlinear loss composition is not used to infer the law: the primary composition object is signed answer-margin response, with CE and installed-head effects as parallel outcomes where registered.
- Shared token difficulty is controlled by matched same-number lexical sources. Relative lexical ratios remain unstable when opposite effects are tiny; the newer claims are therefore absolute-collateral claims only.
- Leakage/post-selection is bounded but not eliminated: CPU crossvalidation used frozen receipts, and the newest causal intervention was unopened, but the text/native outcomes themselves had been opened by a prior QK attempt. It cannot be called pristine held-out text.
- Tripwires are live: the alternate lexical instrument failed, exact factor ordering failed, and neither was rescued. All new-construction native cells were 8/8 and causal direction shifts were far above the 0.002 noise floor.
- Numerical precision is controlled by exact native replay, state/normalized closure, no-op reinstall, Möbius closure, fixed checkpoint hashes, and bounded chunks. Current causal exactness errors are zero or `4.34e-19`.
- The chief remaining modeling dependency is target endpoint calibration. Cross-cell shape coefficients transfer, but the absolute q(empty)/q(EAUW) amplitude is still supplied by two target interventions rather than generated from text/native state.

## Route comparison and decision

1. **Continuous E/A/U/W gate manipulation — selected.** Scale the distributed background while holding grouped X fixed, freeze the binary endpoint law, and test interpolation/extrapolation at `r=-0.5,0.5,1.5` after native-tail installation. This directly changes composition, selective manipulation, and computational specification. It is killed by poor head/task prediction, non-monotone direction control, template instability, or lexical collateral. It is not rank work.
2. **Pristine new-text authority and capability gate — second.** This would most directly improve held-out identification, but no unopened licensed Task14 authority is currently available. Creating one is useful only if it freezes disjoint vocabulary/templates before native outcomes and survives capability screening; it is killed immediately by native failure. Do this after the continuous test rather than delaying a seven-second already-specified manipulation.
3. **Target-amplitude generator from native observables — third.** Predict q(empty) and q(EAUW) without target interventions, then reuse the shared profile. This attacks the remaining endpoint-calibration dependency and eventual simplicity, but fitting it now on opened constructions risks post-selection and needs a genuinely held-out construction. It is killed by failure to beat a direction-only mean on untouched text.
4. **Broad unrelated-task collateral battery — fourth.** This would strengthen selective manipulation, but it is more expensive and less diagnostic until the background gate is shown continuously controllable. It is killed if the intended Task14 response itself is not predictable under continuous edits.

The current route therefore survives the alternatives. The continuous-background preregistration is already frozen at SHA `8a137c17…` and claimed. The implementation must derive its physical price from the compiled installation list, correct the currently hand-written example count before outcome if necessary, run focused tests, and use only the managed GPU lane. After the result, the next branch is either a selective collateral test (if continuous control passes) or a bounded failure equation showing where binary composition ceases to interpolate (if it fails).
