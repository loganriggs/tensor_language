# Preregistration — late_tail_gate_program_composition_probe (Claude, LANE 1 CUDA) — Registered 2026-09-04 03:02Z (box clock)

Sign convention (§2135): every CE number is CE ADDED ABOVE THE REAL MODEL on held-out docs 0–63 (FRESH split; second moments from docs 96–191) — LOWER IS BETTER; ALT arms are the same quantity on docs 64–95 (in no fit). Fractional bars carry a floored denominator (.002) and an additive floor; every cost below is expected ≥ 0 (truncating or removing a cross term is expected to hurt) and the sign assumption is stated per prediction.

## Question
§2804 found the late tail-read gate compressible ONE BLOCK AT A TIME (block 17 at 16 modes .0083; block 16 at 64 .0051; even block 15 at 64 .0063) and found the ten cross terms' removals super-additive (Σ singles .190 vs joint .3668, factor 1.94). Does the compression compose into a program for the whole late stack, or does the compounding that doubles the removal cost also double the truncation cost? And is the compounding a pairwise property (already visible when two cross terms are removed) or a collective one (appearing only when many are)? Also: §2804's two-block program passed its .015 bar by 9e-5 — the same arm on the disjoint docs 64–95 says whether .015 is the number.

## Instrument
Parent late_tail_gate_per_block_probe (§2804): same Gram, whitened modes, Gate(l, k). many(kmap) = the listed blocks at their ranks, the others exact. RANK_OF = 16 at block 17, 64 at block 16, 128 at blocks 8–15 (§2799's per-block 128-mode energy .69–.77 at 8–15). Arms: SPLIT8_1024; GATE_EXACT; singles B{l}_GATE_{RANK_OF[l]} for l = 8..17; CUM_n = blocks 17, 16, …, down to the n-th, all at their ranks (n = 1..10; CUM_10 = the full program); PAIR_16_17_GATE_0 and PAIR_8_9_GATE_0 (two cross terms removed); ALT_GATE_EXACT and ALT_TWO_BLOCK (block 16 at 64 + block 17 at 16) on docs 64–95. Priors from §2804: B16_GATE_0 .0181, B17_GATE_0 .0458, B8_GATE_0 .0304, B9_GATE_0 .0209, B17_GATE_16 .0083, B16_GATE_64 .0051, TWO_BLOCK .01491, Σ singles(GATE_0) .1895; from §2799: joint GATE_0 .3668, GATE_128 .0627. Price: 96 fit + 64 × 25 + 32 × 3 ≈ 1800 GPU document-forwards (~45 s).

## Predictions (scored exactly as written) — each with the value under the hypothesis, under the null, and the operands' assumed signs
- pred_a_instrument: |baseline − 3.0322401| ≤ 1e-4; |SPLIT8_1024 − .0374| ≤ .015; |GATE_EXACT| ≤ 1e-3 on both splits; |B17_GATE_16 − .0083| ≤ .015; |B16_GATE_64 − .0051| ≤ .015; Gram check ≤ 1e-6.
- pred_b_two_block_price_replicates_on_a_disjoint_split: ALT_TWO_BLOCK ≤ .03. Null: ≥ .04. Sign: positive. Worked example: if .015 is the price, the 32-document ALT split (larger sample variance, ~.005) reads .010–.020; if §2804's pass was a split artefact, ALT reads ≥ .04.
- pred_c_full_program_compounds_weakly: CUM_10 ≤ 1.5 × Σ_l B{l}_GATE_{RANK_OF[l]} + .002. Null: CUM_10 ≥ 1.9 × Σ singles − .002. Signs: all positive; Σ singles expected ≈ .04–.06 (ten singles of ≈ .004–.008). Worked example: if the compounding is second order in the residual errors (small truncation residuals interact less than whole removals), the factor is ≈ 1.2 → CUM_10 ≈ .06; if the factor is the removal factor 1.94, CUM_10 ≈ .10.
- pred_d_pair_16_17_removal_already_compounds: PAIR_16_17_GATE_0 ≥ 1.5 × (.0181 + .0458) − .002 = .0939. Null: PAIR_16_17_GATE_0 ≤ 1.1 × .0639 + .002 = .0723. Sign: positive. Worked example: if the super-additivity is pairwise (each block's tail read partly compensates its neighbour's), two removals already show factor ≈ 1.5–1.9 → ≥ .096; if it is collective (compensation needs many other blocks to fail), the pair is additive → ≈ .064–.072. PAIR_8_9 is recorded as the early comparison (no bar).
- pred_e_full_program_near_uniform_128: CUM_10 ≤ .0627 + .01 = .0727. Null: CUM_10 ≥ .0627 + .03 = .0927. Sign: positive. Worked example: the full program differs from §2799's uniform GATE_128 only at blocks 16 (64 modes) and 17 (16 modes); if those blocks stay compressible inside the joint program it adds ≈ .005 → ≈ .068; if their truncation compounds with the other eight it adds ≥ .03.

Bars: {"ce_tol": 1e-4, "repro_tol": 0.015, "exact_tol": 1e-3, "gram_tol": 1e-6, "floor": 0.002, "b_alt": 0.03, "c_factor": 1.5, "d_factor": 1.5, "e_margin": 0.01}. Null bars: {"b_alt": 0.04, "c_factor": 1.9, "d_factor": 1.1, "e_margin": 0.03}.

## What each outcome means
c and e TRUE: the per-block compression composes — a late-stack program with 16/64/128-mode gates costs ≈ .06–.07, and the compounding is a property of REMOVAL not of truncation; the marginal-cost curve (CUM_n − CUM_{n−1}) then says which blocks' compression is expensive in company. c FALSE with null met: the compression does not compose; the late tail reads must be compressed jointly (a joint gate frame) or not at all, and §2804's two-block item is the whole program. d TRUE: the interaction is pairwise; d FALSE with null met: collective — the late tail-read network is redundant (any one or two reads can fail) but not many. b settles §2804's marginal pass. Nothing installs into the §312 frontier; the strict explained fraction is unchanged by any outcome.
