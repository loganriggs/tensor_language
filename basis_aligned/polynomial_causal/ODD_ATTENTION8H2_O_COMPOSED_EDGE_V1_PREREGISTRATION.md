# Compose the inherited-city head8.2 edge through head9.8 O

On the frozen96-row four-template union, run the routing-closed head8.2 program
from normalized block8 current state and paired city embeddings. Add its write
only at frozen framing destinations before block9 reentry/RMS. Using the existing
exact O graph, keep recipient head9.8 odd routing and substitute only the induced
current-value difference at framing sources; inject the resulting O residual
delta before the native suffix. Native plus composed arm costs192 body forwards
under180 seconds, no fitting.

- `pred_a`: native and composed target/work-jobs scores replay the destination-
  role artifact's native/full-inherited arms within `1e-5` across all96 rows.
- `pred_b`: composed O deltas are finite and live in every family, outside the
  final O injection no path is changed, and exactly192 forwards execute.
- `pred_c`: parent native capability remains at least10/12 and composed target
  effect RMS at least `1e-5` in every family; all four unrelated-control ratios
  replay the parent within `1e-5` absolute.
- `pred_d` (unfavorable dedicated interface): counting block8 current, block9
  residual, initial state, first values, two city embeddings, masks and city
  index as dedicated runtime ports exceeds one dense `T*1152` residual write at
  every observed length.

The combined program reuses the884,737-scalar head8 edge and1,048,833-scalar O
graph plus two block9 reentry scalars. Native generation of all state ports and
the suffix remain external. This is conditional composed extraction, not a
standalone language model, corpus OOD result, or quantization.

