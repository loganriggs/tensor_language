# Multi-reader consumer-response basis for the head17.2 source program

The frozen native-head support transfers the regional target effect but loses
unrelated-reader cancellation under cue-role crossover.  Reopen only the 48
`ODD_FRAMING_FRESH_V1_ROWS.json` discovery rows and keep its eight source edges
fixed.  Replace native output coordinates with a single shared residual basis
derived from downstream causal response.

For each row, each of the target plus five fixed preservation readouts, and
both endpoints, differentiate the complete map

`residual17 -> edited head17.2 QK2/value corner -> install/remove at pre-MLP17 -> sparse final readout`.

Normalize each row/readout gradient by its full token×residual Frobenius norm,
sum the resulting residual-coordinate Grams, and eigendecompose that 1152×1152
multi-reader Gram.  This gives one shared response basis; no behavioral effect
values or source additions enter its construction.  Project the *sum* of the
fixed eight propagated source-edge changes tokenwise onto ranks
`1,2,4,8,16,32,64,128`, then recompute the nonlinear head corner and final
suffix exactly.

Controls:

- source-addition PCA bases at the same ranks;
- four seed-`202609160612` random orthogonal nested bases per rank; and
- the unprojected frozen eight-edge program.

Select only among response-basis ranks by frozen minimax gate score, then rank,
response error.  Predictions are: exact instrumentation and orthonormality;
response replay; bidirectional causal fidelity; preservation; selected rank at
most 64; score at least 10% below same-rank source PCA; and score plus `.10` no
larger than the same-rank random median.  A pass is an opened-panel candidate
requiring a frozen panel.  A valid null rejects this shared first-order
multi-reader metric and motivates context-gated or second-order coordinates,
not renewed module/head subset search.

Maximum price: 48 rows; 12 native/edited full-model executions over 96
sequences; 72 VJP/backward calls (six batches × six readers × two endpoints),
49 fixed basis candidates, 4,704 candidate suffix sequences plus 96 complete
corner suffix sequences; zero coefficient fits or parameter updates.
