# Full unembedding / bilinear shared-subterm screen

User-directed full composition, not the previous518-token whole-function search.
For m(u)=D[(Lu)*(Ru)]+b, all output numerators are A phi(u)+Ub with A=UD.
Use every50304 model output row; repeat the output-profile geometry on50257 tokenizer-valid rows to prevent padding-only sharing. Neither materialize vocabulary*d*d nor discard signs.

Objects: input product H_i=sym(l_i r_i^T), exact Gram GI; full-vocabulary output code a_i=U D_i, exact Gram GO=D^T(U^TU)D. Compute the vocabulary-centered code Gram separately; mean component retained explicitly, not removed from the actual model. Negative cosine allows signed reuse. This is a structural candidate screen, not circuit promotion or extraction.

- pred_a instrument: fixed planted shared-input and opposite-output fixtures held, Gram controls versus explicit native-width FP64 products <=1e-9 relative (absolute1e-9 near zero); native finite matrices and valid shape50304x1152,1152x4608. Fixed fixture global u->-u symmetry held; this is quadratic parity, not a discovery about native task behavior.
- pred_b input-operation reuse: at least8 distinct product indices have nearest signed input-function substitution error<=.10 while their partner's centered output-code substitution error>.50 on both full and valid vocabulary. These are one shared product operation feeding different consumers.
- pred_c common output-operation groups: at least8 distinct product indices have nearest signed centered output-code error<=.10 on both vocabulary sets while their input-product substitution error>.50. These are distinct input products feeding a common signed output profile.

Null either/both structures absent at this primitive grain; no rank/threshold sweep to rescue. Whole token functions need not be proportional for either relation. Exact shared input products can be evaluated once and distributed; common output profiles admit explicit sums of their input products. Approximate nominees require held-out native intervention before identification.

Fixed all4608 native products, nearest partner by maximum absolute cosine, self excluded; raw/full and centered geometry reported. Compare both matching signs. Record all per-product partner indices/scales/errors and counts, including baseline raw D output geometry. All original weights remain charged. No learned activation projectors, token labels or linguistic task selection in this screen. Primitive factorization is a candidate language, not presumed unique semantics.

Price: zero body forwards; managed GPU weight contractions. Stream U in512-row chunks; each analysis tensor <=256MiB (largest4608-square FP64,169869312 bytes), candidate bank at most256 columns. Avoid dense full token-pair tensors. Full code Gram consumed all50304rows; valid-vocabulary repeat uses50257rows. Save geometry summaries and nominated pairs, not a giant duplicate weight matrix. No global circuit claim follows from a Gram spectrum or architectural rank bound.
