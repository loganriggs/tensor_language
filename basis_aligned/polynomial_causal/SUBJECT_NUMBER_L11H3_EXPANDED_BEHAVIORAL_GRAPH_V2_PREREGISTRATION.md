# Subject-number L11H3 expanded behavioral graph V2

V1 is retained as an instrument-valid scientific near-miss: its eight-term
main/pair graph reached `0.10153` lexical-OOD relative L2 against the registered
`0.10` bar, while its other three panels passed.

V2 changes only the registered greedy budget from eight to ten terms. It binds
and verifies the exact V1 runner/result, reuses that runner's frozen computation,
and must reproduce the first eight masks before selecting terms nine and ten.
The authority, five ports, 32 corners, eligible main/pair terms, discovery-only
unscaled greedy rule, panels, numerical thresholds, and checkpoint are
unchanged.

The nine-term and ten-term prefixes are tested separately. Each passes only if
relative L2 is at most `0.10`, cosine at least `0.99`, and aligned recovery is
positive on all four panels. V1 remains a failed eight-term claim regardless of
V2's outcome.
