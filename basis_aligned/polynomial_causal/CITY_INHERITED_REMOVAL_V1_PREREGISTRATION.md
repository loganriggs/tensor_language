# Donor-free city inherited-value removal: opened causal screen

New counterfactual; do not transfer paired-swap claims. Remove half of the native
city-token inherited-value contribution in head8.2 at all registered destinations
strictly after the city and before final token. d=-.5 W[QK1(city)*QK2(city)*lambda
V0(city)]. Both routing factors are native recipient functions. Current-value
branch remains unchanged. No donor state or fitted proxy.

Candidate approximation: one-input city_inherited_removal_v1.execute, frozen by
its CPU diagnostic, computes single-head background g2 and d +
D[((Ld)*(Rg2)+(Lg2)*(Rd)+(Ld)*(Rd))/(mean(g2²)+epsilon)]. CPU local error7.2–13%
was inspected; no downstream effect of this removal has been inspected.

Panel: SINGLE_HEAD_FRESH_V1_ROWS, now opened,20cells/40sequences/240endpoint rows.
No outcomes-based row deletion. Full native suffix recomputed. Four unrelated
readers and destination support unchanged from paired tests.
Arms: native, native8 inherited-city removal, exact local-response reentry,
one-input approximation,16same-site per-position norm-matched Gaussian nulls
=800bodyforwards/300s. Null seeds17092600+1000*k+context_id, paired cue signs.
Reference is this new full native removal, not the earlier donor-swap effect.

pred_a: exact local-response reentry agrees with native removal<=1e-4absolute
AND<=1e-5relative allreadouts, localwrite<=1e-4relative, finite/outsidezero,
null norm error<=1e-5 and exactly800forwards.
pred_b: everyfamily>=18capable paired margins>=.1; candidate targetRMS>=1e-5;
predicts native removal<=.35relative L2 EVERY family.
pred_c: each4unrelated RMS<=.5candidate target EVERY family.
pred_d: >=.75capable pairs attenuate; mean attenuation>=.02 EVERY family.
pred_e: candidate targetRMS>=2random median and beats>=15of16 EVERY family.
No fitted baseline gate in this opened screen. A pass requires a subsequently
frozen predictor baseline and fresh confirmation before an OOD claim.

One native input is a property of the new removal operator, not elimination of
the donor dependency from the original swap. Same full MLP8 weights and single
head arrays remain. Full-model input state and later suffix remain external;
no token-only, new-endpoint, corpus, composition or complete-circuit claim.
