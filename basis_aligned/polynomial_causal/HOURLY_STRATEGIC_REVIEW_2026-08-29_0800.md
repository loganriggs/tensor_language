# Hourly strategic review — 2026-08-29 08:00 UTC

## The update

Two new outcomes sharpen the strategy.

First, S1897 replicated the compiled program's site-restoration pattern at 16,110
covered token types. Attention 13 is the best restoration on every role
(`+0.81 / +1.24 / +0.82` enrichment), while attention 5 still collapses the program
to `1.06 / 1.03 / 1.06` times chance. All MLP restoration effects remain tiny, but the
earlier phrase “exactly zero” was too strong: the maximum displayed four-decimal change
is `0.0042`. Because the compiled one-token construction makes MLP restorations nearly
tautological, this is evidence about the simplified program's interaction geometry,
not proof that native MLPs are causally irrelevant.

Second, the new fixed-projector quadratic test is a completed negative result. The
frozen early-MLP rank-64 bases have more mixed quadratic energy in MLP1/MLP2 than
matched random subspaces; their rank-128 union is worse again. Doubling contraction
samples preserves the result. This rules out a proposed cheap canonicalization: the
existing PCA-like correction bases do not partition MLP1/MLP2 into independently
editable tensor subprograms.

No strict explanation ledger moved. The new results prune decompositions and identify
where the simplified program is sensitive, but do not complete extraction, selective
removal, OOD transport, or autonomous state composition.

## Fraction explained

| Currency | Strict explained amount | Largest missing part |
|---|---:|---|
| Structural write interception | 36/36 | semantic and autonomous interfaces |
| Original storage with consequence-certified removal | 5.3481% | 94.6519% has no removal certificate |
| Named causal CE headroom | 10.923% | 4.72714 nat remains unnamed |
| Terminal extraction/removal/OOD cells | 0/68 | no complete behavior-by-path action cell |

Functional imitation is substantially broader than these conservative ledgers. At two
vocabulary coverages, the program reproduces the native model's particular wrong token
about `6.1--6.5` times a marginal-matched null. But the recursive state interface still
fails by `1.09--1.27` nat and a closed-state refit loses about `5.5` nat. The central
problem is not merely recovering outputs at isolated sites; it is carrying a sufficient
state through all nonlinear RMSNorm/residual interfaces without native calls.

## Largest gaps and exact blockers

1. **Family-F causal port.** Native Down is better downstream despite worse local
   NRMSE. The balanced-port mathematics and pure CPU implementation now exist, but the
   fresh 192-document role cannot lawfully be published until an independent row audit.
   The CUDA measurement adapter and result semantic validator are also absent. GPU
   availability is not the blocker.
2. **Autonomous state composition.** Native-stream maps depend on a native upstream
   state. The finite L8-to-L11-to-L14 triangle has a valid 384-unique-document cache,
   but still lacks full source closure, create-only lifecycle, matched PCA/RRR and null
   controls.
3. **Terminal semantics.** The copy/induction contract names six heads, a four-head
   subset and a late pair, but fresh four-role rows, attention adapters, scorer/bootstrap
   authority and result lifecycle are missing. E4.1--E4.3 remain outcomes-free.
4. **Uneven whole-program cost.** S1897 shows a stable late-attention interaction
   profile, while current table rank is spent uniformly. No fit-only allocation has yet
   beaten the frozen CE frontier at exact price. Restoration gain cannot itself be used
   to select and evaluate on the same roles.
5. **OOD/selective editing.** Existing prose content coordinates transport poorly to
   code, and no current simplified coordinate has passed removal plus OOD collateral
   checks.

## Candidate pruning and top five

The ranking uses expected information gain, causal relevance, whole-model composition,
falsifiability, GPU cost, and redundancy.

1. **Fresh Family-F native-Down balanced port.** It attacks a measured causal paradox
   and tests whether downstream observability is a better simplicity principle than
   local reconstruction. It is the highest-return experiment, but is blocked by the
   independent row audit and CUDA adapter.
2. **Fit-only uneven table-rank allocation.** S1897 supplies replicated evidence that
   uniform price is mismatched to the compiled program's interaction structure. Use
   fit-only predictive eigenvalues/causal sensitivities to allocate ranks, then score
   sealed CE and KL at the exact frozen prices. This is cheaper than a new semantic
   circuit and directly improves the executable whole-program frontier.
3. **One terminal copy/induction extraction-removal-OOD cell.** This would be the first
   direct validation of whether a simplicity measure supports practical interpretation,
   but it has more row/adapter/lifecycle blockers than rank allocation.
4. **Controlled finite transport triangle.** It directly tests autonomous composition
   and empirical state realization. Do not expand to generic Hankel models unless the
   sealed L8-to-L14 composition beats local PCA/RRR and shuffles.
5. **One tight-budget shared/private hierarchy.** Flat sharing helped only at rank-64
   and rank-128 prices. A single bounded exact-price follow-up is nonredundant; all
   rank-512 hierarchy, typed dictionaries, and SAE rotations remain pruned.

Pruned this hour: direct-sum/HOSVD based on the v3 correction bases; their union;
declaring MLP restoration effects exactly zero; using restoration gain as an additive
circuit score; more global scaling; rank-512 shared dictionaries; norm matching; and
exact same-depth Family-F gate merging, which already meets its arithmetic-rank lower
bound.

## Safe action executed

The fixed-projector quadratic pilot was preregistered and committed before inspecting
real values. It then ran on mmap-loaded native MLP1/MLP2 weights with two Gaussian
seeds, two matched Haar controls, and both rank-64 and rank-128 projectors. Four
known-answer tests passed. The preregistered 64-sample result failed, and a separately
labeled 128-sample robustness run confirmed every conclusion. Static result:
`FIXED_PROJECTOR_QUADRATIC_CLOSURE_RESULT_2026-08-29.md`.

This is a real CPU evidence outcome, not an unrun runner. It does not count as E3.2 or
E4 evidence and does not move the strict ledgers. The higher-priority native-Down run
was not executed because its own frozen protocol explicitly requires an independent
row audit and a measurement adapter that do not yet exist.

