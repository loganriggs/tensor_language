# Hourly circuit-only review — 2026-09-05 20:35 UTC

## Circuit interpretation targets and controlling goal

The target remains a smaller transparent tensor program that is jointly predictive on held-out and OOD text, composable when replacements are installed together, selectively manipulable under removal/swap/edit, and simpler under literal storage, compute, edge, state, and program pricing. Circuit records must eventually specify what is read, the operation performed, what is written, and the downstream users; allow cross-module grouping and within-module splitting; predict held-out behavior and causal effect; support extraction or a precise background interface; survive selective interventions, plausible controls, data splits, and gauge choices; and expose reusable composition. Native heads and MLPs are candidate boundaries, not semantic units. Rank, reconstruction, and variance are not circuit evidence.

## What changed since 17:18

The active Task14 MLP6-versus-MLP7 complete factorial did not produce scientific evidence. A first 6,064-row physical condition forward failed before outcomes with a 10.23 GiB allocation OOM. The prospectively frozen two-chunk repair then occupied the serial lane for 99 minutes and exited 137 with an empty log and no result artifact. This is a workflow failure, not a causal null. The active claim remains unreleased.

The parallel breadth lane produced three terminals: `possessive_number.their_vs_his` was an honest native-capability null that falsified the proposed grammatical-versus-lexical boundary because pronoun position was uncontrolled; `modal_remoteness.would_vs_will` was a valid mood screen at `resid:18`; and `voice_frame.passive_vs_active` was a valid voice screen at `resid:18`, with the earliest corpus onset at `resid:03`. The retraction is retained rather than rescued post hoc.

## Throughput and systems audit

From 17:18 to 20:25, four terminals landed: one pre-outcome OOM invalid, one honest capability null, and two valid screens. The median scientific GPU screen remained about seven seconds, but the Task14 retry imposed a 99-minute serial stall. The effective rate was far below the one-terminal-per-ten-minute target. Time was dominated by invalid GPU computation and idle/empty-queue time, not scientific design.

The repair is deliberately small: deterministic contiguous condition chunks of at most 256 rows, unchanged logical examples/interventions, and immediate CPU retention of only the registered subject-position vocabulary vectors. The full-logit native-noop maximum is still computed transiently within each chunk. This is shared memory-safe execution within the existing runner, not a new framework.

`CIRCUIT_FOCUS: PASS.` Every scientific terminal advanced circuit breadth, Task14 source splitting, or an honest boundary correction.

`CEREMONY_BUDGET: FAIL, ENGINEERING REPAIR REQUIRED.` The 99-minute invalid run dwarfed scientific execution. The next bounded block is exclusively the tested chunk repair and managed rerun; no new bespoke candidate opens first.

`NOVELTY_LESSON_GATE: PASS.` Claims, Task14 parent results, invalid receipts, fast-screen ledgers, and the board were checked. The repair preserves the frozen MLP6/7 causal question and does not drift into rank, compression, or activation reconstruction.

## Confounds and route decision

- Baseline subtraction and nonlinear composition remain explicit: CE and answer-margin effects are evaluated against the same native row, while Möbius terms describe the finite intervention set function through RMSNorm and downstream nonlinearities.
- Frame mixing is prevented by retaining direction/template cells separately.
- Shared token difficulty is bounded by same-number different-lemma controls, whose maximum was already near the 25% bar and therefore cannot be dropped.
- Leakage and post-selection are bounded by the hash-bound parent, licensed rows, and predictions frozen before MLP6/7 outcomes.
- The live tripwire failure was physical batch memory, not a dead scientific knob. The second amendment binds fixed chunks before another run.
- Precision remains checked by exact parent endpoints, authoritative same-role aggregates, and the full-logit recipient no-op maximum.

The recursive factorial remains the highest-information route because it can decide whether MLP6, MLP7, their interaction, or a direction-dependent organization refines the already causal MLP6--7 family. It directly changes computational specification and within-module splitting. It is killed if exact parent closure fails, lexical collateral exceeds the frozen bar, or none of the registered organization alternatives holds; then MLP6--7 remains the operational unit and the next route becomes causal downstream-equivalence or exact weight contraction, not a finer marginal sweep.

The breadth route remains valuable in parallel, but the post-hoc onset-versus-cue-locality pattern must receive a prospectively registered test before being discussed as a law. Compression/frontier work remains demoted because it cannot change a circuit-identification target here.

## Continuation receipt

The zero-outcome retry amendment is frozen at `circuits/prior_art/task14_head11_3_fresh_matched_subject_mlp8_mlp6_7_source_factorial_v1_oom_retry_amendment.json`. The runner now uses 24 deterministic chunks, 50 physical forwards, 12,224 unchanged example evaluations, and 6,048 unchanged interventions. Ten focused tests and the no-model plan passed. The immediate action is to commit this bounded repair and enqueue the hash-audited runner through `bqrunner`; interpretation must follow when it lands.
