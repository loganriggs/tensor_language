# Conditional preceding-MLP source test

Frozen branch3/8, same64freshpairs/128endpoints,16nativebodyforwards,max18tokens;300second limit. Collect full native MLP15 output (including its bias), scale by block16 re-entry coefficient to define producer m. At actual MLP16 input, define background b=r16+a16-m. This is an edge-specific intervention with native attention16 fixed, even though MLP15 also influences attention16 elsewhere.

A: source RMS and cached native input/tail replay, native gain1branch3/8 writeerrors<=1e-5; exactgain program versus direct packed execution for gains0,.5,1,1.5<=1e-8; exactly16bodyforwards/128endpoints. B: branch8 background-only numerator preserves fullbranch swaps with symmetricrelativeRMS<=.1/sign>=.9/live>=4 inall3activefreshfamilies. C: complementary producer-dependent numerator does so at identical bars. These are opposing approximation hypotheses; either/both may fail. Quotedcontrol remains reported separately. Also report background-only with input normalization recomputed (gain0), retaining native downstream denominator.

No fitting or row selection. Same five-sector algebra applies; m contains a bilinear computation, so the fully substituted numerator can have degree8 in the preceding MLP input, with normalization ports explicit. No dense eighth-order tensor is built. This tests the actual computational source of the existing candidate, not a new semantic circuit claim. Runtime/storage price:16nativeforwards, about1.2MB source tensors plus small polynomial tables; existing frozen packedprogram1,994,688floats and full native background retained. Null: neither isolated source sector predicts the full branch. Reuse the native source collector pattern and exact gain helper; preserve previous attention result unchanged.
