# Hourly review — 12 September00:27 UTC

The targets remain explicit reads/operations/writes; cross-module grouping and within-module splitting; held-out/OOD prediction; extraction/sufficiency; selective manipulation; composition/reuse; and stable identification. Literal simplicity accompanies all four user properties; no new circuit is promoted. The user's later bilinear handoff and weight-first instructions supersede the stale better_math_ideas goal wording.

This hour established that input projection was a major source of quartic replica disagreement. Full-source groups replicate writes and swaps much better, but removal fidelity remains incomplete. Their initial compact spectral extraction fails. Exact output optimization, mixed quadratic-product cores, isotropic repeated-input weighting and harmonic truncation have each been tested with their restrictive assumptions explicit. None repairs the frozen initial input bank. The actual nonlinear coefficient fit moves those readers and reduces native write error from56.9% to22.8%, with improved but failing swap/removal fidelity. It remains unconverged; V3 is live.

There is concrete progress on extraction and reuse interfaces, not yet OOD or semantic identification. The strongest next action is improving convergence of that same exact objective. A geometric preconditioner can distinguish poor coordinate scaling from a representation limit without changing ranks or fitting text. Output-only sweeps on the inadequate initial bank are lower priority. Once a frozen fit passes basic fidelity, evaluate a second start and fresh native interventions; do not rename coefficient features as circuits.

Confounds checked or retained: native background and denominators remain explicit; physical-level similarity does not imply correct input changes; lower-degree harmonic pieces can cancel on text; spherical/Gaussian weighting is an assumption, not the training distribution; signed CE improvement is not preservation; the reused128-endpoint panel is developmental; all weight fits remain independent of it. The live nonlinear gradient is not near its threshold. V2's scaled Gram condition88 does not alone indicate catastrophic degeneracy.

Serial receipts: native spectral screen about3.3 GPU seconds, exact coupled writer3.7seconds, mixed core24.9seconds, repeated-input solve3.8seconds. The nonlinear continuation took1010seconds and needs more optimization. Direct native-effect and negative-result diagnostics used CPU while the managed run was live. Storage recovery removed verified-unused downloaded caches; no model/results were deleted.

- **CIRCUIT_FOCUS: PASS for user-directed discovery/extraction infrastructure.** Two native effect screens and explicit shared-node removal algebra directly constrain circuit claims. Zero promotions.
- **NOVELTY_LESSON_GATE: PASS.** Module dossiers, earlier Gram gauges, input projection failures and amplitude diagnostics were checked; new alternatives changed explicit assumptions.
- **CEREMONY_BUDGET: FAIL on available phase records.** Coarse last-hour records show37.4min review,13.1publication,7.4science and2.1implementation. Several implementation blocks were not marked when they began, so these are not reliable measurements of actual review time; do not retroactively relabel them. Publication churn and bad phase marking both need correction. Immediate repair: mark implementation now and execute one direct preconditioner metric/gradient control using existing kernels, without another reporting framework. Keep subsequent routine results to receipts and board entries until the next major result or hourly update.

Next hourly01:27; mathematical review01:56. Continue the active goal; the liveV3 run is not completion.
