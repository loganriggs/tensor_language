# Which native factor implements middle-entity matching?

The complete L2H1 forward and L2H2 backward join scores have validated
middle-entity truth tables. Their full QK1 and QK2 bilinear forms are not
proportional, so one complete factor cannot simply replace the other. This
does not settle their operations on the actual contextual semantic inputs.

Use the already opened suffix_join_middle_match_reference.py populations:
16 IID24-cycle and16 OODtwo12-cycle worlds, all6 chain orders, all4 cases
(base, keys-only relabeling, values-only relabeling, both). Keep the frozen
pair masks and head1 for B2_later/head2 for B3_later. No fresh-data claim,
head/order choice, amplitude fitting or new replacement grammar.

Extract native L2 Q1K1/d and Q2K2/d on the four original pair-to-pair cells.
Their product must reproduce the native joint attention cells and the saved
joint-score receipt at1e-9 absolute/1e-10 relative RMS. Keep all four cells,
including small ones; do not select only value-to-value cells. Replay the
native query logits against saved endpoints if the runner executes them.

For every population/orientation/factor, compute RMS separately for base,
keys, values and both. With denominator max(min(baseRMS,bothRMS),1e-8), an
individual factor is nominated only if each mismatched-case RMS ratio<=.10
and both matched RMS values>1e-8. Report both fixed alternatives independently.
The joint product must replay its known .10 truth-table gate as positive control.
Do not infer an independent predicate for a factor merely because the product
passes. If neither passes, retain the coupled-product matcher; do not fit a
linear mixture or select a head/task subset after seeing the result.

This is a semantic factor screen, not a physically extracted replacement or
selective factor-removal claim. Follow a positive nomination with a native
factor interchange and fresh confirmation before identification. All opaque
weights remain charged. The initializer invariance/constant-bias route stays
closed; this experiment returns to a contextual join already causally located.
B4FP64,1800s,256MiB per new tensor; managedGPU only. Shared CPU controls and
token generator are reused; no new compiler or rank search.
