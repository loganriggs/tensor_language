# Hourly research review —12 September,04:27 UTC

Circuit targets: explicit read/operation/write specification; grouping across
boundaries and splitting within modules; held-out/OOD prediction; extraction;
selective manipulation; composition/reuse; stable identification. The full goal
is a simpler executable program satisfying all four user properties. No new
circuits are promoted.

The passing developmental outer32 approximation did not survive all fresh and
contextual swap checks. Producer weighting improves fidelity but leaves five
of32context cells failing; six native-capability failures are separate past cells,
so excluding weak-native rows would not rescue the swap verdict. Exact shared
reader graph rewrites work, but dropping small difference parents fails removal:
mixed cross terms explain more than99% of the resulting coefficient error.
Output and CE accounting support a broad vocabulary contribution, not a selective
morphology mechanism. The complete full-U rank32 path is markedly insufficient.

Decision: stop treating the selected two-output slice or a32-output global
truncation as the fundamental discovery object. The user explicitly requested
weight-first composed-path structure. Test common input blocks retaining all
outputs, with exact coefficient scoring and a subsequent causal validation gate.
This targets within-module splitting, independent operations and predictable
joint edits; low reconstruction error alone cannot promote it. The native block
screen completed just after the review boundary and failed separation/stability.
Its exact rotation derivative is now controlled, enabling a meaningful check of
the search restriction rather than another rank sweep.

Confounds: inspected rows are developmental; failed capability cells cannot be
quietly removed; common pre-tanh shifts are not irrelevant after tanh; paired
quartic coefficients are not the fully symmetric tensor; quiet directions can
mimic weak coupling; sketch-selected bases can miss true blocks. Exact numerical
controls, whole-output scoring, random-basis comparison and an executed quiet-
direction counterexample address instruments without establishing circuit identity.

[Phase accounting](HOURLY_PHASE_ACCOUNTING_2026-09-12_0427.json):24.32minutes
science,15.57implementation,10.13publication,8.56review,0.37validation,
1.06unrecorded. Some controls and restoration intervals fall under neighboring
labels; these are boundary categories, not exact subtask timers. Native fresh,
producer,context and full-U executions each took seconds; the context job waited
behind Claude's live job. Several CPU interpretation checks also completed.
No claim of one scientifically independent circuit per experiment arm.

- CIRCUIT_FOCUS: PASS. Extraction and selective-effect failures changed the route;
  the new weight-only structure is only a candidate interface for causal testing.
- CEREMONY_BUDGET: PASS on recorded categories, with attribution limitations above.
  Reuse existing contraction/scoring and block-toy code; no new audit framework.
- NOVELTY_LESSON_GATE: PASS. MLP16/17 dossiers and prior MLP0 commutant tests read;
  the new target and old method are distinguished explicitly.

Next actions: (1) exact input-subspace rotation to audit the fixed-basis block
failure; kill the orthogonal half-block hypothesis if adequate local optimization
also finds large coupling, without claiming a global impossibility; (2) retain
overlapping/shared arithmetic graphs as an alternative because their cross terms
are already known to matter; (3) native validation only for a weight-defined
candidate worth extracting. More outcome-guided tuning on the old morphology
panel has lower information value. Next hourly05:27; math review05:00.
