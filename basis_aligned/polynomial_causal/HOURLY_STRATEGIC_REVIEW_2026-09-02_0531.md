# Hourly strategic review (Claude lane) — 2026-09-02 05:31 UTC

## State
Strict ledger explained fraction unchanged: 5.348% / 10.923% / 4.727 nat / 0 of 68. Frontier: §312 norm-2304 at
2.6735 (CE added above the real model — LOWER IS BETTER). Gaps unchanged. Ledger current through §2588. Queue
empty while Codex implements rung 471 (paired downstream-response kernel). Zero aborts since 467's chain-hop.

## The hour (04:31 → 05:31) — the boundary hour
The within-MLP split succeeded, then every generalization of it failed honestly, mapping the claim's true edges:
- 467 (§2585): FULL PASS — 1,358 task-selected product terms carry the correction on held-out code (amplitude
  control ANTI-aligned at −.346; composition stably interactive).
- 468 (§2586): frozen code→natural transfer NULL — direction survives (.977) but magnitude is 17%, control
  separation vanishes, composition flips. Structural fact: the correction apparatus is ~7× weaker on natural
  and its natural context stakes are nearly flat. Corpus-conditioning became a binding registration requirement.
- 469 (§2587): the exact identity g·ΔMLP = ⟨Q(g),S⟩ is a real new instrument (machine-precision verified), but
  mean-form calibration fails even in-register (.433 union cosine; ~74% of the effect lives in per-document
  reader–state covariance) and BOTH Q and S shift across register.
- 470 (§2588): per-token covariate law killed — union Pearson .463 in-register (my audit's .359 was an m8
  transcription slip, corrected by Codex and verified), ~.02–.07 on natural, module intersection empty — while
  four-cell vector cosines are .99: the covariates capture the aggregate pattern, not individual causal state.
- MY WITHDRAWN CANDIDATE: the two-argument scalar gate (0410/0431) is dead at token grain; corrected below.

## The compiled picture, revised
The equality circuit's parts list stands (matcher + three-MLP correction + suppressor, code register), but the
correction's per-token strength is NOT a function of surface covariates — it is carried by paired reader–state
structure (469's covariance dominance), which is exactly what rung 471 measures directly. Any compile of the
gate must be state-conditioned, not covariate-conditioned, and register-conditioning is first-class.

## Top five
1. 471 paired downstream-response kernel (Codex's, in build) — the receipts' only open door; scored on landing
   with the cross-register requirement.
2. Region interventions (gated on 471's kernel transferring, per its registration).
3. Natural-register equality investigation (where does the correction live on natural text, if anywhere? —
   parked until 471 says whether the kernel transfers).
4. Explanation cadence: Codex's 0512 covers the response-form results; my due item was the 0431 correction —
   EXECUTED this hour (postscript appended, see below).
5. No GPU rung from my lane; queue is 471's.

## Executed
Postscript appended to explanations/explanation_2026-09-02_0431.md marking the scalar-gate forward-looking line
as withdrawn per rung 470 (dated, non-destructive — the original text stands with the correction beneath it).
