# Frozen cubic-bank physical head13.0 screen

Registered 12 September 2026, before execution. The earlier trust-pilot's two
secant endpoints, re-expressed in the exact three-product cluster chart, are
frozen. The ongoing longer optimizer run does not select these programs.
Head13.0 accounts for most coefficient gain; its existing dossier warns that
an earlier leading **value-only** component opposed its remainder and transferred
little regional behavior. This tests the different, full source-cubic projection.

Project the unnormalized joint QK1 × QK2 × value polynomial onto each frozen
rank16 source bank. Retain actual relative rotary positions, original full QK
normalizers and the original physical output matrix O. Remove that additive
physical component, then recompute the native suffix. No arbitrary lift from
four consumer reads. The full native prefix and background remain supplied.

48 existing British/American city prompts, two families of12pairs, shared row
validator. Arms: native, bank0 removal, bank1 removal, full head13.0 removal.
192 body forwards,600seconds including compilation; no data fitting. Native
head13 inputs must match bitwise across arms before reusing projected fields.
Full-head removal is descriptive, not an assumed positive signed control.

- **A, instrument/capability:** stored native margins relative error<=1e-4;
  projected pair FP32/FP64 relative error<=1e-4; each component norm>1e-8;
  each family's mean paired native regional contrast>=.2 and>=10/12positive.
- **B, cheap causal screen:** at least one frozen bank, in BOTH families,
  removes>=10% of the mean paired cue contrast, has>=10/12positive paired
  reductions and unrelated mean absolute margin change<=.5 of target change.
- **C, restart-effect screen:** A/B plus bank0 versus bank1 signed per-context
  removal effects have symmetric relative RMS<=.10 in BOTH families, with
  reference RMS>=1e-5. No fitted correspondence or post-selection repair.

Nulls: weight gains are behaviorally weak/nonspecific, or replica effects differ.
Whole-bank coefficient instability is not assumed to imply head13 instability;
C measures that private head directly. An A miss invalidates behavioral claims.
Even A/B/C passing is an existing-panel screen: fresh/OOD, donor, sufficiency,
extraction with prefix closure and uniqueness remain untested.

Controls: independent full cubic basis versus normalized attention, prior mixed
executor, folded consumer commutation and causal sum; native cached head9.8
FP64 reference and FP32 projection audit. Head13 receives its own FP32/64 check.
The gate algebra is exact over real arithmetic with prescribed rotary tables;
full-native FP32 operation order is not asserted identical to folded arithmetic.
