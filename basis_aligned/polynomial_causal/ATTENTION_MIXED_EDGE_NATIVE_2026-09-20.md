# Exact mixed attention edge and failed causal localization

The block11 mixed attention edge compiles exactly at the declared two-site interface. Native projected mixed-write replay error is 1.254e-15, off-query numerical residue is 5.685e-12, and prior float64 suffix replay is exact. All registered call counts pass. The compiled consumer uses 300 scalar coefficients per context; native source and downstream-reader generation remains required. This is an algebraic path result, not an extracted semantic circuit.

The preregistered causal hypothesis fails. Removing the exact mixed attention11 write halves the number-interaction norm in 0/13 previously failed weak-axis cells. Median remaining interaction is 90.09%; range 62.08%–235.07%. Four cells increase. Therefore this one local term is not sufficient to explain the downstream interaction. Attenuation of interaction is distinct from correction of the quadratic predictor's error; no claim of such correction was tested.

A subsequent CPU transport audit also rejects casually treating a baseline reader as finite downstream transport. Relative error between its signed prediction and actual finite removal response has median 57.82%, maximum 251.63%, across 32 beside_subject cell-settings; unlike_nearby median 16.02%, maximum 79.54%. The absolute removal norms are retained to expose small denominators. These measurements combine movement away from the baseline and nonlinear finite transport; they do not isolate which causes the discrepancy.

Positive redteam: native generators remain charged; exact local replay does not imply sufficiency. Negative redteam: full native algebra replay, saved suffix replay, query/key order checks and planted/dead-axis tests pass. The causal failure is not attributed to a broken fold. This is opened diagnostic data, not new OOD evidence.

Next discriminating route: compare baseline, joint-context tangent, and integrated downstream readers for the same finite removal, then census later-layer interaction sources if local transport cannot explain the predictor residual. Integrating a reader may explain an effect but its derivative cost must be charged; it is not a free extracted program. A full later-layer interaction census must distinguish transported existing interactions from newly generated ones to avoid double counting.

Receipts: `../bilinear_quotient/circuits/followups/attention_mixed_edge_native_v1_result.json`; `ATTENTION_EDGE_TRANSPORT_AUDIT_V1.json`. Preregistration: `ATTENTION_MIXED_EDGE_NATIVE_V1_PREREGISTRATION.md`.
