# Hourly strategic review — 2026-08-28 14:35 UTC

## What changed since the previous review

This review does not reopen the MLP1 protocol. That collector is now independently
GO, committed, and pushed. Its final CPU preflight passed on 37 committed source files,
the exact frozen plan and disjoint 16/16 document cohorts, with protected snapshot
fingerprint `c2aac1c3a7d841aa1c4a7b3aa57b5d215f78eacf12da14ca995256f01af046d7`.
All authority/outcome namespaces remain absent. The no-outcome authority has not been
frozen because the managed GPU queue still occupies the RTX 5090 at roughly 97%
utilization.

The strategic change is the new parallel factorization branch. MLP1 and MLP2 now each
have a separate agent implementing implicit, gauge-controlled folded-third-order tensor
diagnostics. A third agent is formalizing the common minimum-norm/HOSVD, executable-cost,
and MLP0/1/2 composition contract. This lets the physical-gate assay and intrinsic-tensor
factorization answer different questions without serial delay.

Claude's latest context-free results also prune a large family. Increasing current-token
table coverage saturates far below the live model; even an optimistic extrapolation over
the remaining vocabulary stays about 18 percentage points short. The spectacular collapse
at sample count equal to feature dimension (`n = D = 1152`) was a numerical conditioning
artifact: a ridge scaled to the largest eigenvalue restores roughly 9.4--9.9 percentage
points there. It does not make the coverage curve strictly monotone, and it does not close
the context gap.

The one-live-attention sweep completed while this review was being written and closes the
smallest-departure hypothesis. The best single live attention layer recovers only
`+0.38/+0.47/+0.35` percentage points, or about `1.5%/1.7%/1.4%` of the gap to live.
Context is therefore distributed rather than concentrated at one attention depth. More
strikingly, restoring live attention 5 or 6 into the otherwise compiled stream destroys
roughly 11--13 percentage points on all three roles, while layer 4 is almost neutral. This
is direct evidence of a sharply typed interface mismatch around layers 5--6, not evidence
that those native modules are intrinsically harmful.

## Honest explanation ledgers

| Ledger | Current result | What it does and does not mean |
|---|---:|---|
| Structural execution | 36/36 sites | Every operation is executable; semantics not implied |
| Prospectively certified whole-program storage reduction | 5.35% | Rank-640 attention simplification passed predictive/causal tests, not exact identity |
| Older named-behavior ledger | 32.1% $\pm$ 6.4% | Human-labeled behavioral categories under its own denominator |
| Strict named causal recovery | 10.923% | Intervention recovery under a different denominator |
| Strict current-ship executable recovery against $+0.8976$ CE | 0% | No composed replacement has earned this credit yet |

The largest unexplained mass remains the dense MLP banks and their interfaces. MLP0 has
real lexical and low-dimensional structure, but no admitted replacement preserves the
MLP0-to-MLP1-to-MLP2 contract. MLP1 has moderately concentrated response energy without
a repeatable global top subspace. MLP2 demonstrably changes how MLP0/1 errors appear at
the output, but we do not know whether that is a compressible compensator, distributed
damping, or fragile in-distribution cancellation.

## Missing interfaces and anomalous results

1. **MLP0 to MLP1:** local MLP0 accuracy does not determine the state MLP1 reads. The
   physical-gate assay asks which actual MLP1 actions discriminate the incoming code.
2. **MLP1 to MLP2:** MLP2 suppresses much of an exposed MLP1 mismatch, but the previous
   shuffled-write result ruled out a single specially aligned repair vector.
3. **Context injection:** token-only programs are empirically ceilinged, and no single
   live attention restoration buys more than 1.7% of the missing gap. The catastrophic
   layer-5/6 restorations show that contextual modules require compatible upstream and
   downstream representations rather than isolated transplantation.
4. **CE versus internal fidelity:** small average CE can coexist with changed top-token
   decisions, while internal mismatch can be harmless if a later module robustly
   compensates. Behavioral compilation and causal abstraction must remain separate ledgers.
5. **Statistical scale:** the 32-document gate assay is a discriminator, not final proof.
   Its validation independence count is 16 documents even though it has many positions and
   probes. Any pass requires a prospectively larger replication and data-doubling stability.

## Candidate actions considered and pruned

- More PCA or another arbitrary MLP1 response basis is pruned: the smooth spectrum and
  failed split-probe repeatability already answer that local question.
- More token-only coverage is pruned as a route to the whole model: its optimistic ceiling
  remains far below live behavior.
- A generic SAE on MLP0 activations alone is pruned: it could yield overlapping lexical
  features but would not test weight foldability or sparse downstream readers.
- Pure local MSE curves are retained only as diagnostics; they have repeatedly failed to
  predict composition.
- A full dense third-order tensor materialization is pruned: implicit mode-Gram contractions
  provide HOSVD spectra and gauge checks at far lower memory cost.
- Immediate finite gate deletion is pruned: tangent evidence can only authorize a separately
  preregistered small finite-edit assay.

## Ranked top five

1. **Freeze and run the audited MLP1 physical-gate assay when the GPU releases.** It has
   the highest immediate information gain and a binary mathematical branch: sparse native
   gate dictionary versus intrinsic tensor refactorization. It is causal, falsifiable,
   inexpensive, and already source-closed.
2. **Complete the parallel implicit folded-tensor diagnostics for MLP1 and MLP2.** These
   exploit the exact polynomial structure, remove checkpoint scalar gauges, and compare
   Down rank, Tucker/HOSVD multilinear rank, sparse-core price, and potential new gate rank.
   They remain useful regardless of the gate-assay outcome and cost CPU only.
3. **Construct the eight-cell MLP0/1/2 composition cube at matched executable cost.** Test
   `NNN, PNN, NPN, NNP, PPN, PNP, NPP, PPP` and compute pair/triple interaction terms. This
   directly decides whether MLP2 compensation is robustly useful and localizes the interface
   requiring joint refit.
4. **Localize the attention-5/6 interface cliff with a preregistered adjacent-package
   factorial.** Restore compatible pairs or a short contiguous package, separately split
   routing and value, and measure whether the catastrophe is an incoming-state mismatch,
   an outgoing-reader mismatch, or their interaction. This is more informative than another
   one-site sweep and directly targets whole-program composability.
5. **Fit an overlapping sparse MLP0 lexical dictionary jointly with centered continuous
   context residuals and sparse downstream readers.** This captures multi-label structure
   such as city + capitalization while making simplicity pay through MLP1/2 reader sparsity.

## Highest-priority safe action executed

The final CPU launch preflight for priority 1 was executed and passed. It replayed the exact
frozen plan, immutable row and parent hashes, committed source closure, disjoint cohorts, and
empty create-only namespaces. Freezing authority itself constructs the rank-640 CUDA program,
so it is intentionally deferred until the registered one-live-attention job releases the GPU.

Priority 2 is executing concurrently: separate agents are building MLP1 and MLP2 implicit
tensor operators and synthetic gauge/known-answer tests, while the shared mathematics agent
defines canonicalization assumptions, matched cost, and composition metrics. No protected
checkpoint outcome or fresh scientific row has been opened by that work.
