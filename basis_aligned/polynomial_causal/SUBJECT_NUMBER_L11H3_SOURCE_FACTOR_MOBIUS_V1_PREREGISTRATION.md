# Subject-number L11H3 source-factor Möbius V1

Registered after corrected behavioral removal/mediation passed. This is an
opened-panel extraction assay: it makes no new behavioral or OOD claim.

For the corrected 128-row authority, run the frozen same-norm embedding-number
removal and capture exact L11H3 factors for base and removed states. Use the
existing weight-native attention primitive to expose `Q`, `K`, `Q2`, `K2`, and
output-projected effective `U` at each of six source positions. Compute the full
five-factor Möbius transform (31 nonempty factor subsets) independently at every
source. Project every exact dividend onto the frozen rank-one L11H3 writer axis.

The six fixed source roles are `context`, `determiner_1`, `attractor`,
`punctuation`, `determiner_2`, and `subject`. These are positional graph ports,
not learned semantic labels. The 186 resulting source×factor atoms must sum
exactly to the previously measured L11H3 scalar response.

Instrumentation requires source-factor head identity, complete Möbius closure,
and replay of the independently recorded scalar within `1e-4`, with relative
closure at most `1e-5`, exact prices, and finite values. A synthetic five-factor
scalar fixture must close within `1e-12`, guarding against a transform that
silently drops negative results.

Without fitting coefficients, greedily select exact atoms by the greatest
reduction of residual L2. Report curves through eight source×factor atoms, four
factor masks aggregated over sources, and two sources aggregated over factor
masks. A compact atom candidate requires eight-atom relative L2 at most `.25`;
factor-only and source-only candidates require `.35` at their respective
budgets.

For specificity, repeat the identical decomposition and greedy rule on 16
frozen residual-space readout axes orthogonal to the writer axis. The target
eight-atom error must improve by at least `.10` over their median to license a
sparse native atom candidate. Passing licenses freezing the selected atoms for
fresh vocabulary/context causal testing; it is not itself causal removal of the
atoms.

Price: four partial forwards over 256 sequences, 31 exact Möbius terms, 17
readouts, no logits, fitting, gradients, or parameter updates.
