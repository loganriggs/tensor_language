# Hourly circuit-only review — 2026-09-05 22:35 UTC

## Circuit interpretation targets and full goal

The required circuit account still has seven parts: (1) specify what information is read, what operation is performed, what is written, and which downstream computation consumes it; (2) group across native modules and split within them according to downstream operational equivalence; (3) predict held-out and OOD activation plus behavioral effects; (4) extract an executable circuit or an exact interface plus background; (5) selectively remove, swap, and edit it with collateral accounting; (6) predict composition and reuse; and (7) identify it stably across prompts, constructions, controls, and gauges. Native MLP/head boundaries remain hypotheses, not semantic units.

The program goal remains a smaller transparent tensor program that is jointly predictive, composable, manipulable, and literally simpler under storage, compute, edge, state, and program prices. This hour substantially improves Task14 prediction, composition, extraction, and stable operational grouping. It does not finish selective manipulation of the newly extracted interface, eliminate the upstream native computation, establish reuse on other tasks, or provide a whole-model simpler program. The durable goal remains active.

## What changed since 21:35

The continuous E/A/U/W intervention passed: scaling the distributed background through `r=-0.5,0.5,1.5` monotonically steered grouped-MLP6--7 task use through zero, with head cosine at least `0.98664`, live task recovery `0.82261–1.15703`, template difference at most `0.03015`, and lexical effect at most `0.01174`.

A pristine zero-target-anchor transfer then supplied the important null. The normalized factor shape and signed gate transferred, but ten absolute FIT scalars did not: singular-to-plural HOLDOUT normalized MAE was `0.47169`, and factor specificity improved over a uniform law by only `0.70%`. The failure localized to construction-dependent baseline/amplitude rather than the already-stable normalized E/A/U/W shape.

The replacement object was the native downstream answer-margin derivative at the L11H3 projected-write interface. Retrospectively, a midpoint Jacobian-vector product predicted 80 frozen amplitudes at cosine `0.999999992` and relative L2 error `0.000140`. More importantly, a temporally sealed prospective test on 32 new rows, 32 disjoint noun forms, and unseen above/below syntax predicted 64 unopened amplitudes at cosine `0.999999993`, error `0.000116`, and perfect signs, without scale or offset fitting.

Composition then strengthened. One target-free central downstream gradient per row predicted the complete 512-condition E/A/U/W lattice prospectively: overall cosine `0.999961`, relative L2 `0.009332`, perfect signs; the 448 unopened intermediate compositions reached `0.999983` cosine and `0.006073` error. Every syntax/direction and cardinality cell passed. Thus a separate reader per background is unnecessary.

Leave-one-row-out direction readers transferred functionally (cosine `0.97329`, error `0.22980`, perfect signs) and reduced SSE `96.94%` versus a global reader. Their minimum ambient gradient cosine was only `0.77678 < 0.80`, so the result is an operational quotient, not a stable full-vector identity.

The first fixed-reader export was invalid because it registered the internal head width `128` instead of the actual projected-write interface width `1,152`. The invalid receipt was preserved. A width-only v2 exported two finite 1,152-D readers with exact closure; its unchanged strong-opposition prediction honestly failed at cosine `-0.30543` rather than `<=-0.50`.

Finally, those exact 2,304 scalars were committed before a second disjoint authority existed. With 32 new noun forms, unseen inside/above syntax, and zero target-tail forward/backward execution, they prospectively predicted the complete 512-condition causal lattice at cosine `0.96983`, relative L2 `0.29219`, and perfect signs. Intermediate compositions and both templates passed. Swapping the readers yielded cosine `-0.74298` and sign agreement `0.0039`; correct direction assignment reduced SSE `98.31%`. This is an extracted reusable downstream interface, despite the ambient-cosine null.

## Throughput and systems audit

From the 21:35 review through 22:32, authoritative timestamps show 19 Task14 result/license/prediction artifacts and ten managed stages after the pristine null, including two prospective two-stage validations, one honest partial, one invalid instrument, its width-only repair, and a second cross-corpus prospective validation. There were 32 commits including the review; median inter-commit gap was `1.33` minutes, mean `1.82`, and maximum `6.82`. Scientific terminals remained comfortably faster than the ten-minute target. GPU stages generally landed in 4–20 seconds; no direct GPU process competed with the managed runner and queue wait was negligible.

Time was dominated by hypothesis/scorer implementation and temporal sealing, not GPU execution. Shared authority, capability, head construction, chunked intervention, and scoring machinery prevented bespoke audit growth. Avoidable costs were two pre-model static-gate refusals and the 128-versus-1,152 interface-width invalidation. The latter is a substantive implementation lesson: name and assert the actual intervention tensor shape from a compiled dry-run or parent artifact before registering storage. The complete fast suite passes after this work.

`CIRCUIT_FOCUS: PASS.` Every stage changed circuit computation, held-out prediction, composition/reuse, extraction, manipulation, or stable operational grouping; no rank/reconstruction/compression sweep was opened.

`CEREMONY_BUDGET: PASS WITH INTERFACE-WIDTH REPAIR.` Validation stayed smaller than the scientific loop and temporal seals enabled real prospective claims. The width mistake cost one short run; the next artifact must derive its dimension from the tensor interface rather than a remembered internal head width.

`NOVELTY_LESSON_GATE: PASS.` Authorities, claims, prior-art, failed instruments, and results were searched before each candidate. The work applied the lessons that target endpoints cannot stand in for amplitude generation, ambient vector similarity is not operational equivalence, and a screen/diagnostic must be prospectively promoted before identification.

## Confounds and evidence boundary

- Baseline subtraction is exact: each causal amplitude is margin(exact-X) minus margin(base-X) within one row/background. Native margins cancel algebraically.
- Frame mixing is controlled by direction, template, background, and cardinality gates. The strongest claims pass before pooling.
- Nonlinear tail composition is measured directly. Midpoint derivatives nearly exactly integrate each row-specific displacement; the fixed-reader approximation's `29.2%` magnitude error is retained rather than calibrated away.
- Shared token difficulty is sharply reduced by two successive 32-form vocabularies disjoint from all prior Task14 authorities and by unseen reversed templates. Native capability is `1.0` in every registered cell on both authorities.
- Leakage/post-selection is controlled by commit-before-outcome temporal seals. The first prospective predictor existed before endpoint causal outcomes; the full-lattice predictor existed before 448 intermediate outcomes; fixed readers existed before the second authority; second-corpus predictions existed before all 512 causal outcomes.
- Dead knobs are ruled out by direction swapping, which reverses the functional verdict catastrophically. Continuous gains and midpoint placement are independently live.
- Numerical floors are negligible: all current source and downstream closure checks are exactly zero, while causal amplitudes are orders of magnitude larger.
- The raw-gradient stability miss is not reinterpreted as a pass. Only action on the circuit displacement family transfers. Ambient-axis uniqueness remains open.
- Literal simplicity is partial. The downstream interface is now 2,304 fixed scalars with no target-tail execution, but target head displacements still use native upstream computation; this is not yet a standalone small program.

## Alternative routes and decision

1. **Reader-guided absolute edit — selected.** On the second corpus, select the 278 row/background conditions using only the already-sealed criterion `|q_hat|>=0.05`. Set a desired signed margin change of magnitude `0.04`, choose `alpha=desired/q_hat` (therefore `0.122–0.799` on frozen support), and install `h_base + alpha(h_exact-h_base)`. Compare achieved change with the target and with a fixed `alpha=0.5` control. This directly advances selective manipulation and extraction. It is killed by wrong signs, target error above the frozen tolerance, syntax/direction failure, or no improvement over the fixed-gain control.
2. **Fixed upstream displacement law — second.** Export direction/background head-displacement prototypes and combine them with the two fixed readers on a third corpus. This would reduce target upstream compute and advance extraction/simplicity. It is killed by cross-corpus head-vector or causal-effect error. It follows manipulation because the current extracted interface should first demonstrate useful control, not merely another prediction.
3. **Reader quotient geometry — third.** Compare readers only on the span of registered circuit displacements, with held-out displacement action as the identifying norm. This could reconcile ambient cosine `0.77678` with functional transfer, advancing stable identification. It is killed if projected equivalence does not transfer to held-out displacements. It is not selected now because it risks becoming a basis/rank exercise unless tied to a new intervention.
4. **Cross-task reuse — fourth.** Test the fixed reader/upstream interface on a separate agreement construction or task. This is ultimately required for reusable circuits, but the current other number authorities often lack native capability; a capability-first screen should follow the reader-guided edit.

The route changes from pure prediction/extraction to prospective reader-guided manipulation. This is not a lower-error sweep: the decisive measurement is whether a frozen extracted reader computes an edit that achieves a chosen causal margin change under native-tail execution. The next action is to preregister the support, gains, absolute target-error bars, fixed-gain control, literal price, and managed-run implementation before any new gain outcome is opened.
