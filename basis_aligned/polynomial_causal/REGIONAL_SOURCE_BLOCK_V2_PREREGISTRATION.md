# Regional screen V2: grammar and source coverage

Preserve V1 A-pass/B-C-fail outcomes. Fix only family1article from a to the,
avoiding a American; no other row, spelling pair, control or threshold changes.
Report two-source coverage (source0/previous) and a separately declared all-source
program including every causal source, including the query's own position.
Recompile fixed weight-only private query maps for each actual position; no fitting.

For EACH coverage, score V1 bars unchanged: A native replay<=1e-5, collapsed/old
block error<=.1, bothfamilies native cue mean>=.1nat and>=5/8positive. B:A and both
mean removal reductions>=10%native cue effect,>=5/8positive. C:A and both reductions
exceed paired-direction norm-matched control by>=5%native cue effect; unrelated
contrast differential meanabs<=.5regional meanabs. Report both coverages even if
only one passes. All-source includes more native edges and is not same-cost as
two-source; source readers are reused, position-specific private maps/gates are
additional dependencies. Query/source slots coincide on self-attention during
execution; the weight discovery metric did not impose their equality.

7bodyforwards32sequences4-12tokens, atmost8batchedsuffix arms,180seconds, managedGPU.
No semantic claim from prior post-hoc token names. Positive findings remain a
basic prospective screen, pending alternative cues/tasks, OOD, selective controls,
extraction and composition evidence. V1 cached FineWeb preservation is unchanged.
