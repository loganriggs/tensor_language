# Native integration of compiled shared head2

Load frozen666656-scalar package, directly execute on native normalizedquery,
concatenated current/firstsource and relativeR across all sourcepositions.
Arms fullcomponent,originalhead2,compiled64,compiled32,compiledchild0,compiledchild1.
Native fullsuffix evaluates each removal; zeroedit baseline adds seventh arm.
A prior/native replay<=1e-5 and compiled64 vs originalhead2 write<=1e-10,
signed-prefix removal-effect<=1e-5 EACHfamily. B compiled32write<=1e-5,
effect<=1e-3 EACHfamily. C child sum/referencewrite<=1e-10. Changing a child
reader only changes the extracted feature calculation; query/key normalization
still reads original source. Report nonlinear suffix interaction rather than
assuming additive logits.48rows9bodybatches13–19tokens180secmanagedcap,<4MBartifact.
Input generators and final suffix/unembedding remain native outside the package.
No new fit, support selection, fresh-panel claim or token-to-logit extraction.
