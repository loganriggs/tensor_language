# Subject-number strong-direction two-site composition V2

V1's two-site axis effects were diagnostically additive to relative L2 error
`7.62e-5`, but its claim was invalid because the independently weak
plural-to-singular coefficient produced only `.008001` RMS at site 1 against
the frozen `.01` liveliness bar. V2 uses the independently stronger
singular-to-plural cardinality-four coefficient, `-31.14876154002341`, at both
sites. This choice follows the coefficient authority and V1 boundary; no V2
model outcome has been opened.

Freeze 16 fresh two-clause prompts from eight new noun-pair groups and two new
templates. Every recipient subject is singular, both installed changes are
singular-to-plural, subject positions remain 5 and 14, and the lexical and
template sets are disjoint from V1. Reuse the immutable rank-one axis and the
same single coefficient at both sites. Evaluate native, zero, site-1, site-2,
and joint writes.

The exactness, `.75` native capability, `.01` single-site RMS, `.75` positive
fraction, `1e-5` anticausal, `.75` unrelated-control, and composition bars are
unchanged from V1. Predict the two-site effect by adding the independently
measured single-site effects. Overall and per output, require cosine `.95`,
relative L2 `.25`, sign `.90`, and norm ratio `.80`--`1.20`. A valid additive
failure with interaction at least `.20` of joint RMS preserves that interaction
for later compression. No gain, refit, intercept, site coefficient, rank
change, row repair, subgroup, threshold change, gradient, update, or
quantization is allowed. Price remains five forwards and 80 sequences.
