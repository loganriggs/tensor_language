# Hourly research review —12 September06:35 UTC

Circuit targets: explicit reads/operation/writes; cross-boundary grouping and
within-module splitting; held-out/OOD prediction; extraction; selective removal;
composition/reuse; stable identification. The full objective is a simpler executable
program with all four user properties. No new circuit is promoted.

The hour produced a passing developmental screen for one recurring producer
reader, then a failed broader context confirmation: all32write/removal cells pass,
but12swap cells fail. Six failed cells have strong native capability on both sides.
Keep those failures; small absolute errors and weak capability in other cells do
not justify exclusions. The exact local reference is still not a named useful
semantic operation, and corpus OOD and joint reuse are unproved.

Two full-frame fits and their faster continuations remain unconverged. Reusing
support for20PR+steps increases update throughput10.4fold and improves function
agreement0.651→0.820, still below0.95. The mixed planted control exhibits a
suboptimal numerical local maximum, so convergence would not prove global recovery.
The matched-cost native dictionary captures39.67%but has worse native fidelity.
The fully symmetric quartic rescore leaves the frozen graph near9.1%capture.
[One primary comparison](COMPOSED_WEIGHT_COMPARISON_V1_RESULTS.md) contains details.

Decision: do not launch another broad orthogonal-frame continuation now. The
specific recurring node has a useful near-faithful interface. Its671edges consist
of an exact self term plus670 coordinate partners. At the same price, replace the
coordinate restriction with the exact best670dimensional perpendicular partner
subspace. Keep its reader and self term fixed. Weighted SVD solves this conditional
problem globally; it does not guarantee behavioral fidelity. This targets extraction
and shared computation of the existing candidate, not a rank-only discovery sweep.
The CPU feasibility/dominance and self-preservation control has already passed.
Next evaluate unchanged native/context bars with no data fitting or A2 tuning.

Confounds: coefficient error is not native effect error; contextual cache is
previously inspected; local write interventions hold background/denominator fixed;
reader similarity does not establish identical consumers. Centered U is a fitting
metric, not an exactly preserved common-logit channel. Corrected this explicitly.
No new text-guided fit is authorized by these findings.

[Phase accounting](HOURLY_PHASE_ACCOUNTING_2026-09-12_0635.json) is retained with
limitations: restore/review markers include science and verified waits. The two
managed optimization runs each executed about20minutes, overlapping CPU work;
short native/metric/dictionary screens took2–5seconds each. Do not add overlapping
times into an exclusive throughput claim. Many small publications remain excessive.

- CIRCUIT_FOCUS: PASS. Native extraction and broader transfer distinguish the
  recurring component from a generic better coefficient fit.
- CEREMONY_BUDGET: FAIL. Phase attribution is insufficiently precise and too many
  small review/publication units remain. Next bounded block uses existing SVD,
  partner compiler and scorer; no additional framework or broad toy campaign.
- NOVELTY_LESSON_GATE: PASS. Prior isolated dictionary, quartic oracle and shared
  parent rank16SVD checked; current matched-budget exact-self constraint is explicit.

Next hourly07:35; mathematical review08:00. Preserve user-directed weights-first
discovery and the bilinear handoff over stale goal wording.
