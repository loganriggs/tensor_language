# Fresh head9.8 QK1 structured-union screen

Frozen before model execution or outcome access on the new authority. Let
`D={attention5, MLP5, MLP6, MLP7}` and let `R` be the other thirteen sources in
the exact block-9 carry. At head9.8 QK1, compare five recursive arms: native;
remove complete late-touching `DD+DR+RD`; remove query-late `DD+DR`; remove
key-late `DD+RD`; and remove cross-only `DR+RD`. QK2, mixed current/inherited
values, rotary maps, causal reduction, output projection, RMS denominators, all
other heads, residual paths, and the complete suffix remain native.

The 48 outcome-blind rows contain two new prompt families, two new controlled
Manchester/Atlanta and Birmingham/Austin cue pairs, and six one-token UK/US
endpoints. They have zero exact-context overlap with prior row manifests. This
is fresh controlled text, not proof of corpus OOD or pretraining independence.

## Frozen checks and predictions

- `pred_a_exact_instrument`: 30 physical executions; carry and manual-attention
  reconstruction error at most `2e-6`; each structured union agrees with its
  independently grouped one- or two-contraction formula within `2e-6`; finite
  values; row and binding checks pass.
- `pred_b_native_capability`: in each family at least 9/12 matched pairs have a
  positive native UK-minus-US cue difference, and native paired-effect RMS is at
  least `.01`. No row may be dropped after scoring.
- `pred_c_complete_route_live`: complete-routing removal has paired target RMS
  at least `.002` in each family.
- `pred_d_structured_union_selective`: the same one of query-late, key-late, or
  cross-only must, in both families, retain at least 20% of complete-removal
  target RMS and reduce the work/jobs-to-target RMS ratio by at least 30%.
- `pred_e_unrelated_reader_control`: for that candidate in both families, the
  maximum RMS across cat/dog, red/blue, Monday/Tuesday, and apple/orange is at
  most the candidate target RMS.

Candidate order for reporting only is query-late, key-late, then cross-only; a
candidate passes only if its fixed gates hold in both families. No fitting,
gradient, backward pass, parameter update, quantization, or post-score row
selection is permitted. Query-late and key-late are charged as exact rank-one
128-multiply QK1 programs; complete and cross-only are irreducible rank-two
256-multiply programs, in addition to every native background/interface cost.
Cross-only therefore offers grouping but no local multiplication saving.

A pass identifies a more selective causal grouping inside the already causal
head9.8 route. It does not establish corpus OOD, native generation of the source
partition, a standalone extracted model, or downstream head17.2 causality. If
the instrument and capability gates pass but no union passes selectivity, the
terminal verdict is a valid structured-union null and the complete distributed
late-touching group remains the supported unit.
