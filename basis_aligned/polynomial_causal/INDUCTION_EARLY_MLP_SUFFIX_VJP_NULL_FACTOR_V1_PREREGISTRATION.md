# Induction early-MLP suffix-VJP-null response factor V1

The direct LM-head answer-null split removed 31.4–50.3% of vocabulary RMS but failed answer preservation, showing that later nonlinear consumers rotate answer sensitivity. Keep the previously fixed MLP8-12 group and derive one suffix-transported axis per row and module without intervention-outcome fitting: on the joint-edited trajectory, detach each MLP8-12 write, compute the exact reverse-mode derivative of recipient answer cross-entropy through all remaining native modules, and normalize it.

Split each native-minus-edited module response exactly into its scalar projection on that frozen local CE VJP and the orthogonal remainder. Reuse the established self, joint, full, parallel, and null arms on the already opened 96-row FIT/CONFIRM authority. The suffix-null factor passes only if every cell in both splits retains >=.75 answer correctness, has mean CE damage <=.10, and removes >=25% of joint vocabulary RMS. The component partition must close within `1e-5`; all existing exactness/activity checks remain.

This is an opened-row explanatory screen. A pass still requires fresh confirmation and an executable sensitivity generator. Price is 18 forwards/576 sequences plus three reverse-mode passes, zero fitting or weight updates. Do not change groups, bars, rows, gains, ranks, or quantization.
