# Bracket triple-pending payload-rank-two interaction V1

The third nested-pending authority established that the exact L13H8 opener edit requires the bilinear interaction `(p_d-p_r)(u_d-u_r)`. Its six fixed full-term displacement vectors preserved direction but over-amplified on that construction. This test asks whether the payload difference itself has a smaller reusable representation whose amplitude is supplied by the live score.

The fourth construction is frozen before model access in `BRACKET_TRIPLE_PENDING_OOD_V1_ROWS.json`: a brace opens an outer record, one of the three established delimiter types opens a middle note, and a second established type opens the unfinished inner span. There are 36 answer-changing inner-opener rows and 36 answer-preserving middle-opener controls over twelve fresh lexical groups. Both directions give 12 endpoints in each of six ordered closer-pair cells.

Derive a program from target endpoints of the already opened third construction using only the projected L13H8 opener payload `u`, never logits or causal outcomes. Average `u` by the active delimiter type, center the three type prototypes, and take their two-dimensional SVD span. Centering makes this type table rank at most two; no rank selection or sweep occurs. On a fourth-construction endpoint, let `r` and `d` be its recipient and paired donor. The compressed replacement is

`t_hat = p_d * (u_r + (c_type(d) - c_type(r)) @ V_2)`.

Thus the recipient's native payload context is retained, the delimiter payload difference comes from the third-construction rank-two program, and the live donor score supplies interaction magnitude. The exact ceiling is `t_joint = p_d*u_d`; the score-only baseline is `p_d*u_r`. Controls use the same equation, whose type difference is zero because their inner answer is fixed. No full-term vector, outcome fit, scalar gain, threshold tuning, gradient, parameter update, or quantization is allowed.

Price is five forwards over the combined 288 third- and fourth-construction endpoints: native, exact factor replay, exact joint, rank-two compressed joint, and score-only. This is 1,440 sequence evaluations, one outcome-blind three-row SVD, zero backwards or updates.

Frozen bars:

- **A — exact instrument and capability:** both manifests and bindings match; replay maximum final-query logit error is at most `1e-5`; observed price is exact; centered prototype rank is at most two; every target and control native capability cell has accuracy at least `0.75` and positive mean closer margin.
- **B — exact parent remains live:** exact joint effects are donorward on at least `0.90` of endpoints in every ordered target pair.
- **C — rank-two payload source transfer:** predicted versus actual fourth-construction payload differences have cosine at least `0.90` and relative L2 error at most `0.50`.
- **D — downstream compressed transfer:** compressed versus exact joint effects have overall cosine at least `0.95`, relative L2 error at most `0.30`, sign agreement at least `0.90`, and norm ratio in `[0.70, 1.30]`. Each ordered-pair cell has cosine at least `0.85`, relative L2 error at most `0.40`, and sign agreement at least `0.90`.
- **E — interaction improvement:** compressed relative L2 error is at least `0.20` lower than the score-only baseline's error.
- **F — selectivity:** rank-two-program control RMS is at most `0.50` of exact target RMS.

All predicates passing yields `payload_rank2_interaction_transfer`. A valid capable/instrumented failure yields `payload_rank2_interaction_null` and closes this representation without rank growth, refitting, or gain rescue. Failed capability yields `fourth_construction_capability_null`; failed replay or accounting is invalid.
