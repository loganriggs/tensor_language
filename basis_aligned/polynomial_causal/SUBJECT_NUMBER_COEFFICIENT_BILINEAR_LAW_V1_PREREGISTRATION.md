# Subject-number coefficient bilinear law V1

The held subject-number circuit uses one frozen 1,152-dimensional L11H3 write axis and ten direction-by-background-cardinality coefficients. Before reading any new causal outcome, replace that ten-entry table by the four-parameter law

`alpha(d,c) = beta0 + beta_d*d + beta_c*c + beta_dc*d*c`,

where `d=+1` for plural-to-singular, `d=-1` for singular-to-plural, and `c` is the number of active E/A/U/W background factors. Fit the four coefficients by ordinary least squares to the ten weights-only rank-one coefficients. Freeze the axis and all four law coefficients before intervention.

On the already licensed 32-row fresh authority, evaluate all 16 background subsets with four arms: base, native exact, frozen rank one, and the bilinear coefficient law. The law passes if its causal effects reproduce rank one with cosine at least `.995`, relative L2 error at most `.15`, and sign agreement at least `.95`; reproduce native exact with cosine at least `.75`, relative L2 error at most `.75`, and sign agreement at least `.75`; and independently clear cosine `.70`, relative L2 `.85`, and sign `.70` on intermediate cardinalities and cosine `.65`, relative L2 `.90`, and sign `.65` in each fresh template. Instrument closure must remain at most `5e-5`.

This screen reduces the stored coefficient table from ten scalars to four and the charged total interface from 3,466 to 3,460 scalars. A pass establishes a bilinear symbolic generator for the coefficient. It does not yet establish that the model's hidden state itself computes `d` and `c`. No causal outcome may fit a coefficient, gain, intercept, axis, rank, row, subgroup, or threshold. No gradients, parameter updates, or quantization. Price is nine physical forwards, 2,144 sequence evaluations, and 2,048 causal installations.
