# Hourly strategic review — 13 September 2026, 11:25 UTC

Circuit interpretation targets remain: explicit read/compute/write specification; cross-boundary grouping and within-module splitting; held-out/OOD prediction; extraction with a declared interface; selective manipulation; composition/reuse; stable identification. The full goal also requires a simpler executable program. Local compression alone does not establish these properties.

## What changed since 10:25

The three-term MLP10 interaction passes fresh template prompts and its own-component removal check. A signed-strength grid supports conditional reuse; a simple rescaling of the unit interaction fails. These results retain native context and suffix dependence.

Upstream protected low-rank and sparse maps fail live response preservation. Keep exact upstream maps. Two-writer algebra generalizes 28 symmetric products to 19, with a direct compiler and local storage gain. The original writer provenance was corrected: head8.2 and head9.8 were transplanted to a common input in that algebra test, not composed along their original paths. Stable orthogonal coordinates fix catastrophic FP32 cancellation in nearly redundant writer tests.

On original paths, the two raw writer directions fail to capture MLP8 transport. Its exact three-direction rational response replays the native cache. Dropping its quadratic direction works at the original removal amplitude but fails signed reuse. This is evidence to preserve the transport terms, not absent compositional structure.

Exact sharing of one 64-dimensional key read across both QK factors reduces the existing compiled response program's stored scalars by 19.96%. One local CPU timing gives 1.104×. This removes redundancy introduced by earlier compilation; native generators and suffix are outside the price.

Further ordinary or balanced linear-subspace compression fails. Fitting the inside-key product with independent query ports barely changes the result. Folding both actual query maps and rounded relative positions into a shared-query product objective makes a substantial difference: ten starts converge in 26.4 CPU seconds, squared coefficient residual drops 13.42%, and rank48 response error falls from 20–23% to 7.17–7.56% at unchanged capacity. This is the hour's clearest evidence for the user's composed-object direction.

The immediate signed-strength countercheck narrows that positive result: positive multipliers 0.5, 1 and 2 pass the 10% all-family scalar bar; -1 and -2 fail, reaching 15.37% and 10.45%. The largest relative miss has reference norm 0.326 and error norm about 0.050. Its reference is smaller than the positive-strength norm, but this is not a vanishing numerical residual. The original unit-strength pass and signed-strength failure both remain recorded.

## Decision and risks

Keep the exact shared64 executor as reference. The rank48 query-folded candidate is a promising restricted-domain screen, not an adopted reusable circuit. Stop additional rank/initialization sweeps on this same objective: all starts already converge and agree in value. The next scientific question is whether the omitted outside/cross interactions or query/key normalization account for signed-transfer failure. Address the actual composed equation before spending on broader fitting or fresh native suffix runs.

Confounds: the 72 examples are reused validation, not fresh evidence; rank64 transport is shared by candidate/reference; fitting independent query ports was a scope mismatch; individual-map coefficient error need not track composed effect; exact compiler equivalence is not native task preservation; all costs must include adapters and context generators. No new four-property semantic circuit was identified this hour.

## Workflow and operational state

Both managed runners are RUNNING at this review. The latest small fits ran on CPU and are terminal, not queued GPU work. Scientific receipts arrived within minutes, but several successive tiny controls generated repeated scripts and publication edits. Consolidate into the existing primary note and reuse the executor; avoid another framework or another cosmetic fit.

[Recorded phase audit](HOURLY_PHASE_AUDIT_2026-09-13_1125.json) covers 70.38% of the hour: science 516.8s, implementation 256.2s, validation 1298.3s, publication 462.5s, review approximately zero; 1066.2s unrecorded. Some validation labels contain scientific experiment execution, so these are not defensible exclusive ceremony/experiment categories. Do not backfill them.

CIRCUIT_FOCUS: PASS under the user's explicit interaction-compression directive. Improvements concern composed effects, conditional reuse and charged implementations; whole-goal completion remains unproven.

CEREMONY_BUDGET: FAIL TO VERIFY. Missing and merged phase labels prevent a clean budget comparison. Repair prospectively: mark scientific execution as science, implementation separately, validation only for instrument checks, publication before writing. Reduce repeated wrappers and notes; no new tracking machinery.

NOVELTY_LESSON_GATE: PASS. Checked original producer package and existing MLP8 transport/contracted-QK work; corrected provenance, reused cached ports, applied signed-edit and own-response checks, and tested a genuinely different shared-query objective.

Next hourly review: 12:25 UTC. Separate mathematical review remains due at 11:29 UTC. Its highest-value target is the complete even-key interaction with shared query source, position and normalization made explicit.
