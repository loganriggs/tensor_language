# Subject-number rank-one fresh confirmation native capability V1

Freeze 32 rows on two new nine-token constructions (`behind_beside`, `among_behind`) and 16 new one-token singular/plural noun pairs. The authority must have no prompt, token-sequence, or noun overlap with the earlier prospective cardinality-transfer authority. Before any causal run, test native next-token agreement at the subject position for recipient, opposite-number same-lemma, and same-number different-lemma endpoints.

All twelve direction × template × endpoint-role cells contain eight examples and must independently attain at least .75 answer-over-foil accuracy. One batched native forward evaluates 96 endpoints. A pass issues a candidate-scoped license for the already frozen rank-one direction-cardinality program; a failure seals causal confirmation. No rows, cells, threshold, rank, axis, scalar, gain, reader, or quantization may change after outcomes.
