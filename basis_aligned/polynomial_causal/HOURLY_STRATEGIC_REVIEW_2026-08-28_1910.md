# Hourly strategic review — 2026-08-28 19:10 UTC

## Outcome

The new complete bottom-up depth curve localizes the composition failure much more
sharply than the preceding stream summaries. On the discovery `skip7000` slice, the
sequentially calibrated program recovers 64.8% of the live-to-all-substituted gap when
only block 0 is compiled, but 25.9% when block 1 is also compiled. It reaches 17.3% at
block 2 and then saturates near 11--12% through block 7. The block-0 to block-1
transition contributes 38.9 percentage points, or 73.3% of the total fall through
block 7. The same shape appears on `skip11000` and `skip1200`.

This means the early failure is not primarily a gradual accumulation over many deep
layers. It is concentrated at the first composed block-0 -> block-1 interface, with a
smaller block-1 -> block-2 continuation and a flat tail. This raises the already
planned MLP0/MLP1 paired finite-response experiment above another downstream summary
or global gain fit.

The highest-priority safe CPU action was executed: a pure, typed call plan and receipt
boundary now fixes the 22 response arms (`LL`, `LT`, and 20 indexed null transports),
the shared exact-teacher triplet, every perturbation sign, row identity, intervention
unit, expected physical call pattern, and arm-bound reduction receipt. Its exact full-run ledger
is 144 teacher forwards and 3,168 student forwards. Six focused adversarial tests
pass. This does not open final data or produce a scientific outcome; the observed
backend still has to execute the plan atomically and compute the reductions.

## Honest explanation ledgers

These remain separate because they answer different questions:

| Denominator | Current value | Meaning |
|---|---:|---|
| Modules with a tested structural surrogate | 36/36 | Inventory coverage, not semantic explanation |
| Whole-program storage certified removable | 5.3481% | Strict executable compression credit |
| Older human-readable behavior coverage | 32.1% +/- 6.4% | Named behavior under its older ledger |
| Strict named causal CE headroom recovered | 10.923% | Leaves 4.72714 nats unnamed |
| Current compiler's +0.8976-nat gap newly recovered | 0% | No current composed package admitted |
| Final coupled early-MLP actions evaluated | 0/68 | Final role remains unopened |

The discovery depth curve changes localization and priority, not any of these
currencies.

## Largest remaining gaps

1. **The block-0 -> block-1 consumer interface.** Local MLP0 and MLP1 descriptions do
   not yet preserve the response that block 1 expects from block 0. The depth curve
   says this single interface dominates the early composition collapse.
2. **Unexplained composed CE and MLP2 compensation.** MLP2 can change from harmful to
   useful after upstream replacements. No result yet distinguishes genuine upstream
   sufficiency from downstream compensation in the current currency.
3. **Causal edits and selective removal.** No complete simple program has yet matched
   native code, centered-logit, and output-KL responses while limiting collateral
   changes.
4. **Semantic organization of the rank-64 MLP0 code.** The subspace is executable and
   compact, but stable overlapping lexical atoms or a hierarchy have not been earned.
5. **Real OOD and executable price.** Alternate FineWeb skips are replication slices,
   not a broad shift; decoder bytes, routing indices, product counts, and runtime must
   all be charged before a sparse program is called simpler.

## Candidate actions considered and pruning

| Candidate | Information and causal value | Composition/falsifier | Cost and redundancy | Decision |
|---|---|---|---|---|
| Atomic 22-arm paired-response transaction | Directly tests the newly localized block-0 -> block-1 interface | LL vs LT vs 20 false transports; exact teacher; sign and call-ledger failures | Moderate GPU; unique evidence | Retain, priority 1 |
| Exact bilinear response/Jacobian factorization at block 1 | Uses the polynomial tensor to identify which MLP1 products distinguish successful from failed MLP0 writes | Must predict unseen finite responses and improve a matched executable arm | CPU analysis after response tensors; not another local MSE | Retain, priority 2 |
| Convergence-certified sparse weight-action discriminator | Separates sparse-dictionary capacity from encoder optimization; licenses OMP/LISTA or hierarchy | Oracle IHT gap, seeds, held-out curves, CE | GPU running; local unless joined to response test | Retain, priority 3 |
| Complete all 68 observations, 18 norms, frequency bins, CE/KL, and N/E MLP2 cross | Measures residual CE, rare-token failure, and compensation on common support | Full lattice and no-free-rider comparisons | Moderate; required for admission | Retain, priority 4 |
| Rank-64-bond double-sparse/joint DAG program | Could give semantic atoms at low decoder cost with an actually executed parent edge | Six-arm independent/joint/deranged screen plus finite responses | Higher optimization risk; depends on priorities 1 and 3 | Retain, priority 5 |
| Another norm, direction, moment, or routing statistic | Low after five closures | Cannot distinguish B0 from B3/B5 at useful scale | Redundant | Prune |
| Norm minimization before folded HOSVD | Exact gauge canonicalization only | Folded tensor and HOSVD spectra are invariant to scalar balancing | Already closed by proof toy | Prune as compression route |
| Standalone dense CP/Tucker/HOSVD of MLP1/2 | Structural but mode spectra are dense/full-rank | No causal or compositional certificate | Repeats completed negative evidence | Prune |
| CE-only end-to-end refit | May hide interface errors through compensation | Local faithfulness and OOD need not survive | Expensive and weakly diagnostic | Defer as matched functional control only |

## Ranked top five

1. **Wire and execute the atomic 22-arm paired-response transaction.** The depth curve
   now independently localizes the dominant failure to exactly this interface. It has
   the highest information gain for prediction, extraction, and removal.
2. **Factor the observed block-1 response operator in native bilinear coordinates.**
   If LT beats its nulls, identify the minimal tensor products responsible; if it
   fails, determine which missing state variable is required. Either branch is
   falsifiable on held-out edits.
3. **Finish the running sparse weight-action optimizer/oracle discriminator.** A large
   oracle gap licenses alternating dictionary learning plus LISTA; a small gap rejects
   optimizer blame and moves directly to joint/hierarchical structure.
4. **Complete the 68-action observational and MLP2 compensation lattice.** This is the
   first same-support answer to whether independently useful MLP simplifications
   compose and whether exact MLP2 merely repairs them.
5. **Promote only a passing joint program to double-sparse/hierarchical compression,
   OOD, selective-removal, and MDL/runtime tests.** Semantic atoms and certified
   simplicity are downstream of causal interface preservation, not substitutes for it.

## Highest-priority action executed

Added:

- `early_mlp_suffix_transport_v1_response_plan.py`
- `test_early_mlp_suffix_transport_v1_response_plan.py`

The plan contains exactly 69 forwards per four-row batch: one shared exact-teacher
baseline/positive/negative triplet and 22 distinct student triplets. It derives the
student call pattern from each canonical semantic action. The receipt layer rejects:

- swapping `LL`, `LT`, or indexed nulls;
- swapping positive and negative edits;
- changing batch rows or intervention units;
- substituting a physical call pattern;
- missing, duplicated, or reordered forwards;
- incomplete or reordered response-reduction labels.

Focused tests pass 6/6. Existing final-action/capability tests were also exercised
without a new failure. The exact remaining implementation boundary is the observed
atomic backend that consumes this plan, holds raw teacher/student tensors internally,
emits only row-level sufficient statistics, and closes the 144/3,168 call ledger.

The GPU is now running the corrected MLP0 sparse weight-action discriminator behind
the completed depth curve, so the response-plan work used the CPU interval without
duplicating that job.

## 19:12 addendum — sparse optimizer discriminator completed

The queued Stage-0 sparse weight-action run completed in 170.8 seconds on 128 fit and
64 untouched evaluation documents, three seeds, 2,400 steps, and held-out curves.
Its interface audit passes: captured `Down(g)` matches float32 `Wg` to relative RMS
`6.2e-7`; `Down` is bias-free; the external `Down_bias` remains explicit.

| arm | held-out output R2 | seed std | standalone MLP0 CE recovery | oracle IHT gain |
|---|---:|---:|---:|---:|
| positive ReLU TopK | 0.7243 | 0.0010 | 0.9807 | 0.0139 |
| signed normalized TopK | 0.7386 | 0.0002 | 0.9828 | 0.0094 |
| signed + covariance noise | 0.7386 | 0.0001 | 0.9829 | 0.0094 |

All arms converged: the winning curves moved under `8e-5` across their last three
checkpoints and had zero final-to-best gap at reported precision. The preregistered
optimizer-bottleneck criterion fails because oracle IHT gains only 0.009, not 0.05.
The noise criterion also fails. Therefore:

- do not spend the next GPU budget on MOD/K-SVD or longer SAE training;
- prune noise as an unearned complication;
- retain signed normalized TopK as the simpler matched sparse compressor;
- do not interpret atoms as canonical: mean atom match is only about 0.52, although
  the rank-64 decoder-subspace overlap is much stronger at about 0.83;
- the coexistence of 98.3% standalone CE recovery with the newly localized block-1
  composition collapse is direct evidence that standalone CE allows compensation and
  is not a consumer-interface certificate.

This completes the former priority 3 and further raises the paired block-1 response
transaction. The result is optimization- and seed-stable on this held-out split, but
it has not yet passed document-doubling, real OOD, joint composition, or editing.
