# Hourly strategic review — 2026-08-29 16:30 UTC

## Outcome first

The highest-priority action produced a new numerical prerequisite: MLP2 FIT_MEAN is
complete receipt-last on 192 fresh documents and 30,801 eligible positions. The
original draft was made about six times cheaper in transformer depth by stopping
immediately after the exact MLP2 product capture. The successful run then took 10.54
seconds. A direct-script import failure was preserved before authority or model
access, repaired with an isolated-directory regression test, and not hidden.

This is not yet a causal simplification. It freezes means and LOCAL/RMS/MASS/random
supports for the next trajectory-complete selector. Strict whole-model ledgers do not
move.

The special eight-hour window ended at 12:00 UTC and remains closed. Its literal
balance is still six measured negative cells, three scientifically pruned cells, and
E4.1--E4.3 open. Family F remains a receipt-complete negative at its registered local
NRMSE gate; no plan, cache, or unrun runner is counted as an outcome.

## What fraction is explained

| Currency | Current value | Meaning |
|---|---:|---|
| Executable/interceptable structural sites | `36/36` | Complete instrumentation, not semantic understanding |
| Consequence-certified whole-program storage removal | `5.348245316%` | Strict certified simplification |
| Named causal CE | `0.57968 / 5.30682 = 10.923302467%` | Strict named causal effects |
| Unexplained causal CE | `4.72714 nat = 89.076697533%` | Largest quantitative gap |
| Fully passed terminal actions | `0/68` | No complete extraction/removal/OOD action |

The independent predictive compiler remains a different currency. Its current knee
removes about 51% of the compiler's full-rank build for 0.007--0.011 nat, but its
tables/maps are not named causes. Its latest fixed-point check failed separately with
a preserved missing-arm `KeyError`; that does not affect the MLP2 receipt.

## New MLP2 evidence

The candidate keeps 512 of 4,608 native products and folds every omitted mean into
one ordinary bias. Its fixed price is 1,770,624 scalar values, 11.1175% of native
MLP2's grammar. It only earns the corresponding 88.8825% local MLP2 reduction if
finite behavioral gates pass.

LOCAL and RMS supports are nearly identical (Jaccard `0.90335`) because means account
for only `3.163%` of summed RMS singleton score. Weight-only MASS overlaps LOCAL/RMS
by only `0.34`. LOCAL512 captures `21.56%` of summed LOCAL score versus `10.95%` for
random: useful concentration, but not evidence of an ultra-sparse local mechanism.
Every score is exactly invariant to reciprocal product-channel gauge changes.

The consequential missing interface is still downstream response. Local singleton
scores cannot see correlated deletion, RMSNorm, later attention, compensation, or
final logits. The registered SUFFIX response matrix and finite candidate evaluation
are therefore not optional refinements; they are the scientific test.

## Largest remaining gaps

1. MLP0--MLP2 compensation dominates the early-stack balance sheet, but no small
   MLP2 executable has survived finite native and C512 trajectories.
2. The four-head copy bundle is large but collateral and 4.19 times its singleton
   sum; the fresh full 16-subset interaction cube remains unexecuted.
3. Independently promising pieces have not been crossed in a shared factorial
   composition telescope.
4. No candidate has passed extraction, selective removal, transplant, or OOD gates.
5. Native coordinates are gauge-correct but may still be the wrong sparse basis;
   response-conditioned/balanced coordinates remain the alternate entry point.

## Pruning and top five

1. **Finish MLP2 SUFFIX512 and finite CMR.** Highest information gain against the
   largest interacting gap; one site, one budget, exact failure gate.
2. **Execute the fresh 16-subset copy interaction cube.** Ten missing subset arms can
   identify whether a pair/triple explains the nonadditivity, or prune head subsets.
3. **Run the MLP3 K512 native-Down behavioral port.** It separates native output
   semantics from Family F's locally better but behaviorally worse refitted decoder.
4. **Build the common MLP0/MLP2/attention composition telescope.** Required for
   whole-model composability, but depends on a valid MLP2 program.
5. **Move to response-conditioned factors or empirical balanced coordinates if
   native MLP2 units fail.** More assumption-heavy, so it follows the exact native
   falsification.

More MLP1 native-gate screens, unconditional copy mean replacements, local Down
refits, raw HOSVD/PCA, and compiler micro-sweeps remain pruned as the main lane because
they duplicate negatives or optimize a currency that does not establish finite
causal utility.

## Exact next action

Source-close the MLP2 SUFFIX response collector. It must use only `FIT_SELECTOR`, sum
each retained/deleted product's response over all token positions, apply the frozen
context-balanced rank-256 ridge-leverage rule, keep targets and model outcomes out of
fit artifacts, and publish a receipt before `VALIDATION` can open. Then compile each
equal-price 512-product candidate physically—512 Left rows, 512 Right rows, 512 Down
columns, and the folded bias—with no masked native MLP2 call.

