# Hourly circuit review —13September2026,00:12UTC

Targets: explicit readings/operation/write/consumer; cross-boundary grouping and within-module splitting; held-out/OOD prediction; extraction with declared background; selective manipulation; composition/reuse; stable identification. The full smaller predictive, composable and manipulable model remains unfinished. The corrected handoff/pilot governs, not the stale better_math_ideas goal phrase.

## What changed since23:12

Four weight-derived MLP8 modes support city-value donation12.32%,24/24directions, with all64 tested newline controls passing(maxabsoluteCEchange.02533nats). A newcontext confirmation thenfailed: nearquote donation−4.10%,2/24directions despite native12/12positive. Fullquadratic donation stillreverses(−2.35%), while broaderhead9removal35.90%remainspositive. This is a real context limitation, not just rank truncation. Nearquote branch-relative approximation error51.97% also prevents calling native-normalized1.72% uniformly accurate.

Exact provenance and physical source masking split city-token donation (positive96/96) from later-token donation (nearquote opposite24/24). Their physical writes add exactly; final-logit effects compose within1.41–2.68%. Finaltarget-only donation loses22–45% of theeffect, so all-position writes remain part of the interface. Token-only cityphi generation fails magnitude; even an oracle context-independent pairlookup incurs24.67% freshcontrast error. This is an in-sample limitation certificate, not a deployed datafit.

The separate mathematical review at23:28 proved that four rawlinear readings cannot globally replace the RMS-normalized quadratic without a normport. A fiveport executor replays nativephi to1.33e-7; old signed-square pairing gives three realproducts for the numerator, not three semanticcircuits. No autonomous extraction shortcut.

Folding the four MLP8 eigenreaders into MLP7 gives four exactquadratics andsix B/Q7/H8 interaction paths. Nativephi replays1.03e-6; droppingmixedterms misses58–71% cityvaluecontrast. Q-containing/Q-free compositevalue donations pass physical and localcomposition tests(.41–2.53%effecterror). But computational8corner audit finds nearpost changinginformation mostly inS=B+H: Q-only misses93.75%, S-only18.16%. B/H splits about41/59% there. A factor's occurrence in an expression doesnot tell us whichinput supplies changinginformation.

## Decision and alternatives

Continue the cross-boundary path approach, but test rawinput ports before extending a module-levelstory. Nextphysical intervention holds recipient normalizers andotherparents fixed whiledonating Q, B, H orB+H readings into the Q-containing path. A failedS-onlyprediction would distinguish finalposition computationalallocation fromfullnativeeffect. Opposingprediction: Q-only explains mostphysical reversal despite its weakfinalwrite reconstruction.

Then choose basedonthat receipt: ifattention8input drives theeffect, fold its fourreadings intohead-specific OV/QK computations usingexistinghead machinery; if residualB isindispensable, traceits earliergenerators. Do notinfer that a smallerphi/core/readerfit has closed an inputdependency. New independent text and control families are stillneeded beforepromotion; current source/parent tests are diagnostics on reusedcontrolled contexts. A direct broadergeneralization test outranks another hyperparameter sweep.

Generic CP/SDC/sparseTucker searches staylowerpriority until a precise missingpath requires them. The user’s normalizedFrobenius guessedgraph search remainsdeferred, not an implementationcampaign. The current construction supplies algebraicstructure without a newoptimizer.

## Confounds and price

Controlled sentence shifts are not corpusOOD. Citymask positions are external semantic annotations. All nativeprefixes, denominators, background andsuffix remaincharged. Cache-backed pathdonation supplies nativeparent values; it is not Q-only nodeinterchange or wholeMLP7 removal. Exactwrite addition isnot exactlogit addition. Shapley allocations average off-manifold hybrid evaluations and are computational receipts, not unique causalresponsibility. Do notpromote positive means when individualdirections fail; preserve nearquote Q-free later16/24.

Fourreader foldedprogram23,050scalars also needs10,616,832nativeL7/R7weights pluscontextgeneration. No smaller-model adoption. The stablecityedge is small(1.46–4.68% transfer) and useful for specification, not a substitute for explaining thefullregional contrast or model.

## Throughput and workflow

Seven nativejobs completed since23:12:2,632fullforwards,42.23seconds in recordedin-job timers. Peer673 occupied23:46:56–23:51:04; provenance queuedwhile CPUanalysis wasprepared. Claims/preregs toreceipts generally span a fewminutes; theprovenance delay includes thatpeer wait. CPUalgebra, negative-result discriminators andparentallocation advanced circuitquestions. No nativeexecution failure or repeated completedrun inthishour. A rowbuilder encountered non-flat historicaltokenarrays andwasfixedbefore scoring.

CIRCUIT_FOCUS: PASS — freshconfirmation, failurelocalization, selective source/path intervention, localcomposition, declaredinput generation.
CEREMONY_BUDGET: PASS for boundedsharedpreflight — existingexecutor/hooks, hashgate, fixedrows andreplay reused; no newauditframework. Phase logging stillmisses implementation transitions, so numeric labor shares are not defensible. Repair prospectively; do notbackfill inventedminutes. Authoring/interpretation, not these GPUjobs, remain the serialbottleneck.
NOVELTY_LESSON_GATE: PASS — priorMLP8directionalresponse andMLP7p768screens checked; newfourreader upstreamfold is distinct. Negatives received executed fullquadratic, endpoint/city anderrornormalization checks. Compositepath evidence wasnot silentlypromoted to inputsignal attribution.

Next hourlyreview01:12UTC. Next mathematical review02:28UTC from23:28. Immediately preregister and execute the physical input-port discriminator.
