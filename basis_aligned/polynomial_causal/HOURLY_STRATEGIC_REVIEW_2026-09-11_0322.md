# Hourly strategic review — 11 September 03:22 UTC

Circuit targets: explicit reads, computation, writes and downstream use;
grouping across modules and splitting within them; held-out/OOD prediction;
executable extraction; selective manipulation; composition/reuse; and stable
identification. The full goal remains a simpler predictive, manipulable and
composable program. No full circuit adoption was established this hour.

The user's weights-first direction continues to govern discovery. FineWeb is
in-distribution validation; Pile is separately OOD. Source features are now
frozen before a modest FineWeb validation. There is no data-guided refitting or
million-token expansion. Generic eval_tokens_large was traced to Pile and is
excluded; dedicated FineWeb row-cache provenance was checked in both generators.

Since02:22, the joint shared-reader/rank16 partner fit converged from all four
starts to the same function, with3.83%gain missing5%. Its source pullback showed
weak QK/value-space alignment. Executed stream and source-selection red-teams
changed that interpretation: directly selected routing source spaces touch59.6%
of the numerator at the two original distances. Those components remain open,
with most energy in mixed interactions. Source-normalizer omission then produced
10.8–33.3%formal routing-effect error. An exact baseline-port intervention program
now includes the normalizers and values, finite edit order and basis invariance.

One source frame per head across511distances is extremely stable between odd/even
splits (subspace overlap>0.9998), but all-head coverage/retention targets failed.
Validation mean touch41.64% is mostly mixed; discovery-side rank17 bounds constrain
some stronger targets. The fixed adapter costs24,531additional numbers/head and
reuses native head vectors; this is not a full-model saving. FineWeb validation
will compare physical key/value source edits with conditional predictions plus
native downstream replay, on32historically opened rows and one fixed endpoint.

Why this route survives: it now provides a stable input variable and an actual
local intervention interface. Another spectrum alone would add little. The
next test must leave purely formal inputs and establish that the intended
internal edit and conditional forecast agree on native natural states. This is
validation of frozen structure, not using data to choose the source space.

Confounds: numerical cancellation in a small difference between large attention
outputs; source input RMS versus head key RMS; source versus query read edges;
base-value mixing; exact rounded RoPE conventions; native downstream computation
still retained; historically reused data; and post-hoc interpretation of learned
versus random CE changes. These are addressed by zero hooks, explicit untouched
query/base/source-position checks, a fixed random frame control, and saved ports.
CE change is descriptive damage, not semantic selectivity or a discovery objective.

Alternatives: true-touch optimization could improve influence-selected frames;
joint downstream/upstream objectives may identify different variables; and
overlapping output-group/product models remain unfinished. A new rank sweep or
more corpus fitting is lower priority than testing the frozen executable object.
If FineWeb prediction fails, inspect the saved numerical and hook evidence before
changing the structural model. If it passes, semantic and selective tests remain
necessary; a conditional exact formula alone cannot promote a circuit.

Measured GPU science: joint reader181.46s and shared-position spectrum3.26s;
the subsequent native-weight CPU audits each took under one second. Serial
authoring and documentation dominate these small exact calculations. Reuse the
existing predictor, loader and row cache; do not build another framework.

CIRCUIT_FOCUS: PASS under the user's weights-first directive. Stable variables,
conditional intervention and position reuse advanced; no semantic promotion.

CEREMONY_BUDGET: NEEDS REDUCTION. Most current tools are small algebraic routines,
but bespoke runners/reports take much longer than execution. The next test reuses
the existing fixed adapter and one six-arm executor. No additional safety suite.

NOVELTY_LESSON_GATE: PASS. Existing source, normalizer, secant and module dossiers
were checked; no renamed discovery of the old calibration or17.2/17.3 behavior.

Next hourly review04:22 UTC; mathematical review remains04:49 UTC.


Consequence completed03:38: managed frozen FineWeb validation passed numerical
clauses in1.85s. Tiny effect prompted executed no-change audit; absoluteCEbar weak,
relativeeffect prediction informative. Reused vector predictor to implement source
summation; CPU direct/composition controls pass. No datarefit or semantic promotion.
Report explanation0338. Further bespoke runner authoring should be minimized.
