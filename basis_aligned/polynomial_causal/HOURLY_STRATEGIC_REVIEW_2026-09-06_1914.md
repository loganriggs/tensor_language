# Hourly strategic circuit review — 2026-09-06 19:14 UTC

## Circuit interpretation targets and full goal

The target remains a smaller transparent tensor program that is simultaneously predictive
on fresh/OOD text, composable across installed replacements, selectively manipulable, and
literally simpler in storage, compute, edges, states, and program operations. A circuit must
specify reads, operations, writes, and downstream consumers; group/split across native module
boundaries; predict held-out effects; support extraction; survive removals/swaps; compose and
reuse; and have stable operational identification. Compression or low rank alone is not a
circuit result.

## What changed since 18:14

- Regularized constrained DAS did not repair fresh transfer: noise split panels and KL/aligned
  objectives entered worse basins. Multi-task fitting revealed cue-conditioned rotation and a
  rank-two union, but complement disturbance remained 0.328/0.344.
- Exact weight translation of that union was gauge stable (score replay 6.36e-9) and predicted
  large causal readers L9H1/H4. Live testing found the distributed H1/H4/H7 subject-write core.
- On v4, H1/H4/H7 explain 96-99% of H3 response in both sufficiency and necessity. On the
  earlier Later/Previously bank, A2 necessity was 89.72% against the frozen 90% bar and H0—not
  weight-predicted H3—was the largest small remainder. This miss was preserved.
- Three capability-only banks were invalidated before causal outcomes. A controlled new-
  lexicon/construction v8 bank passed. H0 then fell to 1.45%/1.93%; the original triple retained
  99.21%/100.56% in sufficiency and 99.51%/100.49% in necessity, with <=2.97% complements.
  The stable unit is H1/H4/H7 plus a task-conditioned small remainder, not a universal quad.
- Directly actuating the frozen H3 rank-two union falsified its promotion to a circuit variable.
  Full H3 carries 45.1%/37.0% of live-writer behavior. Rank two retains only 74.7%/71.5% of
  full-H3 behavior and 66.3%/71.2% of the L15H5/H1 response; the orthogonal branch carries
  25-29%. Exact controls passed. Rank two remains a useful probe basis, not an identified state.

## Confound audit

- **Post-selection:** H7 was selected on v4 but survived v8; H0 was selected on the cross-cue
  miss and failed v8, correctly preventing promotion. Any next basis must fit only v1/v2 and
  freeze before v8 scoring.
- **Frame mixing/shared difficulty:** v5-v7 capability failures show that cue and frame changes
  cannot be interpreted jointly. V8 held cue semantics fixed while changing lexicon/frame.
- **Baseline and nonlinear composition:** all reported behavioral fractions use signed recovery
  against the same native base/donor denominator. Norm fractions are not assumed additive;
  they are paired with scored behavior and explicit complement arms.
- **Leakage:** the rank-two actuation used serialized v1/v2 axes without updates on v8. The next
  basis must retain that fit/test separation.
- **Dead knobs/precision:** self-clamps, identity paths, projection closure, attention
  reconstruction, and QR orthonormality were live and exact or <=1.2e-7.
- **Weight overclaim:** static incidence succeeded for the large heads but misranked the small
  remainder. It remains a prioritizer whose predictions require live causal confirmation.

## Throughput audit, 18:14-19:14

Authoritative timestamps show 17 temporal receipts, including honest invalid/null outcomes,
and 24 circuit-relevant commits across the shared lane. Codex's end-to-end causal receipts from
18:43 onward landed at roughly 1.5-3 minute intervals; GPU executions themselves were mostly
2-19 seconds. The largest avoidable bucket was repeated capability-bank authoring from 18:57
to 19:02, not preflight or GPU waiting. The correction was to isolate one surface factor on v8.
Shared runners and wrappers kept validation below scientific design/interpretation time.

`CIRCUIT_FOCUS: PASS` — every Codex receipt changed DAS identification, weight-guided edge
selection, held-out stability, selective manipulation, or downstream subspace actuation.

`CEREMONY_BUDGET: PASS` — preflight was shared and sub-second; exactness controls were warranted
for live head-output surgery and did not dominate the experiments.

`NOVELTY_LESSON_GATE: PASS` — prior cDAS necessity, H3 response, joint-reader, weight-atlas,
claim ledger, and capability failures were searched; no activation reconstruction or rank-only
result was promoted as mechanism.

## Different next objects, ranked

1. **Frozen causal-response basis, fit v1/v2 and test v8.** Form the matrix of actual L8H1-
   induced H3 response vectors (rows across examples/positions), subtract task means only as a
   registered variant, and take a small response SVD basis. Test one frozen rank-4 basis against
   full H3 for behavior, orthogonal complement, and L15H5/H1 transport. This changes extraction,
   manipulation, downstream use, and stable identification. Kill it if rank 4 retains <90% of
   behavior or <85% of L15 response, or if its complement exceeds 15%.
2. **Downstream-weighted/Sobolev basis.** Optimize H3 directions under the Jacobian into L15 and
   logits, but only after response-SVD is tested; otherwise it risks recreating the scalar-readout
   cheat. Kill it if it beats response SVD only in-sample or lacks complement selectivity.
3. **Predictive-state causal quotient.** Define H3 states equivalent when all registered downstream
   interventions agree, potentially yielding a task-conditioned core plus nuisance quotient.
   Higher conceptual value but more machinery; promote if fixed-rank response SVD fails across
   cues in structured, repeatable directions.
4. **More scalar cDAS regularizers.** Demoted: existing noise/KL evidence shows hyperparameter
   variants do not change the underspecified object.

The highest-information route is (1). This is not rank drift: rank is a matched price, while the
registered decision is whether an independently fitted state supports fresh behavioral actuation,
selective removal, and a predicted downstream weight interface.
