# Fresh confirmation of the MLP7-present city-write group

Freeze source semantics before selecting new documents. The group is the sum of
all ordered city-side K1*K2*V terms containing the weighted MLP7 write in at least
one key/current-value slot. It is full city write minus the write with that source
absent from those numerator slots. Both query fields and all RMS denominators stay
at the full native state for the reference. Inherited first-value terms remain.
The complement is exact native full city write minus this group; native rounding
residue belongs to complement. This is not native MLP7 module ablation.

Select next20eligible Pile documents with the existing outcome-blind selector,
excluding all frozen document hashes and exact inputs. Same six fixed spelling
probes and four unrelated readers.40paired sequences/240probe rows. No capability
filter or replacement after outcomes. Freeze seeds and thresholds here. Fresh means
new evaluation documents, not different cities/endpoints or pretraining-disjointness.

Predict the group using the frozen full-attention7 five-reader residual6 generator.
Compute its full and MLP7-absent numerator writes with the same generated full
queries/key denominators and approximate mixed8 RMS, then subtract. Keep all source
cross terms. Extend only weight-derived token tables. No omission from the pending
attention7 simplification screen is used in this confirmation.

CPU native capture stores residual6, native MLP7 output, normalized attention8 input,
mixed8 RMS, actual attention8 queries and full city write. Before installation require
expanded reference full write and generated queries relative error<=1e-4 per sequence;
all outputs finite and supported. No model outcome selects rows or terms.

21installed arms: native, exact full-city removal, reference MLP7-present group,
complement, generated MLP7-present group,16same-site per-position norm-matched random
edits matched to the generated group. Seed18096000+1000*k+context_id, same pairedseed
with opposite cue signs. Native MLP8/suffix recomputed. CPU two threads,600second cap:
840installed sequence-equivalent forwards/378block calls plus40capture/18calls,
880total/396calls. Native query/full write replay checks do not require extra forwards.

Gates:
a.20distinct fresh documents/40sequences/240probes, disjoint hashes/inputs; query and
  full-write preflight<=1e-4; finite/support0; null norm error<=1e-5; required calls;
  CUDA uninitialized.
b. Generated-group target-effect relative L2 error<=.35 versus reference group,
  target RMS>=1e-5 and>=90/120native capable contrasts (UK-US margin>=.1).
c. Each control RMS<=.5generated target RMS.
d. Positive attenuation>=.90 and mean>=.02 among capable contrasts. Earlier opened
  source-group88%positive is preserved under its own registration, not promoted by it.
e. Generated target RMS>=2random median and beats all16nulls.

Report reference-group/full/complement effects, native and generated document signs,
untouched/substituted prediction errors, and full-minus-single interaction normalized
by the smaller source effect. That interaction is diagnostic here; no new independence
or random-split-specificity claim. Prior failed composition stays failed. Price all
five-reader/generator weights and residual6 input; native reference capture is external.
