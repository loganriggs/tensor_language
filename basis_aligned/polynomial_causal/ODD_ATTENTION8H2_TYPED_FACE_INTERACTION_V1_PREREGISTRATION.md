# Typed face interaction audit, V1

Registered before computing this diagnostic. Existing replication panel is opened
for this audit, not fresh confirmation. No parameter or support selection.

Question: did the earlier 35% full-effect reconstruction gate also establish the
stronger composition requirement in better_circuits.md?

Compute the behavioral Mobius interaction I = F5-F1-F4+F0; divide its L2 norm
by min(||F1-F0||, ||F4-F0||), separately for the full replication panel, both
cue directions, and six endpoints. pred_a: saved selected support equals (1,4,5)
and all values finite. pred_b (diagnostic, not promotion): every ratio <=0.35.
Null: one or more ratios exceeds 0.35, requiring retention of the interaction
as an inseparable computation rather than claiming independent composition.
The 0.35 audit threshold is new, not a retroactive registered threshold for the
original experiment. Also report aligned interaction and cosine to full effect.
Random same-write splits are not recoverable from stored output scores alone;
record this required null as missing regardless of pred_b. CPU only; 144 rows,
eight corners, five readouts, no model, no fit, no new sampling.
