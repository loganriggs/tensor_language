# ATTENTION5_DEVIATION_LEADERS_TRANSFER_V1 preregistration

The V2 mean-deviation head split selected heads6,7,3 as the three largest
singleton recoveries on its disjoint natural-document discovery panel. Freeze
that set and the parent fit-set mean head input. Test without refitting on the
first24 documents of the already separate `final_natural.pt` and `ood_code.pt`
caches. Run native, mean-only, leaders, complement, all-nine, and the three
leader-singleton arms on each corpus.

- **A — parent and closure:** parent/artifact/cache hashes hold, matched manual
  and model CE agree within.01, and all-nine restoration matches native CE
  within.005 nat on each corpus.
- **B — live correction:** mean-only damage is at least.05 nat on each corpus.
- **C — leader transfer:** heads6/7/3 recover at least50% of mean-only damage on
  each corpus.
- **D — complement insufficiency:** the other six heads recover at most50% on
  each corpus.
- **E — split stability:** every leader singleton has positive recovery on each
  corpus, and leader recovery exceeds complement recovery in both fixed
  12-document halves of each corpus.

Only an all-pass result licenses routing/value/source factorization of heads
6/7/3 as the stable input-dependent attention5 correction. A failure preserves
the discovery ranking as local and redirects to a corpus-conditional or more
distributed operational quotient. This does not revise the fixed gross-write
result, fit a new direction, select a new head after transfer outcomes, or
install a surrogate.
