# Exact single-token native attention replacement

Install the CPU-verified source-column plus query-row identity at attention11. Recompute all five source projections, preserve native input/head RMS, RoPE and first-value mixing. Query-row correction must use updated source keys/values. Baseline context prepared once; no native attention forward inside edit replacement. All output weights retained; uncached source-column writers and row output projection charged. Additional storage: cloned baseline K1/K2/V in current implementation; simplicity is conditional pair-computation reuse, not parameter compression.

Same six contexts and fixed selectors as freshv4 experiment: two old installed-oracle contexts, four now-openedv4 bank-selected contexts.18prefix48suffix6baseline preparations. No refit or new gradients. Old full-native effects replay<=1e-8; zero-edit<=1e-4. Every output's replacement/full effect discrepancy<=.001 normalized by full number effect, all24cells including old replay. Original fidelity10%number/5%control and selectivity.8/.1 kept. Expected exact correction restores fidelity; selector failures should persist. This is a repair on opened outcomes, not fresh OOD. Independent source-port CPU oracle and live omitted mixed-term control already pass.
