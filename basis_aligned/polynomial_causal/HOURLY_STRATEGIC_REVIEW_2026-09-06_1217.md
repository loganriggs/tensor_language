# Hourly circuit strategy review — 2026-09-06 12:17 UTC

## Circuit interpretation targets

The live decomposition must specify what each circuit reads, computes, writes, and feeds downstream; group operationally shared pieces across native modules while splitting cohabiting variables; predict held-out and out-of-distribution behavior; support an executable sufficiency interface; permit selective removal, swap, and edit tests; explain composition and reuse; and remain stable across populations, gauges, and fitting restarts. Literal storage and compute count only after these circuit claims hold.

The program-level goal remains a smaller transparent tensor program that is jointly predictive on fresh and OOD text, composable under installed replacements, manipulable under registered counterfactuals, and simpler under literal storage, compute, edge, state, and program prices. A screen, a useful null, or low loss is not completion.

## What changed since 11:17

Nine Codex circuit receipts reached terminal interpretation: a routing/content null; a local-versus-carried effective-value screen; a deep-versus-direct L9-input null; one float32-invalid block-8 split plus a zero-forward quarantine audit; is/was and has/had MLP4-to-H1/H4 local-value screens; a zero-forward typed shared-path v2 release; and two prospective will/had tests. A failed has/had run was repaired without changing its preregistration, and the failed block-8 instrument remained invalid rather than being numerically rescued.

The within-head result is now precise. Across has/had and is/was, MLP4 changes reach L9H1/H4 principally through the local L9 value branch, not the carried L0 value branch or routing. The first new-construction prediction on fully capable will/had rows was a formal magnitude null: local V9 still explained 99.1%/99.8% of the H1/H4 effect, but full recovery averaged 0.04992 against the registered 0.05 bar and the A2 writer scored 0.0892 against 0.10. No threshold or prompt was changed.

The immediately following head/complement factorial separated algebraic reuse from causal magnitude. On the same will/had authority, H1/H4 recovered 92.7%–107.1% of full attention 9 in every direction/construction cell. The other seven heads ranged from −3.5% to +2.8%, nonlinear interaction stayed below 1%, and H1/H4 moved both P and C less than the full module. Thus the H1/H4 reader group generalizes, while the previously shared MLP4 contextual input supplies only a small part of its new task effect. The typed graph must retain a shared reader interface with task/construction-specific upstream sources.

Independent receipts also established a matched-slot, disjoint-vocabulary number direction and then separated it from the will/had temporal feature. Those results are relevant controls: cross-task transfer is interpretable at a matched read position, and shared auxiliary syntax alone does not explain feature reuse.

## Throughput and systems audit

Repository and runner timestamps show nine Codex terminal receipts in 57 minutes, plus two independently produced number-control receipts. Typical claim-to-result cycles were 3–7 minutes; managed GPU executions were about 2–5 seconds and there was no GPU wait. The fast loop comfortably exceeded the six-receipt/hour target.

The bottleneck is still repeated authoring and review machinery. Seven recent factorial runners total 1,989 lines, including 382 and 358 lines for the two will/had tests, even though both reused frozen operators and each GPU run took about two seconds. The shared cross-task evaluator and inherited factor functions reduced conceptual duplication, but validation, batching, endpoint scoring, coverage, and publication logic remain copied. Scientific design produced the decisions; repeated implementation and preflight consumed most serial wall time.

`CIRCUIT_FOCUS: PASS` — every bounded block changed causal computation, head grouping/splitting, cross-task reuse, held-out prediction, or the integrity of those receipts.

`CEREMONY_BUDGET: FAIL` — bespoke runner code still dominates basic screens by orders of magnitude over GPU execution. Before another GPU job, extract and test a reusable declarative source-group evaluator, then use it for the next H1/H4 source localization. Do not author another 250–400-line runner around the same capture/score loop.

`NOVELTY_LESSON_GATE: PASS` — prior construction transfers and narrative-tense results were searched; two capability-invalid OOD routes were not retried; the 0.04992 near miss remained a null; and the next construction came from an independently sealed capable authority rather than post-outcome prompt search.

## Confounds, alternatives, and decision

All target effects use row-matched native logit endpoints rather than CE. Full attention-9 evidence is frozen from the parent screen, and new H1/H4/complement arms patch disjoint pre-`c_proj` head slices. P and C are retained. The principal remaining confounds are source-frame mixing between the direct and report constructions, downstream nonlinear interaction when source groups are installed separately, and the possibility that H1/H4 are a generic auxiliary readout rather than a temporal reader. Precision is not the explanation for the will/had null: tensor closure was at most 7.63e-6 while the preregistered causal miss was about 8.2e-5 in mean recovery and a larger 0.0108 writer miss in A2. The exact boundary is narrow but valid.

Ranked next moves:

1. Localize the non-MLP4 source terms entering will/had L9H1/H4 using a complete, disjoint semantic partition: prefix before the temporal cue, cue, two-token subject onset, intervening suffix, and self. This changes computational specification and cross-task input reuse. It is killed if no group explains a material fraction in both constructions or if source reconstruction/coverage fails.
2. Test joint attention-9 plus attention-11 composition. Their individual recoveries are about 0.357 and 0.394 while residual recovery rises from 0.073 at resid9 to 0.867 at resid12. This targets cross-boundary grouping and signed composition; it is killed if the joint effect is small or strongly construction-dependent. It follows source localization because knowing what H1/H4 read makes the composition interpretable.
3. Compare the will/had H1/H4 source partition with the established has/had/is/was carrier partition. This tests stable operational equivalence and is killed by direction- or construction-specific group reversals.
4. A function-space basis or rank fit is rejected as the primary action: it would summarize the response but would not identify which source tokens and native edges implement it.

The H1/H4 route remains highest-information because the prospective head grouping passed while the inherited MLP4 edge missed. That opposition directly isolates the unresolved object: a shared downstream reader with different upstream payload construction. The forced next block is therefore a reusable source-group evaluator plus CPU tests, followed by the preregistered five-group will/had source screen. The next mathematical review remains due around 14:30 UTC; the next hourly strategic review is due around 13:17 UTC.
