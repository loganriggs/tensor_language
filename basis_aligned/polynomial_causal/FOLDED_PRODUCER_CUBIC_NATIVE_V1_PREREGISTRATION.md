pred_a exactcontrols/nativeFD<=1e-4/evaluation<=2sec;
pred_b bothstationary and >=20%capturegain;
pred_c >=4source matches>=.9, >=2cross-layer atoms/arm, cancellation<=2.
Null: the folded producer functions do not share a small stable cubic source dictionary. Different layer states remain separate native inputs; coefficient reuse is not semantic reuse.


Object: 27 producer heads from layers8/9/13, each complete joint QK1*QK2*value numerator folded through its output map, actual residual recurrence coefficient, and the four current-stream source readers of the already frozen regional branch. The previous OV fold used two child readers but did not factor this composed joint numerator. Layer selection came from earlier behavioral producer tests; current factor learning is weights-only conditional on that selection.

Normalize each head with its weight-derived reference gate; retain head labels in the objective, since true gates differ. Fit16 shared cubic source functions and exactly eliminate private query-to-four-output writers. Sharing means the same reader coefficients act at different layer boundaries; it does not assume identical source states or prove causal reuse. Native gates/remainder remain charged.

Two independent starts: native key/value-aligned and random. V2 angle-based descent safeguard; common fixed divisor is max initial capture across starts. Each gets4000updates/300seconds,800seconds global cap. Native FD<=1e-4 and evaluation<=2seconds must pass before fitting. Bboth stationary and gain>=20%. C>=4cross-start source-function matches>=.9, >=2atoms with>=10%energy in each of>=2layers per arm, cancellation<=2. Literal incidence is only a structural screen; report absolute capture separately, including held relative position. Do not equate tiny-head normalized overlap with useful reuse. Full-energy denominator uses1024 independent coefficient probes and reports uncertainty. No absent-structure claim from an unconverged or small-capacity miss.

Exact source-projection and fold/gradient controls required. Next consequence is inspecting stable block/cancellation structure and native producer-edge fidelity, not promoting on capture alone. No corpus or token objective fitting.
