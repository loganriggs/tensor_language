# Hourly strategic review — 11 September 01:22 UTC

Targets: explicit read/compute/write specification; cross-boundary grouping and
within-module splitting; held-out/OOD prediction; extraction; selective edits;
composition/reuse; stable identification. The full goal is a simpler executable
program satisfying the user's four properties, none newly proved this hour.

User weights-first structural-discovery direction overrides the older circuit-only
restriction. FineWeb validation follows frozen discovery; Pile is separately OOD.
Since00:22: native-product OLS, full/centered congruence tests, and overlapping
sparse token-function learning completed. Independent precision/start checks
resolved the incomplete spectrum search. Sparse dictionary converged locally;
fixed-support debias explains part of its capture miss. Strongest functions remain
dense input quadratics, so sparse output usage alone is insufficient. No new
semantic circuit promotion or natural-data fit.

Direction: stop congruence metric variants. Test sparse token usage jointly with
simple input products using exact conditional atom updates. This changes what
input computation can be specified for an overlapping token group; capture is
only a coverage measure. Check group survival and input implementation, not only
reconstruction. Prior MLP17 dossier and existing dictionary/product scripts searched;
previous free products had dense output writers, unlike this combined constraint.

Confounds: coefficient geometry versus reachable states, changed sparsity penalty,
output-rank restrictions, spectral initialization, nonunique/gauge-related units,
common-output channel, and semantically suggestive token lists. One fit does not
establish global optimality or factor stability. Dense atom spectra are a negative
about returned functions, not a jointly input-constrained search.

Throughput: native OLS2.59s; FP64 spectrum479.91s withpartial convergence; mixed
spectrum52.50s; centered34.09s; dictionary95.65s. CPU audits7–8s each. Those are
measured execution times, not complete serial candidate latency. Phase logs lump
some science and controls under implementation; exact safeguard share is unknown.
A new framework is unnecessary: reuse native Gram and sparse-code utilities.

CIRCUIT_FOCUS: PASS under explicit weights-first override; targets grouping and
executable input computation, no promotion.
CEREMONY_BUDGET: NOT ESTABLISHED from mixed phase labels; constrain next preflight
to dense objective, exact atom optimum and monotonicity controls using shared code.
NOVELTY_LESSON_GATE: PASS; no repeat of generic MLP17 low-rank/calibration result.

Next hourly02:22; mathematical review01:49 remains due separately. Exact-atom
CPU implementation is the immediate consequence of this review.
