# Shared final readout as a composition predictor

Selective source edits jointly pass native selectivity8/8, but additive weak-effect prediction only1/8. Test whether final RMS/softcap explains their coupling. Capture final pre-RMS states for baseline and each singleton only. Set basis[h0,hs-h0,ha-h0], contract18token rows to54numerator coefficients and shared squared RMS to6Gram coefficients. Evaluate at(1,1) before observing joint result. Native joint final state is not a predictor input. Exact shared readout uses native epsilon and30tanh, no Taylor approximation or fit.

12prefix16suffix; four contexts, four corners. Previous single and joint effects replay<=1e-8. Three calibration readouts reproduce native values<=1e-4; report roundoff relative to weaker effect, must<=.001. Prediction joint/conditional error normalized by min(single number norms), .1number/.05eachcontrol, every8cells. Null: upstream cross-state dynamics matter and final readout alone does not close composition. Predictor60values/input versus18for additive effects; full native singleton/state generation charged. Opened inputs, new conditional prediction—not extraction of producer states. CPU exact-field and omitted-Gram-cross checks precede native use.
