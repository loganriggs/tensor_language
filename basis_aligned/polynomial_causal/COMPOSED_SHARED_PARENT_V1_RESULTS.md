# Folding exposes a stable shared parent, with broad partners

12 September2026. [Registration](COMPOSED_SHARED_PARENT_V1_PREREGISTRATION.md),
[native weight result](COMPOSED_SHARED_PARENT_V1_RESULT.json),
[parent anatomy](COMPOSED_SHARED_PARENT_V1_ANATOMY.json).

The four-start weight-only run completed at05:02:44 in2.45seconds. Numerical
identities, convergence and concentration/stability predictions A/B/C pass;
compact-partner prediction D fails. No new semantic circuit is identified.

All four starts converge in21–36steps, with normalized tangent gradients
7.3–8.7e-9 and absolute parent cosines above0.9999999999998. Best capture is
**2.7891%** of the complete centered-output paired coefficient energy, compared
with1.0488% in the earlier single-layer search. The new optimum reaches94.82%
of the2.9414%analytic one-parent upper bound. This is strong numerical stability
and useful capacity accounting, not proof of global uniqueness.

The parent is a quadratic function of the normalized MLP16 input. It is reused
by a full linear family of producer-coordinate partners, producing a quartic
output component. Its physical reader and partner maps are saved in
`COMPOSED_SHARED_PARENT_V1_PROGRAM.pt`; the producer metric transform is absorbed
into those maps. Formal input-removal and physical-coordinate identities replay
within3.7e-15. These are execution identities, not behavioral selectivity tests.

Only49.01% of this star component's coefficient energy is retained by its best
fixed-parent rank16partner approximation;315partner directions are required
for90%. The previous single-layer star retained55.26% at rank16 and needed279
for90%. Folding improves how much energy one stable shared parent touches, but
does not simplify this partner spectrum. These are different coefficient norms
before/after composition, not a measured behavioral improvement.

## What does the parent itself compute?

Fold the saved physical reader$a_p$into MLP16:

$$
c=\lambda D_{16}^Ta_p,\qquad
q(x)=\sum_jc_j(l_{16,j}^Tx)(r_{16,j}^Tx)
=x^TQx,
\quad Q=\operatorname{sym}(L_{16}^T\operatorname{diag}(c)R_{16}).
$$

The executed CPU anatomy check finds$\|Q\|_F^2=1$as required by the producer
metric and replays the parent on128cached native boundary vectors to1.29e-15.
Its eigenfactor energy is diffuse: top1/2/8/32/128directions contain
4.72/7.78/16.49/28.86/55.25%. Rank90is461 and rank99is814. Positive eigenvalues
carry63.90% of energy; the isotropic identity component carries3.48%.
This rejects a tiny signed-eigenfactor or purely radial description of this
fixed parent in coefficient norm. It does not exclude simpler arithmetic using
the native product dictionary or restricted natural-input behavior.

## Consequence for the full goal

The result supplies a stable weight-defined shared computation with an explicit
interface. It does not yet supply OOD prediction, behavioral extraction,
selective removal or useful composition with other discovered components.
No morphology, calibration or pronoun label is inferred from weight stability.
Dossier checks remain required before any proposed interpretation.

The next useful discriminator is frozen native validation of the exact star and
its weight-defined compact partner approximation. The coefficient spectrum alone
cannot determine the partner approximation's natural-input error, as earlier
experiments already demonstrated. Reuse the existing native tail scorer and
cached development panel first, disclose its reuse, and require fresh/FineWeb/OOD
confirmation for stronger claims. Do not select a rank or refit a reader from
those outcomes. Consumer-first compilation can fold required readers intoD16,
as the [existing exact compilation](PRODUCER_READER_COMPILE_V1_RESULT.json)
demonstrates; parent/background costs are not automatically free or automatically
equal to a full producer matrix.

## Prior-parent audit: substantial refinement, not an independent discovery

The old shared-parent artifact still exists and its SHA matches the original
receipt. Restricting its centered-reader direction to the same MLP16 producer
path gives quadratic-function cosine0.951969 with the new parent, versus
raw-reader cosine0.934563. These compare the same input function; they do not
equate all uses of that reader in the original model.

The [matched-metric audit](COMPOSED_PARENT_REUSE_V1_RESULT.json) then holds the
old parent fixed and computes its exact optimal unrestricted partners in the
new producer metric. It captures2.64067%, or94.678% of the new2.78910% result.
Their projected full-star functions have cosine0.934228. Thus most of the
apparent before/after concentration gain comes from composing/reweighting
around an existing parent, not finding a wholly different parent direction.
The original old star, with its old partners unchanged and restricted to this
path, has lower cosine0.812855 with the new star; updating partners matters.

Numerical predictions A/B/C all pass, with identities within4.44e-16 and
1.32seconds execution. The old full-output-metric parent gives lower matched
capture2.40867% and function cosine0.818922. No new reader was optimized here.

This should be described as a stable refinement of earlier shared-parent
structure, not an independent circuit count. Nor is it an exact alias: even
after optimal scalar rescaling, cosine0.934228 leaves35.67%relative coefficient
function residual. Given the measured counterfactual sensitivity, behavioral
interchangeability requires testing. These are parent/path comparisons only;
no semantic identity or OOD evidence is added.

## Native interchangeability fails; retain the distinction

The [frozen native comparison](PARENT_INTERCHANGE_NATIVE_V1_RESULT.json) completed
in1.33seconds on the same128developmental endpoints. Numerical/reference replay
A passes, but swaps B, removal C and writes D fail. The full old parent receives
its exact optimal unrestricted partners in the new metric; there is no rank
truncation or data fitting in this test.

| Family | Candidate swap relative RMS | Candidate removal-CE error | Candidate write error |
|---|---:|---:|---:|
|A1|5.07%|0.0255nats|11.45%|
|A2|11.51%|0.0420nats|15.31%|
|Past|15.99%|0.0221nats|8.69%|
|Progressive|30.13%|0.0230nats|10.27%|

All64swaps are live, and sign agreement is at least93.75%per family. Exact new
reference writes replay with zero error; the prior reference effects are also
reused for the numerical check. Overall candidate write error is12.26%.
The original old star with unchanged partners is a secondary baseline:
swap errors14.77/45.25/18.67/27.20%, overall write13.46%. Updating partners
therefore addresses a real coordinate/consumer mismatch but does not make the
old and new components behaviorally interchangeable. Both interfaces keep
full partners, so the failed rank16compression is not the explanation here.

The candidate's conditional price is17,253,504values before further exact
consumer folding, including native producer matrices. No combined graph
deduplication or whole-model saving is implemented. Keep the new result described
as a related refinement, while retaining both functions' distinct intervention
semantics. Do not merge graph nodes based solely on the0.952parent cosine or
94.7%capture preservation. This applies the earlier common/difference lesson:
small-looking discrepancies can matter to counterfactual behavior, and must be
preserved rather than hidden by a changed threshold. No circuit is promoted.
