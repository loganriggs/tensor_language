# Content-path mediation of native wrong-source reads

Opened-case source atlas:21 wrong states across20 entity-renaming disagreement
pairs. An off-chain binding-read cut repairs16,15withgoldP gain>=.25. The strongest
wrong-versus-gold source is off-chain in20. Its raw value equals the wrong answer
in15 cases; its two-step endpoint equals it in6, all with an earlier forward join.
This nominates concrete content paths, not a fresh evaluation or adopted repair.

Freeze all21 primary wrong states and each corresponding paired input:42 state
evaluations, including19 correctly answered peers and2 wrong peers. A pair with
two wrong answers contributes two distinct primary/source tests. Source ordinal
is the atlas's largest wrong-versus-gold message source, not chosen again from
new interventions. Derive field type once from the graph: raw if F(source.key)
is the native wrong answer; join if F²(source.key) is that answer and the needed
source record is earlier. Keep every case; require these checks to succeed.

The raw path is literal E/8 at the selected binding-value position. The joined
path is the existing L2H1 endpoint embedding contribution into the selected
binding's key/value positions. Transmit its removal through final V using the
native final source gain, native attention P and unchanged Q/K/residual. Thus
only the explicit additive content path is removed; this is not a physical
source-state edit with recomputed RMS, a whole-token deletion, or a revived
global raw-field abstraction. The retained background is explicit and charged.

Arms native,whole binding-read cut,nominated content-path cut,opposite content-path
cut,joint raw+join content-path cut,identity. When the opposite joined path does
not exist, report that fact; its zero effect is not evidence for specificity.
Every candidate uses export-derived states. Independent native V projection
hooks subtract the projected path; native pattern-column hooks supply whole cuts.
Match source ordinals and graph roles across the two inputs, never hidden states.

A mechanical: nonempty42-state manifest, graph/type correspondence, finite;
all-output content-path candidate/nativeoracle max<=1e-9 andrelativeRMS<=1e-10
(floor1e-6), identity and source sum controls. Whole-read query cuts also replay.
B nominated content: separately in raw/join primary groups, mean reduction of
wrong-minus-gold logit margin is at least.5 of the mean whole-binding-cut
reduction (denominator must be positive); mean wrong-answer probability loss>=.25;
at least half of primary cases repaired by the whole-binding cut are also repaired
by the nominated path. If a repair denominator is empty, report untested, not pass.
C paired specificity: on the19 correctly answered peers, abs mean goldP change
from nominated content removal<=.10. Report raw/join subgroups separately too;
the primary gate pools all19 as declared here. The2 wrong peers are retained but
cannot serve as correctly answered specificity controls.
D composition: joint raw+join path-cut logit effect equals sum of individual
effects at every output within1e-9. These paths use the same fixed native gain;
overlapping physical-state edits with live RMS do not have this guarantee.

No label map, source, field, scale or head may be selected using these outcomes.
All opposite-field effects and all native errors are reported. A passing screen
would localize content supporting these specific errors; fresh matched control
families, predictive identification and structural simplification remain required.
All387968 native constants plus caches are charged. CPU2threads,180s bound; no GPU
needed for this small opened-case study. Preserve earlier field and symmetry nulls.
