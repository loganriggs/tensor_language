# True common-component interchange across original/fronted contexts

The preceding COMMON_SUFFIX_V1 licenses the common `to`/action interface:
it preserves the partial parent effect within3.94% in all32worlds. Existing
fronted factor/gender failures remain. No fresh text, fitted projector, gain,
head, rank, position or row selection is allowed.

Use all16 original/fronted pairs in THIRD_NOUN_CAUSAL_ROLE_ALIGNMENT_V1.
For each layout recover C, the frozen mixed layer9 attention-write component
at its last two positions. Map the two donor positions to the same token roles
in the recipient; assert matching tokens, factor corners, answer readers, and
both cues already visible in either layout. Residual coordinates belong to the
same checkpoint; no fitted alignment or rotary transform is inserted.

Let A_r be the native recipient attention output and B_r=A_r-C_r. Diagonal
output is native F_r(B_r+C_r); cross output is F_r(B_r+T C_p). This is a true
component replacement, not subtraction of the donor component from A_r. Define
E_pr=z_r(B_r+T C_p)-z_r(B_r), using the full native suffix and actual softcapped
three-reader logits. In particular E_rr is exactly the previously measured
native-minus-common-removed effect. The supplied producer includes upstream
value production and layer9 averaged routing; the reader includes recipient
background plus subsequent computation. This test does not separate value
production from attention routing within that producer.

Per pair:8forwards capture native/local-value edited states for both layouts;
4forwards obtain the two removed-C backgrounds;4forwards obtain the two genuine
cross swaps. Total256forwards4096sequenceinstances, no fits or offline decoder,
600s cap. The own-component case is native by construction; native and removed
outputs must replay the prior COMMON_SUFFIX_V1 result in both layouts. Preserve
shared firstV; queries/keys remain native before each output patch.

A instrument: bound inputs, finite/restoredhooks, exact256/4096counts; all native
and removed-C three-reader grids replay with BOTH maxabs<=1e-3 and relative
Frobenius<=1e-5. Inherited local-value controls pass; C pure mixed<=1e-10 before
FP32 installation, no nonzero writes before the common mature positions. Output
patch incoming tensors equal captured native tensors bitwise. Mapping unchanged
outside to/action; matched tokens and corners; CPU map/2x2 controls pass. Tiny
scientific effect fails the relevant denominator gate, not mechanical validity.

All scientific gates require EVERY lexical/action pair and both swap directions.
Use Qoh on the effect table; evaluate the correct answer margin and the centered
three-reader vector separately. Both must satisfy a rule to promote that rule.
Subscripts denote donor,recipient with0original and1fronted:
- B producer-following: ||E10-E11||/||E11||<=.10 and
  ||E01-E00||/||E00||<=.10, for both readout objects.
- C reader-following: ||E10-E00||/||E00||<=.10 and
  ||E01-E11||/||E11||<=.10, for both readout objects.
- D small interaction: ||E11-E10-E01+E00||/max(||E00||,||E11||)<=.10
  for both readout objects. Zero denominators fail; no scalar normalization fit.

Use cross_context_effect_attribution_v1 for exact symmetric producer/reader
change accounting. Save all four output-effect tables, individualrule errors,
and nativecommon-write norms/cosine as diagnostics. If both B/C fail, the simple
context-invariant producer-only and reader-only descriptions fail; preserve the
interaction result rather than choosing the smaller error. This is not an
impossibility theorem for a joint mechanism or a new fitted hypothesis.
No semantic circuit promotion from B/C/D alone; all545902902nativeparameters,
four-corner production and opaque native suffix remain. Structural savings0.
