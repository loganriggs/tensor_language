# Frozen mixed-write operator on longer and reordered structures

Source authority is the original bilinear handoff/pilot and its updated circuit
criterion. Previous mixed-write split passes factor selectivity, fidelity and
composition on an opened lexical bank; gender selectivity fails twice. This test
does not change that operator or rescue those failures.

Use all16 frozen lexical/action worlds twice, with complete32-corner cubes:
1. The SUBJECT VERB the OBJECT in the room near the ATTRACTOR to ACTION.
2. Near the ATTRACTOR, the SUBJECT VERB the OBJECT to ACTION.

The first layout has13 tokens, object position4, category position10, readout12.
The second has11 tokens, object8, category2, readout10: category evidence now
precedes object number. Same vocabulary, actions, factors and three answer tokens
as THIRD_NOUN_VALUE_TRANSFER_V1. All1024 new texts disjoint from the parentbank;
all256 opposite subject/object-number pairs preserve token multisets. This is
structural OOD relative to the intervention-development bank, not a claim about
training-data novelty or grammatical controller capability.

At layer9, remove the same W_V Qoh(normalized input) local-value correction;
capture its complete attention-write difference D; remove Dm=Qoh D or Ds=D-Dm
at every position with the native suffix recomputed. No head, source, gain, rank,
row or vocabulary selection. Reuse generalized shared executors. Equal cube
length and the last edited position are explicit metadata: interaction cannot
appear before max(object position,category position). Preserve native Q/K/Q2/K2
and shared firstV for the local-value arm.

Each new world uses baseline/local-value/mixed-write/complement-write arms:
8 forwards ×32worlds=256. One original husband/defend reference additionally
replays full D at the write interface:10 forwards. Total266 forwards /4256sequence
instances, no fit, no offline decoder,600s executor cap. Reference must validate
before opening the new native outcomes. The dynamic executor must reproduce all
three reference intervention grids and baseline from the previous factorial.

A instrument: bound files, finite/hook restoration,266/4256 counts; reference
baseline/full/mixed/spill three-reader outputs satisfy BOTH maxabs<=1e-3 and
relativeFrobenius<=1e-5 against the completed parent. Full-D/local-value replay
same two bounds. Per-world weight commutation<=1e-4 relative; early mixed input
and early write difference<=1e-6; QK and firstV unchanged. FP64 Dm/Ds algebra
closure/purity/orthogonality<=1e-10; installed mixed-write impurity<=1e-4. A weak
native interaction is a scientific failure, not an invalid instrument.

All scientific gates require every newworld individually; also report eachlayout
separately without pooling or changing an all-world verdict:
- B transfer: native mixed correct-margin RMS>=.05; mixed-only intervention's
  signed projection on that natural interaction>=.05; nonmixed/mixed effect<=.25.
- C target-effect fidelity: mixed-only versus local-value mixed correct-margin
  effect relative error<=.10. Zero target denominator fails.
- D gender: mixed effect on G=himself-herself relative to its effect on
  N=themselves-(himself+herself)/2 <=.25, after Qoh; zero denominator fails.
- E composition: local-value effect versus sum of mixed/complement effects
  relative error<=.10 for both correct-margin and centered three-reader full
  tables. Zero denominator fails.

Passing B/C/E supports structural transfer of this partial native-dependent
operation. D failure preserves the gender limitation. Failed B/C/E closes the
claim of robust structural transfer under these bars: inspect all saved grids,
do not alter text, filter cells, or fit new operators on these cases. All native
parameters545902902 remain; savings0. Independent production still needs native
counterfactual inputs and the native downstream model.
