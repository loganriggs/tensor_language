# Reflection-even regional producer pair

Frozen graded mask45, selected after an exploratory screen and confirmed on72new-template regional prompts plus32new newline prefixes. This is a conditional executable component, not a token-to-logit model.

`scalar(current, tokens, program, index)` computes one producer amplitude. `current` is the actual native normalized layer8 or layer9 attention input, shape[batch,sequence,1152]. `tokens` is the corresponding integer token sequence. Index0is8.2first-value; index1is9.8current-value. `execute(current8,current9,tokens,program)` returns their two physical residual writes. Move all tensors in the loaded program to the input device. UseFP32current inputs to match native projection/RoPE rounding.

Both completeQK factors are computed. The key numerator retains inside-inside plus outside-outside products relative to a frozen64Dsourcebasis. This equals half the sum of the original joint numerator and its source-reflected numerator. Original fullkeynormalizers, unreflected value reading, and actual position tables remain. It is not fullstate reflection or normalization recomputation after reflection.

For physical removal, subtract the first write at attention8.2, run intervening native computation, then supply the resulting layer9context to the second call. Passing pristine layer9context after changing layer8would specify a different intervention. Native contextual generators, otherheadbackground, and model suffix remain required.

Program stores fullQK maps, two64Dkeybases, physicalwriters, head8first-tokenlookup and head9currentreader. Unused head8currentreader, head9firstlookup and former merged-consumerwriter are omitted. Densebasis/QK weights remain opaque numerical content; this package alone does not establish structural minimality or independent extraction.

Primary evidence: [running math note](../../FOLDED_PRODUCER_NATIVE_V1_MATH.md), [fresh confirmation](../../SCALAR_JOINT_KEY_GRADES_CONFIRMATION_V1_RESULT.json). Original one-path49.44%miss and fullcomponentnewlinefailure remain recorded. Explicit paired writer/reader citychallenge is not an independent single-role causal localization test.
