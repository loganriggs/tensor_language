# Generated-context local regional executor

Load local_program.pt and context_program.pt into a dictionary with keys local
and context. Call execute.execute with current8, donor_city8, rho8, token_ids,
recipient_token, donor_token, city, destination and optional strength=.5.
Returns the block9 input change. Inputs and weights must share a device.

Two supplied vector states and one per-position scalar state remain. The full
attention8 output is generated internally with a74-token inherited-value table
compiled from weights. Unsupported tokens raise ValueError. Later native layers
are external.24,860,418floating scalars;38,048native state scalars atT32.
Native and isolated replay gates pass, but no full-suffix/composition/simplicity
claim follows. See manifest and linked receipts.
