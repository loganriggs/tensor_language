# Composed inherited-city relay through head9.8 O

This conditional package executes the identified cross-layer path from block8
state and paired city embeddings through the rank-one head8.2 framing write,
block9 reentry/RMS, and the exact head9.8 O current-value response. It returns
the residual delta to add to head9.8's attention output before the native suffix.

Across96 rows and four templates, native plus package-driven scores replay the
previous intervention within3.34e-6; control scores replay within2.39e-6. The
standalone `CONTROL.json` covers one context from every template. The combined
program reuses1,933,572 static scalars.

The dedicated runtime interface is4.084--4.101 times one dense residual delta
at20--24 tokens because it explicitly receives block8 current, block9 residual,
the initial state, first values, city embeddings and masks. Those ports can be
amortized only as shared model state. Their native generation and the suffix
remain external, so this is a composed conditional circuit rather than a
standalone model or a static whole-model compression.
