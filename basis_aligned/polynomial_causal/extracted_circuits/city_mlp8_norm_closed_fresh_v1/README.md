# Fresh norm-closed MLP8 value mediator

This package computes the MLP8-mediated head9.8 value correction from native `z8`, an upstream `delta`, and token IDs. The edited RMS9 is generated internally from native factors. Native inputs and the later suffix remain external; this is not token-only.
