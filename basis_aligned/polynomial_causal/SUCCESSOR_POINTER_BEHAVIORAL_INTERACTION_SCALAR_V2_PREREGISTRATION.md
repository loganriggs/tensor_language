# Successor pointer behavioral interaction scalar V2

V1 fit one global through-origin coefficient `beta=0.6157508151913872` on the eight opened unprefixed length-six groups. Its prospective prefixed run was invalid because the preregistered parent gate required the exact joint mediator to recover at least half of the complete native corruption target. That gate measured mediator completeness, while the frozen scalar's estimand is the exact joint causal response conditional on live attention and MLP components. V1's diagnostic scalar metrics are not promoted or reused as confirmation.

V2 freezes the V1 coefficient unchanged and evaluates it on a second outcome-blind authority created after V1: `Series:`, `Order:`, `Progression:`, and `Next:` crossed with every five month and three digit length-six endpoint and the same coherent, early-control, and late-incoherent transpositions. All 96 exact token sequences are disjoint from both the unprefixed FIT rows and V1 prefixed rows. There are no fits in V2.

For each row, define post-softcap causal backward-margin effects `e_A`, `e_M`, and `e_AM` from coherent attention8–12, MLP8–12, and union transplants into the late-incoherent recipient. The exact interaction is `e_AM-(e_A+e_M)` and the frozen prediction is `e_AM_hat=0.6157508151913872*(e_A+e_M)`.

The compression-specific parent gate is evaluated separately in every prefix-by-family cell. Exact self replay must be within `1e-5` logits, the all-module ceiling within `1e-4`, and native accuracy at least .75 in every condition. The exact joint effect must have RMS at least .50 and positive fraction at least .80. The interaction must have RMS at least .20 of exact-joint RMS. Exact joint late-forward and early-backward control RMS must each be at most .75 of exact-joint backward RMS. Full native-target projection, cosine, and mean are reported to preserve the mediator-completeness boundary but do not gate this conditional compression estimand.

The frozen scalar passes only if, overall and in all eight prefix-by-family cells, prediction versus exact joint effect has cosine at least .90, relative L2 error at most .35, sign agreement at least .80, and norm ratio from .65 to 1.35. Its late-forward and early-backward predicted control RMS must each be at most .75 of exact-joint backward RMS per cell.

A pass establishes a one-scalar suppressive behavioral interaction adapter across a second independent surface panel. Attention and MLP component response generation, complete native-target recovery, the full 1,152-dimensional interaction, and length-seven behavior remain external. A null rejects the frozen scalar. No coefficient change, family/prefix/endpoint term, intercept, residual basis, refit, subgroup, bar change, gradient, update, or quantization is allowed. Price is 44 forwards over 352 sequence evaluations, zero fits, gradients, or updates.
