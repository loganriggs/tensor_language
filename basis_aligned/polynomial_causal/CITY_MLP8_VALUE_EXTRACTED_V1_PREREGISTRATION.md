# Exact smaller interface for the fresh-confirmed MLP8 value correction

The selected correction is the sum of six-piece helper indices1:5. Direct and block9-normalizer terms are not part of this intervention. Algebraically it depends on native z8,attention8 edit d and edited mixed9 RMS scalar rho9 only. Native mixed9 background and its projected value are unnecessary; edited mixed9 full tensor is unnecessary once its RMS is supplied. This is exact dependency elimination, not frozen-normalizer approximation or token-only closure.

Store L8,R8,C=Wv9.8Down8,lambda9,mixture9 only; remove Wv reader storage. Return (1−mu)lambda/rho9 * C[((Ld*Rz+Lz*Rd+Ld*Rd)/S(z+d))+(Lz*Rz)(1/S(z+d)−1/S(z))]. All four MLP8 response terms remain coupled. Native edited-rho9 is still external; do not label it generated or context-free.

CPU40now-opened fresh-mediation fixtures, no new parameter/row selection. pred_a:standalone mediator delta matches full-helper candidate<=1e-10relative each fixture;finite/exact zero outside original destinations;reject invalid scalar shape/nonpositive norms/batch>1. pred_b:physicalFP32 storage11,206,658values, saves147,456 versus full six-piece program; supplied native scalars36,896 atT32 (z8 plus32rho9), intervention delta separately36,864. pred_c:isolated execution with only package files reproduces all40fixtures<=1e-10relative; no repository import.

Installed prediction check registered separately: native and exactupstreamswap+extractedmediator,80sequence-equivalent forwards/36block calls,2CPUthreads180second cap. Replay prior fresh native/folded-correction scores abs<=1e-4 ANDrel<=1e-5; all5readout effects<=1e-4relative; finite/count/CPU. These are equivalence checks on opened fixtures, not another fresh selective test or independent composition result.
