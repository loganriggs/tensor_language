# Hourly strategic review — 2026-09-07 03:14 UTC

## Circuit targets and controlling goal

A completed circuit must identify what is read, computed, written, and consumed; merge or split
native modules according to causal function; predict held-out and OOD effects; support extraction,
removal, swaps, and edits; compose shared and task-specific branches; and remain stable across
documents, constructions, controls, gauges, and starts. The controlling goal remains a smaller
transparent tensor program that is jointly predictive, composable, manipulable, and cheaper in
literal storage, compute, state, edges, and intervention price. A cached activation program is an
intermediate causal interface, not a literal weight program or completion certificate.

## What changed since 02:14

The complete ten-site downstream response lattice executed all 1,024 subsets under the validated
conditional writer. The full pool reproduced every behavior/mode cell with worst squared residual
`.000640`. Its exact Boolean-lattice/Moebius decomposition is overwhelmingly degree one
(`.9999944` of nonconstant coefficient energy), with degree two only `5.49e-6`; this is exact for
the cached-response intervention and does not assert that the native transformer is additive. The
frozen selector chose `MLP13 + MLP15 + MLP16`, with worst six-cell residual `.1073` on v11.

That three-MLP selection was then tested without fitting on a genuinely text-disjoint temporal v12
bank and an outcome-new is/was v12 bank. Four of five gates passed, but the important transfer gate
failed: temporal mode-2 signed projection fell to `.4263` with squared residual `.3490`, below the
registered `.60/.20` requirements. Is/was remained strong (worst residual `.0403`), temporal
behavior/mode 1 remained strong, and removing each retained MLP reduced mode 2 on both tasks by
`.098`--`.441`. The ten-site upper bound remained nearly exact (`.002664` worst), so the null is
localized to over-compression, not to the conditional writer, gauge-safe modes, or downstream
response inventory.

The complete 128-arm lattice of the seven omitted sites around the frozen core was therefore run as
explicitly outcome-adaptive v12 discovery. Sixty-eight arms met the frozen eligibility rule. The
lexicographic selector added `MLP12 + MLP17`, producing the five-MLP candidate
`MLP12/13/15/16/17`: worst residual `.02311`, temporal mode-2 signed projection `.8823`, and
behavior direction `1.0`. `MLP17` is the substantive repair; the four-site core+MLP17 arm already
has worst residual `.0592`, but its is/was behavior signed projection `.7934` narrowly misses the
registered `.80` rule. `MLP12` raises that cell and improves both mode coordinates. A fresh
four-versus-five-site comparison is therefore scientifically preferable to treating the threshold
selected fifth site as automatically necessary.

Fresh capability discipline caught a real native-boundary confound. Temporal v13 passes with
30/32 jointly correct A1/A2 rows. Is/was v13 passes A1 with 12 jointly correct rows but has zero A2
rows because its reporting construction makes the model default to past tense. No causal outcome
was opened. A second untouched v14 lexicon with simple cue-leading `Within` and `During` frames has
been structurally sealed and is awaiting its capability-only gate.

The six-task circuit/DAS line also improved. Enlarged head sets make the voice and quantifier
directions meet extraction and cross-fitted complement specificity together; dative's A2 deficit
was pool-limited and rose from `.446` to `.695` when A2 was included in the fit pool. The
complementizer direction is primarily a one-sided interrogative marker, not a symmetric
that/whether axis. These results reinforce the user's diagnosis: training-family coverage and
environment structure matter more than another scalar regularization sweep. The constrained-DAS
objective still needs a hard target-sufficiency stage followed by collateral minimization; noise or
KL may regularize the feasible solution but must not replace the feasibility constraint.

## Confounds and route decision

The highest-information route remains fresh causal composition, immediately followed by exact
weight translation if it passes. Confounds audited:

- **Post-selection:** the five-site mask is labeled v12 discovery and cannot graduate without a
  new construction result. The three-site v12 failure remains a null.
- **Native capability:** temporal v13 passed; is/was v13 stopped before causal access. The v14
  replacement changes only language/lexicon authority, not the circuit or metrics.
- **Threshold fragility:** four-site and five-site candidates will both be frozen. This distinguishes
  a substantive `MLP17` repair from the smaller `MLP12` fidelity term.
- **Cached values versus computation:** response replay identifies a manipulable causal program but
  still consumes live/base caches. Promotion requires the literal bilinear expansion of every
  retained MLP and explicit declaration of which activation-conditioned inputs remain external.
- **Weight incidence versus causal use:** static weight scores correlated only `.335/.211` with
  causal necessity and produced the L17H4 false positive. Exact weights will be conditioned on the
  observed paired inputs and finite downstream reader, never ranked by raw tensor norm alone.
- **Nonlinearity:** near-degree-one Moebius energy holds only because component responses are cached;
  the native recurrence and MLP input formation remain nonlinear and must be represented in the
  weight program's dependency boundary.

## Throughput and ranked next moves

The hour completed the 1,024-arm composition lattice, a sealed 18-forward fresh confirmation, a
146-forward residual augmentation cube, two new temporal/is-was authorities, and multiple complete
head-set/DAS follow-ups. GPU work remained serialized; capability and causal jobs were queued ahead
while CPU validation ran. The only material ceremony defect—a descriptive null making the claim
ledger unreadable—was repaired with a read-only legacy compatibility test (`9 passed`).

`CIRCUIT_FOCUS: PASS` — the hour advanced exact composition, falsified an over-compressed circuit,
localized the missing response, and created fresh authorities for a stronger test.

`CEREMONY_BUDGET: PASS` — the expensive computations were complete causal lattices and fresh
confirmation; capability checks prevented invalid causal spending, and shared wrappers limited new
experiment code.

`NOVELTY_LESSON_GATE: PASS` — the three-site transfer failure changed the candidate, the v13 native
failure stopped causal access, and the weight plan incorporates the prior static-incidence false
positive and activation-conditioning lesson.

Ranked moves:

1. **Run the is/was v14 capability-only gate.** Kill this confirmation cycle if either A panel lacks
   12 jointly correct rows; do not change the circuit.
2. **Freeze four-site versus five-site response programs on temporal v13 + is/was v14.** Require
   all task/mode cells and behavior direction to transfer; measure `MLP17` necessity and treat
   `MLP12` as optional unless its fresh fidelity gain is reproducible.
3. **Compile every retained MLP through literal bilinear weights.** Expand
   `Down[(delta Left) Right_base + Left_base (delta Right) + (delta Left)(delta Right)]`, contract
   through the exact residual gains into both physical reader covectors, and verify local tensor
   closure plus end-to-end causal replay. This exposes actual upstream read and downstream write
   interfaces and states the remaining activation inputs explicitly.
4. **Run lexicographic multi-environment DAS after the circuit receipt.** First enforce target
   extraction no worse than the frozen difference-in-means floor across whole construction
   families; then minimize complement/cross-task collateral. Use input/subspace noise and held-out
   KL as regularizers or selectors inside the feasible region, with infeasibility reported as the
   result rather than traded away by a penalty.
