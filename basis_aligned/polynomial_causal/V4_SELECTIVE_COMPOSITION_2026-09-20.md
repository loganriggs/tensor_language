# Selective edits compose behaviorally, but additive prediction fails

The frozen finite-reference source edits jointly pass selectivity in all8openedv4panel/family cells. Joint retention against an independently evaluated jointunitB reference ranges1.210–2.019; maxcollateral.075. Singletons replay exactly. This is composition of two distinct pre11 residual-site patches with fixed x0/cache/background, not joint embedding erasure. Native source construction, per-input gradients and finite-reference selector generation remain charged.

Additive prediction passes only1/8groups under the weaker-component budget. Number interaction ||E_joint-E_subject-E_attractor||/min(single number norms) reaches.4954, with collateral interaction up.078. The joint target remains selective while the weaker edit's conditional increment is not predicted well. Large joint signal cannot stand in for preserving a small component.12prefix20native suffix,1.367s reported run.

## Explicit readout fold and negative result

The follow-up uses only baseline and two singleton final pre-RMS states, never the joint state, to form an affine state prediction h0+s*(hs-h0)+a*(ha-h0). Contract18unembedding rows into54numerator coefficients and the norm into6Gram coefficients. Retain exact native epsilon, RMS and30tanh. The resulting60-value conditional consumer is exact on that affine span; native producer states remain required. The additive baseline costs18output-effect values per input, so this is not a same-price victory claim.

CPU independent state/readout replay3.55e-15; dropping Gram cross terms changes the planted output by3.755. Native three-corner field replay max4.03e-6absolute and.000277relative to weaker effect, below.001instrument budget; old single/joint effects replay0.12prefix16native suffix,1.358s runtime. Shared code native_source_observables32 now has an optional final_state capture, leaving existing call behavior unchanged and verified by replay.

Despite valid instrumentation, affine-state/exact-readout prediction still passes only1/8. Worst number error remains.3839 of the weaker effect. This falsifies readout-only closure under this specific affine-state hypothesis; it does not make final normalization unimportant in general. Joint selectivity is unchanged because it is a native measurement, not a predictor output.

## Executed post-result CPU consequence

V4_READOUT_COMPOSITION_PARTITION_V1.json splits the observed interaction into the readout-predicted component plus its residual. Signed number-projected readout shares range-.138 to.240, depending on panel/structure. Some terms help, some oppose; exact sums are per cell, not additive averages. This is an algebraic prediction split, not separate causal effects or unique attribution. It directs the next folding step upstream to context-dependent joint state computation; adjusting only the final normalizer cannot satisfy the measured prediction requirement.

Previous two-site studies already exposed weak-axis errors with narrower source/control sets. This experiment tests newly selective23-port interventions and all nine outputs; it does not relabel the earlier result as solved. Full goal status: manipulation and joint selectivity supported conditionally on opened examples; independent contextual source/selector extraction, accurate compositional prediction, prospective transfer of the complete program, stable minimal units and literal whole-program simplicity remain missing.

Primary receipts v4_selective_composition_v1_result.json, v4_composition_readout_v1_result.json and managed logs. Preregistrations V4_SELECTIVE_COMPOSITION_V1_PREREGISTRATION.md and V4_COMPOSITION_READOUT_V1_PREREGISTRATION.md. Explicit readout implementation affine_state_readout.py, using shared final_readout_field_program.py. Goal remains active.
