# Hourly strategic review

ACTIVE_TRACK: CIRCUIT

Actual UTC: **2026-09-19T06:18:18Z**. Next deadline: **2026-09-19T07:18:18Z**. Previous Codex review: [05:07 WEIGHT_FOLDING](HOURLY_STRATEGIC_REVIEW_2026-09-19_0507.md), 1h11m11s old. This is one current review, not a backfill. Historical `/workspace` paths were resolved against the live `/workspace/tensor_language` checkout. No GPU model was loaded; no job, timer, service, commit, push, external contact, experiment/result, or `TYPED_FACE_EXTRACTION_V1` implementation was changed.

The prior review's folding handoff—complete regional residual6→MLP8→head9.8 closure—remains unfinished and is preserved for the next folding hour. The intervening lane instead produced a deep number-circuit chain plus late unembedding/subspace folds. Alternation therefore proceeds to `CIRCUIT`; unfinished folding does not license repeating that track.

## Seven circuit targets and full goal

1. **Computational specification:** state what is read, what operation/composition occurs, what is written, and which downstream computations consume it.
2. **Cross-boundary grouping and within-module splitting:** group pieces across modules when a reader treats them as one variable, and split native modules when their pieces serve different computations.
3. **Held-out/OOD prediction:** predict activation and behavioral effects on unseen inputs, task variations, and shifted corpora.
4. **Extraction or sufficiency:** run an isolated circuit at a declared boundary with every retained port and background named.
5. **Selective manipulation:** removal, swap, or edit changes the target against matched controls while preserving unrelated behavior and accounting for redundancy/interactions.
6. **Composition and reuse:** shared subcomputations serve multiple tasks/modules and their joint behavior follows a tested composition law.
7. **Stable identification:** units survive document/corpus splits, gauges, and fitting restarts, or are defined by downstream operational equivalence.

Goal: a smaller transparent tensor program that **predicts held-out/OOD behavior, is extracted at a declared boundary, supports selective manipulation/removal, and composes/reuses**. Simplicity is a fifth, separately priced property—stored scalars/bytes, products, readers, writers, native ports/state, nodes, edges, and execution. Lower replay error, storage, CE, or rank cannot replace any behavioral property.

## What changed and five-property score

Evidence remains typed: v369-v388 are mainly **edits/responses/folds** on opened panel and natural rows; v389 is a weights-only **fold/null**; v390-v397 are unembedding/subspace **folds**, with v393-v394 opened edits. Natural-row counts are not automatically distinct context cells. The current regional and number components remain paths, not completed circuits.

| Property | Evidence at the boundary | Verdict |
|---|---|---|
| Held-out/OOD prediction | The six-unit number chain transfers its signed activity and edit to 128 natural sentences (v369/v371); 50-unit populations transfer at reduced strength (v374-v375), and ten two-site units change the natural margin by −11.1% (v387). But these units were selected on evolving panels, no frozen end-to-end law on declared distinct cells exists, and the proposed `VP0` axis falls to 0.2% on natural text while `VP1` also fails (v395-v396). Regional FineWeb direction remains 88/102. | **PARTIAL** |
| Extraction at a declared boundary | Regional residual6→attention8 and norm-closed MLP8 interfaces execute with explicit ports. The number route now traces MLP1→MLP3→head4.5→MLP5/6/7→MLP8 unit829→heads9.6/12.4/15.1 at noun and post-noun sites, but has no standalone executor or exact port closure. | **PASS only at regional intermediate boundaries; number incomplete** |
| Selective manipulation/removal | Six named number units cost 14.0% panel/8.8% natural margin and beat layer-matched nulls; 50 units cost 34%/18.8%; post-noun units add a 3.6% effect. Compensation under the MLP3 cut beats random sets. These are target-local effects without a preregistered three-reader preservation battery. The `VP0` edit is panel-only after v395. Regional effects remain corpus-direction unstable. | **PARTIAL** |
| Composition/reuse | Ten number units across two sites are locally additive, and a specific restorer response is identified, but neither has smallest-live-piece Möbius scoring against matched random splits. Regional key/value interaction remains 2.3448× the smaller single. No cross-task reuse is established. | **FAIL** |
| Measured simplicity | Six/ten/50-unit descriptions are compact but omit the supporting populations, native ports, and executable price. Regional packages have literal local prices; norm closure increases storage. The concurrent 128-product numerical lower bound prices only an opened 16D MLP8 numerator restriction. | **LOCAL PRICES/LOWER BOUND ONLY** |

The number path gained real causal depth: MLP populations propagate with loss; MLP7 unit1779 specifically compensates after an MLP3 population cut; readers consume number at both noun and post-noun keys; the second site is rebuilt by attention plus MLP4-7. Failures matter equally: no head is strongly identity-like by the tested weight templates (v389); attention4 does not dominate the second-site rebuild; v392 initially omitted lambda-chain scaling; `VP0` is frame-specific and `VP1` is a small sign-unstable side axis. v397 finds a more consistent pronoun-internal `VC1` candidate, but it is still an opened fold, not identification.

## Highest-value CIRCUIT action

Run the already-preserved **regional full-suffix five-arm coupled factorial** on frozen, structurally varied, genuinely distinct `(template, city-pair, endpoint)` cells. Arms are baseline, routing/key single, value single, additive installation without their product, and the true joint native product. Install at the complete head9.8 write boundary, recompute the native suffix, and score the regional reader, at least three unrelated readers, full logits/CE, 16 equal-norm same-site nulls, and matched random splits. Normalize the true-joint-minus-additive interaction by the smallest live single; exact Möbius closure is only accounting.

This changes targets 1, 2, 3, 5, and 6. It is killed if the frozen signed effect is not predicted, unrelated readers breach their gates, either single is dead, or the true interaction is no more specific than matched random splits. It outranks more number attribution because the number subspace search just falsified its first two proposed general axes, whereas the regional route has exact intermediate extraction and an unresolved, large causal interaction at a declared boundary.

**Concrete track connection:** the folded seven-term `QK1*QK2*V` expansion says precisely what the factorial's additive-without-product arm omits. The circuit result can then select a consumer-relevant interaction group for the next fold instead of treating QK1, QK2, value, or a native module as semantic units.

**WEIGHT_FOLDING handoff:** next folding hour, implement and replay the complete regional head9.8 delta with corrected all-nine-head upstream generation, live norm-closed MLP8, native RMS/rotary/re-entry semantics, all source positions, both ordered MLP cross terms, and all seven nonempty `QK1*QK2*V` delta products; publish one deduplicated price.

## Confounds, alternatives, and organization

The factorial must control baseline subtraction and replace-versus-zero semantics; frame, position, city, and lexical mixing; RMS/rotary/MLP/head/suffix nonlinearity; shared token/document difficulty; FineWeb/Pile leakage; dead arms and tripwires; FP32 floors; and post-selection. Report actual context cells, not only rows.

Alternatives are lower value now: (2) promote the number chain only after a frozen distinct-cell preservation/composition battery; another selected unit census is killed by the broad population tails and failed output-axis generalization. (3) A predictive-state quotient around the regional context slot becomes attractive only if the full coupled factorial varies systematically by structure. (4) Function-space/Sobolev or signed-composition bases are demoted until the native interaction's fresh causal specificity is known. The current regional route survives because it closes a declared boundary and poses a discriminating intervention rather than another reconstruction sweep.

Registry/dossier audit found these missing cross-links:

- `CIRCUIT_GRAPH_REGISTRY_V1.md` remains at 49 packages/44 manifests/33 boundaries, omits `city_mlp8_norm_closed_v1`, and calls two rows “four-trait verified” without the separate simplicity/five-property standard.
- The concurrently dirty `COMPUTATION_PATH_REGISTRY.md` now has `PATH-REGIONAL-CITY-001`, but still links the supplied-norm mediator rather than the norm-closed MLP8 package and complete head9.8 endpoint. It has no number-path record spanning v338-v397, including the v392 correction and v395-v396 falsifications.
- `CIRCUIT_REGISTRY.md` and `MODULE_DOSSIERS.md` stop regional navigation before the norm-closed interface and do not consolidate the current two-site number chain. `MLP_MODULE_DOSSIER_INDEX.md` still marks MLP1 unconsolidated and its MLP8 addendum predates norm closure.

The highest-priority missing link is one number-path record that binds aliases (project head names/zero-based indices, unit IDs, noun/post-noun sites, they−he/VP0/VP1/VC1 axes) to v338-v397 and retains the failed axes. No registry was edited because the path registry and live number lane are concurrently changing.

## PAST_HOUR_TIMING

Window: **2026-09-19T05:07:07Z-06:18:18Z**. Twenty-nine receipts v369-v397 finished 05:08:46-06:17:07 and declare 548 forwards. Twenty-seven sane receipt runtimes sum to **57.202s**; v381 and v388 contain corrupt `serial_seconds` near 1.8 million seconds and are excluded. Runner start/end lines measure **76s** of rounded model-process wall time across the 29 science jobs. Serial receipt cadence was usually 1.5-3.5 minutes, except v386: two gate refusals plus a bare result-file wait caused about **10 minutes** of idle time. Parallel durations are not summed as wall time.

- **Design:** unknown. Registered predictions exist in receipts, but no phase boundaries measure design time.
- **Implementation/data building:** unknown. Twenty-nine sibling scripts and repeated derived constants are visible; do not infer labor from commit gaps.
- **Validation:** unknown overall; two v386 gate refusals and one v393 inherited-constant refusal are recorded, but their durations are not separately timestamped.
- **Model execution:** 76s rounded process wall; 57.202s across sane internal receipt timers.
- **Interpretation:** unknown. Board/scorecard updates follow every result, but commit cadence is not labor duration.
- **Documentation/review:** the 05:31 review and 06:14 mathematical review have file timestamps, but their active authoring durations are unknown.
- **Idle/blocked:** about 10m measured for v386's bare wait; all other idle/blocked time unknown.

`RESEARCH_PHASES_2026-09-19.jsonl` had only 04:04 and 05:07 review boundaries before this review. It covers none of the intervening science phases, so no uninterrupted-work inference is valid. The existing helper appended one truthful 06:18:18 review boundary; no phase was backfilled.

## PROCESS_IMPROVEMENT

Ranked by likely saving and cost:

1. **Mandate the existing fail-fast waiter and remove bare result-file loops:** demonstrated saving about 10m on the next gate refusal; seconds of operator cost, but changing the active external lane or its current v398 work would exceed this review's authority. Record as the mandatory next runner practice.
2. **Declarative unit/factor/census executor with cached rows and schema-validated durations:** likely saves 1-2m per candidate and prevents inherited `BATCH`/`PHRASE` constants and corrupt timers; estimated 30-60m plus equivalence tests, too costly and collision-prone while the lane is live.
3. **One number-path registry record:** likely saves 5-10m per handoff for under 10m once the current sequence freezes; currently collides with dirty shared navigation.

**Executed bounded nonconflicting repair:** used `research_phase_clock_v1.py` with `/venv/main/bin/python` and `CUDA_VISIBLE_DEVICES=''` to append only the actual review boundary. No scientific CPU experiment was added because the concurrent 06:11 lower-bound control already consumed the useful isolated CPU opportunity and another result would distract from the required circuit action.

## Explicit verdicts

- `TRACK_ALTERNATION: PASS` — previous `WEIGHT_FOLDING`; current `CIRCUIT`.
- `TRACK_PROGRESS: FAIL` — the declared folding hour spent most of its receipts on number circuit edits and did not execute the selected regional head9.8 closure. v389-v397 eventually produced concrete weights/unembedding folds and honest falsifiers, but late folding work does not erase primary-track drift.
- `CEREMONY_BUDGET: PASS WITH TIMING CAVEAT` — 29 decision-bearing receipts and 548 forwards dominate two reviews, while measured execution is cheap; exact design/implementation/review labor remains unknown because phase coverage is sparse.
- `NOVELTY_LESSON_GATE: FAIL` — the lane repeated derived-script constant failures, used a known-bad bare waiter, omitted lambda scaling once, left the number path orphaned from registries, and displaced the authority-designated regional depth handoff. The bounded repair is truthful timing plus a hard handoff: no unrelated number census should precede the regional factorial, and existing fail-fast/shared machinery must be reused.
