# Natural Pile context transfer of the frozen full-city removal

Frozen CITY_FULL_PILE_V2_ROWS: first20distinct eligible documents among1681 Pile
stream documents, one32token window with exactly one mapped city and12tokens
before it. Requires an immediately preceding in/from/near/outside/around/at/to;
known prior document hashes excluded. No model-score/capability filtering. One original natural city arm and
one single-token UK/US city substitution;20document cells,40sequences,240rows.
Same6latest spelling endpoints are diagnostic readouts, not observed next tokens.
Source monology/pile-uncopyrighted/train; metadata and document hashes retained.
This is corpus-context shift for this component; no pretraining-disjointness or
untouched-corpus-in-all-prior-project-work guarantee. All cells retained.

Frozen full city-value half-removal, single-head frozen-norm response, one native
recipient residual7 input. Exact weight token tables extended without fitting.
Four-feature and cueconstant baselines copied byte-for-byte from CITY_FULL_FRESH_V1
(old opened full-city training targets); no corpus outcome fitting or selection.
Candidate sees native state and more computation than text baselines.

800bodyforwards/300s: native, native8 removal, exact local response reentry,
one-input candidate,16per-position equal-norm Gaussian nulls at block9 input.
Seed17093100+1000*k+context_id, paired signs; same four unrelated readers.
Score all20documents jointly as the Pile corpus, plus descriptive per-document
capability/attenuation. Do not call six endpoint probes independent documents.

pred_a exact reentry<=1e-4absolute AND1e-5relative allreadouts, exact localdelta
<=1e-4relative, finite/outsidezero, null normerror<=1e-5, exactly800forwards.
pred_b >=90of120pairs capable(native UK-US contrast>=.1), candidate targetRMS
>=1e-5, candidate/native effectpredictionerror<=.35relativeL2 on all rows.
pred_c eachfourreader RMS<=.5candidate targetRMS on all rows.
pred_d >=.90ofcapable pairs attenuate and meanattenuation>=.02.
pred_e targetRMS>=2randommedian and beats16of16nulls.
pred_f effecterror<=.8times EACH frozen baseline error.

The capability fraction remains75% (formerly18of24perauthoredfamily), now90of120
foronecorpus group. This is a new prospective dataset grouping, not retroactive
gate adjustment. Capability failure stays a failed transfer result; no row deletion.
No composition, token-only or natural-next-token accuracy claim. Existing inherited-
only sign failure and full-city branch interaction failure remain unchanged.

V1 was rejected before model execution because a bare Phoenix match referred to
Order of the Phoenix. V2 qualification is place-like syntax, not a guarantee of
geographic denotation: some examples refer to local newspapers or teams. Preserve
all20frozen V2cells; no additional semantic exclusions after outcomes.
