# Hourly strategic review — 2026-09-07 13:14 UTC

## Circuit interpretation targets

The target remains a circuit that specifies what is read, computed, and written; can merge across and split within native modules; predicts held-out/OOD effects; executes as a sufficient interface; supports selective removals/swaps/edits; composes and reuses subcomputations; and is stably identified across families, fits, and gauges. Native heads/MLPs and low-rank bases remain proposals until causal execution supports them.

The full program goal remains a smaller transparent tensor program that is jointly predictive, composable, manipulable, and literally simpler in storage, compute, edges, and state. Neither low rank nor exact algebra alone is completion.

## What changed since 12:14

There were 13 terminal follow-up receipts, with median inter-receipt gap 3.87 minutes (target <=10). The main circuit lane moved from a weak DIM/source-basis null to a bidirectional, selective five-MLP rank16 source interface:

- Per-row DIM and per-row SVD did not reproduce the source program; all-position SVD recovered response but raw gain was behaviorally low.
- A frozen gain curve selected 1.15. Reverse OOD execution then passed all five gates: response projections 1.063/1.100, behavior .758/.917, zero control flips, and forward/reverse behavior gaps below .006.
- Exact native compilation `A_s=Down_s^T Q_s` closes on fit/fresh at `8.11e-14`; all five maps are rank16 and the factorization is gauge invariant at `2.96e-15` in float64.
- The static hidden-unit concentration hypothesis failed: only MLP6 puts >=25% map energy in its top 10% units; effective participation is 1,462–3,782 units. This changes the next circuit boundary from raw weights to activation-conditioned weight coefficients.
- The user's DAS diagnosis was adopted precisely: same-family complement optimization can memorize; noise/KL require leave-one-construction-family-out selection and hard target retention. Existing results do not refute regularization; they refute the previous validation/objective combination.
- The broad circuit lane added amended tier-3 coverage and within-family separability results; it continues to supply distinct candidate circuits rather than only variants of this temporal program.

Corrections were preserved: one endpoint-flattening crash; one undercounted helper-call price caught before execution; v1/v2 compiler receipts invalidated by float32 diagnostic and authority-gate mistakes; a zero-forward atomic audit, not a third scientific rerun, established the valid tensor result.

## Is the route still highest-information?

Yes, with one adjustment. The bidirectional causal source interface makes weight translation legitimate; the failed static concentration gate says raw weight magnitude cannot define within-MLP membership. The active activation-conditioned split directly tests cross-boundary grouping/within-module splitting, held-out prediction, extraction, and selective manipulation. It is not a compression sweep: success requires full-model behavioral and response transfer against an equal-size static-weight control and an insufficient complement.

## Confound audit

- Baseline subtraction: patches use explicit donor-minus-base hidden coefficients added to cached base outputs; full-unit replay is a live tripwire.
- Frame mixing: `Q_s`, `A_s`, and hidden coefficients remain site-specific; no vectors are compared across residual gauges without a projector or executed output.
- Nonlinear composition: selected groups are installed simultaneously and downstream layers recompute; singleton energy is not treated as an additive causal verdict.
- Shared difficulty/leakage: masks are fitted only on old families and opened once on fresh temporal/is-was populations; matched controls are separate.
- Dead knobs: full, activation fractions, static-weight control, and complement have distinct masks and exact selected+complement closure.
- Precision/noise: CUDA SVD raw Gram tolerance is explicit; gauge algebra is float64; causal execution stays in native precision.
- Post-selection: fractions and bars are frozen before fresh outcomes. No threshold relaxation is licensed.

## Different routes and kill conditions

1. **Activation-conditioned hidden groups** — highest information now. Kill as a compact circuit if activation-top50 is not functional or does not beat static weights; retain only the exact distributed map.
2. **Family-regularized DAS** — next identification route. Kill the present objective family if no moved noise/KL checkpoint beats pooled step zero under leave-one-family-out validation while meeting hard target constraints.
3. **Downstream Jacobian/reader pullback** — useful after a source group survives. Kill static reader ranking if full response removals fail held-out enrichment, as already happened for L17H4.
4. **Finite causal-response/Hankel modes** — strongest fallback for operational equivalence if native units remain distributed. Kill a shared mode claim if task-balanced modes fail sealed cross-task interchange or require task labels at execution.

## Throughput and workflow

`CIRCUIT_FOCUS: PASS` — source sufficiency, reverse manipulation, exact tensor translation, and a within-MLP causal split all advanced.

`CEREMONY_BUDGET: FAIL` — scientific runs were short, but repeated price and monolithic-gate mistakes consumed too much authoring/review time. Repair already begun: the next runner reuses the tested contribution helper, publishes exact local closure, and will add live forward accounting before another candidate opens.

`NOVELTY_LESSON_GATE: PASS` — prior DIM/DAS/weight receipts were searched; activation reconstruction alone was rejected; control-family and gauge lessons were applied; the new experiment asks a previously unresolved within-MLP causal question.

The active receipt is the claimed and queued 39-forward activation-conditioned full-patch experiment, behind Claude's live v202. No unrelated candidate opens until this receipt lands and the ceremony failure is repaired.
