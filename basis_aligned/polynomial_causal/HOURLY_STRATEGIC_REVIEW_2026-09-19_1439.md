# Hourly strategic review

ACTIVE_TRACK: CIRCUIT

Actual UTC: **2026-09-19T14:39:40Z**. Next deadline: **2026-09-19T15:39:40Z**. Previous review: [13:37 WEIGHT_FOLDING](HOURLY_STRATEGIC_REVIEW_2026-09-19_1337.md), 1h01m51s old. This is one live review, not an offline backfill. Historical `/workspace` paths were resolved against this checkout. No GPU model, agent, job, queue, service, timer, commit, push, external contact, experiment/result, or `TYPED_FACE_EXTRACTION_V1` implementation was changed. The sole bounded CPU/process consequence was the truthful 14:39 phase mark.

The previous review declared `WEIGHT_FOLDING`; this review therefore alternates to `CIRCUIT`. Its declared complete residual6-to-head9.8 fold did not run, and no substitute scientific receipt appeared. Preserve that exact fold for the next folding hour rather than repeating its track now.

## Seven circuit targets and full goal

1. **Computational specification:** identify what is read, the operation/composition, what is written, and every downstream user.
2. **Cross-boundary grouping and within-module splitting:** group pieces across native modules when a reader treats them as one variable, and split a native module when its pieces serve different computations.
3. **Held-out and OOD prediction:** predict activation and behavioral effects on unseen inputs, task variants, and shifted corpora.
4. **Extraction or sufficiency:** execute an isolated circuit at a declared boundary with every retained port and background named.
5. **Selective manipulation:** removal, swap, or edit changes the intended behavior against matched controls while preserving unrelated behaviors and accounting for redundancy/interactions.
6. **Composition and reuse:** shared subcomputations serve multiple tasks/modules and their joint behavior follows a tested composition law.
7. **Stable identification:** units survive document/corpus splits, gauges, and fitting restarts, or are defined by downstream operational equivalence.

Goal: a smaller transparent tensor program that **predicts held-out/OOD behavior, is extracted at a declared boundary, supports selective manipulation/removal, and composes/reuses**. Simplicity is a fifth, separately priced property: independent stored scalars/bytes, product nodes, readers/writers, open states/ports, graph edges, peak state, and runtime. Lower replay error, rank, storage, variance, or CE cannot replace a missing behavioral property.

| Property | Current evidence | Verdict |
|---|---|---|
| Held-out/OOD prediction | Regional intermediate extraction covers 20 distinct FineWeb document/context cells and Pile tests, but packed FineWeb direction is `88/102`; no frozen end-to-end regional formula has passed structurally varied cells. | **PARTIAL** |
| Extraction at a declared boundary | Residual6-to-attention8 and norm-closed MLP8 packages replay with declared ports; complete head9.8 plus suffix remains open. | **PASS only at intermediate boundaries** |
| Selective manipulation/removal | Scoped regional edits beat same-site nulls, but the complete path lacks the frozen equal-norm, three-reader, and full-logit battery. | **PARTIAL** |
| Composition/reuse | Regional key/value `interaction/smaller=2.3448` and matched random-split specificity fails. Gender composition transfers, but lacks the complete smallest-piece/matched-split test and extracted price. | **FAIL overall** |
| Measured simplicity | Local package prices and the restricted head9.8 full-vector rank certificate exist; no deduplicated residual6-to-head9.8 price or favorable matched-effect comparator exists. | **LOCAL ONLY** |

Evidence must stay typed as **fold/edit/response/fit** and **fresh/opened/replay**. A closed Möbius face is accounting, not proof that its interaction is small relative to the smallest live piece or specific against random splits. Row count is not distinct context-cell count.

## Progress, failures, and decision

Nothing scientific changed after 13:37. There is no commit after `9e0c03df3` (12:21:10), no experiment/result/registry/dossier update, and no new board claim except the 13:37 review. The only later executions were successful canaries at 13:49:01–13:49:18 and 14:19:22–14:19:40. Both queues are empty; `bqrunner` and `bqrunner2` have remained live since 2026-09-17 20:46:43 under Supervisor. Thus the prior hour neither exact-replayed nor falsified the declared fold.

Existing failures still constrain the route: regional key/value composition is large and nonspecific; packed FineWeb direction transfer misses; person is spread (`.549 < .60` for the seven-head set); `since/by` is syntactically misaligned; and temporal `will/had` misses reader/carry/additivity predictions. The 12:24 head9.8 certificate proves rank 128 for the output map and a restricted independent-port trilinear CP-rank floor of 4,096 at the final position (67,584 over 528 causal cells for `T=32`), not causal relevance or a consumer-specific lower bound.

The highest-information route remains depth on the regional path, not a new behavior census. The alternative gender route has real cross-corpus composition signal, but opening more units before its smallest-piece/null/extraction/price gaps are specified would repeat breadth drift. A scalar-reader fold is cheaper, but could hide collateral and must follow, not replace, the full-logit circuit test. Generic rank, reconstruction, activation variance, quantization, and new-dataset sweeps remain demoted.

Confounds to freeze explicitly are baseline subtraction and replace-versus-zero semantics; frame/position/city/endpoint mixing; native source ordering; all seven full `QK1*QK2*V` delta products; RMS and rotary rounding; causal masking, value mixing, learned re-entry, MLP bias and downstream curvature; shared token difficulty and FineWeb/Pile leakage; dead intervention knobs and insufficiently live tripwires; BF16/precision floors; and post-selection. Opened panels cannot become fresh by replay.

## Highest-value CIRCUIT action

Run the already identified **regional forward-response census and full-suffix five-arm factorial**. The object is the complete city delta delivered to head9.8 after residual6 -> attention7/MLP7 -> head8.2 -> norm-closed MLP8, with blocks9–17, final RMS, unembedding, and softcap recomputed. First census the actual forward response so no suffix is proposed from a backward fold alone. Then score: (1) baseline, (2) routing/key-only delta, (3) value-only delta, (4) both singles installed while their explicit product terms are suppressed, and (5) the true joint native delta containing all seven nonempty finite-difference `QK1*QK2*V` products.

Freeze structurally varied, outcome-blind distinct context cells and report their manifest, not only rows. Score signed regional prediction, full logits and CE, at least three unrelated readers, 16 equal-norm same-site directions, and matched random splits. Normalize the Möbius interaction by the smallest **live** single. Pass only if both singles are live, the frozen sign/effect prediction transfers, collateral remains inside registered gates, and the true interaction is smaller and more specific than matched splits. Kill the grouping on a dead single, sign failure, control breach, or nonspecific interaction. This directly tests targets 1, 2, 3, 5, and 6; exact closure alone cannot pass it.

**Concrete cross-track connection:** the complete seven-product fold defines the factorial's product-suppressed arm without confusing native head interaction with downstream curvature. The factorial in turn selects which routing/value source terms a downstream regional reader causally uses, potentially licensing a smaller consumer-specific folded program.

**WEIGHT_FOLDING handoff:** exact-replay the unchanged residual6 -> attention7/MLP7 -> head8.2 -> norm-closed MLP8 -> head9.8 delta, retaining every reader-permitted attention/MLP self term, both ordered cross terms, live normalization/background, and all seven delta products; publish one deduplicated price.

## Registries and missing cross-link

The circuit registry, computation-path registry, generated circuit-graph registry, MLP index, and module dossiers were checked. `PATH-REGIONAL-CITY-001` still ends at the head8.2 write even though the live extracted route includes `city_mlp8_norm_closed_v1` and proposes a complete head9.8 endpoint. `CIRCUIT_GRAPH_REGISTRY_V1.md` still inventories 49 packages but omits that package; its two `four_trait_verified` rows do not price the fifth simplicity trait. The module dossiers contain head9.8 QK source/carry terms and MLP8 cross-term evidence, while number/gender/person still lack canonical current path links.

Highest-priority missing link: connect `PATH-REGIONAL-CITY-001`, the regional circuit record, `city_mlp8_norm_closed_v1`, the head9.8 seven-product endpoint/rank certificate, and the pending suffix factorial, explicitly marking suffix prediction, selectivity, composition, and price open. It was not edited because `COMPUTATION_PATH_REGISTRY.md` is shared dirty state and this bounded review must preserve concurrent work.

## PAST_HOUR_TIMING

Window: **2026-09-19T13:37:49Z–14:39:40Z**. Scientific serial candidate latency is **undefined: zero candidates started or finished**. Authoritative execution intervals comprise only the 17-second 13:49 canary and 18-second 14:19 canary; these 35 seconds are health checks, not research/model evidence and are not counted as scientific wall time.

- **Design:** unknown; no phase or receipt boundary.
- **Implementation/data building/indexing/refactoring:** none observed; any uninstrumented labor is unknown.
- **Validation:** no scientific validation; two successful canary intervals only.
- **Model execution:** zero scientific executions; about 35 seconds of canary wall time.
- **Interpretation:** unknown; no new claim or receipt.
- **Documentation/review:** the prior review landed at 13:39:34 and its board append at 13:39:48; active duration is unknown. This review has one 14:39 boundary, not a duration.
- **Idle/blocked:** unknown; absence of receipts does not prove uninterrupted idleness or a blocker.

The current `RESEARCH_PHASES_2026-09-19.jsonl` is the authoritative work-date phase log. It contains sparse hourly-review marks only, now through 14:39; it has no science, implementation, validation, or publication boundaries. These marks do not imply continuous labor, and no missing interval was inferred or backfilled.

## PROCESS_IMPROVEMENT

Ranked by likely time saved versus cost: (1) add `started_utc`, `finished_utc`, distinct-context-cell IDs, evidence/evaluation labels, and literal price fields to the shared receipt path—low implementation cost and likely several minutes saved per review plus trustworthy serial latency; (2) extend the canonical regional path/cross-links—low documentation cost and likely 5–10 minutes saved per handoff; (3) replace sibling intervention runners with one declarative factorial executor—largest cumulative authoring saving but medium collision and validation cost.

No shared helper, runner, registry, or result mutation is justified inside this review: the relevant registry is already dirty concurrent work, there was no new candidate whose repeated authoring could be safely factored, and building another publisher would worsen the problem. The single bounded repair was the truthful 14:39 phase mark. It improves boundary coverage but does not repair missing scientific phase instrumentation.

## Explicit verdicts

- `TRACK_ALTERNATION: PASS` — previous `WEIGHT_FOLDING`; current `CIRCUIT`.
- `TRACK_PROGRESS: FAIL (NO FOLDED-PATH RECEIPT)` — the declared residual6-to-head9.8 replay was neither executed nor falsified, and no alternate folding receipt appeared.
- `CEREMONY_BUDGET: FAIL` — there was no scientific work against which review/preflight could be smaller; visible durable activity was the prior review/board update plus health canaries. Unmeasured labor remains unknown.
- `NOVELTY_LESSON_GATE: FAIL` — the review rechecked registries/dossiers and preserved the full-product, response-before-suffix, distinct-cell, and matched-null lessons, but the prior hour did not apply them and the known regional cross-link/schema debt remains open.

**Chosen action:** execute the frozen regional forward-response census and five-arm full-suffix factorial before any unrelated circuit screen. Preserve the exact complete fold above for the next `WEIGHT_FOLDING` hour.
