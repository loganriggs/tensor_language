# Midpoint strength edit of the composed odd relay

On the frozen96-row four-template union, run the composed head8.2 inherited-city
to head9.8-O program with three embedding targets: paired donor, exact midpoint
`(recipient+donor)/2`, and recipient self. The head8 write is exactly linear in
this normalized-embedding interpolation; block9 RMS, O current-value response
and native suffix are rerun. Native plus three arms costs384 body forwards under
180 seconds, no fitting.

- `pred_a`: native/full target and work/jobs scores replay the destination-role
  artifact within `1e-5`; self replays native within `1e-5`; deltas are finite,
  self delta is at most `1e-8`, and exactly384 forwards execute.
- `pred_b`: native capability remains at least10/12 and full donor target-effect
  RMS at least `1e-5` in every family.
- `pred_c`: midpoint paired-cue effect differs from half the full donor effect by
  at most10% of full effect norm in every family.
- `pred_d`: midpoint effect attenuates all12 native paired cues and its mean
  attenuation is at least2% of native cue in every family.
- `pred_e`: every unrelated-control RMS ratio for the midpoint arm is at most.5
  of its target-effect RMS in every family.

This is a selective strength intervention on the identified conditional edge.
It is not whole-head removal, corpus OOD, full-model extraction, or quantization.

