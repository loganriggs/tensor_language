# Frozen shared-parent native validation

12 September2026. Freeze the discovered program with SHA311c87dd0850813f43f5c2f746ecfc0a0432fdde85424babfdfa3ab880a45ff6.
The exact component is $(a_p^Tp)M_pp$ divided by the native MLP17 RMS-squared
denominator. It is the parent-touching portion of the pure producer/producer
path, not all uses of that input direction elsewhere in the model.

Construct the existing fixed rank16partner approximation from weights only:
$J=(I+aa^T)^{1/2}$, $M_z=M_pH^{1/2}$, and SVD of
$C^TM_zJ$ where$CC^T=U_c^TU_c$. Keep16singular terms and transform back
through$C^{-T}$and$J^{-1}H^{-1/2}$. Rank was fixed by the completed structural
experiment; do not choose it from native effects. Record the49.01%coefficient
capture replay. Save both physical factors and their consumer-first compilation
through$\lambda D_{16}$. This needs17product readers plus16output writers,
along with the original L16/R16input maps, normalization and native background.

Use128endpoints from `NATIVE_RELATION_OUTPUT_FRESH_V1_ROWS.json`, matching
`QUARTIC_GROUP_PORTS_V1_PORTS.pt` and `NATIVE_RELATION_OUTPUT_FRESH_V1_ENDPOINTS.pt`.
These are repeatedly inspected developmental examples, not fresh validation,
training data, a broad FineWeb panel or OOD evidence. No model body forwards.

Use the shared native float32RMS/tanh30tail scorer with exact new star as reference,
duplicate exact star as numerical baseline, rank16star as candidate. Do not supply
the old selected-component reference effects. Removal subtracts the component
from the terminal residual; interchange adds donor-minus-base component writes.

- A: coefficient capture, physical/compiled writes replay<=1e-8; numerical
  baseline effects exactly match reference; all scored outputs finite.
- B: every family swap relative RMS<=0.1, live sign agreement>=0.9,
  at least4live swaps under the unchanged1e-4cutoff.
- C: every family's mean absolute removal-CE disagreement<=0.02nats.
- D: every family's relative residual write error<=0.05.

Report signed mean reference removal effects, live swap magnitude and families
without inferring selective morphology from approximation fidelity. A failure
cannot be repaired by choosing a new rank or dropping examples in this run.
If compact-partner fidelity fails, inspect whether the discrepancy is in scalar
parent values or partner outputs and whether a broad mean effect is hiding
counterfactual differences. The parent is exact here, so no parent refit is needed.

Passing means faithful conditional approximation of this component on this panel.
The goal still requires interpretation/alias checks, held-out and OOD prediction,
selective behavior change, extraction with explicit background and composition.
Literal conditional program price is not a whole-model saving. Budget300seconds.
