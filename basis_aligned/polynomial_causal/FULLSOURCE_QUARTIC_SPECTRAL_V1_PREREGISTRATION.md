# Full-source nested quadratic baseline

11 September2026. The previous compact16Dinput-projected groups accurately approximate their own reference but fail cross-start native replication. Removing that input projection, while retaining frozen output spaces, gives5.66%physical and6–10%swap replica error; removal-CE replication still fails in two families. This experiment attempts a compact implementation of that **different full-source component**, not a repair to the previous22928-float program.

For output writer$w_m$ let$v_m=G_cw_m$, with the centered-unembedding metric$G_c$. Fold it into MLP17:

$$
M_m=\operatorname{sym}\left(L_{17}^\top\operatorname{diag}(D_{17}^\top v_m)R_{17}\right),
\qquad v_m^\top Q(p,p)=p^\top M_mp.
$$

Retain the16largest absolute eigenvalues$\mu_{mj}$ and eigenvectors$a_{mj}$ of each$M_m$. Fold each producer reader through MLP16:

$$
N_{mj}=\lambda\operatorname{sym}\left(L_{16}^\top\operatorname{diag}(D_{16}^\top a_{mj})R_{16}\right).
$$

Retain16largest absolute eigenpairs$(\nu_{mjk},b_{mjk})$ of each$N_{mj}$. Then the compact homogeneous numerator is

$$
\widehat f(x)=\sum_{m=1}^2 w_m\sum_{j=1}^{16}\mu_{mj}
\left[\sum_{k=1}^{16}\nu_{mjk}(b_{mjk}^\top x)^2\right]^2.
$$

Each intermediate now has its own input subspace, rather than sharing one global16Dinput restriction. All coefficients come from weights; no corpus fitting or outcome-guided rank choice. Eigh truncation is optimal in Frobenius norm for each individual symmetric quadratic. It is not optimal for the fully composed quartic norm or native loss. That mismatch is the main falsifier.

Both previously frozen output spaces are tested. A nativefold/producerreader/executable identities<=1e-8. B outer16 with exact nativeproducer has<=10%physicalwrite error against the exact lifted group, eachseed. C outer16/inner16 compact execution has<=10%error, eachseed. Bmiss identifies outerquadratic restriction; Bpass/Cmiss identifies the inner approximation as insufficient. Preserve misses rather than choose ranks from this cache.

Price perseed:589824input-reader floats+512innerweights+32outerweights+2304outputwriter floats=592672floats. Compute512input squares and32outer squares, plus linear reads/reductions/writes. Both source normalizedMLP16input and the actualMLP17input RMS remain native dependencies. Bias and other paths stay outside. This is larger than the projected program because it changes its input coverage. Do not report it as the same-price method.

ManagedGPU only; no bodyforward or newtext,128existingdevelopmental inputstates solely for validation. Cache already used by earlier checks, not fresh/OOD. Budget180seconds. Artifacts must retain source hashes and literal shape counts. If a compact candidate passes, freeze before the existing native-effect scorer and fresh-data validation; no behavioral circuit claim from quadratic eigenvalues alone.
