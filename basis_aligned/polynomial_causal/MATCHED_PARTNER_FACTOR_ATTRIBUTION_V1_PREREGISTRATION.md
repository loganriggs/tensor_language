# Frozen progressive-branch factor attribution

For branch8, coefficient is $c=s t r$, where $r=1/\rho_{17}^2$. Subscripts b/d denote base/donor, bars their arithmetic midpoint, and delta donor minus base. The exact split is

$$\Delta c=\bar r\bar s\Delta t+\bar r\bar t\Delta s+\overline{st}\Delta r.$$

These are partner, shared-parent and normalization changes respectively. The normalized write change is their sum times the frozen output writer. Evaluate the original native final norm and cap at each separately changed state; report nonlinear interaction rather than assuming additive logits. Current branch8 is task-selective on inspected contexts but donor swaps oppose a naive support interpretation.

A: scalar identity<=1e-10 and full swap replay<=1e-5. B: normalization-only margin-effect RMS<=20%of full branch swap RMS in all8progressive cells (ref RMS>=1e-4). C: partner-only margin-effect RMS>=2x parent-only in>=6of8progressive cells. D: sum of three separate effects predicts full swap effect within10%relative RMS in all32cells (report tiny-reference cells rather than exclude). Null: normalization/shared-parent change explains apparent progressive specialization; no privileged partner-input task reading established. All hypotheses fixed before factor-attribution outcomes. Existing1024cached endpoints, no new rows/text fitting. Program price unchanged; this is diagnostic execution of its existing factors.
