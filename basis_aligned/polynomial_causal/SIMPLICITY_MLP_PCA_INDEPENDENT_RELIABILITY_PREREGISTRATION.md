# Rung453 preregistration: independent reliability of complete MLP-PCA consequences

Status: registered after rung451 froze the independent192-document role and rung452 froze uncertainty-separated
comparisons using old outcomes only, but before any candidate or native outcome is computed on the independent role.
GPU execution must use the managed queue; SEALED_CONFIRMATION is forbidden.

## Exact computation

Rebuild the same seven complete mixed104+MLP-PCA candidates and frozen prices as rung450. On the independent role,
measure native, original-native attention16 mean knockout, physical14,984-value MLP16 partner, and every candidate
under unablated/knockout/partner conditions. Run the three conditions in isolated child processes. Report normalized
removal and composition errors over all192 documents and fixed waves `[0,96)` and `[96,192)`.

Compare the new full-role seven-arm error vectors with rung450 using Pearson correlation and mean absolute arm shift.
Pearson is used because this is a continuous-magnitude question, not another arbitrary ranking of near ties. Separately,
score the directions of exactly the13 removal and16 composition pairs frozen by rung452. Also retain the pre-existing
`{8,17}` rank256→384→512 monotone-error sanity check.

## Predictions, bars, and null

- **A — exact/live instrument:** exact candidate maps, complete active replacements, rank104 Q/K, IDs/prices, source,
  row/spec/input hashes, native replay, and mean/intervention counts hold in all three isolated conditions. Expected
  candidate knockout/partner calls are336 each and native knockout/partner calls are48 each. SEALED remains closed.
- **B — continuous magnitudes:** old-versus-independent Pearson is at least`.85` and mean absolute shift is at most
  `.04` for both removal and composition normalized errors.
- **C — pooled separated comparisons:** at least`.85` of the13/16 frozen directions reproduce on all192 documents,
  and the three-arm rank ladder is strictly decreasing for both errors.
- **D — within-new-role reliability:** each fixed96-document wave reproduces at least`.75` of the frozen directions
  for both metrics, and the rank ladder is strictly decreasing for both metrics in both waves.

**Strong null:** any A/inert-effect clause fails; either continuous correlation is negative; or pooled direction
accuracy is below`.60` for either metric. Only A/B/C/D with no null counts MLP-PCA as teaching family2/3. Rung450's
failed raw-total-ranking receipt remains failed either way. This does not fit the consequence predictor, open attention0,
establish semantics/OOD, compress a deployed model, or add deployed values. The23-candidate vocabulary family remains
queued next regardless of this verdict.
