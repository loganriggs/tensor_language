# Attribute the failed instruction-cross control

Frozen 15 September 2026 after the fresh instruction-source result failed its
full-joint work/jobs control in family 1. Reuse all rows and arms without row or
component selection. This opened-panel follow-up asks whether collateral belongs
to the explicit cross term or to the additive routing/value branches.

For each unrelated reader, subtract the additive-without-cross arm from the
instruction-cross and full-cross arms. Normalize those cross-specific RMS effects
by their corresponding paired target cross-effect RMS.

- `pred_a_exact_instrument`: preserve all exact audits within
  $2\times10^{-6}$ over 24 physical batches.
- `pred_b_instruction_cross_selective`: every instruction-cross-specific control
  ratio is at most `.75` in both families.
- `pred_c_full_cross_selective`: every full-cross-specific control ratio is at
  most `.75` in both families.
- `pred_d_failed_reader_mainly_additive`: in failed family 1, the
  additive-without-cross work/jobs RMS is at least `.75` of the full-joint
  work/jobs RMS.

The original full-package selectivity failure remains failed regardless of this
attribution. No fresh-transfer, fitting, update, or quantization claim.
