# Hourly circuit review — 2026-09-10 00:05 UTC

The interpretation targets remain: explicit reads/operations/writes/consumers;
cross-module grouping and within-module splitting; held/OOD prediction;
independent extraction or explicitly priced background; selective manipulation;
composition/reuse; and identification stable to reasonable data and coordinate
changes. The full goal additionally requires reduced structural description,
including every adapter and opaque native weight. None is replaced by a rank,
byte, reconstruction-error or algebra-replay result.

## What changed since23:03

The routed shared-read screen found material joint routing; its crossed-only
explanation failed. The fixed upstream mixed-state removal was invalid under
its first absolute FP32 state bar; a separately registered half-ULP repair
passed with identical outputs and rejected the upstream-variable hypothesis.
At the user's explicit request, the command-mode/matcher path was then paused
in favor of the original handoff's weight and two-circuit mathematics.

A saved MLP11 audit corrected the quadratic coefficient object to its symmetric
part and verified folded attention-writer cross terms. No old transfer verdict
or factor selection was changed. New exact normalized-observable and joint-edit
references pass their planted controls; native initialization remains priced.

The trained full-input MLP1 reader audit is valid: eight forwards/300 sequences,
72.514seconds, all1152 input directions active, smallest/largest singular ratio
.03115, distinct task quadratic spaces. The four-mode reader definition differs
from the old two-mode overlap measurement. All4608 native products have nonzero
coefficients for both tasks; this is implementation co-use, not shared semantics.

A mathematical scope gap was found and repaired: full input support alone does
not exclude a smaller linear-coordinate-plus-norm state. The identity quadratic
is a counterexample. A new zero-forward exact dyadic commutator certificate
closes that larger representation class for MLP1 over all real inputs:
determinant12024 modulo65521, independently replayed in integer arithmetic.
Managed run2.857seconds including1.279seconds CPU verification. No numerical
rank cutoff is used as the impossibility certificate. Nonlinear programs and
restricted input domains remain outside its scope.

The next object is output-side task separation. A minimum-norm joint-reader
right inverse provides local writes that change either existing reader while
preserving the other. On the actual saved readers, condition number1.3985,
worst write-norm increase1.056784 versus unconstrained writes, identities below
8.22e-15. The prospective2x feasibility bar passes. This is an intervention tool,
not a discovered circuit. Seven hook/interchange controls pass; the registered
native test has48 target pairs and16 existing P-control pairs,18 forwards,
no reader fit. Its native runner integration is pending.

## Decision and falsifiers

Stop global linear-input reduction at MLP1; the exact certificate is stronger
than another rank/module sweep. Stop the parked command-matcher continuation.
The next native experiment asks whether removing local reader cross-talk helps
behavioral selectivity after the full nonlinear suffix. Fixed ordinary and dual
single-task edits plus joint/replay arms expose the competing predictions.
Own-task signed effects must retain90% of the ordinary patch, with an ordinary
reference explaining at least1% of the native target contrast. Cross-task effect
RMS must be at most10% of the own-task effect, and specified P collateral must
not worsen, with no top1 flips. A failure prevents promotion; no site/dose scan.

This changes selective-manipulation and within-module-splitting evidence.
It is not a compression sweep. Even a pass would still need reverse interchange,
other-task controls, fresh OOD/removal, and an independently executable shared
computation with a better structural price. Local commuting edits do not imply
additive final effects. All545902902 parameters remain charged.

## Confounds, throughput and systems

Explicitly preserved: opened-versus-fresh data, task-mode rank/noise limitations,
normalization-mediated coupling, dyadic-real versus native-FP32 semantics,
coefficient geometry versus language-data geometry, and local-versus-suffix
selectivity. No native errors are filtered. The native test's temporal P controls
do not cover every iswas collateral family. Dual writes use unchanged reader
coordinates; a small condition number does not identify semantics.

Five managed receipts since23:03: routed read, invalid state, repaired state,
full-input reader, and exact commutator. Their completion gaps from23:03 are
approximately3,11,3,30,7minutes (median7); the30-minute gap includes the user's
math redirection, proofs, constructed controls, saved-weight work and reader
integration. These are receipt intervals, not a measured cognitive-time budget,
and should not be called five identified circuits. Additional CPU receipts
include saved-weight replay, edit-observable closure and dual-writer feasibility.

CIRCUIT_FOCUS: PASS — user-requested mathematics now yields an explicit native
selectivity test; do not remain in lower-bound or coordinate diagnostics afterward.
CEREMONY_BUDGET: PASS on observable workflow — bounded shared controls, existing
loaders/captures, no new framework or full audit rerun; exact thinking/authoring
fractions were not measured and cannot be inferred from GPU seconds.
NOVELTY_LESSON_GATE: PASS — original pilot, existing reader overlap/canonical-angle
work, oblique/dual-reader prior-art search and normalization counterexample checked.

Both Supervisor runners healthy; managed GPU queue empty after the exact
certificate, Claude v451 completed23:53. Preserve concurrent work. The native
interchange primitive is implemented/controlled and its prospective protocol is
written; integrate the existing capture/scorer next. Next hourly review01:05,
next regular mathematical review01:49. Durable goal remains active.
