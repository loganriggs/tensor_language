# Hourly circuit strategy review — 2026-09-06 16:17 UTC

## Circuit interpretation target

The target remains a computation-level circuit: exact reads, computations, writes, routing, held-out sufficiency/necessity, selective manipulation, predictable composition, and reuse across tasks. Descriptive localization, low-rank reconstruction, and answer-margin recovery are intermediate evidence. A completed transparent tensor program must also be literally simpler in stored state, compute, edge set, and description than the transformer computation it replaces.

## What changed since 15:17

The temporal auxiliary path advanced from operation identification to an executable sparse response program. Greedy held-out selection of L11H3, L9H1/H4, L15H5/H1, MLP11, and MLP14 reconstructs 96.65% of heldout A1 and 96.00% of A2 writer response, donorward on every row and with roughly one tenth the best-singleton RMSE. This is the strongest current computation-level circuit, although it remains below Tier 5 because broad OOD manipulation, removal, and reusable compilation are incomplete.

The constrained-DAS red team resolved the apparent optimization paradox. The optimized axis does beat difference-in-means on its registered scalar answer/foil objective (heldout joint squared error 0.00111 versus 0.00196; A2 0.00950 versus 0.02326), without hidden signed cancellation. It loses decisively on centered full-vocabulary causal-effect fidelity (heldout 0.529 versus 0.059; A2 0.448 versus 0.239). Therefore optimization worked; the target was too narrow. The axis is licensed only as a task-margin quotient, not as the full causal state.

Exact gauge-invariant tensor contractions now translate an intervention subspace into reader/writer weight coordinates. On the frozen temporal rank-3 writer subspace, weights alone rank L15H5, L9H1, L11H3, and L9H4 as the top four value readers; all five known readers are at or above the 87.5th percentile. Weight scores prospectively correlate 0.526/0.535 with sealed heldout A1/A2 causal effects. This validates weight translation as a discovery tool rather than a post-hoc story.

The cross-task shared-subspace test reached an instrument correction. Its first run was invalid because it accidentally required every individual row to be native-capable, stricter than the registered 0.85 cell gates. No scientific output was promoted. An independent capability-only manifest reproduced every parent count and froze exact jointly capable IDs, leaving at least seven rows in every task/family/direction stratum. A threshold-identical filtered rerun is queued.

## Portfolio quality and count

The current audit contains 79 legacy canonical registry records, mostly census/localization evidence, and 64 unique latest fast-screen behaviors: 49 screens and 15 nulls. Count is therefore not the bottleneck. The strongest computation-level families are aspectual has/had, tense is/was, and temporal will/had; none yet satisfy the full Tier-5/CF5 standard. Temporal now has the most complete executable multi-module program. Aspectual and tense have an exact shared MLP4→L9H1/H4 operation, but whether they share a value subspace or merely the same tensor machinery with task-typed coordinates is still under test.

## Confounds and alternatives

- DAS target leakage: answer/foil objectives can produce a highly effective quotient axis while discarding most vocabulary-level causal geometry. Future DAS objectives must include centered full-logit effect fidelity or explicitly declare their quotient scope.
- Gauge ambiguity: reader/writer claims must use subspace projectors and invariant contractions, not arbitrary basis-vector coordinates.
- Shared machinery versus shared state: the same head and operation can process distinct task subspaces. Principal angles, cross-projection, causal shared-basis installation, and complement installation are all required.
- Capability selection: exact row IDs are now frozen by a separate no-intervention manifest. No post-outcome threshold changes are allowed.
- Distributed response: the temporal result supports greedy multi-component composition, but selected-module sufficiency does not by itself establish selective removal or rule out omitted parallel paths.

## Throughput audit

Thirty-three commits landed since 15:17 across the shared lanes. This lane completed the DAS target audit, exact reader-operation decomposition, temporal module/head localization, a held-out greedy response program, reusable weight/subspace algebra with tests, prospective weight-reader validation, a portfolio audit, and the cross-task capability repair.

`CIRCUIT_FOCUS: PASS` — work stayed on circuit quality, executable composition, subspace interpretation, and weight-based reader/writer discovery.

`CEREMONY_BUDGET: PASS` — reusable algebra and a capability-only manifest prevented further bespoke threshold repair; GPU executions remained short relative to scientific interpretation.

`NOVELTY_LESSON_GATE: PASS` — the DAS result was not accepted on its original complement justification, gauge dependence was removed, and shared-task claims are being tested at the live internal boundary rather than inferred from shared component names.

## Ranked next moves

1. Finish the capability-filtered L9H1/H4 shared-value test. Promote a shared subspace only if geometry, causal installation, and complement necessity all pass unchanged gates; otherwise record task-typed value axes inside shared machinery.
2. Contract the resulting task/shared/complement projectors through exact V/O and upstream MLP tensors to produce separate reader and writer atlases. Validate newly ranked components causally on sealed rows.
3. Extend the temporal sparse program from sufficiency to selective removal and new-template manipulation, then test whether its rank-3 writer state composes predictably with aspectual/tense subspaces.

This route directly operationalizes subspaces as weights: a causal subspace becomes a projector, the projector becomes exact upstream write and downstream read contractions, and those weight predictions nominate the next causal components rather than merely annotating already-known ones.
