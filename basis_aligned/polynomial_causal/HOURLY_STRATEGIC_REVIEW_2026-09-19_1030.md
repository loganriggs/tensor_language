# Hourly strategic review

ACTIVE_TRACK: CIRCUIT

Actual UTC: **2026-09-19T10:30:09Z**. Next deadline: **2026-09-19T11:30:09Z**. Previous review: [09:27 WEIGHT_FOLDING](HOURLY_STRATEGIC_REVIEW_2026-09-19_0927.md), 1h02m56s old. This is one live review, not an offline backfill. Historical `/workspace` paths were resolved against this checkout. No GPU model, agent, job, queue, service, timer, experiment/result, commit, push, external contact, or `TYPED_FACE_EXTRACTION_V1` implementation was changed. The only CPU consequence was the truthful 10:30 review phase boundary.

The previous hour was declared `WEIGHT_FOLDING`; this review alternates to `CIRCUIT` even though its regional fold was not completed. The prior handoff was the exact coupled residual6-to-head9.8 delta program. Work instead continued the opened subject-number circuit through v506-v539. The folding handoff is preserved below rather than repeated on this circuit hour.

## Seven circuit targets and full goal

1. **Computational specification:** identify what is read, the operation/composition, what is written, and which downstream computations use it.
2. **Cross-boundary grouping and within-module splitting:** group pieces across native modules when a reader treats them as one variable; split a native head/MLP when its pieces serve different computations.
3. **Held-out/OOD prediction:** predict activations and behavioral effects on unseen inputs, task variations, and shifted corpora.
4. **Extraction or sufficiency:** execute the isolated circuit at a declared boundary with every retained port and background named.
5. **Selective manipulation:** removal, swap, or edit changes the intended behavior against matched controls while preserving unrelated behavior and accounting for redundancy/interactions.
6. **Composition and reuse:** shared subcomputations serve multiple tasks/modules and joint behavior follows a tested composition law.
7. **Stable identification:** claimed units survive document/corpus splits, gauges, and fitting restarts, or are defined by downstream operational equivalence.

Goal: a smaller transparent tensor program that **predicts held-out/OOD behavior, is extracted at a declared boundary, supports selective manipulation/removal, and composes/reuses**. Simplicity is the fifth, separately priced property: readers/writers, product nodes, independently stored scalars/bytes, ports/states, edges, and measured execution. Lower error, storage, rank, variance, or CE cannot substitute for a missing behavioral property.

## What changed since 09:27

The number account became more operationally specific, but remained predominantly **edit/fold on opened or replay rows**. Edits v506/v508 refuted fold-nominated MLP12 unit22 and MLP9 unit3428 as causal answer-side relays; v509 instead found two nearly additive population writes. v510-v522 isolated and then correctly demoted a panel-specific MLP17 population: four units causally oppose `they`, but their effect is a frame-induced mixed masculine/singular push absent on natural text, not a reusable number component. This is a useful falsification of a tempting suffix.

Weight templates v523-v530 recover both reader sets from native output/readout directions: all five pronoun heads rank near the top, the agreement template recovers the five verb heads, and block9 heads 9.6/9.7 separate by consumer. Edits v527 reject four template false positives, and the post-noun template promotes second-hop copier 5.3. These are weights-first candidate screens, not an extracted folded path or stable identification.

The two-ended census v531-v538 closes the opened channel picture. The five pronoun readers jointly copy at two sites on both frames; panel-specific extra flow is small relays 5.3/5.6/11.1 plus frame-dependent answer-side MLP polish. The distant-verb write census confirms 11.3, 5.3, 7.8, 13.1, and 9.7 in the same order seen from noun-side value swaps. v539 reports adjacent-site attention `0.313`, five verb writes `0.167`, and rest `0.022`, but its receipt still labels the skipped panel as “distant rows only” and duplicates `text` into `verb_slot`; preserve it as a corrected-run result with schema ambiguity, not clean closure.

Failures/corrections are material: v513 targeted natural rather than panel rows; v514 first replayed natural rows; v521 double-counted an activation buffer; v523 failed when two readers shared a block; v524 needed an integer-key repair; v528 first used unpadded rows; v529 produced NaNs for a zero block-0 direction; and v539 first used 79 distant rows, exceeded its price, then reran on 43 adjacent rows. These faults repeat the prior hour's reference/price/schema lesson.

The work does not declare distinct `(template, noun, context)` cells. The 122 aligned text pairs and 48 panel pairs are reused opened rows; row count is not evidence of independent contexts. Fold, edit, response, and fit remain distinct; fresh, opened, and replay labels cannot be borrowed across receipts.

| Property | Current evidence | Verdict |
|---|---|---|
| Held-out/OOD prediction | Regional intermediate extraction replays on 20 distinct fresh FineWeb cells, but packed sign transfer is `88/102`. Number templates/edit sets have frame checks and natural rows but no frozen untouched-cell or corpus law. | **PARTIAL overall; number route untested** |
| Extraction at a declared boundary | Regional residual6-to-attention8 and norm-closed MLP8 boundaries execute with declared ports. Complete head9.8/suffix and the number token-to-two-readout executor remain open. | **PASS only at intermediate regional boundaries** |
| Selective manipulation/removal | Regional scoped fresh edits beat same-site nulls. Number edits separate reader sets and reject false positives, but use opened rows and lack a fresh three-reader battery plus equal-norm same-site directions at the complete boundary. | **PARTIAL** |
| Composition/reuse | Regional key/value interaction/smaller is `2.3448`; best additive factorial RMS is at least `.5862` of the smaller live single. Number channels are locally additive/reused, but no fresh smallest-piece Mobius test against matched splits exists. | **FAIL overall** |
| Measured simplicity | Regional components have local prices and a 128-product lower-bound witness. Number head/unit counts omit upstream generators, ports, suffix, and deduplicated execution price. | **LOCAL COUNTS ONLY** |

## Highest-value CIRCUIT action

Execute the already-preserved **regional full-suffix five-arm factorial** on structurally varied, genuinely distinct `(template, city pair, endpoint, document)` cells:

1. baseline;
2. frozen head9.8 routing/key delta only;
3. frozen head8.2/MLP8 current-value delta only;
4. both single deltas installed at head9.8 with their explicit product terms suppressed; and
5. the true joint native `QK1*QK2*V` installation.

Freeze target sign and effect predictions before execution. Score the regional reader, at least three unrelated readers, full logits/CE, capability, and every arm's native liveness. Include 16 equal-norm same-site directions and matched random splits of the same live write. The decisive quantity is `joint - additive-without-product`, normalized by the smallest live single and compared with the matched-split distribution. Kill the proposed grouping if either single is dead, target signs fail on distinct cells, collateral exceeds its frozen gate, or the interaction is not specific. A closed Mobius identity, low replay error, or reduced storage is not a pass.

This action directly tests targets 3, 5, and 6 and decides the grouping needed for targets 1/2. It outranks another number census because the regional route is the authority-designated depth target, already has declared intermediate extraction and genuinely distinct-cell evidence, and its unresolved composition failure blocks five-property promotion. The current route survives predictive-state quotients, response bases, and another unit census because those alternatives would begin from opened/post-selected environments rather than test the live coupled object.

**Concrete track connection:** the exact regional fold shows that routing and value meet through seven nonempty native factor products and cannot be represented additively on current cells; the five-arm circuit edit tests whether that algebraic coupling is causally specific. Conversely, number edits show why the next fold must group by downstream consumer and position: one hub feeds almost disjoint pronoun/verb head sets, while fold-large answer units can vanish under edit.

**WEIGHT_FOLDING handoff:** next folding hour must resume the exact `residual6 + token IDs -> attention7 -> MLP7/head8.2 -> norm-closed MLP8 -> head9.8 output delta` replay, retaining every ordered source term and all seven nonempty `QK1*QK2*V` delta products, with a deduplicated program price.

## Confounds and alternatives

The factorial must preserve baseline subtraction and replace-versus-zero semantics; source ordering; frame/position/city/endpoint mixing; FineWeb/Pile leakage; shared token/document difficulty; every RMS denominator, rounded rotary phase, causal mask, learned re-entry, attention product, suffix nonlinearity, final RMS and softcap; live-delta tripwires; precision floors; and post-selection. Structural variants must change the colon/quote/boundary rather than add rows with the same template. A forward response census already exists and rules out treating MLP17's opposing response as the suffix mechanism.

Ranked alternatives: (1) chosen regional factorial; kill on dead singles, sign/collateral failure, or nonspecific interaction. (2) Freeze the number consumer split on genuinely new context cells with a three-reader battery and matched head splits; kill if reader identity or selectivity fails. (3) Build a predictive-state causal quotient only after multiple fresh response environments exist; otherwise it is another fit basis. (4) Signed composition algebra or a lower bound is useful only if it changes the live intervention decision. More opened unit/head censuses are demoted.

## Registry and dossier audit

`CIRCUIT_REGISTRY.md` still links the older four-head subject-number record and does not consolidate the five pronoun heads, corrected five verb heads, second-hop 4.5->5.3 chain, position-specific 701/2059 ending, template false positives, or v506-v539 corrections. `COMPUTATION_PATH_REGISTRY.md` has current `PATH-REGIONAL-CITY-001` and old `PATH-SUBJECT-001`, but no canonical current number path. `CIRCUIT_GRAPH_REGISTRY_V1.md` remains a 49-package inventory, omits `city_mlp8_norm_closed_v1` and the number route, and its two `four_trait_verified/next gap: none` rows do not price simplicity. MLP8/9/12 and MLP17 dossiers do not contain v506-v539; the live facts sit mainly in `in_depth_circuit_number.md` and `SHARED_READOUT_COMPONENTS.md`. Primary ownership of `TYPED_FACE_EXTRACTION_V1` was respected.

The missing cross-link is still one canonical **`PATH-SUBJECT-NUMBER-002`** joining the current circuit record, v338-v539 receipts/corrections, five pronoun heads, five verb heads, copier 4.5->5.3, hub 829/953/1030, detector 701/2059, panel-only MLP17 population, module dossiers, opened-row status, and missing fresh/extraction/composition/simplicity gates. It must mark v513 and the first v514/v521/v523/v524/v528/v539 runs invalid or superseded, and flag v539's stale schema labels. Concurrent registry and narrative work was preserved; no shared file was rewritten.

## PAST_HOUR_TIMING

Window: **2026-09-19T09:27:13Z-10:30:09Z**. Thirty-four successful receipts v506-v539 finished from 09:27:37 to 10:27:44. Their declared internal serial runtimes sum to **69.464s**, median **1.870s**, range **1.670-4.170s**, with **483 declared forwards**. Successful finish cadence is **109.3s per candidate**. This cadence includes unmeasured design, authoring, interpretation, queue gaps, and repairs; it is not active-labor time, and parallel durations were not summed.

- **Design:** unknown; predictions exist, but no science phase boundaries were recorded.
- **Implementation/data/indexing:** unknown. Thirty-four new sibling candidates and repeated derived-script faults show substantial authoring, but commit gaps are not labor time.
- **Validation:** successful serial runtime is included above. Authoritative runlogs additionally show failed/repeated executions for v514, v521, v523, v524, v528, and v539; active repair time is unknown.
- **Model execution:** 69.464s successful internal runtime. Runner timestamps show successful jobs usually occupied 2-5s. Failed/repeated jobs add approximately 14s of runner occupancy (not summed as active labor); scheduled canaries are separate.
- **Interpretation:** unknown; board and two live narratives were updated after most results, without phase marks.
- **Documentation/review:** unknown. This review began at the truthful 10:30 boundary; no duration is inferred.
- **Idle/blocked:** unknown. The longest successful-receipt gaps around v523/v524/v539 contain documented crashes/repairs; they cannot be called idle.

The newest work-date phase log is `RESEARCH_PHASES_2026-09-19.jsonl`. Before this review it contained only six sparse hourly review boundaries from 04:04 through 09:27 and covered none of v506-v539's design, implementation, validation, execution, interpretation, or publication. The helper appended one truthful **10:30:09 review** boundary. Sparse marks do not imply uninterrupted labor and no category was backfilled.

## PROCESS_IMPROVEMENT

Ranked by likely time saved versus cost:

1. **Declarative number-circuit runner** with one row builder, intervention schema, multi-reader scorer, actual-batch price calculation, semantic slot assertions, and receipt writer: likely saves roughly 45-75s of the observed ~109s cadence and would have prevented most v513/v514/v521/v523/v524/v528/v539 faults; medium implementation/equivalence-test cost.
2. **Fail-fast receipt/run validator** requiring `started_utc`, `finished_utc`, evidence/freshness, distinct cell IDs, semantic row filters, nonzero finite templates, and nonduplicated slot payloads: low cost and directly catches stale v539 labels and NaNs.
3. **Canonical `PATH-SUBJECT-NUMBER-002`** after the live sequence freezes: likely saves 5-10 minutes per handoff; low documentation cost.

No process code was changed in this bounded review. The best repair touches the actively changing number runner family and needs equivalence tests across multiple intervention types; a tiny ad hoc checker would add another bespoke layer without validating semantics. The primary scientific action is on the regional route, and concurrent files remained live through v539. The truthful phase mark was the only safe CPU-side organizational consequence.

## Explicit verdicts

- `TRACK_ALTERNATION: PASS` — previous `WEIGHT_FOLDING`; current `CIRCUIT`.
- `TRACK_PROGRESS: FAIL (TRACK DRIFT)` — v506-v539 materially advanced an opened circuit account and weights-first reader screen, but did not produce the declared coupled regional folded-path replay, exact native endpoint program, or path dossier.
- `CEREMONY_BUDGET: PASS WITH MEASUREMENT CAVEAT` — 34 scientific receipts dominate visible output and successful compute is cheap, but design/implementation/validation/review minutes are uninstrumented, so relative active-time shares are unknown.
- `NOVELTY_LESSON_GATE: FAIL` — prior circuit facts were applied, yet repeated sibling-runner faults, stale receipt labels, and the still-missing canonical number path show that the shared-helper and registry lessons were not repaired.

**Highest-value action now:** run the frozen regional five-arm full-suffix factorial on structurally varied distinct cells with full `QK1*QK2*V` coupling, three-reader/full-logit controls, equal-norm directions, matched random splits, and interaction normalized by the smallest live single.
