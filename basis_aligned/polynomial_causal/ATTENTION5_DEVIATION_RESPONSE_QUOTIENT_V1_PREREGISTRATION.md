# ATTENTION5_DEVIATION_RESPONSE_QUOTIENT_V1 preregistration

The frozen heads6/7/3 transfer test found sufficient but non-exclusive recovery:
the complementary six heads also pass50% recovery on natural text and code. Test
whether all nine exact mean-deviation interventions instead share a compact
downstream operational response. On the same24 untouched final-natural documents
and24 code documents, run mean-only, all-nine, native, and nine singleton arms.
For every arm retain its corpus-mean final-vocabulary logit vector and document
cross-entropy. Natural text is discovery and code is held out.

- **A — closure:** hashes/manual-model checks hold and all-nine restoration
  matches native CE within.005 nat on both corpora.
- **B — discovery quotient:** the top three right-singular directions explain at
  least80% of the nine natural singleton-response matrix's squared energy.
- **C — held-out quotient:** projecting the code singleton-response matrix onto
  the frozen natural rank-three response subspace has at most50% relative error.
- **D — response composition:** summing the nine separately measured mean-logit
  responses predicts the jointly restored all-head mean-logit response within
  25% relative error on each corpus.
- **E — stable participation:** median paired natural/code cosine across the nine
  head response vectors is at least.5, and at least seven of nine singleton
  deviations have positive CE recovery on each corpus.

An all-pass result identifies a three-coordinate downstream response quotient,
not a three-head circuit. Failure rejects a shared low-dimensional operational
class at this aggregation and closes attention5 mean-deviation factorization
until a task-defined causal endpoint supplies a more specific boundary. No rank
is selected after outcomes, and no semantic, compression, source, or installed
surrogate claim follows.
