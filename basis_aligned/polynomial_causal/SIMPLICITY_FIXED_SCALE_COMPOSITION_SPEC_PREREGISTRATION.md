# Rung455 preregistration: fixed-scale composition specification audit

Status: registered after rung454's composition strong null and before any independent vocabulary outcome. CPU-only
analysis of already-open consequence bundles; no model, row cache, or SEALED_CONFIRMATION access.

## Claim and exact computation

Rung454's registered composition score divides the interaction

`I(P,Q) = L_PQ - L_P - L_Q + L_N`

by the candidate-dependent additive magnitude

`A(P,Q) = ||(L_P-L_N) + (L_Q-L_N)||_2`.

The stored traces show that `A(P,Q)` can shrink faster with candidate quality than `||I(P,Q)||_2`, reversing a rank
ordering. This rung hash-pins the complete MLP0, MLP-PCA, and vocabulary result/bundle pairs; reconstructs every old
registered ratio; and freezes the candidate-independent companion

`E_comp_fixed(P) = ||I(P,Q)||_2 / ||L_Q-L_N||_2`.

Store the raw interaction norm and RMS, moving denominator, registered ratio reconstruction, fixed partner-effect
denominator, fixed-scale score, and descriptive adjacent-ladder direction receipts for all three already-open families.
The MLP0 ladder is ranks256→384→448→512→640; the MLP-PCA ladder is the layers8+17 ranks256→384→512; vocabulary
uses rung454's14 pre-registered edges. These direction summaries are post-hoc diagnostics, not prospective predicates.

## Predictions and strong null

- **A:** exact hashes, schemas, family names, arm IDs, and token counts hold.
- **B:** compact stored traces reconstruct every registered full composition error within `2e-4`.
- **C:** each family has one positive finite partner-effect denominator shared exactly by all its arms; every serialized
  value is finite; and every fixed score equals raw interaction norm divided by that common denominator.
- **D:** the script loads no model, row cache, independent role, or SEALED_CONFIRMATION outcome and writes a create-only
  specification.

**Strong null:** any A/B/C/D clause fails, an output namespace already exists, or any new outcome is accessed. Passing
only freezes a versioned computational definition and licenses a separately preregistered independent vocabulary test.
It does not repair rung454, count vocabulary as family3, change the original family count2/3, fit a consequence
predictor, establish semantics, or open attention0.
