# Raw mixed-input removal with live RMS

Authority: original bilinear handoff and pilot; predecessor normalization-origin partition.
All 32 worlds and complete five-factor grids in THIRD_NOUN_CAUSAL_ROLE_ALIGNMENT_V1.
Q is the conditional object-number by attractor-kind interaction projector.
Capture native raw layer9 attention input x, normalized u and common write C.
Set xprime=(x.double()-Q*x.double()).to(x.dtype); recompute uprime using native
functional.rms_norm with native dtype/epsilon. Build Cprime=1.65625 W_O P0 W_V Q uprime
at common to/action positions. Preserve native mean routing P0 and background B=A-C.
Install B+Cprime, then recompute the full native suffix. This path intervention changes
the partial value producer only; it is not a global residual intervention changing QK.

Three arms: full C removal (B), raw-input intervention (B+Cprime), compiler identity
(B+Ccompiled). Four native/local captures plus six intervention forwards per world:
320 forwards/5120 sequences, zero fits, 600s cap. Existing token/causal geometry and
native factor controls reused. No head, rank, gain, row or threshold selection.

A instrument: all native factor controls, bitwise RMS replay and incoming write,
raw mixed removal residual <=1e-6 relative to native x; normalized native compiler
bridge <=1e-4 relative to observed C. Native/full-removal replay parent common suffix;
identity replay native, all maxabs <=1e-3 and relative <=1e-5. Finite, hook cleanup,
exact forward counts. Zero scientific denominator fails rather than passing vacuously.
B inherited necessity for this component: raw-input intervention effect agrees with
full C removal within .10 relative, BOTH Q correct-margin and centered Q three-readers,
EVERY world. Opposing prediction C: regeneration retains the original component effect;
raw-input intervention effect <=.10 of full C removal, BOTH readouts, EVERY world.
Intermediate results reject both universal rules. Report per-layout passes, full table
spill, Cprime/C norm as diagnostics; no promotion from magnitude alone. Background and
native four-corner inputs remain required. All545902902 weights retained, saving0.
