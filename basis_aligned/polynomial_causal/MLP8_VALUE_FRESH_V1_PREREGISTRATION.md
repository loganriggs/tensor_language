# Frozen four-mode value path: new context confirmation

12 September2026. No weight fitting or model-score row filtering.72 new full prefixes: three sentence constructions (fronted city, near-quote city, distant city), two city pairs Nottingham/Detroit and Sheffield/Baltimore, six previously tested spelling endpoints. Cities may appear in earlier broader-component panels; full prefixes are new among inspected root row files. Every pair differs in exactly one city token and uses a matched source position.24 prior city prefixes supply native/removal/donor replay anchors. The deterministic builder, rows and frozen dependencies are bound before execution.

Arms: native; remove frozen four-mode MLP8 quadratic contribution only from selected head9.8 value edge; donate paired phi4 numerator with recipient routing and recipient normalization. Native MLP output, QK uses and background otherwise retained. Reuses the previous transfer executor's intervention and exact hooks. Native donors cached before any edits.288 full forwards;180second managed cap. No newline rerun; previous64-prefix preservation evidence remains separately scoped.

A: old24 three-arm replay relative error<=1e-4; selfdonor difference<=1e-8. Each newfamily native paired contrast mean>=.2 and>=10/12 positive pairs.
B: A and eachnewfamily removal coverage>=10% native contrast,>=10/12 positive paired effects; unrelated mean absolute margin change<=.5 target mean absolute change.
C: B and eachnewfamily donor transfer>=10%,>=20/24correct directions; unrelated mean absolute change<=.5 target change.

Null: the frozen path does not support these new sentence constructions at the registered effect size. Keep all row and family failures; capability failure limits interpretation but doesnot remove the row or repair B/C. No new rank, threshold relaxation, semantic writer-role claim or corpus/pretraining OOD claim. Opposing prediction: if the path is tied to earlier constructions, at least one displaced-cue family loses transfer despite native capability. Next action follows observed effects, not automatic promotion.
