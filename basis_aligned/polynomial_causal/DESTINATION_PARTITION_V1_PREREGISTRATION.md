# Framing/clause destinations versus matched random partitions

Use frozen CPU_CONTROL masks,40opened prospective sequences/20context cells.
Same half-strength head8 typed face and fixed odd-value descendant. Split its
source destinations into before-clause framing versus final clause. Nine unique
random partitions match framing cardinality per context; exclude semantic and
label-swapped duplicates. No fitting or score-driven partition change.

Arms: native,full,semantic A/B,nine null A/B =22arms*40=880forwards,300seconds.
The shared executor recomputes the native suffix. All fragments use the same
city inputs and weights; only disjoint destination masks change. Intrinsic head9
write sums should close, so behavioral interaction measures suffix curvature.

pred_a: native/full margins replay saved native/midpoint<=1e-5maxabs; every
partition's two head9 writes sum to full within1e-5relative; finite;880forwards.
pred_b: BOTH semantic pieces targetRMS>=1e-5 AND>=.05fullRMS in every family.
A failure makes claims of two live components inconclusive, without dropping it.
pred_c: per-family semantic interaction norm (full-A-B+native) <=.25times
smaller-single effect norm AND<=.10times full effect norm.
pred_d: semantic worst-family interaction/smaller-single <=.5median of the nine
random worst-family ratios AND below at least8/9random ratios.
Null: nearly linear suffix makes every partition additive, or semantic split is
worse than random. Random partitions are the same total write, not new tasks.

Report all target/control piece norms and interaction statistics. Per-piece
selectivity is untested; whole-path selective results do not transfer by naming
parts. Software composition passed separately; no causal composition promotion
unless all these gates hold and later fresh confirmation succeeds. Earlier
input-branch and physical-product split failures remain unchanged.
