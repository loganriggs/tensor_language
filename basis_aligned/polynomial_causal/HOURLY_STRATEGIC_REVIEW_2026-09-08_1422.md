# Hourly strategic circuit review — 2026-09-08 14:22 UTC

## Circuit interpretation targets

1. **Computational specification:** identify what is read, computed, written, and consumed.
2. **Cross-boundary grouping and within-module splitting:** group operationally equivalent
   pieces across modules and split native heads/MLPs when downstream use differs.
3. **Held-out/OOD prediction:** predict causal effects on unseen text without reselection.
4. **Extraction/sufficiency:** execute an isolated circuit or an explicit interface plus background.
5. **Selective manipulation:** add, remove, swap, or edit the intended behavior with low collateral.
6. **Composition/reuse:** predict joint behavior and reuse shared subcomputations across tasks.
7. **Stable identification:** survive corpus shifts and legal gauges, or be defined by operational
   equivalence under downstream readers.

The full objective remains a smaller transparent tensor program that is jointly predictive on
fresh/OOD text, composable, selectively manipulable, and simpler under literal storage, compute,
edge, state, and program prices. The present P7 plus residual-identity interface is an executable
OOD-stable subcircuit boundary; it is not yet a complete computation or an adopted smaller model.

## What changed since 13:22

One new circuit null receipt landed. The P7 identity-background leave-one-out run completed at
13:43:38 UTC after about four seconds of its own GPU time. Authority and selectivity passed, but
every `without_A11/M11/M12/M15/M13/M16/M10` arm still recovered exactly `1.0` on both OOD phases.
This falsifies stable single-module necessity in the fully clamped sequential replacement and
closes both prospectively conditional A11/M11 split executors. The atomic binder independently
created no bindings. The result means later writer clamps can absorb one omitted predecessor; it
does not show that the live native modules are inert.

The registered redundancy trigger therefore opened a different intervention object. Prior-art
search, preregistration, implementation, focused tests, model-free dry run, and managed enqueue
for the exact seven-player source-conditioned live-module effect game completed by 13:58. It
enumerates every P7 subset twice: source absent as an executed no-op, and source present with
enabled modules recomputing live while the complement clamps native outputs. Exact Möbius,
Shapley, and pair-interaction transforms allocate the source effect without fitting a surrogate.
The job is queued once at reviewed SHA `b53113fe...` behind the verified-live v260 PID `1243778`,
which has completed 10/15 members. Both Supervisor runners remain healthy.

An independent nine-player v271 receipt was also compared rather than treated as equivalent
evidence. Its Shapley efficiency is exact, but most of its effect is direct/unlisted and its
registered pair-redundancy prediction fails. It donor-patches heads and uses a different pair
statistic, so it supplies a prior about missing background carriage without changing the frozen
P7 thresholds.

## Confound audit

- **Baseline subtraction and frame mixing:** source-present and source-absent arms traverse the
  identical hooks. Disabled P7 modules clamp native outputs at their own causal stages; enabled
  modules remain live. All players are downstream of the block-10 source.
- **Nonlinear composition:** the game is vector-valued over registered answer margins. Möbius and
  Shapley transforms are exact for the finite intervention table; pair indices aggregate higher
  orders and are operational dependence, not directed edges.
- **Shared difficulty, leakage, and post-selection:** P7 order, all 128 subsets, OOD rows, signs,
  and `.03` bars were frozen before execution. Newly observed modules or pairs will require a
  separate physical interchange/removal test rather than selection on this OOD bank.
- **Dead knobs, precision, and coverage:** all 128 source-absent no-op arms, all-live parent replay,
  one-call hook coverage, Möbius reconstruction, Shapley efficiency, and exact forward price are
  hard gates.
- **DAS memorization:** no projector or trainable subspace is used. Regularized DAS remains demoted
  until it has sealed optimization/evaluation sets, a unit-dose orthogonal intervention, noise or
  KL/Jacobian control, multiple restarts, and reader/writer validation.
- **Surrogate error:** exact enumeration is affordable at seven players. Tensor-network Shapley
  surrogates would add approximation and selection error without reducing the current bottleneck.

## Alternatives, ranked next moves, and kill evidence

1. **Consume the exact live-module game.** This directly tests specification, grouping,
   manipulation, and stable OOD identification. Kill a P7 mediation claim if no module has stable
   signed Shapley credit above `.03`; kill interaction language if no pair is stable on both phases.
2. **Physical reader-input removal plus module-output rescue for a passing module/pair.** Clamp the
   nominated module's normalized reader input to the source-absent value, then reinstall its
   source-present output. Loss followed by selective rescue would support the directed
   source-to-module edge. Kill it on failed parent replay, unstable OOD direction, or collateral.
3. **Trace the direct/unmasked background route.** If P7 credits are null while `E(empty)` is large,
   search non-P7 downstream modules with a cheap source-effect atlas; do not widen P7 post hoc.
4. **Backward greedy native replacement pruning.** This could find a smaller sufficient clamp set,
   but it answers replacement minimality rather than live source mediation and risks OOD selection;
   retain it only if the live game shows distributed stable credit without a manageable pair.
5. **Regularized DAS or tensor-network surrogate.** Revive only when exact physical tests become
   combinatorially unaffordable and a sealed surrogate/projector error bound beats direct screening.

The current route remains highest information because the LOO null specifically exposed a
sequential-overwrite ambiguity that the source-conditioned live game removes without fitting.
Every successor changes a circuit target through physical causal loss/rescue, not rank or variance.

## Throughput and systems audit

The interval produced one measured circuit null, below the nominal six-per-hour target. Its own
execution took about four seconds after a 21-minute verified wait behind v258. Scientific
interpretation plus design took about 18 minutes; prior-art and authoring about 12 minutes;
focused validation and handoff about 10 minutes; the remaining wall time overlapped v260's live
managed computation. There was no idle queue gap after the effect-game runner became eligible.
The 258-forward exhaustive game is a promoted redundancy audit with a concrete high-risk boundary,
not a basic screen; exact enumeration prevents surrogate overfit and costs less than another
round of ambiguous sequential deletions.

`CIRCUIT_FOCUS: PASS` — the hour closed an invalid module-necessity interpretation and began the
exact live-routing discriminator.

`CEREMONY_BUDGET: PASS` — the preceding failure's forced repair was completed: a measured receipt
was consumed, conditional branches were closed, and the evidence-selected experiment was actually
enqueued. No new generic framework was opened.

`NOVELTY_LESSON_GATE: PASS` — existing factorials, Möbius/Shapley utilities, the v271 receipt, and
the known global-frame/post-selection failures were checked; the new object changes replacement
clamps to source-conditioned live recomputation.

Concrete continuation: keep the exact hash queued once behind stable live PID `1243778`. On landing,
score A--E exactly and immediately preregister the outcome-selected physical reader-edge test or
direct-background atlas; do not end at the attribution receipt.
