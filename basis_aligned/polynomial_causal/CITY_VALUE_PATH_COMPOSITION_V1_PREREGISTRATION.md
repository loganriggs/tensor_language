# Composition of direct and MLP8-mediated value paths

The direct attention8 edit and its induced MLP8 response are distinct native computations feeding the same head9.8 current-value reader. Both pass opened causal magnitude screens; MLP8-mediated correction additionally passes same-boundary nulls and fresh mediator confirmation. Test their joint intervention before treating them as independently reusable pieces. This is not a new arbitrary source partition or a reinterpretation of the failed head8 key/value split.

Use the original opened FineWeb40sequence panel and frozen CITY_FINEWEB_MLP8_VALUE_FOLD_V1 six-piece artifacts. Keep exact native head8cityswap as background. A=direct value response(piece0); B=MLP8 response(sum pieces1:5). Block9 normalizer term remains outside both. At head9.8 value subtract A, B, or A+B with both keys/otherheads/fullsuffix unchanged. Addition at this value boundary is exact; suffix interaction remains testable.

5arms:native,swap,swap−B,swap−A,swap−(A+B). 200sequence-equivalent forwards/90blockcalls,2CPUthreads,180second cap.
pred_a:native/swap/B/A replay CITY_FINEWEB_VALUE_MEDIATION_V1 indices0,1,2,4 abs<=1e-4 ANDrel<=1e-5;offmask0;otherheadsunchanged;subtraction<=1e-5relative;finite/counts/CPU.
pred_b:interaction ||eJoint−eA−eB||<=.35min(||eA||,||eB||), where e are changes relative to fullswap, on all240target probes.
pred_c:both single target RMS>=.10joint RMS, and interaction<=.35smaller single separately in frozen reversed-document and remaining-document groups.

Report all four controls and document-level interactions and signs. No headcross arm is needed because value insertion is linear with fixed keys; this does not assume the downstream model linear. If any composition gate fails retain coupled paths and do not mine further partitions. If passes, require registered same-full-write random-split specificity and fresh jointly installed manipulation before independent composition promotion. Existing original operator direction failures and nonuniform subgroup selectivity remain.
