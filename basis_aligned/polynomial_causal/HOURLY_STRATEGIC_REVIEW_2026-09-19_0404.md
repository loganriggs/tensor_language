# Hourly strategic review

ACTIVE_TRACK: CIRCUIT

Actual UTC: **2026-09-19T04:04:13Z**. Next deadline: **2026-09-19T05:04:13Z**. Previous Codex review: [2026-09-19 03:00 WEIGHT_FOLDING](HOURLY_STRATEGIC_REVIEW_2026-09-19_0300.md), 1h03m55s old at this review's clock. This is one current review, not a backfill. Historical `/workspace` paths were resolved against `/workspace/tensor_language`. No GPU model was loaded; no job, timer, service, commit, push, or external contact was made; `TYPED_FACE_EXTRACTION_V1` and concurrent experiment/results were not modified.

The declared folding hour did **not** build the proposed native residual6-to-head9.8 operator. It did produce a generic CPU Boolean-Mobius identity/falsifier at 03:06, but that is not a native folded-path receipt. The actual lane remained productive circuit work: MLP1's functional role and readers were tested, its number-specific interpretation was rejected, and the number chain was traced from token identity into context-sensitive lexical detectors. Alternation therefore proceeds to `CIRCUIT`; the unfinished fold is preserved for its next folding hour rather than repeated now.

## Seven circuit targets and full goal

1. **Computational specification:** identify what is read, the operation/composition, the write, and downstream users.
2. **Cross-boundary grouping and within-module splitting:** group pieces across native modules when a reader treats them as one variable, and split a native module when its pieces serve different computations.
3. **Held-out and OOD prediction:** predict activations and behavioral effects on unseen inputs, task variants, and shifted corpora.
4. **Extraction or sufficiency:** execute the circuit at a declared boundary with all retained background and native ports stated.
5. **Selective manipulation:** removal, swap, or edit changes the intended behavior against matched controls while preserving unrelated behavior and accounting for redundancy/interactions.
6. **Composition and reuse:** shared subcomputations serve multiple tasks/modules and their joint behavior follows a tested composition law.
7. **Stable identification:** units survive document/corpus splits, gauges, and restarts, or are defined by downstream operational equivalence.

Goal: a smaller transparent tensor program that predicts held-out and OOD behavior, is extracted at a declared boundary, supports selective manipulation/removal, and composes/reuses. Simplicity is priced separately in stored scalars/bytes, native ports and state, readers, products, writers, nodes, edges, and execution. Lower reconstruction error, storage, or CE alone cannot replace a missing behavioral trait. This is the five-property standard: **simple, predicts OOD, extracted, selective, and composes**.

## Progress since 03:00 and five-property score

Evidence types remain separate. v310-v337 comprise folds, responses and edits; none is a fitted mechanism. Natural positions and vocabulary rows are not automatically independent context cells, and the selected MLP1 units make v309-v313 opened with respect to unit choice.

| Property | Current evidence | Verdict |
|---|---|---|
| Held-out/OOD prediction | The regional packed removal still passes 20 fresh Pile documents but fails FineWeb behavioral direction (`88/102 < .90`); its exact residual6-to-attention8 write replays on 20 distinct fresh FineWeb documents. MLP1's cancellation replicates on 2,944 natural positions, while v321 preserves the alphabetic/punctuation ordering but falsifies the filler-derived numeric ordering. v333-v337 broaden token/context panels, but do not provide fresh document-level behavioral prediction. | **PARTIAL** |
| Extraction at a declared boundary | The regional residual6+tokens-to-attention8 write remains exact near `2e-6`, and the norm-closed MLP8 value mediator replays installed readouts while retaining native `z8`, an intervention array, token IDs, and an external suffix. MLP1/number has exact token-to-unit folds and named response paths but no standalone package or closed behavioral suffix. | **PASS at intermediate regional boundaries; not end-to-end** |
| Selective manipulation/removal | Regional fresh Pile removal and mediated correction beat same-site nulls, but FineWeb direction reversals remain. Restoring MLP1 units 3289/624 costs `+.059` nats on natural text versus random-pair effects around `.0002`; however v328 shows MLP1 unconditioning is generic (`0.47`-nat final-token KL, 15% top-1 agreement, gender movement `0.26x` the number margin), so it is not a selective number component. | **PARTIAL; MLP1 number-selectivity rejected** |
| Composition/reuse | Regional key/value interchange remains nonadditive (`interaction/smaller=2.3448`) and direct/value composition fails its reversed subgroup. MLP1-to-block2 splits are locally additive at one boundary, but attention2/3 compensate the MLP routes and no frozen cross-task joint reuse test exists. Exact Mobius closure is only accounting. | **FAIL for tested regional splits; otherwise unestablished** |
| Measured simplicity | Packed regional attention7 saves 884,736 FP32 values locally; the exact norm-closed mediator is an interface closure, not compression. MLP1's two-unit head is small but the valuable context-conditioned tail is dense and restoring the whole token table costs `.70` nats, worse than removing MLP1 (`.41`). No matched-effect executable price supports adoption. | **LOCAL PRICES ONLY** |

Specific changes and failures:

- **edit, natural/opened:** v310-v314 establish that undoing the two-unit cancellation costs `.059` nats, but the linear whole-layer extrapolation fails: the table everywhere costs `.70` nats and the tail carries `.47` nats of value. Attention0/1 self-only makes MLP1 almost exactly the table and costs `.81` nats.
- **response/edit, opened:** v315's block patch-back localizer is invalid because recurrence makes shares sum to `7.5`; path-restricted v316-v317 instead places 45% of the first-order harm at block2, split `.35` through MLP2 and `.08` through attention2. v318-v321 show a weaker MLP2 lookup law and preserve the failed direction/numeric predictions.
- **edit/response, opened pronoun rows:** v322-v328 show that unconditioning MLP1 drops the number margin by 32%, but not through named units 3465/493; blocks2-4 carry the effect, attention compensates MLPs, and ten MLP3 units form a small generic leak channel rather than a number component. The nonselectivity result closes MLP1 as a number-specific circuit.
- **fold/response screens:** v329-v337 identify unit 3465 as a lexical plural-noun-sense detector rather than an `-s`, pronoun, or quantity detector. Four lower chain units transfer to 256 vocabulary pairs; units 953/1030 do not. On pronoun rows the units separate number at the noun, not the answer. v336-v337 show that the immediately preceding word, not context length, changes sharpness: determiners/`of` strengthen some readers, coordinators or numeric/demonstrative contexts can suppress or reverse them. v337 finished at 04:01:23 with only 2/5 registered predictions; it is read as a constraint, not a repaired success.

## Highest-value CIRCUIT action

Run one frozen **regional full-suffix five-arm factorial** on structurally varied, genuinely new context cells. Install the corrected exact residual6-to-attention8 city write before the existing coupled/norm-closed MLP8 value route, keep the complete head9.8 `QK1*QK2*V` product native, and compare: baseline, upstream-write only, value-response only, the additive-without-product construction, and the true joint intervention. Use colon-removed, quote-removed, and moved-quote templates plus new city/endpoints; report distinct `(template, city-pair)` cells separately from probe rows. Freeze gates before outcomes: signed target-effect prediction, at least three unrelated readers, 16 equal-norm same-site random directions, and Mobius interaction divided by the smaller live single with matched random splits.

This action can change targets 1, 2, 3, 5 and 6. Kill the proposed grouping if target prediction fails, collateral exceeds its preregistered gate, either single is dead, or joint interaction is not small and more specific than matched random splits. It outranks another MLP1/number screen because v328 already rejects MLP1 conditioning as number-selective, while the regional route is the designated depth path with fresh causal evidence and an unresolved composition boundary.

Alternatives: (2) a fresh replacement-defined MLP1 top-unit test with several unrelated behaviors could refine its generic role, but cannot rescue number selectivity; kill if it does not reproduce the frozen census effect across distinct documents. (3) a noun-boundary circuit screen comparing determiners, coordinators and numeric/demonstrative predecessors could identify the context reader into units 3465/493/1036/829, but first requires fresh structural cells and matched lexical controls; kill if word identity rather than grammatical relation predicts the change. The regional factorial remains higher value.

Concrete track connection: the regional circuit's failed key/value additivity requires the next folding hour to retain all seven nonempty head9.8 factor-delta products; conversely, MLP1's zero-versus-restore reversal shows that a fold must preserve the replacement-defined net unit write, including self and both ordered token/context cross terms, rather than treating a large isolated term as the causal component.

**WEIGHT_FOLDING handoff:** resume the exact residual6-to-head9.8 output-write closure with native QK1/QK2/V factors, RMS/rotary semantics, norm-closed MLP8 background, every permitted ordered residual-source term and all seven nonempty factor-delta products; require `<=1e-5` exact replay before any omission or suffix claim.

## Confounds, registries, and direction check

Audit baseline subtraction and zero-versus-replacement counterfactuals; frame/position and immediately-preceding-token mixing; RMS, bilinear, full-head and suffix nonlinear composition; shared token/document difficulty; FineWeb/Pile and vocabulary-selection leakage; dead arms/tripwires; FP32 floors; and post-selection. The 2,944 natural positions are not 2,944 independent documents. Vocabulary pair count is not context-cell count. v337's 12 preceding frames by two 256-token classes is a factorial token panel, not twelve fresh behavioral documents.

The regional path remains highest-information after alternatives because it has an exact earlier boundary, a forward response census, fresh edits, and a specific failed composition boundary. MLP1/number produced valuable negative specificity evidence but opening more screens there would violate the depth preference.

Registry/dossier audit:

- `CIRCUIT_GRAPH_REGISTRY_V1.md` still inventories 49 packages/44 manifests/33 boundaries and omits the untracked `city_mlp8_norm_closed_v1` package. It is a generated four-trait inventory, not a five-property certificate.
- `COMPUTATION_PATH_REGISTRY.md` has a concurrent uncommitted `PATH-REGIONAL-CITY-001` entry linking the corrected upstream fold and three older packages, but it does not link the norm-closed MLP8 package or the complete head9.8 folding handoff. It has no MLP1/number computation-path entry.
- `CIRCUIT_REGISTRY.md`, `MODULE_DOSSIERS.md`, and `MLP_MODULE_DOSSIER_INDEX.md` preserve the regional failures through the extracted value mediator, but not the later norm-closed package. The MLP index still labels MLP1 “not yet consolidated,” and the registries do not provide a concise v287-v337 cross-link to the current number dossier.
- `explanations/README.md` still stops at the 03:26 mediator and omits the 03:42 norm-closed report. These are navigation gaps, not missing scientific evidence. No shared registry/dossier repair was made because those files overlap active concurrent ownership.

## PAST_HOUR_TIMING

Window: **2026-09-19T03:00:18Z-04:04:13Z**. Only process start/end records, receipt finish times, review self-report, Git timestamps, and sparse phase boundaries are used.

- v310-v337 produced 28 successful receipts from 03:02:12 through 04:01:23, 506 forwards total. `runner.log` gives **67 seconds** of successful serial model-process wall time plus **2 seconds** for the failed first v310 run. Individual successful processes lasted 2-4 seconds. These are serial lane durations, not parallel sums.
- The lane completed a receipt/commit approximately every two minutes, but prior-art, design, authoring, validation, interpretation and documentation are not separately timestamped per candidate; therefore full serial candidate latency and active-work duration are **unknown**. Commit gaps are not used as labor time.
- The 03:27 lane review self-reports for its broader 02:26-03:27 window: implementation/preregistration 26m, waiting 8m, analysis/recording 22m, review 5m, idle 0. This partially overlaps the present window and is not extrapolated beyond 03:27. Design before authoring is **unknown**; validation time is **unknown** except the 2-second failed v310 model process and pre-execution gate failures noted for v316; model execution is 69s including that failure; interpretation/documentation after 03:27 are **unknown**.
- The newest phase log before this audit was `RESEARCH_PHASES_2026-09-18.jsonl`, with nine sparse boundaries ending 03:00:38 on September 18. No September 19 phases covered the scientific work above. At 04:04:38 this review used the existing helper to create one truthful `review` boundary in `RESEARCH_PHASES_2026-09-19.jsonl`. It does not backfill the missing hour or imply uninterrupted work.

## PROCESS_IMPROVEMENT

Ranked by likely time saved and cost:

1. A declarative shared executor/receipt writer for the 28 sibling MLP1/number runners could remove repeated string-derived scripts and likely save 1-2 minutes per candidate; estimated implementation/equivalence cost remains 30-60 minutes. It is highest potential savings but too large and collision-prone for this bounded review.
2. Consolidating one MLP1/number path record and dossier cross-link likely saves 5-10 minutes per future handoff at under 10 minutes cost, but the computation-path registry is already concurrently dirty and the scientific dossier was changing through v337.
3. Restoring current-day phase boundaries costs seconds and improves later timing accountability. **Executed:** one append-only `review` mark at 04:04:38 using `research_phase_clock_v1.py`; no earlier phase was inferred.

No runner/refactor change was justified: the model processes are already 2-4 seconds and empty queues show execution is not the bottleneck; the repeated authoring layer is the real target, but safely unifying it exceeds this review's bounded repair budget.

## Explicit verdicts

- `TRACK_ALTERNATION: PASS` — the preceding Codex review declared `WEIGHT_FOLDING`; this one declares `CIRCUIT`.
- `TRACK_PROGRESS: FAIL` — no concrete native folded-path receipt advanced the declared residual6-to-head9.8 closure. The generic Mobius CPU identity is useful mathematical control, but does not satisfy the folding track. The miss is recorded without cancelling alternation; its exact handoff is preserved above.
- `CEREMONY_BUDGET: PASS WITH TIMING CAVEAT` — observable work is dominated by 28 scientific receipts and only one five-minute lane review in the overlapping self-reported interval. Exact later implementation/interpretation buckets are unknown, so no stronger wall-time claim is made.
- `NOVELTY_LESSON_GATE: PASS` — the circuit graph, circuit/path registries, module dossiers, MLP index, authorities, current number dossier, failures and latest commits were checked. The new action applies the regional depth priority, forward-response-before-suffix rule, full `QK1*QK2*V`, matched nulls, distinct context cells, and the lesson that exact Mobius closure is not small interaction or specificity.
