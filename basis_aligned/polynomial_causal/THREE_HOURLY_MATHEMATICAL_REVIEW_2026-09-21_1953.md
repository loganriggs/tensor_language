**Three-hour mathematical review — 21 September2026,19:53UTC**

ACTIVE_TRACK: WEIGHT_FOLDING. Previous review16:50; the explicit two-day weights-first focus continues until22September15:10. Full goal remains predictive/OOD, extractable, selectively manipulable, reusable, stable and simple circuits. No current decomposition is adopted as such a circuit.

**Current object and evidence**

The target is the homogeneous quartic MLP16→MLP17 contribution projected through the unembedding. Input dimension1152; each native bilinear MLP has4608products. QR reduces output50304→1152 exactly. The approximation reads32 quadratic features q, each a sum of four bilinear products of learned linear projections, then forms a quadratic function of q. All four polynomial input slots receive the same x. Residual/bias paths, attention backgrounds, both input normalizations and final softcap are outside the polynomial target and must remain explicit at installation.

The frozen feature bank has at most256 independent linear readers. Isotropic local derivative rank256floors are19.13/15.45%, but centered covariance floors are3.76/2.92%; uncentered weighting is dominated96%by the activation mean direction. Therefore rank claims require an explicit metric. Empirical value fitting, Gaussian closure and local derivative fitting have different transfer behavior. The latest joint objective uses calibration values and16centered-covariance Jacobian anchors, separately normalized by teacher energy, plus fixedRMS-coordinate ridge.

For16 output-shared root forms, the direct rank-constrained fit has value error8.28/13.61%and derivative error21.29/27.53%on two opened panels. It is not independent OOD evidence. The primaryrank8 still fails its value gate. A root form is a scalar quartic computation with a shared output effect, not a guaranteed semantic concept.

**Literature matches and limits**

[Reduced Rank Ridge Regression and Its Kernel Extensions, Mukherjee and Zhu2011](https://dept.stat.lsa.umich.edu/~jizhu/pubs/Mukherjee-SADM11.pdf), section2, gives the exact relevant restriction. Stack our normalized values, centered derivatives and ridge rows into an augmented design. With fixedfeatures, writer C, featureGram G and cross X, the objective is tr(CGCᵀ)-2tr(CXᵀ)+constant. CholeskyG=LLᵀ reduces rank-constrained fitting to truncated SVD of XL^{-T}. Our solver matches independent augmented-design projection on five controls. Cost after moments is O(p³+Vp²), p528,V1152. A spectral gap determines the output subspace uniquely; bases retain rotational freedom. This result neither optimizes learned features nor guarantees semantic identity, nonlinear causal validity or transfer. The native exact solver improves its own objective while the primary value gate still fails.

[Uhlig1972, canonical forms for pairs of real symmetric matrices](https://thesis.caltech.edu/17494/) maps directly to two32x32 root forms A,B. A nonsingular pencil and its real canonical block structure permit common coordinates. Our existing implementation supports well-conditioned semisimple real1x1 and complex-conjugate2x2 blocks, not general defective pencils. In a2x2 block, two forms use combinations of u²-v²=(u+v)(u-v) and uv: two shared products suffice. This executable consequence is our derivation for the supported blocks, not a claim that every real pair is simultaneously diagonal. Per pair cost O(32³), stored transformO(32²); block/order/sign freedoms preclude unique semantic units. Matrix reconstruction is checked before accepting the numerical rewrite.

The initial native compiler fell back on one pair because reconstruction1.47e-10 exceeded the unchanged1e-10bar. Equivalent orthogonal output-pair bases reduce the error to<6.5e-13. This is a numerical coordinate issue, not evidence that the pair lacks shared products. V2 tries four fixed bases and retains the spectral fallback if none passes. Its16-form graph now uses384products,322048FP32coefficients and768integerindices; literal parameter/index storage1,294,336bytes. FP32function replay on both opened panels is<6e-7. No threshold was loosened; the V1price failure remains recorded.

[Cohen, Sharir and Shashua2016](https://proceedings.mlr.press/v49/cohen16.html) connects convolutional arithmetic circuits with CP versus HT organization and establishes depth-efficiency results for that architecture. Our q→paired-root graph preserves a hierarchy and local reuse, so flattening it to independent quartic monomials would overcharge computation. Their architecture and tensor-mode assumptions do not supply a recovery algorithm or lower bound for our repeated-input polynomial quotient with arbitrary DAG reuse. We use the connection to organize candidates, not to certify semantic circuits.

[Balle and Mohri2012](https://papers.nips.cc/paper/2012/hash/700fdb2ba62d4554dc268c65add4b16e-Abstract.html) uses Hankel completion and spectral factorization for weighted automata. A usable mapping here would require a string-to-output function, a prefix/suffix Hankel table and a verified finite-state linear transition representation. Our continuous quadratic state updates and explicit normalization do not provide those assumptions, and the required table is not available. Therefore no minimal-realization or recoverability guarantee transfers; this route is currently lower information than the exact root-pair rewrite.

**Decision and executable consequence**

Keep both approximate fitting and exact graph rewriting, but distinguish their claims. The fitting stage has an exact solution only conditional on fixed nonlinear features. The pair compiler gives a cheaper real arithmetic program for already fitted root forms, with no additional approximation beyond checked floating-point execution. Next confirm that the V2replay error is small enough to preserve the existing value-screen margin, then use the existing native_quartic_branch adapter to test model-level replacement with the RMS denominator/background explicit. That screen must include the parent and inexpensive prior baselines; narrow four-feature programs already match ordinary values well at much lower cost.

Semantic identification remains the main unproven end state: no human-readable feature labels, selective removals, stable component matching across restarts, or cross-task reuse are established. Whole-polynomial equality does not determine internal intervention semantics.

**Operational and organization audit**

Current evidence is cross-linked from SHARED_ROOT_BLOCK_INTERPRETATION_V1 and the for_logan overall review. New root programs need exact LFS entries before committing. Preserve V1fallback and primaryrank8 misses. No new compiler framework was built: reuse quadratic_pair_blocks and the managed runner. Native rank fitting2.07s; pair rewrites1.5s; preceding output-PCA block fit9.52s, partly because it repeats a decomposition per rank. Future broad rank sweeps should cache that decomposition; no refactor is needed for the completed four-arm screen. Authoring/interpretation dominates compute time. Nativebranch code and its old failure records were checked before planning installation, avoiding another custom adapter. The circuit-track handoff is a concrete pure-branch replacement with known external denominator/background, not a claim that low rank identifies a concept.
