# Bracket triple-pending payload-rank-two interaction V2 memory correction

V1 loaded the model, completed native and exact-factor forwards, and entered the registered intervention-arm loop, but retained full-vocabulary logits on GPU. It failed with CUDA out-of-memory before result construction; no result or metric was serialized. V2 changes tensor residency only: move native, replay, and each completed arm's logits to CPU before starting the next forward, then release the discarded per-arm factor capture and empty the allocator cache. All frozen rows, third-authority inputs, rank-two type-prototype derivation, live-score equation, exact/score-only arms, bars, predicates, and price of five forwards over 1,440 sequences plus one outcome-blind SVD remain unchanged. V1 outcomes stay unopened and are not reused.
