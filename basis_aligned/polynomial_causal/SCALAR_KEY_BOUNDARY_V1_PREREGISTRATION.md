# Coupled key-subspace boundary robustness

12September2026 before execution. The successful reflection-even component fixed
64 leading weight-derived key directions in each head. Small spectral gaps at
that boundary were already measured. Earlier grade screen held the boundary
fixed; this tests nearby boundaries rather than changing the joint-QK formula.

Reuse the frozen executable package, replacing only the keybasis by nested
prefixes of the existing ordered four-band eigenbasis. Cuts0,48,56,64,72,80,256
are fixed before results. Both heads use the same cut; retain8first/9current
values, all original normalizers, physical writers and sequentially recomputed
native states. Cuts0/256 both implement the complete selected value sectors.

48existing regional email/letter rows and32FineWeb newline prefixes, including
the earlier adverse example. Native plus7cuts; newline also actualmeanhead8.2.
672bodyforwards,120second cap. No data fitting, new directions or alternate masks.

A: previous native,64(mask45), fullsector(mask63), andmeanhead(mask65) outputs or
baseline-subtracted effects replay<=1e-4relative;0/256 effects agree<=1e-5;
original native capability andmeanheadpositivecontrol bars hold.
B: BOTH immediate neighbors56and72 pass originalregional andnewline criteria:
each regionalfamily>=50%pairedcuecoverage,>=10/12positivepairedreductions,
unrelatedmeanabsoluteeffect<=.5target; eachnaturalhalf meanabsoluteCEchange<=.02,
maximumabsolute<=.1. Preserve unfavorable examples and thresholds.
C: B plus48/80also pass, and all four neighboring cuts have signed targetremoval
effectrelativeRMS<=.1 versus64 on eachregionalfamily.

A failure invalidates interpretation. B/C failure narrows boundary stability,
not the earlier frozen64 confirmation. Any passing alternative remains selected
on reused panels and needs freshconfirmation. This is a stability/selectivity
question, not an attempt to call smaller projector rank circuit discovery.
