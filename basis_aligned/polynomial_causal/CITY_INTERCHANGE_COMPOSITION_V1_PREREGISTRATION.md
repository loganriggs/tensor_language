# Does the successful coupled interchange compose from key and value exchanges?

Fresh complete-city interchange passes prediction/selectivity gates. This opened
five-arm test targets its missing composition evidence, not a new source partition
or a rescue of the failed MLP7 subgroup. Two concrete changes to the supported
interface are swapping both city keys together and swapping the complete city value.
Keep recipient queries/background and full native MLP8/suffix in every arm.

Let W(K,V) be recipient-query city write. Define dK=W(Kdonor,Vrecipient)-W(Krecipient,Vrecipient),
dV=W(Krecipient,Vdonor)-W(Krecipient,Vrecipient), and dJoint=W(Kdonor,Vdonor)-W(Krecipient,Vrecipient).
The exact head cross is dJoint-dK-dV, with native reference rounding residue assigned
there. This explicitly preserves both QK factors and the full current/inherited value.

Reuse the now-opened CITY_FULL_INTERCHANGE_V1 panel. CPU preflight reconstructs native
block7 once from saved residual6 (one40sequence block call), then native head8 fields;
require self-removal and full interchange replay<=1e-4relative on every sequence,
finite/support0 and head cross closure<=1e-12. No outcomes choose pieces or thresholds.

Installed arms: native, keyswap, valueswap, dK+dV without headcross, fulljointswap.
Five padded batches/200sequence-equivalent forwards/90block calls,2CPUthreads,
180second cap. The fulljoint arm reuses captured exact native-reference delta.

pred_a: native/fulljoint score replay prior native/reference swap abs<=1e-4 AND
rel<=1e-5; finite/support0, requiredcalls, CUDAuninitialized.
pred_b: joint-minus-sum-of-single target effects has L2norm<=.35 times the smaller
single effect; both single target RMS effects>=.10joint target RMS (no dead piece).
pred_c: report two separate registered accuracy checks: omitting the headcross
changes target effect by<=.10joint norm, and summed-main arm minus sum of single
effects is<=.35smaller single effect. Both must hold for this gate.

If b/c fail, preserve that failure and retain coupled interchange. If they pass,
independent-composition promotion is still withheld until a registered matched
random split of the same full write and fresh jointly installed confirmation.
Report per-document interaction, signs and four control effects, not only aggregate
cancellation. Exact head algebra alone is not compositional behavioral evidence.
