# Can two separate command matchers generate the joint router?

After post-block8 mixed-state removal, the four native shared readers retain
101–104% of their mixed read RMS. The fixed common-upstream-variable proposal
is closed. A different circuit organization is possible: one matcher responds
to each command, and their product generates the joint response without an
upstream mixed state. Native signed product attention supplies a precise
operation and a no-fit test. The earlier small-model hop-query factor null
is relevant prior art, but concerned a single hop instruction and different
checkpoint; it does not decide these actual-bilin18 two-command score functions.

Use all32 opened worlds/128sequences, both queries, both phases/all templates,
fixed L9H1/H4,L11H3,L15H5. Let A_ab(s)=q1_ab dot k1_ab(s)/128 and
B_ab(s)=q2_ab dot k2_ab(s)/128, after native head RMS and RoPE. Let U_ab(s)
be the projected shared payload. The native routing is P=A*B*causal_mask.
Sourcewise character multiplication gives

    P11 = (A11 B00 + A00 B11 + A10 B01 + A01 B10)*causal_mask.

Multiply each term by U00 and sum over sources. This partitions the previously
observed joint-router contribution P11U00. Do not confuse it with the full
shared-read interaction, which also has routing/payload crossed terms.
Individual matcher mixed terms are A11B00 and A00B11; crossed single-command
matcher terms are A10B01 and A01B10. Different layers/heads remain typed.

A: source/parent hashes, pairing/finite/restoration/counts and planted controls.
Native FP32 A*B pattern replay abs<=1e-3/relative<=1e-5; FP64 four-term
convolution abs<=1e-9/relative<=1e-10 against live sums of FP64 A*B.
Joint-router statistics replay prior native read result abs<=1e-4/relative<=1e-5.
The FP32 versus FP64 product difference is a separate native numerical check;
do not assert bitwise agreement across reassociation. Future mixed router at
prior temporal query <=1e-8. Masks and source alignments stay native.

B: crossed matcher terms alone predict full P11U00 within1% relative RMS,
floor1e-6, in each late-query phase, pooled typed fixed-head entries. No
head/query subset rescue or fitted mixing coefficient. This tests which
multiplications matter, not complete final-output or extraction fidelity.

C: all four heads admit two separately command-controlled matchers. For each
head there must be ONE orientation consistent across FIT/HOLDOUT: A temporal
and B iswas, or the reverse. Exchange is an explicit native factor-label
symmetry, not a fitted coordinate. For a temporal-only matcher, the omitted
mode energy is ||M01||²+||M11||²; for iswas-only it is ||M10||²+||M11||².
Divide its square root by the square root of all nonconstant-mode energy,
floor1e-6, and require<=.01 for each matcher, head and phase at the late query.
Pool source coordinates/worlds within these groups with causal support only.
The desired single-command mode RMS must exceed1e-6. Constants may depend on
world/source. Both fixed orientations are evaluated and all failures retained;
no head discarded and no per-world orientation choices. This necessary
function-class test does not require fitting a purported command matcher.

Failure of B or C closes this separate-command matcher proposal. Keep terms
needed by measured full-vector error; no additional factor/rank/boundary scan.
A positive screen still needs new-world prediction, causal factor interchange,
and structural pricing of the native query/key producers and payload adapters.
All545902902 parameters remain charged, no new circuit yet identified.

Managed32full-forward/128sequence native capture, at most192 extra local value
contractions as in parent, plus explicit score extraction. Batch4,length<=27,
tensors<256MiB,alarm600s,no gradients/fitting/model updates. Reuse parent capture,
transform and scorer; add only scoped score observation and this product algebra.
