# Coupled MLP9 and complete attention10 through native suffix

Reuse corrected recipients0/12/24/36 and adjacent donors; freeze all25 strengths
in{-1,0,.5,1,2}². Eight native captures plus200 native/compiled comparisons yield
208 forwards,180second cap. Compiled arm replaces post-MLP9 state and attention10
with weight/context-derived programs; residual re-entry, MLP10 and later blocks
remain native. Inherited first values and x0 remain explicit.

A: post9 and post10 states and all-vocabulary scores relative errors<=1e-5 per case.
B: baseline-subtracted target/control differences<=1e-5+1e-4abs(native effect),
all cases; zero-strength effects exactly zero. C: combined source+attention
programs plus required x0 payload<=75% of source+native attention weights+x0,
first values and re-entry lambdas for each prefix, original tensor dtypes charged.

Null: composition through the native suffix breaks accuracy, or residual-path
dependencies erase the conditional price advantage. Original full model/context
generators remain globally required. No static whole-model saving, long-prefix
win, latency improvement, semantic identification or fitting claim.
