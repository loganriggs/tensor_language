# Hourly strategic review — 2026-09-07 09:14 UTC

## Circuit interpretation targets and controlling goal

The target remains a smaller transparent tensor program that is jointly predictive, composable,
manipulable, and literally priced. A complete circuit must specify what is read, computed, and
written and who consumes it; merge and split native heads/MLPs according to the computation rather
than architecture labels; predict held-out and OOD effects; execute as an isolated circuit or precise
interface plus background; support selective removals, swaps, and edits; compose and reuse across
tasks; and remain stably identified across corpora, fits, gauges, and controls. Rank, reconstruction,
variance, or cross-entropy alone is not a circuit result.

## What changed since 08:14

The rank48 source-to-weight-reader program passed its registered 10% cue-source noise test, then faced
reverse OOD manipulation. Reverse compiled coordinates remained accurate (`.9445` minimum) and
controls remained clean, but even-fit temporal/is-was behavior was `.7998/.7982`, narrowly below the
frozen `.80` bar. The rank48/49/55/all-110 support ladder ruled out missing source support as the
simple explanation: rank55 passed through compensation, while all110 had exact coordinates but only
`.7983` behavior. Support pruning was therefore frozen.

The weight-reuse route produced an important split. Task-separated top-eight MLP response spaces had
only weak quadratic-function overlap. More decisively, inside the already causal joint rank-eight
projector, each task's top two modes captured `.753-.953` of response energy, while cross-task
coordinate energy was only `.0017-.0168` and every exact quadratic-weight principal cosine was below
`.176`. Temporal and is/was reuse the same response sites and interface, but the live MLP quadratic
directions are task-typed. No shared-span causal swap is licensed by these data.

A frozen broad reverse dose response then identified scalar interface undergain: behavior increased
monotonically from `.593-.619` at gain `.75`, through `.798-.829` at `1.0`, to `.996-1.032` at `1.25`,
and overshot at `1.5`. Gain `1.25` retained zero flips and tiny KL on the diagnostic population.
This was explicitly post-hoc, not prospective. A price audit caught that the run used 37 rather than
21 forwards; a hash-bound correction demoted the formal result to 4/5 while leaving its four
scientific gates intact.

Zero-refit gain `1.25` then transferred to genuinely fresh temporal-v13 plus later, already-opened
is/was-v12: behavior was `.996-1.074`, coordinate projection at least `.9407`, and worst target
residual `.00852`. It remained a 4/5 null because even-fit flipped one of 16 temporal paraphrase
controls (`.0625` versus the `.05` ceiling), although control margin RMS was `.0303` and median KL
`.000427`. The active 57-forward localization reverts one response site's gain from `1.25` to `1.0`
at a time. It tests whether collateral behavior belongs to one reader or is a distributed threshold
effect; it does not alter the 48-source support.

The broad circuit lane independently advanced the attention-11.3 number/quantifier computation from
self-value attribution to the head-noun key, identity-invariant noun-number value, an MLP1 noun writer,
and a cross-fit rank-one noun write. A position bug in v165 was preserved and repaired by semantic
anchor in v166 rather than silently discarded.

## DAS regularization verdict

The user's memorization diagnosis remains partly confirmed. Full-vocabulary KL changes the optimized
object and reduces held-out error from about `.4485` to `.2445`, near difference-in-means `.2385`;
tangent noise alone remains near `.4486`. But complete-family validation rejects every learned
rotation and safely retains the pooled step-zero axis. Thus KL is a useful anti-specialization target,
not evidence that the learned complement axis is the transferable mechanism. This H3 penalty sweep is
closed unless a new optimizer freezes selection on complete families and prospectively beats DIM on
an unopened family. More same-row noise/KL tuning is lower information than current circuit tests.

## Confound and systems audit

Baseline subtraction and reverse donor references remain fixed; two independently trained projector
fits are always reported; source support and weight readers are frozen; full-vocabulary KL plus
answer-preserving controls remain live. The current risks are explicit: gain `1.25` was suggested by a
seen dose miss, is/was-v12 is not globally sealed, one control flip can dominate a 16-row fraction,
top-one flips and small KL can disagree near decision boundaries, and uniform gain can mix task-typed
MLP directions. The site-reversion test separates the last issue without relaxing the control bar.
If many site reversions clear the same flip, the effect is threshold-distributed and no site-specific
mechanism may be claimed.

From 08:14 through 09:14, the managed lane produced 18 scientific receipts: seven deep rank48/weight
receipts and eleven broad number/quantifier receipts, approximately one every 3.3 serial minutes. The
deep sequence was source-noise OOD, reverse OOD, reverse support ladder, task-separated quadratic
overlap, joint-projector task usage, gain dose, and fresh gain transfer. One broad run had an invalid
position assumption and was repaired; one deep receipt required a forward-price correction. GPU time
and interpretation dominated; shared OOD/projector and quadratic-metric helpers prevented wholesale
setup duplication. No queue restart or outcome rerun was used to erase a miss.

`CIRCUIT_FOCUS: PASS` — the hour advanced OOD robustness, bidirectional manipulation, within-module
task splitting, weight translation, selective controls, and a separate number-value circuit.

`CEREMONY_BUDGET: PASS WITH CORRECTION` — preflight remained smaller than scientific execution, but
the 21-versus-37 forward miscount was a real audit failure; it was immediately hash-bound and the
formal score reduced. The active runner uses a term-by-term 57-forward count.

`NOVELTY_LESSON_GATE: PASS` — prior DAS, weight-incidence, rank, and response-interface results were
searched; static overlap was not called a circuit, support size was not threshold-chased, and the
post-hoc gain was kept below prospective evidence.

## Ranked next moves

1. **Finish response-site gain localization.** This changes selective manipulation and within-module
   splitting. A unique or small stable site set clearing both cross-fits while retaining `.90-1.10`
   target recovery licenses a frozen gain map; many interchangeable clearing sites kill specificity.
2. **Confirm any site-specific gain map on a new capable construction.** This changes OOD prediction
   and stable identification. A target or control miss kills calibration adoption while preserving
   the uncalibrated rank48 interface.
3. **Promote the MLP1 noun-number/attention-11.3 route with a complete-family removal and OOD dossier.**
   This changes computational specification and selective manipulation for a different behavior;
   failure of noun-number invariance or writer necessity kills promotion.
4. **Map task-typed attention usage through exact Q/K/V/O tensors.** This changes reuse/grouping only
   if the attention sites differ from the MLP null and causal shared-span swaps succeed. Weak weight
   overlap or task-specific swaps kill a shared-computation claim.
5. **Rank47 pruning or new complement-DAS optimization.** Both remain demoted. Rank47 advances only
   simplicity after interface selectivity closes; DAS reopens only with complete-family selection and
   a prospective advantage over DIM.

The current route remains highest information because one response-site experiment can decide whether
the only fresh reverse-control failure is a mechanistically local calibration defect or a distributed
limit of the interface. The job is live in the managed runner; no new pruning or DAS fit is opened.
