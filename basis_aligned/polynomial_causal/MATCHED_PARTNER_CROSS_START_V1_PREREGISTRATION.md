# Whole branch recurrence across fitting starts

The current parent is arm0/node0; independent continued frame arm1/node251 was uniquely matched by weights at cosine0.9995825. Freeze that correspondence and compare the eighth descending weighted SVD mode of its perpendicular partner map to the current eighth mode. Do not choose a different output mode after seeing effects. Both upstream graph fits remain unconverged.

For $f(z)=w(a^Tz)(b^Tz)$ and $g(z)=v(c^Tz)(d^Tz)$, the paired coefficient inner product is

$$\langle f,g\rangle=\tfrac12[(a^Tc)(b^Td)+(a^Td)(b^Tc)]\,(Rw)^T(Rv).$$

Use the full scalar expression including actual reader norms for cosine normalization. Here $R^TR=U_c^TU_c$. This is the paired producer-coefficient metric, not a fully symmetrized quartic or a native behavioral guarantee. Reconstruct reference partner from its compiled reader using $b=\lambda H^{-1/2}D_{16}G_{16}C_b$.

A: reference execution and partner reconstruction<=1e-8, fresh native reference-effect replay<=1e-5, finite; B: whole function cosine>=.95 and absolute partner/output cosines>=.95; C: every fresh family swap relative RMS<=.1, sign>=.9,live>=4 and meanabs removalCE disagreement<=.02; D: each family write error<=.05. Keep all four families including quoted control. Same two-reader/one-writer conditional implementation price10,627,200floats. No fitting or capability exclusion; failure narrows stable identification, not existence of structure. Save independent compiled branch and source bindings.
