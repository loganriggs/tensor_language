# Install existing mixed-edge core in selective-source join

Openedv4, unchanged selectors and source positions. Compile existing attention_mixed_edge_core.py using actual FP32-rounded initial singleton state deltas, native weights, RMS eps, rounded RoPE and first-value mixture. Use identity output readers to produce a1152-vector correction at the later edited query. Add this to the additive singleton postattention11 state, execute nativeMLP11 and suffix12–17. No joint state is a program input; native joint output only scores fidelity. Existing uncorrected/pre11/post11/post17 joins replay as controls.

12prefix16fullsuffix20joined suffixes,8explicitMLP11calls. Original effects/pre11 replay<=1e-8 and final-readout consistency<=.001weak norm. Correctedjoin must meet.1number/.05everycontrol in all8cells; additional precise bar<=.001alloutputs. Export context-generated core and its predicted edge so CPU-only replay is possible;31,296corevalues/input for full1152output directions. Native coefficient generation, singleton states, full downstream suffix and selector costs remain charged. This exact correction does not by itself explain the entire downstream interaction or extract a full circuit. No new OOD claim; earlier mixed-edge causal-insufficiency result remains intact.
