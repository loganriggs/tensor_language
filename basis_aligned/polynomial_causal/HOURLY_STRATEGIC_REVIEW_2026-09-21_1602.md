ACTIVE_TRACK: WEIGHT_FOLDING

21September2026, checkpoint16:02UTC, written after the terminal interchange screen. User's two-day weights-first exploration overrides hourly alternation until22September15:10. Scheduled mathematical review remains13:50, next16:50; a focused trust-region literature review occurred in the interim.

The full target remains predictive/OOD-valid, extractable, selectively manipulable, reusable, simple, stably identified circuits. The current object is still six MLP16 quadratic reads feeding three selected MLP17 components, not the full folded model. The generated interface makes the residual numerator more explicit but supplies RMS17and attention/embedding context. No current result closes these dependencies or establishes semantic units.

The hour produced concrete path evidence: signed read-error decomposition; rejection of a fitting-state constant repair; localization of carry error to the pre-MLP16residual rather than attention17; an implicit cubic coefficient metric; an exact native replay of consistently generated residual projections; a worst-input quadratic solver; a numerical lower bound excluding one fixed-bank edit; and a directed one-square extension that passes source robustness where an equal-cost random extension fails. That extension retains text-transfer failures. Paired and explicit normalizer-clamped context-interchange fitting also reduce calibration objectives while worsening opened-panel comparisons.

The best information is now about **limits of objective proxies**. Global source bounds exceed actual component error by3000times. Better average/cubic/worst-read or paired/interchanged fitting scores do not predict adoption. Do not continue objective variants merely because another small training improvement is possible.

A provenance inspection supplies the next discriminating question: the448fitting positions are seven historical64-tokenprefixes, selected as indices24–27and29–31from a32prefixcache. They are not448independent documents. Earlier expanded calibration also uses64-tokencontexts. Current evaluation uses256-tokenprefixes and positions16–255. This is a concrete support mismatch, not yet a demonstrated cause. Next compare early16–63, middle64–127and late128–255on the same opened panels; verify first64causal-prefix execution invariance. If failures already occur equally at early positions, reject context length as the sole explanation. If length predicts the gap, new calibration must match the intended interface/context range with document-level separation, rather than recycle failed panels for fitting.

Circuit interpretation targets remain explicit: computational read/write specification improved; cross-boundary residual grouping is algebraic but not semantic; partial predictive checks fail; supplied normalization/attention obstruct full extraction; selective manipulation and stable feature identity are unproved; literal graph reuse exists but reusable semantic composition remains unproved. Original source-cost reduction is not enough.

Alternatives ranked: (1)position/support diagnostic because it can falsify a concrete reason for overfit at lowcost; (2)document-separated, length-matched covariance/lifted-moment calibration if supported, maintaining a global coefficient constraint; (3)more generic robust penalties or coefficient sweeps, demoted by this hour's negative results. No new data-based semantic circuit campaign is substituted during the user's focus period.

Runtime receipts include2.6–2.7second code diagnostics,5.46second generated-interface capture,29.87second robust fixed-bank fit,18.33second directed extension,30.87second random control and10.40second six-baseline transfer. Native jobs are managed. An unscaled Cartesian optimizer stopped prematurely; identical-objective rescaling repaired it, and both results are preserved. Authoring/analysis dominated individual GPU runtime; exclusive human-time fractions are unavailable. Reused graph, capture, source and constraint helpers avoid new frameworks. No idle-lane utilization target justifies launching redundant experiments.

TRACK_ALTERNATION: OVERRIDDEN by explicit user two-day weights-first request.
TRACK_PROGRESS: PASS, concrete native path and graph-edit receipts.
CEREMONY_BUDGET: PASS qualitatively; solver certification addressed a real infeasibility claim. Do not add more bespoke audits to routine screens.
NOVELTY_LESSON_GATE: PASS, prior Gaussian/empirical-moment/constant/cubic failures and MIDPOINTseparable-metric work checked before the explicit-interchange test.

Organization: primary current receipts are ROBUST_TRANSFER_INTERPRETATION_V1.md and INTERCHANGE_TRANSFER_AUDIT_V1.json under direct_tensor_match. The overall Logan review remains a conceptual starting point, not an automatically current experiment log. Position-transfer plan and implementation are committed/enqueued at this boundary. Next scheduled review17:02; mathematical review16:50.
