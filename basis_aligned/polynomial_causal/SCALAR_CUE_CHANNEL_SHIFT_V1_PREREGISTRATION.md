# Frozen rank64 regional component under cue-channel shift

Registered12September2026 after token-only row construction, before model scores.
Three fixed families of12pairs: newcities Leeds/Dallas andBirmingham/Houston;
explicitnationality; explicitUK/USstyle instructions. Six old spellingcontrasts.
72newprompts, one-token pairedcuechanges, no capability-based filtering or fitting.
Freeze originalrank64package; boundary alternatives are not selected here.

Proposed native arms: baseline, joint component removal, and paired donor-field
replacement for both heads. Cache donor fields from each native donor context;
on recipient replay recompute its own scalar at each actual sequential state,
subtract it and insert the matching native donor scalar. This specifies the
hybrid conditional intervention, not a full donor-model replacement.216forwards,
120second managedGPU budget. OriginalQKnorms/nativeprefix/background/suffix remain.

A: native noedit algebra<=1e-6relative, matched donor IDs and sequence lengths,
and nonzero component fields. Use existing packagedruntime and one-tokenvalidator.
Eachfamily nativecapability is reported separately: meanpairedcontrast>=.2 and
>=10/12positivepairs. Weak capability invalidates that family's transfer claim,
not the complete measurement; no rows are removed.
B: all three families are capable and jointremoval gives>=50%meanpairedcontrast
reduction,>=10/12positivepaired reductions, unrelatedmeanabsmarginchange<=.5target.
C: B plus paired donor replacement in eachfamily has>=50%mean directed transfer
relative to nativepairedcontrast,>=20/24positive directions and unrelatedmeanabs
change<=.5target. Report everyfamily and everymiss, no aggregate-only rescue.

This tests controlled cue-channel generalization of the frozencomponent. It
is not corpusOOD, a new broadselectivity control, autonomous extraction or
writer-role identification. Existing retainednewlinecontrols and failures remain
on record. Candidate code/binding must implement these semantics before enqueue.
