# A three-writer mixed-edge candidate: unit sufficiency, incomplete amplitude reuse

The all-nine-head unit screen retains only head3 (zero-based fourthhead) or removes each head from the fullcarry correction. Head3 alone is the only singleton passing8/8 cells; its omission is the only single-head omission failing cells (6/8pass). Head3-only worstnumber5.458%,controls3.825%. This changes only the mixed attention interaction after backgroundMLP11, not whole native heads or singleton effects. Every other only-head arm passes6/8. Nativeanchors0 and exactfold precision control pass.

Frozen head3 then passes53/56 amplitude cells, failing the registered all-cell reuse claim. Worstnumber12.6476%,controls9.0523%. Fullcarry passes56/56; removinghead3 passes41/56. Failed head3 cells:

- opposite next_to_subject plural, subject2/attractor1;
- congruent next_to_subject plural, subject-1/attractor1;
- congruent next_to_subject plural, subject2/attractor1.

These remain failures; do not restrict the domain after seeing outcomes. They show a conditional three-writer approximation is insufficient across the registered signed/strengthened domain. They do not prove no smaller cross-head feature representation exists. Native background/suffix and oracle source selector dependencies remain.

## Independent extraction and negative-result controls

export_edge_head3_v1.py slices the actual core head dimension and independently compares its contraction with the original27writer sum. Nine signed/half/doubled/zero amplitude pairs replay at maximum1.78e-15, with only3480values/context versus31296fullcore. Three1152-vector writers dominate storage;21headcoefficients and3source-normcoefficients complete the core. Artifact: ../bilinear_quotient/circuits/followups/v4_edge_head3_program_v1.pt. It needs no checkpoint after preparation, but source/background/suffix preparation is external.

EDGE_HEAD3_TRANSFER_CPU_AUDIT.json matches each failure to fullcarry and exact controls and records reverse-triangle lower bounds on their output discrepancy. These distinguish omission errors from native precision; they do not assign the missing effect to a particular otherhead. Fullcapacity passes, CPU contraction passes, oldnative anchors0: evidence supports a coarse-grouping limitation rather than the tested instrument errors.

Native receipts: v4_edge_head_screen_v1_result.json and v4_edge_head3_transfer_v1_result.json in ../bilinear_quotient/circuits/followups/. Preregistered unit and transfer runners are preserved. Both are opened-text studies; transfer selection used onlyunit outcomes. The new negative suggests testing a shared combination of value branches acrossheads rather than equating a native head with a semantic circuit. Such a support must be frozen before another transfer evaluation.
