# Bracket closure execution v1 — resolved pre-execution hold

The tensor execution path, authority schema, raw sufficient statistics, document
bootstrap primitive, and receipt-last publisher are source-complete. The role,
resampling, simultaneous-family, and no-rescue choices below were prospectively
resolved by `BRACKET_CLOSURE_EXECUTION_V1_AMENDMENT.md`. Model forwards remain
prohibited until a source-bound independent audit and pre-forward authority exist.

Resolved choices (retained here as audit history):

1. The canary preregistration registers separate prose/code roles and a mandatory
   synthetic canary. The row amendment instead freezes mixed-domain FIT/SELECT/OOD
   roles and has no synthetic role.
2. It is unspecified whether document bootstrap draws are by role, by domain, or by
   role×domain after this merge.
3. It is unspecified whether SELECT and OOD use one joint simultaneous family or
   separate discovery/confirmation families.
4. No exact OOD decision conjunction says which SELECT gates must repeat, which may be
   descriptive, and whether OOD can rescue a failed SELECT gate.

The former extraction ambiguity was prospectively resolved in the execution amendment
as exact stored replay added to the identical stored eight-head background. This file
is retained as audit history and is not itself a launch blocker.

FIT rows are metadata-only and cannot be forwarded. No current source can mint an
independent audit.
