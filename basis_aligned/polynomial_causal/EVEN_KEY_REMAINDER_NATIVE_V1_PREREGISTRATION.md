# Full-value remainder versus selective scalar

The full-value even extension failed regional B and unrelated preservation C.
The prior scalar explains nearly the same cue contrast with lower collateral.
Test the frozen additive write R = full-even write minus prior selected scalar
write. This is a new physical removal, not subtraction of two behavioral effects.

Native and remove-R arms on all same 72 regional + 32 natural rows give 208
body forwards. Reuse previous scalar/even removals only when new native anchors
replay within 1e-5 relative. No fitting, new text or changed projection.

A: native anchors and scalar+R/even write replay <=1e-5. B: paired cue-change
norm(R) <=10% of paired scalar cue-change norm, every regional family.
C: sum of separate R and scalar effects predicts even-removal effect within
5% relative L2 for both regional readouts in every family and newline CE in
both natural halves. D: R mean absolute unrelated-readout effect >=scalar
in each family, and mean absolute newline CE >=2x scalar in each half.

Failure of C preserves a nonlinear composition limitation even if B/D pass.
Positive B/D would localize target selectivity to the earlier scalar computation
and much collateral to its complement; it does not name the complement's function.
120 seconds, managed GPU lane1 only, frozen weights and live native suffix.
