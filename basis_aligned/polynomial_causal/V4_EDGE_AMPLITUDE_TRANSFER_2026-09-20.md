# Frozen mixed-edge reuse across signed amplitudes

Frozen source selectors and mixed-edge core were evaluated at seven independent subject/attractor amplitude pairs: (1,1),(.5,1),(1,.5),(-1,1),(1,-1),(2,1),(1,2). All three arms pass all56amplitude-by-family/panel cells. The unit pair is replay; other settings are prospective amplitude conditions on opened text. There is no fresh-text or extracted-selector claim.

| Arm | Worst number error | Worst control error | Pass |
|---|---:|---:|---:|
| Exact attention edge plus native MLP response |0.0539%|0.0292%|56/56|
| Carry-only, no MLP response correction |8.1193%|2.8882%|56/56|
| Direct linear response numerator, exact denominator |0.0689%|0.0539%|56/56|

Errors divide each output discrepancy by the weaker singleton number-effect norm for its cell. Gates remain10%number/5%eachcontrol. Carry's worstnumber occurs under negative subject amplitude; this makes the margin to failure visible instead of averaging it away. Exact positive-control precision remains well below the thresholds. Unit native anchors match exactly, counts12prefix64native84joined, measured runtime2.35seconds plus managed setup. All registered predictions pass.

This supports amplitude reuse of the conditional interaction correction. It does not make native singleton backgrounds free: each new setting regenerates them. Linear coefficients are prepared from those backgrounds; the edge core and source selector weights remain frozen. No joint native state enters predictors; it is used for validation only. Carry-only needs no MLP response coefficients. Linear costs62614values/context/background plus existing upstream core. Native source generation and six-block suffix still dominate extraction requirements.

Primary result: ../bilinear_quotient/circuits/followups/v4_edge_amplitude_transfer_v1_result.json. Preregistration: V4_EDGE_AMPLITUDE_TRANSFER_V1_PREREGISTRATION.md. CPU per-condition audit: V4_EDGE_AMPLITUDE_TRANSFER_CPU_AUDIT.json.

## Next executed diagnostic

EDGE_HEAD_WRITE_SCREEN_V1.json decomposes the exported27writers into nine groups of three value-related writers, respecting actual column ordering. Independent sum replay is at most5.39e-15. Head3 dominates local write norm for close_to (89–95%relative contribution), but not the other template; other heads' changes can be substantial. These are vector norms, not causal contributions. A native all-head omission/sufficiency screen is required before treating head3 or any group as a circuit. Head boundaries are a baseline grouping, not semantic assumptions; branch-level or cross-head shared structure remains possible.
