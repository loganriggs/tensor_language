# Hourly circuit review — 2026-09-06 23:14 UTC

## Circuit interpretation targets and full goal

The target remains a smaller transparent tensor program that is jointly predictive on fresh/OOD
text, composable under joint installation, selectively manipulable, and literally cheaper. Circuit
quality requires: (1) a read/compute/write specification; (2) cross-boundary grouping and native
module splitting where warranted; (3) held-out/OOD prediction; (4) extraction or sufficiency;
(5) selective edits with collateral controls; (6) composition/reuse; and (7) stable identification
across corpora, gauges, and fitting restarts. Rank, reconstruction, variance, and CE are supporting
controls, not circuit discoveries.

## What changed since 22:14

The is/was MLP8-complement route advanced from a transferable five-head screen to a mostly literal
program. L11H1/H3 and L15H5 write at the final query and share the same native-pattern value
transport as L9H1/H4. A corrected fresh v11 source atlas and untouched v12 transfer identify
`post_cue_before_subject` as their stable source: 84.5--88.3% behavior and 88.4--92.0% Q8 with
Q8 cosine at least .991. The local weight compiler closes actual L11/L15 c_v, base-pattern, and
c_proj contractions to <=1.38e-5 full-logit error; both inherited initial-value branches are zero.

The module-first upstream atlas finds MLP9 as the common stable converter. L15 additionally uses
MLP11/12/14. All intervening modules account for about 58% of L11 and 46--56% of L15 response
norm; the rest persists on the MLP8 residual skip. A frozen greedy causal-order MLP9--14 program
reduces the joint residual objective from 1.0 to .706 discovery/.726 evaluation, but its optimum
keeps all six MLPs and still leaves .794 L11 and .659 L15 residual fractions. It is a distributed
correction screen, not a minimal core.

Failures were preserved rather than rationalized. Source-atlas v1 reordered tensor dimensions;
v2 scored closure outside causal destinations. Their outcomes remain invalid. Upstream-atlas v1
compared native attention reconstruction to an intentionally clamped c_proj input; v2 restricted
that identity to native base/live captures and reran independently. These failures changed
instrument scope, not scientific thresholds.

## Throughput and systems audit

Authoritative result timestamps show 12 post-22:14 Codex receipts before this review: nine screens
or capability gates and three honest invalids, with roughly 3--7 serial minutes between receipts
(median about 4.5 minutes). A capability plus causal transfer took about 10 minutes after its fresh
bank existed. Time was dominated by scientific implementation and GPU execution; shared preflight
was about 16--18 seconds per runner. Repeated authoring was concentrated in two lexical banks and
three narrowly repaired diagnostics. There was no idle queue gap material to this route.

`CIRCUIT_FOCUS: PASS` — every unit localized, transferred, compiled, or falsified a circuit edge.

`CEREMONY_BUDGET: PASS` — preflight/review was smaller than design plus execution; repairs addressed
observed invalid instruments rather than adding speculative ceremony.

`NOVELTY_LESSON_GATE: PASS` — prior results and claims were searched; invalid outcomes were not used
for selection; module-first localization preceded greedy composition; no activation variance or
scalar CE result was promoted as mechanism.

## Confound audit

- Baseline subtraction is explicit: all auxiliary arms share MLP8 actuation and the registered
  L9H1/H4 clamp; native base and causal-order identities are zero.
- A1/A2 are scored separately, avoiding frame mixing. Two disjoint lexical banks now separate
  source discovery from transfer and capability selection opened no causal outcome.
- Singleton effects were not summed through a nonlinear model. Module unions and greedy prefixes
  were executed dynamically in causal order.
- The v12 greedy pool is postselected from a v12 atlas; its odd-group evaluation is not independent
  confirmation. No adoption claim is allowed without a fresh population.
- The inherited value branch was a live knob and measured exactly zero at both auxiliary readers.
- Float precision controls are scale-aware. Large invalid discrepancies were preserved; accepted
  weight identities are one to four orders below their frozen bars.
- The remaining response may include non-core attention9 output as well as the raw MLP8 skip,
  because the upstream atlas began at MLP9. Calling it a pure skip before clamping full attention9
  would be a topology error.

## Direction choice and alternatives

The highest-information route remains natural-path tensor compilation, but the next object changes.
Fresh-confirming an unpenalized six-MLP correction would validate an unwieldy branch while leaving
the larger L11 residual unexplained. The better test clamps complete attention9 plus every module
through MLP14 to base, then predicts the remaining L11/L15 post-cue c_v responses from the literal
MLP8 Down write, the exact products of block residual coefficients, native RMS, c_v, base patterns,
and c_proj. This directly separates residual skip from module conversion.

Other approaches remain secondary: a fresh six-MLP confirmation after the skip is quantified;
MLP9 bilinear weight factoring if its conditional correction remains necessary; and a larger
cross-task causal Hankel block after the natural route is complete. Multi-environment noise/KL DAS
is a guardrail for a correctly ranked vector objective, but current rank-one evidence makes another
DAS optimization lower information than the exact weight path.

Ranked next moves:

1. **Full-clamp direct-skip compiler** — changes computational specification and extraction. Pass is
   <=1% causal behavior/Q8 error from literal weights after full attention9--14 clamping. Kill it if
   residual-state/c_v closure fails or another live module contribution remains.
2. **MLP9 conditional weight factor** — changes within-module splitting and composition. Run only
   after subtracting the direct skip; kill if its bilinear terms do not predict the causal reader
   correction on held-out rows.
3. **Fresh complete-program confirmation** — changes stable identification and held-out prediction.
   Freeze only branches that survive steps 1--2; kill adoption if behavior/Q8 coverage falls below
   the established 80% range or answer-preserving collateral grows.

The current route survives alternatives because it has already produced cross-lexicon causal
prediction and exact checkpoint-weight identities. The immediate action is the full-clamp
direct-skip compiler, not another rank sweep or a fresh confirmation of all six correction MLPs.
