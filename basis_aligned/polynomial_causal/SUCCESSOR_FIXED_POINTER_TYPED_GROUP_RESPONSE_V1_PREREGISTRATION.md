# Successor fixed-pointer typed-group response screen V1

The valid fixed-pointer singleton screen found no complete module that passed both month and digit families. Attention8 was the strongest response (digit projection 0.618, month 0.490) but missed the frozen 0.50 month bar. The successor registry permits a new screen only for an independently specified distributed or typed operation. This screen freezes architectural type × depth groups without choosing unions from singleton outcomes.

Reuse all 30 aligned rows from `SUCCESSOR_POINTER_PREFIX_INTERACTION_V1_ROWS.json`. The coherent final-query response is transplanted into the aligned late-swap recipient and, separately, the aligned early-swap control. Candidate groups are:

- `early_attention`: attention0–7 (8 modules)
- `pointer_attention`: attention8–12 (5)
- `late_attention`: attention13–17 (5)
- `pointer_mlp`: MLP8–12 (5)
- `late_mlp`: MLP13–17 (5)
- `pointer_all`: attention/MLP8–12 (10)
- `late_all`: attention/MLP13–17 (10)
- `all_attention`: attention0–17 (18)
- `all_mlp`: MLP0–17 (18)

These are fixed architecture partitions around the independently established pointer depth boundary. No singleton ranking determines membership. The exact self and all-module ceiling instruments remain unchanged from V2. For each family, a group must recover the late backward target with projection at least 0.50 and cosine at least 0.70 while late-forward and early-backward control RMS are each at most 0.50 of target RMS. Native accuracy must remain at least 0.75 and mean target at least 1.0 in every family.

Select passing groups by fewest modules, then greatest worst-family projection, then the listed order. A pass is only a typed-group screen: it must be confirmed on newly frozen lexical rows before registration as a mediator. No pass, gain, subgroup, head split, row change, or threshold rescue is allowed. Price is 23 forwards / 230 sequence evaluations, zero fits, gradients, or updates.
