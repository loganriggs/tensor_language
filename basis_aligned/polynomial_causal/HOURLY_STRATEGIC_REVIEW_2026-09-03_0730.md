# Hourly strategic review — 2026-09-03 07:30 UTC (Claude)

Sign convention §2135: frontier L2 = CE ADDED ABOVE THE REAL MODEL, LOWER IS BETTER; frontier §312 norm-2304 at
+2.6735. Role split: Codex leads direction + owns GPU (rung522 hardening); Claude red-teams + CPU probes + ops.

## Where the program stands — the §2668 inflection

Strict explained fraction unchanged: 5.348% / 10.923% / 4.727 nat / 0 of 68. The MLP10 estimation chapter is
CLOSED this session (§2657-§2668, explanation_0726) with a rigorous, twice-self-corrected bottom line:
- MLP10's reliable causal footprint is one low-dim shared subspace that EXISTS (§2658, bootstrap-stable) but,
  coded by prequential MDL (§2668), captures only ~12% of held-out effect energy and saves ~0 bits over
  mean-only. §2666's "76%" was a signal-fraction, not effect-fraction.
- rung521 (§2662/§2665): the whole-a8 response is reliable but BROAD (non-selective), reproducibly so.
- The wall is uniform: per-unit circuit resolution is noise-limited (§2657, rho 0.016), and the reliable content
  is small. This is measured, not a method failure.

The strategic consequence: the current per-unit MLP10/a8 line has hit a ceiling. Breaking it needs either (a)
MUCH more data (§2659: 26-62x documents to make per-node fingerprints individually reliable), or (b) a better
object than per-source circuit effects. Both are GPU/data-collection = Codex lane; there is no on-direction,
non-redundant, CPU-executable probe left for me on the current bundles (verified this wake: MLP10 exhausted;
cross-module needs a 2nd-module across-source bundle §2667; frontier price-gap data exists on disk but is off
Logan's circuits-not-compression steering).

## Largest gaps
1. Tail dictionaries / COVERAGE CREDIT — ADVANCED this session: §2668 gives the rigorous bits-saved number for
   the shared subspace (~0), the first principled coverage-credit datum. The general accounting (bits-saved per
   validated result, additive) is now well-defined by the MDL frame.
2. m16 remainder — CPU-blocked (no committed bundle).
3. attn5's write price cliff — CPU-blocked; off current steering.

## Candidate moves (brainstorm, pruned)
- **Raise-N re-measure of the shared-subspace held-out coverage** — the DECISIVE test of whether the 12% ceiling
  is N-limited (fixable) or fundamental. Highest information. GPU/Codex lane -> PREREGISTER as a proposal.
- **Better-object re-measure** (fewer, larger-support circuits, or a higher-signal downstream readout) — GPU;
  propose.
- **Cross-module JIVE shared dictionary** — data-blocked (needs a 2nd module source-star bundle).
- **MDL bits-saved accounting of other validated results** (sign gauge §2633, locality laws) — CPU but
  bookkeeping; low mechanistic value; deferred.
- **Frontier price-decomposition** — data-available but off-direction; pruned.

## Ranked top five
1. **Raise-N re-measure (does 4x-32x documents lift held-out coverage above the §2668 12% ceiling?) — EXECUTE as
   a CPU-side PREREGISTRATION for Codex's GPU lane** (the decisive forward experiment; I cannot run GPU/Codex owns
   direction, so I freeze the design + cost).
2. **Better-object re-measure** — propose to Codex (higher-signal downstream readout).
3. **Cross-module JIVE** — propose (needs 2nd-module bundle).
4. **MDL coverage-credit accounting of validated results** — CPU, deferred (bookkeeping).
5. **rung522 post-hoc red-team** — execute when its science lands (waiter armed).

## Executed
Move 1: froze `MLP10_RAISE_N_COVERAGE_REMEASURE_RUNG_PREREGISTRATION.md` — a GPU re-measure of the R520
source-star circuit effects at 4x/16x documents, testing whether the shared-subspace held-out captured fraction
(the honest §2668 metric) rises materially above 0.12, with Spearman-Brown-predicted targets and a literal
forward-count price. Proposed to Codex (not enqueued: GPU data-collection, Codex's lane and direction call). This
is the decisive test of whether the per-unit MLP10 ceiling is N-limited or fundamental. No new CPU probe enqueued
this wake — after this session's over-produce->correct pattern and the just-closed chapter, forcing a redundant
MLP10 re-analysis would be negative value; the honest top move is the raise-N experiment, which is Codex's to run.
