# Does MLP8 create the mixed value consumed by attention9?

Authority: original handoff/pilot and THREE_HOURLY_MATHEMATICAL_REVIEW_2026-09-10_0749.
Novelty: previous all-MLP final-write partition omitted subsequent nonlinear responses;
this tests MLP8's explicit path through RMS and the partial L9 value producer.
Same32original/fronted worlds, complete five-factor grids; no new OOD claim.

At actual normalized MLP8 input, project Left and Right activations into 0,o,h,oh.
New=Down(Lo*Rh+Lh*Ro); inherited=Down(L0*Roh+Loh*R0).
Their sum equals Qoh MLP8 output; output bias has zero mixed component.
For each full/new/inherited branch D set xprime=(x-lambda9[0]*D).to(native dtype),
recompute native RMS, and compile Cprime using native P0/WV/WO/1.65625.
Install B+Cprime where B=Anative-Cobserved at to/action; recompute native suffix.
This is a partial producer path intervention. Other MLP8 consumers and QK stay native.

4native/local captures +6arm forwards perworld=320forwards5120seq,0fits,600s cap.
Capture v3 exposes existing source8 data; completed v2 preserved. All545902902weights
retained, zero structural saving. Full native prefix and counterfactual inputs remain.

A instrument: bound sources, native factor and RMS replay controls, finite/cleanup/counts;
FP64 new+inherited partition closure<=1e-10 relative; compiled sum vs projected native
MLP8 write<=1e-4 relative, native C bridge<=1e-4; unchanged native logits replay bound
RAW_REMOVAL parent maxabs<=1e-3 ANDrelative<=1e-5. Parent fullCremoval is reference,
not a new forward. Reuse planted new-only/inherited-only and permutation controls.
B total source necessity: total MLP8 removal matches full C removal <=.10 relative,
BOTH Q correct margin and centered Q three-readers, EVERY world.
C newly multiplied branch: new removal matches total MLP8 removal <=.10 relative on
both readouts AND total MLP8 effect norm is >=.10 of full C removal in both.
D inherited branch: same criterion for inherited. Small/zero denominators fail.
E composition: total MLP8 effect equals sum of new/inherited effects within .10 relative
on full correct-margin and centered three-reader tables, EVERY world.
B failure forbids naming MLP8 the full source. C/D can localize a partial source only
with explicit B status. Neither C nor D means retain their joint effect; no lesser-error
winner. New here means multiplication at this interface, not newly identified semantics.
