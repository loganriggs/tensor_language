# Fresh V2 reuse of the coupled direct-plus-MLP8 value path

Test whether the frozen coupled value path reuses on a second outcome-blind FineWeb panel. Select the next 20 eligible documents after every existing frozen row manifest, with the same selector, city pairs, 32-token windows, 240 fixed endpoint/control probes, and no capability or outcome filtering. Do not refit, change the operator, or rescue reversed documents.

Run the same five arms: native, complete city swap, swap minus direct value term, swap minus MLP8-mediated value term, and swap minus their coupled joint correction. Use native normalization and the full suffix. Require the existing prediction, support, finite/count, target, control, and signed suppression gates. Report the global group and any native-reversed/other subgroups separately. Composition passes only if both subgroups meet the interaction and preservation gates; a global pass cannot promote the decomposition when a subgroup fails.

This is a reuse/composition test for the declared coupled boundary. It does not establish token-only execution, full-model extraction, or simplicity.
