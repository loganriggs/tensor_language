# Trace the sign reversal through the native suffix

WEIGHT_FOLDING handoff after FineWeb removal and interchange both fail directional gates while their packed generators predict native effects. Freeze the exact native complete-city interchange from CITY_DROP3_FINEWEB_INTERCHANGE_V1; use all20opened documents, no exclusions. Native/edited model pair,80sequence-equivalent forwards/36batched block calls,2CPUthreads,180seconds. CPU only.

Path endpoint: final spelling-margin reader pulled back through final RMS and softcap to transported residual writes. Split attention9 into its nine native output-head maps, retain MLP9 and attention/MLP10–17 plus final normalization. The edit has zero support at the final query token, so block8 final-token residual change must be zero; later contextual attention is necessary. Preserve full native suffix rather than guessing a restricted executor.

Exact finite-change identity: Δr17=sum_l gamma_l Δwrite_l, gamma_l=product of later residual lambdas. Normalize each term by edited final RMS, add native final state times inverse-RMS difference. Contract with the unembedding reader using exact endpoint-specific softcap secants. This is response attribution after the upstream intervention, not an intervention on a downstream head.

pred_a: replay native/exact-swap scores from prior receipt abs<=1e-4 ANDrel<=1e-5; final residual closure<=1e-4relative; margin closure<=1e-4absolute AND1e-3relative; split attention9 heads replay<=1e-4relative; block8 last-token delta exactly0; finite/CPU/counts.
pred_b: transported attention9 response alone predicts target effect to<=.35relative in BOTH reversed-capable documents and remaining documents. Reversed document set is fixed by prior native paired attenuation<0, not by this census.
pred_c: keeping all attention9 heads plus MLP9 and output normalization predicts target effect<=.35relative in BOTH groups.

Report signed aligned fraction and norm ratio together for every head/module, paired attenuation contribution per document, and preservation-reader contributions. Do not choose a new subset from this panel and call it confirmed. A b/c failure requires later context-dependent responses; a pass only nominates an exact reader/weight fold with separate fresh intervention required. Existing typed-face response census is prior art for the identity; novelty is locating these new cross-corpus sign reversals and the head9 recipient split.
