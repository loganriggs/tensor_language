# Recursive key-prefix extraction screen

Freeze both13-update supports. At each prefix layer, apply actual residual
reentry. Execute selected attention/MLP updates on the candidate's current
state; replace omitted updates with that position's write from the native
zero-input trajectory. This is different from selecting saved native updates.
Initialize shared first-layer values directly from the actual normalized input
if attention0 is omitted. Preserve native RMS, RoPE and signed value mixing.

Generate cue-key inputs at8/9/13 from tokens alone for the eight geographic
prefixes. No saved actual prefix state is an input to this generator. All26
updates form a positive control; compare its downstream first-value write with
native execution. Record literal selected attention/MLP call counts and cache
storage, including the one-time zero-trajectory work. Frozen support selection
used original prefixes only. Do not refit supports to these results.

A: native producer/attention/first-branch replay and all26-recursive write replay
<=1e-5, and exact live-call counts match each frozen support. B: A plus forward13
geographic write error <=.1 in EACHfamily. C: A/B plus signed-prefix effect error
<=.1 in EACHfamily,6/6paired transfer signs match native, unrelated meanabsolute
effect <=.5regional meanabsolute effect in EACHfamily. Backward is diagnostic.
The earlier passing mixed-term correction is not silently supplied: it requires
native omitted information which this extraction test explicitly cannot use.

Null: retained updates depend on omitted upstream computation, so conditional
support fidelity does not imply recursive extraction. A miss does not reject
cross-module feature extraction or an alternative shared background. Native
queries and downstream context remain external even if the key generator passes.
Price10fullbodybatches48rows,8prefixes x3supports through13update layers,
two zero-trajectory lengths,5suffixarms,180secmanagedcap,<4MBsavedartifact.
