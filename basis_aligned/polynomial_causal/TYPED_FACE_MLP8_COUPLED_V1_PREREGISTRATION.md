# Coupled local executor precision and standalone boundary

Three declared native arrays: normalized current8, donor_city8, raw postattn8.
Own full head8 retained-write weights and all MLP8 Left/Right/Down factors.
Return delta8+deltaMLP8; no native call inside exported executor. Whole suffix
remains external.40now-opened fresh-panel sequences,240endpoint rows.
Native/native8 midpoint/compiled block9 reentry =120forwards,300seconds.

pred_a: original native/midpoint replay fresh artifact arms[0,3]<=1e-5abs;
full block9 delta error<=1e-4relative; zero outside mask; finite,120forwards.
pred_b: compiled vs native8 all readouts <=1e-4maxabs AND <=1e-5relativeL2.
pred_c: every family's target and four control effect errors<=1e-3relativeL2.
pred_d: isolated copied-package CPU reproduces40native joint-delta fixtures
<=1e-4relative each; zero strength zero; unknown token rejected. Report literal
weight/state/interface price. No fresh science, whole-suffix extraction,
compression or causal-composition claim. Preserve failures without gate rescue.
