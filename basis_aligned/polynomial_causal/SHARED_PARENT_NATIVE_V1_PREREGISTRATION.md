# Shared parent: live executable preservation

14 September2026, before enqueue. Reuse the existing632-forward selective-pair
test and its unchanged A–E predicates. The frozen sparse weights are unchanged.
Only repeated input-key projection and adapter preparation change. Check original
and new scalar executions on every actual pristine/edited context; new F requires
relative scalar error<=1e-10 throughout. CPU replay is not sufficient to assume
GPU precision or full native manipulation preservation.

Preserve historical-reference misses caused by machine differences rather than
relaxing E after seeing results. F is matched locally but is scalar-level evidence,
not a replacement for native capability, removal, donor and unrelated-control bars.
No new corpus/OOD or full-model speed claim.632bodyforwards plus two scalar
evaluations per invoked hook;300seconds. Extra resident adapters256KiB; no additional
serialized weights, no fitted parameters. Source/bindings and row checks precede
managed lane1 enqueue. The old portable package and receipts remain unchanged.
