ACTIVE_TRACK: WEIGHT_FOLDING

# Strategic checkpoint — 22 September 2026, 06:18 UTC

Track continues under the user's two-day weights-first authorization through 15:10 UTC. The full goal remains a smaller executable circuit with computational specification, cross-module grouping and within-module splitting, held-out/OOD prediction, extraction/sufficiency, selective manipulation, composition/reuse, and stable identification. None follows from compression or reconstruction alone. Full third-order tensor scope remains open alongside the selected last-two-MLP quartic investigation.

Since 05:18, the native local-quartic Adam/Muon experiment completed at05:53; the normalization-stage capture completed at05:53; native L-BFGS completed at05:57. Larger-panel consumers, normalization diagnostics, cross-domain scoring, and the new-feature readout oracle were executed and interpreted. Adam improved smaller-output errors modestly, Muon barely changed them, and the toy L-BFGS advantage did not transfer. Even evaluation-fitted shared readouts leave large residuals. Five lean removal failures cross10% after local denominator weighting despite raw errors below10%. These are instrumented negative results, not circuit adoption.

The current route learns new input directions using exact Gaussian moments plus empirical native-sensitivity moments. The selected native path is the bias-free MLP16 contribution entering both MLP17 multiplicative factors, read along16 fixed writers. It omits other self/cross terms. The fixed CP512 parent receives96 new quartic atoms for outputs4–15. The experiment changes the function-space metric at matched capacity and fitting budget; it can clarify whether normalization-sensitive prediction is recoverable in this arithmetic class. It cannot establish semantic selectivity, and adding capacity alone is not the final simplicity objective.

Opposing predictions and stopping rule: two hybrid starts must improve both larger-panel value and matched-response errors15% versus matched GaussianAdam, while retaining90% of Gaussian explained residual energy. Failure demotes this metric mixture; do not expand a tuning sweep to manufacture a passing result. Candidate selection uses calibration objective only. Actual exported coefficients are used for Gaussian fidelity, avoiding reprofiled-score optimism.

Confounds: the 16384-state panel is already opened;256documents, not16384independent samples. Pooled error masks weak outputs. State-wise ratios can explode near zero targets, so common-scale errors and error-energy concentration are also reported. Sensitivity is frozen tangent geometry, not finite causal removal. The current repair excludes coordinate1 and cannot fix its existing removal failures. Fixed writers remain a probe basis with no monosemanticity guarantee. Readout-oracle fits use evaluation labels and are diagnostics only. Hash alignment and packed export checks pass; an import mismatch and64/65-token hash convention were resolved before queueing.

Alternatives ranked: (1) finish this matched hybrid test, because both pure metrics already have distinct failures; (2) characterize remaining input-dependent error with calibration-selected hypotheses and document-held-out validation, rather than retuning frozen readouts; (3) return to full-third-order arithmetic structure and broader output coverage using the established product lower bounds as controls. No additional toy optimizer sweep is justified. If hybrid fails, changing the structural hypothesis is preferable to a dense learning-rate search.

Throughput:05:18–05:46 was dominated by a live shared GPU queue, with diagnostics prepared. Native local fitting consumed446.5s, geometry capture about3s, and L-BFGS228.1s. Larger-panel cached evaluation took1.5s; cross-domain scoring5.9s; the new-feature oracle about2s.06:07–06:17 implemented, tested, documented, and queued the hybrid successor. GPT2 finished06:16:34; Pythiav772 is the current predecessor. Own job remains queued. No direct GPU process or duplicate run was launched. Reused contraction/scoring helpers avoided a new framework. No precise CPU authoring/review split was instrumented; do not invent one.

TRACK_ALTERNATION: OVERRIDDEN by explicit user weights-first period; no approval needed.
TRACK_PROGRESS: PASS — completed native folded-path fits, causal geometry diagnosis, and quantified failures.
CEREMONY_BUDGET: PASS at this implementation boundary — reused15CPUchecks and a native-shaped smoke, no bespoke infrastructure; waiting was shared compute dependency.
NOVELTY_LESSON_GATE: PASS — prior empirical-only overfit, Gaussian-only modest gain, optimizer-transfer failure, and fixed-feature oracle limits determine the new mixed-metric hypothesis.

Continuation receipt: dcb967b48 implements run_hybrid_local_quartic_v1; managed queue hash5b0183cbf680bc48ef37c2cdbba433d04036e1e8724119a0bdd583a891c37f56. Report3c7706d9b explains samples and errors. Latest three-hour math review05:06 remains current; next due08:06. No completion or OOD adoption claim.
