# Full-unembedding extension of the paired producer metric

12 September2026. Context confirmation of the selected two-output program fails five swap cells, despite all removal/write checks passing. This next experiment returns to the user's full-unembedding objective rather than tuning that slice further. Existing single-layer output-rank work is in FULLU_OUTPUT_FUNCTIONS_V1_AUDIT.json; this test adds the preceding producer's exact quadratic-function metric.

For every vocabulary row, the outer form is $S_v$ and its paired two-layer coefficient matrix is $P^T S_v P$. Set $H=PP^T$ and transform the native MLP17 readers to $L_{17}H^{1/2},R_{17}H^{1/2}$. Reuse the exact output-function spectrum routine on these readers and the entire unembedding. Its output Gram equals that of the complete paired tensor without materializing it. The explicit small full/centered-unembedding control agrees within2.4e-15. This norm preserves the input pairing; it is not fully symmetrized quartic Frobenius error or a native-distribution norm.

Compare four frozen arms: full versus centered unembedding, each with identity versus producer input metric. Report spectra at fixed output ranks1,2,8,16,32,64,128,256,512,1152. Only centered rank32 is the registered behavioral comparator; these ranks are descriptive spectra, not an outcome-selected behavioral sweep. The centered producer arm is primary, centered identity is baseline.

Predictions:

- A: old uncentered identity spectrum replays within1e-8; spectral, output-metric orthogonality and physical projector identities pass1e-8; new full-path reference effects are finite.
- B: centered rank32 captured coefficient fraction improves at least10% relatively after producer folding.
- C: the primary centered rank32 approximation of the **complete** pure bias-free MLP16→MLP17 path passes all original developmental families: swap relative RMS<=.10, sign agreement>=.90 with4livepairs, removal CE disagreement<=.02nats and physical-write error<=.05.

The reference is now the entire two-layer path residual write, not the old selected two-output reference. Reuse the established native input/pre/output cache and numerical tail scorer with this newly specified exact reference. This tests extraction/sufficiency and intervention prediction for a broader object. It still does not identify semantic units. No text fitting, new body forwards, attention-path approximation or corpus-OOD claim.

At rank32 the projected decoder can be stored as32-by4608latent Down coefficients and1152-by32residual writers:184,320 values. Native MLP16 and MLP17 L/R require26,542,080 additional values; the exact unprojected two-layer path has31,850,496weight values. Native scale, normalization and teacher background remain dependencies. This is a conditional implementation price, not a full-model deployment saving. Inner sparse/block/shared-function decomposition remains necessary even if an output-rank screen passes.

Managed GPU only; four1152D output eigensolves and Gram products,600second limit, no separate price run because the same native-size Gram/spectral kernels have already been measured. The null is that folding changes the spectrum without yielding a useful full-path approximation. Report it as such, preserve failed predictions, and do not infer absent arithmetic structure from this output-rank restriction.
