# Joint attention11 query context reuse

Freeze Q1 and Q2 together at baseline while applying existing23-port oracle edits; full/Q1/Q2 singleton replay arms. Four OODv2 contexts, subject and attractor, nine contrasts. No refit.12prefix40native suffix calls: baseline, joint-baseline replay, and four arms for each role. Baseline hooks and full/singleton effects must replay prior freeze result<=1e-8. Number error<=10%, every control error<=5% relative to full number-effect L2; native retention>=.8 and max collateral ratio<=.1 in all16cells. Report mixed query effect joint-single1-single2+full, normalized by full effect and by the smaller singleton error (weak-effect caveat explicit). Null: singleton successes fail composition; alternative: joint reuse passes these gates. No fresh OOD, independent source/reader extraction or full-model savings claim. Baseline query computation is still charged.
