# Rung456 preregistration: independent vocabulary fixed-scale reproduction

Status: registered after rung455 froze the fixed-scale definition from already-open data and before this experiment
loads rung451's independent192-document role. GPU execution must use the managed queue. SEALED_CONFIRMATION is
forbidden.

## Object and computation

Rebuild the exact same23 rung454 vocabulary programs, all of which keep the input token embedding exact and change
only the final output vocabulary map. On rung451's candidate-consequence-disjoint192 rows, capture the final hidden
states under native, original-native attention16 mean knockout, and the physical MLP16 rank2 partner once each. Score
every full-vocabulary candidate exactly with the native soft cap and cross entropy.

For candidate `P`, physical partner `Q`, and native model `N`, use rung455's frozen composition score

`||L_PQ-L_P-L_Q+L_N||_2 / ||L_Q-L_N||_2`.

The denominator is one fixed physical-partner effect within a role. Retain the registered removal score as a common
control. Use the same14 adjacent-rank edges and fixed independent waves rows0:96 and96:192. Compare the23 full-role
fixed scores with rung455's old96-document values without fitting or changing a threshold.

## Predictions, bars, and strong null

- **A — exact/live instrument:** all source, bank, row, fixed-spec, fit, and factor hashes hold;23 IDs/prices,
  output-only scope, sparse selections, matrix dimensions, native replay,32,768 attention-mean positions,48 knockout
  calls,48 partner calls, and SEALED closure hold.
- **B — independent continuous reproduction:** old-versus-independent23-arm fixed-score Pearson correlation is at
  least`.90` and mean absolute shift is at most`.10`.
- **C — independent structured ordering:** at least`.85` of the14 frozen rank edges order for both fixed composition
  and removal on all192 documents.
- **D — wave reliability:** each96-document wave orders at least`.70` of the14 edges for both scores, and the
  continuous23-arm wave-to-wave Pearson correlation is at least`.70` for both.

**Strong null:** A fails; the physical partner or any candidate is inert; old-to-independent fixed-score correlation
is negative; either full structured accuracy is below`.50`; or either wave correlation is negative. Passing confirms
that the fixed-scale diagnosis reproduces on independent documents. It does not rewrite rung454, count vocabulary as
teaching family3, fit a consequence predictor, establish semantics, deploy values, or open attention0. A separate
protocol decision and the genuinely new outcome-free third family required by rung454 still follow.
