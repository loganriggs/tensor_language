# Preregistration — late_tail_gate_frame_identity_probe (Claude, LANE 1 CUDA) — Registered Registered 2026-09-04 03:18Z (box clock) (box clock)

Sign convention (§2135): every CE number is CE ADDED ABOVE THE REAL MODEL on held-out docs 0–63 (FRESH split; second moments from docs 96–191) — LOWER IS BETTER. Every CE arm is expected ≥ 0 (gating by a projector loses information relative to the exact block); each prediction states its operands' signs; absolute CE bars state the wobble they include (~.003 per arm; 2 × .003 on a difference of two arms).

## Question
§2806: one pooled 256-dimensional core frame gates all ten late tail reads at .0442. WHAT is that frame? Three named alternatives, each a 256-dim core subspace applied identically at all ten blocks with the same instrument: (i) the core's VARIANCE frame — the top principal directions of the pooled core second moment M̄_c (if the gate simply reads the dominant core variance, this frame prices like the shared frame); (ii) the TOKEN-IDENTITY frame — the unembedding's top right-singular directions projected into the core (if the core tells the tail read "which token is here", this frame prices like the shared frame); (iii) the WEIGHT frame — the top eigenvectors of the unwhitened pooled gate Gram Ḡ (data-free; recorded, no bar). Two further questions on the shared ladder itself: where it reaches (384, 512 modes), and whether the quantitative regularity seen post hoc in §2806 — SHARED_k ≈ 1 − (pooled captured energy at k): k = 32/64/128/256 read .1209/.0981/.0740/.0442 against 1 − .882/.903/.926/.954 = .118/.097/.074/.046 (deviations .003 .001 .000 .002) — holds at the two NEW ranks it was not fitted on.

## Instrument
Parent late_tail_gate_shared_frame_probe (§2806): same Gram, pooled whitening M̄_c, Pi_shared(k), GateP(l, Pi), retained energy. New frames (all 768×768 core projectors, applied at all ten late MLPs): PCA_k = U_k U_kᵀ with U the eigenvectors of M̄_c (k = 128, 256, 512); WEIGHT_256 = top-256 eigenvectors of Ḡ (orthogonal projector); UNEMB_256 = orth(U_coreᵀ V_U[:, :256]) with V_U the unembedding's top right-singular vectors (orthogonal projector); RANDOM_256 = M̄_c^{1/2} Q Qᵀ M̄_c^{−1/2}, Q seed-0 random. Arms: SPLIT8_1024; GATE_EXACT; UNIFORM_128 (own frames; §2799 .0627); SHARED_256/384/512; PCA_128/256/512; WEIGHT_256; UNEMB_256; RANDOM_256. Weight-side: pooled captured energy at 16…512; pooled retained energy tr(Ḡ Π M̄_c Πᵀ)/tr(Ḡ M̄_c) and per-block retained energy under every frame; overlap of every 256-frame's read space with the shared-256 read space (random pair .333). Price: 96 fit + 64 × 13 ≈ 930 GPU document-forwards (~35 s).

## Predictions (scored exactly as written) — hypothesis value / null value / operand signs / wobble included
- pred_a_instrument: |baseline − 3.0322401| ≤ 1e-4; |SPLIT8_1024 − .0374| ≤ .015; |GATE_EXACT| ≤ 1e-3; |UNIFORM_128 − .0627| ≤ .015; |SHARED_256 − .0442| ≤ .015; Gram check ≤ 1e-6; frame check ≤ 1e-6.
- pred_b_gate_frame_is_not_the_variance_frame: PCA_256 − SHARED_256 ≥ .02 (includes 2 × .003). Null: ≤ .005. Signs: both positive. Worked example: the shared read space is M̄_c^{−1/2}V̄; it coincides with the variance frame only if Ḡ is isotropic over the core. If the weights select specific core directions (Ḡ structured — §2804's per-block whitened Grams have eff-rank 1.6–21 and rank-90 ≤ 138), the variance frame at 256 retains ≈ .6–.8 of the pooled energy against the shared frame's .954 and prices ≈ .1–.2 (+.06 to +.15); if the gate simply reads the dominant core variance, PCA_256 = SHARED_256 within wobble (≤ .005).
- pred_c_shared_ladder_reaches_025_at_512: SHARED_512 ≤ .025 (includes .003). Null: ≥ .04. Sign: positive. Worked example: pooled captured rises .926 → .954 from 128 to 256; if it rises a further ≈ .03 to ≈ .98 at 512 the §2806 regularity gives ≈ .015–.02; if the remaining energy is spread thinly over the last 512 dims, ≥ .04.
- pred_d_cost_equals_lost_pooled_energy_at_new_ranks: max over k ∈ {384, 512} of |SHARED_k − (1 − captured_k)| ≤ .01 (includes .003; captured_k = Σ_{i≤k} λ̄_i / Σ λ̄_i of the pooled whitened Gram). Null: both deviations ≥ .02. Signs: SHARED_k > 0, 1 − captured_k ∈ (0, 1). Worked example: the four fitted ranks deviate by ≤ .003; if the regularity is a law of the nested pooled family it holds at 384 and 512 to ≤ .01; if it was a coincidence of four points it fails by ≥ .02 at one of them (e.g. cost stops falling while captured keeps rising).
- pred_e_gate_frame_is_not_the_token_identity_frame: UNEMB_256 − SHARED_256 ≥ .02 (includes 2 × .003). Null: ≤ .005. Signs: both positive. Worked example: if the core's control of the tail read is "which token is current", the unembedding's top-256 core directions retain most pooled energy and price within wobble of the shared frame; if the control is a different feature set, UNEMB_256 retains far less and prices ≥ +.02 (a random 256-frame prices ≈ .2–.3).

Bars: {"ce_tol": 1e-4, "repro_tol": 0.015, "exact_tol": 1e-3, "gram_tol": 1e-6, "frame_tol": 1e-6, "floor": 0.002, "b_margin": 0.02, "c_cost": 0.025, "d_dev": 0.01, "e_margin": 0.02}. Null bars: {"b_margin": 0.005, "c_cost": 0.04, "d_dev": 0.02, "e_margin": 0.005}.

## What each outcome means
b TRUE and e TRUE: the shared gate frame is a SPECIFIC control subspace — neither the dominant core variance nor token identity; its directions are a new object (next: what they read — the §2803 linear-read directions, attention-written features, position). b FALSE with null met: the gate reads the dominant core variance — the program simplifies to "gate by the core's top-256 PCs", no gate-specific frame at all. e FALSE with null met: the gate reads token identity — the tail read is switched by the current token, which ties the lineage to §2800's rare/novel-token result. c and d TRUE: the ladder is a law (cost = lost pooled energy) and the 512-frame costs ≤ .025 — the program's rank/cost trade-off is then computable from the Gram alone, without CE runs. d FALSE: the four-point regularity was a coincidence; the ladder must be priced by CE at each rank. Nothing installs into the §312 frontier; the strict explained fraction is unchanged by any outcome.
