# Coupled routing/value program through the native suffix

Freeze corrected recipient rows0/12/24/36 and adjacent donors. Eight native
capture passes supply source ports and post-attention states. For each recipient,
test all25(a,b) in{-1,0,.5,1,2}² with both a native source-write intervention and
a compiled post-MLP9 replacement.208 total forwards,180second cap.

A: post-MLP9 state and all-vocabulary score relative errors<=1e-5 per case.
B: baseline-subtracted regional target/control discrepancies<=1e-5+1e-4 times
absolute native effect per case; zero-strength effects exactly zero.
C: coupled prepared tensor bytes<=93%generic prepared bytes for each prefix.

Preparation uses original weights, native pristine post-attention state, and
directly contracted routing/value/mixed source writes once per prefix. The mixed
strength is a*b; independently edited mixed amplitudes are outside this interface.
No fitted targets or quantization. Null: local FP32 error grows through the suffix,
or real prepared-context costs erase the predicted saving. Native prefix/suffix,
source ports and all original weights remain explicit dependencies.
