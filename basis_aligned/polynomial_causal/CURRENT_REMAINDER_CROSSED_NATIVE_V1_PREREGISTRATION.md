# Routing/value interchange in the full current remainder

Freeze all48 article-corrected rows and original weights. Rc(r,v)=G_even(r)Z_R(v),
where Z_R(v)=(1−lambda)V(v)−(V(v)·c)u. Opposite-cue donor is the adjacent paired row.
Hold the recipient native prefix/suffix fixed; replace only this component's write.

Compute independent changes dG Z, G dZ, dG dZ and full donor G'Z'−GZ. Do not
define the mixed term by subtraction. Arms: native, routing-only, values-only,
joint, mixed-only, sum of all three fields.288 forwards,180second cap.

A: old native anchor and joint/recomposed scores relative error<=1e-5; native
component self-reference and field identity<=1e-10. B: value-only paired-cue
effect error<=20% of joint AND joint cue norm>=1%full-head effect, each family.
C: sum of three separately measured effects predicts joint within5% for paired
cue and individual control, each family. D: omit mixed effect and still meet5%.
Effect denominators use floor1e-8. Record all gates separately.

Null: both routing and values are consequential, or their interaction is needed.
No universal sufficiency, semantic role or static compression follows from a pass.
Charge all original model weights/context generators and FP64 cached routing
T×T plus value T×128 ports per prefix. No new learned model coefficients.
