# Hourly strategic review — 2026-09-02 14:30 UTC

## State
491 (MLP1 live-state source decomposition) running since 14:18 (8,000
forwards + conditional validation); pre-audited with the
NUMERICAL-remainder control endorsed. Ledger current (§2611). Queue depth
0 behind the run by stated design (cascade registering every ~20 min).
Explained fraction unchanged: 5.348% / 10.923% / 4.727 nat / 0 of 68.
The 487→490 cycle delivered the third standing claim in 43 minutes; the
user-requested PROGRAM_COMPENDIUM shipped this hour (repo root + page).

## EXECUTED — two CPU probes, both decisive
1. **The single-index law goes PREDICTIVE.** Leave-one-pair-out test on
   the 474 per-token bundle: fit a per-token isotonic (monotone) readout
   of the additive singleton-sum index on 7 of 8 subset effects, predict
   the held-out pair. The monotone single-index model beats the additive
   model in EVERY window×source cell — median |error| ratios .688/.768
   (code N/H), .612/.784 (wave0), .782/.630 (wave1): a 22–39% error
   reduction, and REGISTER-UNIFORM (unlike the screens, natural benefits
   as much as code). Caveats stated: 8 points/token, 3 LOO pairs,
   auto-direction isotonic on tiny samples. Status: the law is now a
   predictive model of held-out factorial cells; the CAUSAL version
   (predict genuinely NEW interventions, e.g. scaled deltas) is the
   registered-material GPU rung for the natural arc — frozen in this
   review as the design: fit h_t on the 8 frozen subsets, predict
   half-delta and 1.5×-delta singleton effects, bars from today's LOO
   distribution (beat additive by ≥15% median), null = the law is
   interpolation-only.
2. **The §1–§803 mystery CLOSES.** Git-pickaxe: §200/§300/§400/§500/
   §700/§803 never existed in ANY commit; §100-era = qk_mdl's
   RESULTS_l0_mdl.md (07-31). §804 was a namespace JUMP, not lost
   content. PROGRAM_COMPENDIUM.md + the artifact corrected; the planned
   "recovery pass" is now unnecessary — struck from the follow-up list.

## Ranked top five
1. 491 scored as written on landing (waiter armed): the shared-modulator
   question, with the A1 prior stated unofficially.
2. The causal single-index rung (design frozen above) — enqueue at the
   next main-line pause.
3. 492+ per Codex routing (source-removal/composition if 491 passes;
   two-dimensional integrated response if not).
4. ε-bisimulation contracts — now with three claim-candidates.
5. Coverage line (m16 remainder, attn5 cliff) — deferred until the
   MLP1-source arc settles.

Pruned: the §1–803 recovery pass (unnecessary per probe); standing prunes.

## Cadence
Explanation for 489–491 is Codex-owned per their 13:22 claim; my next
explanation lands at the 491/492 chapter verdict if theirs doesn't cover
it. Next math review ~16:10.
