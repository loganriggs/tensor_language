# Twelve-vector attention consumer through native suffix

Use the existing four short recipients and four longer natural recipients,
with their fixed adjacent donors. Twelve distinct capture passes plus8×25×2
native/compiled comparisons =412forwards,180second cap. Source remains the exact
coupled MLP9 program. Compile attention10 directly into12vector ports using the
shared denominator; do not invert the expanded16basis.

A: post9/post10 states and full-score errors<=1e-5 relative per case.
B: baseline-subtracted regional target/control or natural next-token CE and
newline-minus-comma discrepancies<=1e-5+1e-4abs(native effect), every case;
zero-strength effects exactly zero. C: factored attention consumer payload<=85%
expanded16consumer payload, every context. D: combined source/attention/x0
payload<=independent source/native-attention/input payload, every context.
Expect D may fail at long prefixes; retain it separately from C.

All25 strengths{-1,0,.5,1,2}², mixed strength a*b, same rows and weights as earlier
checks. Original generators remain globally required. No fitting, quantization,
speed, corpus independence or new semantic-role claim. Save native x0/lambdas
with the first example, alongside source and attention programs.
