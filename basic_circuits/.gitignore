# regenerated artifacts (produced by the scripts; not source).
# Each experiment's figures live in <experiment>/results/ and ARE tracked;
# only the json summaries below are regenerated and ignored.
pullback_summary.json
uand_results.json
