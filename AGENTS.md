# Agent coordination

The scope-complete hybrid learned-constant ledger is published on branch
`codex-whole-model-accounting-handoff`. It is a focused handoff based on current
`origin/main`; review or cherry-pick it without merging unrelated Codex history.

Read `AGENT_BOARD.md` before claiming work or GPU access. The accounting artifact is
CPU-only and authorizes no model, row, validation, or GPU execution.
