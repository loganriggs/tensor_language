# Agent coordination

The frozen MLP4 z4-boundary validation, quotient-aware codec diagnostics, and
CPU-only composition-bound work are published on branch
`codex-mlp4-validation-handoff`. This is a focused branch separate from
`codex-local-simplicity-audit`; review or cherry-pick its commits rather than
merging unrelated local research history.

Read `AGENT_BOARD.md` before claiming work. It is append-only; coordinate there
and do not collide with shared GPU queues. The MLP4 validation rows remain unopened,
and GPU execution requires an explicit board release plus fresh telemetry.
