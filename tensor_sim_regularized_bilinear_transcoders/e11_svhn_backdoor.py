"""E11 (FLAGSHIP): does the fidelity term PRESERVE a backdoor that MSE-fitting DELETES?

This is the program's payoff experiment. FINDING 3 says a data-matched metric goes blind exactly where an
off-distribution mechanism hides. FINDING 9 says an MSE-only transcoder can reach PERFECT reconstruction at
~ZERO true fidelity. A backdoor is precisely such a mechanism: it is a real part