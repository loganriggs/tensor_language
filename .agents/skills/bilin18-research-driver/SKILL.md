---
name: bilin18-research-driver
description: Keep the bilin18/Theseus mechanistic-interpretability research program actively advancing, coordinated, mathematically rigorous, and periodically reconsidered. Use when working in /workspace/tensor_language or /workspace/theseus-bench on the compiled predictive/manipulable tensor-program goal, including research direction, experiment design, analysis, or GPU coordination.
---

# Bilin18 Research Driver

Advance the project toward a smaller, transparent tensor program that is jointly:

- predictive on fresh and OOD text;
- composable when replacements are installed together;
- manipulable under registered removals, swaps, and edits;
- simpler under literal storage, compute, edge, state, and program pricing.

Low CE alone is not completion. A useful result should improve or clarify at least one
adoption ledger without silently regressing the others.

## Restore working memory

At the start of a research turn, read only the current slices needed to recover live state:

1. `/workspace/tensor_language/AGENT_BOARD.md` (protocol, ownership, latest messages).
2. The newest `explanation_*.md` listed first in
   `/workspace/tensor_language/basis_aligned/polynomial_causal/explanations/README.md`
   (goal, current route, independent routes). When
   `EIGHT_HOUR_SIX_DIRECTION_ROTATION_2026-09-01.md` is live, also read its current status row and
   latest checkpoint before choosing work.
3. Recent commits and both repositories' dirty status. Treat uncommitted queue, log,
   result, ledger, registry, and board changes as live shared work.
4. The tail/relevant sections of `BILIN18_CONNECTION.md` and `BENCHMARK_BACKLOG.md`.
5. The newest mathematical/hourly review and live runner/queue state when GPU work matters.

Do not reread the full historical ledger unless the question requires it. Preserve explicit
retractions and the convention that CE added above the real model is damage: lower is better.

## Default active loop

When a durable goal is active, continue taking the next safe, in-scope, high-information step
instead of stopping merely because one analysis or run finished.

1. State the current uncertainty or decision the next step resolves.
2. Compare plausible actions by information value, relevance to the full goal, cost, and risk
   of an invalid instrument.
3. Prefer a discriminating test with written opposing predictions over a broad sweep.
4. Execute CPU analysis immediately when safe. For GPU work, coordinate and preregister first.
5. Score predictions exactly as written; preserve misses, nulls, invalid instruments, and
   corrections.
6. Update durable evidence and coordination state, then commit and push the completed unit.
7. Re-read the outcome before choosing the next step; do not let queued follow-ups substitute
   for interpretation.

Pause only for a real dependency, missing authority, unsafe collision, or a user-controlled
choice that materially changes scope. Waiting for one live run is not a reason to go idle when
useful receipt analysis, mathematical derivation, falsifier design, or documentation remains.

## Hourly strategic checkpoint

At the first safe boundary after each elapsed hour of active work, perform a step-back review.
Use the timestamp of the latest Codex strategic review or board checkpoint; do not create noisy
duplicates within the same hour.

The checkpoint must:

1. Restate the full predictive + composable + manipulable + simplicity goal.
2. Summarize what changed since the prior checkpoint, including failures and corrections.
3. Ask whether the current path is still the highest-information route to the goal.
4. Audit likely confounds: baseline subtraction, frame mixing, nonlinear loss composition,
   shared token difficulty, leakage, dead knobs, insufficiently live tripwires, precision/noise
   floors, and post-selection.
5. Generate or revisit genuinely different approaches—not cosmetic variants of the same sweep.
   Consider function-space modes, tangent/Sobolev matching, predictive-state causal quotients,
   shared invariants, causal-response bases, signed composition algebra, and lower bounds, but
   search beyond this list when evidence suggests a new object.
6. Rank the best next moves by expected information and state what evidence would kill each.
7. Change direction when the evidence warrants it. If it does not, record why the current route
   survives the alternatives.

Write a concise review under `basis_aligned/polynomial_causal/` when it changes strategy or
creates durable reasoning. Post a short append-only board entry for material direction or
ownership changes. A reminder is not itself progress; take the chosen concrete next step.

When a time-boxed rotation ledger is active, protect one decision budget for every listed direction even if
another route looks promising early. At the boundary, write the result or honest no-signal receipt, update the
status row, and immediately start the next direction. Use any remaining final block to compare all routes under
one scorecard and exploit the best one or two. Do not let a long queued GPU run erase the other directions:
leave a preregistration and cheap screen, then revisit the receipt when it lands.

## Shared GPU and repository rules

- Follow `AGENT_BOARD.md`; claim a rung before building and do not take another agent's range.
- GPU work goes only through the managed runner and repository enqueue helper. Never launch a
  competing direct GPU process.
- Every experiment needs registered predictions, measured bars, a stated null, literal price,
  live configuration tripwires, and syntax/dry-run gates appropriate to the script.
- Coordinate before changing queues. Inspect GPU/process state before assuming a lane is idle.
- Preserve unrelated dirty changes. Stage only owned files. Pull/rebase before push without
  stashing or rewriting another agent's live work.
- This workspace is not volume-backed; push each durable completed unit.

## Decision standard

Distinguish three levels explicitly:

- **Screen:** correlation, cosine, or an in-sample fit suggesting an object.
- **Identification:** the object predicts held-out configurations/documents and survives controls.
- **Adoption:** a legal implementation improves the priced Pareto frontier and passes prediction,
  composition, certificate, and intervention checks relevant to its claim.

Do not describe a screen as a repair or a low-dimensional description as a causal mechanism.
When a route fails, extract the mathematical constraint it establishes and choose a route that
changes the object, not merely its hyperparameters.
