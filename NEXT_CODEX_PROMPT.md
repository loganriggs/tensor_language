# Paste this into the next Codex session

Continue the bilin18 tensor-language research program at `/workspace/tensor_language`. Read `/workspace/AGENTS.md` if present, then `CODEX_RESEARCH_SESSION_STARTUP.md` completely, and apply the `bilin18-research-driver` skill. This is a continuation of the existing goal: a simpler executable decomposition with OOD prediction, extraction, selective removal, and composition/reuse. Inspect the existing goal; if this new thread has none, create that goal and actively pursue it. Do not mark the goal complete after one experiment.

The previous instance was expiring on 13 September 2026. First run `/venv/main/bin/python research_session_startup.py` and inspect its restoration needs. Follow `session_recovery/README.md` for environment, model snapshot, saved skill, and Supervisor runner restoration. A read-only startup check does not itself start services. Once dependencies and checkpoint are ready, use `--install-skill --install-runners --start-runners` as appropriate. Do not restart already-live runners or replay completed experiments. GPU work goes only through managed `bqrunner` lane1 with preregistration, dry run and reviewed source hash; `bqrunner2` is CPU-only. These in-scope restoration and execution actions are authorized; do not ask me for routine confirmation.

Read `basis_aligned/polynomial_causal/explanations/for_logan/research_update_2026-09-13_final_compression.md` for the final full report, then the current board tail, result pointers, primary receipts, module dossiers, and both repositories’ Git/live process state. Preserve concurrent Claude work. The original handoff and appended success criterion govern, not `better_math_ideas.md`.

My current direction is compression of composed interactions or retained terms, comparing independent and shared graphs. This explicitly overrides older skill restrictions against compression. The three settings are (1) MLP9→attention10→MLP10 and its larger parent, (2) head17.2→MLP17→selected outputs, (3) full-unembedding→MLP17. Discover from weights first; use FineWeb for in-distribution validation and label Pile as OOD. Count all nodes, edges, surviving producers, adapters, masks, stored weights, and real execution costs. Keep all failed criteria; red-team negative results and execute a discriminating countercheck rather than declaring structure absent.

The best new result is the 25%-entry-pruned, still-64-direction head9.8 parent reader fitted against the complete two-QK even numerator. It passes five corpus averages on 96 additional prefixes and the tested composition/selective-pair aggregates, but has individual/sign exceptions. It saves only 1.806% of the declared parent interface and is not faster. Frozen weights are `SPARSE_COMPLETE_EVEN_FIT_V1_PROGRAM.pt`, key `0.25`; the validated portable package is `extracted_circuits/sparse_even_key_producers_8_2_9_8_v1/`. Native contexts and suffix remain required. More regular two-of-four candidates fail; do not rebrand them as successes or restart completed fits.

Next research action: resume setting2 orientation. Inspect `INTERACTION_SPARSE_REGIONAL_V1_MATH.md`, `SPARSE_INTERACTION_EXECUTOR_V1_MATH.md`, `INTERACTION_SHARED_WRITES_V1_MATH.md`, `HEAD17_OUTPUT_BLOCK_FIT_V1_MATH.md`, `THREE_GROUP_SHARED_DAG_V1_MATH.md`, `SHARED_KEY_VALUE_MOMENT_V1_MATH.md`, and the head17.2/MLP17 dossiers. Determine whether a weights-only error objective composed with the actual retained three-contraction producer can improve the current sparse mixed operator at matched total cost. Derive and execute a small/native-weight CPU control before fitting; account jointly for both QK factors and values and keep RMS/background dependencies explicit. This new fit was not built or queued before expiry. Avoid duplicating prior shared-output, balanced-contrast, coordinate-chart or value-pullback attempts. A final14:55CPU bound in `SHARED_KEY_VALUE_METRIC_BOUND_V1_RESULT.json` shows the already-tested unnormalized Gaussian shared-QK/value correction distorts the independent-value squared-error metric by at most1.107; a new objective should add the actual retained dependencies, not merely repeat that marginal correction. This is not a native-behavior bound.

Maintain hourly strategic/circuit reviews and separate three-hour mathematical reviews. Restore deadlines from the newest review filenames: last recorded 14:28 and 14:29 UTC on 13 September; old next deadlines 15:28 and 17:29. If overdue after migration, review at the first safe boundary and establish fresh deadlines—do not invent work or backfill a report for each offline hour. Mathematical reviews must use primary literature, define actual tensors and assumptions, and produce an executed consequence. Mark phases using `research_phase_clock_v1.py` before implementation, computation, validation and publication. The last ceremony-budget audit failed: keep routine results to primary receipts and short board entries, with narrative only hourly, at major decisions, or when I ask.

Continue useful work autonomously. Before yielding with the goal active, post a concrete board claim and actually perform the next CPU analysis, begin the committed implementation, or leave a verified audited GPU job queued/live. Give concise progress updates. Put fuller updates I specifically request in `explanations/for_logan/`, update its `LATEST.md`, and explain terms, shapes and computations with rendered display LaTeX. Commit/push owned durable work; the old instance had no persistent volume. Do not send Slack/email or spawn subagents without separate authorization.
